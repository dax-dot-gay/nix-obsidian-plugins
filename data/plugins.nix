pkgs: {
    "13th-age-statblocks" = (
    let
        baseUrl = "https://github.com/ben/obsidian-13th-age-statblocks/releases/download/0.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "13th-age-statblocks";
        version = "0.1.8";
        outputHash = "sha256-StJ9F/PGAOXFp4EoTm7+VqN3bflzX5lEp1gPyoZ8zvg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render 13th Age statblocks.'';
            homepage = "https://github.com/ben/obsidian-13th-age-statblocks";
        };
    }
);
    "2hop-links-plus" = (
    let
        baseUrl = "https://github.com/L7Cy/obsidian-2hop-links-plus/releases/download/0.37.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "2hop-links-plus";
        version = "0.37.0";
        outputHash = "sha256-dNFl7L12rZM+0d4PnCR25u50/JcEaejguK1pAe7bsFY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Related links up to 2 hops away are displayed in a card format, allowing for easy browsing through connections between notes. Each card contains a preview of the corresponding note.'';
            homepage = "https://github.com/L7Cy/obsidian-2hop-links-plus";
        };
    }
);
    "3d-graph" = (
    let
        baseUrl = "https://github.com/AlexW00/obsidian-3d-graph/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "3d-graph";
        version = "1.0.5";
        outputHash = "sha256-O1w7goV1QYQFgD3RAXmITUUen4vCPRr769WlhUCFx2Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a 3D graph view.'';
            homepage = "https://github.com/AlexW00/obsidian-3d-graph";
        };
    }
);
    "3d-graph-new" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-3d-graph/releases/download/1.1.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "3d-graph-new";
        version = "1.1.11";
        outputHash = "sha256-TV4uv5A5jrSGVs3oLGOVNP5fRwdxRkT2P/Wa6tdTTe0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A 3D Graph view.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-3d-graph";
        };
    }
);
    "3d-embeds" = (
    let
        baseUrl = "https://github.com/ElmoNeedsArson/Obsidian-3D-embed/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "3d-embeds";
        version = "1.1.6";
        outputHash = "sha256-5sGC6gTHC02r83ClirPGb558MISVijYTBeB5a3MqZjg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed 3DModels in markdown notes.'';
            homepage = "https://github.com/ElmoNeedsArson/Obsidian-3D-embed";
        };
    }
);
    "devo-nlink-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/DEVONlink-obsidian/releases/download/2.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "devo-nlink-obsidian";
        version = "2.2.1";
        outputHash = "sha256-fmd95rK7pHQCW7L/DjE4haIMdmt/X6Y9ncAKLvIKhJs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open or reveal the current note in DEVONthink.'';
            homepage = "https://github.com/ryanjamurphy/DEVONlink-obsidian";
        };
    }
);
    "oa-file-hider" = (
    let
        baseUrl = "https://github.com/Eldritch-Oliver/file-hider/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "oa-file-hider";
        version = "1.1.1";
        outputHash = "sha256-BKvXpovUySxtlmtyQ0EYZgNl1SQfubWVw6drwRUpACk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide files and folders from the file explorer.'';
            homepage = "https://github.com/Eldritch-Oliver/file-hider";
        };
    }
);
    "obsidian-anki-sync" = (
    let
        baseUrl = "https://github.com/debanjandhar12/Obsidian-Anki-Sync/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-anki-sync";
        version = "1.0.0";
        outputHash = "sha256-FdHqo4fsFsta+Nfyv/vl9QViSj68RxantwhxoHo93wE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make flashcards and sync them to Anki.'';
            homepage = "https://github.com/debanjandhar12/Obsidian-Anki-Sync";
        };
    }
);
    "abbreviations" = (
    let
        baseUrl = "https://github.com/WoodenMaiden/obsidian-abbreviations/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "abbreviations";
        version = "1.1.0";
        outputHash = "sha256-YKbrh7SWxJVkufloU565iMl39aoyd5/VPG3q9nu6SRs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily create abbreviations that will be expanded after hitting `Space`.'';
            homepage = "https://github.com/WoodenMaiden/obsidian-abbreviations";
        };
    }
);
    "abbreviations-mark" = (
    let
        baseUrl = "https://github.com/dragonish/obsidian-abbreviations/releases/download/1.7.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "abbreviations-mark";
        version = "1.7.4";
        outputHash = "sha256-zO3gJXKSzurkxegwR5YdVELecR3me3PK6aKjR4SQBb0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Implements automatic marking of abbreviations and acronyms (terminology).'';
            homepage = "https://github.com/dragonish/obsidian-abbreviations";
        };
    }
);
    "abbrlink" = (
    let
        baseUrl = "https://github.com/Hoshino-Yumetsuki/obsidian-plugin-abbrlink/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "abbrlink";
        version = "1.2.1";
        outputHash = "sha256-PioYInUxS8zntNjgPReKr5/6MT2m2o5f5JDD/YsIJqc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically generate permanent short links for your markdown files.'';
            homepage = "https://github.com/Hoshino-Yumetsuki/obsidian-plugin-abbrlink";
        };
    }
);
    "about-blank" = (
    let
        baseUrl = "https://github.com/Ai-Jani/about-blank/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "about-blank";
        version = "1.2.0";
        outputHash = "sha256-UyWi5EcEDK+/lTCMBAOHZkFuhnI1mKFsc/VMtZ7z8Lg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize the empty file (New tab) itself a little bit by adding "Commands" or "Open files". And edit these: Grouping, Set icon, Ask before execution, Register as a new command.'';
            homepage = "https://github.com/Ai-Jani/about-blank";
        };
    }
);
    "account-viewer" = (
    let
        baseUrl = "https://github.com/muaz742/obsidian-accointing-viewer/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "account-viewer";
        version = "1.1.0";
        outputHash = "sha256-mgGpkS6A77kOemk+6kRSxBigoTMZH58omaFifJyfLvw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically generate accounting tables from Markdown code blocks tagged with accounting.'';
            homepage = "https://github.com/muaz742/obsidian-accointing-viewer";
        };
    }
);
    "accounting-journal-ledger" = (
    let
        baseUrl = "https://github.com/JavierRibaldelRio/accounting-journal-ledger/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "accounting-journal-ledger";
        version = "1.0.0";
        outputHash = "sha256-PMw+p6VX+FDOXwaFEN9fICZds1zhdBXnMxZ3lIpK1AI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tool for recording simple journal entries in class, based on the Spanish libro diario and libro mayor (ledger). Uses double-entry bookkeeping.'';
            homepage = "https://github.com/JavierRibaldelRio/accounting-journal-ledger";
        };
    }
);
    "ace-code-editor" = (
    let
        baseUrl = "https://github.com/Raven-Pensieve/obsidian-ace-code-editor/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ace-code-editor";
        version = "2.0.2";
        outputHash = "sha256-rhQC82I3k7jggzD57NhInn+jEwD1HpuAa5FdKQmBWNQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An enhanced code editor using Ace editor, providing syntax highlighting, code folding, and other advanced editing features.'';
            homepage = "https://github.com/Raven-Pensieve/obsidian-ace-code-editor";
        };
    }
);
    "actions-uri" = (
    let
        baseUrl = "https://github.com/czottmann/obsidian-actions-uri/releases/download/1.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "actions-uri";
        version = "1.8.4";
        outputHash = "sha256-u2aZ+AxewhlsEGH+4B+Osc2rpAw++s9ARIlOaKVJQVw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add additional `x-callback-url` endpoints to the app for common actions — it"s a clean, super-charged addition to Obsidian URI.'';
            homepage = "https://github.com/czottmann/obsidian-actions-uri";
        };
    }
);
    "activity-heatmap" = (
    let
        baseUrl = "https://github.com/zakhij/obsidian-activity-heatmap/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "activity-heatmap";
        version = "1.0.5";
        outputHash = "sha256-r0WjY+Vf8qzcf86wIH7aS4ZE7f0IolOcyO4jN0Ym6+s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tracks and visualizes app activity, similar to GitHub"s contribution chart.'';
            homepage = "https://github.com/zakhij/obsidian-activity-heatmap";
        };
    }
);
    "adamantine-pick" = (
    let
        baseUrl = "https://github.com/notlibrary/obsidian-adamantine-pick/releases/download/0.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "adamantine-pick";
        version = "0.5.5";
        outputHash = "sha256-riTA4GXp831AzvcNoryJ7oR5ge12sWFcXDdSjGDu9oY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embeddable Pikchr diagrams renderer.'';
            homepage = "https://github.com/notlibrary/obsidian-adamantine-pick";
        };
    }
);
    "adapt-to-current-view" = (
    let
        baseUrl = "https://github.com/churnish/adapt-to-current-view/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "adapt-to-current-view";
        version = "1.1.4";
        outputHash = "sha256-hx4UNDGt3hI2ktThJRhgJG255gqLTW1od6KWPP531nA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set different accent colors for Reading view, Live Preview and Source.'';
            homepage = "https://github.com/churnish/adapt-to-current-view";
        };
    }
);
    "additional-icons" = (
    let
        baseUrl = "https://github.com/matthewturk/obsidian-additional-icons/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "additional-icons";
        version = "1.0.0";
        outputHash = "sha256-vTQIhPdAIzokfxu7eyiyAHCb5WUVVibHfT+nZpfuPnA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add additional iconsets.'';
            homepage = "https://github.com/matthewturk/obsidian-additional-icons";
        };
    }
);
    "adjacency-matrix-exporter" = (
    let
        baseUrl = "https://github.com/danielegrazzini/adjacency-matrix-exporter/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "adjacency-matrix-exporter";
        version = "1.0.0";
        outputHash = "sha256-Jw1D0uCTOtsVW7bFa+dDqTEqyUkER/zc+k2uHeF+o+0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a numerical adjacency matrix of your vault in two ways: Absolute and Normalized.'';
            homepage = "https://github.com/danielegrazzini/adjacency-matrix-exporter";
        };
    }
);
    "adjacency-matrix-maker" = (
    let
        baseUrl = "https://github.com/SkepticMystic/adjacency-matrix-maker/releases/download/0.6.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "adjacency-matrix-maker";
        version = "0.6.6";
        outputHash = "sha256-LRb98mTch6C0gsw3o/kLFDQAwri759p3kPHhCxQDGdU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create an interactive adjacency matrix of your vault.'';
            homepage = "https://github.com/SkepticMystic/adjacency-matrix-maker";
        };
    }
);
    "advanced-canvas" = (
    let
        baseUrl = "https://github.com/Developer-Mike/obsidian-advanced-canvas/releases/download/6.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-canvas";
        version = "6.0.1";
        outputHash = "sha256-DtizY4+sgxsN2fbuzekCQIDDyihE8+yA5bT5Lo18QMA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Supercharge your canvas experience. Create presentations, flowcharts and more.'';
            homepage = "https://github.com/Developer-Mike/obsidian-advanced-canvas";
        };
    }
);
    "advanced-canvas-filter" = (
    let
        baseUrl = "https://github.com/CHex0K/advanced-canvas-filter/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-canvas-filter";
        version = "0.0.1";
        outputHash = "sha256-4E67aOYkEOwVUop5og/8njKW2RwaL43601QPYwBd1eg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Filter Canvas to show only items with specified tags.'';
            homepage = "https://github.com/CHex0K/advanced-canvas-filter";
        };
    }
);
    "advanced-copy" = (
    let
        baseUrl = "https://github.com/leschuster/obsidian-advanced-copy/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-copy";
        version = "0.10.0";
        outputHash = "sha256-hcenqxH69SS8m/8zkGbaS6qD4p1Qja+WIYFeMajCCUo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy Markdown and transform it into HTML, Anki, or any custom format. Create custom profiles with versatile templates tailored to your workflow.'';
            homepage = "https://github.com/leschuster/obsidian-advanced-copy";
        };
    }
);
    "advanced-cursors" = (
    let
        baseUrl = "https://github.com/SkepticMystic/advanced-cursors/releases/download/0.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-cursors";
        version = "0.5.1";
        outputHash = "sha256-6+2X5Wth+L482L35h4z3aFQ0y1LPTygdw80EvncFNb0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use multiple cursors even more powerfully.'';
            homepage = "https://github.com/SkepticMystic/advanced-cursors";
        };
    }
);
    "advanced-debug-mode" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-advanced-debug-mode/releases/download/1.9.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-debug-mode";
        version = "1.9.5";
        outputHash = "sha256-BQEWNRnlizlx6QnMa9RR/eeoO4GChZpicmQ8F9Ge20g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhances debugging experience.'';
            homepage = "https://github.com/mnaoumov/obsidian-advanced-debug-mode";
        };
    }
);
    "advanced-merger" = (
    let
        baseUrl = "https://github.com/antoKeinanen/obsidian-advanced-merger/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-merger";
        version = "1.7.0";
        outputHash = "sha256-Mi5p3NkNZTaG34LARCw6dfgwPImVcovfyMfV+s7XbIQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Merge a folder of notes for easier export.'';
            homepage = "https://github.com/antoKeinanen/obsidian-advanced-merger";
        };
    }
);
    "advanced-note-composer" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-advanced-note-composer/releases/download/3.26.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-note-composer";
        version = "3.26.3";
        outputHash = "sha256-hzD2hSJpjDkUZeRtXKCWBONitOMBSc5VqHAqsNHud6M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhances Note composer core plugin.'';
            homepage = "https://github.com/mnaoumov/obsidian-advanced-note-composer";
        };
    }
);
    "advanced-progress-bars" = (
    let
        baseUrl = "https://github.com/cactuzhead/Advanced-Progress-Bars/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-progress-bars";
        version = "1.3.1";
        outputHash = "sha256-BmY0ZaxuZBLDcXeoc3/JGZtdrALhjzOzGU7iKa+22UI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Progress bars that can change color depending on completion value - fully customizable.'';
            homepage = "https://github.com/cactuzhead/Advanced-Progress-Bars";
        };
    }
);
    "advanced-random-note" = (
    let
        baseUrl = "https://github.com/karstenpedersen/obsidian-advanced-random-note/releases/download/0.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-random-note";
        version = "0.0.11";
        outputHash = "sha256-tySNyJY/aUuPkKfxwL/kRX9JWG1jtE/vv8aq8w3KnaY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create commands from custom queries to open random notes.'';
            homepage = "https://github.com/karstenpedersen/obsidian-advanced-random-note";
        };
    }
);
    "advanced-ruby" = (
    let
        baseUrl = "https://github.com/peter-yanase/obsidian-advanced-ruby/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "advanced-ruby";
        version = "2.0.0";
        outputHash = "sha256-ozb2Svb13qqh+6zcIQrmmJZuR3Ae1Iwm9zxCRiw00bc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable complex, language-independent ruby annotation rendering.'';
            homepage = "https://github.com/peter-yanase/obsidian-advanced-ruby";
        };
    }
);
    "age-encrypt" = (
    let
        baseUrl = "https://github.com/Mr-1311/obsidian-age-encrypt/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "age-encrypt";
        version = "1.2.0";
        outputHash = "sha256-RNGev36TZXVpQtLCXxNfN8snlcl8pUJjP5oUEbigFXU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Secure content encryption using age encryption library'';
            homepage = "https://github.com/Mr-1311/obsidian-age-encrypt";
        };
    }
);
    "ai-agent" = (
    let
        baseUrl = "https://github.com/TheManuelML/obsidian-agent/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-agent";
        version = "1.1.2";
        outputHash = "sha256-5bKRfIMoRe47SSk9eAdgGwBFNqYSlXkme2aeeNWWMZc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Empower your vault with Google Gemini. This AI agent integrates Google"s artificial intelligence language models to help you perform tasks and conduct web searches.'';
            homepage = "https://github.com/TheManuelML/obsidian-agent";
        };
    }
);
    "ai-assistant" = (
    let
        baseUrl = "https://github.com/qgrail/obsidian-ai-assistant/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-assistant";
        version = "1.6.2";
        outputHash = "sha256-eZxJFJeTUgpM1ySaTUUjrML9/QfHjsFCp8V6TmmGS9M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI Assistant.'';
            homepage = "https://github.com/qgrail/obsidian-ai-assistant";
        };
    }
);
    "ai-blog-generator" = (
    let
        baseUrl = "https://github.com/garethng/obsidian-blog-generator/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-blog-generator";
        version = "1.0.4";
        outputHash = "sha256-3FKYjIRMuqUnv1zCA76goqG5CC3PVWpIZbgQ1MRUg9Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate blog posts from your notes using OpenAI'';
            homepage = "https://github.com/garethng/obsidian-blog-generator";
        };
    }
);
    "ai-bot" = (
    let
        baseUrl = "https://github.com/kuzzh/obsidian-ai-bot/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-bot";
        version = "0.1.1";
        outputHash = "sha256-/joMLot4jxRwq0JycYcF3kwu17Dg8vMCsIMuA9kLU18=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Polish, summarize, translate, analyze, and interpret code using AI'';
            homepage = "https://github.com/kuzzh/obsidian-ai-bot";
        };
    }
);
    "ai-chat-as-md" = (
    let
        baseUrl = "https://github.com/cpbotha/obsidian-ai-chat-as-md/releases/download/1.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-chat-as-md";
        version = "1.8.1";
        outputHash = "sha256-4LKUHe1/5O05Q5WIcqLOCsNMi6kWmFdz1pGusFwpmYM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Multiple branching AI conversations as Markdown hierarchy'';
            homepage = "https://github.com/cpbotha/obsidian-ai-chat-as-md";
        };
    }
);
    "ai-commander" = (
    let
        baseUrl = "https://github.com/yzh503/obsidian-aicommander-plugin/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-commander";
        version = "1.3.4";
        outputHash = "sha256-eyS3s6x390WevQPeBusrMl+HYDn1jHC3Wlt9DMZVSrs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate audio transcripts, images, and text in context of PDF attachments or web search results using OpenAI and Bing API.'';
            homepage = "https://github.com/yzh503/obsidian-aicommander-plugin";
        };
    }
);
    "ai-companion" = (
    let
        baseUrl = "https://github.com/kowshik24/obsidian-ai-assistant/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-companion";
        version = "1.0.9";
        outputHash = "sha256-PJ9Xpi95rEs0qTLBGSFsAHTLVHAiDisJk43rZVlM1Eo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI companion accessible with /ai command in notes. Supports OpenAI integration.'';
            homepage = "https://github.com/kowshik24/obsidian-ai-assistant";
        };
    }
);
    "ai-editor" = (
    let
        baseUrl = "https://github.com/buszk/obsidian-ai-editor/releases/download/0.5.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-editor";
        version = "0.5.6";
        outputHash = "sha256-dZn0qIOdOOYvoOM2jm9oFWgmTgdhb1o5U+iNXDA5M+g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Elevate your workflow with customizable LLM actions.'';
            homepage = "https://github.com/buszk/obsidian-ai-editor";
        };
    }
);
    "ai-enhanced-anki-sync" = (
    let
        baseUrl = "https://github.com/jannusgoe/obsidian-ankisync/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-enhanced-anki-sync";
        version = "1.0.1";
        outputHash = "sha256-aM2FXE0bFsEHF1E8iJQM1gYP9fLJMVPU7ya/XfRTD9A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync ai-enhanced flashcards with Anki via AnkiConnect'';
            homepage = "https://github.com/jannusgoe/obsidian-ankisync";
        };
    }
);
    "ai-helper" = (
    let
        baseUrl = "https://github.com/davidjconnolly/obsidian-ai-helper/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-helper";
        version = "1.0.2";
        outputHash = "sha256-t8A73S9lg2+N+d7MC5h8Ez3/Vv0kf1oGVhL7SXq4NLk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An AI helper tool for summarizing text and providing a chatbot to let you ask questions about your notes.'';
            homepage = "https://github.com/davidjconnolly/obsidian-ai-helper";
        };
    }
);
    "ai-hub" = (
    let
        baseUrl = "https://github.com/hish-math/obsidian-ai-hub/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-hub";
        version = "1.0.2";
        outputHash = "sha256-rLggARQHzV+zkkYL0jJTjcLFBjFGPsXrCQktrPoYaws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate with AI models (currently Google"s Gemini only) to quickly generate and refine notes.'';
            homepage = "https://github.com/hish-math/obsidian-ai-hub";
        };
    }
);
    "ai-image-analyzer" = (
    let
        baseUrl = "https://github.com/Swaggeroo/obsidian-ai-image-analyzer/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-image-analyzer";
        version = "1.2.1";
        outputHash = "sha256-s2eQ1/kS2K2Uc9p2y0N/vAIXTgD6xexKU7Rz40NMlgA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Analyze images with AI to get keywords of the image.'';
            homepage = "https://github.com/Swaggeroo/obsidian-ai-image-analyzer";
        };
    }
);
    "ai-image-ocr" = (
    let
        baseUrl = "https://github.com/rootiest/obsidian-ai-image-ocr/releases/download/0.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-image-ocr";
        version = "0.9.1";
        outputHash = "sha256-iQ6p7pTw7OMLNLNXYNwdGgBKiG52bMZi/N1Jcs9/a5A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extracts text from images using AI Vision models.'';
            homepage = "https://github.com/rootiest/obsidian-ai-image-ocr";
        };
    }
);
    "ai-latex-generator" = (
    let
        baseUrl = "https://github.com/aaaaayushh/ai-latex-generator/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-latex-generator";
        version = "1.0.5";
        outputHash = "sha256-DBGOQ9QPlBqhC7R62v89PFoQX0/5+jD/Igopl/FOLWM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert natural language to LaTeX equations using a local LLM.'';
            homepage = "https://github.com/aaaaayushh/ai-latex-generator";
        };
    }
);
    "ai-mentor" = (
    let
        baseUrl = "https://github.com/clementpoiret/ai-mentor/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-mentor";
        version = "1.3.1";
        outputHash = "sha256-HgapHjDE6mijtqFPrkt4zdkeo5fHVNx6+CgfXqF6bmU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Meet your open source AI mentor. Ask questions, get answers, and learn new things.'';
            homepage = "https://github.com/clementpoiret/ai-mentor";
        };
    }
);
    "ai-note-tagger" = (
    let
        baseUrl = "https://github.com/jaspermayone/obsidian-ai-tagger/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-note-tagger";
        version = "1.2.0";
        outputHash = "sha256-AByvOztV82/vpmq9GSxPTc+r+fdxamrDxthIfKGvqOo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically tag your notes using AI and update frontmatter with generated tags.'';
            homepage = "https://github.com/jaspermayone/obsidian-ai-tagger";
        };
    }
);
    "ai-nutrition-tracker" = (
    let
        baseUrl = "https://github.com/rnaidenov/obsidian-ai-nutrition-tracker-plugin/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-nutrition-tracker";
        version = "2.0.0";
        outputHash = "sha256-sUXahJR4bJJT3Mq96T4aB0M+9peBDIpla7XaxrujMhE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered nutrition tracking with food image analysis and daily logging.'';
            homepage = "https://github.com/rnaidenov/obsidian-ai-nutrition-tracker-plugin";
        };
    }
);
    "ai-providers" = (
    let
        baseUrl = "https://github.com/pfrankov/obsidian-ai-providers/releases/download/1.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-providers";
        version = "1.10.0";
        outputHash = "sha256-9SN9gtCmTLNshfaEYqU13btYm2gPHIddqOIG6UhMi08=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A hub for setting AI providers (OpenAI-like, Ollama and more) in one place.'';
            homepage = "https://github.com/pfrankov/obsidian-ai-providers";
        };
    }
);
    "ai-research-assistant" = (
    let
        baseUrl = "https://github.com/InterwebAlchemy/obsidian-ai-research-assistant/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-research-assistant";
        version = "2.0.3";
        outputHash = "sha256-l96ltVatdaBnLyzZ6TwwpDArGjt+31+6WGOSP7LPGEo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A Prompt Engineering research utility for generative AI models like OpenAI"s ChatGPT that facilitates archiving and searching conversations and live editing a conversation"s context/memory.'';
            homepage = "https://github.com/InterwebAlchemy/obsidian-ai-research-assistant";
        };
    }
);
    "ai-summarize" = (
    let
        baseUrl = "https://github.com/ravenwits/obsidian-ai-summarize/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-summarize";
        version = "2.0.0";
        outputHash = "sha256-/Cd7EFI4SFgXb8AP7APhEqr8SbApNI/w5M+CQwOTsIc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Summarize your notes using AI'';
            homepage = "https://github.com/ravenwits/obsidian-ai-summarize";
        };
    }
);
    "ai-summary" = (
    let
        baseUrl = "https://github.com/irbull/obsidian-ai-summary/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-summary";
        version = "1.0.7";
        outputHash = "sha256-G7wgs6gNNGYTxHEwGDq3NVlHxjG51Os5OV0V/0jqAbw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use OpenAI to generate a summary of referenced notes.'';
            homepage = "https://github.com/irbull/obsidian-ai-summary";
        };
    }
);
    "ai-tagger" = (
    let
        baseUrl = "https://github.com/lucagrippa/obsidian-ai-tagger/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-tagger";
        version = "1.2.6";
        outputHash = "sha256-u6VChDkAunfSXChb17BB2PUnOfz3960B7vcXatDfWvc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simplify tagging. Instantly analyze and tag your document with one click for efficient note organization. OpenAI API key required.'';
            homepage = "https://github.com/lucagrippa/obsidian-ai-tagger";
        };
    }
);
    "ai-tagger-universe" = (
    let
        baseUrl = "https://github.com/niehu2018/obsidian-ai-tagger-universe/releases/download/1.0.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-tagger-universe";
        version = "1.0.17";
        outputHash = "sha256-q1XRlXkPi1byAmqhtyj8a9DypNEMmXmjLAuPImllm88=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically analyze note content and add relevant tags using AI'';
            homepage = "https://github.com/niehu2018/obsidian-ai-tagger-universe";
        };
    }
);
    "ai-templater" = (
    let
        baseUrl = "https://github.com/TfTHacker/obsidian-ai-templater/releases/download/1.0.20";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-templater";
        version = "1.0.20";
        outputHash = "sha256-dqoSYmE1khf4qJ1Nk0V3muebpBszjlZ+Rm2iG1PCZSQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI Extension for the Templater plugin with the OpenAI Client Library.'';
            homepage = "https://github.com/TfTHacker/obsidian-ai-templater";
        };
    }
);
    "ai-tools" = (
    let
        baseUrl = "https://github.com/solderneer/obsidian-ai-tools/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-tools";
        version = "1.1.4";
        outputHash = "sha256-OkaR3fTkEUfw2FJ7nu2bh9WN5ULLGei/Q9gNJn3YHVM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adding powerful semantic search, generative answers, and other AI tools, using Supabase + OpenAI.'';
            homepage = "https://github.com/solderneer/obsidian-ai-tools";
        };
    }
);
    "ai-transcriber" = (
    let
        baseUrl = "https://github.com/mssoftjp/obsidian-ai-transcriber/releases/download/0.9.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-transcriber";
        version = "0.9.7";
        outputHash = "sha256-/LUMucSaYQlFOsW78yixs0Uov9dNZsF5RHsFoR1a/FQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered speech-to-text transcription using OpenAI GPT-4o and Whisper APIs.'';
            homepage = "https://github.com/mssoftjp/obsidian-ai-transcriber";
        };
    }
);
    "ai-writer" = (
    let
        baseUrl = "https://github.com/Donovan-Ye/obsidian-ai-writer-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-writer";
        version = "1.0.3";
        outputHash = "sha256-QfQQtxTjP7YLyXa21ygzKpHlzKtNhSxGQA+SdeU1UA0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use AI to generate high-quality articles with knowledge fragments.'';
            homepage = "https://github.com/Donovan-Ye/obsidian-ai-writer-plugin";
        };
    }
);
    "ai-zhipu" = (
    let
        baseUrl = "https://github.com/TarsLab/obsidian-ai-zhipu/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-zhipu";
        version = "2.0.2";
        outputHash = "sha256-QINryNt8jUuIPzDwCx8fFWf99dBlOlEkPjXqgAcuwxw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate text using the ZhipuAI API.'';
            homepage = "https://github.com/TarsLab/obsidian-ai-zhipu";
        };
    }
);
    "ai-llm" = (
    let
        baseUrl = "https://github.com/Sparky4567/obsidian_ai_plugin/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ai-llm";
        version = "1.1.3";
        outputHash = "sha256-YFTN4qAJRE1yKsnUZHYlHt+oVVCkGwKtNhPdrCwiwu8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate local machine learning (OLLAMA) functionality into your notes, enhancing their capabilities.'';
            homepage = "https://github.com/Sparky4567/obsidian_ai_plugin";
        };
    }
);
    "air-quotes" = (
    let
        baseUrl = "https://github.com/alangrainger/obsidian-air-quotes/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "air-quotes";
        version = "1.0.9";
        outputHash = "sha256-mjDXJgewADkJ8MM8IQLsjC1gUB4vl94uFTT5bicssfk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and insert quotes from a source text as you type.'';
            homepage = "https://github.com/alangrainger/obsidian-air-quotes";
        };
    }
);
    "alfonso-money-manager" = (
    let
        baseUrl = "https://github.com/smartlife-gpt/alfonso-money-manager-obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alfonso-money-manager";
        version = "1.0.0";
        outputHash = "sha256-V2J4eTLiMds0PhdvdwDHBKwEf7+XhfblP0l42YDEK+k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Alfonso Money Manager data viewer and financial analytics tool'';
            homepage = "https://github.com/smartlife-gpt/alfonso-money-manager-obsidian";
        };
    }
);
    "alias-management" = (
    let
        baseUrl = "https://github.com/WithMarcel/alias-management/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alias-management";
        version = "0.0.1";
        outputHash = "sha256-lwwaMvbrYrE/vmyBWT6vW7G3k7RP3LsUWKHku4IPA08=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Identify duplicate notes based on similar aliases and filenames.'';
            homepage = "https://github.com/WithMarcel/alias-management";
        };
    }
);
    "alias-picker" = (
    let
        baseUrl = "https://github.com/rostunic/obsidian-alias-picker/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alias-picker";
        version = "1.0.2";
        outputHash = "sha256-Dg1JTWEy3PShZ/NEsxLYAs4S2vaktznaL/RlbTbjmdA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pick aliases or blocks of links.'';
            homepage = "https://github.com/rostunic/obsidian-alias-picker";
        };
    }
);
    "alignment-tracker" = (
    let
        baseUrl = "https://github.com/FioPio/ObsidianAlignmentTracker/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alignment-tracker";
        version = "1.0.1";
        outputHash = "sha256-oqA/xIgNBNUxYo0Wuq2UqfqAQW/7VpPBSOh5IlsrZDw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track character alignment using a 3x3 grid.'';
            homepage = "https://github.com/FioPio/ObsidianAlignmentTracker";
        };
    }
);
    "aloud-tts" = (
    let
        baseUrl = "https://github.com/adrianlyjak/obsidian-aloud-tts/releases/download/0.14.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "aloud-tts";
        version = "0.14.4";
        outputHash = "sha256-Wavgq/YRrW+E2NT+2VOiXEffVflDzii6DBtuol6ySKM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Speak text from your notes. Converts text to speech in real-time using lifelike voices from OpenAI.'';
            homepage = "https://github.com/adrianlyjak/obsidian-aloud-tts";
        };
    }
);
    "alpha-bullet" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-alpha-bullet/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alpha-bullet";
        version = "0.1.3";
        outputHash = "sha256-2vIphRUR0DBZFyNSgkY4ur4oi76AFOHJCXBJuYq/opY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sorts bulleted lists alphabetically.'';
            homepage = "https://github.com/Mara-Li/obsidian-alpha-bullet";
        };
    }
);
    "alt-click-to-copy" = (
    let
        baseUrl = "https://github.com/veersheth/obsidian-alt-click-to-copy/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alt-click-to-copy";
        version = "1.0.0";
        outputHash = "sha256-ZE/RdJ8F5LUQZP+9cOlUnQ04dJ1vRFXANTvhEFEqHbQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Alt-click on codeblocks to copy its data to the clipboard'';
            homepage = "https://github.com/veersheth/obsidian-alt-click-to-copy";
        };
    }
);
    "always-color-text" = (
    let
        baseUrl = "https://github.com/Kazi-Aidah/always-color-text/releases/download/1.15.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "always-color-text";
        version = "1.15.0";
        outputHash = "sha256-4XBqKbvDC3Ufk2IXzLLIsvcy8z/Wgy6AfLyNZkx2+FE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically colors and highlights texts across the vault.'';
            homepage = "https://github.com/Kazi-Aidah/always-color-text";
        };
    }
);
    "alx-folder-note-folderv" = (
    let
        baseUrl = "https://github.com/aidenlx/alx-folder-note-folderv/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "alx-folder-note-folderv";
        version = "1.0.0";
        outputHash = "sha256-YX1N6c6j9j3r6uTh5m4q9wTgd/JibMRjYyEYtzLKjkY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Optional `folderv` component for alx-folder-note.'';
            homepage = "https://github.com/aidenlx/alx-folder-note-folderv";
        };
    }
);
    "ampliflow-page" = (
    let
        baseUrl = "https://github.com/pbjorklund/obsidian-ampliflow-page/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ampliflow-page";
        version = "1.0.3";
        outputHash = "sha256-B6ShSIAsQ5IcfGOVUeAOAgmBwHKfozc2luGEEn3Xr8M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish notes easily to AmpliFlow (https://www.ampliflow.se)'';
            homepage = "https://github.com/pbjorklund/obsidian-ampliflow-page";
        };
    }
);
    "anchor-display-text" = (
    let
        baseUrl = "https://github.com/rca-umb/anchor-link-display-text/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "anchor-display-text";
        version = "1.4.0";
        outputHash = "sha256-eSSmUPoVWR7x/FOFbNLMnYvC68rp81F5/PTPOHlL0BQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically uses the linked heading as the display text for anchor links.'';
            homepage = "https://github.com/rca-umb/anchor-link-display-text";
        };
    }
);
    "android-nomedia" = (
    let
        baseUrl = "https://github.com/calomancer/android.nomedia/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "android-nomedia";
        version = "0.1.0";
        outputHash = "sha256-pEeNS4dVUYGkFxWaCWOXVtmIxG70Vep63h7rMENf1dU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide media from your vault on Android devices by adding the "nomedia" file to each folder.'';
            homepage = "https://github.com/calomancer/android.nomedia";
        };
    }
);
    "animated-cursor" = (
    let
        baseUrl = "https://github.com/kotaindah55/animated-cursor/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "animated-cursor";
        version = "1.3.4";
        outputHash = "sha256-6e72Ot9WQdADb/0JJ2hi5N8KNI+kJCgZ1yL4G1c0Oa0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple yet smooth animated cursor.'';
            homepage = "https://github.com/kotaindah55/animated-cursor";
        };
    }
);
    "anki-helper" = (
    let
        baseUrl = "https://github.com/panAtGitHub/anki-helper/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "anki-helper";
        version = "0.2.2";
        outputHash = "sha256-We9M/Cefoyr7VMcTE7zQowhgcr5ChXv6SUAsSrx88Xs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Standardizes Anki card display for seamless integration with the expand_to_anki plugin.'';
            homepage = "https://github.com/panAtGitHub/anki-helper";
        };
    }
);
    "anki-integration" = (
    let
        baseUrl = "https://github.com/rifts-obsidian-laboratory/anki-integration/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "anki-integration";
        version = "1.4.3";
        outputHash = "sha256-9R5CzmdnjUbZSRUODeJlcWxS4iavZM5a+jNpF+C4jag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create flashcards from your notes with a seamless interface, structuring them with metadata and syncing effortlessly via AnkiConnect.'';
            homepage = "https://github.com/rifts-obsidian-laboratory/anki-integration";
        };
    }
);
    "anki-sync-plus" = (
    let
        baseUrl = "https://github.com/RochaG07/anki-sync-plus/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "anki-sync-plus";
        version = "1.0.1";
        outputHash = "sha256-xR1r3SePKjlli+auLSrUue589CBtxYWu2HsfrOWOlks=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integration between Obsidian and Anki.'';
            homepage = "https://github.com/RochaG07/anki-sync-plus";
        };
    }
);
    "annotate-audio" = (
    let
        baseUrl = "https://github.com/12-VidE/annotate-audio/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "annotate-audio";
        version = "1.5.2";
        outputHash = "sha256-VTIKwkXvABZrzxct8Gnagpuh4xrijqKIYc6IlOcyEU8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Listen to an audio and add comments to it.'';
            homepage = "https://github.com/12-VidE/annotate-audio";
        };
    }
);
    "another-dynamic-highlights" = (
    let
        baseUrl = "https://github.com/tine-schreibt/aDHL/releases/download/1.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "another-dynamic-highlights";
        version = "1.2.8";
        outputHash = "sha256-J1MbpfIVg7iFAQD9uWgtYKa2zXJGKzwx+j3wWBrzCY0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create pretty static highlighters from search or regEx. Group by tag and set commands for toggling. Based on Dynamic Highlights. Works well with Highlightr.'';
            homepage = "https://github.com/tine-schreibt/aDHL";
        };
    }
);
    "another-name" = (
    let
        baseUrl = "https://github.com/EurFelux/obsidian-another-name-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "another-name";
        version = "1.0.1";
        outputHash = "sha256-/nouzN3ikXSlet8KFrLH3uMM2pJ83BzVQxxSUpECq0k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add an another name after your inline title!'';
            homepage = "https://github.com/EurFelux/obsidian-another-name-plugin";
        };
    }
);
    "another-simple-todoist-sync" = (
    let
        baseUrl = "https://github.com/eudennis/ultimate-todoist-sync-for-obsidian-experiment/releases/download/0.5.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "another-simple-todoist-sync";
        version = "0.5.11";
        outputHash = "sha256-OROiZRX24qujr+pQ/btlpxb2epcS1B7bl4g6MAaf2zI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple plugin to sync with Todoist. Fork from Ultimate Todoist Sync plugin.'';
            homepage = "https://github.com/eudennis/ultimate-todoist-sync-for-obsidian-experiment";
        };
    }
);
    "another-sticky-headings" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-sticky-headings/releases/download/2.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "another-sticky-headings";
        version = "2.1.3";
        outputHash = "sha256-rwAAHihq+79lyvCuSoN4yFUD6Ad4m2RVelXGWRDV7DM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display headings tree during editing and preview to indicate the current position.'';
            homepage = "https://github.com/zhouhua/obsidian-sticky-headings";
        };
    }
);
    "antidote-grammar-checker-integration" = (
    let
        baseUrl = "https://github.com/Heziode/obsidian-antidote/releases/download/2.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "antidote-grammar-checker-integration";
        version = "2.1.2";
        outputHash = "sha256-2vspfNSUYBUR0JFepPtS6q8DCoKSGfhCO4+4c4uBSE4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Unofficial integration of Antidote, a powerful English and French grammar checker.'';
            homepage = "https://github.com/Heziode/obsidian-antidote";
        };
    }
);
    "any-block" = (
    let
        baseUrl = "https://github.com/any-block/any-block/releases/download/3.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "any-block";
        version = "3.4.3";
        outputHash = "sha256-4IqWQuIKtWtNm9YYHzL/eZcKATG0/UN4yp8wACHQQEw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take any fragment as a block and render it into multiple effects. You can select a section by list/heading/table/quote/codeBlock/markdown-it-container(`:::`), and turn into table/tabs/dir/cards/column/mindmap/mermaid/PlantUML/markmap/timeLine/jsonChart/nodeTree and more.'';
            homepage = "https://github.com/any-block/any-block";
        };
    }
);
    "anysocket-sync" = (
    let
        baseUrl = "https://github.com/lynxaegon/obsidian-anysocket-sync/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "anysocket-sync";
        version = "1.5.0";
        outputHash = "sha256-N/mQcj0r68pu7upfcf+71Qdm/PrNqrGpXNKANbatpSY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Self-Hosted synchronization for you Vault using AnySocket'';
            homepage = "https://github.com/lynxaegon/obsidian-anysocket-sync";
        };
    }
);
    "aosr" = (
    let
        baseUrl = "https://github.com/linanwx/aosr/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "aosr";
        version = "1.1.6";
        outputHash = "sha256-gXH06ZCCeSOmMOEIxiGR8guc+N9kONwlS58ojbKq4tc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Another Obsidian spaced repetition. Use flashcards to review and remember knowledge.'';
            homepage = "https://github.com/linanwx/aosr";
        };
    }
);
    "api-designer" = (
    let
        baseUrl = "https://github.com/ruveydayilmaz/obsidian-api-designer-plugin/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "api-designer";
        version = "1.0.7";
        outputHash = "sha256-pKU2ei2m9d9ozofjk4PLec417AEIOpVuF82E+copY5A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Design and document API endpoints visually without leaving your notes.'';
            homepage = "https://github.com/ruveydayilmaz/obsidian-api-designer-plugin";
        };
    }
);
    "api-request" = (
    let
        baseUrl = "https://github.com/Rooyca/obsidian-api-request/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "api-request";
        version = "2.1.0";
        outputHash = "sha256-Cq1zPJkv+nxvoV+1CGi/VP6D9tFJ1VUuomWvM9FcP6c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Request and retrieve data from APIs. The responses are delivered in a JSON format for easy integration with your notes.'';
            homepage = "https://github.com/Rooyca/obsidian-api-request";
        };
    }
);
    "apl-render" = (
    let
        baseUrl = "https://github.com/vzsky/apl-obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "apl-render";
        version = "1.0.0";
        outputHash = "sha256-M3exFh4QGXCSCwFzmBSEDS0ge6GZrVk6MllMzCD2cCA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render APL syntax.'';
            homepage = "https://github.com/vzsky/apl-obsidian";
        };
    }
);
    "apple-books-highlights" = (
    let
        baseUrl = "https://github.com/atfzl/obsidian-apple-books-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "apple-books-highlights";
        version = "1.0.1";
        outputHash = "sha256-VkLceIct1D1NrZhtws6ceLgD0tvOrzt1hqAKtdTYERM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Apple Books highlights automatically.'';
            homepage = "https://github.com/atfzl/obsidian-apple-books-plugin";
        };
    }
);
    "apple-books-import-highlights" = (
    let
        baseUrl = "https://github.com/bandantonio/obsidian-apple-books-highlights-plugin/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "apple-books-import-highlights";
        version = "1.8.0";
        outputHash = "sha256-uhdGZAIp/fY23ZEJ4vsZsbIAo9ee6JlFIAgQWUJzJOQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import your Apple Books highlights and notes to Obsidian.'';
            homepage = "https://github.com/bandantonio/obsidian-apple-books-highlights-plugin";
        };
    }
);
    "aprils-automatic-timelines" = (
    let
        baseUrl = "https://github.com/April-Gras/obsidian-auto-timelines/releases/download/0.13.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "aprils-automatic-timelines";
        version = "0.13.10";
        outputHash = "sha256-L06UWrzk/n/tmhXDz9/3FPauFD4hGdAEi0Mgf8X3j7s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple timeline generator for story tellers.'';
            homepage = "https://github.com/April-Gras/obsidian-auto-timelines";
        };
    }
);
    "aqu-blur-mode" = (
    let
        baseUrl = "https://github.com/dangehub/obsidian-blur-mode/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "aqu-blur-mode";
        version = "1.0.6";
        outputHash = "sha256-/p6HYvzdtj8eyb1tO7C0+EzzNLxeNRlvREbyLXwQ7M0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Blur anything you want to keep your privacy.'';
            homepage = "https://github.com/dangehub/obsidian-blur-mode";
        };
    }
);
    "arcana" = (
    let
        baseUrl = "https://github.com/A-F-V/obsidian-arcana/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arcana";
        version = "1.7.0";
        outputHash = "sha256-J6tATC8sQG9NlEUgXQkKolDBuabLfe5IS8PUIVMZROE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Supercharge your note-taking through AI-powered insights and suggestions.'';
            homepage = "https://github.com/A-F-V/obsidian-arcana";
        };
    }
);
    "arcane-obfuscate" = (
    let
        baseUrl = "https://github.com/travismlee/arcane-obfuscate/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arcane-obfuscate";
        version = "1.0.3";
        outputHash = "sha256-dptTm73zx3xR3csanq1UGGtxyWZqR/u0cUiVmpRg9tk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Obfuscate text with an arcane runic effect.'';
            homepage = "https://github.com/travismlee/arcane-obfuscate";
        };
    }
);
    "archive-to-single-note" = (
    let
        baseUrl = "https://github.com/mwoz123/archive-to-single-note/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "archive-to-single-note";
        version = "2.0.1";
        outputHash = "sha256-h6y8tEHw/baiM+RZ5wFSDdSzt34iuNHJvP0l3fYyCd0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create single file archive/trash and merge(archive) old notes with it.'';
            homepage = "https://github.com/mwoz123/archive-to-single-note";
        };
    }
);
    "archivist-importer" = (
    let
        baseUrl = "https://github.com/archivistai/archivist-obsidian-importer/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "archivist-importer";
        version = "0.1.6";
        outputHash = "sha256-+H7O6BF/zopV8NkXf33eyvATdiUhowQX9HlMKmClpNk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import selected vault files into Archivist campaigns.'';
            homepage = "https://github.com/archivistai/archivist-obsidian-importer";
        };
    }
);
    "archwiki-reader" = (
    let
        baseUrl = "https://github.com/lucifayr/archwiki-obsidian/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "archwiki-reader";
        version = "2.0.0";
        outputHash = "sha256-/Epu2FcSp7/PAN8ZQTNbq0cFzDwZH0UMGxGt7MFkxYo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Read any ArchWiki page.'';
            homepage = "https://github.com/lucifayr/archwiki-obsidian";
        };
    }
);
    "arena" = (
    let
        baseUrl = "https://github.com/0xroko/obsidian-arena-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arena";
        version = "1.0.3";
        outputHash = "sha256-3UUKVucPYCM7lLVRl+pHU3fnc7OuuC/6DUon4hUpjhA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save Are.na blocks as notes.'';
            homepage = "https://github.com/0xroko/obsidian-arena-plugin";
        };
    }
);
    "arena-manager" = (
    let
        baseUrl = "https://github.com/javierarce/arena-manager/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arena-manager";
        version = "1.0.5";
        outputHash = "sha256-kF6SCOJIZXKN7Ch8PjdsGnllhC+l2r1MjUxhPn5SMcc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish content from your vault to Arena and the other way around.'';
            homepage = "https://github.com/javierarce/arena-manager";
        };
    }
);
    "arenasys-ai-chat" = (
    let
        baseUrl = "https://github.com/arenasys/obsidian-ai-chat/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arenasys-ai-chat";
        version = "1.0.4";
        outputHash = "sha256-tCSwtRX8Tv+DbalSBjHoc6MD9Hp1POpfnR7JL7gOsYo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Chat with AI about your notes.'';
            homepage = "https://github.com/arenasys/obsidian-ai-chat";
        };
    }
);
    "arrows-in-md" = (
    let
        baseUrl = "https://github.com/artisticat1/arrows/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arrows-in-md";
        version = "1.0.0";
        outputHash = "sha256-k0rzzrKLnWk0cuXI+wEz/rqw//lAMf/uxbpX4xZW9qk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Draw arrows across different parts of your notes, similar to on paper.'';
            homepage = "https://github.com/artisticat1/arrows";
        };
    }
);
    "arweave-uploader" = (
    let
        baseUrl = "https://github.com/konfuzz/arweave-uploader-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "arweave-uploader";
        version = "1.0.1";
        outputHash = "sha256-1zqyHZhD6NbB1/yjmPyvbdRH2LKIQhoQv5P2LcBCJ94=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert your notes to HTML and upload to Arweave.'';
            homepage = "https://github.com/konfuzz/arweave-uploader-plugin";
        };
    }
);
    "asana" = (
    let
        baseUrl = "https://github.com/mryanb/obsidian-asana/releases/download/0.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "asana";
        version = "0.1.8";
        outputHash = "sha256-yNJhvQn5NC92yZsKeqopp/CarA8c7HzPhlB4DD8ES6s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Asana tasks from highlighted text or the current line.'';
            homepage = "https://github.com/mryanb/obsidian-asana";
        };
    }
);
    "ascii-tree-generator" = (
    let
        baseUrl = "https://github.com/michalekmatej/obsidian.md-ascii-tree-generator/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ascii-tree-generator";
        version = "1.2.0";
        outputHash = "sha256-4rW/E7X9WNHyfHQEzOAKd90sgxiHbp3dQLZjxoHNRdw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert indented code blocks with hierarchy markers into formatted ASCII tree diagrams.'';
            homepage = "https://github.com/michalekmatej/obsidian.md-ascii-tree-generator";
        };
    }
);
    "asciidoc-reader" = (
    let
        baseUrl = "https://github.com/Voidgrown/obsidian-asciidoc/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "asciidoc-reader";
        version = "0.2.0";
        outputHash = "sha256-Mz/S4kAZjVRwwXIYYluS4AmtEP6/3cQ68Xj3f0nhphw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables the rendering of AsciiDoc.'';
            homepage = "https://github.com/Voidgrown/obsidian-asciidoc";
        };
    }
);
    "asciidoctor-editor" = (
    let
        baseUrl = "https://github.com/dzruyk/obsidian-asciidoc/releases/download/0.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "asciidoctor-editor";
        version = "0.1.10";
        outputHash = "sha256-HIgeglA8d1XDxIwQH6aurriFyqC6QoaFbFTulYrh/jM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View and modify asciidoc pages'';
            homepage = "https://github.com/dzruyk/obsidian-asciidoc";
        };
    }
);
    "asciinema-player" = (
    let
        baseUrl = "https://github.com/deeplook/obsidian-asciinema-player/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "asciinema-player";
        version = "1.0.5";
        outputHash = "sha256-soucP/3xyfSBYHqEW+PQwVdpIXM22tmkR1nRQ0UA9Ro=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed local and remote Asciinema asciicast files in Markdown notes.'';
            homepage = "https://github.com/deeplook/obsidian-asciinema-player";
        };
    }
);
    "askify-obsidian-sync" = (
    let
        baseUrl = "https://github.com/helloworldkr/Askify-Obsidian-Sync/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "askify-obsidian-sync";
        version = "1.0.3";
        outputHash = "sha256-wAfE58CntD3HDoEB3Cwyjuwzojp6QcJoKPL7zGYyPow=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncing between Obsidian and Askify.'';
            homepage = "https://github.com/helloworldkr/Askify-Obsidian-Sync";
        };
    }
);
    "at-people" = (
    let
        baseUrl = "https://github.com/backmind/obsidian-at-people/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "at-people";
        version = "1.5.1";
        outputHash = "sha256-toAdT8QxLqjoPvFIJt7LVy0vuEVl/Q2u35YDNC6rJzA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use the @ to create links to people files with smart fuzzy search, accent-insensitive matching, and backlink-based ranking.'';
            homepage = "https://github.com/backmind/obsidian-at-people";
        };
    }
);
    "at-symbol-linking" = (
    let
        baseUrl = "https://github.com/Ebonsignori/obsidian-at-symbol-linking/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "at-symbol-linking";
        version = "2.1.0";
        outputHash = "sha256-oyxWAxNRxw8soI1Sz3h0/ZoevVzTCpnfkMhw1RsbG48=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link to notes with any symbol (e.g. `@`). Can map symbols to link to a specific directory or to create files in directories using templates.'';
            homepage = "https://github.com/Ebonsignori/obsidian-at-symbol-linking";
        };
    }
);
    "attachment-management" = (
    let
        baseUrl = "https://github.com/trganda/obsidian-attachment-management/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "attachment-management";
        version = "0.10.0";
        outputHash = "sha256-BTUT8V5Wk6sZq5QvIfSr75w84W3nes6H8lG3n7Ri95c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize attachment path, auto-rename attachments, etc.'';
            homepage = "https://github.com/trganda/obsidian-attachment-management";
        };
    }
);
    "attachment-manager" = (
    let
        baseUrl = "https://github.com/chenfeicqq/obsidian-attachment-manager/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "attachment-manager";
        version = "1.2.2";
        outputHash = "sha256-U44aEi+9aSo2UVwrACEAorBQzI45Wqu9shpPBiwD6PQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Attachment Manager: Attachment folder name binding note name, automatically rename, automatically delete, show/hide.
附件管理器：附件文件夹名称绑定笔记名、自动重命名、自动删除、显示/隐藏。'';
            homepage = "https://github.com/chenfeicqq/obsidian-attachment-manager";
        };
    }
);
    "attachment-uploader" = (
    let
        baseUrl = "https://github.com/zhuxining/obsidian-attachment-uploader/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "attachment-uploader";
        version = "1.1.0";
        outputHash = "sha256-QngaAQkXRd/W0DxDPhJaBrIoGpMTyEELENpm8zFG1wU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload attachments to PicSee or uPic, with customization for commands and file type.'';
            homepage = "https://github.com/zhuxining/obsidian-attachment-uploader";
        };
    }
);
    "attachments-cache" = (
    let
        baseUrl = "https://github.com/luisbs/obsidian-attachments-cache/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "attachments-cache";
        version = "0.7.0";
        outputHash = "sha256-mWlf1JB3QRgpWvDY2thF6Opi5HgKk+rNyoULRvsz08Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Store images and other attachments on the vault'';
            homepage = "https://github.com/luisbs/obsidian-attachments-cache";
        };
    }
);
    "attachments-md-indexer" = (
    let
        baseUrl = "https://github.com/iinkov/obsidian-attachments-md-indexer/releases/download/2.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "attachments-md-indexer";
        version = "2.1.4";
        outputHash = "sha256-ClDexCXQADUhEOTKJOQRogNAi3d88419nT1DN62KeQw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates searchable metadata notes for Canvas files, PDFs, and images, enhancing graph view visibility and plugin compatibility.'';
            homepage = "https://github.com/iinkov/obsidian-attachments-md-indexer";
        };
    }
);
    "audiopen-sync" = (
    let
        baseUrl = "https://github.com/jonashaefele/audiopen-obsidian/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "audiopen-sync";
        version = "0.2.0";
        outputHash = "sha256-LlfI2FVLGYo9XHPfcrVZc7PUeTdyHRU3XETgJD64O6Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes from AudioPen.'';
            homepage = "https://github.com/jonashaefele/audiopen-obsidian";
        };
    }
);
    "augmented-canvas" = (
    let
        baseUrl = "https://github.com/MetaCorp/obsidian-augmented-canvas/releases/download/0.1.20";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "augmented-canvas";
        version = "0.1.20";
        outputHash = "sha256-edJiYv8bv+lHJW0+pL18w+e911gnOj8V+tKUpS1nK6A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Augment Canvas with AI features.'';
            homepage = "https://github.com/MetaCorp/obsidian-augmented-canvas";
        };
    }
);
    "auto-anki" = (
    let
        baseUrl = "https://github.com/cadrianxyz/obsidian-auto-anki/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-anki";
        version = "0.2.1";
        outputHash = "sha256-lLAPVnSXLPeefZc0ae2UaVseIpv8bFFH1gB0hQk2C08=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Using AI to automate card creation for Spaced Repetion in Anki.'';
            homepage = "https://github.com/cadrianxyz/obsidian-auto-anki";
        };
    }
);
    "auto-archive" = (
    let
        baseUrl = "https://github.com/shanedonburke/obsidian-auto-archive/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-archive";
        version = "1.0.7";
        outputHash = "sha256-onh2EAIfMqRMt5ZTEkAWvgCVPcmOHllPelwdXu7qyvM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically archive notes once they reach a certain age.'';
            homepage = "https://github.com/shanedonburke/obsidian-auto-archive";
        };
    }
);
    "auto-bullet" = (
    let
        baseUrl = "https://github.com/takitsuba/obsidian-auto-bullet/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-bullet";
        version = "1.1.0";
        outputHash = "sha256-mJ2ElW832wAQ9NQMUx/z5ibmcYcotUTg2pF4mSyIeA8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically inserts bullet points when you type spaces or tabs at the beginning of a line.'';
            homepage = "https://github.com/takitsuba/obsidian-auto-bullet";
        };
    }
);
    "auto-card-link" = (
    let
        baseUrl = "https://github.com/nekoshita/obsidian-auto-card-link/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-card-link";
        version = "1.2.3";
        outputHash = "sha256-x/hDLzIU3DN7aHMh/GEha7vdi6fUobVYATaWYn2Z0mU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically fetches metadata from a URL and makes it as a card-styled link.'';
            homepage = "https://github.com/nekoshita/obsidian-auto-card-link";
        };
    }
);
    "auto-class" = (
    let
        baseUrl = "https://github.com/nathonius/obsidian-auto-class/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-class";
        version = "2.3.0";
        outputHash = "sha256-Kc6coJJxt+0UO1NEftrpR65pr7roET/sxErmUuyHKhc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically apply CSS classes to Markdown views based on a note"s path.'';
            homepage = "https://github.com/nathonius/obsidian-auto-class";
        };
    }
);
    "auto-classifier" = (
    let
        baseUrl = "https://github.com/HyeonseoNam/auto-classifier/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-classifier";
        version = "1.3.0";
        outputHash = "sha256-0g2p/nRVzx4h/Qf37Iapv+xOOPSOKr/3zgRlrPFRDpM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically classify tag from your notes using ChatGPT API. It analyze your note (It can be title, frontmatter, content or selected area) and automatically insert tag where you set.'';
            homepage = "https://github.com/HyeonseoNam/auto-classifier";
        };
    }
);
    "auto-close-tags" = (
    let
        baseUrl = "https://github.com/k0src/Obsidian-Auto-Close-Tags-Plugin/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-close-tags";
        version = "1.1.2";
        outputHash = "sha256-N8rH1Rj6PGkriVqr4/yJdL4WBuNls8gbW5jv8W+R9sI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto close HTML tags.'';
            homepage = "https://github.com/k0src/Obsidian-Auto-Close-Tags-Plugin";
        };
    }
);
    "auto-correct-capitals" = (
    let
        baseUrl = "https://github.com/Ummler/obsidian-auto-correct-capitals/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-correct-capitals";
        version = "1.1.2";
        outputHash = "sha256-nK2Py16yajIFpTnoIDrySry8eUTD3pq7OYpI5hENLfQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Correct words with the first two letters in uppercase.'';
            homepage = "https://github.com/Ummler/obsidian-auto-correct-capitals";
        };
    }
);
    "auto-daily-note" = (
    let
        baseUrl = "https://github.com/litegral/auto-daily-note/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-daily-note";
        version = "1.0.0";
        outputHash = "sha256-U2r9tUH8/DqpZqsI4yUb1mnmR75v9HRCoGVDka9jrGA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically creates today"s daily note. Daily note plugin must be enabled'';
            homepage = "https://github.com/litegral/auto-daily-note";
        };
    }
);
    "auto-definition-link" = (
    let
        baseUrl = "https://github.com/nmcarp99/Obsidian-Auto-Definition-Link/releases/download/8.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-definition-link";
        version = "8.6.0";
        outputHash = "sha256-TZ6KG7+3QtjmsJh2HpQ5OTLMls0EkntcTJ6oSU7RUPg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically converts text to definition links within the current folder when you type them.'';
            homepage = "https://github.com/nmcarp99/Obsidian-Auto-Definition-Link";
        };
    }
);
    "auto-displaystyle-inline-math" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-auto-displaystyle-inline-math/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-displaystyle-inline-math";
        version = "0.2.2";
        outputHash = "sha256-QabaDGvrfasfBLR2cI6l1TtH7KHV0orMwen0KaOAMAY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically make all inline math \displaystyle.'';
            homepage = "https://github.com/RyotaUshio/obsidian-auto-displaystyle-inline-math";
        };
    }
);
    "auto-embed" = (
    let
        baseUrl = "https://github.com/GnoxNahte/obsidian-auto-embed/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-embed";
        version = "1.2.6";
        outputHash = "sha256-zPHxy26CiYFG1Dut4K13bxu02qFMOLFpqBnr6Zfft/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps to embed links using markdown instead of iframe.'';
            homepage = "https://github.com/GnoxNahte/obsidian-auto-embed";
        };
    }
);
    "auto-file-organizer" = (
    let
        baseUrl = "https://github.com/mofukuru/auto_file_organizer/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-file-organizer";
        version = "1.1.1";
        outputHash = "sha256-jnphwbgJSq6FyIGM10p1iFo0JnapMWjUwpW8Ls0JuU8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically organizes files into folders based on their extensions.'';
            homepage = "https://github.com/mofukuru/auto_file_organizer";
        };
    }
);
    "auto-filename" = (
    let
        baseUrl = "https://github.com/rcsaquino/obsidian-auto-filename/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-filename";
        version = "1.2.1";
        outputHash = "sha256-5plKgJOsaNFOluIl77Fi1Z/ZUXyeqxKvxIhKOFfW/zQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically rename files on the go based on the first x characters of files.'';
            homepage = "https://github.com/rcsaquino/obsidian-auto-filename";
        };
    }
);
    "auto-folder-collapse" = (
    let
        baseUrl = "https://github.com/DarioCasciato/obsidian-auto-folder-collapse/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-folder-collapse";
        version = "1.2.4";
        outputHash = "sha256-+hTyWjoElCQVm0UvUaVPr9VG+ivND3BjyQTChEl0Tlg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically collapses subfolders when a parent folder is collapsed'';
            homepage = "https://github.com/DarioCasciato/obsidian-auto-folder-collapse";
        };
    }
);
    "auto-folder-note-paste" = (
    let
        baseUrl = "https://github.com/d7sd6u/obsidian-auto-folder-note-paste/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-folder-note-paste";
        version = "1.0.1";
        outputHash = "sha256-UUtnAYyKMPMSuHl5+pXhB8lU4qA7k11R9y4l/1pWveU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automagically convert the note to a folder note when pasting or drag"n"dropping an attachment.'';
            homepage = "https://github.com/d7sd6u/obsidian-auto-folder-note-paste";
        };
    }
);
    "auto-front-matter" = (
    let
        baseUrl = "https://github.com/conorzhong/obsidian-auto-front-matter/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-front-matter";
        version = "2.0.2";
        outputHash = "sha256-XrpGA2rwiZkziUBqnomtaOD8RVjeHAEWozSUr/4bVFA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto update front matter.'';
            homepage = "https://github.com/conorzhong/obsidian-auto-front-matter";
        };
    }
);
    "auto-glossary" = (
    let
        baseUrl = "https://github.com/ennioitaliano/obsidian-auto-glossary/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-glossary";
        version = "0.9.0";
        outputHash = "sha256-OzsEiBnrnnq/W5hOByzeDwD6QOqjaKk1vG1KQOsofW8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically create a glossary of files, an index of files, or both.'';
            homepage = "https://github.com/ennioitaliano/obsidian-auto-glossary";
        };
    }
);
    "auto-hide-cursor" = (
    let
        baseUrl = "https://github.com/jmxo/obsidian-auto-hide-cursor/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-hide-cursor";
        version = "1.1.0";
        outputHash = "sha256-vVZxcite9jnsH3iHPYmfaQ0uxt7HHlC4Y/4zeCw9PGc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide the cursor when scrolling and show it again when moving the mouse.'';
            homepage = "https://github.com/jmxo/obsidian-auto-hide-cursor";
        };
    }
);
    "auto-hyperlink" = (
    let
        baseUrl = "https://github.com/take6/obsidian-plugin-auto-hyperlink/releases/download/0.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-hyperlink";
        version = "0.4.2";
        outputHash = "sha256-h8f7Mp0gZvca2oPn/Z4r1HJRIvrscvvr+xnyEdXPfX4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert hyperlink according to user-defined rule.'';
            homepage = "https://github.com/take6/obsidian-plugin-auto-hyperlink";
        };
    }
);
    "auto-journal" = (
    let
        baseUrl = "https://github.com/Ebonsignori/obsidian-auto-journal/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-journal";
        version = "1.4.1";
        outputHash = "sha256-WyYHi76xjFu66E6Ss7+Tg8TZRJgxT5WZpSd1ZiZUQhg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Opinionated journaling automation like daily notes but with backfills for the days when Obsidian wasn"t opened.'';
            homepage = "https://github.com/Ebonsignori/obsidian-auto-journal";
        };
    }
);
    "auto-keyword-linker" = (
    let
        baseUrl = "https://github.com/danrhodes/AutoKeywordLinker/releases/download/3.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-keyword-linker";
        version = "3.3.0";
        outputHash = "sha256-HF2FUzClUu62DPWtUbtV1+4OO+IRI6n/e8VPJPUSUzY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically creates backlinks for specified keywords with variations and preview mode.'';
            homepage = "https://github.com/danrhodes/AutoKeywordLinker";
        };
    }
);
    "auto-literature" = (
    let
        baseUrl = "https://github.com/hucorz/obsidian-autoLiterature/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-literature";
        version = "1.2.2";
        outputHash = "sha256-0yqQobj7xbuo5TsNSIPAps1ZDdEDorulF9hWWm9mwSY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Assist you in taking notes for your literature.'';
            homepage = "https://github.com/hucorz/obsidian-autoLiterature";
        };
    }
);
    "auto-math" = (
    let
        baseUrl = "https://github.com/loglux/auto-math-for-obsidian/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-math";
        version = "0.2.6";
        outputHash = "sha256-cW1J6vuviHIWTP+RjtDO5SKu3sxFouO7Up5zSjGdlH8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto-expands LaTeX snippets. External rules with live reload, Custom Rules Editor, and default math pack.'';
            homepage = "https://github.com/loglux/auto-math-for-obsidian";
        };
    }
);
    "auto-moc" = (
    let
        baseUrl = "https://github.com/dalcantara7/obsidian-auto-moc/releases/download/1.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-moc";
        version = "1.5.3";
        outputHash = "sha256-6aj1D1lO/AP7f52n+2bI47Vylnx4lWwyXgPOcZM4PRI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Look for missing linked mentions to the current note and import them into the current note.'';
            homepage = "https://github.com/dalcantara7/obsidian-auto-moc";
        };
    }
);
    "auto-mover" = (
    let
        baseUrl = "https://github.com/al0cam/AutoMover/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-mover";
        version = "1.0.8";
        outputHash = "sha256-OrU0/kew5GUzxncwOQyLWpo1teJ0+wvOy8yI+MPFKhM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically move files and notes with specified names into their designated folders according to rules you define.'';
            homepage = "https://github.com/al0cam/AutoMover";
        };
    }
);
    "auto-note-importer" = (
    let
        baseUrl = "https://github.com/uppinote20/obsidian-auto-note-importer/releases/download/0.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-note-importer";
        version = "0.8.1";
        outputHash = "sha256-45v9PRSUAdT5Gc4fQAejxNCAmEBrG4JEfTv1YSbpreY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically import notes from an external database like Airtable into your Vault.'';
            homepage = "https://github.com/uppinote20/obsidian-auto-note-importer";
        };
    }
);
    "auto-note-mover" = (
    let
        baseUrl = "https://github.com/farux/obsidian-auto-note-mover/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-note-mover";
        version = "1.2.0";
        outputHash = "sha256-kFHx63aud4PVf9J6f2z0Ox87L9fGlDEqyRT4SHjMKGw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically move the active notes to their respective folders according to rules you set.'';
            homepage = "https://github.com/farux/obsidian-auto-note-mover";
        };
    }
);
    "auto-pause" = (
    let
        baseUrl = "https://github.com/ckep1/obsidian-autopause/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-pause";
        version = "1.1.1";
        outputHash = "sha256-fveLxEFVh7uehXvko8ypl4fZ6SdMKnEwuA+RkEz7Hes=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows one audio track to be played at a time, pausing or stopping any others.'';
            homepage = "https://github.com/ckep1/obsidian-autopause";
        };
    }
);
    "auto-periodic-notes" = (
    let
        baseUrl = "https://github.com/jamiefdhurst/obsidian-auto-periodic-notes/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-periodic-notes";
        version = "1.1.2";
        outputHash = "sha256-1zsaNHfkzeBxBDbhM7NncOjWdNa3ryyOw/aJ5KzCcR0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates new periodic notes automatically in the background and allows these to be pinned in your open tabs, requires the Periodic Notes plugin.'';
            homepage = "https://github.com/jamiefdhurst/obsidian-auto-periodic-notes";
        };
    }
);
    "auto-reading-mode" = (
    let
        baseUrl = "https://github.com/kelvinc6/auto-reading-mode/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-reading-mode";
        version = "1.0.4";
        outputHash = "sha256-EUGNPsRcggKH8Nx9XU0DXdVpL/MjE9Wm7MLotoUc7Ao=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically switches previously opened Markdown pages into reading mode.'';
            homepage = "https://github.com/kelvinc6/auto-reading-mode";
        };
    }
);
    "auto-replacer" = (
    let
        baseUrl = "https://github.com/Alecell/auto-replacer/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-replacer";
        version = "1.1.0";
        outputHash = "sha256-tUCQBgvgNalRwnoE+dmk+6Xd0XOOf44zV3laXYTeOd8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replace text in your notes automatically using regex rules and JavaScript functions.'';
            homepage = "https://github.com/Alecell/auto-replacer";
        };
    }
);
    "auto-strikethrough-task" = (
    let
        baseUrl = "https://github.com/Nomekuma/auto-strikethrough-task-obsidian-pluggin/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-strikethrough-task";
        version = "0.0.3";
        outputHash = "sha256-r0bVnli2Kh5rKeG1q7SGPQm2VTliP981sv30vxre/hs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically adds strikethrough to completed tasks.'';
            homepage = "https://github.com/Nomekuma/auto-strikethrough-task-obsidian-pluggin";
        };
    }
);
    "auto-tag" = (
    let
        baseUrl = "https://github.com/CtrlAltFocus/obsidian-plugin-auto-tag/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-tag";
        version = "0.3.0";
        outputHash = "sha256-AtVcEp5SwsEMTxtqE2hsm1AeF623xAO1+HKykpwMLNk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily generate relevant tags for your notes or selected text.'';
            homepage = "https://github.com/CtrlAltFocus/obsidian-plugin-auto-tag";
        };
    }
);
    "auto-tasks" = (
    let
        baseUrl = "https://github.com/jamiefdhurst/obsidian-auto-tasks/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-tasks";
        version = "0.8.0";
        outputHash = "sha256-rFcbUg5B3GE7ERy1t89XCUHCjHeZobLulIcPlDkM2bA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Combine periodic notes with tags and tasks to automatically manage your daily, weekly and project TODO lists. Requires the Periodic Notes and Tasks plugins.'';
            homepage = "https://github.com/jamiefdhurst/obsidian-auto-tasks";
        };
    }
);
    "auto-template-trigger" = (
    let
        baseUrl = "https://github.com/numeroflip/obsidian-auto-template-trigger/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "auto-template-trigger";
        version = "1.3.2";
        outputHash = "sha256-D392YzgNII1DAZp9MahxiaaGpJLfxYqoHSZ7V1PSPWc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically prompt for a template, when creating a note.'';
            homepage = "https://github.com/numeroflip/obsidian-auto-template-trigger";
        };
    }
);
    "autocorrect-formatter" = (
    let
        baseUrl = "https://github.com/b-yp/obsidian-autocorrect/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "autocorrect-formatter";
        version = "1.0.6";
        outputHash = "sha256-SfbPYcERW/xz/XTxrvhkkpnbK7TXwSThLOrgAu1eTdo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format Markdown content using Autocorrect.'';
            homepage = "https://github.com/b-yp/obsidian-autocorrect";
        };
    }
);
    "autofit-tabs" = (
    let
        baseUrl = "https://github.com/bwya77/autofit-tabs/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "autofit-tabs";
        version = "1.0.8";
        outputHash = "sha256-E/R2p+Grn56jbIpQNeceD8jC0Hlz88uCPQjq2E/89P8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically adjusts tab header widths in real-time to perfectly fit each tab"s title content while maintaining a clean, seamless interface that prevents awkward text truncation and ensures optimal readability of your document titles.'';
            homepage = "https://github.com/bwya77/autofit-tabs";
        };
    }
);
    "autogen" = (
    let
        baseUrl = "https://github.com/AidanTilgner/AutogenObsidianPlugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "autogen";
        version = "1.1.0";
        outputHash = "sha256-3RdWi4kQEoLSVeYqhR4tmTn/Swlut69ffbZEa7OHZvs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''In place autogeneration of content based on prompts within notes'';
            homepage = "https://github.com/AidanTilgner/AutogenObsidianPlugin";
        };
    }
);
    "automatic-linker" = (
    let
        baseUrl = "https://github.com/kdnk/obsidian-automatic-linker/releases/download/4.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "automatic-linker";
        version = "4.2.0";
        outputHash = "sha256-1EmUQ7GsceVaFnJ4qfvOpueuS6Q0AVs8yvTxLW/Pc8E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''automatically converts plain text file references into wiki links'';
            homepage = "https://github.com/kdnk/obsidian-automatic-linker";
        };
    }
);
    "automatic-list-styles" = (
    let
        baseUrl = "https://github.com/WiseGuru/obsidian-automatic-list-styles/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "automatic-list-styles";
        version = "0.1.1";
        outputHash = "sha256-5+WZ/7E4VDLrXOq2eREiKXtxEhQdAhJLyQZTdY/kC8I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically formats the styles of ordered lists, incrementing the list style for each layer.'';
            homepage = "https://github.com/WiseGuru/obsidian-automatic-list-styles";
        };
    }
);
    "automatic-renumbering" = (
    let
        baseUrl = "https://github.com/OmriLeviGit/Auto-List-Management-Obsidian/releases/download/2.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "automatic-renumbering";
        version = "2.0.13";
        outputHash = "sha256-EoTc4+hkRGLFE45hs4AlyukVzJvr4W/OiiqR1MckHN0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically reorder checklists and numbered lists as you edit them.'';
            homepage = "https://github.com/OmriLeviGit/Auto-List-Management-Obsidian";
        };
    }
);
    "automatic-table-of-contents" = (
    let
        baseUrl = "https://github.com/johansatge/obsidian-automatic-table-of-contents/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "automatic-table-of-contents";
        version = "1.8.0";
        outputHash = "sha256-ujN6YXwzHaqJN0AhJODp97SevwAjHjOZbajGyvSskuU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a table of contents in a note that updates itself when the note changes.'';
            homepage = "https://github.com/johansatge/obsidian-automatic-table-of-contents";
        };
    }
);
    "automatic-tags" = (
    let
        baseUrl = "https://github.com/Jamalam360/obsidian-automatic-tags/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "automatic-tags";
        version = "1.1.1";
        outputHash = "sha256-eOsXShqDBpu4SSBnMYAw4aAWN4C22N+mrXebMAPHijk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add tags to new notes automatically based on their path.'';
            homepage = "https://github.com/Jamalam360/obsidian-automatic-tags";
        };
    }
);
    "automation" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-automation/releases/download/0.2.0-b1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "automation";
        version = "0.2.0-b1";
        outputHash = "sha256-p1C5xN68/Ea6S9pS+U+apV7tB1WFhm2jhnhZu0SY3t4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute commands on specific events.'';
            homepage = "https://github.com/Benature/obsidian-automation";
        };
    }
);
    "autoplay-and-loop" = (
    let
        baseUrl = "https://github.com/Wapply/obsidian-autoplay-and-loop/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "autoplay-and-loop";
        version = "1.0.2";
        outputHash = "sha256-Ln4kKXAnShmNez3M7hWcG3Emb8JIJaJYUg8LugST3fI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AutoPlay videos inside and outside notes.'';
            homepage = "https://github.com/Wapply/obsidian-autoplay-and-loop";
        };
    }
);
    "avatar" = (
    let
        baseUrl = "https://github.com/maradotwebp/obsidian-avatar/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "avatar";
        version = "1.1.0";
        outputHash = "sha256-XY5kAfy42VabNRoLbQpxEGbCZD2VqPjcTe9iQGDpcJo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display an avatar image in your notes.'';
            homepage = "https://github.com/maradotwebp/obsidian-avatar";
        };
    }
);
    "aw-watcher-obsidian" = (
    let
        baseUrl = "https://github.com/LordGrimmauld/aw-watcher-obsidian/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "aw-watcher-obsidian";
        version = "1.3.0";
        outputHash = "sha256-O3ozKN305WghPItlECrgGQ/Z5jjnqeAqRz0Or7g/IBQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate with ActivityWatch to allow detailed tracking of time spent in Obsidian.'';
            homepage = "https://github.com/LordGrimmauld/aw-watcher-obsidian";
        };
    }
);
    "awesome-image" = (
    let
        baseUrl = "https://github.com/AwesomeDog/obsidian-awesome-image/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "awesome-image";
        version = "0.1.3";
        outputHash = "sha256-PO26mmC+ZMYGuiSYfwAQ2SveptX4icsJS9LZxqRXCFs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''One-stop solution for image management and viewing.'';
            homepage = "https://github.com/AwesomeDog/obsidian-awesome-image";
        };
    }
);
    "awesome-reader" = (
    let
        baseUrl = "https://github.com/AwesomeDog/obsidian-awesome-reader/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "awesome-reader";
        version = "0.1.4";
        outputHash = "sha256-/gZ+VLJiTSNitYQaeHPlEFaSn/c9H3ixhy0BelWZYbA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add support for multiple ebook formats. Remember reading progress and create notes from the table of contents.'';
            homepage = "https://github.com/AwesomeDog/obsidian-awesome-reader";
        };
    }
);
    "ayanite" = (
    let
        baseUrl = "https://github.com/jemstelos/obsidian-ayanite/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ayanite";
        version = "1.0.0";
        outputHash = "sha256-VSzrxcwffKl7vpbB4j844D5CDdGoHneIoT+U5Ao6QhQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An integrated thinking environment - advanced AI chat and knowledge copilot (Closed source).'';
            homepage = "https://github.com/jemstelos/obsidian-ayanite";
        };
    }
);
    "azure-linker" = (
    let
        baseUrl = "https://github.com/srz2/obsidian-azure-devops-linker/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "azure-linker";
        version = "1.0.3";
        outputHash = "sha256-Xrfq0xghPWktnAyNbf8BE5nRAV8VM6EN6AWQozeiQZc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly format an Azure DevOps issue tag as a link to you Azure DevOps instance.'';
            homepage = "https://github.com/srz2/obsidian-azure-devops-linker";
        };
    }
);
    "babashka" = (
    let
        baseUrl = "https://github.com/filipesilva/obsidian-babashka/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "babashka";
        version = "1.0.5";
        outputHash = "sha256-H8PH5qUHlSKZTm1EYEnCPRiAt+juPwwuLxKVubFmgms=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Evaluate Clojure(Script) code blocks in Babashka.'';
            homepage = "https://github.com/filipesilva/obsidian-babashka";
        };
    }
);
    "back-it-up" = (
    let
        baseUrl = "https://github.com/hammadxp/back-it-up/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "back-it-up";
        version = "1.0.0";
        outputHash = "sha256-u8MJNmdohbCNUFYmQ8nyWirCls1qHzj94BHThijr0/8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly make a copy or snapshot of a note.'';
            homepage = "https://github.com/hammadxp/back-it-up";
        };
    }
);
    "background-image" = (
    let
        baseUrl = "https://github.com/shmolf/obsidian-editor-background/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "background-image";
        version = "1.3.0";
        outputHash = "sha256-OeLJDnftjqlvYel/DByOcN6mpUu9aTeWdkr1i/02JxE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Specify a remote URL as the background image, and a few settings to tweak the experience.'';
            homepage = "https://github.com/shmolf/obsidian-editor-background";
        };
    }
);
    "backgroundset" = (
    let
        baseUrl = "https://github.com/Youngmoss/obsidian-backgroundset/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "backgroundset";
        version = "0.8.0";
        outputHash = "sha256-rql8xayfOD1mC0XRcFMSYa2UwEPMA0flzzqnqD8oEa8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allow you to set background image set(folder)'';
            homepage = "https://github.com/Youngmoss/obsidian-backgroundset";
        };
    }
);
    "backlink-cache" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-backlink-cache/releases/download/2.12.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "backlink-cache";
        version = "2.12.1";
        outputHash = "sha256-Ftquol79Lsf/WzdY3IbWR/WnKWZE08sZGARqTwIH8xM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Store backlink cache to speed up `app.metadataCache.getBacklinksForFile`.'';
            homepage = "https://github.com/mnaoumov/obsidian-backlink-cache";
        };
    }
);
    "backlink-full-path" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-backlink-full-path/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "backlink-full-path";
        version = "1.6.2";
        outputHash = "sha256-niHe3R4UNmxZgC02GISkNpq0knjwhYk+jq5jWHbuRUM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Shows the backlink"s full path in the backlinks panel.'';
            homepage = "https://github.com/mnaoumov/obsidian-backlink-full-path";
        };
    }
);
    "backlink-settings" = (
    let
        baseUrl = "https://github.com/calvinwyoung/obsidian-backlink-settings/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "backlink-settings";
        version = "1.0.1";
        outputHash = "sha256-S/iIPjVMKeQIaj2SRUiA312+Fi0VpJBsjc+p5XcbrwI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allow saving default settings for the backlinks / "Linked mentions" pane at the bottom of notes.'';
            homepage = "https://github.com/calvinwyoung/obsidian-backlink-settings";
        };
    }
);
    "backtick-text-selector" = (
    let
        baseUrl = "https://github.com/cool-RR/backtick-text-selector/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "backtick-text-selector";
        version = "1.0.8";
        outputHash = "sha256-1n2F+Q0Dz2/hIYMcYp6EEyXxS0s5yFt27CDeaIzz9TU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily select text in backticks.'';
            homepage = "https://github.com/cool-RR/backtick-text-selector";
        };
    }
);
    "badges" = (
    let
        baseUrl = "https://github.com/gapmiss/badges/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "badges";
        version = "1.1.0";
        outputHash = "sha256-cK4widNv73aBl0KvXmSRKD5ilYtLHYArR8GpXi1lrbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add inline badges/callouts to notes.'';
            homepage = "https://github.com/gapmiss/badges";
        };
    }
);
    "banners-reloaded" = (
    let
        baseUrl = "https://github.com/dgcreations00/obsidian-banners-reloaded/releases/download/1.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "banners-reloaded";
        version = "1.2.7";
        outputHash = "sha256-Lc/vMcTurwMQ2Gkc0+jWGIbUtOzGHGJw1HrR6cLbfm0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple, fast, and lightweight way to add customizable banners to your notes.'';
            homepage = "https://github.com/dgcreations00/obsidian-banners-reloaded";
        };
    }
);
    "banyan" = (
    let
        baseUrl = "https://github.com/ratiger/obsidian-banyan/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "banyan";
        version = "1.4.2";
        outputHash = "sha256-iEMFHlFT2aHhHRpthIwFPW9V66uS8wx/3Dim6Myrd9k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A homepage to browse, organize, and access notes in card form easily with multi-tag filtering.'';
            homepage = "https://github.com/ratiger/obsidian-banyan";
        };
    }
);
    "barcode-generator" = (
    let
        baseUrl = "https://github.com/noxonad/obsidian-barcode-generator/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "barcode-generator";
        version = "1.1.0";
        outputHash = "sha256-0q8N8DSGozj12cCZMonQjWBtpqLoyViVcCJTZW+Kmgw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate customizable barcodes into your notes.'';
            homepage = "https://github.com/noxonad/obsidian-barcode-generator";
        };
    }
);
    "battery-indicator" = (
    let
        baseUrl = "https://github.com/Opisek/obsidian-battery-indicator/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "battery-indicator";
        version = "1.0.2";
        outputHash = "sha256-X0PDy8SO00qTmJr0gyRXVpfvrStshuuSoxzqpWXisPM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays current battery level in the status bar.'';
            homepage = "https://github.com/Opisek/obsidian-battery-indicator";
        };
    }
);
    "battlesnake-viewer" = (
    let
        baseUrl = "https://github.com/EnderInvader/battlesnake-viewer/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "battlesnake-viewer";
        version = "0.1.4";
        outputHash = "sha256-9GIe5IdHE34IbGaT20HAbK7vfOPc6VFJ6SltZppKZCM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render BattleSnake positions diagrams in note preview.'';
            homepage = "https://github.com/EnderInvader/battlesnake-viewer";
        };
    }
);
    "bbawj-semantic-search" = (
    let
        baseUrl = "https://github.com/bbawj/obsidian-semantic-search/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bbawj-semantic-search";
        version = "0.4.1";
        outputHash = "sha256-1Dm6A7EEWN0iNNCqNLKL3DpbZ36BSfkSICXOaE52rOo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Semantic search for files using OpenAI"s text embeddings.'';
            homepage = "https://github.com/bbawj/obsidian-semantic-search";
        };
    }
);
    "bearings" = (
    let
        baseUrl = "https://github.com/jeetsukumaran/obsidian-bearings/releases/download/1.4.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bearings";
        version = "1.4.8";
        outputHash = "sha256-upCoOA6E66EEraiEdMvXxfOlXNEgUNUbTGz5nDcABp4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Flow through dynamically-scoped collapsible tree views of your vault"s semantic and logical architectures.'';
            homepage = "https://github.com/jeetsukumaran/obsidian-bearings";
        };
    }
);
    "beautiful-contact-cards" = (
    let
        baseUrl = "https://github.com/seth10/beautiful-obsidian-contacts/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "beautiful-contact-cards";
        version = "1.6.2";
        outputHash = "sha256-B5E6ATInmG7rngJjoTcSUgTKzeZ5/PmU4jNM906YF0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Renders "contact" code blocks with tappable links for phone, social media, etc.'';
            homepage = "https://github.com/seth10/beautiful-obsidian-contacts";
        };
    }
);
    "beautitab" = (
    let
        baseUrl = "https://github.com/andrewmcgivery/obsidian-beautitab/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "beautitab";
        version = "1.6.1";
        outputHash = "sha256-DqMzagF7zIwVEBAn1rE2tcsNp58US8WWPeY+qNSxawc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates a customizable new tab view with beautiful backgrounds, quotes, search, and more.'';
            homepage = "https://github.com/andrewmcgivery/obsidian-beautitab";
        };
    }
);
    "beeminder-word-count-plugin" = (
    let
        baseUrl = "https://github.com/kenzan100/beeminder-obsidian-word-count/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "beeminder-word-count-plugin";
        version = "1.0.1";
        outputHash = "sha256-QPbyyQYFCUOK+STs+1/OSH0GtG/WBQcZJlEF9h5zGdc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post word counts directly to Beeminder.'';
            homepage = "https://github.com/kenzan100/beeminder-obsidian-word-count";
        };
    }
);
    "better-canvas-lock" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-better-canvas-lock/releases/download/0.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-canvas-lock";
        version = "0.0.15";
        outputHash = "sha256-b+DsZlZLSrsQvYD9nnKjohAmuh7Md8cvwU2jj+RlCY0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance the read-only mode in Canvas with fully lock the scroll, zoom, drag-and-drop in read-only!'';
            homepage = "https://github.com/Mara-Li/obsidian-better-canvas-lock";
        };
    }
);
    "better-comment-toggle" = (
    let
        baseUrl = "https://github.com/MrGVSV/obsidian-better-comment-toggle/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-comment-toggle";
        version = "1.0.3";
        outputHash = "sha256-Mqj3bJ7Mn/KqNvaZy1yOqWQZegJzVtU+irL3ncjhzaU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improved comment toggling.'';
            homepage = "https://github.com/MrGVSV/obsidian-better-comment-toggle";
        };
    }
);
    "better-export-pdf" = (
    let
        baseUrl = "https://github.com/l1xnan/obsidian-better-export-pdf/releases/download/1.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-export-pdf";
        version = "1.11.0";
        outputHash = "sha256-9q5gHFJsMXnS+tskkmXaUzoNvWywtqpnboGEff4vfPE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export your notes to PDF, supports export preview, add bookmarks outline and header/footer.'';
            homepage = "https://github.com/l1xnan/obsidian-better-export-pdf";
        };
    }
);
    "better-heading-hierarchy" = (
    let
        baseUrl = "https://github.com/rogerfan48/better-heading-hierarchy/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-heading-hierarchy";
        version = "1.0.5";
        outputHash = "sha256-0t5TbMauDlyas7bCOZhhFAcsML3qdeu47NuYWmMOnD0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add guide lines to make the hierarchy of Markdown headings more visually clear.'';
            homepage = "https://github.com/rogerfan48/better-heading-hierarchy";
        };
    }
);
    "better-inline-fields" = (
    let
        baseUrl = "https://github.com/dsarman/better-inline-fields/releases/download/0.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-inline-fields";
        version = "0.3.5";
        outputHash = "sha256-yKkrsH1mdm1UNhnqh8dBSXGcA0e4TAf2U6yL86dg5Uo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance Dataview-style inline fields.'';
            homepage = "https://github.com/dsarman/better-inline-fields";
        };
    }
);
    "better-link-clicker" = (
    let
        baseUrl = "https://github.com/eniverz/obsidian-better-link-clicker-plugin/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-link-clicker";
        version = "1.3.2";
        outputHash = "sha256-YfxZXGnh9zRLR9hkOkXp83JY88mc671r1Qo4VlhlTgA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Changes link click behavior to edit on click and navigate on Ctrl+Click.'';
            homepage = "https://github.com/eniverz/obsidian-better-link-clicker-plugin";
        };
    }
);
    "better-markdown-links" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-better-markdown-links/releases/download/3.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-markdown-links";
        version = "3.4.4";
        outputHash = "sha256-eu2dk49enCLXKzNL33ic6WsDhhBBNRWiU3mnvctnHOQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds support for angle bracket links and manages relative links properly'';
            homepage = "https://github.com/mnaoumov/obsidian-better-markdown-links";
        };
    }
);
    "better-mathjax" = (
    let
        baseUrl = "https://github.com/greasycat/BetterMathjax/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-mathjax";
        version = "1.0.1";
        outputHash = "sha256-vei1HJKDxPuuzdyq0LLSMQfaR0hyf3GcwG30k5fBTOQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Math autocompletion and customizable snippets.'';
            homepage = "https://github.com/greasycat/BetterMathjax";
        };
    }
);
    "better-mindmap" = (
    let
        baseUrl = "https://github.com/linem-davton/obsidian-better-mindmap/releases/download/1.0.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-mindmap";
        version = "1.0.18";
        outputHash = "sha256-anDUY54AB2lkZzpEX+mSHNm+FJkM3slbNJAYSskMHe4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize notes as interactive mind maps'';
            homepage = "https://github.com/linem-davton/obsidian-better-mindmap";
        };
    }
);
    "better-order-list" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Better-Order-List/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-order-list";
        version = "1.1.1";
        outputHash = "sha256-C70JMYnrAIbAqqaWlIkJ3s/jUkUimTJK3yoEdzJZQ4M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Support new line order list like 1、 or (1)., etc.'';
            homepage = "https://github.com/Quorafind/Obsidian-Better-Order-List";
        };
    }
);
    "better-pdf-plugin" = (
    let
        baseUrl = "https://github.com/MSzturc/obsidian-better-pdf-plugin/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-pdf-plugin";
        version = "1.4.0";
        outputHash = "sha256-6hQ0LhdDlJFjQhnR4Lf1CcyRs14mudHNb6PdPhmx5lM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert, scale, rotate and cut-out PDF pages into your notes.'';
            homepage = "https://github.com/MSzturc/obsidian-better-pdf-plugin";
        };
    }
);
    "better-plugins-manager" = (
    let
        baseUrl = "https://github.com/eondrcode/obsidian-manager/releases/download/0.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-plugins-manager";
        version = "0.4.3";
        outputHash = "sha256-SCajSP23b9T0YQ8JmCVaOWuoidpRzcMVZAKHimjw14k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''打造您的极致插件管理体验，让插件管理变得更加直观、高效，同时提升您的工作流程和个性化设置。'';
            homepage = "https://github.com/eondrcode/obsidian-manager";
        };
    }
);
    "better-recall" = (
    let
        baseUrl = "https://github.com/FlorianWoelki/obsidian-better-recall/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-recall";
        version = "0.0.4";
        outputHash = "sha256-n72LiXQQVPqbPtos6Q4fDpE0IyvzQwZSbTRoxJMNsEw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add anki-like spaced repetition and recall to your vault.'';
            homepage = "https://github.com/FlorianWoelki/obsidian-better-recall";
        };
    }
);
    "better-search-views" = (
    let
        baseUrl = "https://github.com/ivan-lednev/better-search-views/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-search-views";
        version = "0.3.0";
        outputHash = "sha256-MHX7U2kg+bIoZ4D1pNUCw71H/v97M3ilHDXNl7yf5/Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upgrade global search, backlinks and queries with outliner-like context trees.'';
            homepage = "https://github.com/ivan-lednev/better-search-views";
        };
    }
);
    "better-word-count" = (
    let
        baseUrl = "https://github.com/lukeleppan/better-word-count/releases/download/0.10.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "better-word-count";
        version = "0.10.1";
        outputHash = "sha256-7T8K3b6wgr6BHJxgcj4mddeHAJF6WyrQmTUsFUdsprc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Count the words of selected text in the editor.'';
            homepage = "https://github.com/lukeleppan/better-word-count";
        };
    }
);
    "bibdesk-integration" = (
    let
        baseUrl = "https://github.com/alberti42/obsidian-bibdesk-integration/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bibdesk-integration";
        version = "1.2.1";
        outputHash = "sha256-LpEqpctbYsjjStBHdpGvyuJFts0OEY8PN5BiQDSyJrw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import BibTex citations into your notes and open PDF documents linked in a BibDesk library'';
            homepage = "https://github.com/alberti42/obsidian-bibdesk-integration";
        };
    }
);
    "bible-linker-pro" = (
    let
        baseUrl = "https://github.com/Floydv149/bibleLinkerPro/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bible-linker-pro";
        version = "1.8.0";
        outputHash = "sha256-EOwwPSIXvrqKn8jVgnuWTgXLnM7W/uu/bi7qC/hwK4E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Converts Bible texts to JW Library links'';
            homepage = "https://github.com/Floydv149/bibleLinkerPro";
        };
    }
);
    "bible-sidecar" = (
    let
        baseUrl = "https://github.com/janisringli/bible-sidecar-obsidian-plugin/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bible-sidecar";
        version = "1.2.5";
        outputHash = "sha256-ESfG/001IBuNYUYQj3o0mZuDzAEssCR6Fih+UVb/c8Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open the bible in your preferred translation in a split-screen view within your workspace'';
            homepage = "https://github.com/janisringli/bible-sidecar-obsidian-plugin";
        };
    }
);
    "biblib" = (
    let
        baseUrl = "https://github.com/callumalpass/obsidian-biblib/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "biblib";
        version = "1.8.0";
        outputHash = "sha256-oMIbfvcI79NK4dkHo4gTw+cre9y/V/gJ/kofYqRJOvM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create literature notes and manage bibliographic references'';
            homepage = "https://github.com/callumalpass/obsidian-biblib";
        };
    }
);
    "bibtex-entry-view" = (
    let
        baseUrl = "https://github.com/awfrok/obsidian-plugin-bibtex-entry-view/releases/download/0.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bibtex-entry-view";
        version = "0.2.4";
        outputHash = "sha256-uC5EZTSf8iVpe5UaI0EpUt+6xmJx6UH3dL5ohrd43oo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Load the bibtex entry of a given bibkey from a given bib file and show the entry in the code block of bibkey.'';
            homepage = "https://github.com/awfrok/obsidian-plugin-bibtex-entry-view";
        };
    }
);
    "bibtex-manager" = (
    let
        baseUrl = "https://github.com/akopdev/obsidian-bibtex-manager/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bibtex-manager";
        version = "1.1.0";
        outputHash = "sha256-x/Dh+TNXDufEZWlZEjQSEDWoaqsn+iJwmbA1IwXyD/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a literature notes from a BibTeX entries.'';
            homepage = "https://github.com/akopdev/obsidian-bibtex-manager";
        };
    }
);
    "bibtex-scholar" = (
    let
        baseUrl = "https://github.com/liu-qilong/bibtex-scholar/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bibtex-scholar";
        version = "1.1.0";
        outputHash = "sha256-Lqpnjx0p8YPaGz0/JRj7FqEwPaD2s0QBu5MBWIMZd58=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A complete BibTeX citation management solution designed for contextual, frictionless literature reviews and paper writing--right inside your notes.'';
            homepage = "https://github.com/liu-qilong/bibtex-scholar";
        };
    }
);
    "big-calendar" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Big-Calendar/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "big-calendar";
        version = "1.2.0";
        outputHash = "sha256-sza7IbgU0zu+oMi/gjeG13x0SyCiTFx4wDfoWOS5SBw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A big calendar for Obsidian. All events from your daily notes OR tasks used TASKS/DATAVIEW/KANBAN format.'';
            homepage = "https://github.com/Quorafind/Obsidian-Big-Calendar";
        };
    }
);
    "bin-theme-controller" = (
    let
        baseUrl = "https://github.com/Binaris00/Theme-Controller/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bin-theme-controller";
        version = "1.0.5";
        outputHash = "sha256-sh7bP6hLZl8F7oJ9L/BZjeYS7ZVxjmlUN89iSWgKDyA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set when and how the themes will be displayed.'';
            homepage = "https://github.com/Binaris00/Theme-Controller";
        };
    }
);
    "birthday-tracker" = (
    let
        baseUrl = "https://github.com/Raboro/Obsidian-Birthday-Tracker-Plugin/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "birthday-tracker";
        version = "1.2.2";
        outputHash = "sha256-7aDWEh49Ir/C7rg7CH5Xk4mDc6KuUWq4wb7Xwf9PsIw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep track of all birthdays of your family and friends.'';
            homepage = "https://github.com/Raboro/Obsidian-Birthday-Tracker-Plugin";
        };
    }
);
    "bitcoin-block-stamp" = (
    let
        baseUrl = "https://github.com/sfr0xyz/obsidian-bitcoin-block-stamp/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bitcoin-block-stamp";
        version = "1.2.0";
        outputHash = "sha256-6sKWpRsS9QILKj7nfi2eYRMmUnYq3+7aU95z52wYegc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Stamp your notes with the current Bitcoin block.'';
            homepage = "https://github.com/sfr0xyz/obsidian-bitcoin-block-stamp";
        };
    }
);
    "blaze-jump" = (
    let
        baseUrl = "https://github.com/henryco/BlazeJump-Obsidian/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blaze-jump";
        version = "1.0.7";
        outputHash = "sha256-pEtHBX5y7jjRJVA2hFsQJSNB1Hbf1x+ZwgZNuEGyo1o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Navigate through text at blazing speed.'';
            homepage = "https://github.com/henryco/BlazeJump-Obsidian";
        };
    }
);
    "blindfold-obsidian" = (
    let
        baseUrl = "https://github.com/vzsky/blindfold-obsidian/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blindfold-obsidian";
        version = "1.0.2";
        outputHash = "sha256-SQkaltWt4wnRDB1etzRLFkv5faGR0X9mGF7t/DkpA2U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fold text by making it completely hidden.'';
            homepage = "https://github.com/vzsky/blindfold-obsidian";
        };
    }
);
    "block-link-plus" = (
    let
        baseUrl = "https://github.com/Jasper-1024/block-link-plus/releases/download/2.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "block-link-plus";
        version = "2.0.12";
        outputHash = "sha256-HGuf5dC7UElMAmTP/lVckIPkhLyGHlYh74Ix4bj/zAE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simplifies the creation of multi-line blockquotes, with customizable block IDs.'';
            homepage = "https://github.com/Jasper-1024/block-link-plus";
        };
    }
);
    "blockier" = (
    let
        baseUrl = "https://github.com/blorbb/obsidian-blockier/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blockier";
        version = "1.2.1";
        outputHash = "sha256-L7O8Jads4UyxvVMwmn60pWdwBZfQhkzgR+fkeDruVG0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extra block editing utilities.'';
            homepage = "https://github.com/blorbb/obsidian-blockier";
        };
    }
);
    "blockquote-levels" = (
    let
        baseUrl = "https://github.com/czottmann/obsidian-blockquote-levels/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blockquote-levels";
        version = "1.1.0";
        outputHash = "sha256-0Lo3Jf6o+tMQTou+F3es2A0DeD4kN6Bd8D4gJ/19tlo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add commands for increasing/decreasing the blockquote level of the current line or selection(s).'';
            homepage = "https://github.com/czottmann/obsidian-blockquote-levels";
        };
    }
);
    "blockreffer" = (
    let
        baseUrl = "https://github.com/tyler-dot-earth/obsidian-blockreffer/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blockreffer";
        version = "1.5.0";
        outputHash = "sha256-lrwwlNls2I8vT4NweP85OrLbEsGdoGuVtaQmVV/teMU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and embed blocks with ^block-references.'';
            homepage = "https://github.com/tyler-dot-earth/obsidian-blockreffer";
        };
    }
);
    "blue-star" = (
    let
        baseUrl = "https://github.com/Lio5n/blue-star/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blue-star";
        version = "1.2.5";
        outputHash = "sha256-yd/xEGajBvVoj8Z2k/lZA8MNBw41Z1+1geTl2g8VVBA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate Anki flashcards in multiple ways.'';
            homepage = "https://github.com/Lio5n/blue-star";
        };
    }
);
    "blueprint" = (
    let
        baseUrl = "https://github.com/madx/blueprint-obsidian-plugin/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blueprint";
        version = "0.6.0";
        outputHash = "sha256-cKbxf8LwZ0VQ3xR21kcJKqjjUTw1+Qk09oO+ePArkfw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write once, update everywhere - note templates that pull from Properties.'';
            homepage = "https://github.com/madx/blueprint-obsidian-plugin";
        };
    }
);
    "blueprint-renderer" = (
    let
        baseUrl = "https://github.com/goderyu/obsidian-blueprint-renderer/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blueprint-renderer";
        version = "1.0.1";
        outputHash = "sha256-EdHQqrrNkgFy6XaDlFcB7ERHAyuH5bMQ4eqFIzRT5tI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Unreal Engine Blueprint nodes as interactive visual diagrams using BlueprintUE rendering engine'';
            homepage = "https://github.com/goderyu/obsidian-blueprint-renderer";
        };
    }
);
    "bluesky" = (
    let
        baseUrl = "https://github.com/eharris128/obsidian-bluesky/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bluesky";
        version = "1.0.8";
        outputHash = "sha256-7iinB9SB1LeIboFc94ewdvqiA6qkC3fEVdFu+vyt1eg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post to Bluesky.'';
            homepage = "https://github.com/eharris128/obsidian-bluesky";
        };
    }
);
    "blur" = (
    let
        baseUrl = "https://github.com/gapmiss/blur/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "blur";
        version = "1.0.0";
        outputHash = "sha256-nodoFr6kASKyJcABizXL5l+l6P3Au9oONTFxLBvfieM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create obfuscated blocks of text.'';
            homepage = "https://github.com/gapmiss/blur";
        };
    }
);
    "bmo-chatbot" = (
    let
        baseUrl = "https://github.com/longy2k/obsidian-bmo-chatbot/releases/download/2.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bmo-chatbot";
        version = "2.3.3";
        outputHash = "sha256-KMhyI7htEoCTIxlOuvV4JKEbbTJ52sdAPuTyGYhYJN0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate and brainstorm ideas while creating your notes using Large Language Models (LLMs) such as OpenAI"s "gpt-3.5-turbo" and "gpt-4".'';
            homepage = "https://github.com/longy2k/obsidian-bmo-chatbot";
        };
    }
);
    "boardgame-search" = (
    let
        baseUrl = "https://github.com/Marlon154/boardgame-search/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "boardgame-search";
        version = "2.3.0";
        outputHash = "sha256-H1RLdWqTh5ILX2qM0Ifde0W3Wlk+1jr0PrtuB5FGva0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps board game enthusiasts track their game collection seamlessly within their notes.'';
            homepage = "https://github.com/Marlon154/boardgame-search";
        };
    }
);
    "book-clipper" = (
    let
        baseUrl = "https://github.com/fardm/obsidian-book-clipper/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "book-clipper";
        version = "0.4.0";
        outputHash = "sha256-Q5ln36GKowIJ+5SKVBPlqdwQ7KypUquFTBA8+/F0t/s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save book details from websites into your notes.'';
            homepage = "https://github.com/fardm/obsidian-book-clipper";
        };
    }
);
    "book-smith" = (
    let
        baseUrl = "https://github.com/Yeban8090/book-smith/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "book-smith";
        version = "1.0.5";
        outputHash = "sha256-ZSLZQHXqmXSE+Ze4/sj+Zuid0NxLjxxihwLk0ock4+M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simplify long-form writing and book creation. Organize chapters, track progress, and export your manuscript in various formats for a seamless publishing workflow.'';
            homepage = "https://github.com/Yeban8090/book-smith";
        };
    }
);
    "bookfusion" = (
    let
        baseUrl = "https://github.com/BookFusion/obsidian-plugin/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bookfusion";
        version = "1.0.6";
        outputHash = "sha256-y98Ox2+RQXzfCkXR2z5ciBPqUNbwwVNRRjH36GwgawM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import your BookFusion highlights & annotations into your vault.'';
            homepage = "https://github.com/BookFusion/obsidian-plugin";
        };
    }
);
    "bookmarks-caller" = (
    let
        baseUrl = "https://github.com/namikaze-40p/obsidian-bookmarks-caller/releases/download/0.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bookmarks-caller";
        version = "0.6.5";
        outputHash = "sha256-LBKJJoKXikyICMdGFgkQ3zo11Q5J+rCBG3kkhljkQxU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily open bookmarks.'';
            homepage = "https://github.com/namikaze-40p/obsidian-bookmarks-caller";
        };
    }
);
    "booknav" = (
    let
        baseUrl = "https://github.com/jemberton/obsidian-booknav-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "booknav";
        version = "1.0.0";
        outputHash = "sha256-KsjHEkhF1LAl703oKLsrEeeMi/UmWL2Py3rstwaJml8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Book style page navigation.'';
            homepage = "https://github.com/jemberton/obsidian-booknav-plugin";
        };
    }
);
    "bookshelf" = (
    let
        baseUrl = "https://github.com/weph/obsidian-bookshelf/releases/download/0.20.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bookshelf";
        version = "0.20.0";
        outputHash = "sha256-kb/4TVfw3FZk8WEZFGqtn+Urx/sQs/Y5XAhp1Y7d9ig=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your books and reading progress'';
            homepage = "https://github.com/weph/obsidian-bookshelf";
        };
    }
);
    "booksidian-plugin" = (
    let
        baseUrl = "https://github.com/MichaBrugger/booksidian_plugin/releases/download/0.10.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "booksidian-plugin";
        version = "0.10.1";
        outputHash = "sha256-9fMTdyu9Gw4tFTwV8jNKefq2FgtXVz2K5VNtqKDJIbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Access your Goodreads shelves.'';
            homepage = "https://github.com/MichaBrugger/booksidian_plugin";
        };
    }
);
    "bookxnote-sync" = (
    let
        baseUrl = "https://github.com/CodeListening/obsidian-bookxnote/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bookxnote-sync";
        version = "0.3.1";
        outputHash = "sha256-T9fFLh/otzywSRu8qDXEm8YHcxysw8jcITo2ETeBbcw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''同步bookxnote中的读书笔记'';
            homepage = "https://github.com/CodeListening/obsidian-bookxnote";
        };
    }
);
    "boost-link-suggestions" = (
    let
        baseUrl = "https://github.com/jglev/obsidian-boost-link-suggestions/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "boost-link-suggestions";
        version = "1.1.0";
        outputHash = "sha256-RRq7jZ7H2VMhBG8NSzrMJuo2HDTMby2jnmA6EAL/PBk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Alternative inline link suggester that orders results by link count and manual boosts.'';
            homepage = "https://github.com/jglev/obsidian-boost-link-suggestions";
        };
    }
);
    "bottom-to-top" = (
    let
        baseUrl = "https://github.com/lizard-heart/obsidian-bottom-to-top-text/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bottom-to-top";
        version = "1.0.2";
        outputHash = "sha256-MKTZtJ0YNv+KcwL6pVKnfyK4rtnlLyWdyWs9Qe7hivk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makes text go from left-to-right, bottom-to-top.'';
            homepage = "https://github.com/lizard-heart/obsidian-bottom-to-top-text";
        };
    }
);
    "bpmn-plugin" = (
    let
        baseUrl = "https://github.com/joleaf/obsidian-bpmn-plugin/releases/download/7.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bpmn-plugin";
        version = "7.3.3";
        outputHash = "sha256-bLeA+wkzsZ3I0WsD9xtR0TWGZ7J2jYxev6r3NDbCqfg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable viewing BPMN diagrams using bpmn-js.'';
            homepage = "https://github.com/joleaf/obsidian-bpmn-plugin";
        };
    }
);
    "brain" = (
    let
        baseUrl = "https://github.com/lusob/obsidian-brain/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "brain";
        version = "1.0.3";
        outputHash = "sha256-+AA40q6NAFwf5g9FzxhxU5Dn885l9YDMDVRy1T5lStQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A ChatGPT powered chatbot specifically focused on question answering over your vault notes.'';
            homepage = "https://github.com/lusob/obsidian-brain";
        };
    }
);
    "brain-dump-mode" = (
    let
        baseUrl = "https://github.com/yesjinu/brain-dump-mode/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "brain-dump-mode";
        version = "1.1.0";
        outputHash = "sha256-UiBLAgZX6S58KloWzwmtFumhx6XOwbx1RsejZTcgpIk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Done is better than perfect. Complete your first-messy-draft before you make it perfect. Your delete key will be DISABLED and all you can do is JUST BURN YOUR KEYBOARDS🔥'';
            homepage = "https://github.com/yesjinu/brain-dump-mode";
        };
    }
);
    "braincache" = (
    let
        baseUrl = "https://github.com/XSPGMike/braincache_obsidian/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "braincache";
        version = "0.0.7";
        outputHash = "sha256-bRwfgI4TG4O3Mzc7tFp2KKQnKvoKwRyPs4C8mvhFOuY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create braincache flashcards.'';
            homepage = "https://github.com/XSPGMike/braincache_obsidian";
        };
    }
);
    "brainframe" = (
    let
        baseUrl = "https://github.com/pedersen/obsidian-brainframe/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "brainframe";
        version = "0.3.0";
        outputHash = "sha256-BtTW/rNNiR2T6NNk3WAxzmQ90DM2tjwQt4oExKUHkiA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tools to make Obsidian more into our second brains.'';
            homepage = "https://github.com/pedersen/obsidian-brainframe";
        };
    }
);
    "breadcrumbs" = (
    let
        baseUrl = "https://github.com/SkepticMystic/breadcrumbs/releases/download/4.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "breadcrumbs";
        version = "4.6.0";
        outputHash = "sha256-dGcULpM2O5zsTUHM/lBOpscC4N1TN0SZZ3Iz0wwUqSI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualise the hierarchy of your vault using a breadcrumb trail or matrix view.'';
            homepage = "https://github.com/SkepticMystic/breadcrumbs";
        };
    }
);
    "break-page" = (
    let
        baseUrl = "https://github.com/corentin-godefroy/Obsidian-BreakPage/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "break-page";
        version = "1.0.0";
        outputHash = "sha256-ILi1fWdgUVOifytmXA2NB/tL4EndFQdLRTxgXvcvvoo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add shortkey and command to insert a break page formating for pdf exports.'';
            homepage = "https://github.com/corentin-godefroy/Obsidian-BreakPage";
        };
    }
);
    "broken-links" = (
    let
        baseUrl = "https://github.com/ipshing/obsidian-broken-links/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "broken-links";
        version = "1.2.2";
        outputHash = "sha256-t4RtVt3dDLDM1of20hEmRNuAAu3xDQjG3kV9Cz8HeGk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find broken links in your vault that don"t connect to notes.'';
            homepage = "https://github.com/ipshing/obsidian-broken-links";
        };
    }
);
    "browser-history" = (
    let
        baseUrl = "https://github.com/noy4/browser-history/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "browser-history";
        version = "1.1.1";
        outputHash = "sha256-Hvo3QeGdpne9lXLppWIz69/2VBhw5gTkhtfQtkP1Dj0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your browser history to notes.'';
            homepage = "https://github.com/noy4/browser-history";
        };
    }
);
    "browser-interface" = (
    let
        baseUrl = "https://github.com/jason-lieb/obsidian-browser-interface-plugin/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "browser-interface";
        version = "1.0.6";
        outputHash = "sha256-FSC2d6kPXYOnoCm95p3XALFMWrGc0Zy0M9eRdr3OVG8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to interact with the browser.'';
            homepage = "https://github.com/jason-lieb/obsidian-browser-interface-plugin";
        };
    }
);
    "buckwalter-transliteration" = (
    let
        baseUrl = "https://github.com/amrojjeh/obsidian-buckwalter/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "buckwalter-transliteration";
        version = "1.0.6";
        outputHash = "sha256-CZrEMHt+N3d9jrh862Pd7kNGEjk4QSMkDsYtAVnIoHU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Renders Arabic using Buckwalter"s encoding scheme.'';
            homepage = "https://github.com/amrojjeh/obsidian-buckwalter";
        };
    }
);
    "bujo-bullets" = (
    let
        baseUrl = "https://github.com/frankolson/obsidian-bujo-bullets/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bujo-bullets";
        version = "1.2.1";
        outputHash = "sha256-24ybx5O6xR/JJ5fqaB3+1IXOIjz6yOIulZe++y836XM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Alternate checkbox types to support Bullet Journal bullets.'';
            homepage = "https://github.com/frankolson/obsidian-bujo-bullets";
        };
    }
);
    "bulk-exporter" = (
    let
        baseUrl = "https://github.com/symunona/obsidian-bulk-exporter/releases/download/2.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bulk-exporter";
        version = "2.0.15";
        outputHash = "sha256-pO6MhZYDznHUS/nk8M/6yS2ontHfKNN8p+36aaVca/I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Filter, export notes from your vault using metadata into a customizable new structure.'';
            homepage = "https://github.com/symunona/obsidian-bulk-exporter";
        };
    }
);
    "bulkopen-selected-links" = (
    let
        baseUrl = "https://github.com/autohub7/obsidian-open-selected-links/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bulkopen-selected-links";
        version = "1.1.0";
        outputHash = "sha256-sUqgNKmiBtHV4/zBrQWqwxVy3Ih8AFPSt10qnjbRArU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily open all selected links in edit mode.'';
            homepage = "https://github.com/autohub7/obsidian-open-selected-links";
        };
    }
);
    "buttons" = (
    let
        baseUrl = "https://github.com/shabegom/buttons/releases/download/0.9.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "buttons";
        version = "0.9.13";
        outputHash = "sha256-cUX+SHKfLb+SqZvFWau+V8zxi46tMwosTWvbPR0Zw2U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Buttons in your notes to run commands, open links, and insert templates.'';
            homepage = "https://github.com/shabegom/buttons";
        };
    }
);
    "bytefield" = (
    let
        baseUrl = "https://github.com/natri0/obsidian-bytefield/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "bytefield";
        version = "0.0.3";
        outputHash = "sha256-TChVj9fCGwq3/8XloM6Yrz+lTm8eEgZex9syumjik6w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds diagrams that show how structures are laid out in memory / network.'';
            homepage = "https://github.com/natri0/obsidian-bytefield";
        };
    }
);
    "ca-sync" = (
    let
        baseUrl = "https://github.com/bhavers/obsidian-ca/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ca-sync";
        version = "1.1.0";
        outputHash = "sha256-xBL2AFXxhxwNuMwSgUQ8qb2z7t8yVItKfFz4JvYZP3k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronise Cognitive Architect (aka IBM IT Architect Assistant) architectures.'';
            homepage = "https://github.com/bhavers/obsidian-ca";
        };
    }
);
    "calc-craft" = (
    let
        baseUrl = "https://github.com/klaudyu/CalcCraft/releases/download/2.3.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calc-craft";
        version = "2.3.7";
        outputHash = "sha256-YzbyTssAfprO3JWPeboQcXV5tVHEjHN/o79jahj7ag8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable table-based calculations with a spreadsheet-like approach, utilizing references. Highlight the dependencies within tables and identifies circular references with support for array formulas.'';
            homepage = "https://github.com/klaudyu/CalcCraft";
        };
    }
);
    "calctex" = (
    let
        baseUrl = "https://github.com/Developer-Mike/obsidian-calctex/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calctex";
        version = "2.0.0";
        outputHash = "sha256-isMzzj+INFQjXnTNdBdnBOcuY/fj8HPjw1e7Z2v28gY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Calculate LaTeX formulas.'';
            homepage = "https://github.com/Developer-Mike/obsidian-calctex";
        };
    }
);
    "calculite" = (
    let
        baseUrl = "https://github.com/gfxholo/calculite/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calculite";
        version = "1.0.1";
        outputHash = "sha256-zpqsxtFnrWVpCAS9uJnv8nlDBJdd5jwZFKOows73its=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Standard calculator with a simple button layout.'';
            homepage = "https://github.com/gfxholo/calculite";
        };
    }
);
    "calendar" = (
    let
        baseUrl = "https://github.com/liamcain/obsidian-calendar-plugin/releases/download/2.0.0-beta.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calendar";
        version = "2.0.0-beta.2";
        outputHash = "sha256-+58jg7BJmgn7iv50YF7nMNh7oyZldOaapmQy/+pnPXs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple calendar widget.'';
            homepage = "https://github.com/liamcain/obsidian-calendar-plugin";
        };
    }
);
    "calendar-bases" = (
    let
        baseUrl = "https://github.com/edrickleong/obsidian-calendar-bases/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calendar-bases";
        version = "0.2.8";
        outputHash = "sha256-MKuDgF5tL2tq90FQTHFdRPKDGfQxcNr4G0bGuZWZTyk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a calendar layout to bases so you can display notes with dates in an interactive calendar view.'';
            homepage = "https://github.com/edrickleong/obsidian-calendar-bases";
        };
    }
);
    "calendar-event-sync" = (
    let
        baseUrl = "https://github.com/stephendolan/obsidian-calendar-event-sync/releases/download/2.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calendar-event-sync";
        version = "2.1.2";
        outputHash = "sha256-BRSbhzuQZDQ/Hi3nJUIgfLi2p4av6K0H2nPBZ9cHpbQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your current note with a relevant calendar event.'';
            homepage = "https://github.com/stephendolan/obsidian-calendar-event-sync";
        };
    }
);
    "calendarium" = (
    let
        baseUrl = "https://github.com/javalent/calendarium/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calendarium";
        version = "2.1.0";
        outputHash = "sha256-NnuWK1uPl6P6W/Z+ViQAJJwT1L9kw8bXrlfiaLa9Y1c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Craft mind-bending fantasy and sci-fi calendars.'';
            homepage = "https://github.com/javalent/calendarium";
        };
    }
);
    "callout-copy-buttons" = (
    let
        baseUrl = "https://github.com/alythobani/obsidian-callout-copy-buttons/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "callout-copy-buttons";
        version = "1.0.2";
        outputHash = "sha256-WGM7CBOvdLmm03OdEKw/LGr9Uasf0xLAD8mYZrs2Y6k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds copy buttons to callout blocks in your notes.'';
            homepage = "https://github.com/alythobani/obsidian-callout-copy-buttons";
        };
    }
);
    "callout-integrator" = (
    let
        baseUrl = "https://github.com/Cleoche/obsidian-callout-integrator/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "callout-integrator";
        version = "1.1.4";
        outputHash = "sha256-8ki6U3yuU4/zz5WIlCce5knES+n/tTbn/tdHqfxAGH4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate long blocks of text into callouts.'';
            homepage = "https://github.com/Cleoche/obsidian-callout-integrator";
        };
    }
);
    "callout-manager" = (
    let
        baseUrl = "https://github.com/eth-p/obsidian-callout-manager/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "callout-manager";
        version = "1.1.1";
        outputHash = "sha256-la2f1IXfYqVrf7nEJi95SwV+yq14fOCLeOY/BPvW+8s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily create and customize callouts.'';
            homepage = "https://github.com/eth-p/obsidian-callout-manager";
        };
    }
);
    "callout-menu" = (
    let
        baseUrl = "https://github.com/anareaty/callout-menu/releases/download/1.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "callout-menu";
        version = "1.2.7";
        outputHash = "sha256-dXTp5b2rSHkr4pXf1M5KmONHhvDg3v5uykVg9pnhqG0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds some extra options to callouts context menu and allows you to add your own custom callouts.'';
            homepage = "https://github.com/anareaty/callout-menu";
        };
    }
);
    "callout-suggestions" = (
    let
        baseUrl = "https://github.com/cwfryer/obsidian-callout-suggestions/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "callout-suggestions";
        version = "1.0.2";
        outputHash = "sha256-G9X7AI73dTbuiDIcglpwm9MbGR+MSjDbQ5A30nuDzsA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a fuzzy searched suggestion modal for callouts.'';
            homepage = "https://github.com/cwfryer/obsidian-callout-suggestions";
        };
    }
);
    "callout-toggles" = (
    let
        baseUrl = "https://github.com/alythobani/obsidian-callout-toggles/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "callout-toggles";
        version = "1.2.1";
        outputHash = "sha256-jbMQHncomKKGpVX9twIC5GZHoqhWongD+/A+h1varOM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly add, change, or remove callouts in your notes.'';
            homepage = "https://github.com/alythobani/obsidian-callout-toggles";
        };
    }
);
    "calloutx" = (
    let
        baseUrl = "https://github.com/br4in/calloutX/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "calloutx";
        version = "1.0.0";
        outputHash = "sha256-lcgZUxHA+8faZFPkP+1gPkGRP4RgTEWX+5olXZffXOw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An easy way to explore, visualise, and modify callout icons.'';
            homepage = "https://github.com/br4in/calloutX";
        };
    }
);
    "cannoli" = (
    let
        baseUrl = "https://github.com/DeabLabs/cannoli/releases/download/2.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cannoli";
        version = "2.3.3";
        outputHash = "sha256-OlMlvpkWXvn+N9DlrXySBD9j3y2bjQ+DxL4AJMMWZAs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and run LLM scripts in canvas.'';
            homepage = "https://github.com/DeabLabs/cannoli";
        };
    }
);
    "canvas-card-bg-remover" = (
    let
        baseUrl = "https://github.com/luxmargos/obsidian-canvas-card-bg-remover/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-card-bg-remover";
        version = "0.1.4";
        outputHash = "sha256-QOesMxtF4axQaLjEr0Ud+qQbVJVp0pgtdnvCwFkuXeM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make the background of cards transparent in canvas files.'';
            homepage = "https://github.com/luxmargos/obsidian-canvas-card-bg-remover";
        };
    }
);
    "canvas-connect" = (
    let
        baseUrl = "https://github.com/camadkins/obsidian-canvas-connect/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-connect";
        version = "1.0.4";
        outputHash = "sha256-4mqbwHiXqMF96eYUy5IMdTHCg7vwl6jDXx7Izvs5I5k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamically update connection anchors in Canvas view with smart routing.'';
            homepage = "https://github.com/camadkins/obsidian-canvas-connect";
        };
    }
);
    "canvas-css-class" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-canvas-css-class/releases/download/2.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-css-class";
        version = "2.5.0";
        outputHash = "sha256-HbE3i3QwdOMnL07Jbkt/ItwNuNaJygmyVbo+QidT9Sk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a CSS class to the canvas, but also other attributes.'';
            homepage = "https://github.com/Mara-Li/obsidian-canvas-css-class";
        };
    }
);
    "canvas-dailynote" = (
    let
        baseUrl = "https://github.com/andrewmcgivery/obsidian-canvas-dailynote/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-dailynote";
        version = "1.0.2";
        outputHash = "sha256-MAuqXdh/eRh8JZBG9qOEkhBB5vvafaiqB1R0utTd50g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a daily note node to the canvas that will always show today"s note.'';
            homepage = "https://github.com/andrewmcgivery/obsidian-canvas-dailynote";
        };
    }
);
    "canvas-explorer" = (
    let
        baseUrl = "https://github.com/hjamet/Canvas-Explorer/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-explorer";
        version = "1.1.0";
        outputHash = "sha256-kSX/SUgJG3FJ8lruV/WTcNK/I53l2Xp4zxbKWS9Evfg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Explore your vault by iteratively adding or ignoring linked notes, ultimately generating a customizable canvas that visually represents the preserved notes and their connections.'';
            homepage = "https://github.com/hjamet/Canvas-Explorer";
        };
    }
);
    "canvas-filter" = (
    let
        baseUrl = "https://github.com/IKoshelev/Obsidian-Canvas-Filter/releases/download/0.9.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-filter";
        version = "0.9.4";
        outputHash = "sha256-vDlFc9AdsR45qqOFz90x9IKeaSDqsK6IWdeuVASxzCE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Filter Canvas to only show items of specific color, tags or only connected to currently selected node.'';
            homepage = "https://github.com/IKoshelev/Obsidian-Canvas-Filter";
        };
    }
);
    "canvas-format-brush" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-canvas-format-brush/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-format-brush";
        version = "0.1.1";
        outputHash = "sha256-fjTEIvTR4JWoUy3beGfPgqitYMGCf99GqMUtgHQ6Pws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy and paste formatting attributes (size and color) between canvas elements, similar to the format painter in Word.'';
            homepage = "https://github.com/wenlzhang/obsidian-canvas-format-brush";
        };
    }
);
    "canvas-keyboard-pan" = (
    let
        baseUrl = "https://github.com/nathonius/obsidian-canvas-pan/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-keyboard-pan";
        version = "1.0.1";
        outputHash = "sha256-+a7aloLOwAs49gWASuFIV32hJssnvU424POTccgcLoE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pan around your canvas using the keyboard'';
            homepage = "https://github.com/nathonius/obsidian-canvas-pan";
        };
    }
);
    "canvas-link-optimizer" = (
    let
        baseUrl = "https://github.com/Qbject/obsidian-canvas-link-optimizer/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-link-optimizer";
        version = "1.3.0";
        outputHash = "sha256-aHC8iRBRwxcisncxiKl/ZtHkQxib4qbe9NISVZrnKFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Optimize Canvas links by displaying a page thumbnail.'';
            homepage = "https://github.com/Qbject/obsidian-canvas-link-optimizer";
        };
    }
);
    "canvas-link-to-group" = (
    let
        baseUrl = "https://github.com/TGRRRR/Canvas-link-to-group/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-link-to-group";
        version = "0.0.6";
        outputHash = "sha256-tVvgcQR+sW9lqXxYrHcU+IM8hekq7+XvID8npKRHYBg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create links to specific groups in Canvas and jump directly to them.'';
            homepage = "https://github.com/TGRRRR/Canvas-link-to-group";
        };
    }
);
    "canvas-links" = (
    let
        baseUrl = "https://github.com/aqav/obsidian-canvas-links/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-links";
        version = "2.0.1";
        outputHash = "sha256-ew4fvfJpKM2s5oI0HmTFuNZuYsugoyKdfNQNzFHk0Ps=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add views to show "outgoing links" and "backlinks" of canvas in Obsidian.'';
            homepage = "https://github.com/aqav/obsidian-canvas-links";
        };
    }
);
    "canvas-llm" = (
    let
        baseUrl = "https://github.com/farlenkov/obsidian-canvas-llm/releases/download/1.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-llm";
        version = "1.1.7";
        outputHash = "sha256-/1uHn+KaRhlPPRHNLH4T9v2aJBmsMuJGQPRtpG6Zopw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A canvas-like UI to talk with LLMs.'';
            homepage = "https://github.com/farlenkov/obsidian-canvas-llm";
        };
    }
);
    "canvas-mindmap" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Canvas-MindMap/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-mindmap";
        version = "1.0.2";
        outputHash = "sha256-Am3No/4gxL4up/kZqIIVPxPQITuSEqf+YtXw0VMRprk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make your canvas work like a mindmap.'';
            homepage = "https://github.com/Quorafind/Obsidian-Canvas-MindMap";
        };
    }
);
    "canvas-mindmap-helper" = (
    let
        baseUrl = "https://github.com/tim-smart/obsidian-canvas-mindmap/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-mindmap-helper";
        version = "0.2.0";
        outputHash = "sha256-D92EhPofU3B4hg2psBl5tNsBNncTotbG/9KT121ukjM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make the Canvas work like a mindmap.'';
            homepage = "https://github.com/tim-smart/obsidian-canvas-mindmap";
        };
    }
);
    "canvas-minimap" = (
    let
        baseUrl = "https://github.com/ifree/Obsidian-canvas-minimap/releases/download/1.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-minimap";
        version = "1.0.15";
        outputHash = "sha256-OV4Pfj2kE+zpKiSYuSuJoZ5H04WK9cHf/hzu2Jnkjqo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Obsidian Canvas minimap support.'';
            homepage = "https://github.com/ifree/Obsidian-canvas-minimap";
        };
    }
);
    "canvas-node-screenshot" = (
    let
        baseUrl = "https://github.com/cestfredy/obsidian-canvas-node-screenshot/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-node-screenshot";
        version = "1.0.0";
        outputHash = "sha256-mwTDhZ7tf9/rZLnJK3jkVM9B96vgvfXFWdDMj18amQE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Capture node effortlessly with precision screenshot.'';
            homepage = "https://github.com/cestfredy/obsidian-canvas-node-screenshot";
        };
    }
);
    "canvas-picture-in-picture" = (
    let
        baseUrl = "https://github.com/h-sphere/obsidian-canvas-picture-in-picture/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-picture-in-picture";
        version = "0.1.0";
        outputHash = "sha256-guoEQOAs+sGD+ZdxpfKmIPyfIaSk5Z6K2+NWKYzAUfU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables ability to pin Canvas nodes and float them above the board (Picture-in-Picture mode)'';
            homepage = "https://github.com/h-sphere/obsidian-canvas-picture-in-picture";
        };
    }
);
    "canvas-presentation" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Canvas-Presentation/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-presentation";
        version = "0.0.3";
        outputHash = "sha256-kI4BzD7V/pphZgeyehfR/gH4IvGw2oUdSFUKStiC1sA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display cards based on sequence.'';
            homepage = "https://github.com/Quorafind/Obsidian-Canvas-Presentation";
        };
    }
);
    "canvas-randomnote" = (
    let
        baseUrl = "https://github.com/jmilldotdev/obsidian-canvas-randomnote/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-randomnote";
        version = "1.0.1";
        outputHash = "sha256-GPwNMwzLeUDW9csTPQnM8aWOCzpA7btJp+MAlDavowg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add random notes from your vault to the canvas.'';
            homepage = "https://github.com/jmilldotdev/obsidian-canvas-randomnote";
        };
    }
);
    "canvas-send-to-back" = (
    let
        baseUrl = "https://github.com/Zachatoo/obsidian-canvas-send-to-back/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-send-to-back";
        version = "0.1.0";
        outputHash = "sha256-ZZ6/DoG+1puikSewPYLkte20683j/p5j7nWgvY0NTJU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move cards in canvas to the top or behind all other cards.'';
            homepage = "https://github.com/Zachatoo/obsidian-canvas-send-to-back";
        };
    }
);
    "canvas-task-importer" = (
    let
        baseUrl = "https://github.com/jordaeday/canvas-task-importer/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas-task-importer";
        version = "1.0.2";
        outputHash = "sha256-50WLU9Lcno6UKeDRp1F2yIlheJrq4WQjmZyb9w8h8Dc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import assignments from Canvas LMS.'';
            homepage = "https://github.com/jordaeday/canvas-task-importer";
        };
    }
);
    "canvas2document" = (
    let
        baseUrl = "https://github.com/slnsys/obsidian-canvas2document/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvas2document";
        version = "1.3.2";
        outputHash = "sha256-JMpYW1Fp5C6Ki0bBVc5pNdwEtaBFOfkYeVVvZc69Vso=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a complete Canvas to a long form document, integrating all cards, notes, images and other media content into a single markdown file.'';
            homepage = "https://github.com/slnsys/obsidian-canvas2document";
        };
    }
);
    "canvasblocks" = (
    let
        baseUrl = "https://github.com/Kay607/obsidian-canvasblocks/releases/download/1.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "canvasblocks";
        version = "1.8.1";
        outputHash = "sha256-ySg3txbq9IdYEVWAKWG15gHc0xyQyJgehTmh4kHdNAQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute python scripts from canvas'';
            homepage = "https://github.com/Kay607/obsidian-canvasblocks";
        };
    }
);
    "cao" = (
    let
        baseUrl = "https://github.com/iamgodot/CAO/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cao";
        version = "1.6.1";
        outputHash = "sha256-ZiRY+JFkgMCkv74xvTIxEwSNq00+I2wKycnDtqyN7NY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate Claude AI for chatting in notes.'';
            homepage = "https://github.com/iamgodot/CAO";
        };
    }
);
    "capitaliser" = (
    let
        baseUrl = "https://github.com/eoliversec/obsidian-capitalise-plugin/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "capitaliser";
        version = "0.1.1";
        outputHash = "sha256-2ZqcLOmzpErksSsKzehLkU1VIXOA6H1+dAOlN3aviDU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Cycle text capitalisation (lowercase, Capitalise Each Word, UPPERCASE).'';
            homepage = "https://github.com/eoliversec/obsidian-capitalise-plugin";
        };
    }
);
    "card-board" = (
    let
        baseUrl = "https://github.com/roovo/obsidian-card-board/releases/download/0.7.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "card-board";
        version = "0.7.9";
        outputHash = "sha256-UPQWUmmB2PlPGiNbJWMOH8+syf77y1iPeMq6jc13es0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display Markdown tasks on kanban-style boards.'';
            homepage = "https://github.com/roovo/obsidian-card-board";
        };
    }
);
    "card-forge" = (
    let
        baseUrl = "https://github.com/carlsverre/obsidian-card-forge/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "card-forge";
        version = "1.0.2";
        outputHash = "sha256-9zPdQShNdVIQWZeHCrpyJQoT7ebEW5AGf+2PwXM9iQw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert notes into printable cards.'';
            homepage = "https://github.com/carlsverre/obsidian-card-forge";
        };
    }
);
    "card-note" = (
    let
        baseUrl = "https://github.com/cycsd/obsidian-card-note/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "card-note";
        version = "1.7.0";
        outputHash = "sha256-z0s6wuEXZHas55UurocTd61pSSwz9qA5GC1YRW+TTt4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly extract your thoughts in the Canvas and Excalidraw.'';
            homepage = "https://github.com/cycsd/obsidian-card-note";
        };
    }
);
    "card-viewer" = (
    let
        baseUrl = "https://github.com/vsme/obsidian-card-viewer/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "card-viewer";
        version = "1.0.12";
        outputHash = "sha256-rOi2HB1/US8O+9JpAxAUp8v31oK/zdjLE4FyQ4GlHqI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display cards for movies, TV shows, books, and music.'';
            homepage = "https://github.com/vsme/obsidian-card-viewer";
        };
    }
);
    "cardify" = (
    let
        baseUrl = "https://github.com/joshuakto/obsidian-cardify/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cardify";
        version = "1.0.4";
        outputHash = "sha256-5IKr++txIEFypZeLzOCJX/W9Y2ehaAppuEfNpl8xuWE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create links for blocks in a Markdown file and generate Markdown file for each link within a folder. Enabling drag-and-drop of cards onto canvas.'';
            homepage = "https://github.com/joshuakto/obsidian-cardify";
        };
    }
);
    "cards-view" = (
    let
        baseUrl = "https://github.com/jillro/obsidian-cards-view-plugin/releases/download/1.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cards-view";
        version = "1.10.0";
        outputHash = "sha256-qyxDeFDtN9cJq0G5m3+ROgekjk/DeBqcDBxZI1UOFQA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays a card view of your notes.'';
            homepage = "https://github.com/jillro/obsidian-cards-view-plugin";
        };
    }
);
    "caret" = (
    let
        baseUrl = "https://github.com/jcollingj/caret/releases/download/0.2.80";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "caret";
        version = "0.2.80";
        outputHash = "sha256-iIlbWkpUQZrDTqNyTN6qoCRWJNwWf5KlCxwaFSD9bFs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An AI workbench. Canvas, Chat and more LLM powered features.'';
            homepage = "https://github.com/jcollingj/caret";
        };
    }
);
    "cbcr-text-eater-de" = (
    let
        baseUrl = "https://github.com/clockblocker/filler-de/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cbcr-text-eater-de";
        version = "0.0.2";
        outputHash = "sha256-ZHVsDSH2+rcQptrOnJ5WBWUBSafvWAG2S2UTHEuCBvA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Building your a personal German dictionary, based on the contexts in the German texts, that you have read'';
            homepage = "https://github.com/clockblocker/filler-de";
        };
    }
);
    "change-case" = (
    let
        baseUrl = "https://github.com/dbrockman/obsidian-change-case/releases/download/2.0.139";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "change-case";
        version = "2.0.139";
        outputHash = "sha256-D2jNW3zq80Jrq0jAhztDyk6DVB2EwmmrnOEiT7dyppg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change the case (UPPER CASE, camelCase, snake_case, etc) of the current selection.'';
            homepage = "https://github.com/dbrockman/obsidian-change-case";
        };
    }
);
    "character-insertion" = (
    let
        baseUrl = "https://github.com/TakamiChie/Obsidian_CharacterInsertionPlugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "character-insertion";
        version = "1.0.0";
        outputHash = "sha256-QSrl/xlpMRXPt6VEsUtWWvEGEqSIt0/X62x0WvqkbsA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert a specified symbol under the cursor.'';
            homepage = "https://github.com/TakamiChie/Obsidian_CharacterInsertionPlugin";
        };
    }
);
    "character-sheets" = (
    let
        baseUrl = "https://github.com/Grayvox/obsidian-character-sheets/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "character-sheets";
        version = "1.1.0";
        outputHash = "sha256-F9kGL76KI6GhIAkyJIgduve53XGiIX2Gga5/IQ5S4e4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create character sheets for your very own traumatized little guys.'';
            homepage = "https://github.com/Grayvox/obsidian-character-sheets";
        };
    }
);
    "chat-cbt" = (
    let
        baseUrl = "https://github.com/clairefro/obsidian-chat-cbt-plugin/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chat-cbt";
        version = "3.0.0";
        outputHash = "sha256-qZ3ZlAxY3lH2x++0tK5lIkiva5vQt7KAUbmIgaTiarc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered journaling assistant for your notes, inspired by cognitive behavioral therapy (CBT).'';
            homepage = "https://github.com/clairefro/obsidian-chat-cbt-plugin";
        };
    }
);
    "chat-clips" = (
    let
        baseUrl = "https://github.com/sleepingraven/obsidian-chat-clips/releases/download/0.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chat-clips";
        version = "0.5.1";
        outputHash = "sha256-fnsAWivoaxZn8U4WwxYXawE+6txDN2c6WLKKw2LXDlY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Record chat in ordinary markdown list.'';
            homepage = "https://github.com/sleepingraven/obsidian-chat-clips";
        };
    }
);
    "chat-stream" = (
    let
        baseUrl = "https://github.com/rpggio/obsidian-chat-stream/releases/download/1.4.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chat-stream";
        version = "1.4.8";
        outputHash = "sha256-TXrnxrV09L4wdbutSFCqSJ1MeoIfrsIeXUJlWbizFPs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create branching GPT chats using canvas notes.'';
            homepage = "https://github.com/rpggio/obsidian-chat-stream";
        };
    }
);
    "chat-with-bard" = (
    let
        baseUrl = "https://github.com/Artel250/Obsidian-Gemini-Assistant/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chat-with-bard";
        version = "0.3.1";
        outputHash = "sha256-IrAl55/fbcNmzKcGwDhVMFMvyiIHdUc2xHhkrizKMYo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A Google Gemini integration, completely for free.'';
            homepage = "https://github.com/Artel250/Obsidian-Gemini-Assistant";
        };
    }
);
    "chatgpt-definitions" = (
    let
        baseUrl = "https://github.com/julix14/chatGPT-Obsidian/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chatgpt-definitions";
        version = "1.0.1";
        outputHash = "sha256-vufZql0BkX2bthH/qleStoiaQH3pIv6eBz9uM3jKXfY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Let your AI assistant ChatGPT define words and concepts for you.'';
            homepage = "https://github.com/julix14/chatGPT-Obsidian";
        };
    }
);
    "chatgpt-md" = (
    let
        baseUrl = "https://github.com/bramses/chatgpt-md/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chatgpt-md";
        version = "3.1.0";
        outputHash = "sha256-8/LbszXCHFs0hTCGeonbgQvjSrSzEE9kkhwNdY/ssDk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A seamless integration of ChatGPT, OpenRouter.ai and local LLMs via Ollama into your notes.'';
            homepage = "https://github.com/bramses/chatgpt-md";
        };
    }
);
    "chatgpt-prompt" = (
    let
        baseUrl = "https://github.com/Coduhuey/ChatGPT-Prompt-Plugin-For-Obsidian/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chatgpt-prompt";
        version = "1.0.9";
        outputHash = "sha256-RqsF8VWB51Amx072Gde3iCwIkg7bNyfiOsZ5yoiegq4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send templated prompts to ChatGPT when you open a file.'';
            homepage = "https://github.com/Coduhuey/ChatGPT-Prompt-Plugin-For-Obsidian";
        };
    }
);
    "chatty" = (
    let
        baseUrl = "https://github.com/SSaquif/obsidian-chatty/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chatty";
        version = "1.0.2";
        outputHash = "sha256-hi/g6tZfpF9Ca/cQ6x/I3EYaGDUJR1yRQOHbUWYAGBY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to listen to your notes using text-to-speech. Uses the browser"s built-in speech synthesis capabilities and your default system voices.'';
            homepage = "https://github.com/SSaquif/obsidian-chatty";
        };
    }
);
    "check-and-delete" = (
    let
        baseUrl = "https://github.com/Danitiate/check-and-delete/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "check-and-delete";
        version = "1.0.4";
        outputHash = "sha256-UQlAN1L+JLthrsJXu5F77e7beQELWFfXP3eNOEuYF3k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly clean up temporary list-items with the press of a button'';
            homepage = "https://github.com/Danitiate/check-and-delete";
        };
    }
);
    "checkbox-autochecker" = (
    let
        baseUrl = "https://github.com/klaasklee/checkbox-autochecker-obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-autochecker";
        version = "1.0.0";
        outputHash = "sha256-cmSEMvq54Cqa9ZMz9rLrM8G38yBedl7Q6hbY09LYWTQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes or plugins between vaults.'';
            homepage = "https://github.com/klaasklee/checkbox-autochecker-obsidian";
        };
    }
);
    "checkbox-reorder" = (
    let
        baseUrl = "https://github.com/Erl-koenig/obsidian-checkboxReorder/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-reorder";
        version = "2.0.2";
        outputHash = "sha256-I2uySxxCuBOkdCDmbz1FXvFfn4xZQFgZH4g1mmPjHmg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reorder completed checkboxes to the end of the according list.'';
            homepage = "https://github.com/Erl-koenig/obsidian-checkboxReorder";
        };
    }
);
    "checkbox-sort" = (
    let
        baseUrl = "https://github.com/mattang687/obsidian-checkbox-sort/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-sort";
        version = "0.1.3";
        outputHash = "sha256-YfScfYmjN+5ReHCpd4bTVUDiNJyQyMEOQeunLWjXY9s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically moves completed checkboxes to the end of the list'';
            homepage = "https://github.com/mattang687/obsidian-checkbox-sort";
        };
    }
);
    "checkbox-sounds" = (
    let
        baseUrl = "https://github.com/yasd251/checkbox-sounds-plugin/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-sounds";
        version = "1.0.6";
        outputHash = "sha256-2xKnyQ6pK6NYgoej29fREwwHiADMWGUEbuYnec+tHQU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a nice completion sound to checkboxes when ticked off'';
            homepage = "https://github.com/yasd251/checkbox-sounds-plugin";
        };
    }
);
    "checkbox-style-menu" = (
    let
        baseUrl = "https://github.com/ReticentEclectic/checkbox-style-menu/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-style-menu";
        version = "1.2.0";
        outputHash = "sha256-Q/Mwn5GdIR1n7I5LX2b++z0jimfsuko3HTMaO2KcBKc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides an intuitive menu for quickly changing checkbox styles.'';
            homepage = "https://github.com/ReticentEclectic/checkbox-style-menu";
        };
    }
);
    "checkbox-styling-helper" = (
    let
        baseUrl = "https://github.com/jaewonE/checkbox-styling-helper/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-styling-helper";
        version = "1.3.0";
        outputHash = "sha256-4ORLIr8McMVG++6GAULaX5JyIczoFpG5bWgC/v4BPO4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps you styling checkboxes in preview mode.'';
            homepage = "https://github.com/jaewonE/checkbox-styling-helper";
        };
    }
);
    "checkbox-sync" = (
    let
        baseUrl = "https://github.com/groldsf/obsidian_check_plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-sync";
        version = "1.2.0";
        outputHash = "sha256-tucW50l4c1IRmWliyuq8oPhLiynTVmRyxhvHP8ZHO7I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically checks the parent checkbox if all child checkboxes are completed, and unchecks it otherwise.'';
            homepage = "https://github.com/groldsf/obsidian_check_plugin";
        };
    }
);
    "checkbox-time-tracker" = (
    let
        baseUrl = "https://github.com/udus122/checkbox-time-tracker/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checkbox-time-tracker";
        version = "0.9.0";
        outputHash = "sha256-hFgxfPzzq3OMsCXXeF+a/2R+76cMyleCBlbujTM2iCk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert timestamp when you check off the checkbox'';
            homepage = "https://github.com/udus122/checkbox-time-tracker";
        };
    }
);
    "checklist-progress" = (
    let
        baseUrl = "https://github.com/acidghost/obsidian-checklist-progress/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "checklist-progress";
        version = "0.0.4";
        outputHash = "sha256-Imq83uLvB5ZzI359zzLMv+06Tp4fm58sBNaR23emV/0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically fill progress (as fraction or percentage) of check-lists.'';
            homepage = "https://github.com/acidghost/obsidian-checklist-progress";
        };
    }
);
    "chem" = (
    let
        baseUrl = "https://github.com/Acylation/obsidian-chem/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chem";
        version = "0.4.1";
        outputHash = "sha256-QGSI7tmSne1EJRTOjTwAS6DtY80jFJRSj/uiVCqn4Jc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Providing chemistry supports. Rendering SMILES strings into chemistry structures.'';
            homepage = "https://github.com/Acylation/obsidian-chem";
        };
    }
);
    "chemical-structure-renderer" = (
    let
        baseUrl = "https://github.com/xaya1001/obsidian-Chemical-Structure-Renderer/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chemical-structure-renderer";
        version = "1.2.0";
        outputHash = "sha256-u3POXq9mRiwJ+VXWB0OyG7X2a3BZ9LKhFR0E6tkkTUM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render chemical structures from SMILES strings into PNG or SVG format using Ketcher and Indigo Service.'';
            homepage = "https://github.com/xaya1001/obsidian-Chemical-Structure-Renderer";
        };
    }
);
    "chess-study" = (
    let
        baseUrl = "https://github.com/chrislicodes/obsidian-chess-study/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chess-study";
        version = "1.2.0";
        outputHash = "sha256-7xIIOFCEdK8h2T+BqLERI54A10Q21MwsiDesD9Lh7IU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A chess study helper and PGN viewer/editor.'';
            homepage = "https://github.com/chrislicodes/obsidian-chess-study";
        };
    }
);
    "chesser-obsidian" = (
    let
        baseUrl = "https://github.com/SilentVoid13/Chesser/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chesser-obsidian";
        version = "0.2.1";
        outputHash = "sha256-UMaziEidPc45gBsyygZ+dTmUII5MeSZQBKXxMon5s9s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A chess game viewer/editor.'';
            homepage = "https://github.com/SilentVoid13/Chesser";
        };
    }
);
    "chinese-calendar" = (
    let
        baseUrl = "https://github.com/DevilRoshan/obsidian-lunar-calendar/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chinese-calendar";
        version = "1.0.13";
        outputHash = "sha256-9S2Ifs/FwZvCJb/Hko76F9yUhCPlCczD9J64XAN6Ql4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''符合中国习惯的日历，可以显示农历、节日、调休、节气等信息，支持月视图和年视图切换，支持点击日期创建笔记，支持使用QuickAdd插件创建笔记。'';
            homepage = "https://github.com/DevilRoshan/obsidian-lunar-calendar";
        };
    }
);
    "chord-lyrics" = (
    let
        baseUrl = "https://github.com/nevernotmove/obsidian-chordlyrics/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chord-lyrics";
        version = "0.3.0";
        outputHash = "sha256-Qp8BrlyvlKYEODshiESP50Z88lmdcX5OT//asgIUznI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display chord names over lyrics. Supports line wrapping, section headers and auto-detection.'';
            homepage = "https://github.com/nevernotmove/obsidian-chordlyrics";
        };
    }
);
    "chord-sheets" = (
    let
        baseUrl = "https://github.com/olvidalo/obsidian-chord-sheets/releases/download/1.6.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chord-sheets";
        version = "1.6.6";
        outputHash = "sha256-TiPkdmco93coS6JKXAE4wOoLAlq57v07hIhDDPXxDzI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Work with chord sheets (chords over lyrics or inline) in Live Preview and reading mode: Chord diagrams for guitar, ukulele and mandolin, transpose, autoscroll, and more.'';
            homepage = "https://github.com/olvidalo/obsidian-chord-sheets";
        };
    }
);
    "christmas" = (
    let
        baseUrl = "https://github.com/Matse2005/Obsidian-Christmas/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "christmas";
        version = "1.0.1";
        outputHash = "sha256-MGhG9qIj8M2V6/ysigfj/gHL/IY4Srgfl2JTxXrOiSI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track the time left till Christmas.'';
            homepage = "https://github.com/Matse2005/Obsidian-Christmas";
        };
    }
);
    "chronology" = (
    let
        baseUrl = "https://github.com/Canna71/obsidian-chronology/releases/download/1.1.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chronology";
        version = "1.1.14";
        outputHash = "sha256-IuyDpqDb3q69sUO6GjmX65Lla6sPvSXo5J+wRXNUt9s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A calendar and a timeline of the note"s creation and modification.'';
            homepage = "https://github.com/Canna71/obsidian-chronology";
        };
    }
);
    "chronos" = (
    let
        baseUrl = "https://github.com/clairefro/obsidian-plugin-chronos/releases/download/3.6.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chronos";
        version = "3.6.3";
        outputHash = "sha256-rBU6DIzjWrGUsiJWIRiXC93V03SDor4j8MTJ28kYIWc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render interactive timelines inline in your notes from simple markdown'';
            homepage = "https://github.com/clairefro/obsidian-plugin-chronos";
        };
    }
);
    "chronotyper" = (
    let
        baseUrl = "https://github.com/BambusControl/obsidian-chronotyper/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "chronotyper";
        version = "2.1.0";
        outputHash = "sha256-NS74aES7oLsPVliBEnJO4AOMYhEq2DY9DTgRkYSMM0Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track how long you edit notes directly in the note properties'';
            homepage = "https://github.com/BambusControl/obsidian-chronotyper";
        };
    }
);
    "cicada-sync" = (
    let
        baseUrl = "https://github.com/adapole/cicada-sync/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cicada-sync";
        version = "1.0.1";
        outputHash = "sha256-iYd0/8JEyEmhpfVRA1XD8VUUpg7UhAQhLRtEE1RMIoE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''uses Git to synchronize vaults for team collaboration.'';
            homepage = "https://github.com/adapole/cicada-sync";
        };
    }
);
    "circuit-sketcher" = (
    let
        baseUrl = "https://github.com/code-forge-temple/circuit-sketcher-obsidian-plugin/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "circuit-sketcher";
        version = "1.3.2";
        outputHash = "sha256-2DLveviY9INLeZuO3EiZRigcVn58HFnh/ECjx8vw/qk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Draw circuits on a canvas using circuit-sketcher-core.'';
            homepage = "https://github.com/code-forge-temple/circuit-sketcher-obsidian-plugin";
        };
    }
);
    "citation-callouts" = (
    let
        baseUrl = "https://github.com/Marvive/Citation-Callouts/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "citation-callouts";
        version = "1.0.0";
        outputHash = "sha256-F2Hrm4+Mhwvek41u0n1lSJur4tnlZxZYcZmG56V9X24=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Elegantly formats quotes with citations from various sources into custom callouts.'';
            homepage = "https://github.com/Marvive/Citation-Callouts";
        };
    }
);
    "cjk-count" = (
    let
        baseUrl = "https://github.com/vrabe/obsidian-cjk-count/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cjk-count";
        version = "1.1.2";
        outputHash = "sha256-I5/E8diF1gW+ZR5P0PsHmiPdnhrFmqsz/J0X4pJHZHU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A word counter that only counts Chinese, Japanese and Korean (CJK) characters.'';
            homepage = "https://github.com/vrabe/obsidian-cjk-count";
        };
    }
);
    "class-relation-visualization" = (
    let
        baseUrl = "https://github.com/Cold-dragon7/Obsidian-Class-Relation-Visualization/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "class-relation-visualization";
        version = "0.5.0";
        outputHash = "sha256-wJJkrX0U9gLVhoyS6WC1NQeXOC5DYlm+PRt30A9X+a8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''You can visualize your classes and their inheritance, composition, aggregation relation.'';
            homepage = "https://github.com/Cold-dragon7/Obsidian-Class-Relation-Visualization";
        };
    }
);
    "clear-todos" = (
    let
        baseUrl = "https://github.com/gutentag2012/obsidian-plugin-clear-todos/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "clear-todos";
        version = "1.0.3";
        outputHash = "sha256-MEsK4/qQmecpeJFQYPsIYl2kod7qTPg/gvz6rK66y70=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly remove all completed todos from your notes or selected text.'';
            homepage = "https://github.com/gutentag2012/obsidian-plugin-clear-todos";
        };
    }
);
    "click-clack" = (
    let
        baseUrl = "https://github.com/Acylation/obsidian-click-clack/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "click-clack";
        version = "0.1.2";
        outputHash = "sha256-4DJbdNisUAFlappLJzOXFjClSg2d7Zy9C/GNt30WlFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simulates typewriter / mechanical keyboard sounds.'';
            homepage = "https://github.com/Acylation/obsidian-click-clack";
        };
    }
);
    "click-hint" = (
    let
        baseUrl = "https://github.com/kbwo/obsidian-click-hint/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "click-hint";
        version = "1.0.2";
        outputHash = "sha256-PgOaUECxm+e0voO3oXFHAQQzttnE2iRMsb/Wneo9qiU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provide keyboard-driven hints for clickable elements.'';
            homepage = "https://github.com/kbwo/obsidian-click-hint";
        };
    }
);
    "click-up-sync" = (
    let
        baseUrl = "https://github.com/hokim-m/click-up-x-obsidian/releases/download/0.6.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "click-up-sync";
        version = "0.6.4";
        outputHash = "sha256-fiEOpLSsfbfi9cMSRpIIyzJ+sCVW2h8anAA7+aA9P8A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage ClickUp space from notes.'';
            homepage = "https://github.com/hokim-m/click-up-x-obsidian";
        };
    }
);
    "clipboard-manager" = (
    let
        baseUrl = "https://github.com/ayu5h-raj/clipboard-manager/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "clipboard-manager";
        version = "1.0.0";
        outputHash = "sha256-Cbt4CmfxZt6d9l/dWWMYhDo/f6qUWt+irG3xEVMFdQU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A clipboard manager that stores clipboard history and provides search functionality with real-time updates'';
            homepage = "https://github.com/ayu5h-raj/clipboard-manager";
        };
    }
);
    "cliplet" = (
    let
        baseUrl = "https://github.com/namikaze-40p/obsidian-cliplet/releases/download/0.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cliplet";
        version = "0.3.4";
        outputHash = "sha256-eKiJTQXWW3VM3D3XB/FToZAfU+0+vUYCdj/JgaLqDuQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A clipboard and snippet manager — your own, separate from the OS clipboard.'';
            homepage = "https://github.com/namikaze-40p/obsidian-cliplet";
        };
    }
);
    "clipper-catalog" = (
    let
        baseUrl = "https://github.com/soundslikeinfo/obsidian-clipper-catalog/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "clipper-catalog";
        version = "1.3.1";
        outputHash = "sha256-GkxSILJJh0oEeMgioi9yK8icmmUAYGMKTV7hijh0thA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A catalog view of all the clippings gathered with a common source property.'';
            homepage = "https://github.com/soundslikeinfo/obsidian-clipper-catalog";
        };
    }
);
    "clippermaster" = (
    let
        baseUrl = "https://github.com/aqeja/ClipperMaster-for-Obsidian/releases/download/0.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "clippermaster";
        version = "0.1.10";
        outputHash = "sha256-6QsM7hNUi7bgXu6yYLQyYw+vWpBnufBu0UnniIu091E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Works with the ClipperMaster Chrome extension to clip structured content from the web and save it to your vault as Markdown files.'';
            homepage = "https://github.com/aqeja/ClipperMaster-for-Obsidian";
        };
    }
);
    "clojure-plugin-host" = (
    let
        baseUrl = "https://github.com/farcaller/obsidian-clojure-plugin-host/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "clojure-plugin-host";
        version = "0.1.0";
        outputHash = "sha256-SYEMOHvpuzN160tL7v2qj0vfarPokuTgrsiq0aUx+Z8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A Clojure plugin host, allowing the creation of simple Clojure-based plugins right inside the editor.'';
            homepage = "https://github.com/farcaller/obsidian-clojure-plugin-host";
        };
    }
);
    "clone-vault" = (
    let
        baseUrl = "https://github.com/laantorchaweb/clone-vault/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "clone-vault";
        version = "1.0.3";
        outputHash = "sha256-BBJ/jGL7Nm/LAG0dDh112hSg08E1wg7OTSML2c3yoJI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Clones the current vault by copying settings and folder structure without note contents.'';
            homepage = "https://github.com/laantorchaweb/clone-vault";
        };
    }
);
    "close-window-when-empty" = (
    let
        baseUrl = "https://github.com/TaylorJadin/close-window-when-empty/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "close-window-when-empty";
        version = "1.0.1";
        outputHash = "sha256-WZ20ttiDK86VI/Insylt4IrtQ5ScDGKmMk3n3Nah50s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Close the window when the last note is closed, kind of how browsers work.'';
            homepage = "https://github.com/TaylorJadin/close-window-when-empty";
        };
    }
);
    "cloud-atlas" = (
    let
        baseUrl = "https://github.com/open-horizon-labs/obsidian-client/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cloud-atlas";
        version = "1.2.0";
        outputHash = "sha256-TxP/zt5MckVauhYnYKoU68tOAVvVSCxVSCW+gAXdL+Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''The most effective way to use LLMs in your vault: Add your current note, reference backlinks/forward links, and a Canvas mode to assemble a prompt with all the context you.'';
            homepage = "https://github.com/open-horizon-labs/obsidian-client";
        };
    }
);
    "cloud-storage" = (
    let
        baseUrl = "https://github.com/yingjialong/obsidian-CloudStorage/releases/download/1.6.34";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cloud-storage";
        version = "1.6.34";
        outputHash = "sha256-NxhFj2kxULRvaU0eR1RZTz4o5rTOacW5IejJG/+BtRk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows users to upload local files to the cloud, reducing the burden on local vaults and enabling seamless synchronization across multiple devices.'';
            homepage = "https://github.com/yingjialong/obsidian-CloudStorage";
        };
    }
);
    "cloud-sync" = (
    let
        baseUrl = "https://github.com/ai-bytedance/obsidian-cloud-sync/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cloud-sync";
        version = "1.0.2";
        outputHash = "sha256-D8+/F0F8SOlrF+pcI21KA4NfQN/a7EzMzoT5TpONTwo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''将笔记同步到多种云盘服务，提供端到端加密保护。'';
            homepage = "https://github.com/ai-bytedance/obsidian-cloud-sync";
        };
    }
);
    "cloudatlas-o-am" = (
    let
        baseUrl = "https://github.com/open-horizon-labs/obsidian-am/releases/download/0.9.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cloudatlas-o-am";
        version = "0.9.9";
        outputHash = "sha256-IZb+W9qoCinUimNhLyBsu1NDFYHh7PpXC6EzGnDZQt0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integration with Amazing Marvin (unofficial). Supports exporting AM tasks/projects, creating new tasks and completing tasks.'';
            homepage = "https://github.com/open-horizon-labs/obsidian-am";
        };
    }
);
    "cloudinary" = (
    let
        baseUrl = "https://github.com/uday-samsani/obsidian-cloudinary/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cloudinary";
        version = "1.1.0";
        outputHash = "sha256-/BWgStBC2ctjisj5eLLIu1oo8ZOVz+hqzlUipVUG3Rc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload content (images, videos, audio) to Cloudinary and insert (copy or drag both) them into your notes.'';
            homepage = "https://github.com/uday-samsani/obsidian-cloudinary";
        };
    }
);
    "cloze" = (
    let
        baseUrl = "https://github.com/DearVikki/obsidian-cloze-plugin/releases/download/0.1.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cloze";
        version = "0.1.18";
        outputHash = "sha256-9v5URA+pFkL7nU7qXx2hQI9U7IwAnWLgyQVZBDeqSa4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert highlights, underlines, bolded texts or any selected texts into clozes.'';
            homepage = "https://github.com/DearVikki/obsidian-cloze-plugin";
        };
    }
);
    "cluster" = (
    let
        baseUrl = "https://github.com/lorens-osman-dev/cluster/releases/download/4.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cluster";
        version = "4.0.0";
        outputHash = "sha256-1gEzexKPR3fkJNF0ZxnakaI+6vUw4ybZZ27kYneS0s4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make the notes clustering simpler on mobile devices and work well on PCs ether, Notes Clustering is the process of creating hierarchical notes structures.'';
            homepage = "https://github.com/lorens-osman-dev/cluster";
        };
    }
);
    "cm-chs-patch" = (
    let
        baseUrl = "https://github.com/aidenlx/cm-chs-patch/releases/download/1.12.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cm-chs-patch";
        version = "1.12.0";
        outputHash = "sha256-iZ39vSvD27i+vjiysC1heSBf7N0KA+px4/+oALIRSp8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A patch for Obsidian"s built-in CodeMirror editor to support Simplified Chinese word splitting.'';
            homepage = "https://github.com/aidenlx/cm-chs-patch";
        };
    }
);
    "cm-typewriter-scroll-obsidian" = (
    let
        baseUrl = "https://github.com/deathau/cm-typewriter-scroll-obsidian/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cm-typewriter-scroll-obsidian";
        version = "0.2.2";
        outputHash = "sha256-bS+i7o+grD+XoabQAGsXX+SBt8PQTRxP4tGvJSHZA2k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Typewriter-style scrolling which keeps the view centered in the editor.'';
            homepage = "https://github.com/deathau/cm-typewriter-scroll-obsidian";
        };
    }
);
    "cmd-search" = (
    let
        baseUrl = "https://github.com/SpaceshipCaptain/CmdSearch/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cmd-search";
        version = "1.0.0";
        outputHash = "sha256-JLdSEYBaKsyaIhKjgoriahYODEKZDKZCkGaRY9CXAmw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use the command pallete to search web. Add your own URLs.'';
            homepage = "https://github.com/SpaceshipCaptain/CmdSearch";
        };
    }
);
    "cmdr" = (
    let
        baseUrl = "https://github.com/jsmorabito/obsidian-commander/releases/download/0.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cmdr";
        version = "0.5.5";
        outputHash = "sha256-GlQzM7N8WHUR//w7POJWb5ENa+2D1PRtpcee7LcMHjk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize your workspace by adding commands everywhere, create macros and supercharge your mobile toolbar.'';
            homepage = "https://github.com/jsmorabito/obsidian-commander";
        };
    }
);
    "cmenu-plugin" = (
    let
        baseUrl = "https://github.com/chetachiezikeuzor/cMenu-Plugin/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cmenu-plugin";
        version = "1.1.2";
        outputHash = "sha256-NoAY7oZTPGOW48Ts/nU9LV/ZVDI7L1zRSbo6yUoWBTQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a minimal text formatting toolbar for a smoother writing/editing experience.'';
            homepage = "https://github.com/chetachiezikeuzor/cMenu-Plugin";
        };
    }
);
    "coalesce" = (
    let
        baseUrl = "https://github.com/bfloydd/coalesce/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "coalesce";
        version = "2.0.0";
        outputHash = "sha256-9L4IJkLFeSJE1N6c/jnNAa+45qtfhs3xN+33iEdWGz0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Coalesce your backlinked notes into a single view'';
            homepage = "https://github.com/bfloydd/coalesce";
        };
    }
);
    "coco-askai" = (
    let
        baseUrl = "https://github.com/yamfeel/coco-askai/releases/download/1.1.24";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "coco-askai";
        version = "1.1.24";
        outputHash = "sha256-et8eDvciX9lKRMCfWI4Nv7hzi5xDmh8l8qZIaqcVcV4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Let your questions flow swiftly with CoCo AskAI. (Closed source)'';
            homepage = "https://github.com/yamfeel/coco-askai";
        };
    }
);
    "code-block-from-selection" = (
    let
        baseUrl = "https://github.com/dy-sh/obsidian-code-block-from-selection/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-block-from-selection";
        version = "1.0.8";
        outputHash = "sha256-V5rPVNnwZDGfnm2bKk8F21EVTJeVYoJl5eT7YuWEtyk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn the selected text into a code block.'';
            homepage = "https://github.com/dy-sh/obsidian-code-block-from-selection";
        };
    }
);
    "code-block-plugin" = (
    let
        baseUrl = "https://github.com/paddan/code-block-plugin/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-block-plugin";
        version = "1.0.4";
        outputHash = "sha256-0TNXoVJsDpuEPnKDm32C8dpymsN3x1ym6CLn0yDFxrI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert text into code blocks with automatic language detection.'';
            homepage = "https://github.com/paddan/code-block-plugin";
        };
    }
);
    "code-blocks-commands" = (
    let
        baseUrl = "https://github.com/dragonish/code-blocks-commands/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-blocks-commands";
        version = "1.1.0";
        outputHash = "sha256-BTygZK0fzTLIoyqU+NXy8oCKzq/ILNWDwZFjFBiooVU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provide commands to insert code blocks with markup, and support triggering commands with backticks.'';
            homepage = "https://github.com/dragonish/code-blocks-commands";
        };
    }
);
    "code-emitter" = (
    let
        baseUrl = "https://github.com/mokeyish/obsidian-code-emitter/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-emitter";
        version = "0.4.0";
        outputHash = "sha256-dubmW5KUyMi5DnLQi52ptMaOhu/hUaojUZg6Lr36qlM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allow code blocks to be executed interactively in a sandbox like Jupyter notebooks. Supported language Rust, Kotlin, Python, JavaScript, TypeScript, etc.'';
            homepage = "https://github.com/mokeyish/obsidian-code-emitter";
        };
    }
);
    "code-files" = (
    let
        baseUrl = "https://github.com/lukasbach/obsidian-code-files/releases/download/1.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-files";
        version = "1.1.9";
        outputHash = "sha256-zE8FKB9DOUYDFIQdAtuWDC5gDU2C3Yo4Ba0NZmYPAXI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit code files with VS Code"s powerful Monaco Editor.'';
            homepage = "https://github.com/lukasbach/obsidian-code-files";
        };
    }
);
    "code-language-completer" = (
    let
        baseUrl = "https://github.com/stanley-910/obsidian-code-language-completer/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-language-completer";
        version = "1.1.1";
        outputHash = "sha256-zXoAplh+W/srpU/yXPFBSKmEy4cNuGR/JI2mc7pmOYM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Autosuggests and completes codeblock language options based on history.'';
            homepage = "https://github.com/stanley-910/obsidian-code-language-completer";
        };
    }
);
    "code-link" = (
    let
        baseUrl = "https://github.com/observerw/obsidian-code-link/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-link";
        version = "1.0.4";
        outputHash = "sha256-fd2KxqJJIl0nauKBbIgQy5wM3K2JkMU3xYFrLR0aaV4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link to code files in your notes'';
            homepage = "https://github.com/observerw/obsidian-code-link";
        };
    }
);
    "code-styler" = (
    let
        baseUrl = "https://github.com/mayurankv/Obsidian-Code-Styler/releases/download/1.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "code-styler";
        version = "1.1.7";
        outputHash = "sha256-/zzhXcC9ul5vRYN6/9sIBA+VfXPqSkLuba+4rKNGE/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Style codeblocks and inline code in reading view and editing view.'';
            homepage = "https://github.com/mayurankv/Obsidian-Code-Styler";
        };
    }
);
    "codeblock-customizer" = (
    let
        baseUrl = "https://github.com/mugiwara85/CodeblockCustomizer/releases/download/1.4.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "codeblock-customizer";
        version = "1.4.7";
        outputHash = "sha256-d4I2dZcJSPQIWVYRqCfO9KNfx3bp/ZjqRFZkcYm4zEo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize your code blocks in editing, and reading mode as well.'';
            homepage = "https://github.com/mugiwara85/CodeblockCustomizer";
        };
    }
);
    "codeblock-tabs" = (
    let
        baseUrl = "https://github.com/JeminMau/Obsidian-CodeBlock-Tabs/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "codeblock-tabs";
        version = "1.0.0";
        outputHash = "sha256-VNhQYBQMOGWqJ5WNAAyc7hkQFXMqv72RrPiVlQpvFpY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create tab group for contiguous code blocks.'';
            homepage = "https://github.com/JeminMau/Obsidian-CodeBlock-Tabs";
        };
    }
);
    "codeblock-template" = (
    let
        baseUrl = "https://github.com/sylcool/obsidian-codeblock-template/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "codeblock-template";
        version = "1.3.1";
        outputHash = "sha256-t7BHVTDE6tv/mmGnhu+pucDxsI/in4r55keraR2JVRM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reuse content within code blocks, with the ability to use variables.'';
            homepage = "https://github.com/sylcool/obsidian-codeblock-template";
        };
    }
);
    "codeless-heatmap-calendar" = (
    let
        baseUrl = "https://github.com/webtechaccess/obsidian-heatmap-calendar/releases/download/0.1.35";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "codeless-heatmap-calendar";
        version = "0.1.35";
        outputHash = "sha256-c+yO8DxoIoEp4Z+pv3KyugrUbfOcVvF9UnChnIRn07Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fully-featured activity visualizer powered by Toggl data and more.'';
            homepage = "https://github.com/webtechaccess/obsidian-heatmap-calendar";
        };
    }
);
    "codename" = (
    let
        baseUrl = "https://github.com/dstack/obsidian-codename/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "codename";
        version = "1.0.0";
        outputHash = "sha256-GZv+aAb3HxYvM+gRpMmL8UcX4eCx3Pz8ysWF2hb/hqI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Solve the hardest problem: naming things.'';
            homepage = "https://github.com/dstack/obsidian-codename";
        };
    }
);
    "coder" = (
    let
        baseUrl = "https://github.com/rudimuc/obsidian-coder/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "coder";
        version = "1.3.1";
        outputHash = "sha256-dIgaC+OrHRMv9CWv8amV5JHJK29oPlMNJzcM7ycgQd4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Encoding and decoding text using Base64, ROT13 and Atbash ciphers.'';
            homepage = "https://github.com/rudimuc/obsidian-coder";
        };
    }
);
    "codestats" = (
    let
        baseUrl = "https://github.com/Miskamyasa/obsidian-codestats/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "codestats";
        version = "1.0.3";
        outputHash = "sha256-NQ47BvyrIdyiH/WK/AomLdlpu0xrnXfCgTlgWkTEHao=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track your coding progress and earn XP for writing Markdown.'';
            homepage = "https://github.com/Miskamyasa/obsidian-codestats";
        };
    }
);
    "collapse-linked-mentions" = (
    let
        baseUrl = "https://github.com/n810K/obsidian-collapse-linked-mentions/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "collapse-linked-mentions";
        version = "1.1.1";
        outputHash = "sha256-EQAR5q4ndBNjTFnDe535gkhcuVJCgtKtGUJExPZltaY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically collapse embedded backlink mentions'';
            homepage = "https://github.com/n810K/obsidian-collapse-linked-mentions";
        };
    }
);
    "collapse-node" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Collapse-Node/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "collapse-node";
        version = "2.0.0";
        outputHash = "sha256-8IWn/Y5VkwMTUsvvOU4NCf5HyMNiG/5cmfZ8algveHA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collapse node in canvas.'';
            homepage = "https://github.com/Quorafind/Obsidian-Collapse-Node";
        };
    }
);
    "collapsible-code-blocks" = (
    let
        baseUrl = "https://github.com/bwya77/collapsible-code-blocks/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "collapsible-code-blocks";
        version = "1.0.6";
        outputHash = "sha256-YeJyJBlJH17AJaK7sjJUxbbmXH1j3YdRQajhR9QZW8Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makes code blocks collapsible in preview mode as well as enabling scroll-able code blocks.'';
            homepage = "https://github.com/bwya77/collapsible-code-blocks";
        };
    }
);
    "color-cycler" = (
    let
        baseUrl = "https://github.com/tjbrennan/obsidian-color-cycler/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "color-cycler";
        version = "1.1.1";
        outputHash = "sha256-ClFaOhI3RE39OPmIro3Yi3zO2ILxWd+pYxg2uxLawDI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamically change the accent color of the theme.'';
            homepage = "https://github.com/tjbrennan/obsidian-color-cycler";
        };
    }
);
    "color-folders-files" = (
    let
        baseUrl = "https://github.com/Mithadon/obsidian-color-folders-files/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "color-folders-files";
        version = "1.4.1";
        outputHash = "sha256-vmiDk6Y7C3w7wn2omX1szXQzHNBa0x3tvAdkc9Pmxww=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize the appearance of folders and files in the file explorer.'';
            homepage = "https://github.com/Mithadon/obsidian-color-folders-files";
        };
    }
);
    "color-palette" = (
    let
        baseUrl = "https://github.com/ALegendsTale/obsidian-color-palette/releases/download/2.15.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "color-palette";
        version = "2.15.0";
        outputHash = "sha256-6j/EgMNSaKS3fPmd5Yf+yLCD4bwqaRaDyRVg9PNqCpI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and insert color palettes into your notes.'';
            homepage = "https://github.com/ALegendsTale/obsidian-color-palette";
        };
    }
);
    "colored-tags" = (
    let
        baseUrl = "https://github.com/pfrankov/obsidian-colored-tags/releases/download/6.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "colored-tags";
        version = "6.1.2";
        outputHash = "sha256-bn1j0GtlXzNnnZZ5dRdLOYSfl+yqu885BdsqDowquwQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Colorize tags in different colors to visually distinguish them from each other. Colors of nested tags are mixed with parent tags. Text color contrast is automatically matched to comply with AA level WCAG 2.1.'';
            homepage = "https://github.com/pfrankov/obsidian-colored-tags";
        };
    }
);
    "colored-tags-wrangler" = (
    let
        baseUrl = "https://github.com/code-of-chaos/obsidian-colored_tags_wrangler/releases/download/1.0.0-BETA-002";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "colored-tags-wrangler";
        version = "1.0.0-BETA-002";
        outputHash = "sha256-dJcKhKPb7MeCIv/5ohQ3DalTuTmuieuiOSoQruaQZ3g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Assign colors to tags. Has integrations with other plugins, like Kanban.'';
            homepage = "https://github.com/code-of-chaos/obsidian-colored_tags_wrangler";
        };
    }
);
    "colored-text" = (
    let
        baseUrl = "https://github.com/erincayaz/obsidian-colored-text/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "colored-text";
        version = "1.3.0";
        outputHash = "sha256-HPIGLyft8qkSkIPSF/KgIZzcrG3pBBmAW6wVIr683tg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Color the selected texts.'';
            homepage = "https://github.com/erincayaz/obsidian-colored-text";
        };
    }
);
    "colorful-note-background" = (
    let
        baseUrl = "https://github.com/andresgongora/obsidian-colorful-note-background/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "colorful-note-background";
        version = "1.0.6";
        outputHash = "sha256-JjZMpQrlNZAirGnKNouY+w5GGbHfTkbSJOqeeurQziU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set note background based on file location or frontmatter metadata.'';
            homepage = "https://github.com/andresgongora/obsidian-colorful-note-background";
        };
    }
);
    "colorful-note-borders" = (
    let
        baseUrl = "https://github.com/rusi/obsidian-colorful-note-borders/releases/download/0.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "colorful-note-borders";
        version = "0.2.4";
        outputHash = "sha256-YV7OoZLAckDFe+DlEGje5diAWumSL76zAiK/9tVPs6E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add customizable colorful borders to notes based on folder location or frontmatter metadata, enhancing visual organization.'';
            homepage = "https://github.com/rusi/obsidian-colorful-note-borders";
        };
    }
);
    "colorizelt" = (
    let
        baseUrl = "https://github.com/WiNE-iNEFF/colorizelt/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "colorizelt";
        version = "0.0.7";
        outputHash = "sha256-yOKMWTTzg6PiqfmCsTOQDxolg2gukWzY64ta9Wy9sjU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easy color and clear selected text'';
            homepage = "https://github.com/WiNE-iNEFF/colorizelt";
        };
    }
);
    "combo-colors" = (
    let
        baseUrl = "https://github.com/kevinkickback/Combo-Colors/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "combo-colors";
        version = "1.2.1";
        outputHash = "sha256-mPKM3wI+brKcKEx/78ZNjgOl99fKkHMzuo1HOhmNS9Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically apply color to fighting game combo notations.'';
            homepage = "https://github.com/kevinkickback/Combo-Colors";
        };
    }
);
    "come-down" = (
    let
        baseUrl = "https://github.com/mntno/obsidian-come-down/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "come-down";
        version = "1.1.0";
        outputHash = "sha256-Y1C/yFXdQ0p/N4be5vQO3B72BJV2MK3G42izpjdirQE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Maintains a cache of your notes’ embedded external images.'';
            homepage = "https://github.com/mntno/obsidian-come-down";
        };
    }
);
    "come-through" = (
    let
        baseUrl = "https://github.com/mntno/obsidian-come-through/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "come-through";
        version = "0.6.0";
        outputHash = "sha256-/SJisFLrBALaiymIjFO4XghRVXqf5ffWMBiHJPzdqEY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Drill flashcards using spaced repetition.'';
            homepage = "https://github.com/mntno/obsidian-come-through";
        };
    }
);
    "command-block-list" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-command-block-list/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "command-block-list";
        version = "0.1.4";
        outputHash = "sha256-ImgPxr9nmPCPQwFKgY/Bil6WUX1xQ3nxPhOGPT1rGQk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide unwanted commands from the command palette.'';
            homepage = "https://github.com/RyotaUshio/obsidian-command-block-list";
        };
    }
);
    "command-line" = (
    let
        baseUrl = "https://github.com/sstallion/obsidian-command-line/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "command-line";
        version = "1.2.0";
        outputHash = "sha256-SV5e6XpJpz2Tnrdx4ATrrRj/L9AKG+JGTxGVHuE9ztU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy command lines from your notes to the clipboard.'';
            homepage = "https://github.com/sstallion/obsidian-command-line";
        };
    }
);
    "command-tracker" = (
    let
        baseUrl = "https://github.com/namikaze-40p/obsidian-command-tracker/releases/download/0.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "command-tracker";
        version = "0.5.3";
        outputHash = "sha256-lWqN3yXMc19DnB84LrSvD5LOmxPgMXoIbbWa8fZCisk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track the number of times the command is used.'';
            homepage = "https://github.com/namikaze-40p/obsidian-command-tracker";
        };
    }
);
    "commando-command-repeater" = (
    let
        baseUrl = "https://github.com/qaptoR/Commando/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "commando-command-repeater";
        version = "1.0.0";
        outputHash = "sha256-HFSKTEP/IU0jKA+NpvbGE65T7z50v5jb9LHXj6AJDrY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable repeated calls of a command using defined or per-invocation values.'';
            homepage = "https://github.com/qaptoR/Commando";
        };
    }
);
    "comments" = (
    let
        baseUrl = "https://github.com/JasperSurmont/obsidian-comments/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "comments";
        version = "1.0.3";
        outputHash = "sha256-1q1ZbtADhp1zp3oh9NqAkKFgjVbPzfpCGDiqsjtKc7w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add comments to your notes to facilitate collaberation'';
            homepage = "https://github.com/JasperSurmont/obsidian-comments";
        };
    }
);
    "companion" = (
    let
        baseUrl = "https://github.com/rizerphe/obsidian-companion/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "companion";
        version = "1.8.0";
        outputHash = "sha256-hByyk9CWWEIZq+VFdZhJ3KtmOqxjiPU0u8ZNmDjicl4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Autocomplete with AI, including ChatGPT, through a copilot-like interface.'';
            homepage = "https://github.com/rizerphe/obsidian-companion";
        };
    }
);
    "completed-area" = (
    let
        baseUrl = "https://github.com/DahaWong/obsidian-completed-area/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "completed-area";
        version = "0.2.0";
        outputHash = "sha256-TwEQ8XpTmQs0EgJc2xY9ddod8lu0HtrGbk5PW9cdC6Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move completed to-do items to a separate completed area.'';
            homepage = "https://github.com/DahaWong/obsidian-completed-area";
        };
    }
);
    "completed-task-display" = (
    let
        baseUrl = "https://github.com/heliostatic/completed-task-display/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "completed-task-display";
        version = "1.0.11";
        outputHash = "sha256-YTmHy5L09fqT+j1ygWpFVCuxUyMeN+gsn4bcRAamxK8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Controls for displaying or hiding completed tasks.'';
            homepage = "https://github.com/heliostatic/completed-task-display";
        };
    }
);
    "completed-tasks" = (
    let
        baseUrl = "https://github.com/mgussekloo/obsidian-completedtasks/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "completed-tasks";
        version = "1.4.0";
        outputHash = "sha256-Wz4odE8QtBNlP2fH+iB1DRsIyP1c9/U8K2WOvCrf224=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically sort completed tasks to the bottom of the list.'';
            homepage = "https://github.com/mgussekloo/obsidian-completedtasks";
        };
    }
);
    "compress" = (
    let
        baseUrl = "https://github.com/networkmastered/obsidian-compress/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "compress";
        version = "1.2.5";
        outputHash = "sha256-YcF+/zd1CInuNZGPWcPFea8Y54jT39ds3tTD5Q7BTWQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make your files smaller with compression!'';
            homepage = "https://github.com/networkmastered/obsidian-compress";
        };
    }
);
    "conditional-properties" = (
    let
        baseUrl = "https://github.com/diegoeis/obsidian-conditional-properties/releases/download/0.16.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "conditional-properties";
        version = "0.16.2";
        outputHash = "sha256-8GL2WCgFlE7ScsyF87qEV4Hwr31uBmM6O9uGgkRmlbU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automate frontmatter updates using conditional rules (IF/THEN).'';
            homepage = "https://github.com/diegoeis/obsidian-conditional-properties";
        };
    }
);
    "confluence-converter" = (
    let
        baseUrl = "https://github.com/addozhang/obsidian-confluence-converter/releases/download/0.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "confluence-converter";
        version = "0.5.5";
        outputHash = "sha256-C/4MBBh7lgP43aakREwCmU4RRUd3de2GkV3Q0N2m8O8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert markdown to Confluence wiki markup.'';
            homepage = "https://github.com/addozhang/obsidian-confluence-converter";
        };
    }
);
    "confluence-integration" = (
    let
        baseUrl = "https://github.com/markdown-confluence/obsidian-integration/releases/download/5.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "confluence-integration";
        version = "5.5.2";
        outputHash = "sha256-2A3QhaXczb087R2a4zz8IVk1uIDUVS09hblJ4q8MU6w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish Markdown content to Atlassian Confluence. It supports some Obsidian Markdown extensions for richer content.'';
            homepage = "https://github.com/markdown-confluence/obsidian-integration";
        };
    }
);
    "confluence-link" = (
    let
        baseUrl = "https://github.com/BungaRazvan/confluence-link/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "confluence-link";
        version = "1.4.3";
        outputHash = "sha256-/V8TO7etKpPXKIi/twKj9CPXX982Edna2VxS5U9Y1oA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload files to Confluence pages'';
            homepage = "https://github.com/BungaRazvan/confluence-link";
        };
    }
);
    "confluence-sync" = (
    let
        baseUrl = "https://github.com/kerry/obsidian-confluence-sync/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "confluence-sync";
        version = "1.0.8";
        outputHash = "sha256-VWKZbz+FIRCwij5RczNZUzeXfM0FtwLzBPFzAgVFhKg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes with Confluence'';
            homepage = "https://github.com/kerry/obsidian-confluence-sync";
        };
    }
);
    "confluence-to-obsidian" = (
    let
        baseUrl = "https://github.com/KkEi34/confluence-to-obsidian-plugin/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "confluence-to-obsidian";
        version = "0.2.6";
        outputHash = "sha256-V4lMv5ckIHgYUImMb9a2wL7/cmmfwnWe3dWdIwdFyGY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Confluence space into your vault.'';
            homepage = "https://github.com/KkEi34/confluence-to-obsidian-plugin";
        };
    }
);
    "connections" = (
    let
        baseUrl = "https://github.com/evancleve/obsidian-connections/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "connections";
        version = "1.1.5";
        outputHash = "sha256-PP0oqsRkVugTMftUXOJQ1+pyrJr6DoOX6JinMhfEtjQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define and view named connections between your notes.'';
            homepage = "https://github.com/evancleve/obsidian-connections";
        };
    }
);
    "consecutive-lists" = (
    let
        baseUrl = "https://github.com/jtucker2/obsidian-consecutive-lists/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "consecutive-lists";
        version = "1.0.1";
        outputHash = "sha256-mqqepdNLXVGuVmdNOWRbv70DWPjMvgUdpMqh2us8M3A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create consecutive lists that are displayed separately in reading mode.'';
            homepage = "https://github.com/jtucker2/obsidian-consecutive-lists";
        };
    }
);
    "consistent-attachments-and-links" = (
    let
        baseUrl = "https://github.com/dy-sh/obsidian-consistent-attachments-and-links/releases/download/3.33.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "consistent-attachments-and-links";
        version = "3.33.4";
        outputHash = "sha256-ECS7G4X/fgRyoqHXVLn2Irv8eo2ts4b4nxjJ0dosS4s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move note attachments and update links automatically.'';
            homepage = "https://github.com/dy-sh/obsidian-consistent-attachments-and-links";
        };
    }
);
    "console" = (
    let
        baseUrl = "https://github.com/dellermann/obsidian-console/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "console";
        version = "1.0.3";
        outputHash = "sha256-h7A872oVGjV/wLm6gYxMP9WiWjI8f3ESavs8zaJQqPk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render console commands and their output.'';
            homepage = "https://github.com/dellermann/obsidian-console";
        };
    }
);
    "contact-cards" = (
    let
        baseUrl = "https://github.com/aegixx/obsidian-contact-cards/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "contact-cards";
        version = "1.3.0";
        outputHash = "sha256-aMOWPgIcialYhSIBUdY0Y967q902dx8kh+rw86Qr+AQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transforms YAML data into a simple business/contact card with power features.'';
            homepage = "https://github.com/aegixx/obsidian-contact-cards";
        };
    }
);
    "content-addressed-attachments" = (
    let
        baseUrl = "https://github.com/NateScarlet/obsidian-content-addressed-attachments/releases/download/0.1.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "content-addressed-attachments";
        version = "0.1.13";
        outputHash = "sha256-uhcCY8Kouj11Zdce1ltFfUYZxMbMrXG4VBwx+KETx2Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Content-addressed attachment storage for automatic deduplication. Works entirely locally; optionally uses GitHub private repositories for hosting.'';
            homepage = "https://github.com/NateScarlet/obsidian-content-addressed-attachments";
        };
    }
);
    "content-cards" = (
    let
        baseUrl = "https://github.com/liqms/obsidian-content-cards/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "content-cards";
        version = "1.2.3";
        outputHash = "sha256-eBzCGw1PNtWW/DKBlx1YfjJLOkkt/X/ZgOKLSiEGP7k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert content cards in Markdown, such as timeline, highlightblock, target card, book information card, music information card, movie information card, photoes ablum, business card, content subfield, countdown, etc.'';
            homepage = "https://github.com/liqms/obsidian-content-cards";
        };
    }
);
    "content-linker" = (
    let
        baseUrl = "https://github.com/Medill-East/obsidian-content-linker/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "content-linker";
        version = "1.5.1";
        outputHash = "sha256-aEf1/E3yumiEckVdU9EdLGt1QnV0sNSyqfRqzL1paV4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and add bi-directional links to existing content.'';
            homepage = "https://github.com/Medill-East/obsidian-content-linker";
        };
    }
);
    "content-os" = (
    let
        baseUrl = "https://github.com/eharris128/obsidian-content-os/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "content-os";
        version = "1.0.3";
        outputHash = "sha256-z5ktB3wmi8HW/dZgMf2Emxxh84lnLHDna+qCsy2bmac=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post directly to LinkedIn from your vault.'';
            homepage = "https://github.com/eharris128/obsidian-content-os";
        };
    }
);
    "contentful-publisher" = (
    let
        baseUrl = "https://github.com/ziyafenn/obsidian-contentful-publisher/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "contentful-publisher";
        version = "1.0.0";
        outputHash = "sha256-TIEP4kRCLjkzAo3NiSDTnbbbDSBfjXuLEo6hT4OBOSg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your Contentful content.'';
            homepage = "https://github.com/ziyafenn/obsidian-contentful-publisher";
        };
    }
);
    "context-command-hider" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-context-menu-hider/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "context-command-hider";
        version = "0.0.7";
        outputHash = "sha256-2nug39YafnlljHf5/HZtabGKncmoo/lBQOGSu0mihkU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide any command from the right-click menu.'';
            homepage = "https://github.com/Mara-Li/obsidian-context-menu-hider";
        };
    }
);
    "contextual-note-templating" = (
    let
        baseUrl = "https://github.com/Balibaloo/obsidian-contextual-note-templating/releases/download/3.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "contextual-note-templating";
        version = "3.2.0";
        outputHash = "sha256-AB+7xhVrIkSKaMwb/naeP0120nkEaKFkteoiLZ4FYQs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prompts for values and templates to create notes.'';
            homepage = "https://github.com/Balibaloo/obsidian-contextual-note-templating";
        };
    }
);
    "contextual-sidecar" = (
    let
        baseUrl = "https://github.com/matthewturk/obsidian-sidecar-panel/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "contextual-sidecar";
        version = "0.4.0";
        outputHash = "sha256-YdxB2OgAqc2KwGvwFPFj7ytXkIQ6IaEWV05HZqHo2no=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a context-dependent sidecar panel.'';
            homepage = "https://github.com/matthewturk/obsidian-sidecar-panel";
        };
    }
);
    "continuous-mode" = (
    let
        baseUrl = "https://github.com/gasparschott/obsidian-continuous-mode/releases/download/3.6.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "continuous-mode";
        version = "3.6.10";
        outputHash = "sha256-fVwehyX028nNo8XHij1CB5lNXUJ21kakwTyBFbnu/Vs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays all open notes in a tab group as a continuous scrollable page (sometimes called Scrivenings mode).'';
            homepage = "https://github.com/gasparschott/obsidian-continuous-mode";
        };
    }
);
    "contribution-graph" = (
    let
        baseUrl = "https://github.com/vran-dev/obsidian-contribution-graph/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "contribution-graph";
        version = "0.10.0";
        outputHash = "sha256-uV/f2KuqPyLvzV/+uSVH7xnBGFPvxvi8I1/U1EAayEU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate an interactive heatmap to visualize and track your productivity.'';
            homepage = "https://github.com/vran-dev/obsidian-contribution-graph";
        };
    }
);
    "control-characters" = (
    let
        baseUrl = "https://github.com/joethei/obsidian-control-characters/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "control-characters";
        version = "0.4.0";
        outputHash = "sha256-SQs/6ougvzrKPT0OIZCnyJpy2orm0JeIwAErSFD8LxA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show control/non-printing characters in edit mode.'';
            homepage = "https://github.com/joethei/obsidian-control-characters";
        };
    }
);
    "convert-base64-to-png" = (
    let
        baseUrl = "https://github.com/nykkolin/obsidian-convert-base64-to-png/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "convert-base64-to-png";
        version = "1.0.0";
        outputHash = "sha256-T7WBvaJJzjkU+qAkDfY7WPjirOsXXa/Fp58/6he6iSE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert base64-encoded images in notes to local PNG images.'';
            homepage = "https://github.com/nykkolin/obsidian-convert-base64-to-png";
        };
    }
);
    "convert-katex-to-mathjax" = (
    let
        baseUrl = "https://github.com/pejakovic/obsidian-convert-katex-to-mathjax/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "convert-katex-to-mathjax";
        version = "1.1.0";
        outputHash = "sha256-Ip/Pzi6Xeo5hHsIOX31pMC/GmkgXg9RRYU/n4VpOhig=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Converts KaTeX format to MathJax format'';
            homepage = "https://github.com/pejakovic/obsidian-convert-katex-to-mathjax";
        };
    }
);
    "convert-url-to-iframe" = (
    let
        baseUrl = "https://github.com/FHachez/obsidian-convert-url-to-iframe/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "convert-url-to-iframe";
        version = "0.5.0";
        outputHash = "sha256-n9P1CWK4wxSfmWeKOdK1m5jEf65EtJcTNCsaq8Btnrk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a URL (e.g. YouTube) into an iframe (preview).'';
            homepage = "https://github.com/FHachez/obsidian-convert-url-to-iframe";
        };
    }
);
    "cooklang-obsidian" = (
    let
        baseUrl = "https://github.com/cooklang/cooklang-obsidian/releases/download/0.6.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cooklang-obsidian";
        version = "0.6.4";
        outputHash = "sha256-MvqojGnnCyvxsR9EwHTBHOq+EzoFRcXcxtiynLtsGes=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit and display CookLang recipes.'';
            homepage = "https://github.com/cooklang/cooklang-obsidian";
        };
    }
);
    "cooklang-viewer-and-editor" = (
    let
        baseUrl = "https://github.com/rveciana/obsidian-cooklang/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cooklang-viewer-and-editor";
        version = "0.1.0";
        outputHash = "sha256-RLJA6Ra0dqgIwjN75O9ISAE9o+xBMCwyp/rtTegMLQM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display and edit recipes written in the Cooklang format.'';
            homepage = "https://github.com/rveciana/obsidian-cooklang";
        };
    }
);
    "cooksync" = (
    let
        baseUrl = "https://github.com/furst/cooksync-obsidian/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cooksync";
        version = "1.0.3";
        outputHash = "sha256-qs+wAh/CbYsZFi6/y3M4hFt4fkhBiIEjqaAF0yYffb0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically imports recipes collected using Cooksync.'';
            homepage = "https://github.com/furst/cooksync-obsidian";
        };
    }
);
    "copilot" = (
    let
        baseUrl = "https://github.com/logancyang/obsidian-copilot/releases/download/3.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copilot";
        version = "3.2.7";
        outputHash = "sha256-4yEVv/tOpcrzwlLXty0Tl14bKNFWf1KLig0J2I1cO08=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Your AI Copilot: Chat with Your Second Brain, Learn Faster, Work Smarter.'';
            homepage = "https://github.com/logancyang/obsidian-copilot";
        };
    }
);
    "copilot-auto-completion" = (
    let
        baseUrl = "https://github.com/j0rd1smit/obsidian-copilot-auto-completion/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copilot-auto-completion";
        version = "1.1.3";
        outputHash = "sha256-E/TbK+uaZYP0nEEkpfCXIymk7zxGnkUeIlamcsJiFl0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A highly configurable copilot-like auto-completion using the ChatGPT API.'';
            homepage = "https://github.com/j0rd1smit/obsidian-copilot-auto-completion";
        };
    }
);
    "copy-as-html" = (
    let
        baseUrl = "https://github.com/jenningsb2/copy-as-html/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-as-html";
        version = "1.1.3";
        outputHash = "sha256-63AW3SfurePigCWgmM8j3s+k+P3W6KZC4NxKVFLaIlI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert the selected Markdown to HTML and copy it to the clipboard.'';
            homepage = "https://github.com/jenningsb2/copy-as-html";
        };
    }
);
    "copy-as-latex" = (
    let
        baseUrl = "https://github.com/mo-seph/obsidian-copy-as-latex/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-as-latex";
        version = "0.1.9";
        outputHash = "sha256-4eXA1F9HbdtLZBwVhTAlZ4k8sgrgy17AaiJUQQhqqJM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly copy Markdown as LaTeX, with citations.'';
            homepage = "https://github.com/mo-seph/obsidian-copy-as-latex";
        };
    }
);
    "copy-as-source" = (
    let
        baseUrl = "https://github.com/gapmiss/copy-as-source/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-as-source";
        version = "1.0.3";
        outputHash = "sha256-02qd3AM0+mSvRe77ArxoqkIlYAfVovmcoo4H/CDjLkY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Select and copy source HTML in reading view.'';
            homepage = "https://github.com/gapmiss/copy-as-source";
        };
    }
);
    "copy-document-as-html" = (
    let
        baseUrl = "https://github.com/mvdkwast/obsidian-copy-as-html/releases/download/0.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-document-as-html";
        version = "0.8.1";
        outputHash = "sha256-0OXb0TsJT/zHrEDHL/a+0TjxGBxsskTnEFQNfyoWJJU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy the current document to clipboard as HTML, including images.'';
            homepage = "https://github.com/mvdkwast/obsidian-copy-as-html";
        };
    }
);
    "copy-image" = (
    let
        baseUrl = "https://github.com/felipe-ds-lima/obsidian-copy-image-plugin/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-image";
        version = "1.1.1";
        outputHash = "sha256-raUU/dgKg5L6NVWit1r1CULgJhKgR2/MsRIb/52ex80=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily copy images to clipboard by right-clicking an image.'';
            homepage = "https://github.com/felipe-ds-lima/obsidian-copy-image-plugin";
        };
    }
);
    "copy-image-text" = (
    let
        baseUrl = "https://github.com/msgk239/obsidian-copy-image-text/releases/download/1.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-image-text";
        version = "1.1.8";
        outputHash = "sha256-tSBV6K0UsOd7/PKdM0/fy7sfYRvKFTlxwcawNctyLZQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy note content (including text and images) to clipboard. 复制笔记内容（包括文本和图片）到剪贴板。'';
            homepage = "https://github.com/msgk239/obsidian-copy-image-text";
        };
    }
);
    "copy-inline-code" = (
    let
        baseUrl = "https://github.com/ozavodny/obsidian-copy-inline-code-plugin/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-inline-code";
        version = "2.0.0";
        outputHash = "sha256-9XSKlYqbR7r6OekH2bg8m7f97kbYvpOewOztkx6yX/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily copy the contents of an inline code element with a single click.'';
            homepage = "https://github.com/ozavodny/obsidian-copy-inline-code-plugin";
        };
    }
);
    "copy-local-graph-paths" = (
    let
        baseUrl = "https://github.com/0melette/copy-local-graph-paths/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-local-graph-paths";
        version = "1.0.1";
        outputHash = "sha256-EGBjDlpzMVZqefwANN0EkophFuWTZ8a6g0v2InR1dL0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copies file paths of local graph links to clipboard.'';
            homepage = "https://github.com/0melette/copy-local-graph-paths";
        };
    }
);
    "copy-metadata" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-copy-metadata/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-metadata";
        version = "0.0.4";
        outputHash = "sha256-LdXRVkoLInCGDZj3DO/Kf83Rub7hUsp04LZmcYCh0YI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy file metadata, e.g., creation time, to clipboard. Insert copied metadata to file name.'';
            homepage = "https://github.com/wenlzhang/obsidian-copy-metadata";
        };
    }
);
    "copy-note" = (
    let
        baseUrl = "https://github.com/kzhovn/copy-command-obsidian/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-note";
        version = "0.2.1";
        outputHash = "sha256-TSC6vVL+7RMYrULJ+VaYA3E57TClqh5UQ1jSRnM7WNA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add commands to copy active note and copy folders.'';
            homepage = "https://github.com/kzhovn/copy-command-obsidian";
        };
    }
);
    "copy-path" = (
    let
        baseUrl = "https://github.com/shumadrid/obsidian-copy-path/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-path";
        version = "0.3.0";
        outputHash = "sha256-Pv5dhdb84ddGLOCzxGM7ztEHFIuRAy5MpLQrTrZ+TQ4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds context menu actions for copying the full/vault-relative path of files and folders.'';
            homepage = "https://github.com/shumadrid/obsidian-copy-path";
        };
    }
);
    "copy-plaintext" = (
    let
        baseUrl = "https://github.com/FinickySpider/Obsidian-Copy-as-Plaintext/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-plaintext";
        version = "1.0.3";
        outputHash = "sha256-OwiW31N5aV7U3UJUPgoaUtuMVlHW8P8L/aneFJjyuJ8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a copy as plaintext command to the command palette and context menu.'';
            homepage = "https://github.com/FinickySpider/Obsidian-Copy-as-Plaintext";
        };
    }
);
    "copy-publish-url" = (
    let
        baseUrl = "https://github.com/kometenstaub/copy-publish-url/releases/download/0.4.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-publish-url";
        version = "0.4.5";
        outputHash = "sha256-70hAx/BHjIdCyE1piXK0Hsq+J/SthJOxKOiEZsDThBU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy or open the URL of the corresponding note on your Publish site. You can also open its Git commit history on GitHub.'';
            homepage = "https://github.com/kometenstaub/copy-publish-url";
        };
    }
);
    "copy-recursive-content" = (
    let
        baseUrl = "https://github.com/SeardnaSchmid/copy-recursive-content/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-recursive-content";
        version = "1.0.10";
        outputHash = "sha256-yLd3oiSGSOnamEhT2EXcAk0T/YlQ8lf/CoBLt4bv3a0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy the contents of files and folders in a structured JSON format.'';
            homepage = "https://github.com/SeardnaSchmid/copy-recursive-content";
        };
    }
);
    "copy-section" = (
    let
        baseUrl = "https://github.com/skztr/obsidian-plugin-section-copy/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-section";
        version = "1.2.4";
        outputHash = "sha256-V1R9HlszXj76kge/o69MijcazJbAyko7WcHqVKWmQMk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a Copy button to the top of Headed sections.'';
            homepage = "https://github.com/skztr/obsidian-plugin-section-copy";
        };
    }
);
    "copy-url-in-preview" = (
    let
        baseUrl = "https://github.com/NomarCub/obsidian-copy-url-in-preview/releases/download/1.12.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-url-in-preview";
        version = "1.12.1";
        outputHash = "sha256-BQU90er21Ky81Y4BOMLK43Z8eUGkYk7KqCtZkNFXujc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Image context menus (mostly on right click): Copy to clipboard, Open in default app, Show in system explorer, Reveal file in navigation, Open in new tab.'';
            homepage = "https://github.com/NomarCub/obsidian-copy-url-in-preview";
        };
    }
);
    "copy-without-links" = (
    let
        baseUrl = "https://github.com/adiron/obsidian-strip-internal-links/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "copy-without-links";
        version = "1.1.0";
        outputHash = "sha256-v6ZBgTBhzOXiRQpm2CuMj4LOeDfM8qJ2oa0Iw7ACih8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy the contents of files or selections within to the clipboard, without internal links.'';
            homepage = "https://github.com/adiron/obsidian-strip-internal-links";
        };
    }
);
    "countdown-to" = (
    let
        baseUrl = "https://github.com/guicattani/obsidian-countdown-to/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "countdown-to";
        version = "1.5.0";
        outputHash = "sha256-0DQQMmhgPx03aKykMxxOmy7ObFE8JyIfAcDeVWMi/qE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create countdown progress bars in your notes.'';
            homepage = "https://github.com/guicattani/obsidian-countdown-to";
        };
    }
);
    "course-module-loader" = (
    let
        baseUrl = "https://github.com/QuintSmart/obsidian-course-module-loader/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "course-module-loader";
        version = "1.0.0";
        outputHash = "sha256-R06A/irYJUKcvky7vHJa/3cPmj6unN+qS/FOLSdTH0s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Downloads and unzips course module zip files from a URL into a specified vault folder, skipping existing files.'';
            homepage = "https://github.com/QuintSmart/obsidian-course-module-loader";
        };
    }
);
    "crackboard" = (
    let
        baseUrl = "https://github.com/bruce-pain/crackboard-obsidian/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "crackboard";
        version = "1.0.2";
        outputHash = "sha256-frDMwRlPZDucP95d9xyIEtDlB2Dd20HL38uXnIYHzmA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''tpot leaderboard productivity tracker'';
            homepage = "https://github.com/bruce-pain/crackboard-obsidian";
        };
    }
);
    "crafty" = (
    let
        baseUrl = "https://github.com/liolle/Crafty/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "crafty";
        version = "1.3.2";
        outputHash = "sha256-Rb9vy185R170PZ6u+UiUgCw9IAlK3akNX6i1TeAkIv0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add tooltip to any canvas node and quickly navigate between canvas nodes.'';
            homepage = "https://github.com/liolle/Crafty";
        };
    }
);
    "creases" = (
    let
        baseUrl = "https://github.com/liamcain/obsidian-creases/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "creases";
        version = "0.7.0";
        outputHash = "sha256-qo5wi8ayoNvkOQFe2h1lWtiztms4Bapaaamkwiwosk4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tools for effectively folding Markdown sections.'';
            homepage = "https://github.com/liamcain/obsidian-creases";
        };
    }
);
    "create-folder-notes-with-dropdown" = (
    let
        baseUrl = "https://github.com/SturdyShawn/Create-folder-notes-with-dropdown/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "create-folder-notes-with-dropdown";
        version = "1.0.0";
        outputHash = "sha256-jOkWsRhZe7xdyoK9YvKfojGLFgZtXP0fNGd8fG05qtI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''create-folder-notes-with-dropdown is a plugin designed to help users quickly create Markdown files in existing or nonexisting folders with dropdown.'';
            homepage = "https://github.com/SturdyShawn/Create-folder-notes-with-dropdown";
        };
    }
);
    "create-note-in-folder" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-create-note-in-folder/releases/download/1.13.0-0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "create-note-in-folder";
        version = "1.13.0-0";
        outputHash = "sha256-ttI/8ZuYLWTM05E2Fm8pCwPWpRuonY9SKWMQrYEzN3E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add commands to create a note in a specific folder.'';
            homepage = "https://github.com/Mara-Li/obsidian-create-note-in-folder";
        };
    }
);
    "create-note-list" = (
    let
        baseUrl = "https://github.com/andrewheekin/obsidian-create-note-list/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "create-note-list";
        version = "1.0.6";
        outputHash = "sha256-c3XGZlLQ6Vqll9+shBJJ2sliWl0noacC7qUdll16P8Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates a bulleted list of notes contained within the parent folder and prepends to current note content after YAML frontmatter.'';
            homepage = "https://github.com/andrewheekin/obsidian-create-note-list";
        };
    }
);
    "create-note-with-date" = (
    let
        baseUrl = "https://github.com/kargnas/obsidian-create-note-with-date/releases/download/0.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "create-note-with-date";
        version = "0.2.5";
        outputHash = "sha256-VG73MIE16PXDMZ06cGKYxpSRat1f+zNbTA3UBeXpjUs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a new note with today"s date in the current directory'';
            homepage = "https://github.com/kargnas/obsidian-create-note-with-date";
        };
    }
);
    "create-task" = (
    let
        baseUrl = "https://github.com/simonknittel/obsidian-create-task/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "create-task";
        version = "1.4.2";
        outputHash = "sha256-yeWGIy0/D0QPc83l7o38uiZN1Xa7hhtuFCoIdV+Zvq8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create tasks faster from anywhere.'';
            homepage = "https://github.com/simonknittel/obsidian-create-task";
        };
    }
);
    "cron" = (
    let
        baseUrl = "https://github.com/cdloh/obsidian-cron/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cron";
        version = "1.1.2";
        outputHash = "sha256-JvrgZycDFSKLWCM/s9/DvrDNwhvwEU79/nwK+M+hqSk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple CRON / scheduler to regularly run user scripts or commands.'';
            homepage = "https://github.com/cdloh/obsidian-cron";
        };
    }
);
    "crossbow" = (
    let
        baseUrl = "https://github.com/shoedler/crossbow/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "crossbow";
        version = "1.4.0";
        outputHash = "sha256-5hIBOJdWvXxvhuj0aqii/VGxDfDkVmnWncTlu+Cjl34=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find backlinks in your notes.'';
            homepage = "https://github.com/shoedler/crossbow";
        };
    }
);
    "crosslink-advanced" = (
    let
        baseUrl = "https://github.com/d7sd6u/obsidian-crosslink-advanced/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "crosslink-advanced";
        version = "1.0.3";
        outputHash = "sha256-X/xJqR5PnKYRCe+FxIbmC+0hqw25jRZHT2bZL+xjDbw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tag files using folders and symlinks system (ftags).'';
            homepage = "https://github.com/d7sd6u/obsidian-crosslink-advanced";
        };
    }
);
    "crumbs-obsidian" = (
    let
        baseUrl = "https://github.com/tgrosinger/crumbs-obsidian/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "crumbs-obsidian";
        version = "0.1.2";
        outputHash = "sha256-SOLxUvG1SDiaG63WjguQbvDix1rj5WmoTa24uk+jwbg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Breadcrumb navigation.'';
            homepage = "https://github.com/tgrosinger/crumbs-obsidian";
        };
    }
);
    "crypt-it" = (
    let
        baseUrl = "https://github.com/remotely-save/crypt-it/releases/download/0.9.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "crypt-it";
        version = "0.9.8";
        outputHash = "sha256-XqoSIbN1vTNJt/SE952PzzKfJmfkvbiLXfjRNHxqv5I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate encrypted version of file(s) using rclone encryption format.'';
            homepage = "https://github.com/remotely-save/crypt-it";
        };
    }
);
    "cryptsidian" = (
    let
        baseUrl = "https://github.com/triumphantomato/cryptsidian/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cryptsidian";
        version = "1.1.0";
        outputHash = "sha256-suC4Id8mwGh1zJtJR3X3v2UxSSpsvnkYXFBzI3dVXh4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Encrypt all files in your vault with a password.'';
            homepage = "https://github.com/triumphantomato/cryptsidian";
        };
    }
);
    "css-editor" = (
    let
        baseUrl = "https://github.com/Zachatoo/obsidian-css-editor/releases/download/1.14.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "css-editor";
        version = "1.14.0";
        outputHash = "sha256-v0/jYdmKEFwr7q6y76GjfmKyjMLSTngFBEwaqaVYV5M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit CSS snippet files.'';
            homepage = "https://github.com/Zachatoo/obsidian-css-editor";
        };
    }
);
    "css-inlay-colors" = (
    let
        baseUrl = "https://github.com/GRA0007/obsidian-css-inlay-colors/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "css-inlay-colors";
        version = "1.4.1";
        outputHash = "sha256-CPEG618wygTNjO1SpAyHFYjXun3mO03Q7fRiDycJmG8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show inline color hints for CSS colors'';
            homepage = "https://github.com/GRA0007/obsidian-css-inlay-colors";
        };
    }
);
    "css-inserter" = (
    let
        baseUrl = "https://github.com/Erallie/css-inserter/releases/download/1.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "css-inserter";
        version = "1.0.15";
        outputHash = "sha256-GqICN3OVCj/KPqD2q9kL98VTruLmWwtTOHKTIpNA+SQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Inserts user-defined CSS snippets into the selected text.'';
            homepage = "https://github.com/Erallie/css-inserter";
        };
    }
);
    "css-snippets" = (
    let
        baseUrl = "https://github.com/jdbrice/obsidian-css-snippets/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "css-snippets";
        version = "0.1.2";
        outputHash = "sha256-EhC895wM6tgmALsLVkVnM2SKT4W4RMlpdIEsO9bc2A0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Load and manage CSS snippets.'';
            homepage = "https://github.com/jdbrice/obsidian-css-snippets";
        };
    }
);
    "csv-allinone" = (
    let
        baseUrl = "https://github.com/Hangeol-Chang/obsidian-csv-allinone/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "csv-allinone";
        version = "0.1.5";
        outputHash = "sha256-A8m9zXd8T2yh56sbnfsqeLMzezdQ/VFdNKIHI4G4RMc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''all about CSV.'';
            homepage = "https://github.com/Hangeol-Chang/obsidian-csv-allinone";
        };
    }
);
    "csv-codeblock" = (
    let
        baseUrl = "https://github.com/elrindir/obsidian-csv-codeblock/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "csv-codeblock";
        version = "1.1.0";
        outputHash = "sha256-7ddUQB1vnTzi9v1MPpPlGiXJ/75Er71G3jzeCDWG9tg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render code blocks with CSV format.'';
            homepage = "https://github.com/elrindir/obsidian-csv-codeblock";
        };
    }
);
    "csv-lite" = (
    let
        baseUrl = "https://github.com/LIUBINfighter/csv-lite/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "csv-lite";
        version = "1.7.0";
        outputHash = "sha256-Q/+etbrXTXvqp6nQBMqD97XUKbv4P+QomaEhMPrV4S4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Just open and edit a CSV file in workspace view, no more. Keep it simple.'';
            homepage = "https://github.com/LIUBINfighter/csv-lite";
        };
    }
);
    "csv-obsidian" = (
    let
        baseUrl = "https://github.com/deathau/csv-obsidian/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "csv-obsidian";
        version = "0.0.1";
        outputHash = "sha256-3c76OJjFURDnmCgnc5bLxW17Kdj/a3HcjfwlI6vTGWc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit CSV files.'';
            homepage = "https://github.com/deathau/csv-obsidian";
        };
    }
);
    "ctrl-xa" = (
    let
        baseUrl = "https://github.com/nbossard/obsidian-CtrlXA/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ctrl-xa";
        version = "1.0.9";
        outputHash = "sha256-j5Ux3ONAKhIuBR4uz+wYzuzUvkm07/pWMiUAacRhyAw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Cycle through various items with keyboard shortcuts. Such as days, months, true-false, log level,... anything you need.'';
            homepage = "https://github.com/nbossard/obsidian-CtrlXA";
        };
    }
);
    "cubox-sync" = (
    let
        baseUrl = "https://github.com/OLCUBO/obsidian-cubox/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cubox-sync";
        version = "1.0.7";
        outputHash = "sha256-UinH7m459AE3SjoDmgPlyzOGhE6qlbL8pM2TICu4YPM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Cubox articles & annotations'';
            homepage = "https://github.com/OLCUBO/obsidian-cubox";
        };
    }
);
    "current-file" = (
    let
        baseUrl = "https://github.com/2shortplanks/current-file/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "current-file";
        version = "1.0.0";
        outputHash = "sha256-X0jo6zCAB91825y/fAGmQ8ueG6yVWP6Pb39v40oLapw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows external applications to know what file the desktop app is currently viewing.'';
            homepage = "https://github.com/2shortplanks/current-file";
        };
    }
);
    "current-file-tags" = (
    let
        baseUrl = "https://github.com/trung-tran-swe/obsidian-current-file-tags/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "current-file-tags";
        version = "1.0.2";
        outputHash = "sha256-+bSepzzJYEtKj0QCA0+gTq8Yinu2kzbqInjLqL2V8xQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the active Markdown file"s tags and associated files.'';
            homepage = "https://github.com/trung-tran-swe/obsidian-current-file-tags";
        };
    }
);
    "current-folder-notes-pamphlet" = (
    let
        baseUrl = "https://github.com/Caffa/Obsidian-Current-Folder-Note-Display-Plugin/releases/download/1.7.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "current-folder-notes-pamphlet";
        version = "1.7.15";
        outputHash = "sha256-lvuXyekzylhOizD+mbq863WUXHmy6H0GZYy5CfGSx3I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Shows a list of notes in the current folder, and allows you to filter the titles to include or exclude notes.'';
            homepage = "https://github.com/Caffa/Obsidian-Current-Folder-Note-Display-Plugin";
        };
    }
);
    "current-view" = (
    let
        baseUrl = "https://github.com/LucEast/obsidian-current-view/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "current-view";
        version = "1.5.2";
        outputHash = "sha256-6OaSHrVXq8HWDnb6gqVCxFEQ4JZu6iO3vEN6sIO7Q8A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically set the view mode (Reading, Live Preview, Source) for notes using folder rules, file patterns, or frontmatter'';
            homepage = "https://github.com/LucEast/obsidian-current-view";
        };
    }
);
    "cursor-bridge" = (
    let
        baseUrl = "https://github.com/lengff123/cursor-bridge/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cursor-bridge";
        version = "1.0.2";
        outputHash = "sha256-4Jna7e0ZfVuDNTABa61QFxG+0+zLBbGGmFHlKjFs2Q8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Seamlessly bridge your notes with Cursor, the AI-powered code editor.'';
            homepage = "https://github.com/lengff123/cursor-bridge";
        };
    }
);
    "cursor-goaway" = (
    let
        baseUrl = "https://github.com/liuxingyu521/obsidian-plugin-cursor-goaway/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cursor-goaway";
        version = "1.5.0";
        outputHash = "sha256-ftpV3SaEkiB+Q+hWINX0KwePtM5jLQyOmTjQFjkzmMk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide the cursor after opening a note'';
            homepage = "https://github.com/liuxingyu521/obsidian-plugin-cursor-goaway";
        };
    }
);
    "cursor-jump" = (
    let
        baseUrl = "https://github.com/sangwon-j/obsidian-jump/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cursor-jump";
        version = "0.1.9";
        outputHash = "sha256-okdUgtCFC866rVj5oPVdJ5zsTU8Je5UwNnPlhiDEZ4I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly jump between list items and headings throughout same or upper/lower level.'';
            homepage = "https://github.com/sangwon-j/obsidian-jump";
        };
    }
);
    "cursor-position-history" = (
    let
        baseUrl = "https://github.com/fgubler/obsidian-cursor-position-history/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cursor-position-history";
        version = "1.3.0";
        outputHash = "sha256-m0Vm56xLWGs2Ba/wU4OVwXOInbb+GWE8GgT/qpzQTog=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remember the previous positions of the cursor across files: auto-scroll to the correct position in a new file. Navigate backward and forward through your cursor position history without losing context.'';
            homepage = "https://github.com/fgubler/obsidian-cursor-position-history";
        };
    }
);
    "cursor-position-on-title-enter" = (
    let
        baseUrl = "https://github.com/chaintng/cursor-position-on-title-enter/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cursor-position-on-title-enter";
        version = "1.0.5";
        outputHash = "sha256-y1XM8eK5c24rP+K29qzpJTmSRNOpxVwCugVJLPP2Bws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set the cursor position after pressing Enter on the note title.'';
            homepage = "https://github.com/chaintng/cursor-position-on-title-enter";
        };
    }
);
    "custom-classes" = (
    let
        baseUrl = "https://github.com/LilaRest/obsidian-custom-classes/releases/download/2.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-classes";
        version = "2.6.1";
        outputHash = "sha256-d80cxk729J8+uML+qDruy2RZFOCuxZefk/SLskz+xJY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add your own HTML classes to chosen Markdown elements directly from your notes.'';
            homepage = "https://github.com/LilaRest/obsidian-custom-classes";
        };
    }
);
    "custom-commands" = (
    let
        baseUrl = "https://github.com/Staaaaaaaaaan/obsidian-custom-commands/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-commands";
        version = "1.0.7";
        outputHash = "sha256-ehaLvBnf8/EcNq0vxMLPYN44IXYManTZ31zUIn6QSb8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create custom commands to be executed in the command palette, and by hotkey. Currently supports opening specific notes, creating notes, inserting snippets, and executing sequences of commands.'';
            homepage = "https://github.com/Staaaaaaaaaan/obsidian-custom-commands";
        };
    }
);
    "custom-comment" = (
    let
        baseUrl = "https://github.com/Jack-Chronicle/custom-comment/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-comment";
        version = "0.2.8";
        outputHash = "sha256-HfiqEO5WKmB/LXSjam3hZshMEcJcPFeOv5jyikNkT4Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create custom keyboard shortcuts that enclose comments.'';
            homepage = "https://github.com/Jack-Chronicle/custom-comment";
        };
    }
);
    "custom-file-viewer" = (
    let
        baseUrl = "https://github.com/peabody28/obsidian-extension-custom-file-viewer/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-file-viewer";
        version = "1.0.0";
        outputHash = "sha256-1MyyrGrw077CzsHJRxYG9llYChDIMb5gRWpav5f73Ac=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open files with custom external applications based on file extension'';
            homepage = "https://github.com/peabody28/obsidian-extension-custom-file-viewer";
        };
    }
);
    "custom-font-loader" = (
    let
        baseUrl = "https://github.com/pourmand1376/obsidian-custom-font/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-font-loader";
        version = "1.8.0";
        outputHash = "sha256-Taj6MNuAg04BDw4ebhwnhHrS8+n+RF04HKxAcX4HL3A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize your vault with any font you want (+ Support for Android and iOS).'';
            homepage = "https://github.com/pourmand1376/obsidian-custom-font";
        };
    }
);
    "custom-image-auto-uploader" = (
    let
        baseUrl = "https://github.com/haierkeys/obsidian-custom-image-auto-uploader/releases/download/2.9.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-image-auto-uploader";
        version = "2.9.5";
        outputHash = "sha256-lfHD61nzmZ0AR4BNZTBZA1ZWCuSwdPIfpo9jhSu1WLE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''You can upload and save images from your notes to your remote server, home NAS, or sync them to your cloud storage (Alibaba Cloud OSS, Amazon S3, Cloudflare R2).'';
            homepage = "https://github.com/haierkeys/obsidian-custom-image-auto-uploader";
        };
    }
);
    "custom-list-character" = (
    let
        baseUrl = "https://github.com/lilian-pouliquen/obsidian-custom-list-character/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-list-character";
        version = "1.1.0";
        outputHash = "sha256-YcIRVJRUlFM6M4ElKH6r+L8UkcPUBVMqs984hsxYylc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Choose the character to use when creating a bullet list between "-", "*" and "+".'';
            homepage = "https://github.com/lilian-pouliquen/obsidian-custom-list-character";
        };
    }
);
    "custom-new-file-name" = (
    let
        baseUrl = "https://github.com/hokolamp/obsidian-custom-new-file-name/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-new-file-name";
        version = "1.0.8";
        outputHash = "sha256-QiMSIuacvY0YqnAF1rice/KRniPB6Qxn/3OFy/oNXFk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables the creation of new notes with custom formatted names, including dynamic datetime stamps.'';
            homepage = "https://github.com/hokolamp/obsidian-custom-new-file-name";
        };
    }
);
    "custom-node-size" = (
    let
        baseUrl = "https://github.com/jackvonhouse/custom-node-size/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-node-size";
        version = "1.0.0";
        outputHash = "sha256-hPSfsGqVesOrr5uqZOCAs9gym94wRZS1BYxOA08uvoY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize nodes size for improved graph understanding.'';
            homepage = "https://github.com/jackvonhouse/custom-node-size";
        };
    }
);
    "custom-note-width" = (
    let
        baseUrl = "https://github.com/0skater0/obsidian-custom-note-width/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-note-width";
        version = "2.1.0";
        outputHash = "sha256-kOMedpGvLE0df0bCDYxtyeak4qBjTrPo3KLRHYQ+nOo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Effortlessly change the line width of each note individually. Provides multiple ways to customize the experience, making it both efficient and user-friendly to adjust line widths.'';
            homepage = "https://github.com/0skater0/obsidian-custom-note-width";
        };
    }
);
    "custom-save" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-custom-save/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-save";
        version = "1.0.9";
        outputHash = "sha256-PoM5p+TQpvLPzGqO2tetEM2U84Zv6dpTFGH6Hj/hSec=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add custom save actions.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-custom-save";
        };
    }
);
    "custom-selected-word-count" = (
    let
        baseUrl = "https://github.com/banisterious/obsidian-custom-selected-word-count/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-selected-word-count";
        version = "1.6.2";
        outputHash = "sha256-8qgRv+hweK8pXEqxwUqJzkcKaFyvp8+NUsJ1jLcFXK0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides advanced word counting features for selected text across all view modes. Goes beyond basic word counting with customizable path exclusion, and more.'';
            homepage = "https://github.com/banisterious/obsidian-custom-selected-word-count";
        };
    }
);
    "custom-sidebar-icons" = (
    let
        baseUrl = "https://github.com/Raven-Pensieve/obsidian-custom-icons/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-sidebar-icons";
        version = "2.0.2";
        outputHash = "sha256-FUQ/tjLY1tpaOUNi3FuQFHfp4XXhGTyUQAMweEG1PF0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your workspace with customizable icons for documents and folders.'';
            homepage = "https://github.com/Raven-Pensieve/obsidian-custom-icons";
        };
    }
);
    "custom-slides" = (
    let
        baseUrl = "https://github.com/davidvkimball/obsidian-custom-slides/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-slides";
        version = "1.1.6";
        outputHash = "sha256-zVuL3EBv56yg1exlD8pXU5yxQ4YPTTZJlNRt72kPxhs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize the Slides core plugin"s navigation, styles, and behavior.'';
            homepage = "https://github.com/davidvkimball/obsidian-custom-slides";
        };
    }
);
    "custom-sort" = (
    let
        baseUrl = "https://github.com/SebastianMC/obsidian-custom-sort/releases/download/3.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-sort";
        version = "3.1.6";
        outputHash = "sha256-bozfoFAU4uQmd+fcKQ9BclxYVMaX0uETfiwlpxxM+1g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manual or automatic config-driven reordering and sorting of files and folders in File Explorer.'';
            homepage = "https://github.com/SebastianMC/obsidian-custom-sort";
        };
    }
);
    "custom-state-for-task-list" = (
    let
        baseUrl = "https://github.com/OkamiWong/obsidian-custom-state-for-task-list/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-state-for-task-list";
        version = "1.0.3";
        outputHash = "sha256-RE+T8FcN9yg5jJbjwLLTI4bSuMSzhU1qe8MrrI3STCQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add custom states to task list items.'';
            homepage = "https://github.com/OkamiWong/obsidian-custom-state-for-task-list";
        };
    }
);
    "custom-syntax-highlights" = (
    let
        baseUrl = "https://github.com/Outsiders17711/Obsidian-Custom-Syntax-Highlights/releases/download/0.5.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-syntax-highlights";
        version = "0.5.4";
        outputHash = "sha256-ZXlHgDB6b5ZQ8MRp/HRhzzPKAWZC4vhp9Hw1tEkL9uI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display files with custom extensions as syntax-highlighted code blocks in reading view with configurable extension-to-language mappings.'';
            homepage = "https://github.com/Outsiders17711/Obsidian-Custom-Syntax-Highlights";
        };
    }
);
    "custom-theme-studio" = (
    let
        baseUrl = "https://github.com/gapmiss/custom-theme-studio/releases/download/1.7.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "custom-theme-studio";
        version = "1.7.7";
        outputHash = "sha256-7jKyn1yXGn8SzO9LbkWJGZCw9Bado5ZtTen6WbFBps0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and customize themes with a built-in CSS editor. Modify colors, styles, and export your custom theme.'';
            homepage = "https://github.com/gapmiss/custom-theme-studio";
        };
    }
);
    "customizable-menu" = (
    let
        baseUrl = "https://github.com/kzhovn/obsidian-customizable-menu/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "customizable-menu";
        version = "2.2.0";
        outputHash = "sha256-TSnSZ0uApivLDniZtrhA987E7rj05VZ/N2xSv1CgwyI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add any command to Obsidian"s right-click menu.'';
            homepage = "https://github.com/kzhovn/obsidian-customizable-menu";
        };
    }
);
    "customjs" = (
    let
        baseUrl = "https://github.com/saml-dev/obsidian-custom-js/releases/download/1.0.21";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "customjs";
        version = "1.0.21";
        outputHash = "sha256-IrTsbCAdfVlwB2NplCRw3XbRe1wal2VStnkWZIhSYDc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reuse custom JavaScript across desktop and mobile.'';
            homepage = "https://github.com/saml-dev/obsidian-custom-js";
        };
    }
);
    "cut-the-fluff" = (
    let
        baseUrl = "https://github.com/adamfletcher/obsidian-cut-the-fluff/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cut-the-fluff";
        version = "0.1.6";
        outputHash = "sha256-876Jz+Mhe4JxFucy6TZMAahd0mftxcjQVXzOsyA81pg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make your writing clearer by checking for unnecessary complexity and clutter.'';
            homepage = "https://github.com/adamfletcher/obsidian-cut-the-fluff";
        };
    }
);
    "cycle-in-sidebar" = (
    let
        baseUrl = "https://github.com/houcheng/obsidian-cycle-in-sidebar-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cycle-in-sidebar";
        version = "1.0.3";
        outputHash = "sha256-Vosihqhpfh5Oc9Ao4c3AEd+HMFd6tXjUURFF8LoJ9XU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hotkeys to cycle through tabs in the left or right sidebars.'';
            homepage = "https://github.com/houcheng/obsidian-cycle-in-sidebar-plugin";
        };
    }
);
    "cycle-through-panes" = (
    let
        baseUrl = "https://github.com/Vinzent03/tab-switcher/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cycle-through-panes";
        version = "1.4.0";
        outputHash = "sha256-S4EEc4GWRdUMDc+3WrM7Wcv0af/gyPXmkUKPHhTLszs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Switch your tabs with Ctrl + Tab in recently used order like in a browser.'';
            homepage = "https://github.com/Vinzent03/tab-switcher";
        };
    }
);
    "cypher" = (
    let
        baseUrl = "https://github.com/aths7/cypher/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "cypher";
        version = "1.0.0";
        outputHash = "sha256-MvtH8lfNJxNBKX+BywIK21wU+H0XM9sSdQlcy2Qepps=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hides text in a simple diagrammatic cypher to make reading unrecognizable for viewers.'';
            homepage = "https://github.com/aths7/cypher";
        };
    }
);
    "d2-obsidian" = (
    let
        baseUrl = "https://github.com/terrastruct/d2-obsidian/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "d2-obsidian";
        version = "1.1.4";
        outputHash = "sha256-FaN7U43vJAl9MtK0T/B7Cs1dwpCPHVfaZUwgpZTyJ2k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official D2 plugin. D2 is a modern diagram scripting language that turns text to diagrams.'';
            homepage = "https://github.com/terrastruct/d2-obsidian";
        };
    }
);
    "d3-tagvis" = (
    let
        baseUrl = "https://github.com/mason-bryant/Obsidian-Tagvis/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "d3-tagvis";
        version = "0.1.5";
        outputHash = "sha256-bb6Yuvx6cvlMkN5VlkEUSoEAmsmbw2NieW8dsUpX97E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize your tags as a configurable sunburst visualization.'';
            homepage = "https://github.com/mason-bryant/Obsidian-Tagvis";
        };
    }
);
    "daggerforge" = (
    let
        baseUrl = "https://github.com/Torutu/daggerforge/releases/download/2.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daggerforge";
        version = "2.1.10";
        outputHash = "sha256-0j7888Mm9DLkIKmzJLn6mc0Z0ZaqjQkOwWTbLNWTl2o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easy access to DaggerHeart"s Adversaries + creating your own.'';
            homepage = "https://github.com/Torutu/daggerforge";
        };
    }
);
    "daily-activity" = (
    let
        baseUrl = "https://github.com/trydalch/obsidian-daily-activity/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-activity";
        version = "1.0.0";
        outputHash = "sha256-UKuIOn2tpFrlU674y9GfV4ZXmGMP9L4ZOboHJYblhTc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Output a list of the files changed/created today in the active file.'';
            homepage = "https://github.com/trydalch/obsidian-daily-activity";
        };
    }
);
    "daily-news-briefing" = (
    let
        baseUrl = "https://github.com/ChenziqiAdam/Daily-News-Briefing/releases/download/1.11.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-news-briefing";
        version = "1.11.8";
        outputHash = "sha256-UG0STkPqZfFuD1qdcsho9UH3FXgQcMMgk5Ie2j5By0s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get AI-powered daily news summaries in your vault. Features customizable topics, smart filtering, and automated scheduling.'';
            homepage = "https://github.com/ChenziqiAdam/Daily-News-Briefing";
        };
    }
);
    "daily-note-automater" = (
    let
        baseUrl = "https://github.com/davidpedrero/obsidian-daily-notes-automater/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-automater";
        version = "1.0.0";
        outputHash = "sha256-iks4b6hQ742ITicdUf0acx01/+2CC9uEnE4T3nGre7A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automates the creation of a daily note'';
            homepage = "https://github.com/davidpedrero/obsidian-daily-notes-automater";
        };
    }
);
    "daily-note-calendar" = (
    let
        baseUrl = "https://github.com/bartkessels/daily-note-calendar/releases/download/2.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-calendar";
        version = "2.8.0";
        outputHash = "sha256-QULPDJbvwqOXYuKnF8ooB8vEfUzcOb/93Rw6xPbui78=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Navigate to your weekly- and daily-notes using a calendar view.'';
            homepage = "https://github.com/bartkessels/daily-note-calendar";
        };
    }
);
    "daily-note-collector" = (
    let
        baseUrl = "https://github.com/LA/obsidian-daily-note-collector/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-collector";
        version = "1.3.0";
        outputHash = "sha256-CUWbzA/9aeIM/6UnIX0O7dF9WLAU/kpmPN49OuuOqZA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds links to new notes to your daily note.'';
            homepage = "https://github.com/LA/obsidian-daily-note-collector";
        };
    }
);
    "daily-note-creator" = (
    let
        baseUrl = "https://github.com/mario-holubar/obsidian-daily-note-creator/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-creator";
        version = "1.0.0";
        outputHash = "sha256-Ai1Iz3IlFUdZGn6XEbQputpfWMvKrhgDWi2yfEJW+x8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically creates missing daily notes'';
            homepage = "https://github.com/mario-holubar/obsidian-daily-note-creator";
        };
    }
);
    "daily-note-navbar" = (
    let
        baseUrl = "https://github.com/karstenpedersen/obsidian-daily-note-navbar/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-navbar";
        version = "0.2.1";
        outputHash = "sha256-mMmWO6YKG9sec0/wzwHDgLVQvj9TN40jh+MrpOPyTA4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Navigate between sequential daily notes with ease.'';
            homepage = "https://github.com/karstenpedersen/obsidian-daily-note-navbar";
        };
    }
);
    "daily-note-pinner" = (
    let
        baseUrl = "https://github.com/lukemt/obsidian-daily-note-pinner/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-pinner";
        version = "1.1.0";
        outputHash = "sha256-/UiX7PYSfiAj1UCm4P7LS5AvRspB/KhXG02W0xaZQTM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pin the daily note of the present day. Unpin all daily notes of past and future days.'';
            homepage = "https://github.com/lukemt/obsidian-daily-note-pinner";
        };
    }
);
    "daily-note-structure" = (
    let
        baseUrl = "https://github.com/db-developer/daily-note-structure/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-note-structure";
        version = "1.4.0";
        outputHash = "sha256-w030dBhqZHC0fK4vCWlOZbY2l4A07B+CcmC7sY+BTtk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''One-Click create a structure for and including your daily notes.'';
            homepage = "https://github.com/db-developer/daily-note-structure";
        };
    }
);
    "daily-notes-editor" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Daily-Notes-Editor/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-notes-editor";
        version = "1.1.0";
        outputHash = "sha256-EuhXlgAexxyJxOHtiYoN7MVCCeqBR2rnNCrqZhbNRQM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit daily notes in one page (inline), which works similar to Roam Research"s default daily note view.'';
            homepage = "https://github.com/Quorafind/Obsidian-Daily-Notes-Editor";
        };
    }
);
    "daily-notes-tweaks" = (
    let
        baseUrl = "https://github.com/coignard/obsidian-daily-notes-tweaks/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-notes-tweaks";
        version = "1.0.1";
        outputHash = "sha256-AUKhZSRs+Vq70VlPmPj1J/6B4M0i621THutf6cFJSlU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a random daily note and automatically switch past daily notes to reading mode.'';
            homepage = "https://github.com/coignard/obsidian-daily-notes-tweaks";
        };
    }
);
    "daily-prompt" = (
    let
        baseUrl = "https://github.com/Erl-koenig/obsidian-dailyPrompt/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-prompt";
        version = "1.0.0";
        outputHash = "sha256-hSpq1rNuMFdSywt4gIsFUYr5CSBguq1Xm+5VCq8tQfs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set up custom prompts and automatically fill them into your daily notes.'';
            homepage = "https://github.com/Erl-koenig/obsidian-dailyPrompt";
        };
    }
);
    "daily-random-note" = (
    let
        baseUrl = "https://github.com/D4rkP1xel/daily-random-note/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-random-note";
        version = "1.0.5";
        outputHash = "sha256-lj4WF2anslDlr/ygfEaGVu8SnsPN5uMEc8JhtPBbxts=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically open daily random notes based on your preferences.'';
            homepage = "https://github.com/D4rkP1xel/daily-random-note";
        };
    }
);
    "daily-routine" = (
    let
        baseUrl = "https://github.com/sechan100/daily-routine-2/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-routine";
        version = "1.0.8";
        outputHash = "sha256-g4/vua/dgXJM/bZdUM/8Gh2C4Mz1hc1nugtZZYc+6yY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your daily tasks as "ROUTINES" and organize your everyday life more effectively.'';
            homepage = "https://github.com/sechan100/daily-routine-2";
        };
    }
);
    "daily-statistics" = (
    let
        baseUrl = "https://github.com/yefengr/obsidian-daily-statistics/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-statistics";
        version = "1.1.6";
        outputHash = "sha256-N5rlRJLS3Q3AcE4E6WNPU71B58UA2Wdn1/eF6IfnMag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Count the number of words written each day and display it on a calendar.'';
            homepage = "https://github.com/yefengr/obsidian-daily-statistics";
        };
    }
);
    "daily-summary" = (
    let
        baseUrl = "https://github.com/CSLukkun/ob_daily_summary/releases/download/0.0.22";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-summary";
        version = "0.0.22";
        outputHash = "sha256-9gEBimA4I4hakx7y4rdeLGgAO8F5qmUmCHbGi3BHhSE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use LLM to summarize what you did today.'';
            homepage = "https://github.com/CSLukkun/ob_daily_summary";
        };
    }
);
    "daily-task-auto-generator" = (
    let
        baseUrl = "https://github.com/maigamo/Daily-Task-Auto-Generator/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "daily-task-auto-generator";
        version = "1.0.2";
        outputHash = "sha256-A9vXXPi8zhJ9gdPiEMehtd14K8lH/k/3Q/vNSk/8/bU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Daily Task Auto Generator can helps you automatically generate daily task files with customizable templates.'';
            homepage = "https://github.com/maigamo/Daily-Task-Auto-Generator";
        };
    }
);
    "dangerous-mode" = (
    let
        baseUrl = "https://github.com/vanshkumar/dangerous-obsidian/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dangerous-mode";
        version = "1.1.4";
        outputHash = "sha256-OtIM7eAB89kan5D3hhGelvOIddiAfDgd6jqRx83lzn0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dangerous writing mode: keep typing or after 5 seconds of inactivity the current note is erased.'';
            homepage = "https://github.com/vanshkumar/dangerous-obsidian";
        };
    }
);
    "dangerzone-writing-plugin" = (
    let
        baseUrl = "https://github.com/akaalias/dangerzone-writing-plugin/releases/download/0.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dangerzone-writing-plugin";
        version = "0.0.11";
        outputHash = "sha256-48OeiCZS6sQ9Vxe4KSoQvoLjOSX+JbvyB3/LVYpyunE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''This plugin is dangerous! When you start it, you have to write without stopping for 100 seconds. If you stop, think and look around, after 3 seconds the plugin will DELETE what you"ve written in this note.'';
            homepage = "https://github.com/akaalias/dangerzone-writing-plugin";
        };
    }
);
    "darlal-switcher-plus" = (
    let
        baseUrl = "https://github.com/darlal/obsidian-switcher-plus/releases/download/6.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "darlal-switcher-plus";
        version = "6.1.1";
        outputHash = "sha256-TE0lOEZPORw4jl4fRRbrDeD03tOQCBghIRvEQ8yHYOc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhanced Quick Switcher, search open panels, and symbols.'';
            homepage = "https://github.com/darlal/obsidian-switcher-plus";
        };
    }
);
    "data-cards" = (
    let
        baseUrl = "https://github.com/Sophokles187/data-cards/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "data-cards";
        version = "1.1.0";
        outputHash = "sha256-Bvr5lQCO6TxLw+ZzUxH/4o1DbI0y1RSFEdF1whnVsJM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform Dataview query results into visually appealing, customizable card layouts.'';
            homepage = "https://github.com/Sophokles187/data-cards";
        };
    }
);
    "data-entry" = (
    let
        baseUrl = "https://github.com/waynevanson/data-entry-obsidian-plugin/releases/download/9.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "data-entry";
        version = "9.0.1";
        outputHash = "sha256-fhI5cyvQfSY0Zsy7YpjLRRps1WdLlCOlqUuw3B57WoE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create forms that save data simply; the data view of data entry.'';
            homepage = "https://github.com/waynevanson/data-entry-obsidian-plugin";
        };
    }
);
    "data-fetcher" = (
    let
        baseUrl = "https://github.com/qf3l3k/obsidian-data-fetcher/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "data-fetcher";
        version = "1.1.4";
        outputHash = "sha256-S9py0/vclGre28iQAtoE/GLki/Sg2H349bo3PVTnlo4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch data from multiple sources (REST APIs, RPC, gRPC, GraphQL) and insert results into notes'';
            homepage = "https://github.com/qf3l3k/obsidian-data-fetcher";
        };
    }
);
    "data-files-editor" = (
    let
        baseUrl = "https://github.com/ZukTol/obsidian-data-files-editor/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "data-files-editor";
        version = "1.3.0";
        outputHash = "sha256-gRv2DEYmypPA3txuXEo+ktfh2Ih8loS83gsg9/d/HAs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and edit .txt, .json and .xml files.'';
            homepage = "https://github.com/ZukTol/obsidian-data-files-editor";
        };
    }
);
    "datacore" = (
    let
        baseUrl = "https://github.com/blacksmithgu/datacore/releases/download/0.1.29";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "datacore";
        version = "0.1.29";
        outputHash = "sha256-/kXOL8XqJBzpTciY+w0+ov2QMFCTQNTOEK4f1KKSKlI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An even faster reactive query engine for the data obsessed.'';
            homepage = "https://github.com/blacksmithgu/datacore";
        };
    }
);
    "dataview" = (
    let
        baseUrl = "https://github.com/blacksmithgu/obsidian-dataview/releases/download/0.5.70";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dataview";
        version = "0.5.70";
        outputHash = "sha256-mBXQrFHSZaqj6P9ZGzGfw1UfnQKroTlkilI/IOl7Q+g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Advanced queries over your vault for the data-obsessed.'';
            homepage = "https://github.com/blacksmithgu/obsidian-dataview";
        };
    }
);
    "dataview-autocompletion" = (
    let
        baseUrl = "https://github.com/dnlbauer/obsidian-dataview-autocompletion/releases/download/0.9.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dataview-autocompletion";
        version = "0.9.5";
        outputHash = "sha256-w0zVsBPl4EnTykDVGq2C39X8z1et3LYjBYFJDt+m7ic=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds autocompletion to Dataview metadata fields'';
            homepage = "https://github.com/dnlbauer/obsidian-dataview-autocompletion";
        };
    }
);
    "dataview-properties" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-dataview-properties/releases/download/0.0.25-0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dataview-properties";
        version = "0.0.25-0";
        outputHash = "sha256-1EtHIb2bfgP8NHxS5nhetEhhnxdoaqy0kxNez/nLhqc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automagically copy dataview inline field (and their values, even calculated!) into frontmatter properties and keep them sync.'';
            homepage = "https://github.com/Mara-Li/obsidian-dataview-properties";
        };
    }
);
    "dataview-publisher" = (
    let
        baseUrl = "https://github.com/udus122/dataview-publisher/releases/download/0.3.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dataview-publisher";
        version = "0.3.10";
        outputHash = "sha256-L/DTxlpz/4g8ouCJKYwCXHxNnVeImyaHI80TPVEr4vg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Output markdown from your Dataview queries and keep them up to date. You can also publish them.'';
            homepage = "https://github.com/udus122/dataview-publisher";
        };
    }
);
    "dataview-serializer" = (
    let
        baseUrl = "https://github.com/dsebastien/obsidian-dataview-serializer/releases/download/2.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dataview-serializer";
        version = "2.6.0";
        outputHash = "sha256-MovbGzpVv8qcZUifyLhBnuKv2VoaH1b0SQs7qAoNbkE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Serialize Dataview queries to Markdown, and keep the Markdown representation up to date'';
            homepage = "https://github.com/dsebastien/obsidian-dataview-serializer";
        };
    }
);
    "date-inserter" = (
    let
        baseUrl = "https://github.com/namikaze-40p/obsidian-date-inserter/releases/download/0.6.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "date-inserter";
        version = "0.6.8";
        outputHash = "sha256-khFZs7ujly+WCg1LdQE6cdeOYpdvN/7pF8B+jy8Y8Dg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert a date at the cursor position using a calendar.'';
            homepage = "https://github.com/namikaze-40p/obsidian-date-inserter";
        };
    }
);
    "date-range-expander" = (
    let
        baseUrl = "https://github.com/mildeveloper/obsidian-date-range-expander/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "date-range-expander";
        version = "1.0.2";
        outputHash = "sha256-QPsyxvA4fBWnIQhG3/oQ3Wc02NIlEouIzcsFpB9DNQ4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly add a range of day references given a date duration.'';
            homepage = "https://github.com/mildeveloper/obsidian-date-range-expander";
        };
    }
);
    "datepicker" = (
    let
        baseUrl = "https://github.com/joycode-hub/datepicker-plugin/releases/download/0.3.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "datepicker";
        version = "0.3.25";
        outputHash = "sha256-EfA9IbnVdwjW2LzP2Jo4UkVkYYSd3MhMsYuCAkJhJTE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use a date picker to modify and insert date/time anywhere in your markdown notes.'';
            homepage = "https://github.com/joycode-hub/datepicker-plugin";
        };
    }
);
    "datetime-language-changer" = (
    let
        baseUrl = "https://github.com/ZetabS/datetime-language-changer/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "datetime-language-changer";
        version = "1.0.2";
        outputHash = "sha256-WT0H6XQA4De8WKti1feL7mwRnrdQi6+YyvtYyhoe5IQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize the language used for datetime formatting by changing moment.js"s locale.'';
            homepage = "https://github.com/ZetabS/datetime-language-changer";
        };
    }
);
    "day-one-importer" = (
    let
        baseUrl = "https://github.com/MarcDonald/obsidian-day-one-importer/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "day-one-importer";
        version = "1.3.2";
        outputHash = "sha256-A3CRlONW/vzGlwswm/NXiMwB6JPLNoZ2EqyidFRZAFo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Day One journals.'';
            homepage = "https://github.com/MarcDonald/obsidian-day-one-importer";
        };
    }
);
    "day-planner-og" = (
    let
        baseUrl = "https://github.com/ebullient/obsidian-day-planner-og/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "day-planner-og";
        version = "1.0.0";
        outputHash = "sha256-6RrrVdl94AyHSz1ku22FcyG9buci4DSB5eIPcK6icQY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Day planning from a simple task list in a Markdown note (bare bones, preserves the features and behavior of the original plugin).'';
            homepage = "https://github.com/ebullient/obsidian-day-planner-og";
        };
    }
);
    "days-since" = (
    let
        baseUrl = "https://github.com/gndclouds/days-since-obsidian/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "days-since";
        version = "2.0.3";
        outputHash = "sha256-/gD4ZNIaNAqUlQS+3k7N01mguJoNVHm5k1FiFG4MieQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Relive and celebrate your life"s milestones on a personal, interactive timeline. A nostalgic journey through your history with anniversary reminders and cherished memories.'';
            homepage = "https://github.com/gndclouds/days-since-obsidian";
        };
    }
);
    "decks" = (
    let
        baseUrl = "https://github.com/dscherdi/decks/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "decks";
        version = "1.7.0";
        outputHash = "sha256-yMBHCUDJJ/b3upXxQAMuRa+5ARnKw4yo3mXIAvXFf/o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Decks - Spaced repetition flashcards plugin with FSRS algorithm.'';
            homepage = "https://github.com/dscherdi/decks";
        };
    }
);
    "deep-sit" = (
    let
        baseUrl = "https://github.com/bassio/obsidian-deepsit/releases/download/0.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "deep-sit";
        version = "0.2.4";
        outputHash = "sha256-7tBMjlgqzILiELO7A16l5SYskcBUdZ91EGbJB0G0Mv0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Focused study and research tool that allows citing and reviewing primary sources through a Zotero integration.'';
            homepage = "https://github.com/bassio/obsidian-deepsit";
        };
    }
);
    "deepl" = (
    let
        baseUrl = "https://github.com/friebetill/obsidian-deepl/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "deepl";
        version = "1.0.7";
        outputHash = "sha256-iU8lNzZc7Ai/5WycdDdNSv07h3Crob+0Ilqj9meDMN8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Translate selected text into more than 25 languages with DeepL.'';
            homepage = "https://github.com/friebetill/obsidian-deepl";
        };
    }
);
    "deepseek-ai-assistant" = (
    let
        baseUrl = "https://github.com/mali-i/deepseek-ai-assistant/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "deepseek-ai-assistant";
        version = "2.3.0";
        outputHash = "sha256-pQosVZV56jx4SnKb1YPTAvSg8GS7ZiUyoPb2Hc9coWk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Help you to study with ai-prompting'';
            homepage = "https://github.com/mali-i/deepseek-ai-assistant";
        };
    }
);
    "default-query-in-backlink" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-default-query-in-backlink/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "default-query-in-backlink";
        version = "0.1.0";
        outputHash = "sha256-33c//0ZNUPoWWRZNFqoUCSTa8QOJry1XsGMn8zuWXaw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically input default query in search input of backlinks in document.'';
            homepage = "https://github.com/Benature/obsidian-default-query-in-backlink";
        };
    }
);
    "default-template" = (
    let
        baseUrl = "https://github.com/raeperd/obsidian-default-template/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "default-template";
        version = "1.2.4";
        outputHash = "sha256-D9abxPXpALVq7XyhFSzBEqXcz1/ErLCI7UL55waE8Mw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically apply templates to new notes with user-configurable template selection.'';
            homepage = "https://github.com/raeperd/obsidian-default-template";
        };
    }
);
    "definition-list" = (
    let
        baseUrl = "https://github.com/shammond42/definition-list/releases/download/0.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "definition-list";
        version = "0.2.5";
        outputHash = "sha256-aOyCi/t/AgFemGsmJANz8Dr8g68qoN6NJoce1+QDoxo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds HTML definition list support.'';
            homepage = "https://github.com/shammond42/definition-list";
        };
    }
);
    "dendron-tree" = (
    let
        baseUrl = "https://github.com/levirs565/obsidian-dendron-tree/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dendron-tree";
        version = "1.3.0";
        outputHash = "sha256-2qxVtlipkiJ/CIzTELw64tBpUSMAdZlsIvcj9kwOeXE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add tree for exploring Dendron note.'';
            homepage = "https://github.com/levirs565/obsidian-dendron-tree";
        };
    }
);
    "desci" = (
    let
        baseUrl = "https://github.com/Obsidian-Desci/Obsidian-Desci/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "desci";
        version = "1.0.0";
        outputHash = "sha256-yo0ty2XmvDOZalseeIImHHyhrQN5AMMeMIdQucmxGRo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Web3, IPFS, and Desci integrations.'';
            homepage = "https://github.com/Obsidian-Desci/Obsidian-Desci";
        };
    }
);
    "desk" = (
    let
        baseUrl = "https://github.com/davidlandry93/obsidian-desk/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "desk";
        version = "1.0.6";
        outputHash = "sha256-P96DFaKZaf8dlyeQzSEuIPdSxV4UUS2JPpdGL9jh09E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A desk to see notes at a glance. Requires Dataview.'';
            homepage = "https://github.com/davidlandry93/obsidian-desk";
        };
    }
);
    "devops-companion" = (
    let
        baseUrl = "https://github.com/jkom4/obsidian-devops-compagnon/releases/download/0.12.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "devops-companion";
        version = "0.12.3";
        outputHash = "sha256-CKWSCf2bm1w/fSpM5MNMyGT2rDqAqOOP3z+kwi8gOXA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Document infrastructure as clearly as you build it.'';
            homepage = "https://github.com/jkom4/obsidian-devops-compagnon";
        };
    }
);
    "diagram-zoom-drag" = (
    let
        baseUrl = "https://github.com/Ssentiago/image-zoom-drag/releases/download/6.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "diagram-zoom-drag";
        version = "6.2.0";
        outputHash = "sha256-9U3W3otujkBWMVR0O2mqoyG3BVJEKEqr6wdyje1Lwiw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make any image or diagram interactive with zoom, drag, and control panels'';
            homepage = "https://github.com/Ssentiago/image-zoom-drag";
        };
    }
);
    "dialogue-mode" = (
    let
        baseUrl = "https://github.com/patrickchiang/obsidian-dialogue-mode/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dialogue-mode";
        version = "1.1.0";
        outputHash = "sha256-99zsg7LfpulSu0bzqjIy/piLzGFtJcjkL3W4Fl33btw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dialogue mode for editing speech in writing.'';
            homepage = "https://github.com/patrickchiang/obsidian-dialogue-mode";
        };
    }
);
    "diarian" = (
    let
        baseUrl = "https://github.com/Erallie/diarian/releases/download/1.2.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "diarian";
        version = "1.2.25";
        outputHash = "sha256-paUNRVTznleVU7hBgHLh5/Fy0sXuFojLlFMdzFbHGRs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''All-in-one journaling toolkit.'';
            homepage = "https://github.com/Erallie/diarian";
        };
    }
);
    "diary-ics" = (
    let
        baseUrl = "https://github.com/mousebomb/obsidian-diary-ics/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "diary-ics";
        version = "1.0.11";
        outputHash = "sha256-tdIOSnvfMfC3zFTbb71AazJDwz3fZX/SCUx4U0eH/74=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync diary entries to system calendar via ICS feed.'';
            homepage = "https://github.com/mousebomb/obsidian-diary-ics";
        };
    }
);
    "dictionary-translator" = (
    let
        baseUrl = "https://github.com/grover572/obsidian-Dictionary-translator/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dictionary-translator";
        version = "1.0.4";
        outputHash = "sha256-vmvkf7tm70RRCyj+Oo6xvijq3iuS8O7ErrW25mVRW4o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps you to quickly underline word translations and insert word cards in your notes.'';
            homepage = "https://github.com/grover572/obsidian-Dictionary-translator";
        };
    }
);
    "diffzip" = (
    let
        baseUrl = "https://github.com/vrtmrz/diffzip/releases/download/0.0.20";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "diffzip";
        version = "0.0.20";
        outputHash = "sha256-z4/zdLWD+dhszeDBZRgREFWA7RGmKS8odJG4BX3obXA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Back our vault up with lesser storage.'';
            homepage = "https://github.com/vrtmrz/diffzip";
        };
    }
);
    "digital-paper" = (
    let
        baseUrl = "https://github.com/danferns/digital-paper-obsidian-plugin/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "digital-paper";
        version = "1.0.4";
        outputHash = "sha256-TALyR2o7P2KfE831CC0SRYbIn9zfQ1TejupkZ/cUUjY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''turn off backspace and undo, just like writing with a pen on real paper.'';
            homepage = "https://github.com/danferns/digital-paper-obsidian-plugin";
        };
    }
);
    "digitalgarden" = (
    let
        baseUrl = "https://github.com/oleeskild/obsidian-digital-garden/releases/download/2.76.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "digitalgarden";
        version = "2.76.4";
        outputHash = "sha256-ZkpQhP/qSbzoT2Zync0GaEQt6YQfFBECT1OeXxafofE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes to a digital garden for others to enjoy.'';
            homepage = "https://github.com/oleeskild/obsidian-digital-garden";
        };
    }
);
    "disable-tabs" = (
    let
        baseUrl = "https://github.com/davidvkimball/obsidian-disable-tabs/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "disable-tabs";
        version = "1.0.10";
        outputHash = "sha256-Trpo3NakORh2sy40j1PPXmnhNahwUgF70PkMVoQ8tNI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Disables having more than one tab open at a time.'';
            homepage = "https://github.com/davidvkimball/obsidian-disable-tabs";
        };
    }
);
    "disciples-journal" = (
    let
        baseUrl = "https://github.com/scottTomaszewski/obsidian-disciples-journal/releases/download/0.12.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "disciples-journal";
        version = "0.12.0";
        outputHash = "sha256-SLIiifx1IzPo1ST4N0P2HoxcyNMknio8LrGCOFFnSSI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed Bible references and passages in your notes and read the Bible.'';
            homepage = "https://github.com/scottTomaszewski/obsidian-disciples-journal";
        };
    }
);
    "discord-message-sender" = (
    let
        baseUrl = "https://github.com/okawak/discord_message_sender/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "discord-message-sender";
        version = "0.2.8";
        outputHash = "sha256-z0GW/d6PeOUhANLSoNiNHW7iuM50vfqcmVf0GnYvMag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send messages from a Discord channel to your Vault'';
            homepage = "https://github.com/okawak/discord_message_sender";
        };
    }
);
    "discord-timestamps" = (
    let
        baseUrl = "https://github.com/Erallie/discord-timestamps/releases/download/1.0.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "discord-timestamps";
        version = "1.0.19";
        outputHash = "sha256-Q4HpUt9nJOcaH7CBBjfdWPblh0NWg0UobNAAsOGArZI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays discord timestamps in read mode as they would appear in Discord.'';
            homepage = "https://github.com/Erallie/discord-timestamps";
        };
    }
);
    "discordian-plugin" = (
    let
        baseUrl = "https://github.com/radekkozak/discordian-plugin/releases/download/0.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "discordian-plugin";
        version = "0.7.1";
        outputHash = "sha256-mAmZ9B7qnOY3pOrhrkQh4Dmsnh0BcMjBqZRMPpqWncg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fine-grained control of Discordian Theme.'';
            homepage = "https://github.com/radekkozak/discordian-plugin";
        };
    }
);
    "discrete" = (
    let
        baseUrl = "https://github.com/shkarlsson/obsidian-discrete/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "discrete";
        version = "0.2.0";
        outputHash = "sha256-qH9HTYsPx7iYgagncHjstusL6E4ssgEkpN7MXlWNDvQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Filter in Explorer based on file frontmatter metadata'';
            homepage = "https://github.com/shkarlsson/obsidian-discrete";
        };
    }
);
    "disk-usage" = (
    let
        baseUrl = "https://github.com/Promptier/disk-usage/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "disk-usage";
        version = "1.0.0";
        outputHash = "sha256-5QWmE6L0WEXRF6zlf3vW7SQJRKLmPutSuvqknp/aCUs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Measure disk usage for tracking size of folders and file types.'';
            homepage = "https://github.com/Promptier/disk-usage";
        };
    }
);
    "display-relative-path-img" = (
    let
        baseUrl = "https://github.com/dyc2424748461/obsidian-display-relative-path-img/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "display-relative-path-img";
        version = "0.0.4";
        outputHash = "sha256-EyerOdBxqB/yHONLN15NTvFTvBbG0nmdqe3Tx29ZYG8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the image of the <img> tag'';
            homepage = "https://github.com/dyc2424748461/obsidian-display-relative-path-img";
        };
    }
);
    "dmn-eval" = (
    let
        baseUrl = "https://github.com/joleaf/obsidian-dmn-eval-plugin/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dmn-eval";
        version = "0.5.0";
        outputHash = "sha256-jJAsatls5MW/QiiE2+4cB710nsKBhBQMLnCZ73ejkF4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable evaluating/executing DMNs.'';
            homepage = "https://github.com/joleaf/obsidian-dmn-eval-plugin";
        };
    }
);
    "dmn-plugin" = (
    let
        baseUrl = "https://github.com/joleaf/obsidian-dmn-plugin/releases/download/0.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dmn-plugin";
        version = "0.5.1";
        outputHash = "sha256-0gF07hSh2YaK7auPiKUyzGaJ05AQU4QNGLv1B9C46Cc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable viewing DMNs using dmn-js.'';
            homepage = "https://github.com/joleaf/obsidian-dmn-plugin";
        };
    }
);
    "docbase-unofficial" = (
    let
        baseUrl = "https://github.com/kuvanov-2/obsidian-docbase/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "docbase-unofficial";
        version = "1.0.0";
        outputHash = "sha256-0tC6THAR5F3A0mEgf6MiFr7itl8p9Tx80/hWzJSaytA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pull and push notes to DocBase'';
            homepage = "https://github.com/kuvanov-2/obsidian-docbase";
        };
    }
);
    "docx-exporter" = (
    let
        baseUrl = "https://github.com/kanshurichard/obsidian-docx-exporter/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "docx-exporter";
        version = "1.0.2";
        outputHash = "sha256-UttPOR6KUhUs2d3C3qDbj+cQEVZleG+aFeZ4/zHsW0k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export notes to Microsoft Word docx files with mobile devices support.'';
            homepage = "https://github.com/kanshurichard/obsidian-docx-exporter";
        };
    }
);
    "docxer" = (
    let
        baseUrl = "https://github.com/Developer-Mike/obsidian-docxer/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "docxer";
        version = "2.3.0";
        outputHash = "sha256-DPEA6TugsRitgq2gWe1Pm7Tcudk+YEYpkwu9oH3j5Hg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Word files easily. Adds a preview mode for .docx files and the ability to convert them to markdown (.md) files.'';
            homepage = "https://github.com/Developer-Mike/obsidian-docxer";
        };
    }
);
    "doing" = (
    let
        baseUrl = "https://github.com/Rooyca/doing/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "doing";
        version = "1.0.3";
        outputHash = "sha256-3gH0kLr+UqY+lWvHNn2sm4aM05PA9FgJ/OB0jIJl+D8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''It helps you remember what you were doing.'';
            homepage = "https://github.com/Rooyca/doing";
        };
    }
);
    "double-click-image-opener" = (
    let
        baseUrl = "https://github.com/atman-33/double-click-image-opener/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "double-click-image-opener";
        version = "1.0.1";
        outputHash = "sha256-e+FQoxWkIyOf37xvayZyiOlA3JOo6tRIEfZYSsNkDso=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open images with your system"s default application by double-clicking.'';
            homepage = "https://github.com/atman-33/double-click-image-opener";
        };
    }
);
    "double-colon-conceal" = (
    let
        baseUrl = "https://github.com/msrch/obsidian-double-colon-conceal/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "double-colon-conceal";
        version = "1.3.1";
        outputHash = "sha256-PVwai1xqSWThMnY91HEuDYTOCXjM4qyoMdzLkxhs+3Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display double colon (i.e. Dataview inline fields) as a single colon for more natural reading experience.'';
            homepage = "https://github.com/msrch/obsidian-double-colon-conceal";
        };
    }
);
    "double-row-toolbar" = (
    let
        baseUrl = "https://github.com/lorens-osman-dev/double-row-toolbar/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "double-row-toolbar";
        version = "1.0.2";
        outputHash = "sha256-nlIai8j+N3T9h7T+a9gY6XfOQUceT5YFY5fR7OGvYbU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a second row to the toolbar on mobile devices, allowing for more quick access buttons.'';
            homepage = "https://github.com/lorens-osman-dev/double-row-toolbar";
        };
    }
);
    "double-switch" = (
    let
        baseUrl = "https://github.com/carlrobert/double-switch/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "double-switch";
        version = "0.7.0";
        outputHash = "sha256-w60z+oDK2nW80vVsF4fyUj0fZ2OiOkBCGaXyJU+sHO8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggling dark/light mode switches themes automatically'';
            homepage = "https://github.com/carlrobert/double-switch";
        };
    }
);
    "downloadpdf" = (
    let
        baseUrl = "https://github.com/frieda21/downloadPDF/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "downloadpdf";
        version = "1.2.6";
        outputHash = "sha256-vzFmHtPxqQZ/W7Z+9o+h5DHMSeZa3jaVC5x/y+LLRmE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to download all included PDF files.'';
            homepage = "https://github.com/frieda21/downloadPDF";
        };
    }
);
    "draft-indicator" = (
    let
        baseUrl = "https://github.com/beardicus/obsidian-draft-indicator-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "draft-indicator";
        version = "1.0.0";
        outputHash = "sha256-fkGJIr9G8o1gTDRGYjGu+7ylUZjKO/bQszmwtMNIMRU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show draft status in the file explorer.'';
            homepage = "https://github.com/beardicus/obsidian-draft-indicator-plugin";
        };
    }
);
    "drag-to-scroll" = (
    let
        baseUrl = "https://github.com/constsz/obsidian-drag-to-scroll/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "drag-to-scroll";
        version = "1.0.2";
        outputHash = "sha256-MyJrwMo1QFleeIJQ58oj6D5ePdTAia3Vou4iph8xCLA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Drag to scroll - just like on a touch device!'';
            homepage = "https://github.com/constsz/obsidian-drag-to-scroll";
        };
    }
);
    "draw-harada-method" = (
    let
        baseUrl = "https://github.com/yildbs/obsidian-harada-method-plugin/releases/download/1.0.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "draw-harada-method";
        version = "1.0.14";
        outputHash = "sha256-40760uUsi12MTWYp+Bmwmvr8uFRwQIwffuM+Lxvp1tI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Draw the harada method. Create your own 1 goal, 8 plans, and 64 actions!'';
            homepage = "https://github.com/yildbs/obsidian-harada-method-plugin";
        };
    }
);
    "draw-steel-elements" = (
    let
        baseUrl = "https://github.com/SteelCompendium/draw-steel-elements/releases/download/5.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "draw-steel-elements";
        version = "5.1.1";
        outputHash = "sha256-BvS70uo+Y5idA33UvXxKRxFg32aT1bDnaZEK9TRy3HQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Components to support the Draw Steel TTRPG by MCDM.'';
            homepage = "https://github.com/SteelCompendium/draw-steel-elements";
        };
    }
);
    "draw-steel-retainer" = (
    let
        baseUrl = "https://github.com/MartinDampier/draw-steel-retainer/releases/download/1.0.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "draw-steel-retainer";
        version = "1.0.19";
        outputHash = "sha256-s968bXOdH8NUqkYIOaRdMDF7xT78i5+JFGgvpMvXPgY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides Director facing sidebar tools for the TTRPG Draw Steel'';
            homepage = "https://github.com/MartinDampier/draw-steel-retainer";
        };
    }
);
    "drawio-obsidian" = (
    let
        baseUrl = "https://github.com/zapthedingbat/drawio-obsidian/releases/download/1.5.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "drawio-obsidian";
        version = "1.5.4";
        outputHash = "sha256-lI4lXiDkdT8TK/cyZO6eT22XJ8lswW6m1loLSSiXYX0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and edit Draw.io diagrams.'';
            homepage = "https://github.com/zapthedingbat/drawio-obsidian";
        };
    }
);
    "dropbox-photo-grid" = (
    let
        baseUrl = "https://github.com/alimoeeny/obsidian-dropbox-photo-grid/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dropbox-photo-grid";
        version = "1.0.8";
        outputHash = "sha256-Peju5bgEa2YX1TqitD5AAjPhFxEd8Giu1HbTeaZ0GjA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch images from your dropbox based on date or path'';
            homepage = "https://github.com/alimoeeny/obsidian-dropbox-photo-grid";
        };
    }
);
    "due-when" = (
    let
        baseUrl = "https://github.com/andrewbaxter439/due-when/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "due-when";
        version = "1.0.8";
        outputHash = "sha256-G5oqmlH6W855hsG4G44nFgFLtosEDY61nHGw3qZwKrk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add shortcuts to insert due dates for end of this week or end of next week.'';
            homepage = "https://github.com/andrewbaxter439/due-when";
        };
    }
);
    "duplicate-detector" = (
    let
        baseUrl = "https://github.com/Wishmater/obsidian-plugin-duplicate-detector/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "duplicate-detector";
        version = "0.0.3";
        outputHash = "sha256-hJT49b2XjsK+wLo8XbQO14GsOwhgfLODq3vCPTuHHPY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlights duplicate lines in the open note.'';
            homepage = "https://github.com/Wishmater/obsidian-plugin-duplicate-detector";
        };
    }
);
    "duplicate-line" = (
    let
        baseUrl = "https://github.com/msztolcman/obsidian-duplicate-line/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "duplicate-line";
        version = "1.0.0";
        outputHash = "sha256-fY+MrbcSB6bb7q8CaOg9PSIKtjiwvA+yf88fOe4k43Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Duplicate line or selection with single hotkey.'';
            homepage = "https://github.com/msztolcman/obsidian-duplicate-line";
        };
    }
);
    "dust-calendar" = (
    let
        baseUrl = "https://github.com/a-nano-dust/dust-obsidian-calendar/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dust-calendar";
        version = "1.4.0";
        outputHash = "sha256-RJsJoC2+oQSkrkVezyW05572E+uNTxxUMNsuzKSOhh8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''更符合中国习惯的日历，可以显示农历、节气、节假日、调休信息，支持月视图和年视图切换，支持关联创建周期性笔记。'';
            homepage = "https://github.com/a-nano-dust/dust-obsidian-calendar";
        };
    }
);
    "dynamic-line-height-cjk" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-dynamic-line-height-cjk/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dynamic-line-height-cjk";
        version = "0.2.8";
        outputHash = "sha256-ydhrmstShH11BcSfcPpXheMgURf4f+tdx0g7TwlCW5E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamically adjust line height for lines & paragraphs containing CJK characters.'';
            homepage = "https://github.com/RyotaUshio/obsidian-dynamic-line-height-cjk";
        };
    }
);
    "dynamic-outline" = (
    let
        baseUrl = "https://github.com/theopavlove/obsidian-dynamic-outline/releases/download/1.19.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dynamic-outline";
        version = "1.19.0";
        outputHash = "sha256-MWDxABqnH2MDYKIVY02J6KTzCRo7k3Oqu7w9Ex0ZNOk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a customizable GitHub-like floating table of contents.'';
            homepage = "https://github.com/theopavlove/obsidian-dynamic-outline";
        };
    }
);
    "dynamic-text-concealer" = (
    let
        baseUrl = "https://github.com/mattcoleanderson/obsidian-dynamic-text-concealer/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dynamic-text-concealer";
        version = "1.1.1";
        outputHash = "sha256-tN0h7oTmxozKAvdhtScgTABA748QbhkrxptLYvvCW1Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Conceal or replace user-configured text patterns in Live Preview and reading mode.'';
            homepage = "https://github.com/mattcoleanderson/obsidian-dynamic-text-concealer";
        };
    }
);
    "dynamic-timetable" = (
    let
        baseUrl = "https://github.com/L7Cy/obsidian-dynamic-timetable/releases/download/4.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "dynamic-timetable";
        version = "4.6.1";
        outputHash = "sha256-22lQO8v9/J0zqh0RTTZhQtAp9vsBBGfmLLS8F0amwRE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Calculate the estimated time of completion from the estimated time of the task and dynamically create a timetable.'';
            homepage = "https://github.com/L7Cy/obsidian-dynamic-timetable";
        };
    }
);
    "e-daiary" = (
    let
        baseUrl = "https://github.com/wholetomy/obsidian-e-daiary/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "e-daiary";
        version = "1.0.2";
        outputHash = "sha256-c9kVl8jFwLRoJVvlDrFpIiUb/zql+d7AF2EFOk3iazk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates directories and notes based on the day of the year.'';
            homepage = "https://github.com/wholetomy/obsidian-e-daiary";
        };
    }
);
    "e-upload" = (
    let
        baseUrl = "https://github.com/Appleec/e-obsidian-upload-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "e-upload";
        version = "1.0.2";
        outputHash = "sha256-DHfNoBa4ZKobZ3ZibBW84qtPJo7ZUqp1jHydPIp1A2A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Support uploading files to different platforms.'';
            homepage = "https://github.com/Appleec/e-obsidian-upload-plugin";
        };
    }
);
    "ear-training" = (
    let
        baseUrl = "https://github.com/shiwer/ear-training-obsidian-plugin/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ear-training";
        version = "0.0.4";
        outputHash = "sha256-5Lsb2RLRYf64L7zKczGB3SNe+Xht4JKqP5KU4mcSFf8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get ear training exercises inside your vault.'';
            homepage = "https://github.com/shiwer/ear-training-obsidian-plugin";
        };
    }
);
    "easy-bake" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-easy-bake/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-bake";
        version = "1.1.0";
        outputHash = "sha256-zlQvIiuxjrS2OQkV6xAazDnmaGi5zQkvCPCk6GNPn7s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily compile many notes down to a single file.'';
            homepage = "https://github.com/obsidian-community/obsidian-easy-bake";
        };
    }
);
    "easy-copy" = (
    let
        baseUrl = "https://github.com/Moyf/easy-copy/releases/download/1.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-copy";
        version = "1.5.3";
        outputHash = "sha256-mdUQgtLFuxhCgGE/7Ya7cBeoVKSZPJNmULVGGqZIShM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily copy the text within inline code, bold text (and many other formats), or quickly generate an elegant link to a heading or block.'';
            homepage = "https://github.com/Moyf/easy-copy";
        };
    }
);
    "easy-fonts" = (
    let
        baseUrl = "https://github.com/jose-elias-alvarez/obsidian-easy-fonts/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-fonts";
        version = "1.0.0";
        outputHash = "sha256-IETD37P/XBWb4NDHZd9G3ReYPspJEuUyI0ZxP48qvjw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily load custom fonts, even on mobile.'';
            homepage = "https://github.com/jose-elias-alvarez/obsidian-easy-fonts";
        };
    }
);
    "easy-keep-view" = (
    let
        baseUrl = "https://github.com/tazihad/obsidian-easy-keep-view/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-keep-view";
        version = "1.0.1";
        outputHash = "sha256-4Dp+FS+O7MBd2dgh+q3f0ve82+/FDmJLzvgYZPPjcMQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easy Keep View mimics the Google Keep interface, allowing you to view and manage your notes with image thumbnails and excerpts.'';
            homepage = "https://github.com/tazihad/obsidian-easy-keep-view";
        };
    }
);
    "easy-test" = (
    let
        baseUrl = "https://github.com/forrest1398/obsidian-easyTest-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-test";
        version = "1.2.0";
        outputHash = "sha256-KELE5NdLvqDf5i/24wzDSVdB2Jztt16DQeVGcrwy1Qo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily create simple tests'';
            homepage = "https://github.com/forrest1398/obsidian-easyTest-plugin";
        };
    }
);
    "easy-timeline" = (
    let
        baseUrl = "https://github.com/Romelium/obsidian-easy-timeline/releases/download/2.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-timeline";
        version = "2.2.1";
        outputHash = "sha256-iIzPaF7sVWPT0KqTEHEeWrxzTzd3mFg48H5XdDhwzV8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''This simplifies creating and visualizing timelines from text using dates and metadata.'';
            homepage = "https://github.com/Romelium/obsidian-easy-timeline";
        };
    }
);
    "easy-tracker" = (
    let
        baseUrl = "https://github.com/hunter-ji/obsidian-easy-tracker/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-tracker";
        version = "1.0.6";
        outputHash = "sha256-X6SyHA/1aIj27ax1hJVslA8KRKflzhA1mKQe/vjgmCo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Instantly track goals and habits. Simple, beautiful, and configuration-free.'';
            homepage = "https://github.com/hunter-ji/obsidian-easy-tracker";
        };
    }
);
    "easy-typing-obsidian" = (
    let
        baseUrl = "https://github.com/Yaozhuwa/easy-typing-obsidian/releases/download/6.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easy-typing-obsidian";
        version = "6.0.7";
        outputHash = "sha256-+TI1n3RSteickc2QP819E/ZQXoI8urd0jKVx8kWfJuc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto format when typing.'';
            homepage = "https://github.com/Yaozhuwa/easy-typing-obsidian";
        };
    }
);
    "easylink" = (
    let
        baseUrl = "https://github.com/isitwho/EasyLink/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "easylink";
        version = "1.2.1";
        outputHash = "sha256-PpL00wLxVvU/iyTvb/imWIDY/OU7R/KnVlr2G0BPODw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Select text in your editor to find the most similar content from other notes and easily create links.'';
            homepage = "https://github.com/isitwho/EasyLink";
        };
    }
);
    "eccirian" = (
    let
        baseUrl = "https://github.com/Enthalpiex/Eccirian-Encrypt/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "eccirian";
        version = "1.0.13";
        outputHash = "sha256-A2N7+fXh6iNVa4QhFWDSpJ0PQgZrGTvuQg0vjHZPMwc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Next-generation file encryption solution based on modern cryptography.'';
            homepage = "https://github.com/Enthalpiex/Eccirian-Encrypt";
        };
    }
);
    "edge-tts" = (
    let
        baseUrl = "https://github.com/travisvn/obsidian-edge-tts/releases/download/3.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "edge-tts";
        version = "3.4.3";
        outputHash = "sha256-ZkdlPuynRPh9ANes7eGpsq1B4YWfnHes9OgX7nheBPk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Read notes aloud using Microsoft Edge Read Aloud API (free, high quality text-to-speech).'';
            homepage = "https://github.com/travisvn/obsidian-edge-tts";
        };
    }
);
    "edit-gemini" = (
    let
        baseUrl = "https://github.com/Basil-Mori/obsidian-edit-gemini/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "edit-gemini";
        version = "1.0.2";
        outputHash = "sha256-aQnIaW/xZqibv/FPNtlzIuhyVlvEV+yWykkz1NLB7qQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit and create .gmi files.'';
            homepage = "https://github.com/Basil-Mori/obsidian-edit-gemini";
        };
    }
);
    "edit-history" = (
    let
        baseUrl = "https://github.com/antoniotejada/obsidian-edit-history/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "edit-history";
        version = "0.3.0";
        outputHash = "sha256-Njywev0c15b0oOOalm/DCM3IayKi6X5PbBd4Q6p/7PQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep an automatic edit history of your notes, review the history, diff against or restore previous edits.'';
            homepage = "https://github.com/antoniotejada/obsidian-edit-history";
        };
    }
);
    "edit-in-neovim" = (
    let
        baseUrl = "https://github.com/TheseusGrey/edit-in-neovim/releases/download/1.8.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "edit-in-neovim";
        version = "1.8.5";
        outputHash = "sha256-YaNEwv1amxqSXLZgEuCC23GIewIMlnH+rBIXCXNVSdc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a Neovim buffer for the currently open file'';
            homepage = "https://github.com/TheseusGrey/edit-in-neovim";
        };
    }
);
    "edit-link-alias" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-edit-link-alias/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "edit-link-alias";
        version = "1.0.4";
        outputHash = "sha256-vaMSB0ZwS62YNFg0r5xdxB1AFyEEGDm0JduJU9AvYpo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds edit link alias command.'';
            homepage = "https://github.com/mnaoumov/obsidian-edit-link-alias";
        };
    }
);
    "edit-mdx" = (
    let
        baseUrl = "https://github.com/timppeters/obsidian-edit-mdx/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "edit-mdx";
        version = "1.0.1";
        outputHash = "sha256-M40H8xty9mYu5EcSQXGVcHgbgcK+LXbL+odjamtKuos=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit and create .mdx files.'';
            homepage = "https://github.com/timppeters/obsidian-edit-mdx";
        };
    }
);
    "editing-mode-hotkey" = (
    let
        baseUrl = "https://github.com/Signynt/obsidian-editing-mode-hotkey/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "editing-mode-hotkey";
        version = "1.0.0";
        outputHash = "sha256-Lhfh+oCzluwins9e68ZGW8UjRXEOsK9kpRNsF6+miK0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a command and hotkey to toggle the default editing mode (between Live Preview and Source)'';
            homepage = "https://github.com/Signynt/obsidian-editing-mode-hotkey";
        };
    }
);
    "editing-toolbar" = (
    let
        baseUrl = "https://github.com/PKM-er/obsidian-editing-toolbar/releases/download/4.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "editing-toolbar";
        version = "4.0.7";
        outputHash = "sha256-oFQgTTeJk1U4/hUYzGlEqHw14/BZMP+krVjKpER4cGo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''The Editing Toolbar is modified from cMenu, which provides more powerful customization settings and has many built-in editing commands to be a MS Word-like toolbar editing experience.'';
            homepage = "https://github.com/PKM-er/obsidian-editing-toolbar";
        };
    }
);
    "editor-autofocus" = (
    let
        baseUrl = "https://github.com/mgussekloo/obsidian-editor-autofocus/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "editor-autofocus";
        version = "1.0.0";
        outputHash = "sha256-7v+I3pIykV43zpd+WnBnRmIL1v6wVIaM0JmIi/9wZhk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Autofocus the editor when opening a new file.'';
            homepage = "https://github.com/mgussekloo/obsidian-editor-autofocus";
        };
    }
);
    "editor-commands-remap" = (
    let
        baseUrl = "https://github.com/c4ctus5/editor-commands-remap/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "editor-commands-remap";
        version = "1.0.2";
        outputHash = "sha256-1QvLmXxCROjeblz0KeJI2TJDLmN8P9W7XDMhP0bNqO0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Map hotkeys to editor commands.'';
            homepage = "https://github.com/c4ctus5/editor-commands-remap";
        };
    }
);
    "editor-width-slider" = (
    let
        baseUrl = "https://github.com/MugishoMp/obsidian-editor-width-slider/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "editor-width-slider";
        version = "1.0.5";
        outputHash = "sha256-Ke8EGUD1ERVurKrHzdlZALE1e3gQWJoit3eleT3pAwM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize Obsidian"s editor width with a slider for a tailored editing experience.'';
            homepage = "https://github.com/MugishoMp/obsidian-editor-width-slider";
        };
    }
);
    "efficient-word-count" = (
    let
        baseUrl = "https://github.com/blueheron786/obsidian-efficient-word-count/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "efficient-word-count";
        version = "1.0.0";
        outputHash = "sha256-Y3dCxRUGMocoTWN30tp6Kks7v0yCbRZSVrXRrJsJ3hc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Efficiently calculates and caches word counts for notes, with folder exclusion. Uses cache to avoid recalculating word counts for unchanged notes.'';
            homepage = "https://github.com/blueheron786/obsidian-efficient-word-count";
        };
    }
);
    "ego-rock" = (
    let
        baseUrl = "https://github.com/echo-bravo-yahoo/ego-rock/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ego-rock";
        version = "2.1.0";
        outputHash = "sha256-87selHKm+rURaKamhVJLe973vxXXPX+KfbN2xElkDfc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A basic taskwarrior UI for listing and modifying tasks.'';
            homepage = "https://github.com/echo-bravo-yahoo/ego-rock";
        };
    }
);
    "eleven-labs" = (
    let
        baseUrl = "https://github.com/veritas1/eleven-labs-obsidian-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "eleven-labs";
        version = "1.1.0";
        outputHash = "sha256-uzVBZOeRA3agQEIL2SB2D1pVdvBqtHB1yUms36I81Jk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn your notes into text-to-speech audio files with Eleven Labs.'';
            homepage = "https://github.com/veritas1/eleven-labs-obsidian-plugin";
        };
    }
);
    "emacs-text-editor" = (
    let
        baseUrl = "https://github.com/Klojer/obsidian-emacs-text-editor/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emacs-text-editor";
        version = "0.10.0";
        outputHash = "sha256-PEJ2eBQUcUHK8Dy0SAM2Qq6bNXcBo/HDZH7rhGRBymA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Partial emulation of Emacs text editor.'';
            homepage = "https://github.com/Klojer/obsidian-emacs-text-editor";
        };
    }
);
    "email-block-plugin" = (
    let
        baseUrl = "https://github.com/joleaf/obsidian-email-block-plugin/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "email-block-plugin";
        version = "1.1.1";
        outputHash = "sha256-b7QS0FJqUPfOglxEBDOKXE8MpR5HFWYUy/6NeEBQ2x0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render an email code block.'';
            homepage = "https://github.com/joleaf/obsidian-email-block-plugin";
        };
    }
);
    "embed-code-file" = (
    let
        baseUrl = "https://github.com/almariah/embed-code-file/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "embed-code-file";
        version = "1.2.0";
        outputHash = "sha256-hcJ/a3g2O6Gl1mklOmWwzb32gimNmv/bZhd6rB/PtWA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed code file from vault using code blocks.'';
            homepage = "https://github.com/almariah/embed-code-file";
        };
    }
);
    "eml-reader" = (
    let
        baseUrl = "https://github.com/pulsovi/obsidian_eml_reader/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "eml-reader";
        version = "1.1.2";
        outputHash = "sha256-6fwnaxpbszC4KA920l++SV2TTLYMcdV/6k1uXaU+uMY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A preview mode for embedded .eml files.'';
            homepage = "https://github.com/pulsovi/obsidian_eml_reader";
        };
    }
);
    "emoji-autocomplete" = (
    let
        baseUrl = "https://github.com/KraXen72/obsidian-emoji-autocomplete/releases/download/1.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-autocomplete";
        version = "1.5.3";
        outputHash = "sha256-6d3221N7v5fSwMoDOufaCrDZek4MyG9CWdlYsSL7rbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Smart suggestions when typing emoji shortcodes & more! :star:'';
            homepage = "https://github.com/KraXen72/obsidian-emoji-autocomplete";
        };
    }
);
    "emoji-magic" = (
    let
        baseUrl = "https://github.com/SimplGy/obsidian-emoji-magic/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-magic";
        version = "0.2.2";
        outputHash = "sha256-ws1RdnoJjEsdxbWGErRlZ8OVdqOUSaV50mIAXaC4wzw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily add emoji, with a powerful keyword search.'';
            homepage = "https://github.com/SimplGy/obsidian-emoji-magic";
        };
    }
);
    "emoji-picker" = (
    let
        baseUrl = "https://github.com/alifa98/obsidian-emoji-picker/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-picker";
        version = "2.0.0";
        outputHash = "sha256-GE7/cm2brhqznWNcKAZzsgOwdjxLTT8Y99O7nljwjeU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An Emoji Picker that allows you to insert the latest released emojis in your notes.'';
            homepage = "https://github.com/alifa98/obsidian-emoji-picker";
        };
    }
);
    "emoji-selector" = (
    let
        baseUrl = "https://github.com/infinitesum/obsidian-emoji-selector/releases/download/1.3.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-selector";
        version = "1.3.7";
        outputHash = "sha256-OfUUinwYpQzuKkZTk7PNTfN9sKjkWMS2ve0PQpvr/eU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert custom emojis with quick search, auto-suggestions, and customizable templates.'';
            homepage = "https://github.com/infinitesum/obsidian-emoji-selector";
        };
    }
);
    "emoji-shortcodes" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-emoji-shortcodes/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-shortcodes";
        version = "2.2.0";
        outputHash = "sha256-K7l+RBg6lOCJCGFY6NHfTPSsw/oJjEfndtnXmCIV4oc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable the use of Markdown Emoji shortcodes :smile:, just like in Slack or Discord.'';
            homepage = "https://github.com/phibr0/obsidian-emoji-shortcodes";
        };
    }
);
    "emoji-tags-titler" = (
    let
        baseUrl = "https://github.com/Cyfine/EmoTagsTitler/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-tags-titler";
        version = "1.0.0";
        outputHash = "sha256-26I5P+9Qbp8F54Bp73XTpf8SYTzFanjG4botR2etOsk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add the emojis contained in the tags to the beginning of the note title.'';
            homepage = "https://github.com/Cyfine/EmoTagsTitler";
        };
    }
);
    "emoji-titler" = (
    let
        baseUrl = "https://github.com/HyeonseoNam/obsidian-emoji-titler/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "emoji-titler";
        version = "1.0.4";
        outputHash = "sha256-hLmp7JcgdsE17zqla87e63zdHahrT72S9rODU19bXN0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily insert an emoji in the title using a keyboard shortcut.'';
            homepage = "https://github.com/HyeonseoNam/obsidian-emoji-titler";
        };
    }
);
    "enhance-youtube-links" = (
    let
        baseUrl = "https://github.com/Git-Sum/obsidian-enhance-youtube-links/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhance-youtube-links";
        version = "1.0.10";
        outputHash = "sha256-7sAOeMLtAzM51dZ/etPitRK60u+UO6peOE/kmkaof+U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get metadata from a YouTube video.'';
            homepage = "https://github.com/Git-Sum/obsidian-enhance-youtube-links";
        };
    }
);
    "enhanced-annotations" = (
    let
        baseUrl = "https://github.com/ycnmhd/obsidian-enhanced-annotations/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhanced-annotations";
        version = "0.1.9";
        outputHash = "sha256-wuCcbCc7IUSyaP1mEkBkhq1nmHkzesKamP6Dalccg9k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a sidebar view for comments and highlights.'';
            homepage = "https://github.com/ycnmhd/obsidian-enhanced-annotations";
        };
    }
);
    "enhanced-canvas" = (
    let
        baseUrl = "https://github.com/RobertttBS/obsidian-enhanced-canvas/releases/download/1.0.22";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhanced-canvas";
        version = "1.0.22";
        outputHash = "sha256-R0Y3U+jzc/X2G4GZYWTBEZ/Q8VBTFyE3OIT3HG+aJQc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncs Canvas node connections with properties in note, automating document relationship management.'';
            homepage = "https://github.com/RobertttBS/obsidian-enhanced-canvas";
        };
    }
);
    "enhanced-copy" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-enhanced-copy/releases/download/1.1.0-6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhanced-copy";
        version = "1.1.0-6";
        outputHash = "sha256-+2HchLE5J0wR+8dmaJauVzt5daXGAMxlYHB82rntmb4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy your selection and add it some edit to paste in other markdown software. Allows to keep markdown in reading view, removing wikilinks in editing, copy from locked canvas, and more!'';
            homepage = "https://github.com/Mara-Li/obsidian-enhanced-copy";
        };
    }
);
    "enhanced-image" = (
    let
        baseUrl = "https://github.com/situ2001/obsidian-enhanced-image/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhanced-image";
        version = "0.0.1";
        outputHash = "sha256-RBMEcn+rxuo//KktqMa2JnhkP+7ccBgR+fzFP1EKijU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance the experience of image seamlessly. For example, operations for image in context menu, command palette.'';
            homepage = "https://github.com/situ2001/obsidian-enhanced-image";
        };
    }
);
    "enhanced-publisher" = (
    let
        baseUrl = "https://github.com/chararch/obsidian-enhanced-publisher/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhanced-publisher";
        version = "1.1.0";
        outputHash = "sha256-5fDpzgk/pR69G5htj1gze+cz7AI9qtgVkX1dn6t187o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''增强发布插件，支持图片时自动存储、HTML预览和发布到微信公众号等内容平台。'';
            homepage = "https://github.com/chararch/obsidian-enhanced-publisher";
        };
    }
);
    "enhanced-tables" = (
    let
        baseUrl = "https://github.com/pistacchio/obsidian-enhanced-tables/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enhanced-tables";
        version = "1.0.5";
        outputHash = "sha256-GRI0HuEHVW+XJDe9HY8Fc9z/TA0x7vcjxb8sLV74cL4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add programmable controls to selected tables.'';
            homepage = "https://github.com/pistacchio/obsidian-enhanced-tables";
        };
    }
);
    "enlightenment-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/enlightenment-obsidian/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "enlightenment-obsidian";
        version = "1.2.1";
        outputHash = "sha256-xVp/Jj/W2DpO2TPl5b+NK6kFSFIMFvEgpsou6zskyGM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pay attention to what you"re paying attention to. Enlightenment adds a "zen mode" for Preview, hiding the contents of your notes except for what"s underneath your pointer.'';
            homepage = "https://github.com/ryanjamurphy/enlightenment-obsidian";
        };
    }
);
    "entity-linker" = (
    let
        baseUrl = "https://github.com/Ankush-Chander/obsidian-entity-linker/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "entity-linker";
        version = "1.1.0";
        outputHash = "sha256-T5iDkRL6I/buvAWzRv2vT8/s7ts+LtMD9xn8itn5hPc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link research terms to standard entities'';
            homepage = "https://github.com/Ankush-Chander/obsidian-entity-linker";
        };
    }
);
    "epiphany" = (
    let
        baseUrl = "https://github.com/Epiphany-Notes/epiphany-obsidian-plugin/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "epiphany";
        version = "2.0.0";
        outputHash = "sha256-6uqGlPtJCoNMec+UtMv7V2430kv8iU6e635/oK+qHEE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize voice notes from the Epiphany app directly into your vault'';
            homepage = "https://github.com/Epiphany-Notes/epiphany-obsidian-plugin";
        };
    }
);
    "epub-importer" = (
    let
        baseUrl = "https://github.com/aoout/obsidian-epub-importer/releases/download/0.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "epub-importer";
        version = "0.8.4";
        outputHash = "sha256-W3dEWZ6E0ZVUiq4IcH69FgG4pf1aqTEAIg5qpwjZjpg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import EPUB files as Markdown.'';
            homepage = "https://github.com/aoout/obsidian-epub-importer";
        };
    }
);
    "etymology-lookup" = (
    let
        baseUrl = "https://github.com/clairefro/obsidian-plugin-etymology-lookup/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "etymology-lookup";
        version = "2.0.1";
        outputHash = "sha256-DEE0zzMqnxzeh2BSMFywtSwGKNNFSMRs8RgEs2RfaB4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get the etymology of words in your notes, from Online Etymology Dictionary'';
            homepage = "https://github.com/clairefro/obsidian-plugin-etymology-lookup";
        };
    }
);
    "event-highlight" = (
    let
        baseUrl = "https://github.com/playmean/obsidian-event-highlight-plugin/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "event-highlight";
        version = "0.7.0";
        outputHash = "sha256-7ksymMf5UF1oxuOrAZJAvH+bdi9Xt2BM6Orsa71qryg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render colored bars with relative event dates'';
            homepage = "https://github.com/playmean/obsidian-event-highlight-plugin";
        };
    }
);
    "evernote-decryptor" = (
    let
        baseUrl = "https://github.com/rcmdnk/obsidian-evernote-decryptor/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "evernote-decryptor";
        version = "2.2.0";
        outputHash = "sha256-lJhrYWti7h3kM4v/V2kJJQVpYj6XIGW/GysZejXdX50=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage encrypted data imported from Evernote.'';
            homepage = "https://github.com/rcmdnk/obsidian-evernote-decryptor";
        };
    }
);
    "every-day-calendar" = (
    let
        baseUrl = "https://github.com/Sporarum/every-day-calendar/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "every-day-calendar";
        version = "1.0.1";
        outputHash = "sha256-4WBMgciVOs6A/OaN8Iez851Cky/sXiSpR3mwA5I6czE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create calendars inspired by Simone Giertz"s Every Day Calendar'';
            homepage = "https://github.com/Sporarum/every-day-calendar";
        };
    }
);
    "everyday-classical-music" = (
    let
        baseUrl = "https://github.com/Eloliuyx/everyday-classical-music-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "everyday-classical-music";
        version = "1.0.0";
        outputHash = "sha256-QlgUN8MlTjGpmhmOA7/HVcDAfc5Sfx3akxYxLo8GiSk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a different piece of classical music each day with a YouTube link in your daily note.'';
            homepage = "https://github.com/Eloliuyx/everyday-classical-music-plugin";
        };
    }
);
    "excalibrain" = (
    let
        baseUrl = "https://github.com/zsviczian/excalibrain/releases/download/0.2.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "excalibrain";
        version = "0.2.17";
        outputHash = "sha256-OajjDEEw9AFyCezjArbzsz59ToIjs9FfUm/K2IJCXRI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''ExcaliBrain is inspired by TheBrain and Breadcrumbs. It is an interactive, structured mind-map of your vault generated based on the folders and files in your vault by interpreting the links, Dataview fields, tags and YAML frontmatter in your Markdown files.'';
            homepage = "https://github.com/zsviczian/excalibrain";
        };
    }
);
    "excalidraw-cn" = (
    let
        baseUrl = "https://github.com/korbinzhao/obsidian-excalidraw-cn-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "excalidraw-cn";
        version = "1.0.0";
        outputHash = "sha256-RcXUk5+VslHAnPS0b/GjdDcJHYHRccyrm6pVUO66bRE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Excalidraw supporting Chinese hand write font by default.'';
            homepage = "https://github.com/korbinzhao/obsidian-excalidraw-cn-plugin";
        };
    }
);
    "excel" = (
    let
        baseUrl = "https://github.com/ljcoder2015/obsidian-excel/releases/download/1.3.24";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "excel";
        version = "1.3.24";
        outputHash = "sha256-215/BaJhuQD2ydODcBYL8RwU7HNgVSeWfskuHq2V02w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create spreadsheets and easily embed them in Markdown.'';
            homepage = "https://github.com/ljcoder2015/obsidian-excel";
        };
    }
);
    "execute-code" = (
    let
        baseUrl = "https://github.com/twibiral/obsidian-execute-code/releases/download/2.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "execute-code";
        version = "2.1.2";
        outputHash = "sha256-Pfl5PhKMrasl2kD+xtTKh1WIsWX1+UTvmlQzLlJYaiA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute code snippets within a note.'';
            homepage = "https://github.com/twibiral/obsidian-execute-code";
        };
    }
);
    "exercises" = (
    let
        baseUrl = "https://github.com/AlexCCavaco/obsidian-exercises/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "exercises";
        version = "0.1.3";
        outputHash = "sha256-N+MluPy8jQG01tHtcyb5lbXCu7L6TyTI+dfUr8HDfy4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create interactive exercises in your notes.'';
            homepage = "https://github.com/AlexCCavaco/obsidian-exercises";
        };
    }
);
    "exmemo-assistant" = (
    let
        baseUrl = "https://github.com/exmemo-ai/obsidian-exmemo-assistant/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "exmemo-assistant";
        version = "1.0.1";
        outputHash = "sha256-wNUNEjMEo7xN/P0ka53NJvqR0/+TY1H13fONPec4tEo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Using LLMs to manage files and generating metadata such as tags and summaries.'';
            homepage = "https://github.com/exmemo-ai/obsidian-exmemo-assistant";
        };
    }
);
    "exmemo-client" = (
    let
        baseUrl = "https://github.com/exmemo-ai/obsidian-exmemo-client/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "exmemo-client";
        version = "1.0.2";
        outputHash = "sha256-0HB4D8l08s5OCM13Jmu0gHds4AFMFoNTRedSC/r8o7c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A client for the ExMemo service, used to search, sync, and organize data from the server.'';
            homepage = "https://github.com/exmemo-ai/obsidian-exmemo-client";
        };
    }
);
    "exmemo-tools" = (
    let
        baseUrl = "https://github.com/exmemo-ai/obsidian-exmemo-tools/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "exmemo-tools";
        version = "1.2.0";
        outputHash = "sha256-d6NUNkFt1tRGqNfS0PqlcyLA5wNKjy6D1fH69Pvhk3U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use LLMs for smart document management and optimization, including relocating files, enhancing text, and generating tags and summary.'';
            homepage = "https://github.com/exmemo-ai/obsidian-exmemo-tools";
        };
    }
);
    "expand-selection" = (
    let
        baseUrl = "https://github.com/atait/obsidian-expand-selection/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "expand-selection";
        version = "1.0.4";
        outputHash = "sha256-uTmMPZGZA2ZVc6q4RPRVeWpdYCXFDPaoV7Lpp9KVuvA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Expands the editor selection to lines, sections, or the entire note for convenience on mobile.'';
            homepage = "https://github.com/atait/obsidian-expand-selection";
        };
    }
);
    "expiration-date-tracker" = (
    let
        baseUrl = "https://github.com/Raboro/obsidian-expiration-date-tracker-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "expiration-date-tracker";
        version = "1.0.3";
        outputHash = "sha256-WVoQfNU/uxMSw6nwoXIXQrF7G4yG1bXWlKXdWLewKgE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep track of all expiration dates, for example, for your groceries.'';
            homepage = "https://github.com/Raboro/obsidian-expiration-date-tracker-plugin";
        };
    }
);
    "explain-selection-with-ai" = (
    let
        baseUrl = "https://github.com/BWurster/explain-selection-with-ai/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "explain-selection-with-ai";
        version = "1.0.1";
        outputHash = "sha256-dF/a1bm4ibdd9FHaN36m4fsslZyAP1dESQ22C4/HEd4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use an OpenAI Chat Completion API-compatible LLM endpoint to expand on selected text in the context of your notes.'';
            homepage = "https://github.com/BWurster/explain-selection-with-ai";
        };
    }
);
    "explorer-colors" = (
    let
        baseUrl = "https://github.com/VaguelyElectric/obsidian-explorer-colors/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "explorer-colors";
        version = "1.1.1";
        outputHash = "sha256-SizytPhYBl5a21J7LBdnApMBJGk6RmkRcXVxYUVu9VQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows for files and folders to be configured with custom colors and other settings.'';
            homepage = "https://github.com/VaguelyElectric/obsidian-explorer-colors";
        };
    }
);
    "explorer-hider" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-explorer-hider/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "explorer-hider";
        version = "1.4.3";
        outputHash = "sha256-vFqUuFkJ577EWvWgRoUu7SrlxoCTPmRNcxKrvk8l3T8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide a file or a folder from the explorer (and bookmarks) using a little bit of auto-managed CSS.'';
            homepage = "https://github.com/Mara-Li/obsidian-explorer-hider";
        };
    }
);
    "export-graph-view" = (
    let
        baseUrl = "https://github.com/seantiz/obsidian_egv_plugin/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "export-graph-view";
        version = "1.2.3";
        outputHash = "sha256-jya2eqS8c2vYdBbYEqymuZ2WWTyfSX+b6/zrXTwqe0Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export your vault"s graph view to mermaid and dot format.'';
            homepage = "https://github.com/seantiz/obsidian_egv_plugin";
        };
    }
);
    "export-to-html" = (
    let
        baseUrl = "https://github.com/kalvn/obsidian-export-to-html/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "export-to-html";
        version = "1.1.0";
        outputHash = "sha256-InLWsv0JDBZhcSIFEy27SojQWZtXoye766URTaXcw5U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export your Markdown notes as HTML, directly in the clipboard or as a file.'';
            homepage = "https://github.com/kalvn/obsidian-export-to-html";
        };
    }
);
    "extended-file-support" = (
    let
        baseUrl = "https://github.com/Nick-de-Bruin/obsidian-extended-file-support/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "extended-file-support";
        version = "1.4.1";
        outputHash = "sha256-F84LoQ6WYOCeN2UCF4dP3CvlKCfwZQmW2b7yKw7Z7Mg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds file support for various file types. Allows viewing and embedding these filetypes. Includes: .kra, .psd, .obj, .glb, .gltf, and more.'';
            homepage = "https://github.com/Nick-de-Bruin/obsidian-extended-file-support";
        };
    }
);
    "extended-graph" = (
    let
        baseUrl = "https://github.com/ElsaTam/obsidian-extended-graph/releases/download/2.7.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "extended-graph";
        version = "2.7.7";
        outputHash = "sha256-mVWtI8odM3s1BKepsYNjWpemUhBYKDvxeUORgvFMFDc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extends the features of the core Graph view, display images, manage states, remove links, change node shapes, and more.'';
            homepage = "https://github.com/ElsaTam/obsidian-extended-graph";
        };
    }
);
    "extended-markdown-syntax" = (
    let
        baseUrl = "https://github.com/kotaindah55/extended-markdown-syntax/releases/download/2.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "extended-markdown-syntax";
        version = "2.0.11";
        outputHash = "sha256-kcbm0BpuzGqcbK4kIUzQb5lhACLeGNadcqKuSGqs+A8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extend your Markdown syntax using delimiters instead of HTML tags, such as underlining, superscript, subscript, highlighting, and spoiler.'';
            homepage = "https://github.com/kotaindah55/extended-markdown-syntax";
        };
    }
);
    "extended-task-lists" = (
    let
        baseUrl = "https://github.com/joeriddles/extended-task-lists/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "extended-task-lists";
        version = "1.0.11";
        outputHash = "sha256-eKEpaYuCrU4YzWLErU8N7Fcs3AHAMb84EfxgyA4IxLg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extended reader view support for task lists, including in-progress and won"t do task items.'';
            homepage = "https://github.com/joeriddles/extended-task-lists";
        };
    }
);
    "external-file-card" = (
    let
        baseUrl = "https://github.com/James-Yu/external-file-card/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "external-file-card";
        version = "0.0.7";
        outputHash = "sha256-nWC9ncfN92hAp1yf68xIFcOLapSmXloLb2wa5EU9oxc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display file cards for external files.'';
            homepage = "https://github.com/James-Yu/external-file-card";
        };
    }
);
    "external-file-embed-and-link" = (
    let
        baseUrl = "https://github.com/oylbin/obsidian-external-file-embed-and-link/releases/download/1.5.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "external-file-embed-and-link";
        version = "1.5.9";
        outputHash = "sha256-E5cJ6iTYn12ifpZ7Y5rI8XYltrRgroof4rv7FaM5IJI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed and link local files outside your vault with relative paths for cross-device and multi-platform compatibility.'';
            homepage = "https://github.com/oylbin/obsidian-external-file-embed-and-link";
        };
    }
);
    "external-links" = (
    let
        baseUrl = "https://github.com/jivimberg/external-links/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "external-links";
        version = "1.0.12";
        outputHash = "sha256-k0jDSwGwgk5scCgtMj5ISvckG9YQWZ+amGAucLKJDQg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''List external links on the right side panel.'';
            homepage = "https://github.com/jivimberg/external-links";
        };
    }
);
    "external-links-view" = (
    let
        baseUrl = "https://github.com/theamritanair/obsidian-external-links/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "external-links-view";
        version = "1.0.5";
        outputHash = "sha256-5QxGZKx3TmxoFNqMPi2vQrXjXUA66crhoWjZDRm32FA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View all external links from your notes in one organized place, sorted by their source notes.'';
            homepage = "https://github.com/theamritanair/obsidian-external-links";
        };
    }
);
    "external-rename-handler" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-external-rename-handler/releases/download/3.0.40";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "external-rename-handler";
        version = "3.0.40";
        outputHash = "sha256-MilDmq6HBl48iJeTsY8v1hGD3xpY+vbeb53A+7PkWGM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Handles renames made outside of the app.'';
            homepage = "https://github.com/mnaoumov/obsidian-external-rename-handler";
        };
    }
);
    "extract-highlights-plugin" = (
    let
        baseUrl = "https://github.com/akaalias/extract-highlights-plugin/releases/download/0.0.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "extract-highlights-plugin";
        version = "0.0.18";
        outputHash = "sha256-PrlhvRXL+Uz1n6sD+fqdWEwYTWvKwW/xbKnxclBwcOM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract all ==highlights== in a note into your clipboard.'';
            homepage = "https://github.com/akaalias/extract-highlights-plugin";
        };
    }
);
    "extract-url" = (
    let
        baseUrl = "https://github.com/trashhalo/obsidian-extract-url/releases/download/0.12.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "extract-url";
        version = "0.12.1";
        outputHash = "sha256-yf2YpP/tBLdbYRHDPeGChpgaAOOVFxmIaWinAzcbJ00=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract URL converting content into Markdown.'';
            homepage = "https://github.com/trashhalo/obsidian-extract-url";
        };
    }
);
    "fantasy-content-generator" = (
    let
        baseUrl = "https://github.com/Gregory-Jagermeister/Fantasy-Content-Generator/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fantasy-content-generator";
        version = "1.2.4";
        outputHash = "sha256-+deFwo/3CweTADn1Um4k/LoyRepnKSSv7Vx3mj26aAo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A fantasy content generator for all your TTRPG and world-building needs.'';
            homepage = "https://github.com/Gregory-Jagermeister/Fantasy-Content-Generator";
        };
    }
);
    "fantasy-name" = (
    let
        baseUrl = "https://github.com/Lukewh/fantasy-name/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fantasy-name";
        version = "0.1.0";
        outputHash = "sha256-/JLnOpSFJtlnBk5x7qb+LHgjc9TqgzMAnwkPSG2e4l0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert a random fantasy name.'';
            homepage = "https://github.com/Lukewh/fantasy-name";
        };
    }
);
    "fast-forward-link" = (
    let
        baseUrl = "https://github.com/IdanLib/ObsidianFastForwardLinkPlugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fast-forward-link";
        version = "1.0.0";
        outputHash = "sha256-AUBwoFwCdpMtZF3/IGdptLJYZsGdFjRXeUKkWdCnE0s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fast-forward multiple links to a single target note. Create custom link shorthands (like `ps` > `photoshop`) to create synonyms, streamline navigation, and keep your vault organized.'';
            homepage = "https://github.com/IdanLib/ObsidianFastForwardLinkPlugin";
        };
    }
);
    "fast-image-upload" = (
    let
        baseUrl = "https://github.com/eust-w/obsidian-image-auto-upload/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fast-image-upload";
        version = "1.1.2";
        outputHash = "sha256-F4ozGG5W/oySmXMslvOGBD7byTb3IwTr46bZOmwxLyE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fast upload images from your clipboard by gopic'';
            homepage = "https://github.com/eust-w/obsidian-image-auto-upload";
        };
    }
);
    "fast-text-color" = (
    let
        baseUrl = "https://github.com/Superschnizel/obsidian-fast-text-color/releases/download/1.1.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fast-text-color";
        version = "1.1.11";
        outputHash = "sha256-ykJZj0doGV55JTimxUdVgKyzefkDmsOdycdMpR0gq/I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly apply fully integrated text coloring and formatting with a custom syntax and a keyboard-centric interface.'';
            homepage = "https://github.com/Superschnizel/obsidian-fast-text-color";
        };
    }
);
    "fastimer" = (
    let
        baseUrl = "https://github.com/vkostyanetsky/ObsidianFastimer/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fastimer";
        version = "1.2.4";
        outputHash = "sha256-8IVw7p91QOXLIbDeBR6BQ3LWz/OSg5K0ZeEH7Gi13Ic=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intermittent fasting tracker.'';
            homepage = "https://github.com/vkostyanetsky/ObsidianFastimer";
        };
    }
);
    "favorite-note" = (
    let
        baseUrl = "https://github.com/mahmudz/obsidian-favorite-plugin/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "favorite-note";
        version = "2.0.3";
        outputHash = "sha256-CIQWwLyjRPZ7lTzhQZ8hTmn5ny79VhC+TEV94xVFaiY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mark your note as favorite.'';
            homepage = "https://github.com/mahmudz/obsidian-favorite-plugin";
        };
    }
);
    "featured-image" = (
    let
        baseUrl = "https://github.com/johansan/obsidian-featured-image/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "featured-image";
        version = "1.2.6";
        outputHash = "sha256-0CuoLdAkTUlJoF1UTfrDTk8M04ALVkwok63ke4Zy9h4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Featured Image automatically sets a featured image property in your notes based on the first image, YouTube link, or Auto Card Link image found in your document. This allows you to create rich note galleries using Folder Notes and Dataview.'';
            homepage = "https://github.com/johansan/obsidian-featured-image";
        };
    }
);
    "feedly-annotations" = (
    let
        baseUrl = "https://github.com/Fleker/feedly-for-obsidian/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "feedly-annotations";
        version = "1.3.0";
        outputHash = "sha256-L54273GEEh47lg+suOVpzZwAchEfbcVY1xgJ8G6H580=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncs Feedly highlights and annotations to a folder in your vault.'';
            homepage = "https://github.com/Fleker/feedly-for-obsidian";
        };
    }
);
    "feeds" = (
    let
        baseUrl = "https://github.com/lukemt/obsidian-feeds/releases/download/0.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "feeds";
        version = "0.0.11";
        outputHash = "sha256-dh0RDcHRhnoaYs399M07DKgHKcb4nKKQqnyd/78Nr7M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create feeds of topic-specific bullet points.'';
            homepage = "https://github.com/lukemt/obsidian-feeds";
        };
    }
);
    "fetch-prayer-times" = (
    let
        baseUrl = "https://github.com/Eccys/obsidian-prayer-times/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fetch-prayer-times";
        version = "1.2.0";
        outputHash = "sha256-LDHxiLJkHmwVr2PAzObA+P8gJsPZoop/JpdnW7NbMu4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetches current prayer times using the Aladhan API and saves them to a note.'';
            homepage = "https://github.com/Eccys/obsidian-prayer-times";
        };
    }
);
    "ffmpeg-converter" = (
    let
        baseUrl = "https://github.com/MrAnyx/obsidian-ffmpeg-converter/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ffmpeg-converter";
        version = "1.1.0";
        outputHash = "sha256-vOAuFH2cQUkH6Owl5nyqqaodIfmk8LFJbXbdmsGgUjQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert most used file formats into another format to optimize your vault space.'';
            homepage = "https://github.com/MrAnyx/obsidian-ffmpeg-converter";
        };
    }
);
    "fight-note" = (
    let
        baseUrl = "https://github.com/Loac/obsidian-fight-note/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fight-note";
        version = "1.2.0";
        outputHash = "sha256-jsDy12Q5EqTrE7elC3Cs+rMy5g2iaBu2YcNNQB5mBw8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Tekken notation into an easy-to-read format (partially useful for other fighting games: Guilty Gear, Street Fighter and etc).'';
            homepage = "https://github.com/Loac/obsidian-fight-note";
        };
    }
);
    "figma-embed" = (
    let
        baseUrl = "https://github.com/kocheck/obsidian-figma-viewer/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "figma-embed";
        version = "1.0.4";
        outputHash = "sha256-fjW18ZZnszpSjQnqqirjHHiCRzJtgVqQbKoGGf3wGEE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed Figma files as inline previews.'';
            homepage = "https://github.com/kocheck/obsidian-figma-viewer";
        };
    }
);
    "file-chucker" = (
    let
        baseUrl = "https://github.com/kenlim/file-chucker-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-chucker";
        version = "1.0.1";
        outputHash = "sha256-G7YyIKg2YAmxzfuXPNcIlo+QQP7qtfK0TkQDi5ub6h0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly move a file to a new or existing folder, then open the next file.'';
            homepage = "https://github.com/kenlim/file-chucker-plugin";
        };
    }
);
    "file-cleaner-redux" = (
    let
        baseUrl = "https://github.com/husjon/obsidian-file-cleaner-redux/releases/download/1.14.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-cleaner-redux";
        version = "1.14.0";
        outputHash = "sha256-yVgc49g/QqXbehHkqNx2QJFp4oKRe1VsWNVcjHDr+Ks=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Help you to clean empty files and unused attachments in the vault.'';
            homepage = "https://github.com/husjon/obsidian-file-cleaner-redux";
        };
    }
);
    "file-diff" = (
    let
        baseUrl = "https://github.com/friebetill/obsidian-file-diff/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-diff";
        version = "1.1.2";
        outputHash = "sha256-dbjF5zgRtaES5lfc3XBnnpdXvl4+2wVJZdlp8Etz8rs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View the differences between two files with merge options.'';
            homepage = "https://github.com/friebetill/obsidian-file-diff";
        };
    }
);
    "file-explorer-markdown-titles" = (
    let
        baseUrl = "https://github.com/Dyldog/file-explorer-markdown-titles/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-explorer-markdown-titles";
        version = "1.0.2";
        outputHash = "sha256-SHWp1ldXJrjYM30kIDgPJqzauZdsPYcdzuProKSEbkM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show the first Markdown header of a note in the file explorer.'';
            homepage = "https://github.com/Dyldog/file-explorer-markdown-titles";
        };
    }
);
    "file-explorer-note-count" = (
    let
        baseUrl = "https://github.com/ozntel/file-explorer-note-count/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-explorer-note-count";
        version = "1.2.4";
        outputHash = "sha256-YzMuritgLB96/JHR2xEDg5Rto2vygFuVUaQDpLtGRo4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''See the number of notes in each folder within the file explorer.'';
            homepage = "https://github.com/ozntel/file-explorer-note-count";
        };
    }
);
    "file-explorer-plus" = (
    let
        baseUrl = "https://github.com/kelszo/obsidian-file-explorer-plus/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-explorer-plus";
        version = "1.3.1";
        outputHash = "sha256-ggo6H7ZztyfoY0Y+fxRfmXGwOkoIVb/3OvzkZg8cwUI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide and pin files and folders in the file explorer using custom filters, such as wildcards and regex, based on their names, paths, and tags. Additionally, achieve the same with a single click in the file menu.'';
            homepage = "https://github.com/kelszo/obsidian-file-explorer-plus";
        };
    }
);
    "file-forgetting-curve-obsidian" = (
    let
        baseUrl = "https://github.com/ptrsvltns/file-forgetting-curve-obsidian/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-forgetting-curve-obsidian";
        version = "1.0.1";
        outputHash = "sha256-JKL9C0kOeUgK5azo4B4EpPg/QyiyMha/zOdpGrWo6pc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''File Forgetting Curve.'';
            homepage = "https://github.com/ptrsvltns/file-forgetting-curve-obsidian";
        };
    }
);
    "file-ignore" = (
    let
        baseUrl = "https://github.com/Feng6611/Obsidian-File-Ignore/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-ignore";
        version = "1.1.1";
        outputHash = "sha256-DXqP/izy5S59TJr38x2vkW6V9z7fqnMPpFi+llbxfsM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Controls file indexing by managing dot prefixes on files/folders to truly hide/exclude them, providing a .gitignore-like experience.'';
            homepage = "https://github.com/Feng6611/Obsidian-File-Ignore";
        };
    }
);
    "file-include" = (
    let
        baseUrl = "https://github.com/tillahoffmann/obsidian-file-include/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-include";
        version = "1.1.2";
        outputHash = "sha256-yDt2pu+FTKC6tOR2aAGCR5q4xaDuKKMf+iV09IlEAe8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Include or embed files in Obsidian Markdown.'';
            homepage = "https://github.com/tillahoffmann/obsidian-file-include";
        };
    }
);
    "file-index" = (
    let
        baseUrl = "https://github.com/Steffo99/obsidian-file-index/releases/download/3.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-index";
        version = "3.0.3";
        outputHash = "sha256-KOWlWisa2nJ+jJl5X29MB27eHbQNK70oCVA6VU0IMXQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a metadata file about the files present in the Vault.'';
            homepage = "https://github.com/Steffo99/obsidian-file-index";
        };
    }
);
    "file-indicators" = (
    let
        baseUrl = "https://github.com/JakobMick/obsidian-file-indicators/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-indicators";
        version = "1.1.0";
        outputHash = "sha256-MFxmpUL7WE9B4bD4J9wTmA5zHQHslZmjNcMmK5kssOo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add custom indicators to the file explorer.'';
            homepage = "https://github.com/JakobMick/obsidian-file-indicators";
        };
    }
);
    "file-manager" = (
    let
        baseUrl = "https://github.com/jfsicilia/obsidian-file-manager/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-manager";
        version = "1.4.0";
        outputHash = "sha256-qPBArmRJ0djOSja88HHNWRkUtVTSNn1MEPPAsKCxhrA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds missing features to the file explorer.'';
            homepage = "https://github.com/jfsicilia/obsidian-file-manager";
        };
    }
);
    "file-order" = (
    let
        baseUrl = "https://github.com/lukasbach/obsidian-file-order/releases/download/0.0.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-order";
        version = "0.0.14";
        outputHash = "sha256-0kkrT1kZTPDTg4wamcGtUpv9ckyv4MI1SF7si8HKnZo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use number-prefixes in your file names to define a custom order, and drag-and-drop the files to update that order.'';
            homepage = "https://github.com/lukasbach/obsidian-file-order";
        };
    }
);
    "file-preview" = (
    let
        baseUrl = "https://github.com/xhuajin/obsidian-file-preview/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-preview";
        version = "1.1.4";
        outputHash = "sha256-JZ+mjuI899eOYdYpgSKwGgcdKuY2ZeD/L/cg/vY0TIk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add file preview contents under file in file explorer.'';
            homepage = "https://github.com/xhuajin/obsidian-file-preview";
        };
    }
);
    "file-publisher" = (
    let
        baseUrl = "https://github.com/yiglas/obsidian-file-publisher/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-publisher";
        version = "0.0.1";
        outputHash = "sha256-r1pAgszXGchCngTGE0RdUctoToDTpQO8qMUcw96AfvA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish a file to a given POST API.'';
            homepage = "https://github.com/yiglas/obsidian-file-publisher";
        };
    }
);
    "file-share" = (
    let
        baseUrl = "https://github.com/muckmuck96/obsidian-file-share/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-share";
        version = "1.2.0";
        outputHash = "sha256-xPU+W0RoMQG73N+zZ6pkiAmEn1DiOeOgVdftsL+bdek=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables end-to-end encrypted file sharing directly between vaults through a socket server.'';
            homepage = "https://github.com/muckmuck96/obsidian-file-share";
        };
    }
);
    "file-title-updater" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-file-title-updater/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-title-updater";
        version = "0.6.1";
        outputHash = "sha256-UGv8xEU67yiPEYyOe+WvWoeeShUl5rB3++G8/HRlQ7g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize titles between filename, frontmatter, and first heading in notes.'';
            homepage = "https://github.com/wenlzhang/obsidian-file-title-updater";
        };
    }
);
    "file-tree-alternative" = (
    let
        baseUrl = "https://github.com/ozntel/file-tree-alternative/releases/download/2.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-tree-alternative";
        version = "2.6.0";
        outputHash = "sha256-nz4TlPvfHUaxgsEQjqsMlhcTp31TSIdYAeYQIq3R/EM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An alternative file tree view with separate folder and file panes.'';
            homepage = "https://github.com/ozntel/file-tree-alternative";
        };
    }
);
    "file-tree-generator" = (
    let
        baseUrl = "https://github.com/Unarray/file-tree-generator/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "file-tree-generator";
        version = "1.0.0";
        outputHash = "sha256-WQcTjeN3pcIlBGBSAOufxT+IdcBVrVRBczLWF9BkFbE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate a file tree using callouts.'';
            homepage = "https://github.com/Unarray/file-tree-generator";
        };
    }
);
    "filename-styler" = (
    let
        baseUrl = "https://github.com/marc-f/obsidian-file-name-styler/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "filename-styler";
        version = "1.2.3";
        outputHash = "sha256-eF7hlTwKMrPu4n10Z5PMhoXg0eqRo1qT0Td7Hky0Mas=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize and style file names in the file explorer using patterns, prefixes, suffixes, colors, and icons.'';
            homepage = "https://github.com/marc-f/obsidian-file-name-styler";
        };
    }
);
    "fileorganizer2000" = (
    let
        baseUrl = "https://github.com/Nexus-JPF/note-companion/releases/download/3.6.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fileorganizer2000";
        version = "3.6.18";
        outputHash = "sha256-BCEQ93ws1KVhMBxaSTR4eGCaGmOjxqEWhUzA57aWNy4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered note organization and chat. Requires subscription or self-hosting with your own API keys.'';
            homepage = "https://github.com/Nexus-JPF/note-companion";
        };
    }
);
    "fill-in-the-blank" = (
    let
        baseUrl = "https://github.com/mister-mcgee/obsidian-fill-in-the-blank/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fill-in-the-blank";
        version = "1.0.4";
        outputHash = "sha256-vCWO3Ah0qxSNrOUlNOPGUcP0X9E+RJ9+eDXbwov2uRM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use --magic-- to render inline text as blank line(s) instead.'';
            homepage = "https://github.com/mister-mcgee/obsidian-fill-in-the-blank";
        };
    }
);
    "filtered-opener" = (
    let
        baseUrl = "https://github.com/Balibaloo/obsidian-filtered-opener/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "filtered-opener";
        version = "1.3.2";
        outputHash = "sha256-rmmLgNsI25jgEXH12RDCqoMdXxE+wXtfR3DjRq9WbZQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open notes and folders, chose from sets defined by filters.'';
            homepage = "https://github.com/Balibaloo/obsidian-filtered-opener";
        };
    }
);
    "find-and-replace-in-selection" = (
    let
        baseUrl = "https://github.com/dy-sh/obsidian-find-and-replace-in-selection/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "find-and-replace-in-selection";
        version = "1.0.2";
        outputHash = "sha256-/AVJeLy7QFhoz94iONp77peJqpS/t8LCp4PvO96qoUo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find what you are looking for in the selected text and replace it with the specified text.'';
            homepage = "https://github.com/dy-sh/obsidian-find-and-replace-in-selection";
        };
    }
);
    "find-orphaned-images" = (
    let
        baseUrl = "https://github.com/josmarcristello/Obsidian-Find-Orphaned-Images/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "find-orphaned-images";
        version = "1.1.3";
        outputHash = "sha256-DwGX4K3lRxCcGiXu5jCoyUxGUvCHfeGima3L9OB6b+c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Finds images in the vault that are not linked to any notes. Either lists, or deletes them.'';
            homepage = "https://github.com/josmarcristello/Obsidian-Find-Orphaned-Images";
        };
    }
);
    "find-unlinked-files" = (
    let
        baseUrl = "https://github.com/Vinzent03/find-unlinked-files/releases/download/1.10.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "find-unlinked-files";
        version = "1.10.1";
        outputHash = "sha256-WL2xTTzOt3IHdNIkLW5d7Iu94TYpLEZ15N1KnWDDs0Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find files that are not linked anywhere and would otherwise be lost in your vault. In other words: files with no backlinks.'';
            homepage = "https://github.com/Vinzent03/find-unlinked-files";
        };
    }
);
    "findoc" = (
    let
        baseUrl = "https://github.com/studiowebux/obsidian-findoc/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "findoc";
        version = "0.9.0";
        outputHash = "sha256-OHenZXWZXHojH22qP4Byx2jM7SVb+/del02siKDrvxo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Financial documentation and tracking using CSV format and Chart.js.'';
            homepage = "https://github.com/studiowebux/obsidian-findoc";
        };
    }
);
    "finnish-spellcheck" = (
    let
        baseUrl = "https://github.com/antoKeinanen/obsidian-finnish-spellcheck/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "finnish-spellcheck";
        version = "1.1.2";
        outputHash = "sha256-U09HyCtQeEj320eUgj4MjpSCNFmZVOROmnL0izoA8zc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Spellchecker for the Finnish language using Voikko. / Oikolukija suomenkielellä, joka hyödyntää Voikkoa.'';
            homepage = "https://github.com/antoKeinanen/obsidian-finnish-spellcheck";
        };
    }
);
    "fit" = (
    let
        baseUrl = "https://github.com/joshuakto/fit/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fit";
        version = "1.4.0";
        outputHash = "sha256-Cv5SIG/4zmwC2/+yuC8Uer3wXKthuZY8MDyP8YvS9g8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Minimalist File gIT (FIT) to sync your files across mobile and desktop devices using GitHub.'';
            homepage = "https://github.com/joshuakto/fit";
        };
    }
);
    "fix-math" = (
    let
        baseUrl = "https://github.com/loglux/fix-math-for-obsidian/releases/download/1.0.22";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fix-math";
        version = "1.0.22";
        outputHash = "sha256-Z9ycgM6nsB1u/HIsPWAq8VS8M4TQ03AzkK8nqm347kE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert LaTeX equations from ChatGPT and AI assistants to the correct Markdown format: block equations to display math, inline to inline math.'';
            homepage = "https://github.com/loglux/fix-math-for-obsidian";
        };
    }
);
    "fix-require-modules" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-codescript-toolkit/releases/download/11.7.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fix-require-modules";
        version = "11.7.2";
        outputHash = "sha256-TEOJMOrNa11Y3BSdGvQTLAwZoEpOFFOce2W+5N18828=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows to do a lot of things with JavaScript/TypeScript scripts from inside the Obsidian itself'';
            homepage = "https://github.com/mnaoumov/obsidian-codescript-toolkit";
        };
    }
);
    "flashcard-gen" = (
    let
        baseUrl = "https://github.com/chloedia/Obsidian_Quiz_Generator/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flashcard-gen";
        version = "1.0.2";
        outputHash = "sha256-UnmSJUSaX5rpPjeuiihP60OE7Pw5qK3pXudOOJ9kOa4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate quizzes and questions from your notes.'';
            homepage = "https://github.com/chloedia/Obsidian_Quiz_Generator";
        };
    }
);
    "flashcard-learning" = (
    let
        baseUrl = "https://github.com/gaetanmuck/obsidian-flashcard-learning/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flashcard-learning";
        version = "2.0.0";
        outputHash = "sha256-R5LSF9wghaAD0+cqJIUzaFlIsjRoKD3N00EmcNJWje8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improved flashcard learning system.'';
            homepage = "https://github.com/gaetanmuck/obsidian-flashcard-learning";
        };
    }
);
    "flashcards-llm" = (
    let
        baseUrl = "https://github.com/crybot/obsidian-flashcards-llm/releases/download/0.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flashcards-llm";
        version = "0.0.13";
        outputHash = "sha256-XeevuWbSeJBQl4wp5u5e03ReJPXv9jNyg8QthdarNlI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use Large Language Models (such as ChatGPT) to automatically generate flashcards from your notes.'';
            homepage = "https://github.com/crybot/obsidian-flashcards-llm";
        };
    }
);
    "flashcards-obsidian" = (
    let
        baseUrl = "https://github.com/reuseman/flashcards-obsidian/releases/download/1.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flashcards-obsidian";
        version = "1.6.5";
        outputHash = "sha256-aZG4pZ5MIYS3p+tIHRw1e+DjsL4PrNeJ+5cZHut6ag4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Anki integration.'';
            homepage = "https://github.com/reuseman/flashcards-obsidian";
        };
    }
);
    "float-search" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Float-Search/releases/download/4.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "float-search";
        version = "4.3.0";
        outputHash = "sha256-K/wH9/Zy+zl++DXp9HpdhyJinGmmYYM9l0k3RxJ/OnY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search text by using Obsidian default search view.'';
            homepage = "https://github.com/Quorafind/Obsidian-Float-Search";
        };
    }
);
    "floating-headings" = (
    let
        baseUrl = "https://github.com/k0src/Floating-Headings-Obsidian-Plugin/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "floating-headings";
        version = "1.1.2";
        outputHash = "sha256-8lnd/Lhw9ApS59J/oH7Q2jsqxyCEsbDv3MMIoOQnlTk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays a floating, collapsible outline of a note"s headings on the right side of the editor. Expands on hover, click to navigate.'';
            homepage = "https://github.com/k0src/Floating-Headings-Obsidian-Plugin";
        };
    }
);
    "floating-highlights" = (
    let
        baseUrl = "https://github.com/KarthikRaju391/obsidian-float/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "floating-highlights";
        version = "1.1.0";
        outputHash = "sha256-+punLqU4bNcFzfqO84aAWLr8PxUxtyiV89Znh3wh4RM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adding animations to focus more on the highlights as you scroll down while in reading mode.'';
            homepage = "https://github.com/KarthikRaju391/obsidian-float";
        };
    }
);
    "floating-toc" = (
    let
        baseUrl = "https://github.com/PKM-er/obsidian-floating-toc-plugin/releases/download/2.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "floating-toc";
        version = "2.7.1";
        outputHash = "sha256-zE/ydb5em7efEDhC8YVoTDoduX5CgS4paUIcvfrIgAU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A floating directory that hovers a widget of the current directory on the notes page.'';
            homepage = "https://github.com/PKM-er/obsidian-floating-toc-plugin";
        };
    }
);
    "floccus-bookmarks-to-markdown" = (
    let
        baseUrl = "https://github.com/mddevils/floccus-bookmarks-to-markdown/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "floccus-bookmarks-to-markdown";
        version = "0.2.2";
        outputHash = "sha256-Rmb+K1O6pjzwneQv6vBG0x0aNorL2UQ0H6EcqD+bij8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import and convert your bookmarks from Floccus.'';
            homepage = "https://github.com/mddevils/floccus-bookmarks-to-markdown";
        };
    }
);
    "flomo-importer" = (
    let
        baseUrl = "https://github.com/jia6y/flomo-to-obsidian/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flomo-importer";
        version = "1.3.1";
        outputHash = "sha256-vfqzdacZ/L8OHqn7yw0DB/2OqJbpzWLU9p4tkG8BSRk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make Flomo memos to motes.'';
            homepage = "https://github.com/jia6y/flomo-to-obsidian";
        };
    }
);
    "flow" = (
    let
        baseUrl = "https://github.com/tavva/flow/releases/download/1.2.4-beta.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flow";
        version = "1.2.4-beta.2";
        outputHash = "sha256-YKlYfkWjtcxgwiN+dWup1ZJTobf6nJeIIEX8yGUjouI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Implements key processes in David Allen"s Getting Things Done (GTD) methodology (Closed source)'';
            homepage = "https://github.com/tavva/flow";
        };
    }
);
    "flowcharts" = (
    let
        baseUrl = "https://github.com/land0r/obsidian-flowchart-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flowcharts";
        version = "1.0.0";
        outputHash = "sha256-UMHvAs0MbPQ5+W4fdarmqv4hfwzLrxFhs6CUJUGgICY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render flowcharts using flowchart.js markup.'';
            homepage = "https://github.com/land0r/obsidian-flowchart-plugin";
        };
    }
);
    "flowershow" = (
    let
        baseUrl = "https://github.com/flowershow/obsidian-flowershow/releases/download/4.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "flowershow";
        version = "4.0.15";
        outputHash = "sha256-3bOipxUajrd9KxYbsQ9LEJ+eKFBml7PvCOyCbbfFGUI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish notes with Flowershow.'';
            homepage = "https://github.com/flowershow/obsidian-flowershow";
        };
    }
);
    "focus-active-sentence" = (
    let
        baseUrl = "https://github.com/artisticat1/focus-active-sentence/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "focus-active-sentence";
        version = "1.1.0";
        outputHash = "sha256-HcI1UliVFLK6BGkV3iSjoCk7hqlcoHbjSmD7KIgdGUY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight the sentence the cursor is currently resting on.'';
            homepage = "https://github.com/artisticat1/focus-active-sentence";
        };
    }
);
    "focus-time" = (
    let
        baseUrl = "https://github.com/astradev123/obsidian-focus-time/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "focus-time";
        version = "1.3.0";
        outputHash = "sha256-pjwvubqk9Vp9PAi2cF/G+snrpEOf4D5BO7ErN7/Tv34=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track the time spent on each note and visualize the data.'';
            homepage = "https://github.com/astradev123/obsidian-focus-time";
        };
    }
);
    "focus-tracker" = (
    let
        baseUrl = "https://github.com/jeetsukumaran/obsidian-focus-tracker/releases/download/4.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "focus-tracker";
        version = "4.0.0";
        outputHash = "sha256-2Guic6kgwcticpEaBNR8Y4t0sG44MblYaDfbCg9TWvU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track and align your focus.'';
            homepage = "https://github.com/jeetsukumaran/obsidian-focus-tracker";
        };
    }
);
    "fold-anywhere" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Fold-Anywhere/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fold-anywhere";
        version = "2.1.1";
        outputHash = "sha256-tS35dO86L9AmqmTo1gddyjVrIDArPpsxpQDjCDzmjtM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set start and end marker, and then fold any text anywhere in live preview mode.'';
            homepage = "https://github.com/Quorafind/Obsidian-Fold-Anywhere";
        };
    }
);
    "fold-properties" = (
    let
        baseUrl = "https://github.com/itsonlyjames/obsidian-fold-properties/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fold-properties";
        version = "0.2.0";
        outputHash = "sha256-/PY3So4sodavJfF7PsiDe4qFAeeGBhuTjOJLzAkKR9c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds fold/unfold properties function to folder and file context menu'';
            homepage = "https://github.com/itsonlyjames/obsidian-fold-properties";
        };
    }
);
    "fold-properties-by-default" = (
    let
        baseUrl = "https://github.com/tbergeron/obsidian-fold-properties-by-default/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fold-properties-by-default";
        version = "1.0.8";
        outputHash = "sha256-9DeUbZtG5Vuy1CLypegriDOZjsS7J/RBxBJHC83B87I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Always have editor/metadata properties folded by default'';
            homepage = "https://github.com/tbergeron/obsidian-fold-properties-by-default";
        };
    }
);
    "folder-by-tags-distributor" = (
    let
        baseUrl = "https://github.com/RevoTale/obsidian-folder-by-tags-distributor/releases/download/1.0.21";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-by-tags-distributor";
        version = "1.0.21";
        outputHash = "sha256-mdVVvGqTvQtKdmSDWf2H/6ajb1hz6moqaofxeldtDMs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically move notes into existing folders by tags specified in note.'';
            homepage = "https://github.com/RevoTale/obsidian-folder-by-tags-distributor";
        };
    }
);
    "folder-file-splitter" = (
    let
        baseUrl = "https://github.com/XuQuan-nikkkki/FolderFile-Splitter-Plugin/releases/download/0.2.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-file-splitter";
        version = "0.2.10";
        outputHash = "sha256-j/2oU/sixtOpSXOJ0AyNtYDl3KuxVFJpXEGkdCTBK1c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Splits folder and file lists into separate views, simplifying navigation, improving clarity, and making it more intuitive to organize your notes'';
            homepage = "https://github.com/XuQuan-nikkkki/FolderFile-Splitter-Plugin";
        };
    }
);
    "folder-filelist" = (
    let
        baseUrl = "https://github.com/band/obsidian-folder-filelist/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-filelist";
        version = "1.2.0";
        outputHash = "sha256-YpbimVXr8agWugluouQPn1SqsTlVVDztFPAYcynW730=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and maintain a list of wikilinks to files in specified folders.'';
            homepage = "https://github.com/band/obsidian-folder-filelist";
        };
    }
);
    "folder-links" = (
    let
        baseUrl = "https://github.com/SteveOverSea/obsidian-folder-links/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-links";
        version = "1.2.5";
        outputHash = "sha256-CvshwWJGET4p/AO5NNAEwsVYtzKnEVwWlnLfU8cipuw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Clicking a link to a folder makes it revealed in the navigation.'';
            homepage = "https://github.com/SteveOverSea/obsidian-folder-links";
        };
    }
);
    "folder-navigator" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-folder-navigator/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-navigator";
        version = "0.5.0";
        outputHash = "sha256-BwKWOpJHbbF1HAmB1VGmRqV9yd5/DSiMMWYFkjtIbGM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly navigate to folders in your vault using fuzzy search.'';
            homepage = "https://github.com/wenlzhang/obsidian-folder-navigator";
        };
    }
);
    "folder-note-plugin" = (
    let
        baseUrl = "https://github.com/xpgo/obsidian-folder-note-plugin/releases/download/0.7.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-note-plugin";
        version = "0.7.3";
        outputHash = "sha256-pq5SQEWNaXc06jbfVrfKf3NV3XM83JSUrHMtu3jTYh0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add description note to a folder.'';
            homepage = "https://github.com/xpgo/obsidian-folder-note-plugin";
        };
    }
);
    "folder-notes" = (
    let
        baseUrl = "https://github.com/LostPaul/obsidian-folder-notes/releases/download/1.8.20-1-beta";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-notes";
        version = "1.8.20-1-beta";
        outputHash = "sha256-6h0pLP2ybR/KSvTankMl3QgcAgfX2N4dxJfXFp2mAeE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes within folders that can be accessed without collapsing the folder, similar to the functionality offered in Notion.'';
            homepage = "https://github.com/LostPaul/obsidian-folder-notes";
        };
    }
);
    "folder-overview" = (
    let
        baseUrl = "https://github.com/LostPaul/obsidian-folder-overview/releases/download/0.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-overview";
        version = "0.5.3";
        outputHash = "sha256-1vTqczRac74xtRAqkBdILzh+OrU/T8ebLdG3JgY8xJI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides a dynamic overview of your vault or folders in the format of a codeblock.'';
            homepage = "https://github.com/LostPaul/obsidian-folder-overview";
        };
    }
);
    "folder-periodic-notes" = (
    let
        baseUrl = "https://github.com/andrewheekin/obsidian-folder-periodic-notes/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-periodic-notes";
        version = "1.0.7";
        outputHash = "sha256-ghYAYKoHwIlrqg/SGwvNa5h7FjmItS9IAZInZILlvrA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Periodic notes in a year, month, and day folder hierarchy.'';
            homepage = "https://github.com/andrewheekin/obsidian-folder-periodic-notes";
        };
    }
);
    "folder-tabulation" = (
    let
        baseUrl = "https://github.com/SpeedaRJ/obsidian_folder_tabulation/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folder-tabulation";
        version = "1.0.2";
        outputHash = "sha256-SqjMzs/DSpBkCC6ZjWgFRXDaMXhvPcPOB/wVsRWc3jg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables navigation between files in a folder via hotkeys and commands.'';
            homepage = "https://github.com/SpeedaRJ/obsidian_folder_tabulation";
        };
    }
);
    "foldercanvas" = (
    let
        baseUrl = "https://github.com/nancyel/obsidian-foldercanvas-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "foldercanvas";
        version = "1.0.3";
        outputHash = "sha256-D0QVFAhNbBL83EbzT20jbwv8yt/ZQGso7HKw+KNMMPU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate a Canvas view of your folder structure.'';
            homepage = "https://github.com/nancyel/obsidian-foldercanvas-plugin";
        };
    }
);
    "folders2graph" = (
    let
        baseUrl = "https://github.com/ratibus11/folders2graph/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "folders2graph";
        version = "1.1.1";
        outputHash = "sha256-cI5vXDjyNXnO/08H17mxJ3Z5M0K3LBWFUPo1KVVqTLA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display your vault folder structure into your graphs.'';
            homepage = "https://github.com/ratibus11/folders2graph";
        };
    }
);
    "font-size" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-font-size/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "font-size";
        version = "0.1.4";
        outputHash = "sha256-/vKHeJ/mm13QWADV0eE9kAhNuyGx+/eLt16+i+QswVA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adjust font size via commands.'';
            homepage = "https://github.com/RyotaUshio/obsidian-font-size";
        };
    }
);
    "fontsource" = (
    let
        baseUrl = "https://github.com/fontsource/obsidian-fontsource/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fontsource";
        version = "1.0.0";
        outputHash = "sha256-co/Na2SH4iW/Q9cn8AAqaQGEGIqGEraDXTg6cPui9J0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Load custom fonts from Fontsource into your notes.'';
            homepage = "https://github.com/fontsource/obsidian-fontsource";
        };
    }
);
    "food-tracker" = (
    let
        baseUrl = "https://github.com/forketyfork/obsidian-food-tracker/releases/download/0.17.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "food-tracker";
        version = "0.17.2";
        outputHash = "sha256-VyPJIC8ySFIEW7+HapBRaMhTLK2e5kHE4LNO6wTAsfY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track your food intake (calories, macronutrients) and nutritional information'';
            homepage = "https://github.com/forketyfork/obsidian-food-tracker";
        };
    }
);
    "foodiary" = (
    let
        baseUrl = "https://github.com/vkostyanetsky/ObsidianFoodiary/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "foodiary";
        version = "1.2.5";
        outputHash = "sha256-WobamQezia/wHt0HGJzHzEsmY4cSohGZks5ULblr8Eo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Food tracker, macronutrient and calorie calculator.'';
            homepage = "https://github.com/vkostyanetsky/ObsidianFoodiary";
        };
    }
);
    "footlinks" = (
    let
        baseUrl = "https://github.com/DahaWong/obsidian-footlinks/releases/download/0.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "footlinks";
        version = "0.1.7";
        outputHash = "sha256-/uJesi7w9jok5Hv/n7/Eli5MqnkUDrnbEAFbgafSctY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract URLs from main text to footer.'';
            homepage = "https://github.com/DahaWong/obsidian-footlinks";
        };
    }
);
    "force-read-mode" = (
    let
        baseUrl = "https://github.com/al3xw/force-read-mode/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "force-read-mode";
        version = "1.0.4";
        outputHash = "sha256-gSPtk1Wb6f8QzPoTP/B+Uy+W0Y1OZss3UlReBLUd0ss=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Forces Markdown files in specified folders to open in read-only mode.'';
            homepage = "https://github.com/al3xw/force-read-mode";
        };
    }
);
    "format-hotkeys-obsidian" = (
    let
        baseUrl = "https://github.com/anstosa/format-hotkeys-obsidian/releases/download/0.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "format-hotkeys-obsidian";
        version = "0.1.7";
        outputHash = "sha256-HXaUcAlIFYrW4q+pf/pKN3G4T4DNoIWUf4qxm/pDvhs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Google Docs style formatting hotkeys.'';
            homepage = "https://github.com/anstosa/format-hotkeys-obsidian";
        };
    }
);
    "format-with-prettier" = (
    let
        baseUrl = "https://github.com/alexgavrusev/obsidian-format-with-prettier/releases/download/0.17.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "format-with-prettier";
        version = "0.17.0";
        outputHash = "sha256-bw8AmOfcIjZeuoYmJJRkRTrkQU2W2YMJ7q2eyGJ+CUw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format files in your vault using Prettier.'';
            homepage = "https://github.com/alexgavrusev/obsidian-format-with-prettier";
        };
    }
);
    "formatto-format" = (
    let
        baseUrl = "https://github.com/evasquare/formatto/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "formatto-format";
        version = "1.3.2";
        outputHash = "sha256-+aKr4v447S07OMedk30WzTdQTcbW5jNO0k7JPjqF+34=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple, fast, and easy-to-use Markdown formatter.'';
            homepage = "https://github.com/evasquare/formatto";
        };
    }
);
    "forms" = (
    let
        baseUrl = "https://github.com/sorinmircea/obsidian-forms-plugin/releases/download/1.0.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "forms";
        version = "1.0.18";
        outputHash = "sha256-FISZhQbhQZ5BbMyup6XR0FRoWkS6yb65lL1qYA0bPGk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish beautiful forms and get responses straight to your vault.'';
            homepage = "https://github.com/sorinmircea/obsidian-forms-plugin";
        };
    }
);
    "fountain" = (
    let
        baseUrl = "https://github.com/bgrundmann/obsidian-fountain/releases/download/0.26.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fountain";
        version = "0.26.2";
        outputHash = "sha256-n3128makcELgikB+gbpWoL3gEx+sRIY7Csx6EgGuYYE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write and view screenplays in Fountain format with index cards and lots of other features.'';
            homepage = "https://github.com/bgrundmann/obsidian-fountain";
        };
    }
);
    "fountain-editor" = (
    let
        baseUrl = "https://github.com/chuangcaleb/obsidian-fountain-editor/releases/download/1.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fountain-editor";
        version = "1.4.4";
        outputHash = "sha256-iT4V9jjfDU2K0EqIUMdM+2A/kAaF2cTOKr+MgQiTe+w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fountain (screenplay) syntax highlighting in the editor.'';
            homepage = "https://github.com/chuangcaleb/obsidian-fountain-editor";
        };
    }
);
    "freeform" = (
    let
        baseUrl = "https://github.com/tmcw/obsidian-freeform/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "freeform";
        version = "1.0.3";
        outputHash = "sha256-FWgPTdYrjW8WRaoXI/p0/SdCXa1EvgrzT3eNQ53cP0w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create visualizations, run custom JS, and mix live programs with your notes'';
            homepage = "https://github.com/tmcw/obsidian-freeform";
        };
    }
);
    "friend-tracker" = (
    let
        baseUrl = "https://github.com/buzzguy/friend-tracker/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "friend-tracker";
        version = "1.3.3";
        outputHash = "sha256-dGA6whpYYoU8o7+b4Ve4yRwD4kogHP+c4lQYA+nux/k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep track of friends, birthdays, and interactions'';
            homepage = "https://github.com/buzzguy/friend-tracker";
        };
    }
);
    "front-matter-timestamps" = (
    let
        baseUrl = "https://github.com/pookhaosc1/obsidian-front-matter-timestamps/releases/download/1.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "front-matter-timestamps";
        version = "1.5.3";
        outputHash = "sha256-pw0D1jIij8DLxVhOTGC/KkhrCrVqtxzb28UDO7j5DC8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically manages and updates "created" and "modified" timestamps in the frontmatter of your notes.'';
            homepage = "https://github.com/pookhaosc1/obsidian-front-matter-timestamps";
        };
    }
);
    "frontmatter-alias-display" = (
    let
        baseUrl = "https://github.com/notmuhammad/obsidian-frontmatter-alias-display/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-alias-display";
        version = "1.0.1";
        outputHash = "sha256-r25dqoKZ1J1cUbvgFFA8YBP/gzYMWIAU1kwKhue7VII=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show frontmatter aliases as display names in the File Explorer.'';
            homepage = "https://github.com/notmuhammad/obsidian-frontmatter-alias-display";
        };
    }
);
    "frontmatter-generator" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/Obsidian-Frontmatter-Generator/releases/download/1.0.24";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-generator";
        version = "1.0.24";
        outputHash = "sha256-22X9P8RMC60aNwFq4PCFbxR0AQeXmsQu/t7vYSE0/xw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate frontmatter for your notes from JSON and JavaScript.'';
            homepage = "https://github.com/HananoshikaYomaru/Obsidian-Frontmatter-Generator";
        };
    }
);
    "frontmatter-link-classes" = (
    let
        baseUrl = "https://github.com/zmeeeeeva/obsidian-plugin-metadata-link-classes/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-link-classes";
        version = "1.0.0";
        outputHash = "sha256-Rc59PMRYID8XwpJPl5/HU+jv7l3geeV50dTh1x1iJxI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds classes to internal links based on frontmatter metadata.'';
            homepage = "https://github.com/zmeeeeeva/obsidian-plugin-metadata-link-classes";
        };
    }
);
    "frontmatter-markdown-links" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-frontmatter-markdown-links/releases/download/2.6.31";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-markdown-links";
        version = "2.6.31";
        outputHash = "sha256-OELRdA294BvaW/sMdkdn5PqVA/H29HX8MgGCBEXtrgw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds support for markdown links in frontmatter.'';
            homepage = "https://github.com/mnaoumov/obsidian-frontmatter-markdown-links";
        };
    }
);
    "frontmatter-modified-date" = (
    let
        baseUrl = "https://github.com/alangrainger/obsidian-frontmatter-modified-date/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-modified-date";
        version = "1.5.2";
        outputHash = "sha256-clGC6u1ml5QTNKV67SJcPU746eNNr+C6ACk5OU7bH/A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically update a frontmatter modified date field when the file is modified.'';
            homepage = "https://github.com/alangrainger/obsidian-frontmatter-modified-date";
        };
    }
);
    "frontmatter-to-html-attributes" = (
    let
        baseUrl = "https://github.com/IllDepence/obsidian-frontmatter-to-html-attributes/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-to-html-attributes";
        version = "0.1.0";
        outputHash = "sha256-3/E//TBKsi1lMbj1fkJ/3i3NRAwkc8lXoDECcRR1B48=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makes YAML frontmatter available as data-* attributes in HTML, enabling metadata based CSS styling.'';
            homepage = "https://github.com/IllDepence/obsidian-frontmatter-to-html-attributes";
        };
    }
);
    "frontmatter-viewmode" = (
    let
        baseUrl = "https://github.com/yunidev-uk/obsidian-frontmatter-viewmode/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "frontmatter-viewmode";
        version = "1.0.3";
        outputHash = "sha256-hO13zR5vpa2DsGli32r97vRyfd5+05og5ROTdzOLxIw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use YAML frontmatter to specify a view mode per note.'';
            homepage = "https://github.com/yunidev-uk/obsidian-frontmatter-viewmode";
        };
    }
);
    "full-screen-cross-platform" = (
    let
        baseUrl = "https://github.com/DonkeyPacific/obsidian-full-screen-cross-platform-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "full-screen-cross-platform";
        version = "1.0.3";
        outputHash = "sha256-OuQUtxhvf6KKVlRc/Xm38PfVvXR7FralmR+qoAy4fNk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle fullscreen across all platforms.'';
            homepage = "https://github.com/DonkeyPacific/obsidian-full-screen-cross-platform-plugin";
        };
    }
);
    "future-dates" = (
    let
        baseUrl = "https://github.com/slonoed/obsidian-future-dates/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "future-dates";
        version = "1.3.4";
        outputHash = "sha256-TUndEuB0Le+kE0KCMmRehMkc3B3DdMNOE0TMgkrM9T4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show list of future dates in vault'';
            homepage = "https://github.com/slonoed/obsidian-future-dates";
        };
    }
);
    "fuzzy-chinese-pinyin" = (
    let
        baseUrl = "https://github.com/lazyloong/obsidian-fuzzy-chinese/releases/download/2.28.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fuzzy-chinese-pinyin";
        version = "2.28.0";
        outputHash = "sha256-jBud+8s346l9STUUkGqV0LETfBjyjZqNOlt7abHTLWU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fuzzy search using Chinese Pinyin.'';
            homepage = "https://github.com/lazyloong/obsidian-fuzzy-chinese";
        };
    }
);
    "fuzzy-note-creator" = (
    let
        baseUrl = "https://github.com/HaloGamer33/Obsidian-Fuzzy-Note-Creator/releases/download/0.6.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "fuzzy-note-creator";
        version = "0.6.3";
        outputHash = "sha256-kyanz4y1rDAoA3Q73/K9CQZQoqJSzS3YXd1fCNJBYa0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes in folders with the help of a fuzzy finder.'';
            homepage = "https://github.com/HaloGamer33/Obsidian-Fuzzy-Note-Creator";
        };
    }
);
    "game-search" = (
    let
        baseUrl = "https://github.com/CMorooney/obsidian-game-search-plugin/releases/download/0.2.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "game-search";
        version = "0.2.14";
        outputHash = "sha256-eu+rSVK9KcALUCDnp6q8ZcyzA47nd3aETy8AGmAP73Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Help insert game metadata into game notes or vaults, optionally sync with Steam Library and Wishlist'';
            homepage = "https://github.com/CMorooney/obsidian-game-search-plugin";
        };
    }
);
    "gamified-pkm" = (
    let
        baseUrl = "https://github.com/saertna/obsidian-gamified-pkm/releases/download/0.0.97";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gamified-pkm";
        version = "0.0.97";
        outputHash = "sha256-QzY2pzYLFzWuXOvpmZEmB2a9QJ1jNn129/bbQ7ZpVPo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your Personal Knowledge Management with gamification elements. Boost motivation and achieve growth as you engage with your PKM.'';
            homepage = "https://github.com/saertna/obsidian-gamified-pkm";
        };
    }
);
    "garble-text" = (
    let
        baseUrl = "https://github.com/kurakart/garble-text/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "garble-text";
        version = "1.2.0";
        outputHash = "sha256-+J/Oec+8YISBhJf5DwFPMGFWN/12wXyRen4H6PuFL+o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn all content in the app (notes, sidebar, etc) into garbled text so you can take screenshots without exposing sensitive data.'';
            homepage = "https://github.com/kurakart/garble-text";
        };
    }
);
    "gay-toolbar" = (
    let
        baseUrl = "https://github.com/ChasKane/gay-toolbar/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gay-toolbar";
        version = "2.1.1";
        outputHash = "sha256-WT/jTd0x7HDj/oFobwYVvniZHmr71DXtpU27E93ns7I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Colorful, customizable toolbar, designed for mobile'';
            homepage = "https://github.com/ChasKane/gay-toolbar";
        };
    }
);
    "gdscript-syntax-highlighting" = (
    let
        baseUrl = "https://github.com/RobTheFiveNine/obsidian-gdscript/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gdscript-syntax-highlighting";
        version = "1.1.0";
        outputHash = "sha256-x1/tAE6oGtnhbq6anxrmJ1CVJ1J3NpG/N+kG1WLqH7o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add live GDScript syntax highlighting to code block.'';
            homepage = "https://github.com/RobTheFiveNine/obsidian-gdscript";
        };
    }
);
    "gemini-assistant" = (
    let
        baseUrl = "https://github.com/eatgrass/obsidian-gemini-assistant/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gemini-assistant";
        version = "1.0.4";
        outputHash = "sha256-5MXDgKNTzlEJ41Rckx4JthransL/mj3t98GbT2kH4Ws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Your Gemini AI assistant'';
            homepage = "https://github.com/eatgrass/obsidian-gemini-assistant";
        };
    }
);
    "gemini-generator" = (
    let
        baseUrl = "https://github.com/BjarneRentz/obsidian-gemini-generator/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gemini-generator";
        version = "1.3.3";
        outputHash = "sha256-LiCn0qrqXJpTczJrWpSB/BEVFfzJRuN1F38XSGISOds=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Let Google Gemini generator your notes!'';
            homepage = "https://github.com/BjarneRentz/obsidian-gemini-generator";
        };
    }
);
    "gemini-scribe" = (
    let
        baseUrl = "https://github.com/allenhutchison/obsidian-gemini/releases/download/4.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gemini-scribe";
        version = "4.7.0";
        outputHash = "sha256-wgJCQGktwRS2c3mpFNgjapqSG/2C4A/QDmS/nIj8wDk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to interact with Gemini and use your notes as context.'';
            homepage = "https://github.com/allenhutchison/obsidian-gemini";
        };
    }
);
    "gemmy" = (
    let
        baseUrl = "https://github.com/ericaxu/gemmy/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gemmy";
        version = "1.0.1";
        outputHash = "sha256-w427OZ2qP+EU3aaZ2cQyVcSxW+ElyZJDOi6EnhPg0qg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Obsidian Unhelper. 2023 April Fool"s plugin brought to you by Obsidian.'';
            homepage = "https://github.com/ericaxu/gemmy";
        };
    }
);
    "gene-ai" = (
    let
        baseUrl = "https://github.com/MatissJurevics/Gene-AI/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gene-ai";
        version = "1.3.4";
        outputHash = "sha256-30T/VixXxQgxOsOepmzWh1v//E/CvZ2AK0pkCkrPQX0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate text using the OpenAI API.'';
            homepage = "https://github.com/MatissJurevics/Gene-AI";
        };
    }
);
    "generate-hash" = (
    let
        baseUrl = "https://github.com/zigahertz/obsidian-hash/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "generate-hash";
        version = "1.0.2";
        outputHash = "sha256-fD/VsLLA2VW0NRavx3FesYtfPIQvfOalCvOumyj9N9w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates a cryptographically strong pseudorandom hash.'';
            homepage = "https://github.com/zigahertz/obsidian-hash";
        };
    }
);
    "generate-timeline" = (
    let
        baseUrl = "https://github.com/Shanshuimei/obsidian-generate-timeline/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "generate-timeline";
        version = "1.2.6";
        outputHash = "sha256-r6on7SDaOmNj+kUKfOZ/JPeND3nY9F6KXYNwNy2T2oA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate timelines from tag or folder automatically by any time properties.'';
            homepage = "https://github.com/Shanshuimei/obsidian-generate-timeline";
        };
    }
);
    "generic-initiative-tracker" = (
    let
        baseUrl = "https://github.com/beaurancourt/obsidian-generic-initiative-tracker/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "generic-initiative-tracker";
        version = "1.3.0";
        outputHash = "sha256-JkGNPBHGi1A/2pTVX7g2YPFGATtVxu9z/+PfkwGsNAc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''TTRPG generic initiative tracker.'';
            homepage = "https://github.com/beaurancourt/obsidian-generic-initiative-tracker";
        };
    }
);
    "geocoding-properties" = (
    let
        baseUrl = "https://github.com/jose-elias-alvarez/obsidian-geocoding-properties/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "geocoding-properties";
        version = "2.1.0";
        outputHash = "sha256-AsFXa15H9CBi4IabPMXI4YApkK4YQxBkB5D3eM83SzI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert address and location data from geocoding APIs as YAML properties.'';
            homepage = "https://github.com/jose-elias-alvarez/obsidian-geocoding-properties";
        };
    }
);
    "get-info-plugin" = (
    let
        baseUrl = "https://github.com/chetachiezikeuzor/Get-Info-Plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "get-info-plugin";
        version = "1.1.0";
        outputHash = "sha256-32R6dp5pgzrS4NtEj7XwydxxUEKV2RxT35h/G4yV1cE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tuck a menu inside your status bar and show helpful information for your chosen file.'';
            homepage = "https://github.com/chetachiezikeuzor/Get-Info-Plugin";
        };
    }
);
    "get-stock-information" = (
    let
        baseUrl = "https://github.com/mikejongbloet/obsidian-get-stock-information/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "get-stock-information";
        version = "1.0.0";
        outputHash = "sha256-EvEq2QEUAkl2+wEt7iYqZYQAkLNBd7KOp/oPvQPp7us=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take a stock symbol and returns a callout block with the latest stock information.'';
            homepage = "https://github.com/mikejongbloet/obsidian-get-stock-information";
        };
    }
);
    "geulo-youtube-liked-video" = (
    let
        baseUrl = "https://github.com/zzunebye/obsidian-google-liked-video/releases/download/3.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "geulo-youtube-liked-video";
        version = "3.1.2";
        outputHash = "sha256-mZbPCQOrE911XyUczjrPkKU76v4V87WB+hbj2U2lrY4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch all the YouTube videos you have liked, search and sort them with multiple sort options, and add them to your daily note.'';
            homepage = "https://github.com/zzunebye/obsidian-google-liked-video";
        };
    }
);
    "gh-links-shortener" = (
    let
        baseUrl = "https://github.com/dbarnett/obsidian-gh-links-shortener/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gh-links-shortener";
        version = "0.1.0";
        outputHash = "sha256-4XXHNYh8+NSBqJlbqQRlqelOExiySgDkgWbKSpV4pMg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Modifies pasted GitHub links to use short GitHub ref text as the link title.'';
            homepage = "https://github.com/dbarnett/obsidian-gh-links-shortener";
        };
    }
);
    "ghcat-reminder" = (
    let
        baseUrl = "https://github.com/anil-e/obsidian_gchat_plugin/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ghcat-reminder";
        version = "1.0.6";
        outputHash = "sha256-kP+9c2AcsCpqt5+2kTq9YbJhYcum7UlAa+jWMmpANjs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send notifications to Google Chat Webhook based on due dates in tasks.'';
            homepage = "https://github.com/anil-e/obsidian_gchat_plugin";
        };
    }
);
    "ghost-fade-focus" = (
    let
        baseUrl = "https://github.com/skipadu/obsidian-ghost-fade-focus/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ghost-fade-focus";
        version = "3.0.0";
        outputHash = "sha256-7stHP1rXZf5o8rbxIvx97Oj1WuZ19PSO40yHec4R7+U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Focused on the current line, others faded like a ghost!'';
            homepage = "https://github.com/skipadu/obsidian-ghost-fade-focus";
        };
    }
);
    "giphy" = (
    let
        baseUrl = "https://github.com/LucJager/giphy-plugin/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "giphy";
        version = "1.0.8";
        outputHash = "sha256-Ocs0oVg7ykHvjvSh2d3q9U8rbk1ppXKXSnTFJYJ2kbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and insert gifs in a note.'';
            homepage = "https://github.com/LucJager/giphy-plugin";
        };
    }
);
    "gistr" = (
    let
        baseUrl = "https://github.com/Aetherinox/obsidian-gistr/releases/download/1.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gistr";
        version = "1.7.1";
        outputHash = "sha256-lWthLGhscX9OIBEVK4JeRDjxV9Lj0ftASFly+KMuSBw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use your notes to embed, create, and update gists for Github and Opengist.'';
            homepage = "https://github.com/Aetherinox/obsidian-gistr";
        };
    }
);
    "git-changelog" = (
    let
        baseUrl = "https://github.com/shumadrid/obsidian-git-changelog/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "git-changelog";
        version = "0.6.0";
        outputHash = "sha256-dFsaxPyIWMUJwwelBILISIEPCPktG8cW0NGIDrmVWc0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Uses Git to display dynamic vault and file changelogs in the sidebar, useful for spotting data loss.'';
            homepage = "https://github.com/shumadrid/obsidian-git-changelog";
        };
    }
);
    "git-file-explorer" = (
    let
        baseUrl = "https://github.com/MateusMolina/obsidian-git-file-explorer/releases/download/0.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "git-file-explorer";
        version = "0.7.1";
        outputHash = "sha256-aY4DYgBVYMtwl22xeycCWujRB79XCBiIhiE8ufAoTVI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add relevant git information of detected git repositories to the file explorer.'';
            homepage = "https://github.com/MateusMolina/obsidian-git-file-explorer";
        };
    }
);
    "git-integration" = (
    let
        baseUrl = "https://github.com/noradroid/obsidian-git-integration/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "git-integration";
        version = "0.4.0";
        outputHash = "sha256-iJYK5sSgoXRY00dpCDj7HdkFSuEgiI7EE4uudMNNkRE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily backup vault on a remote repository.'';
            homepage = "https://github.com/noradroid/obsidian-git-integration";
        };
    }
);
    "git-url" = (
    let
        baseUrl = "https://github.com/khuongduy354/obsidian-git-url/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "git-url";
        version = "1.1.0";
        outputHash = "sha256-d8cKLl3WDgSTBvLSezKZsNrZ9sYyTNLbl/a/qvvwKr0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a url to your file on your git remote repo.'';
            homepage = "https://github.com/khuongduy354/obsidian-git-url";
        };
    }
);
    "githobs" = (
    let
        baseUrl = "https://github.com/GabAlpha/obsidian-githobs/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "githobs";
        version = "1.1.1";
        outputHash = "sha256-ybTosp8BXHZxc0ullyHrLnuq+p8PYRL5i1qx35rO49E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use Obsidian as Github issue editor with logic taken from Git'';
            homepage = "https://github.com/GabAlpha/obsidian-githobs";
        };
    }
);
    "github" = (
    let
        baseUrl = "https://github.com/kazhuravlev/obsidian-github/releases/download/1.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github";
        version = "1.11.0";
        outputHash = "sha256-8qP25o1bsHfpX7TiDhf38ja2KqKzFpJaN9OwZ5Ra1TY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your data from GitHub into your Vault.'';
            homepage = "https://github.com/kazhuravlev/obsidian-github";
        };
    }
);
    "github-copilot" = (
    let
        baseUrl = "https://github.com/Pierrad/obsidian-github-copilot/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-copilot";
        version = "1.2.2";
        outputHash = "sha256-HXQAplgT9hUH7+0PN06mwzNNbpTIhzhY3vvc7Q+aZPk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Implement suggestions from Github Copilot directly in your editor.'';
            homepage = "https://github.com/Pierrad/obsidian-github-copilot";
        };
    }
);
    "github-embeds" = (
    let
        baseUrl = "https://github.com/MrGVSV/obsidian-github-embeds/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-embeds";
        version = "1.0.7";
        outputHash = "sha256-QjDFOlxLKayKmnwq6ooftE+1atGenkH6ygqWS/Z0Aq8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed GitHub issues, PRs, and code snippets.'';
            homepage = "https://github.com/MrGVSV/obsidian-github-embeds";
        };
    }
);
    "github-gitless-sync" = (
    let
        baseUrl = "https://github.com/silvanocerza/github-gitless-sync/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-gitless-sync";
        version = "1.0.7";
        outputHash = "sha256-kICkHAgNIvchx6on0MCuc8dpmVkoxuVPXNQBPRU0j/I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync a GitHub repository with vaults on different platforms without requiring git installation'';
            homepage = "https://github.com/silvanocerza/github-gitless-sync";
        };
    }
);
    "github-issue-augmentation" = (
    let
        baseUrl = "https://github.com/samprintz/obsidian-issue-augmentation-plugin/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-issue-augmentation";
        version = "0.1.0";
        outputHash = "sha256-uoLSEjvLrC7sAvbBPB9Au6vhsaZZNe2Nd5eqJudn23U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Augment GitHub issue IDs.'';
            homepage = "https://github.com/samprintz/obsidian-issue-augmentation-plugin";
        };
    }
);
    "github-issues" = (
    let
        baseUrl = "https://github.com/LonoxX/obsidian-github-issues/releases/download/1.9.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-issues";
        version = "1.9.2";
        outputHash = "sha256-/qXdFIeNKY/NykwOiMuokslWcmBwGNcauMFgSlvNgNM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track Github Issues and pull requests directly in your vault'';
            homepage = "https://github.com/LonoxX/obsidian-github-issues";
        };
    }
);
    "github-link" = (
    let
        baseUrl = "https://github.com/nathonius/obsidian-github-link/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-link";
        version = "1.2.0";
        outputHash = "sha256-0Taqv2om47h8UlRfQkMW7O+3WCWTUKxAfbnjn8lI7RQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enrich your notes with issue and pull request content from GitHub.'';
            homepage = "https://github.com/nathonius/obsidian-github-link";
        };
    }
);
    "github-stars" = (
    let
        baseUrl = "https://github.com/flyingnobita/obsidian-github-stars/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-stars";
        version = "1.5.1";
        outputHash = "sha256-KSvpUfz7OZkEUVkICBMsimBPQEoxWynY0HLi4pSIbck=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays the number of stars for GitHub repositories mentioned in notes.'';
            homepage = "https://github.com/flyingnobita/obsidian-github-stars";
        };
    }
);
    "github-sync" = (
    let
        baseUrl = "https://github.com/kevinmkchin/Obsidian-GitHub-Sync/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-sync";
        version = "1.0.6";
        outputHash = "sha256-9kc6ImbxLmZWKj9i8poqBlRoJR3dUgGPLaFqj8L3TFg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync vault to personal GitHub.'';
            homepage = "https://github.com/kevinmkchin/Obsidian-GitHub-Sync";
        };
    }
);
    "github-tasks" = (
    let
        baseUrl = "https://github.com/Epistemic-Technology/obsidian-github-tasks/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-tasks";
        version = "1.1.0";
        outputHash = "sha256-DwOp9+iNDFYgVUg+MZIg0/mp6TTpR7A1pUNuC/x/JPw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync issues and pull requests from your GitHub account to tasks.'';
            homepage = "https://github.com/Epistemic-Technology/obsidian-github-tasks";
        };
    }
);
    "github-tracker" = (
    let
        baseUrl = "https://github.com/schaier-io/obsidian-github-tracker-plugin/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "github-tracker";
        version = "1.1.1";
        outputHash = "sha256-upHaUGlkNAUEdGptThSSmghLNTgtqClBMc6LijAut2g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track GitHub issues and pull requests in your vault'';
            homepage = "https://github.com/schaier-io/obsidian-github-tracker-plugin";
        };
    }
);
    "gitlab-wiki-export" = (
    let
        baseUrl = "https://github.com/jrabmer/obsidian-to-gitlab-wiki/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gitlab-wiki-export";
        version = "1.2.3";
        outputHash = "sha256-yLEk5y65ERvxGvG9bW5sEFisGpGRqxdnnM3Up52F7lU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makes your entire vault Gitlab Wiki compatible and exports it to a specified location.'';
            homepage = "https://github.com/jrabmer/obsidian-to-gitlab-wiki";
        };
    }
);
    "gladdis" = (
    let
        baseUrl = "https://github.com/AurelienStebe/Gladdis/releases/download/0.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gladdis";
        version = "0.6.2";
        outputHash = "sha256-zIEzpprnp+7KBiwgzKmB33v8vYCCA7O5ZXz8ywUEO4Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Gladdis (Generative Language Artificial Dedicated & Diligent Intelligence System) - it"s an AI chatbot.'';
            homepage = "https://github.com/AurelienStebe/Gladdis";
        };
    }
);
    "glasp" = (
    let
        baseUrl = "https://github.com/glasp-co/obsidian-glasp-plugin/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "glasp";
        version = "0.1.6";
        outputHash = "sha256-KFInmlv+fI6egkI6TrzQ9w1RAqQX+zvxeUiznH/3evE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import your Glasp highlights and notes into your vault.'';
            homepage = "https://github.com/glasp-co/obsidian-glasp-plugin";
        };
    }
);
    "global-markdown-encrypt" = (
    let
        baseUrl = "https://github.com/shlemiel/globaloe/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "global-markdown-encrypt";
        version = "0.0.7";
        outputHash = "sha256-PS5uM0FlfMdVWKxruBPXXy68Cu+fnOnMUUjjfmFpCPA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''In-memory AES256-GCM Markdown encryption.'';
            homepage = "https://github.com/shlemiel/globaloe";
        };
    }
);
    "global-proxy" = (
    let
        baseUrl = "https://github.com/windingblack/obsidian-global-proxy/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "global-proxy";
        version = "1.0.5";
        outputHash = "sha256-8sgN6v1WpSHXTh2QZY1KOC9QO145S4GILEx8tluDigg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Configure network proxies for users in areas with restricted networks.'';
            homepage = "https://github.com/windingblack/obsidian-global-proxy";
        };
    }
);
    "global-search-and-replace" = (
    let
        baseUrl = "https://github.com/MahmoudFawzyKhalil/obsidian-global-search-and-replace/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "global-search-and-replace";
        version = "0.5.0";
        outputHash = "sha256-pi2NP1/g/kJuQwism6afiHgCxVQ6XgR8BqA48Dpipqk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and replace in all vault files.'';
            homepage = "https://github.com/MahmoudFawzyKhalil/obsidian-global-search-and-replace";
        };
    }
);
    "glsl-viewer" = (
    let
        baseUrl = "https://github.com/iY0Yi/obsidian_glslviewer/releases/download/0.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "glsl-viewer";
        version = "0.1.8";
        outputHash = "sha256-V1zsX2u5e4l6NKI53ArbDY8VXnvVjP0CUXZFGi1X2E4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preview GLSL shaders.'';
            homepage = "https://github.com/iY0Yi/obsidian_glslviewer";
        };
    }
);
    "gnome-terminal-loader" = (
    let
        baseUrl = "https://github.com/CheeseCake87/gnome-terminal-loader/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gnome-terminal-loader";
        version = "1.0.0";
        outputHash = "sha256-FloweAhS5aSaEZehXVEXo2P1FvcMEN2pvqub13Bpv6k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sidebar action icons to quickly open the Gnome Terminal or to have the Gnome Terminal run a Python module.'';
            homepage = "https://github.com/CheeseCake87/gnome-terminal-loader";
        };
    }
);
    "go-up" = (
    let
        baseUrl = "https://github.com/JinmuGo/obsidian-go-up/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "go-up";
        version = "1.1.2";
        outputHash = "sha256-6tOoGsLm+w/RjgU0HqwbrZowv3pTqyAS08S5eZuHqtI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly navigate to a specified "parent" page'';
            homepage = "https://github.com/JinmuGo/obsidian-go-up";
        };
    }
);
    "goal-tracker" = (
    let
        baseUrl = "https://github.com/GizmoRay/obsidian-goal-tracker/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "goal-tracker";
        version = "1.0.8";
        outputHash = "sha256-/7xHvwBJyWkC/XQcyc5TlN7bkvShLHfPVM4NTE19I5A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track your goals with a calendar view'';
            homepage = "https://github.com/GizmoRay/obsidian-goal-tracker";
        };
    }
);
    "goban-sgf" = (
    let
        baseUrl = "https://github.com/StinsonZhao/obsidian-plugin-goban-sgf/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "goban-sgf";
        version = "1.3.3";
        outputHash = "sha256-94T8hUihhtIN0yMtlOqg/C9THmq3IUREZlpnDI/o3fc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Record Go games (SGF format goban).'';
            homepage = "https://github.com/StinsonZhao/obsidian-plugin-goban-sgf";
        };
    }
);
    "goboard" = (
    let
        baseUrl = "https://github.com/dsokolov/goboard/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "goboard";
        version = "1.0.0";
        outputHash = "sha256-QME6NwoVF7oxM1Zt4gozLWjqS9V5KP7j+aEXBi8307g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Go game diagrams from Markdown code blocks.'';
            homepage = "https://github.com/dsokolov/goboard";
        };
    }
);
    "google-blogger" = (
    let
        baseUrl = "https://github.com/privet-kitty/obsidian-blogger/releases/download/0.9.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-blogger";
        version = "0.9.7";
        outputHash = "sha256-Bx8Wt1u7c+CdnPIXOs+y8I9Jtc/ocanzkxKPiE6PpCY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish notes to Google Blogger.'';
            homepage = "https://github.com/privet-kitty/obsidian-blogger";
        };
    }
);
    "google-calendar" = (
    let
        baseUrl = "https://github.com/YukiGasai/obsidian-google-calendar/releases/download/1.10.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-calendar";
        version = "1.10.16";
        outputHash = "sha256-Q7TkSYZC5/MB5H2Y3Yi1S2B9lkQiG2L7z/vddRKNdPg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interact with your Google Calendar.'';
            homepage = "https://github.com/YukiGasai/obsidian-google-calendar";
        };
    }
);
    "google-calendar-importer" = (
    let
        baseUrl = "https://github.com/lexafaxine/GoogleCalendarImporter/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-calendar-importer";
        version = "1.1.1";
        outputHash = "sha256-TCNpx07sXV70H4jH0yF/lnf9NGWjoQDwnN2jHbWD2K4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple and light-weighted google calendar importer, allow injecting the events / tasks of a day automatically to your daily notes, or import it to anywhere with a command.'';
            homepage = "https://github.com/lexafaxine/GoogleCalendarImporter";
        };
    }
);
    "google-contacts" = (
    let
        baseUrl = "https://github.com/aleksejs1/obsidian-contact-sync-plugin/releases/download/1.1.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-contacts";
        version = "1.1.11";
        outputHash = "sha256-GzSE++lc/OuKG+p7Xcyz/u+WuLwpL8o9QYcw/gwuDLw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize your Google Contacts with separate contact-notes'';
            homepage = "https://github.com/aleksejs1/obsidian-contact-sync-plugin";
        };
    }
);
    "google-drive-sync" = (
    let
        baseUrl = "https://github.com/RichardX366/Obsidian-Google-Drive/releases/download/2.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-drive-sync";
        version = "2.2.2";
        outputHash = "sha256-6priTJAs5+xi9++imV4DPKKT42Cpx73kVTXXjFIq2xs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncs a vault into Google Drive for cross-platform use (works for iOS).'';
            homepage = "https://github.com/RichardX366/Obsidian-Google-Drive";
        };
    }
);
    "google-keep-import" = (
    let
        baseUrl = "https://github.com/daledesilva/obsidian_google-keep-import/releases/download/1.1.31";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-keep-import";
        version = "1.1.31";
        outputHash = "sha256-gBTS3dH3MMxrRgK+CHVljXfHLpCqQ1Pd9tgYof/K0AY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Google Keep backup json files and their attachments. Can also be used to import other files.'';
            homepage = "https://github.com/daledesilva/obsidian_google-keep-import";
        };
    }
);
    "google-photos" = (
    let
        baseUrl = "https://github.com/alangrainger/obsidian-google-photos/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "google-photos";
        version = "2.0.0";
        outputHash = "sha256-CJeB9oT6TCFarLZCYWg8bavKxMQi7rKRRTQNrXVopyg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Google Photos integration.'';
            homepage = "https://github.com/alangrainger/obsidian-google-photos";
        };
    }
);
    "gpg-crypt" = (
    let
        baseUrl = "https://github.com/tejado/obsidian-gpgCrypt/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gpg-crypt";
        version = "0.6.0";
        outputHash = "sha256-IAQT3MU/TC/s0BZTLHdCmS6oiaJwv5JyWwzkwc0cdCY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Encrypt your notes using GPG. Supports smartcards for enhanced security! Keep your information safe and accessible only to you.'';
            homepage = "https://github.com/tejado/obsidian-gpgCrypt";
        };
    }
);
    "gpg-encrypt" = (
    let
        baseUrl = "https://github.com/lajg-dev/Obsidian-Plugin-GPG-Inline-Encrypt/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gpg-encrypt";
        version = "1.1.5";
        outputHash = "sha256-Rb6DjqXTYOfu7SeTHLQJL4t6EDEjwDiaILjn9AkUF1o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Encrypt partial text or complete notes using GPG technology, it is compatible with security keys such as YubiKey or traditional GPG encryption methods'';
            homepage = "https://github.com/lajg-dev/Obsidian-Plugin-GPG-Inline-Encrypt";
        };
    }
);
    "gpt-assistant" = (
    let
        baseUrl = "https://github.com/M7mdisk/obsidian-gpt/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gpt-assistant";
        version = "0.1.3";
        outputHash = "sha256-ySEdCymfSphvy7ygIAML9wx6L8X17BlfCVbNbWhvB8M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use a GPT-3 based model on your notes and get personalized answers from your knowledge base.'';
            homepage = "https://github.com/M7mdisk/obsidian-gpt";
        };
    }
);
    "gpt-liteinquirer" = (
    let
        baseUrl = "https://github.com/ittuann/obsidian-gpt-liteinquirer-plugin/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gpt-liteinquirer";
        version = "1.1.5";
        outputHash = "sha256-7Ak7zZoJVvscCflXa2fsidLTTNShZiUhKzjNyFqhLxA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Experience OpenAI ChatGPT assistance, drafting content without interrupting your creative flow.'';
            homepage = "https://github.com/ittuann/obsidian-gpt-liteinquirer-plugin";
        };
    }
);
    "gpt3-notes" = (
    let
        baseUrl = "https://github.com/micahke/obsidian-gpt3-notes/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gpt3-notes";
        version = "0.2.8";
        outputHash = "sha256-yx1uMhRYp0b+wr1xnDJvvldua1Nl83jVGW4tUcUKPaE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate notes on any subject using OpenAI"s GPT-3.5 and GPT-4 language models.'';
            homepage = "https://github.com/micahke/obsidian-gpt3-notes";
        };
    }
);
    "granola-sync" = (
    let
        baseUrl = "https://github.com/tomelliot/obsidian-granola-sync/releases/download/2.0.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "granola-sync";
        version = "2.0.19";
        outputHash = "sha256-/bxJrNsMGV77jAb+kemkNKVWvLoOd+s4M6pMuLDalL8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Granola notes to your vault.'';
            homepage = "https://github.com/tomelliot/obsidian-granola-sync";
        };
    }
);
    "graph-analysis" = (
    let
        baseUrl = "https://github.com/SkepticMystic/graph-analysis/releases/download/0.15.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "graph-analysis";
        version = "0.15.4";
        outputHash = "sha256-BTDZJMmTpi3CxOTPsaCnbbC2xhYbKoPfJN/+RX6JSC8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find hidden connections between notes in your vault using cool graph algorithms.'';
            homepage = "https://github.com/SkepticMystic/graph-analysis";
        };
    }
);
    "graph-banner" = (
    let
        baseUrl = "https://github.com/ras0q/obsidian-graph-banner/releases/download/2.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "graph-banner";
        version = "2.3.3";
        outputHash = "sha256-yG2QXYFDcxew67AgqKjwyPc4FqaJMdEEFWVWp16Gfks=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a local graph view to the note header'';
            homepage = "https://github.com/ras0q/obsidian-graph-banner";
        };
    }
);
    "graph-link-types" = (
    let
        baseUrl = "https://github.com/natefrisch01/Graph-Link-Types/releases/download/0.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "graph-link-types";
        version = "0.3.3";
        outputHash = "sha256-xgjDLe9z9oOiYF/4Hd2O7nZxT0gOv6NRJ9KLvJzoPNg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link types for graph view.'';
            homepage = "https://github.com/natefrisch01/Graph-Link-Types";
        };
    }
);
    "graph-nested-tags" = (
    let
        baseUrl = "https://github.com/drPilman/obsidian-graph-nested-tags/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "graph-nested-tags";
        version = "1.0.4";
        outputHash = "sha256-LMZr2cWd7LOC+HdVmC1kRFqict+TPvOF4abX37qAGOE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link nested tags in graph view.'';
            homepage = "https://github.com/drPilman/obsidian-graph-nested-tags";
        };
    }
);
    "graphic-organizer" = (
    let
        baseUrl = "https://github.com/nickfreedom/obsidian-graphic-organizer/releases/download/0.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "graphic-organizer";
        version = "0.1.7";
        outputHash = "sha256-zqrrNQTbaKjWqMz+kLkqKsG4c2scily1FCpeGlwB7Fw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interactive tree view for visualizing and managing your vault"s file hierarchy.'';
            homepage = "https://github.com/nickfreedom/obsidian-graphic-organizer";
        };
    }
);
    "graphs" = (
    let
        baseUrl = "https://github.com/DylanHojnoski/obsidian-graphs/releases/download/1.9.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "graphs";
        version = "1.9.3";
        outputHash = "sha256-O9OHaP7UcAm2z1/j1xl+T5CrtpVI7dRpVbR059xSMZQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create interactive graphs by writing YAML'';
            homepage = "https://github.com/DylanHojnoski/obsidian-graphs";
        };
    }
);
    "grappling-hook" = (
    let
        baseUrl = "https://github.com/chrisgrieser/grappling-hook/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "grappling-hook";
        version = "1.4.0";
        outputHash = "sha256-qrd2KOEsklONlt4gf3fdY4H7v3WZwdWUDzdYCZcQWVg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Blazingly fast file switching to bookmarked files. For people for whom using the Quick Switcher still takes too much time.'';
            homepage = "https://github.com/chrisgrieser/grappling-hook";
        };
    }
);
    "gridexplorer" = (
    let
        baseUrl = "https://github.com/Devon22/obsidian-gridexplorer/releases/download/3.1.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gridexplorer";
        version = "3.1.12";
        outputHash = "sha256-yY4uFzfzxuUgTz95hwjmaNZ4rn5Pp5EPOCEtBxj1iT8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Browse note files in a grid view.'';
            homepage = "https://github.com/Devon22/obsidian-gridexplorer";
        };
    }
);
    "grind-manager" = (
    let
        baseUrl = "https://github.com/dromse/obsidian-gamified-tasks/releases/download/1.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "grind-manager";
        version = "1.11.0";
        outputHash = "sha256-MxQqsejiSS4/L1JvC0Zps16r7AeXtorpn0zA4ivZPVk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Gamify your task management with rewards system, craft your tasks by tags.'';
            homepage = "https://github.com/dromse/obsidian-gamified-tasks";
        };
    }
);
    "gslogimaker-my-bible" = (
    let
        baseUrl = "https://github.com/GsLogiMaker/my-bible-obsidian-plugin/releases/download/1.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gslogimaker-my-bible";
        version = "1.3.5";
        outputHash = "sha256-COjPMHMT49mPYC1kWGr6F4+q2qKLLMR9GuzrgYe3bsE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Your own customizable markdown bible for your personal vault!'';
            homepage = "https://github.com/GsLogiMaker/my-bible-obsidian-plugin";
        };
    }
);
    "gtd-no-next-step" = (
    let
        baseUrl = "https://github.com/saibotsivad/obsidian-gtd-no-next-step/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gtd-no-next-step";
        version = "1.1.0";
        outputHash = "sha256-qtKL+tijm0qdenybNcGlFgrO2P9SCZ/yLO9x9HoX2eI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a badge to Getting Things Done (GTD) "project" files with no defined next step.'';
            homepage = "https://github.com/saibotsivad/obsidian-gtd-no-next-step";
        };
    }
);
    "gtp-preview" = (
    let
        baseUrl = "https://github.com/Barba828/obsidian-plugin-gtp-preview/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gtp-preview";
        version = "1.0.6";
        outputHash = "sha256-/fVn4vrmat6aAkqNCmIA8olTZUXgyEn/EDuTQZ2KmYE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Supports rendering of GuitarPro files such as `gtp/gp/gp5/gpx`.'';
            homepage = "https://github.com/Barba828/obsidian-plugin-gtp-preview";
        };
    }
);
    "guid-front-matter" = (
    let
        baseUrl = "https://github.com/llimllib/obsidian-guid-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "guid-front-matter";
        version = "1.2.0";
        outputHash = "sha256-dXl04F52gSpacOndLdYMQmcBN5xBo+Wd7Jk7HAHfIFY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a globally unique ID to every Markdown document"s frontmatter.'';
            homepage = "https://github.com/llimllib/obsidian-guid-plugin";
        };
    }
);
    "guid-renamer" = (
    let
        baseUrl = "https://github.com/taskscape/ObsidianUniqueFileName/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "guid-renamer";
        version = "1.0.0";
        outputHash = "sha256-RKuVAL0U8NnQTx5Y/tvpLRDAc+EkuLshZe21tQ1T8lQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename selected file with a random GUID'';
            homepage = "https://github.com/taskscape/ObsidianUniqueFileName";
        };
    }
);
    "guitar-chord" = (
    let
        baseUrl = "https://github.com/Barba828/obsidian-plugin-chord/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "guitar-chord";
        version = "1.0.5";
        outputHash = "sha256-c0a6iqEzz7LYEaYNFX4pnKuPg2DEJBFAmixIBoPo1OY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly enter and display guitar chords, with optional chords based on music theory. No need to write in code blocks, they can be inserted and edited directly in the document.'';
            homepage = "https://github.com/Barba828/obsidian-plugin-chord";
        };
    }
);
    "gyazo" = (
    let
        baseUrl = "https://github.com/nota/gyazo-obsidian-plugin/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "gyazo";
        version = "1.0.4";
        outputHash = "sha256-JCIS9ACA2RZCR024r14yWJw+eKQPHpynAdoe1Rx/41g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display your Gyazo captures and embed them into your notes.'';
            homepage = "https://github.com/nota/gyazo-obsidian-plugin";
        };
    }
);
    "habit-calendar" = (
    let
        baseUrl = "https://github.com/hedonihilist/obsidian-habit-calendar/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "habit-calendar";
        version = "1.2.0";
        outputHash = "sha256-y/Sot7Q0xH7Vi/GQLQLIhTgpSuRj85Ao1wg80XmLeYY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Monthly Habit Calendar for DataviewJS. Render a calendar inside DataviewJS code block, showing your habit status within a month.'';
            homepage = "https://github.com/hedonihilist/obsidian-habit-calendar";
        };
    }
);
    "habit-tracker" = (
    let
        baseUrl = "https://github.com/Narsail/habit-tracker-obsidian/releases/download/0.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "habit-tracker";
        version = "0.3.4";
        outputHash = "sha256-vm/HXEusiLH7j7WJMeIMDNNHf4k95aWps9IGiqDI1JU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the Habits of a calendar week.'';
            homepage = "https://github.com/Narsail/habit-tracker-obsidian";
        };
    }
);
    "habit-tracker-21" = (
    let
        baseUrl = "https://github.com/zincplusplus/habit-tracker/releases/download/2.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "habit-tracker-21";
        version = "2.4.2";
        outputHash = "sha256-82TdFTK5C7EXPJS6tNoM0EzeDRrWboOf5A25RdcRHjw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A minimalist, elegant habit tracker that helps you build lasting habits with clear progress visualization.'';
            homepage = "https://github.com/zincplusplus/habit-tracker";
        };
    }
);
    "hackerone" = (
    let
        baseUrl = "https://github.com/Neolex-Security/obsidian-hackerone/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hackerone";
        version = "1.1.0";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch HackerOne bug reports.'';
            homepage = "https://github.com/Neolex-Security/obsidian-hackerone";
        };
    }
);
    "hackmd-sync" = (
    let
        baseUrl = "https://github.com/thor314/hackmd-obsidian/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hackmd-sync";
        version = "2.0.3";
        outputHash = "sha256-sIZ58q0I/QQq93te3mjHUMHrH0QyQjoBIz1DFmx9H/8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes to from HackMD'';
            homepage = "https://github.com/thor314/hackmd-obsidian";
        };
    }
);
    "hadith-lookup" = (
    let
        baseUrl = "https://github.com/iadnanmukhtar/obsidian-hadith-lookup-plugin/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hadith-lookup";
        version = "1.2.1";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Inserts Hadith and Quranic ayat and passages using a reference ID from the Ḥadīth Unlocked API (https://hadithunlocked.com)'';
            homepage = "https://github.com/iadnanmukhtar/obsidian-hadith-lookup-plugin";
        };
    }
);
    "halo" = (
    let
        baseUrl = "https://github.com/halo-sigs/obsidian-halo/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "halo";
        version = "1.1.1";
        outputHash = "sha256-Q34gURyXXeCpNGWOiCibBczJpEUEcwnYC05XhNWcles=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish content to Halo sites.'';
            homepage = "https://github.com/halo-sigs/obsidian-halo";
        };
    }
);
    "hamsterbase" = (
    let
        baseUrl = "https://github.com/hamsterbase/obsidian-hamsterbase/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hamsterbase";
        version = "0.8.0";
        outputHash = "sha256-t4B6tE6MxE2P9EZD7Cx5Hl9qbs/W01Eyr66FVkHMDZg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official HamsterBase integration.'';
            homepage = "https://github.com/hamsterbase/obsidian-hamsterbase";
        };
    }
);
    "handlebars-dynamic" = (
    let
        baseUrl = "https://github.com/hided62/obsidian-handlebars-dynamic/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "handlebars-dynamic";
        version = "1.1.2";
        outputHash = "sha256-Gy4/hGzATEqFg+caGlps7ejdm3O8D0z8dSaC0dHvHoo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Handlebars dynamic templating. Define template files and use them dynamically via hb blocks. Template recursion is also possible.'';
            homepage = "https://github.com/hided62/obsidian-handlebars-dynamic";
        };
    }
);
    "handwriting-ocr" = (
    let
        baseUrl = "https://github.com/ikmolbo/handwriting-ocr-obsidian-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "handwriting-ocr";
        version = "1.0.0";
        outputHash = "sha256-N2oTqmKknFp9qmIAIPud7MczExTYi19T7ciLAWLDFLE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform handwritten documents and scanned images into editable text with Handwriting OCR"s AI-powered handwriting to text conversion.'';
            homepage = "https://github.com/ikmolbo/handwriting-ocr-obsidian-plugin";
        };
    }
);
    "handwritten-notes" = (
    let
        baseUrl = "https://github.com/FBarrca/obsidian-handwritten-notes/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "handwritten-notes";
        version = "1.4.0";
        outputHash = "sha256-X7YXg4wuGlvzwLhUpFlfhggwvogJEGMzjmy3cNQkh+0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Annotate PDFs and create handwritten notes inside your vault.'';
            homepage = "https://github.com/FBarrca/obsidian-handwritten-notes";
        };
    }
);
    "hanko" = (
    let
        baseUrl = "https://github.com/Telehakke/hanko/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hanko";
        version = "1.0.1";
        outputHash = "sha256-GbB0YQ5L4+JPclNdhtpU63LjH9YogXBxRWh2ag5A1gY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Register and paste any text.'';
            homepage = "https://github.com/Telehakke/hanko";
        };
    }
);
    "hanzi-writer" = (
    let
        baseUrl = "https://github.com/pakrentos/hanzi-writer-obsidian/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hanzi-writer";
        version = "0.0.1";
        outputHash = "sha256-VJ3rrpbdQ2cfS+cV/cF1leLV9iUaDdzJnA0PBG8UKfI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interactive Chinese character writing blocks using Hanzi Writer'';
            homepage = "https://github.com/pakrentos/hanzi-writer-obsidian";
        };
    }
);
    "hard-breaks" = (
    let
        baseUrl = "https://github.com/bkis/obsidian-hard-breaks/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hard-breaks";
        version = "0.1.1";
        outputHash = "sha256-qGFpItX5MK4IxvzuLZuFpHGpFbgx15GFtnC29DJu6gg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn soft line breaks in Markdown into hard line breaks.'';
            homepage = "https://github.com/bkis/obsidian-hard-breaks";
        };
    }
);
    "hardcover" = (
    let
        baseUrl = "https://github.com/aliceinwaterdeep/obsidian-hardcover/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hardcover";
        version = "2.0.1";
        outputHash = "sha256-OuWQ2ih6XMRX80w86w6KWjwun7c4kqfE1XwNxgmBqWM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Hardcover library to your notes.'';
            homepage = "https://github.com/aliceinwaterdeep/obsidian-hardcover";
        };
    }
);
    "harn-weather" = (
    let
        baseUrl = "https://github.com/marcueberall/obsidian-harn-weather/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "harn-weather";
        version = "1.0.1";
        outputHash = "sha256-xWjxVncfOO9STNTXTpQEzIo1i4mcomBKWLKmyfNm5/s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Weather generator for the HârnMaster: Roleplaying in the World of Kèthîra rule system.'';
            homepage = "https://github.com/marcueberall/obsidian-harn-weather";
        };
    }
);
    "harper" = (
    let
        baseUrl = "https://github.com/Automattic/harper-obsidian-plugin/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "harper";
        version = "2.0.0";
        outputHash = "sha256-fsASiKSphpxYOqy0XZIhJ+5c5FbhmZZRZhMxfgwxgUo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''The fastest grammar and spell checker that respects your privacy.'';
            homepage = "https://github.com/Automattic/harper-obsidian-plugin";
        };
    }
);
    "harpoon" = (
    let
        baseUrl = "https://github.com/rodrez/obsidian-harpoon/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "harpoon";
        version = "1.0.11";
        outputHash = "sha256-3cX8AqeutJee/d684oz4Acinr9bM73w0ja3XeWjHCTk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use shortcuts to manage and navigate your top four frequently-used files.'';
            homepage = "https://github.com/rodrez/obsidian-harpoon";
        };
    }
);
    "hash-pasted-image" = (
    let
        baseUrl = "https://github.com/hardingadonis/hash-pasted-image/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hash-pasted-image";
        version = "1.3.0";
        outputHash = "sha256-Lbz9PSBIxsNnabOmRlt+grolKwTHpMMlpgjPRWD0J4c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto rename pasted images added to the vault via hash algorithm SHA-512'';
            homepage = "https://github.com/hardingadonis/hash-pasted-image";
        };
    }
);
    "hatena" = (
    let
        baseUrl = "https://github.com/takmatsukawa/obsidian-hatena/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hatena";
        version = "1.1.1";
        outputHash = "sha256-HgIklUEWlNBO+cNPRwN7TKVX6u5qgdd8JiefoHel5kc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post directly to your Hatena Blog.'';
            homepage = "https://github.com/takmatsukawa/obsidian-hatena";
        };
    }
);
    "header-adjuster" = (
    let
        baseUrl = "https://github.com/Netajam/header-adjuster/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "header-adjuster";
        version = "1.0.4";
        outputHash = "sha256-HSJkRmPQUUVFteePtPLngqeZ3gkodqPynqSeWvnc9WU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily adjust header levels in Markdown documents by increasing or decreasing their levels. Supports full document adjustments or specified line ranges, with default settings and commands for convenience.'';
            homepage = "https://github.com/Netajam/header-adjuster";
        };
    }
);
    "header-counter" = (
    let
        baseUrl = "https://github.com/nancyel/header-counter/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "header-counter";
        version = "0.1.0";
        outputHash = "sha256-UEJrdP+GIfn1mATjqHDBz5c+V7DeHBVTcLtrtvS54I4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Count the number of headers in the current note.'';
            homepage = "https://github.com/nancyel/header-counter";
        };
    }
);
    "header-enhancer" = (
    let
        baseUrl = "https://github.com/HoBeedzc/obsidian-header-enhancer-plugin/releases/download/0.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "header-enhancer";
        version = "0.5.1";
        outputHash = "sha256-fKY4PZsz/KA51aURYbr2f6svtdPs72FoZ+MmjTs148w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Level up your headers, customize your notes. Header Enhancer makes your notes header better and more useful.'';
            homepage = "https://github.com/HoBeedzc/obsidian-header-enhancer-plugin";
        };
    }
);
    "header-navigation" = (
    let
        baseUrl = "https://github.com/talwrii/obsidian-header-navigation/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "header-navigation";
        version = "1.0.3";
        outputHash = "sha256-HvhiV4oGwgBrpk7+1DwlNTf0+JiSQbmgsix0071KBB4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly jump between headers and toggle folding'';
            homepage = "https://github.com/talwrii/obsidian-header-navigation";
        };
    }
);
    "heading-decorator" = (
    let
        baseUrl = "https://github.com/dragonish/obsidian-heading-decorator/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heading-decorator";
        version = "3.1.0";
        outputHash = "sha256-Qyel1NlBElxlUlIwlX1IxQ7vxXisegJJRmmQy32/VaA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Implement displaying specific content around headings based on their levels.'';
            homepage = "https://github.com/dragonish/obsidian-heading-decorator";
        };
    }
);
    "heading-helper" = (
    let
        baseUrl = "https://github.com/sidkhuntia/obsdian-headings-helper/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heading-helper";
        version = "1.0.3";
        outputHash = "sha256-+zAUrE3JhZrj8QXdikADx4Jn+HhIIi3pXwxNn1XlGI8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Cycle heading levels and display visual heading indicators in the gutter.'';
            homepage = "https://github.com/sidkhuntia/obsdian-headings-helper";
        };
    }
);
    "heading-level-indent" = (
    let
        baseUrl = "https://github.com/svonjoi/obsidian-heading-level-indent/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heading-level-indent";
        version = "3.0.0";
        outputHash = "sha256-6J/UYDGS6j5p3GiYp4VGHUEP9VLW0ULMSsAP2x5aQZI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Indenting content under headers based on their level.'';
            homepage = "https://github.com/svonjoi/obsidian-heading-level-indent";
        };
    }
);
    "heading-toggler" = (
    let
        baseUrl = "https://github.com/Lord-Turmoil/heading-toggler-obsidian/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heading-toggler";
        version = "1.1.2";
        outputHash = "sha256-HDKfDAwNEmnXEwKWl+oeLQXvsZjNtlybUaJX3ea0ZGM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily toggle heading levels in Markdown documents with shortcuts.'';
            homepage = "https://github.com/Lord-Turmoil/heading-toggler-obsidian";
        };
    }
);
    "headings-in-explorer" = (
    let
        baseUrl = "https://github.com/patrickchiang/obsidian-headings-in-explorer/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "headings-in-explorer";
        version = "1.0.3";
        outputHash = "sha256-Lrbma2OS6N9bR7pAaXrmy2V6cZY4WTkNoXoekNtdGcc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show headings in the file explorer.'';
            homepage = "https://github.com/patrickchiang/obsidian-headings-in-explorer";
        };
    }
);
    "heatmap-calendar" = (
    let
        baseUrl = "https://github.com/Richardsl/heatmap-calendar-obsidian/releases/download/0.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heatmap-calendar";
        version = "0.7.1";
        outputHash = "sha256-r1YhsY3pbglDjFYDn14OkuduVmqx1T/d6arPs/LNuaE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Activity Year Overview for DataviewJS, Github style – Track Goals, Progress, Habits, Tasks, Exercise, Finances, "Dont Break the Chain" etc.'';
            homepage = "https://github.com/Richardsl/heatmap-calendar-obsidian";
        };
    }
);
    "heatmap-tracker" = (
    let
        baseUrl = "https://github.com/mokkiebear/heatmap-tracker/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heatmap-tracker";
        version = "2.2.0";
        outputHash = "sha256-K29woRjqS0hOb+XRSWPjEzOvW3PfYK2NA8lL/1gcNlc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize your activity and track goals, progress, habits, tasks, exercise, finances, and more—all in a single, interactive heatmap!'';
            homepage = "https://github.com/mokkiebear/heatmap-tracker";
        };
    }
);
    "hello-nemesis" = (
    let
        baseUrl = "https://github.com/adiguno/hello-nemesis/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hello-nemesis";
        version = "1.0.7";
        outputHash = "sha256-UYjfa/hSQrWQUJMnes5QYr82OSs/634LsjwvnLMq8dI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Uses OpenAI to challenge your ideas.'';
            homepage = "https://github.com/adiguno/hello-nemesis";
        };
    }
);
    "helpmate" = (
    let
        baseUrl = "https://github.com/TfTHacker/obsidian42-HelpMate/releases/download/1.0.26";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "helpmate";
        version = "1.0.26";
        outputHash = "sha256-C2t7ioKXOZuXgv/SxbcHnZxtDagQtD376QIwm+y5ffw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a sidebar view to see help sites for the plugins and themes you have installed.'';
            homepage = "https://github.com/TfTHacker/obsidian42-HelpMate";
        };
    }
);
    "hemingway-mode" = (
    let
        baseUrl = "https://github.com/jobedom/obsidian-hemingway-mode/releases/download/1.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hemingway-mode";
        version = "1.0.15";
        outputHash = "sha256-Hu+EBCYD+uqTibAa4QWd7+1whm+qW6y3nKz+JIVHOy4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prevents any editing, only letting you write ahead.'';
            homepage = "https://github.com/jobedom/obsidian-hemingway-mode";
        };
    }
);
    "hephaistos-importer" = (
    let
        baseUrl = "https://github.com/runa-rist/hephaistos-importer/releases/download/2.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hephaistos-importer";
        version = "2.2.2";
        outputHash = "sha256-oTV1cPfyb/bPOKqitymGVwj1YlkXJ7yJo13fU6SLZjQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Imports main stats from the Starfinder RPG character website Hephaistos.'';
            homepage = "https://github.com/runa-rist/hephaistos-importer";
        };
    }
);
    "heti" = (
    let
        baseUrl = "https://github.com/moeyua/obsidian-heti/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heti";
        version = "1.0.0";
        outputHash = "sha256-V8IuC/T/ddKD090xSqFZN9pefzFUs9Ahxg5d4GuJbj4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''专为中文内容展示设计的排版样式增强。它基于通行的中文排版规范而来，可以带来更好的阅读体验。'';
            homepage = "https://github.com/moeyua/obsidian-heti";
        };
    }
);
    "hexo-auto-updater" = (
    let
        baseUrl = "https://github.com/lifeodyssey/obsidian-hexo-auto-update/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hexo-auto-updater";
        version = "0.0.2";
        outputHash = "sha256-pbc59e4p3lR+ssUFo92nZP+/i/a4VGe8vxqXaPos6jg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Monitor changes in your vault, and automatically commit and push them to your Hexo blog repository.'';
            homepage = "https://github.com/lifeodyssey/obsidian-hexo-auto-update";
        };
    }
);
    "hexo-publisher" = (
    let
        baseUrl = "https://github.com/zhenlohuang/obsidian-hexo-publisher/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hexo-publisher";
        version = "0.0.5";
        outputHash = "sha256-8DCya4Wpd9UJaiJ6jnqQjteimCOHiUuJFanXX52w0I0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes to Hexo.'';
            homepage = "https://github.com/zhenlohuang/obsidian-hexo-publisher";
        };
    }
);
    "hexo-toolkit" = (
    let
        baseUrl = "https://github.com/sissilab/obsidian-hexo-toolkit/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hexo-toolkit";
        version = "1.0.3";
        outputHash = "sha256-yzxRG34kHuAkG1U9WIjpqZreFesgiR+mmm6QsoRhGbo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Maintain Hexo posts.'';
            homepage = "https://github.com/sissilab/obsidian-hexo-toolkit";
        };
    }
);
    "heycalmdown-navigate-cursor-history" = (
    let
        baseUrl = "https://github.com/heycalmdown/navigate-cursor-history/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "heycalmdown-navigate-cursor-history";
        version = "1.1.0";
        outputHash = "sha256-N77afw2wa1wVSk6zb5BIciRkbv7/n5BtrHyG6suWjYQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remember the recent cursor position history and jump back and forth like VS Code.'';
            homepage = "https://github.com/heycalmdown/navigate-cursor-history";
        };
    }
);
    "hi-note" = (
    let
        baseUrl = "https://github.com/CatMuse/HiNote/releases/download/0.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hi-note";
        version = "0.5.5";
        outputHash = "sha256-AMoXz6HmZP5/uXZPTl/0FsXoAdMW0YQ4Emu4pOhSmyg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add comments to highlighted notes, use AI for thinking, and flashcards for memory.'';
            homepage = "https://github.com/CatMuse/HiNote";
        };
    }
);
    "hi-words" = (
    let
        baseUrl = "https://github.com/CatMuse/HiWords/releases/download/0.5.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hi-words";
        version = "0.5.7";
        outputHash = "sha256-Gg2eXjqwt/d35Fh5PiVVlE7BPtopxWbRRdaQXUBqWOg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Effortlessly grow your vocabulary as you read, with automatic highlighting and translation of unfamiliar words.'';
            homepage = "https://github.com/CatMuse/HiWords";
        };
    }
);
    "hidden-folder-obsidian" = (
    let
        baseUrl = "https://github.com/ptrsvltns/hidden-folder-obsidian/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hidden-folder-obsidian";
        version = "1.0.7";
        outputHash = "sha256-JSuh6/HSC9LGjvSb6IQkkXiD1Sl93I3hg6KhSmLk4qQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hidden Folder.'';
            homepage = "https://github.com/ptrsvltns/hidden-folder-obsidian";
        };
    }
);
    "hidden-hyperlinks" = (
    let
        baseUrl = "https://github.com/lawrencefeng17/obsidian-hidden-hyperlinks/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hidden-hyperlinks";
        version = "1.0.3";
        outputHash = "sha256-NDcEWexBtD6PxteQzIPferi91FCje27Pv1wUGbEg3rU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide text behind display text and copy on click.'';
            homepage = "https://github.com/lawrencefeng17/obsidian-hidden-hyperlinks";
        };
    }
);
    "hide-commands-in-menu" = (
    let
        baseUrl = "https://github.com/bomian98/obsidian-hide-commands-in-menu/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hide-commands-in-menu";
        version = "0.1.9";
        outputHash = "sha256-I+IGtWqAd7C0YvlE1HX1XLxhIXMlKpMWm2bDM8jN3b8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to hide any command to different menu.'';
            homepage = "https://github.com/bomian98/obsidian-hide-commands-in-menu";
        };
    }
);
    "hide-folders" = (
    let
        baseUrl = "https://github.com/JonasDoesThings/obsidian-hide-folders/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hide-folders";
        version = "1.5.2";
        outputHash = "sha256-quE+P1SnYo30P7tzQm1/xrylcVme7PgGhGqTPr8cYyM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly toggle the visibility of specific folders in the file navigator based on configured names. Useful for hiding attachment folders.'';
            homepage = "https://github.com/JonasDoesThings/obsidian-hide-folders";
        };
    }
);
    "hide-index-files" = (
    let
        baseUrl = "https://github.com/d7sd6u/obsidian-hide-index-files/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hide-index-files";
        version = "1.0.2";
        outputHash = "sha256-IJavS+mZxIufLDzSHeUvdOeDnmYLjwhb9gmCF/kPsr8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide index files (folder notes) more reliably.'';
            homepage = "https://github.com/d7sd6u/obsidian-hide-index-files";
        };
    }
);
    "hide-traffic-lights" = (
    let
        baseUrl = "https://github.com/tavva/obsidian-plugin-hide-traffic-lights/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hide-traffic-lights";
        version = "1.0.2";
        outputHash = "sha256-81a8MtN9hDVZ9B0bdwq6rglNoNwA2IQsiYlFpavAz+U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hides macOS traffic light buttons for a distraction-free experience. Hover over the top-left corner to reveal them.'';
            homepage = "https://github.com/tavva/obsidian-plugin-hide-traffic-lights";
        };
    }
);
    "hierarchical-backlinks" = (
    let
        baseUrl = "https://github.com/jasonmotylinski/hierarchical-backlinks/releases/download/1.10.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hierarchical-backlinks";
        version = "1.10.2";
        outputHash = "sha256-PpYpW0G01L56q7L2wJ6oA7ZuP2QLwW6yk2rhJEA6Q10=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays backlinks in a tree structure.'';
            homepage = "https://github.com/jasonmotylinski/hierarchical-backlinks";
        };
    }
);
    "hierarchical-outgoing-links" = (
    let
        baseUrl = "https://github.com/jasonmotylinski/hierarchical-outgoing-links/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hierarchical-outgoing-links";
        version = "1.2.2";
        outputHash = "sha256-iTbgZhn7twVovs7kMRqZ3mcTmu42+jZ3h+LkQWuMKs4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays outgoing links in a tree structure.'';
            homepage = "https://github.com/jasonmotylinski/hierarchical-outgoing-links";
        };
    }
);
    "hierarchy" = (
    let
        baseUrl = "https://github.com/kdnk/obsidian-hierarchy/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hierarchy";
        version = "0.7.0";
        outputHash = "sha256-lRMBQjLx2nRI7+qphglxqrAjKdM4snZZhLgOQMY6qrU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize the hierarchy of your link.'';
            homepage = "https://github.com/kdnk/obsidian-hierarchy";
        };
    }
);
    "highlight-active-folder-section" = (
    let
        baseUrl = "https://github.com/justanotherjurastudent/highlight-active-folder-section/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "highlight-active-folder-section";
        version = "1.5.0";
        outputHash = "sha256-tPWPgqtTgrKG8VWJPd0U0VscNxEaub1rCAxmddAEPpU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight the active folder section and the title in the file explorer.'';
            homepage = "https://github.com/justanotherjurastudent/highlight-active-folder-section";
        };
    }
);
    "highlight-helper" = (
    let
        baseUrl = "https://github.com/byfun/obsidian-highlight-helper/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "highlight-helper";
        version = "1.0.1";
        outputHash = "sha256-tgY2Iao9sx55cRPHZ9LMFlezuHToAgXEDXFUkIEmsDc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helper to collect highlights.'';
            homepage = "https://github.com/byfun/obsidian-highlight-helper";
        };
    }
);
    "highlightr-plugin" = (
    let
        baseUrl = "https://github.com/chetachiezikeuzor/Highlightr-Plugin/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "highlightr-plugin";
        version = "1.2.2";
        outputHash = "sha256-0Qk+v2U3B+4Pttj8ydRpMH7czjp5Jwx2TO7qGbX3Nzs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A minimal and aesthetically pleasing highlighting menu that makes color-coded highlighting much easier with a configurable assortment of highlight colors.'';
            homepage = "https://github.com/chetachiezikeuzor/Highlightr-Plugin";
        };
    }
);
    "hill-charts" = (
    let
        baseUrl = "https://github.com/stufro/obsidian-hill-charts/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hill-charts";
        version = "0.2.0";
        outputHash = "sha256-YQF0LhJ5rvwSlPtpVJhJ37P4fL/Z9liaBsnEn/ey5no=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add hill charts to your notes.'';
            homepage = "https://github.com/stufro/obsidian-hill-charts";
        };
    }
);
    "hints-plugin" = (
    let
        baseUrl = "https://github.com/slpbx/obsidian-plugin/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hints-plugin";
        version = "1.2.1";
        outputHash = "sha256-eVCHfoYoPAd812J3pMB1co1W1ahUSZViv6OWfYdP5xU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save data directly to Obsidian with specified template. Capture from Telegram, WhatsApp, Slack, Email, SMS, Raycast and more.'';
            homepage = "https://github.com/slpbx/obsidian-plugin";
        };
    }
);
    "historica" = (
    let
        baseUrl = "https://github.com/nhannht/obsidian-historica/releases/download/0.3.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "historica";
        version = "0.3.6";
        outputHash = "sha256-vOm2s9OqARKKWie/hfUOL/f503l6g9FDnQaoEheszCU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intelligently generates timeline from your content'';
            homepage = "https://github.com/nhannht/obsidian-historica";
        };
    }
);
    "history-today" = (
    let
        baseUrl = "https://github.com/Yaob1990/obsidian-history-today/releases/download/1.0.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "history-today";
        version = "1.0.17";
        outputHash = "sha256-XaFF+z5Uw2wGGl0as/UC/5tuiGQHAt298MMDtEyUG60=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View and review your historical notes from this day across previous years'';
            homepage = "https://github.com/Yaob1990/obsidian-history-today";
        };
    }
);
    "hk-code-block" = (
    let
        baseUrl = "https://github.com/HeekangPark/obsidian-hk-code-block/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hk-code-block";
        version = "0.4.1";
        outputHash = "sha256-a76jIX4O1ZMXuKJm4YySP3f9Va2SsQN5ZHerVGjDycI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improve the display of code blocks in reading view with titles, line numbers, line highlights, and more.'';
            homepage = "https://github.com/HeekangPark/obsidian-hk-code-block";
        };
    }
);
    "hledger-notes" = (
    let
        baseUrl = "https://github.com/bzimor/obsidian_hledger/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hledger-notes";
        version = "1.1.2";
        outputHash = "sha256-IC5upkr4m3ixwkYtsMgN6FTNZSmYrIEZAQnFWqpj+20=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and manage hledger entries directly in your vault.'';
            homepage = "https://github.com/bzimor/obsidian_hledger";
        };
    }
);
    "hoarder-sync" = (
    let
        baseUrl = "https://github.com/jhofker/obsidian-hoarder/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hoarder-sync";
        version = "2.0.0";
        outputHash = "sha256-1/e8mF/HzCxLetWYGtM0d0JAV9BzKNWEqPQtg/CEJHM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Hoarder bookmarks'';
            homepage = "https://github.com/jhofker/obsidian-hoarder";
        };
    }
);
    "home-tab" = (
    let
        baseUrl = "https://github.com/olrenso/obsidian-home-tab/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "home-tab";
        version = "1.2.2";
        outputHash = "sha256-1bFlxu7eUtQizCt6t2CRS6wKl1n5BmWihfEyULUsiMI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A browser-like search tab for your local files.'';
            homepage = "https://github.com/olrenso/obsidian-home-tab";
        };
    }
);
    "homekey-action" = (
    let
        baseUrl = "https://github.com/shichishima/obsidian-homekey-plugin/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "homekey-action";
        version = "0.2.0";
        outputHash = "sha256-hqySE/xBYM9Ixn/FHtvUmFFIF9U7exhWkueXwiTioQc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move cursor to beginning of text, considering Markdown heading characters.'';
            homepage = "https://github.com/shichishima/obsidian-homekey-plugin";
        };
    }
);
    "homepage" = (
    let
        baseUrl = "https://github.com/mirnovov/obsidian-homepage/releases/download/4.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "homepage";
        version = "4.4.0";
        outputHash = "sha256-17COtlzXBHClA3GWe4fXUWA4SCqhZQmQDdD9YM5cCYw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a specified note, canvas, or workspace on startup, or set it for quick access later.'';
            homepage = "https://github.com/mirnovov/obsidian-homepage";
        };
    }
);
    "homework-manager" = (
    let
        baseUrl = "https://github.com/kadisonm/obsidian-homework-plugin/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "homework-manager";
        version = "1.1.1";
        outputHash = "sha256-3ZLRcSMFhRwhUBYf6wwqWcT6L5D0DkGEw6X2ltkdeVM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep track of homework through a to-do list.'';
            homepage = "https://github.com/kadisonm/obsidian-homework-plugin";
        };
    }
);
    "horizontal-blocks" = (
    let
        baseUrl = "https://github.com/iCodeAlchemy/horizontal-blocks/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "horizontal-blocks";
        version = "1.1.2";
        outputHash = "sha256-WJIjW5x430Xb+UCzgJAz//4XGoOcV/vgodLrZPxnT8s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Bring Notion-style layouts with blocks side-by-side, resizable Markdown blocks that support texts, images, embeds, and internal links.'';
            homepage = "https://github.com/iCodeAlchemy/horizontal-blocks";
        };
    }
);
    "hotkey-helper" = (
    let
        baseUrl = "https://github.com/pjeby/hotkey-helper/releases/download/0.3.21";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hotkey-helper";
        version = "0.3.21";
        outputHash = "sha256-MBvgSy2VDG428IMt3CPkR4uqK4O0fCbGTkVLKOKDoss=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily see and access any plugin"s settings or hotkey assignments (and conflicts) from the Community Plugins tab.'';
            homepage = "https://github.com/pjeby/hotkey-helper";
        };
    }
);
    "hotkeysplus-obsidian" = (
    let
        baseUrl = "https://github.com/argenos/hotkeysplus-obsidian/releases/download/0.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hotkeysplus-obsidian";
        version = "0.2.7";
        outputHash = "sha256-8X3Pj/06jEZRTwHvyjENwxtDFmCBa6GmcshjQa+yFqg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Additional hotkeys to do common things.'';
            homepage = "https://github.com/argenos/hotkeysplus-obsidian";
        };
    }
);
    "hotstrings" = (
    let
        baseUrl = "https://github.com/wakywayne/obsidian-hotstrings/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hotstrings";
        version = "1.0.0";
        outputHash = "sha256-KDTXAhuXHhe7YM78YCvYbFUaaTny9DO0HvlvKXWa8Gk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to set custom hot strings that get expanded to text once typed.'';
            homepage = "https://github.com/wakywayne/obsidian-hotstrings";
        };
    }
);
    "hover-external-link" = (
    let
        baseUrl = "https://github.com/jamiebrynes7/obsidian-hover-external-link/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hover-external-link";
        version = "1.0.0";
        outputHash = "sha256-pCTYcOazyUYr4CJt8z/Mct3YgXoM/gGE4X4nshTJhkw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hover on external links to see the destination URL.'';
            homepage = "https://github.com/jamiebrynes7/obsidian-hover-external-link";
        };
    }
);
    "hover-reveal" = (
    let
        baseUrl = "https://github.com/Asrieal/HoverReveal/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hover-reveal";
        version = "1.0.11";
        outputHash = "sha256-ugIzDVrXGVxl5+qtfo3PWjOuVntVlZP9PCYxliioa44=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Shows hidden text in tooltips when hovering over marked elements using [visibleText]{tooltipText} syntax.'';
            homepage = "https://github.com/Asrieal/HoverReveal";
        };
    }
);
    "html-checkboxes" = (
    let
        baseUrl = "https://github.com/anareaty/html-checkboxes/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "html-checkboxes";
        version = "1.1.2";
        outputHash = "sha256-PSTuEbHCb7vvu9p/SZ0mgxhlsa/PQy02C8Iso/YVlVg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows to quickly add HTML checkboxes to your notes and makes them clickable.'';
            homepage = "https://github.com/anareaty/html-checkboxes";
        };
    }
);
    "html-server" = (
    let
        baseUrl = "https://github.com/Pr0dt0s/obsidian-html-server/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "html-server";
        version = "1.0.8";
        outputHash = "sha256-kgiWmxF5ReaoIO/E1IO7V5u0YLKHUqaeDLRk7CAZx5I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Spin up a local http server to access your vault via a web browser.'';
            homepage = "https://github.com/Pr0dt0s/obsidian-html-server";
        };
    }
);
    "html-tabs" = (
    let
        baseUrl = "https://github.com/ptournet/obsidian-html-tabs/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "html-tabs";
        version = "1.1.0";
        outputHash = "sha256-c/ygfbNKJpgQMvOQC6L0xa/AJMQvmEslnkw1TOp66NU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and render Tabs and tab panels in your notes.'';
            homepage = "https://github.com/ptournet/obsidian-html-tabs";
        };
    }
);
    "http-link-maker" = (
    let
        baseUrl = "https://github.com/kennethac/obsidian-http-links-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "http-link-maker";
        version = "1.0.0";
        outputHash = "sha256-87p7h4y2Xnhy7BBqtVS72oBDboDrXSPB4aDVgHPxFb8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate HTTP Links to your vault files for universal URL recognition'';
            homepage = "https://github.com/kennethac/obsidian-http-links-plugin";
        };
    }
);
    "hugo-codeblock-highlight" = (
    let
        baseUrl = "https://github.com/aarol/obsidian-hugo-codeblock-highlight/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hugo-codeblock-highlight";
        version = "1.2.0";
        outputHash = "sha256-/VLVRVRWGmeexs/2XbPgSOMgLYF6fp+5WRGkgMW3myM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlights lines in code blocks using Hugo"s hl_lines syntax.'';
            homepage = "https://github.com/aarol/obsidian-hugo-codeblock-highlight";
        };
    }
);
    "hugo-preview-obsidian" = (
    let
        baseUrl = "https://github.com/fzdwx/hugo-preview-obsidian/releases/download/0.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hugo-preview-obsidian";
        version = "0.1.7";
        outputHash = "sha256-7TBcgAX1cXzPxSzxN5EfwU89GkwH4kQSYbZ6NlPojM0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preview your Hugo site.'';
            homepage = "https://github.com/fzdwx/hugo-preview-obsidian";
        };
    }
);
    "hugo-publish" = (
    let
        baseUrl = "https://github.com/kirito41dd/obsidian-hugo-publish/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hugo-publish";
        version = "1.0.13";
        outputHash = "sha256-Trx5OjIbLA1ajnKDZAdtuFxAnv2RuI6cYTgH2VxH1tc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your blog to hugo site.'';
            homepage = "https://github.com/kirito41dd/obsidian-hugo-publish";
        };
    }
);
    "hugo-sync" = (
    let
        baseUrl = "https://github.com/crayhuang/Obsidian-Hugo-Sync/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hugo-sync";
        version = "0.0.3";
        outputHash = "sha256-csHbjfFj+OKlQ1pdAD06hFUmO19Acei5goxnqG5ltuA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync and convert the selected notes to Hugo blog.'';
            homepage = "https://github.com/crayhuang/Obsidian-Hugo-Sync";
        };
    }
);
    "hunchly" = (
    let
        baseUrl = "https://github.com/shadowoption/Hunchly-obsidian-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hunchly";
        version = "1.0.2";
        outputHash = "sha256-aJ7wRuRIFYXExr1fqmizB7F7bp7w3Knl3iiIP9w1dJ4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import notes and images from Hunchly.'';
            homepage = "https://github.com/shadowoption/Hunchly-obsidian-plugin";
        };
    }
);
    "hydrate" = (
    let
        baseUrl = "https://github.com/hydrateagent/hydrate/releases/download/0.1.37";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hydrate";
        version = "0.1.37";
        outputHash = "sha256-iKqnggQgaPbX6fIXOpBfFTI+/DZCAxRdTnNvEk7Xhww=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Cursor-like note agent with MCP support.'';
            homepage = "https://github.com/hydrateagent/hydrate";
        };
    }
);
    "hyperlink-remover" = (
    let
        baseUrl = "https://github.com/AlphaHasher/obsidian-remove-links/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "hyperlink-remover";
        version = "2.4.0";
        outputHash = "sha256-y1D3EZ4Tkly0I/J5HEG+YMHN3YCaIX/DdtASnN5z6ps=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily remove hyperlinks and wikilinks from selected text or the entire note.'';
            homepage = "https://github.com/AlphaHasher/obsidian-remove-links";
        };
    }
);
    "ibook" = (
    let
        baseUrl = "https://github.com/bingryan/obsidian-ibook-plugin/releases/download/2.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ibook";
        version = "2.0.12";
        outputHash = "sha256-OOntDf+lE501dlCvQwDIiJfFsaiJoWDUnNdSXVMDLao=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export your Apple iBook highlights and annotations into your vault.'';
            homepage = "https://github.com/bingryan/obsidian-ibook-plugin";
        };
    }
);
    "ical" = (
    let
        baseUrl = "https://github.com/andrewbrereton/obsidian-to-ical-plugin/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ical";
        version = "2.0.1";
        outputHash = "sha256-smt6uuJ3UssSL0M+VG/iadMvTr0Nk+nKxWs+0injau8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Scan your vault for tasks that contain dates. Create an iCalendar file and store it in your vault or on Gist. You can then show this calendar in any iCal-compatible client such as Outlook, Google Calendar, Apple Calendar, etc.'';
            homepage = "https://github.com/andrewbrereton/obsidian-to-ical-plugin";
        };
    }
);
    "icloud-contacts" = (
    let
        baseUrl = "https://github.com/Trulsaa/obsidian-icloud-contacts/releases/download/0.4.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "icloud-contacts";
        version = "0.4.15";
        outputHash = "sha256-p/XzeaUJADquHfzevrYiXjG8UQBuB9hCDTUEnk048Jc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Imports contacts from iCloud and manages a note for each contact.'';
            homepage = "https://github.com/Trulsaa/obsidian-icloud-contacts";
        };
    }
);
    "iconic" = (
    let
        baseUrl = "https://github.com/gfxholo/iconic/releases/download/1.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "iconic";
        version = "1.1.9";
        outputHash = "sha256-x+0QNEE5sVUZ8M+/Hw1suCTQe0q2VUN416Xr6lqwtoU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize your icons and their colors directly from the UI, including tabs, files & folders, bookmarks, tags, properties, and ribbon commands.'';
            homepage = "https://github.com/gfxholo/iconic";
        };
    }
);
    "iconoir-icons" = (
    let
        baseUrl = "https://github.com/gapmiss/iconoir-icons/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "iconoir-icons";
        version = "1.0.0";
        outputHash = "sha256-cWEQ1iaOJvyXMb3IgF98mfiiGhgaJatHQPbvW6VT2pc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and display customized SVG Iconoir icons.'';
            homepage = "https://github.com/gapmiss/iconoir-icons";
        };
    }
);
    "ics" = (
    let
        baseUrl = "https://github.com/open-horizon-labs/obsidian-ics/releases/download/1.12.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ics";
        version = "1.12.0";
        outputHash = "sha256-93DQyakHRW+0oiae3UXNkcVvOXPerSlnM46M2RxNe0Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add events from calendar ics on the web to daily notes on demand. Includes vdir support. Daily Planner, Templater and Dataview friendly.'';
            homepage = "https://github.com/open-horizon-labs/obsidian-ics";
        };
    }
);
    "idealogs-annotator" = (
    let
        baseUrl = "https://github.com/idlgcl/obsidialogs/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "idealogs-annotator";
        version = "1.1.5";
        outputHash = "sha256-crRdD5+O5t3PzT+WYLmh+gW5gxNUgbWqchBajA6N/IY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''For viewing and linking Idealogs articles.'';
            homepage = "https://github.com/idlgcl/obsidialogs";
        };
    }
);
    "idle-monitor" = (
    let
        baseUrl = "https://github.com/alberto98fx/idle-monitor-obsidian/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "idle-monitor";
        version = "1.0.5";
        outputHash = "sha256-nWA14LiXYil7NVgETUajTQc/byjTmO1l3TojhPZANNM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get notified when you stop typing to stay motivated.'';
            homepage = "https://github.com/alberto98fx/idle-monitor-obsidian";
        };
    }
);
    "idorecall" = (
    let
        baseUrl = "https://github.com/iDoRecall/idorecall/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "idorecall";
        version = "1.0.0";
        outputHash = "sha256-fNOxEIkp4/uMAiBzhxwVP7/8M/haqNzoQmXv5sI0moo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create recalls from your notes.'';
            homepage = "https://github.com/iDoRecall/idorecall";
        };
    }
);
    "ignore-filters-boost" = (
    let
        baseUrl = "https://github.com/Lavton/obsidian-ignore-filter-shortcut/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ignore-filters-boost";
        version = "1.0.2";
        outputHash = "sha256-6h91CelbkP/hQRrC66Rbu89wKzJ69knPEfutk/XP5YM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Update excluded files from the file explorer.'';
            homepage = "https://github.com/Lavton/obsidian-ignore-filter-shortcut";
        };
    }
);
    "ii-quicker" = (
    let
        baseUrl = "https://github.com/wish5115/obsidian-ii-quicker/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ii-quicker";
        version = "1.1.6";
        outputHash = "sha256-GWTwan7X23Hqti/irTPoKdF5mPnZ5Yg0W+GHvNRbYH8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly insert common Markdown code and HTML code, and customize your own insertion commands.'';
            homepage = "https://github.com/wish5115/obsidian-ii-quicker";
        };
    }
);
    "image-border-style" = (
    let
        baseUrl = "https://github.com/shenoy-anurag/obsidian-image-border-style/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-border-style";
        version = "1.2.0";
        outputHash = "sha256-fNUXO9ATxSyU3JOKV4Wy5zAZpwRYboW3aoC6eGELOPg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Border styling for images in markdown notes.'';
            homepage = "https://github.com/shenoy-anurag/obsidian-image-border-style";
        };
    }
);
    "image-captions" = (
    let
        baseUrl = "https://github.com/alangrainger/obsidian-image-captions/releases/download/1.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-captions";
        version = "1.1.7";
        outputHash = "sha256-YfgK4f0QfPydatm3qo6+04Cisdcuejo/IqvVBlsyUOg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add captions to images with inline Markdown and link support. The caption format is compatible with the Commonmark spec and other Markdown applications.'';
            homepage = "https://github.com/alangrainger/obsidian-image-captions";
        };
    }
);
    "image-classify-paste" = (
    let
        baseUrl = "https://github.com/ostoe/Ob-ImagePastePlugin/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-classify-paste";
        version = "0.1.4";
        outputHash = "sha256-pb8Bo80xv9CUbz4iaufhuOk4TtScvpN9CV/LYkoRVbQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste your image like Typora, the image link name not ![[Paste xxx]] but ![some name](relative-directory/xxx.png) with a relative directory.'';
            homepage = "https://github.com/ostoe/Ob-ImagePastePlugin";
        };
    }
);
    "image-converter" = (
    let
        baseUrl = "https://github.com/xRyul/obsidian-image-converter/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-converter";
        version = "1.4.3";
        outputHash = "sha256-nunsdQoB9em9gYuUzR8nSbNXxsAhpT/R55+Vfn3rtQA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert, compress, resize, annotate, markup, draw, crop, rotate, flip, align, drag-resize, rename with variables, and batch process images: WEBP, JPG, PNG, HEIC, TIF'';
            homepage = "https://github.com/xRyul/obsidian-image-converter";
        };
    }
);
    "image-darkmodifier" = (
    let
        baseUrl = "https://github.com/0qln/obsidian-image-darkmodifier/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-darkmodifier";
        version = "1.2.0";
        outputHash = "sha256-tDc/3H1hFHvK56EgEeasPXr1YZMHQQCuXKe/ck/FbSU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn your inline images into darkmode (...and more!).'';
            homepage = "https://github.com/0qln/obsidian-image-darkmodifier";
        };
    }
);
    "image-embedder" = (
    let
        baseUrl = "https://github.com/sky150/obsidian-image-embedder/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-embedder";
        version = "1.0.0";
        outputHash = "sha256-1OFkoyoY0ty1wp0e6+4EFOauIshKciJKGibf1QE1NJE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically downloads and embeds images from URLs when pasting'';
            homepage = "https://github.com/sky150/obsidian-image-embedder";
        };
    }
);
    "image-helper" = (
    let
        baseUrl = "https://github.com/byfun/obsidian-image-helper/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-helper";
        version = "1.0.1";
        outputHash = "sha256-AvEMkyVn5JFM840wvFFXbXL2Mh7VX7bKdhBfWuHas+E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Context menu to convert image format in reading view.'';
            homepage = "https://github.com/byfun/obsidian-image-helper";
        };
    }
);
    "image-inline" = (
    let
        baseUrl = "https://github.com/ZackaryW/obsidian-image-inline/releases/download/3.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-inline";
        version = "3.0.4";
        outputHash = "sha256-WerIZyoXsmPFRX5tu+t4r+gsMpHVUvhySp41ZLL91eE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste your image without attachment files'';
            homepage = "https://github.com/ZackaryW/obsidian-image-inline";
        };
    }
);
    "image-magician" = (
    let
        baseUrl = "https://github.com/luxmargos/obsidian-image-magician-plugin/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-magician";
        version = "0.1.9";
        outputHash = "sha256-31cjQUM+aY47A4SE9DYgcIvl2WyPSg80gAHPtTkt3PQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Supports viewing and exporting various image formats using ImageMagick.'';
            homepage = "https://github.com/luxmargos/obsidian-image-magician-plugin";
        };
    }
);
    "image-metadata" = (
    let
        baseUrl = "https://github.com/alexeiskachykhin/obsidian-image-metadata-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-metadata";
        version = "1.2.0";
        outputHash = "sha256-7W5yx9cLYVLwvOAWpGmMq0SlnT18qVfw9anNJvQ7n+w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Annotate photos with Exif and other metadata right from the image viewer screen.'';
            homepage = "https://github.com/alexeiskachykhin/obsidian-image-metadata-plugin";
        };
    }
);
    "image-notes-photes-io" = (
    let
        baseUrl = "https://github.com/Kanaries/photes-io-obsidian-plugin/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-notes-photes-io";
        version = "0.3.1";
        outputHash = "sha256-f6Ag2NC9Mgs5qIm05XDiPU8gnxCS3DguOSsqvcRZLJ0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn your images into text notes with AI.'';
            homepage = "https://github.com/Kanaries/photes-io-obsidian-plugin";
        };
    }
);
    "image-ocr" = (
    let
        baseUrl = "https://github.com/kaffarell/obsidian-tesseract-ocr/releases/download/0.3.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-ocr";
        version = "0.3.8";
        outputHash = "sha256-8ts5ui/RPmTvgnAEpxPpuz3Yu9MaToXb64ePswLf++c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run OCR on images and paste the result in the description.'';
            homepage = "https://github.com/kaffarell/obsidian-tesseract-ocr";
        };
    }
);
    "image-picker" = (
    let
        baseUrl = "https://github.com/ariamckinley/obsidian-image-picker/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-picker";
        version = "1.1.1";
        outputHash = "sha256-P3gDn8Q6LlHVbc3rlKSBnuuyX3NzOnIDFKah0OE9rVg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A blazingly fast way to browse, search, and embed pictures across your vault'';
            homepage = "https://github.com/ariamckinley/obsidian-image-picker";
        };
    }
);
    "image-preview-on-icon-hover" = (
    let
        baseUrl = "https://github.com/rama1997/Image-Preview-On-Icon-Hover/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-preview-on-icon-hover";
        version = "1.0.3";
        outputHash = "sha256-PwUXqESEV0EOW6jZ1Wgv/fi0pYT0AE2k/0RS+EnrexU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds custom image previews when hovering over various UI icons.'';
            homepage = "https://github.com/rama1997/Image-Preview-On-Icon-Hover";
        };
    }
);
    "image-search" = (
    let
        baseUrl = "https://github.com/razeghi71/obsidian-image-search/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-search";
        version = "0.1.0";
        outputHash = "sha256-HAtvzRhJ8yyeiTQc6MYW8kXsS4nWF76LAyk4olyZd+o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and insert images using Google API'';
            homepage = "https://github.com/razeghi71/obsidian-image-search";
        };
    }
);
    "image-share" = (
    let
        baseUrl = "https://github.com/iqijun/obsidian-image-share/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-share";
        version = "0.1.2";
        outputHash = "sha256-zb2D41B6kl/ZXuyvV4I//oZYP/CI/f6HHCSMJTwWIQM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share selected text as beautiful images.'';
            homepage = "https://github.com/iqijun/obsidian-image-share";
        };
    }
);
    "image-size" = (
    let
        baseUrl = "https://github.com/cynicalight/obsidian-image-size/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-size";
        version = "1.1.2";
        outputHash = "sha256-Mn/jmILc3ETv4e3BBhWbE/P9QKBpGTviupfGT85ws8U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set the default size for pasted images.'';
            homepage = "https://github.com/cynicalight/obsidian-image-size";
        };
    }
);
    "image-to-text-ocr" = (
    let
        baseUrl = "https://github.com/dario-baumberger/obsidian-image-to-text-ocr/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-to-text-ocr";
        version = "1.0.4";
        outputHash = "sha256-JeMJEONHhX+pHv50caAhLJxPC9+DLs9hGmbnXcH68MY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a image in your note to text.'';
            homepage = "https://github.com/dario-baumberger/obsidian-image-to-text-ocr";
        };
    }
);
    "image-tools" = (
    let
        baseUrl = "https://github.com/Hosstell/image-tools-obsidian-plugin/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-tools";
        version = "1.1.2";
        outputHash = "sha256-A6CXxfgM15j/KXqPSaZe0qE92G5mnYp5dMtR14jtoZU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Formatter for image on page.'';
            homepage = "https://github.com/Hosstell/image-tools-obsidian-plugin";
        };
    }
);
    "image-upload-toolkit" = (
    let
        baseUrl = "https://github.com/addozhang/obsidian-image-upload-toolkit/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-upload-toolkit";
        version = "1.6.0";
        outputHash = "sha256-EG/1zp6gYDKkWMpfMjhvawoFjWlIlvSK86BFN3QnHDE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload local images embedded in Markdown to remote store and export Markdown with image public URL for publishing to static site. Currently, it supports Imgur, Aliyun OSS, ImageKit and AWS S3.'';
            homepage = "https://github.com/addozhang/obsidian-image-upload-toolkit";
        };
    }
);
    "image-uploader-for-note" = (
    let
        baseUrl = "https://github.com/yy4382/obsidian-image-upload/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-uploader-for-note";
        version = "1.0.3";
        outputHash = "sha256-x9tReQlAKCstBb3bMRnoi9fU23ttRl8Z+JCFzaepF7M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images in a note, and remove the images from the vault if they"re exclusively used within that note.'';
            homepage = "https://github.com/yy4382/obsidian-image-upload";
        };
    }
);
    "image-window" = (
    let
        baseUrl = "https://github.com/javalent/second-window/releases/download/2.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-window";
        version = "2.0.6";
        outputHash = "sha256-6lIrXojKE0aC4dChl6tZcJwrtC2cml9ta6I+3J8fDek=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allow images & notes to be viewed in new windows.'';
            homepage = "https://github.com/javalent/second-window";
        };
    }
);
    "image2latex" = (
    let
        baseUrl = "https://github.com/Hugo-Persson/obsidian-ocrlatex/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image2latex";
        version = "1.1.2";
        outputHash = "sha256-ABtp5iesi8fYxRtidLicNjfGn8MgiIkvDGIkQu0oVhU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert your images with math to LaTeX code.'';
            homepage = "https://github.com/Hugo-Persson/obsidian-ocrlatex";
        };
    }
);
    "image-collector" = (
    let
        baseUrl = "https://github.com/tdaykin/obsidian_image_collector/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "image-collector";
        version = "1.0.3";
        outputHash = "sha256-Pbm/XuTUhStktByxFKjI3PNk5rMzff9NASZlpbYt0zg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collects all images from current note and saves them to a new folder'';
            homepage = "https://github.com/tdaykin/obsidian_image_collector";
        };
    }
);
    "images-to-notes" = (
    let
        baseUrl = "https://github.com/rodolfo-terriquez/images-to-notes/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "images-to-notes";
        version = "1.1.2";
        outputHash = "sha256-XIxZzd2CA9BEVG1LQIfpluprkiz39OCy9KihgTj3d8k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn photos of handwritten or printed notes into Markdown using AI.'';
            homepage = "https://github.com/rodolfo-terriquez/images-to-notes";
        };
    }
);
    "imdb-sync" = (
    let
        baseUrl = "https://github.com/aaachen/IMDb-Obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "imdb-sync";
        version = "1.0.0";
        outputHash = "sha256-eT0lwX+hAqrdRenEA8tJFmCQxkSVq45/e6WTFbr9kJM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your IMDb list.'';
            homepage = "https://github.com/aaachen/IMDb-Obsidian";
        };
    }
);
    "imdone-obsidian-plugin" = (
    let
        baseUrl = "https://github.com/imdone/imdone-obsidian-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "imdone-obsidian-plugin";
        version = "1.1.0";
        outputHash = "sha256-5FDALDmGBBD1cUMEGpiTI4rZORWh8oLck+QUZwwwuVQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open cards in imdone kanban from their source in Obsidian.'';
            homepage = "https://github.com/imdone/imdone-obsidian-plugin";
        };
    }
);
    "img2html" = (
    let
        baseUrl = "https://github.com/0x1DA9430/img2html/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "img2html";
        version = "1.0.2";
        outputHash = "sha256-iO8tFEqWO9VqlkW534tL4GJ1oMetNWUSsQM2MJ94JVY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste images as HTML format instead of wikilink or markdown format.'';
            homepage = "https://github.com/0x1DA9430/img2html";
        };
    }
);
    "imgbb-uploader" = (
    let
        baseUrl = "https://github.com/jordanhandy/obsidian-imgbb-uploader/releases/download/0.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "imgbb-uploader";
        version = "0.3.3";
        outputHash = "sha256-GkcFdX2eOlrjR5qzjGn/Oc4I04hziiE4qB+0aG1+d+U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images from your clipboard to ImgBB.'';
            homepage = "https://github.com/jordanhandy/obsidian-imgbb-uploader";
        };
    }
);
    "imgur-tencent-cos" = (
    let
        baseUrl = "https://github.com/bobostudio/obsidian-imgur-plugin/releases/download/1.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "imgur-tencent-cos";
        version = "1.2.9";
        outputHash = "sha256-2v1zVDnIA4qxhYWsv030QYni1LUaNpKQ792nm9MisnM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images to Tencent COS for Imgur.'';
            homepage = "https://github.com/bobostudio/obsidian-imgur-plugin";
        };
    }
);
    "immersive-translate" = (
    let
        baseUrl = "https://github.com/imfenghuang/obsidian-immersive-translate/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "immersive-translate";
        version = "0.0.2";
        outputHash = "sha256-cmzqUK+wQsltxQxMp/HDnwJ7yN1oxyuo7eBvNJ4CTAc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A free-to-use translatation service for foreign language markdown file.'';
            homepage = "https://github.com/imfenghuang/obsidian-immersive-translate";
        };
    }
);
    "immich" = (
    let
        baseUrl = "https://github.com/Talal-A/obsidian-immich/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "immich";
        version = "0.2.1";
        outputHash = "sha256-9s54oMDGsWdEq+UIGvqyo03BrW4gSo47evjn+Tkm4sw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link your Immich images within your vault.'';
            homepage = "https://github.com/Talal-A/obsidian-immich";
        };
    }
);
    "import-attachments-plus" = (
    let
        baseUrl = "https://github.com/alberti42/obsidian-import-attachments-plus/releases/download/1.5.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "import-attachments-plus";
        version = "1.5.12";
        outputHash = "sha256-LHO4lO+wTYigI8dBq27Qz56nggaEeKhXdOxK3I4BC8Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import and manage attachments by enabling moving and linking'';
            homepage = "https://github.com/alberti42/obsidian-import-attachments-plus";
        };
    }
);
    "import-github-readme" = (
    let
        baseUrl = "https://github.com/chasebank87/import-github-readme/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "import-github-readme";
        version = "1.0.0";
        outputHash = "sha256-6XxmjK0xQDf/fJ1NVF+SDlotJzn1g8zW6w/bfnTN9u0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to import a GitHub README file into your vault.'';
            homepage = "https://github.com/chasebank87/import-github-readme";
        };
    }
);
    "import-todoist" = (
    let
        baseUrl = "https://github.com/dukex/obsidian-import-todoist/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "import-todoist";
        version = "1.0.0";
        outputHash = "sha256-rLaa3PQwTz/q/xKKEv3bemcE74Aijeid5/yzrQKL8Fw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Todoist tasks as Markdown tasks.'';
            homepage = "https://github.com/dukex/obsidian-import-todoist";
        };
    }
);
    "improved-random-note" = (
    let
        baseUrl = "https://github.com/ShockThunder/improved-random-note/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "improved-random-note";
        version = "1.0.4";
        outputHash = "sha256-+RVKnU8SLmwF+cj2W+E8bQn24wsphJBQSyQWDOoXMjY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improved interaction with the knowledge base in so-called wandering mode by opening specific Random Notes.'';
            homepage = "https://github.com/ShockThunder/improved-random-note";
        };
    }
);
    "imswitch-mathblock" = (
    let
        baseUrl = "https://github.com/cailurus/imswitch_mathblock_obsidian/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "imswitch-mathblock";
        version = "1.0.6";
        outputHash = "sha256-aK2V2QBdzs6+ooEGtq81HItjdUsN4xvIJVKUg1v39z0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically switch input method in math block'';
            homepage = "https://github.com/cailurus/imswitch_mathblock_obsidian";
        };
    }
);
    "inbox" = (
    let
        baseUrl = "https://github.com/Zachatoo/obsidian-inbox/releases/download/3.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inbox";
        version = "3.0.2";
        outputHash = "sha256-/1wzTAjAfsoYlaM+c83i93Q9Trq3OidOFp55DdEcBKQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show in app notification on startup if there is data to process in the "inbox" note.'';
            homepage = "https://github.com/Zachatoo/obsidian-inbox";
        };
    }
);
    "inbox-organiser" = (
    let
        baseUrl = "https://github.com/jamiefdhurst/obsidian-inbox-organiser/releases/download/0.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inbox-organiser";
        version = "0.2.5";
        outputHash = "sha256-1kQyC8xVBO1L8+UFD3q6nHwfIU0DOTamuinBwsQnFmM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Capture any new notes into an inbox and periodically prompt to organise these into other folders within the vault.'';
            homepage = "https://github.com/jamiefdhurst/obsidian-inbox-organiser";
        };
    }
);
    "inboxer" = (
    let
        baseUrl = "https://github.com/eoinhurrell/obsidian-inboxer/releases/download/1.5.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inboxer";
        version = "1.5.4";
        outputHash = "sha256-zNqh3e7qgv/VBI0I1zlcWV9NSFKYuIDROJHbX7XZwt0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds commands to quickly add entries to INBOX and TIMELINE sections of notes'';
            homepage = "https://github.com/eoinhurrell/obsidian-inboxer";
        };
    }
);
    "incomplete-files" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-incomplete-files/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "incomplete-files";
        version = "1.0.12";
        outputHash = "sha256-7v7XKkOGNVMsLh92mxw0Fl1WZHwmkyFrzRNFtXxS6v8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rule based keep track of your incomplete files.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-incomplete-files";
        };
    }
);
    "incremental-id" = (
    let
        baseUrl = "https://github.com/adziok/obsidian-incremental-id/releases/download/0.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "incremental-id";
        version = "0.4.2";
        outputHash = "sha256-J9kJhPPI1hAVSTzKxZ6T4Ol/G5PMXQKJ/AJowgK4qx0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate Jira-like IDs for each note.'';
            homepage = "https://github.com/adziok/obsidian-incremental-id";
        };
    }
);
    "index-checker" = (
    let
        baseUrl = "https://github.com/pavloDeshko/obsidian-index-checker/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "index-checker";
        version = "1.1.1";
        outputHash = "sha256-5vACaQ8AgPH/767EYOeQoPMYyrEzVfg36zzHbAGj9vs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make sure your index "MOC" files (notes or Canvas) contain all links they should contain.'';
            homepage = "https://github.com/pavloDeshko/obsidian-index-checker";
        };
    }
);
    "index-notes" = (
    let
        baseUrl = "https://github.com/adanielnoel/obsidian-index-notes/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "index-notes";
        version = "1.1.2";
        outputHash = "sha256-mRcEExmPlJPakgCSxsTNLqqg+uI9XXH+SqI7MnH/WdY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep your notes indexed based on their (hierarchical) tags'';
            homepage = "https://github.com/adanielnoel/obsidian-index-notes";
        };
    }
);
    "influx" = (
    let
        baseUrl = "https://github.com/jensmtg/influx/releases/download/2.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "influx";
        version = "2.3.1";
        outputHash = "sha256-t1fua8HDoQPga/DrfaLPOWzwFTqY7DxyLSTQhDAiql4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A bullet journaling plugin that aggregates a terse stream of backlinked clippings in the footer of notes.'';
            homepage = "https://github.com/jensmtg/influx";
        };
    }
);
    "infoflow" = (
    let
        baseUrl = "https://github.com/InfoFlow/Obsidian-InfoFlow/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "infoflow";
        version = "1.1.0";
        outputHash = "sha256-Y7r+wIZ6bcvYafZhNXH06b0eSuRg0t+yDnzW8uleZC8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your saved InfoFlow.app articles and highlights, similar to ReadWise and Omnivore'';
            homepage = "https://github.com/InfoFlow/Obsidian-InfoFlow";
        };
    }
);
    "infostacker" = (
    let
        baseUrl = "https://github.com/taskscape/InfostackerPlugin/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "infostacker";
        version = "1.1.3";
        outputHash = "sha256-yk1D288beba//XvEk1zAH2Hz/9iqq/f7XwJPISh8Wys=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share your notes with attachments easy!'';
            homepage = "https://github.com/taskscape/InfostackerPlugin";
        };
    }
);
    "infranodus-graph-view" = (
    let
        baseUrl = "https://github.com/noduslabs/infranodus-obsidian-plugin/releases/download/0.9.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "infranodus-graph-view";
        version = "0.9.4";
        outputHash = "sha256-YHt1uE3yvpHJwd7GsuC1P6Ce96eS1qh4qIf5z5h72CQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interactive 3D graph view: text analysis, topic modeling, gap detection, and AI. (Closed source)'';
            homepage = "https://github.com/noduslabs/infranodus-obsidian-plugin";
        };
    }
);
    "initiative-tracker" = (
    let
        baseUrl = "https://github.com/Obsidian-TTRPG-Community/initiative-tracker/releases/download/13.0.21";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "initiative-tracker";
        version = "13.0.21";
        outputHash = "sha256-ESRefd5peLIg89B4OBKXdX2Cq9BzPBNSzEgZnUU5LhM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''TTRPG initiative tracker.'';
            homepage = "https://github.com/Obsidian-TTRPG-Community/initiative-tracker";
        };
    }
);
    "ink" = (
    let
        baseUrl = "https://github.com/daledesilva/obsidian_ink/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ink";
        version = "0.4.0";
        outputHash = "sha256-sYxbByPsRKsVILDKbfJq1Cw1eY0XRGsUKf1Ff8Bngjw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hand write or draw directly between paragraphs using a digital pen, stylus, or Apple pencil. Useful for handwriting, sketches, scribbles, or even math equations and scientific notation. Runs on the tldraw framework and drawing provides an infinite canvas.'';
            homepage = "https://github.com/daledesilva/obsidian_ink";
        };
    }
);
    "ink-player" = (
    let
        baseUrl = "https://github.com/uglyboy-tl/obsidian-ink-player/releases/download/2.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ink-player";
        version = "2.0.7";
        outputHash = "sha256-Y1KiDxBcbOQNIJUwOIeAjc3mYlq/VudifJS4mdCS7xw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Playing interactive fiction powered by Inkle"s ink engine.'';
            homepage = "https://github.com/uglyboy-tl/obsidian-ink-player";
        };
    }
);
    "inkporter" = (
    let
        baseUrl = "https://github.com/AmadeussSystem/Inkporter/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inkporter";
        version = "3.0.0";
        outputHash = "sha256-Hy5K7wfqb7KffucMCazZoHb/QBbzlW7enOHhYTvO+/o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''digitize handwritten notes with intelligent ink isolation, adaptive theming, and automated workflows.'';
            homepage = "https://github.com/AmadeussSystem/Inkporter";
        };
    }
);
    "inline-admonitions" = (
    let
        baseUrl = "https://github.com/scottTomaszewski/obsidian-inline-admonitions/releases/download/1.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-admonitions";
        version = "1.10.0";
        outputHash = "sha256-FkorlbvLQkDhb9iA7VueAAL/VVtxFlpyBPwAcUfqHFs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Inline callouts to make text pop.'';
            homepage = "https://github.com/scottTomaszewski/obsidian-inline-admonitions";
        };
    }
);
    "inline-callouts" = (
    let
        baseUrl = "https://github.com/gapmiss/inline-callouts/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-callouts";
        version = "0.1.4";
        outputHash = "sha256-L3kzbcAmSA1duoX7tKa6CHYlVt8SSrWUYT25J9n2Jjk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add inline callouts/badges/icons to notes.'';
            homepage = "https://github.com/gapmiss/inline-callouts";
        };
    }
);
    "inline-checkbox-groups" = (
    let
        baseUrl = "https://github.com/bwya77/Inline-Checkbox-Groups/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-checkbox-groups";
        version = "1.0.1";
        outputHash = "sha256-eb3lWSFlCAHDMOoqX/EVttsKcd3yhHrnBxexxdJXJmc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create multiple checkboxes on a single line, separated by a customizable separator character (default "|"), with the option to automatically cross out text when all checkboxes in the line are checked.'';
            homepage = "https://github.com/bwya77/Inline-Checkbox-Groups";
        };
    }
);
    "inline-code-copy" = (
    let
        baseUrl = "https://github.com/lhinxue/obsidian-inline-code-copy-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-code-copy";
        version = "1.0.1";
        outputHash = "sha256-zyNEjO7RX3kk42gf12QU4lbwxYjW1AR2QZv60BOZab0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy inline code on click in reading view.'';
            homepage = "https://github.com/lhinxue/obsidian-inline-code-copy-plugin";
        };
    }
);
    "inline-code-highlight" = (
    let
        baseUrl = "https://github.com/Dimava/inline-code-highlight/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-code-highlight";
        version = "1.0.1";
        outputHash = "sha256-MKZvynRtgk36gUTRzp32rwwC9Gv5KX2KKyRB0PRPldQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight inline `"md **code**` blocks as well as you do the ```md **big**``` ones.'';
            homepage = "https://github.com/Dimava/inline-code-highlight";
        };
    }
);
    "inline-encrypter" = (
    let
        baseUrl = "https://github.com/solargate/obsidian-inline-encrypter/releases/download/1.5.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-encrypter";
        version = "1.5.8";
        outputHash = "sha256-DQ0lwwbYf2Eychbm1pxJXZQXJxDv0fKGeK4ypfxaDbE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Encrypt secrets in your notes.'';
            homepage = "https://github.com/solargate/obsidian-inline-encrypter";
        };
    }
);
    "inline-local-graph" = (
    let
        baseUrl = "https://github.com/TKOxff/obsidian-inline-local-graph/releases/download/0.9.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-local-graph";
        version = "0.9.8";
        outputHash = "sha256-rvEBTeQcctBeXVCwDx0NyU/IQhzPC5iuMNVgS9iJva0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize the local graph inline within your notes.'';
            homepage = "https://github.com/TKOxff/obsidian-inline-local-graph";
        };
    }
);
    "inline-math" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-inline-math/releases/download/0.3.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-math";
        version = "0.3.6";
        outputHash = "sha256-iqRZxiwuBF4NqnaYgiDlmjJoiEsqKAKBfZUJAWGwL3s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remove flickering inline math.'';
            homepage = "https://github.com/RyotaUshio/obsidian-inline-math";
        };
    }
);
    "inline-spoilers" = (
    let
        baseUrl = "https://github.com/logonoff/obsidian-inline-spoilers/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inline-spoilers";
        version = "1.2.5";
        outputHash = "sha256-b8chKAH7KPx2JQikA/cAk0Kl4qDhP8P5WqXcVXRj0CA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds Discord-like syntax for inline spoilers in reader mode.'';
            homepage = "https://github.com/logonoff/obsidian-inline-spoilers";
        };
    }
);
    "inlineai" = (
    let
        baseUrl = "https://github.com/FBarrca/obsidian-inlineAI/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inlineai";
        version = "1.2.4";
        outputHash = "sha256-/QwIj294JqwHg3oC71nFvwXjF90XM+LPCgSD29kO/e0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered suggestions, contextual edits, and advanced text transformations directly into your editor.'';
            homepage = "https://github.com/FBarrca/obsidian-inlineAI";
        };
    }
);
    "inscribe" = (
    let
        baseUrl = "https://github.com/ahmetildirim/obsidian-inscribe/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "inscribe";
        version = "1.2.1";
        outputHash = "sha256-DijQ+V02twjE8McuhrLOmprqJpFqTmWZymmhMFhDt54=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Inline autocompletion powered by AI.'';
            homepage = "https://github.com/ahmetildirim/obsidian-inscribe";
        };
    }
);
    "insert-arknights-url-banner" = (
    let
        baseUrl = "https://github.com/Rerurate514/insert_ArknightsURL_banners/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insert-arknights-url-banner";
        version = "1.0.1";
        outputHash = "sha256-Zc7IB/Zd5ckXbShj9/o6KMk8Nv6ge3g8+riebu+lk5w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Select the Arknights image and place it in the banner property.'';
            homepage = "https://github.com/Rerurate514/insert_ArknightsURL_banners";
        };
    }
);
    "insert-heading-link" = (
    let
        baseUrl = "https://github.com/Signynt/insert-heading-link/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insert-heading-link";
        version = "1.0.1";
        outputHash = "sha256-ZSQaIwg8H6Al3XtaYZ+eN4Xr6xyWna8OEOCMOV2Rk9E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a command to create a link to a heading.'';
            homepage = "https://github.com/Signynt/insert-heading-link";
        };
    }
);
    "insert-multiple-attachments" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-insert-multiple-attachments/releases/download/1.3.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insert-multiple-attachments";
        version = "1.3.7";
        outputHash = "sha256-qi4gF6Vok/gwgf7XCdAhkVq+RtfiOZki2u7cyLgSZVM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows to insert multiple attachments at a time.'';
            homepage = "https://github.com/mnaoumov/obsidian-insert-multiple-attachments";
        };
    }
);
    "insert-new-line" = (
    let
        baseUrl = "https://github.com/freddyouellette/obsidian-insert-new-line-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insert-new-line";
        version = "1.2.0";
        outputHash = "sha256-ucES2FXGVllRhVrDsRSe3tieyiznLz5MdQGGNym+lg4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert a new line above or below the current line.'';
            homepage = "https://github.com/freddyouellette/obsidian-insert-new-line-plugin";
        };
    }
);
    "insert-unsplash-image" = (
    let
        baseUrl = "https://github.com/cloudy9101/obsidian-image-inserter/releases/download/0.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insert-unsplash-image";
        version = "0.8.4";
        outputHash = "sha256-3CEl5Ck8cmGii4+sBJq5025eE+VAlOzC5bQZdVWvEvc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Help users easily search and insert images to editors from Unsplash.'';
            homepage = "https://github.com/cloudy9101/obsidian-image-inserter";
        };
    }
);
    "insighta" = (
    let
        baseUrl = "https://github.com/HongjianTang/obsidian-insighta/releases/download/1.0.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insighta";
        version = "1.0.14";
        outputHash = "sha256-qaQ+KO8GjgZBwvCMbEI8FMnnafe5HjXA2l1Q96xzdqA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract a set of atomic notes from a long article and create MOC by note title using LLM'';
            homepage = "https://github.com/HongjianTang/obsidian-insighta";
        };
    }
);
    "insta-toc" = (
    let
        baseUrl = "https://github.com/iLiftALot/insta-toc/releases/download/7.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "insta-toc";
        version = "7.0.0";
        outputHash = "sha256-6b9K3y7cFqNA/y6zzeX+Rsr9D+eZ8Fvh2oIZIaJRWQ4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simultaneously generate, update, and maintain a table of contents for your notes.'';
            homepage = "https://github.com/iLiftALot/insta-toc";
        };
    }
);
    "instant-above-divider" = (
    let
        baseUrl = "https://github.com/sedationh/obsidian-instant-above-divider/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "instant-above-divider";
        version = "1.3.2";
        outputHash = "sha256-JY6X8TKrSQsZIQPvmtMwgLzpFiLR3FVznWf+MvEG/n8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly insert a divider line at the beginning of your note.'';
            homepage = "https://github.com/sedationh/obsidian-instant-above-divider";
        };
    }
);
    "instapaper" = (
    let
        baseUrl = "https://github.com/Instapaper/obsidian-instapaper/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "instapaper";
        version = "1.2.1";
        outputHash = "sha256-ns2rWa4kTluTDr5bfX5umJ/QYB0e1PmKh9kLwuQbvNY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Instapaper integration.'';
            homepage = "https://github.com/Instapaper/obsidian-instapaper";
        };
    }
);
    "intelligence" = (
    let
        baseUrl = "https://github.com/ransurf/obsidian-intelligence/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "intelligence";
        version = "1.0.2";
        outputHash = "sha256-Oyti0Gb1/hoziJHwYtlBQdzgbIud0p7CBYiXFokQl6k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''OpenAI GPT Assistants functionality'';
            homepage = "https://github.com/ransurf/obsidian-intelligence";
        };
    }
);
    "interactive-code-blocks" = (
    let
        baseUrl = "https://github.com/Windesheim-HBO-ICT/Obsidian-Interactive-Code-Block-Plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "interactive-code-blocks";
        version = "1.0.1";
        outputHash = "sha256-1SjdJBsg1jIf9ZZASZkshH0euYomsjbRsqaYNWa/OnI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preview interactive code blocks!'';
            homepage = "https://github.com/Windesheim-HBO-ICT/Obsidian-Interactive-Code-Block-Plugin";
        };
    }
);
    "interactive-ratings" = (
    let
        baseUrl = "https://github.com/peritus/obsidian-interactive-ratings/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "interactive-ratings";
        version = "1.0.8";
        outputHash = "sha256-lXxcWyKOXq1TuaiLRL3J2FRSIGxcJ5+o5Wav0rRr+PE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit symbol ratings in your notes interactively.'';
            homepage = "https://github.com/peritus/obsidian-interactive-ratings";
        };
    }
);
    "interactivity" = (
    let
        baseUrl = "https://github.com/ichichikin/obsidian-plugin-interactivity/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "interactivity";
        version = "0.0.4";
        outputHash = "sha256-ZujG9+yGGF1QU2cfQ33CAzh58SwHz4oT6+ZgbEnuUQY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interactivity lets you run local shell commands and scripts directly within your notes, providing the output alongside your written content. Use your favorite tools like Python, Node.js, Java, Perl, PHP, bash, or any other REPLs while taking notes.'';
            homepage = "https://github.com/ichichikin/obsidian-plugin-interactivity";
        };
    }
);
    "invio" = (
    let
        baseUrl = "https://github.com/frontend-engineering/Invio/releases/download/0.7.40";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "invio";
        version = "0.7.40";
        outputHash = "sha256-qO19IatrVkfgcgMcNabrf5h9t3HPUQKBwyrvFootwVM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export docs as websites and deploy them to AWS S3 or compatible COS. Invio streamlines the process of publishing, enabling users to easily share their work online.'';
            homepage = "https://github.com/frontend-engineering/Invio";
        };
    }
);
    "ioc-lens" = (
    let
        baseUrl = "https://github.com/acgabbert/IOC-Lens/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ioc-lens";
        version = "1.1.0";
        outputHash = "sha256-5RBEDEklisqtdZ5RJkhOGf1Pc79MyYXS24butp89X4U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extracts and displays security-relevant indicators such as IP addresses, domains, and file hashes to enhance your cyber security note-taking process.'';
            homepage = "https://github.com/acgabbert/IOC-Lens";
        };
    }
);
    "iron-vault" = (
    let
        baseUrl = "https://github.com/iron-vault-plugin/iron-vault/releases/download/1.108.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "iron-vault";
        version = "1.108.1";
        outputHash = "sha256-VKfCuC6VkZghDU4O/HjFLeCgDRIqtBUJb+TmJgm4dYI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Gameplay plugin/VTT for the Ironsworn/Starforged family of tabletop RPGs.'';
            homepage = "https://github.com/iron-vault-plugin/iron-vault";
        };
    }
);
    "jade-publisher" = (
    let
        baseUrl = "https://github.com/LucasJi/jade-publisher/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jade-publisher";
        version = "1.0.0";
        outputHash = "sha256-N885wZYyh52E+RYnOhGen2xCByvMlwp/kd/7U82gkg0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your vault and changes to your Jade service.'';
            homepage = "https://github.com/LucasJi/jade-publisher";
        };
    }
);
    "janitor" = (
    let
        baseUrl = "https://github.com/Canna71/obsidian-janitor/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "janitor";
        version = "1.1.1";
        outputHash = "sha256-78mcC1k2v2AY6VlkIAo3rc15ZHCu1MbalAx7wMincLU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Perform cleanup tasks on your vault.'';
            homepage = "https://github.com/Canna71/obsidian-janitor";
        };
    }
);
    "japanese-helper" = (
    let
        baseUrl = "https://github.com/OverFitted/obsidian-japanese-helper/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "japanese-helper";
        version = "1.0.3";
        outputHash = "sha256-VsiXiaZqdPsO2sKt9SG+08sF/GsxpdLQKkZv0cRlO4s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Streamline Japanese note-taking with romaji to kana conversion'';
            homepage = "https://github.com/OverFitted/obsidian-japanese-helper";
        };
    }
);
    "japanese-manuscript-counter" = (
    let
        baseUrl = "https://github.com/YoFujii0705/japanese-manuscript-counter/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "japanese-manuscript-counter";
        version = "1.0.2";
        outputHash = "sha256-TlPHjLahAoFIQuxz8vOW3hfyOF0M4choAefkcGQ4XWw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays the number of Japanese characters and their equivalent in 400-character manuscript paper in real time. Provides an accurate count taking into account line breaks, paragraphs, and character breaks.'';
            homepage = "https://github.com/YoFujii0705/japanese-manuscript-counter";
        };
    }
);
    "japanese-novel-ruby" = (
    let
        baseUrl = "https://github.com/k-quels/japanese-novel-ruby/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "japanese-novel-ruby";
        version = "1.5.2";
        outputHash = "sha256-8sPlCsQq0VfmXf3DPAbXBm/Hh/qoBwJTNBRRlYS93AQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Treat ruby(Furigana) ​​marks commonly used in Japanese novels'';
            homepage = "https://github.com/k-quels/japanese-novel-ruby";
        };
    }
);
    "japanese-word-splitter" = (
    let
        baseUrl = "https://github.com/sonarAIT/cm-japanese-patch/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "japanese-word-splitter";
        version = "1.0.0";
        outputHash = "sha256-dEDiwVip6AKkZX6XRiczIen4RLWjJCmO1Etu4iXraGI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A patch for Obsidian"s built-in CodeMirror Editor to support Japanese word splitting.'';
            homepage = "https://github.com/sonarAIT/cm-japanese-patch";
        };
    }
);
    "jelly-snippets" = (
    let
        baseUrl = "https://github.com/rabirabirara/obsidian-jelly-snippets/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jelly-snippets";
        version = "0.2.0";
        outputHash = "sha256-VQYSw5otEcuuvUCyT+DPy/5442DTQT8+Wp2Xj7ANvck=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple text snippets plugin.'';
            homepage = "https://github.com/rabirabirara/obsidian-jelly-snippets";
        };
    }
);
    "jira-issue-managing" = (
    let
        baseUrl = "https://github.com/angelperezasenjo/obsidian-to-jira/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jira-issue-managing";
        version = "1.0.3";
        outputHash = "sha256-ynXDwm1AKYLtve3EM+xVPdejdJ8FJBh7i+Z50IKMDKE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Update and creating of Jira issues directly'';
            homepage = "https://github.com/angelperezasenjo/obsidian-to-jira";
        };
    }
);
    "jira-linker" = (
    let
        baseUrl = "https://github.com/srz2/obsidian-jira-linker/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jira-linker";
        version = "1.1.3";
        outputHash = "sha256-yuhJx0d8sJ4qScrJsp8QYakCTYRkGMQ4SE4ez1x4URg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly format a Jira issue tag as a link to you Jira instance.'';
            homepage = "https://github.com/srz2/obsidian-jira-linker";
        };
    }
);
    "jira-links-shortener" = (
    let
        baseUrl = "https://github.com/rplatonovs/obsidian-jira-links-shortener/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jira-links-shortener";
        version = "1.0.6";
        outputHash = "sha256-i+s0tHfr+IczAU7cIT5CphLI79T/g4AqmAG8iZaEZ1Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Modifies pasted JIRA links to use JIRA issue number as the link title'';
            homepage = "https://github.com/rplatonovs/obsidian-jira-links-shortener";
        };
    }
);
    "jira-sync" = (
    let
        baseUrl = "https://github.com/Alamion/obsidian-jira-sync/releases/download/1.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jira-sync";
        version = "1.4.4";
        outputHash = "sha256-SavAimi1kzipHJUfuxMaVXdmhyeYjzSs219C2Wnp1cU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get Jira issues, create and update them. Issue status and worklog management.'';
            homepage = "https://github.com/Alamion/obsidian-jira-sync";
        };
    }
);
    "jisage-japanese-indentation" = (
    let
        baseUrl = "https://github.com/Telehakke/jisage-japanese-indentation/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jisage-japanese-indentation";
        version = "1.0.1";
        outputHash = "sha256-LK83OM1UpBVdrlvyox9HuhfAuudVoeMEkJx/USOtVt0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display "Jisage" (indenting with a full-width space at the beginning of a line) text correctly.'';
            homepage = "https://github.com/Telehakke/jisage-japanese-indentation";
        };
    }
);
    "journal-folder" = (
    let
        baseUrl = "https://github.com/chfourie/obsidian-journal-folder/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "journal-folder";
        version = "1.3.0";
        outputHash = "sha256-Fd1uRF37Mbas5HBJVh+u5oowSOqRnsWA0BCH7Pj0z44=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Utilities for folder-based journaling.'';
            homepage = "https://github.com/chfourie/obsidian-journal-folder";
        };
    }
);
    "journal-review" = (
    let
        baseUrl = "https://github.com/Kageetai/obsidian-plugin-journal-review/releases/download/3.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "journal-review";
        version = "3.0.2";
        outputHash = "sha256-T44KZczcW5RRtB5sV0oLz8vkNZ8PAyR9Na3QqlitfWI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Review your daily notes on their anniversaries, like "what happened today last year".'';
            homepage = "https://github.com/Kageetai/obsidian-plugin-journal-review";
        };
    }
);
    "journaling" = (
    let
        baseUrl = "https://github.com/Ordeeper/obsidian-journaling-plugin/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "journaling";
        version = "1.0.5";
        outputHash = "sha256-tlrj5JAdWeg4oLSgQE6D1zfph3+rWK9DsDhspzb6UGg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View daily notes in a journal-like format, similar to Logseq. It enhances note organization and facilitates better reflection by consolidating daily notes into a continuous journaling view.'';
            homepage = "https://github.com/Ordeeper/obsidian-journaling-plugin";
        };
    }
);
    "journals" = (
    let
        baseUrl = "https://github.com/srg-kostyrko/obsidian-journal/releases/download/2.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "journals";
        version = "2.1.10";
        outputHash = "sha256-KZXgDEy1nVOTsVcJ7yG1F4MBPGJAzdz2TcMNywJaLRA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your journals.'';
            homepage = "https://github.com/srg-kostyrko/obsidian-journal";
        };
    }
);
    "journalyst" = (
    let
        baseUrl = "https://github.com/Justin-Arnold/Journalyst/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "journalyst";
        version = "1.1.0";
        outputHash = "sha256-KBzZuA/5wFCfyX+3GCgMYi5nh875sNm0g4s8qtSg2iM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Journalyst enables easy creation of topic-specific journals. Organize your life into categories like sleep, routines, or work, with daily or recurring entries for effortless tracking and reflection.'';
            homepage = "https://github.com/Justin-Arnold/Journalyst";
        };
    }
);
    "js-engine" = (
    let
        baseUrl = "https://github.com/mProjectsCode/obsidian-js-engine-plugin/releases/download/0.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "js-engine";
        version = "0.3.4";
        outputHash = "sha256-CEch9wxoWEERbSA/+1AjVfC6ouKlKHiphcSEU0SxXEY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run JavaScript from within your notes.'';
            homepage = "https://github.com/mProjectsCode/obsidian-js-engine-plugin";
        };
    }
);
    "json-table" = (
    let
        baseUrl = "https://github.com/dario-baumberger/obsidian-json-table/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "json-table";
        version = "1.0.4";
        outputHash = "sha256-jGyjGsR/PF2VMbKIoEklXPKIli9+VEq+SuigcEdwGDI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simply switch between JSON and tables. Generate a table from a JSON string or a URL (which returns JSON) in your notes. Generate JSON from a table in your notes.'';
            homepage = "https://github.com/dario-baumberger/obsidian-json-table";
        };
    }
);
    "juggl" = (
    let
        baseUrl = "https://github.com/HEmile/juggl/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "juggl";
        version = "1.5.0";
        outputHash = "sha256-V+J6XTUiqDMNDX2MTQu03mO1zaRn96nZENdkP/DJxLE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a completely interactive, stylable and expandable graph view.'';
            homepage = "https://github.com/HEmile/juggl";
        };
    }
);
    "juliaplots" = (
    let
        baseUrl = "https://github.com/ivnmansi/juliaplots/releases/download/0.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "juliaplots";
        version = "0.5.5";
        outputHash = "sha256-YmTkaXmL9quyT1aJp2aCLW8rEUWN1aUbZwcCHwzwXVI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Plot a function graph inside of your notes using Julia.'';
            homepage = "https://github.com/ivnmansi/juliaplots";
        };
    }
);
    "jupymd" = (
    let
        baseUrl = "https://github.com/d-eniz/jupymd/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jupymd";
        version = "1.7.0";
        outputHash = "sha256-ky2hCU0xhGi/55MgElmcQDppJTsIdv5rYbE//NGQ4Dc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link and sync Markdown notes with Jupyter notebooks via Jupytext.'';
            homepage = "https://github.com/d-eniz/jupymd";
        };
    }
);
    "jura-links" = (
    let
        baseUrl = "https://github.com/justanotherjurastudent/Jura-Links/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jura-links";
        version = "1.5.1";
        outputHash = "sha256-/PbowmSMmasuxWHDsMCanSQ1l+niLiY0PESk6GSc0LI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Verlinke deine notierten Gesetzesnormen, Aktenzeichen und Zeitschriften-Fundstellen mit Gesetzesanbietern.'';
            homepage = "https://github.com/justanotherjurastudent/Jura-Links";
        };
    }
);
    "just-share-please" = (
    let
        baseUrl = "https://github.com/Ellpeck/ObsidianJustSharePlease/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "just-share-please";
        version = "0.2.0";
        outputHash = "sha256-jbhWompwFEAq+oxJ6qtGUeDdhmXUExMPZkiGh9E4SRE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly and easily share individual notes online using an anonymized link. Also easy to self-host!'';
            homepage = "https://github.com/Ellpeck/ObsidianJustSharePlease";
        };
    }
);
    "jw-library-linker" = (
    let
        baseUrl = "https://github.com/msakowski/obsidian-library-linker/releases/download/0.13.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "jw-library-linker";
        version = "0.13.1";
        outputHash = "sha256-sJw3eNezHhunTdmXL3i9n6HXQ+WmY13eF6yXdMYK3nU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Converts JW Library and Bible references to actual links in JW Library.'';
            homepage = "https://github.com/msakowski/obsidian-library-linker";
        };
    }
);
    "kale-graph" = (
    let
        baseUrl = "https://github.com/olillin/obsidian-kale-graph/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kale-graph";
        version = "1.1.1";
        outputHash = "sha256-lrXqX1E9knnyJTyY68tPzzol6BJR+p6nY90SP0G/g+0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render mathematical graphs.'';
            homepage = "https://github.com/olillin/obsidian-kale-graph";
        };
    }
);
    "kanban-bases-view" = (
    let
        baseUrl = "https://github.com/xiwcx/obsidian-bases-kanban/releases/download/0.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kanban-bases-view";
        version = "0.8.1";
        outputHash = "sha256-MsWgXJV97c2uTwXyAN5Ivq/i5YFg91xAFLfxHVObvNo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A kanban-style drag-and-drop custom view for Bases.'';
            homepage = "https://github.com/xiwcx/obsidian-bases-kanban";
        };
    }
);
    "kanban-status-updater" = (
    let
        baseUrl = "https://github.com/ankit-kapur/obsidian-kanban-status-updater-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kanban-status-updater";
        version = "1.0.0";
        outputHash = "sha256-sbG971+Xn+To/R+yujZqiFxA+wiBgCKJGlJNlPWEy40=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically updates a "status" property in a note when its card is moved on a Kanban board'';
            homepage = "https://github.com/ankit-kapur/obsidian-kanban-status-updater-plugin";
        };
    }
);
    "keep-the-rhythm" = (
    let
        baseUrl = "https://github.com/benjaminezequiel/keep-the-rhythm/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "keep-the-rhythm";
        version = "0.2.8";
        outputHash = "sha256-pRgQS/6bsKROl/SS1mNEBXRG62bextMTRuQ5KtMiBR8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize daily word count with a heatmap.'';
            homepage = "https://github.com/benjaminezequiel/keep-the-rhythm";
        };
    }
);
    "keepsidian" = (
    let
        baseUrl = "https://github.com/lc0rp/KeepSidian/releases/download/2.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "keepsidian";
        version = "2.0.10";
        outputHash = "sha256-6vlJ3v1FXnfBsfdOqFsCuteKLweNCGa1OhU2UlPsUWQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands to import Google Keep notes.'';
            homepage = "https://github.com/lc0rp/KeepSidian";
        };
    }
);
    "ketcher" = (
    let
        baseUrl = "https://github.com/yulei-chen/obsidian-ketcher/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ketcher";
        version = "0.1.4";
        outputHash = "sha256-Clwabr7scfht5PvdtgB1ke8QXotJtNTJaOmwBl95xPM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View or draw chemical structures and reactions using Ketcher.'';
            homepage = "https://github.com/yulei-chen/obsidian-ketcher";
        };
    }
);
    "key-promoter" = (
    let
        baseUrl = "https://github.com/joethei/obsidian-key-promoter/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "key-promoter";
        version = "1.4.0";
        outputHash = "sha256-8slQhfazmPP8LIuBdBIk7goLq3B6bR9aNjf3+AxREec=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Learn keyboard shortcuts by showing them when using the mouse.'';
            homepage = "https://github.com/joethei/obsidian-key-promoter";
        };
    }
);
    "key-value-list" = (
    let
        baseUrl = "https://github.com/christianwannerstedt/obsidian-key-value-list/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "key-value-list";
        version = "1.7.0";
        outputHash = "sha256-tAKnYeAV6UKOBrDQrHjm022vBqmy5MnA7yLWQcfOD/o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn lists into neatly formatted key-value lists.'';
            homepage = "https://github.com/christianwannerstedt/obsidian-key-value-list";
        };
    }
);
    "keyboard-analyzer" = (
    let
        baseUrl = "https://github.com/noname-oni/obsidian-keyboard-analyzer/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "keyboard-analyzer";
        version = "0.4.1";
        outputHash = "sha256-6dGaY9HZoVSg3Alpy4LK2EsyOMeU7iJ1NnJ3160/DVI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''See and analyse your keyboard hotkeys and shortcuts.'';
            homepage = "https://github.com/noname-oni/obsidian-keyboard-analyzer";
        };
    }
);
    "keyboard-formatter" = (
    let
        baseUrl = "https://github.com/Lauloque/Keyboard-Formatter/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "keyboard-formatter";
        version = "1.2.1";
        outputHash = "sha256-vApae5UBpozt9vcvXXEysjJzvfT+LNDdz0/XQuDaR8k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format selected text into HTML <kbd> tags for representing keyboard keys and mouse inputs.'';
            homepage = "https://github.com/Lauloque/Keyboard-Formatter";
        };
    }
);
    "keyshots" = (
    let
        baseUrl = "https://github.com/KrazyManJ/obsidian-keyshots/releases/download/2.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "keyshots";
        version = "2.8.1";
        outputHash = "sha256-WLAYUlWQIxi7U/Gpoh3lLlQd1yBJdmoL3TjNx/FCxMs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add classic hotkey/shortcuts commands from popular IDEs like Visual Studio Code or JetBrains Family.'';
            homepage = "https://github.com/KrazyManJ/obsidian-keyshots";
        };
    }
);
    "keyword-highlighter" = (
    let
        baseUrl = "https://github.com/marcel-goldammer/obsidian-keyword-highlighter/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "keyword-highlighter";
        version = "1.3.2";
        outputHash = "sha256-PdeAZJhu9cUDCrFcbaMbfm7jRe35KRou6eyToUdxn+g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically highlight specified keywords within your notes for enhanced visibility and quick reference.'';
            homepage = "https://github.com/marcel-goldammer/obsidian-keyword-highlighter";
        };
    }
);
    "khoj" = (
    let
        baseUrl = "https://github.com/khoj-ai/khoj/releases/download/2.0.0-beta.28";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "khoj";
        version = "2.0.0-beta.28";
        outputHash = "sha256-dgahfdw2ssp/yjlCrvaBq93jyBp4VWBFLEfeCxthZkM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An AI personal assistant for your digital brain.'';
            homepage = "https://github.com/khoj-ai/khoj";
        };
    }
);
    "kikijiki-habit-tracker" = (
    let
        baseUrl = "https://github.com/kikijiki/obsidian-habit-tracker/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kikijiki-habit-tracker";
        version = "1.1.0";
        outputHash = "sha256-/0bIKXJUQtRiKt0OUwoLLVQF4aqAeDnSx3KVb1Bz00U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple habit tracker'';
            homepage = "https://github.com/kikijiki/obsidian-habit-tracker";
        };
    }
);
    "kill-and-yank" = (
    let
        baseUrl = "https://github.com/inouetakuya/obsidian-kill-and-yank/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kill-and-yank";
        version = "1.2.0";
        outputHash = "sha256-p0wmqpvv+5u9FrljnPuOF59IuZ/qhLekf03+UOIabP0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable kill and yank (like Emacs) in the editor.'';
            homepage = "https://github.com/inouetakuya/obsidian-kill-and-yank";
        };
    }
);
    "kindle-highlights-import" = (
    let
        baseUrl = "https://github.com/LeonLuttenberger/obsidian-kindle-highlight-import/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kindle-highlights-import";
        version = "1.3.2";
        outputHash = "sha256-DgznySGuFsfDhNsYCpN5QQI42ScpFZlq1MACN5+7hyc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Imports the Kindle highlights HTML file and saves it as a note in your vault.'';
            homepage = "https://github.com/LeonLuttenberger/obsidian-kindle-highlight-import";
        };
    }
);
    "kindle-html-importer" = (
    let
        baseUrl = "https://github.com/l2xu/kindle_html_importer/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kindle-html-importer";
        version = "1.0.3";
        outputHash = "sha256-bxqB1R607Cz+7dNRMNjyDMDScqY3jKRCpREOaT93eEg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Imports the kindle highlights from the html file (you get from the kindle app) into a note.'';
            homepage = "https://github.com/l2xu/kindle_html_importer";
        };
    }
);
    "kindle-vocab" = (
    let
        baseUrl = "https://github.com/bao-tg/kindle-vocab/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kindle-vocab";
        version = "1.0.3";
        outputHash = "sha256-Nn8AoRqOy+CfPhEIJT0XPpIvCiwaYHGvlHzVIpuvbfM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create the Markdown file from your Kindle Vocab Builder in your vault.'';
            homepage = "https://github.com/bao-tg/kindle-vocab";
        };
    }
);
    "kkh" = (
    let
        baseUrl = "https://github.com/okikae/obsid-kkh/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kkh";
        version = "0.0.7";
        outputHash = "sha256-RWOyY7ltFaGrDBk6zS+5rlFo+UrbkY38eO4C8sKOq6Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replace words in a string using kkh dictionary.'';
            homepage = "https://github.com/okikae/obsid-kkh";
        };
    }
);
    "koi-sync" = (
    let
        baseUrl = "https://github.com/metagov/koi-obsidian-plugin/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "koi-sync";
        version = "1.1.5";
        outputHash = "sha256-eXn99wxHhPXQ7C5z2bJmqJN2gcb2TiIbAkOmL0uqTo0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronizes data from a Slack Telescope instance using the KOI-net protocol.'';
            homepage = "https://github.com/metagov/koi-obsidian-plugin";
        };
    }
);
    "koncham-workspace" = (
    let
        baseUrl = "https://github.com/manogna4/obsidian-koncham-workspace/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "koncham-workspace";
        version = "0.0.3";
        outputHash = "sha256-MwL+rQAYlW7OB076Tzff+s0i7GDNuSZp1ZAncscR1fg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Workspace management.'';
            homepage = "https://github.com/manogna4/obsidian-koncham-workspace";
        };
    }
);
    "koreader-highlights-importer" = (
    let
        baseUrl = "https://github.com/t5k6/obsidian-koreader-highlights/releases/download/1.4.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "koreader-highlights-importer";
        version = "1.4.5";
        outputHash = "sha256-YLGqJHtq5j4CAboVeThINopbUfDmn+tyj6g16X62rvY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Imports highlights and metadata from KoReader'';
            homepage = "https://github.com/t5k6/obsidian-koreader-highlights";
        };
    }
);
    "korean-book-search" = (
    let
        baseUrl = "https://github.com/lazerfit/korean-book-search/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "korean-book-search";
        version = "1.4.2";
        outputHash = "sha256-zgvpl0F1BeU8/taksgkqZCd/1ITImx56M3K2tjlh5zI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically fills in YAML frontmatter with Korean book information from Aladin API based on the note title.'';
            homepage = "https://github.com/lazerfit/korean-book-search";
        };
    }
);
    "korean-spellchecker" = (
    let
        baseUrl = "https://github.com/dldisud/obsidian-korean-spellchecker/releases/download/1.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "korean-spellchecker";
        version = "1.9.1";
        outputHash = "sha256-n5+JZaRj6zPTFj8e+WxnOFCBa/jxkGQcrZtpshNKqRY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Checks Korean spelling and grammar using an online service. Features a custom dictionary to exclude specific nouns (e.g., names, technical terms) from being flagged as errors.'';
            homepage = "https://github.com/dldisud/obsidian-korean-spellchecker";
        };
    }
);
    "kr-book-info-plugin" = (
    let
        baseUrl = "https://github.com/kmsk99/kr-book-info-plugin/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kr-book-info-plugin";
        version = "1.4.0";
        outputHash = "sha256-wUwwjypc28yW4m2kQgCAA0VipOZGTtusxe5TijtF6S8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Crawl Yes24 to get book information.'';
            homepage = "https://github.com/kmsk99/kr-book-info-plugin";
        };
    }
);
    "kv-store" = (
    let
        baseUrl = "https://github.com/Darren-project/obsidian-kv/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "kv-store";
        version = "1.2.0";
        outputHash = "sha256-OA6/naY8wS/JNKcuINamo+l2B9JA4t+h8q/YkM2S0co=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a key-value store. Use it to store and retrieve key-value pairs in your vault.'';
            homepage = "https://github.com/Darren-project/obsidian-kv";
        };
    }
);
    "lancaster-university-week" = (
    let
        baseUrl = "https://github.com/IMB11/ObsidianLUWeeks/releases/download/1.0.0+2425";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lancaster-university-week";
        version = "1.0.0+2425";
        outputHash = "sha256-uh0d9Ulw1AR6I9oeLB63QEUHCCm4+mya9NQ2DxwXGKY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''(2024-2025 Academic Year) Extends moment.js to provide a custom "Lancaster University Week" value or "VACATION" if not in term. Use "LUW" in the Daily Note format to use!'';
            homepage = "https://github.com/IMB11/ObsidianLUWeeks";
        };
    }
);
    "language-translator" = (
    let
        baseUrl = "https://github.com/twentytwokhz/language-translator/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "language-translator";
        version = "1.0.4";
        outputHash = "sha256-vI0BvrzfQUf1VAKwBq/odYvhMo/QJ+/6N5b9Ud7MdUI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Translate selected text in the desired language.'';
            homepage = "https://github.com/twentytwokhz/language-translator";
        };
    }
);
    "languagetool" = (
    let
        baseUrl = "https://github.com/wrenger/obsidian-languagetool/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "languagetool";
        version = "1.4.3";
        outputHash = "sha256-IyxGNAB+pHpInBmXL8bQ+9ncsJFZuB1txqRjQEmuKo8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Unofficial integration of the LanguageTool spell and grammar checker.'';
            homepage = "https://github.com/wrenger/obsidian-languagetool";
        };
    }
);
    "lapel" = (
    let
        baseUrl = "https://github.com/liamcain/obsidian-lapel/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lapel";
        version = "0.1.6";
        outputHash = "sha256-/WZnyEZaYpocfXu6SsaNVvQUGzZTcQ9NDJ7U5R8mfUI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show the heading levels in the gutter of the editor.'';
            homepage = "https://github.com/liamcain/obsidian-lapel";
        };
    }
);
    "large-language-models" = (
    let
        baseUrl = "https://github.com/eharris128/Obsidian-LLM-Plugin/releases/download/0.23.1-beta.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "large-language-models";
        version = "0.23.1-beta.2";
        outputHash = "sha256-wpphFRY/bXJeFiv6WqUi0bBIktuC25vI7vPV3xlMsQg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables access to LLMs via remote providers (OpenAI, Claude, Gemini) and local LLMs via GPT4ALL.'';
            homepage = "https://github.com/eharris128/Obsidian-LLM-Plugin";
        };
    }
);
    "lark-style-countdown-timer" = (
    let
        baseUrl = "https://github.com/MoshiQAQ/obsidian-lark-countdown-plugin/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lark-style-countdown-timer";
        version = "0.1.1";
        outputHash = "sha256-wcTCEtUKeLb2nBULXLI88x1WdfObw/P556q5XqrctUQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Feishu/Lark-style countdown blocks.'';
            homepage = "https://github.com/MoshiQAQ/obsidian-lark-countdown-plugin";
        };
    }
);
    "last-edit-location" = (
    let
        baseUrl = "https://github.com/awfrok/obsidian-plugin-last-edit-location/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "last-edit-location";
        version = "0.1.4";
        outputHash = "sha256-aUY+LH4bvUUgqroMEIJUgb1mUJozW6WOrO3MhVoecoI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Put the cursor at the last edit location when opening a note. Work well with multiple notes.'';
            homepage = "https://github.com/awfrok/obsidian-plugin-last-edit-location";
        };
    }
);
    "last-modified-timestamp-in-status-bar" = (
    let
        baseUrl = "https://github.com/Yustynn/obsidian-last-modified-timestamp-in-status-bar/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "last-modified-timestamp-in-status-bar";
        version = "1.3.0";
        outputHash = "sha256-ouXinejk54wxQIZE/9NPbMW5em+P0baWBU4cRZy5En4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamic display of file modification timestamp in the status bar.'';
            homepage = "https://github.com/Yustynn/obsidian-last-modified-timestamp-in-status-bar";
        };
    }
);
    "last-position" = (
    let
        baseUrl = "https://github.com/Saktawdi/obsidian-last-position/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "last-position";
        version = "0.2.6";
        outputHash = "sha256-nI7Tapn/N9O+lmztsE/HWqZHZd0zDj9bM64YtCYq5Rw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically scroll to the last viewed position when opening the markdown document.'';
            homepage = "https://github.com/Saktawdi/obsidian-last-position";
        };
    }
);
    "latex-algorithms" = (
    let
        baseUrl = "https://github.com/SamZhang02/obsidian-latex-algorithms/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-algorithms";
        version = "1.0.2";
        outputHash = "sha256-fgBI6racKh4dlb9eiY29nY+5ZWZ3a6K9XolzTcIQLaM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Facilitate writing algorithm blocks in LaTeX.'';
            homepage = "https://github.com/SamZhang02/obsidian-latex-algorithms";
        };
    }
);
    "latex-autocomplete" = (
    let
        baseUrl = "https://github.com/Yanis-Gerst/latex-autocomplete/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-autocomplete";
        version = "1.0.3";
        outputHash = "sha256-Qx9fVnOoWs1KtxGBNa5PpMSX7MIwgcSjNXJ7i2amm38=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple auto-completion for LaTeX. As simple as typing "\".'';
            homepage = "https://github.com/Yanis-Gerst/latex-autocomplete";
        };
    }
);
    "latex-exporter" = (
    let
        baseUrl = "https://github.com/mscott99/Latex-Exporter/releases/download/0.1.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-exporter";
        version = "0.1.12";
        outputHash = "sha256-eHlttAjXkC6Zr1yGbVQOM1z4N/4TQDU5XDwcwN55IeM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write a LaTeX paper.'';
            homepage = "https://github.com/mscott99/Latex-Exporter";
        };
    }
);
    "latex-math" = (
    let
        baseUrl = "https://github.com/zarstensen/obsidian-latex-math/releases/download/2.0.0-beta.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-math";
        version = "2.0.0-beta.1";
        outputHash = "sha256-7dfY3GaGxsn1BH5yJpGzsRmXvfUqmxeEahSIxke5sSk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Evaluate, solve and much more within LaTeX blocks using Sympy.'';
            homepage = "https://github.com/zarstensen/obsidian-latex-math";
        };
    }
);
    "latex-matrices" = (
    let
        baseUrl = "https://github.com/Deltekk/Obsidian-Latex-Matrices/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-matrices";
        version = "1.0.1";
        outputHash = "sha256-Fbev5LVSaZcHcdSYUwZFkGZMRHoFBX/SEPCLBUAHfeE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Speed up LaTeX matrices writing.'';
            homepage = "https://github.com/Deltekk/Obsidian-Latex-Matrices";
        };
    }
);
    "latex-ocr" = (
    let
        baseUrl = "https://github.com/lucasvanmol/obsidian-latex-ocr/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-ocr";
        version = "0.9.0";
        outputHash = "sha256-KEuohhKN0ySk/tOpUoOCOCFYZUHFF2ODL+Dt7Es4Djs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate LaTeX equations from images in your vault or clipboard.'';
            homepage = "https://github.com/lucasvanmol/obsidian-latex-ocr";
        };
    }
);
    "latex-panel-helper" = (
    let
        baseUrl = "https://github.com/myluster/Obsidian-LaTeX-Helper/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-panel-helper";
        version = "1.0.0";
        outputHash = "sha256-zPw65ltXsugICWeOf4Ll3qRfks9K2Ce7AGL+YrndMmk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides a convenient panel with categorized LaTeX symbols, real-time search, and a pop-out window to enhance your mathematical note-taking efficiency!'';
            homepage = "https://github.com/myluster/Obsidian-LaTeX-Helper";
        };
    }
);
    "latex-render" = (
    let
        baseUrl = "https://github.com/jvsteiner/obsidian-latex-render/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-render";
        version = "0.1.2";
        outputHash = "sha256-Rl+tuMydkSXZevIfqDcdVKpQG0PFfgbTFpWiLGWvK5c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render small latex snippets in your notes as SVG and PNG files.'';
            homepage = "https://github.com/jvsteiner/obsidian-latex-render";
        };
    }
);
    "latex-to-unicode" = (
    let
        baseUrl = "https://github.com/fjdu/obsidian-latex-unicode/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latex-to-unicode";
        version = "1.0.0";
        outputHash = "sha256-B75c9U7/SrW6EFlRjdmR3HeBW3zFxS8JvIcHDjtVjRs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert LaTeX commands into unicode sequences.'';
            homepage = "https://github.com/fjdu/obsidian-latex-unicode";
        };
    }
);
    "latexocr" = (
    let
        baseUrl = "https://github.com/JackBarker7/obsidian-latexocr/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "latexocr";
        version = "1.0.1";
        outputHash = "sha256-LM4UrtJYyGd0VtE+WRHXWZQXKop5ndYdP7e5zlOtST0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run LaTeX-OCR if it is installed locally.'';
            homepage = "https://github.com/JackBarker7/obsidian-latexocr";
        };
    }
);
    "lava-vtt-uploader" = (
    let
        baseUrl = "https://github.com/lavaforge/obsidian-lava-vtt-uploader/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lava-vtt-uploader";
        version = "1.0.0";
        outputHash = "sha256-1S4+BCNuqB3DGpTTgu3xDRcGEnGyqpIcGDcfgw3lDus=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display images from your vault in Lava VTT.'';
            homepage = "https://github.com/lavaforge/obsidian-lava-vtt-uploader";
        };
    }
);
    "lavadocs" = (
    let
        baseUrl = "https://github.com/SaalikLok/lavadocs-obsidian/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lavadocs";
        version = "1.0.4";
        outputHash = "sha256-spF6vMoQl8XvTA1vr8Wlo5T5uLYSWgJ+6JCDA+GHNls=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Public docs, from the fires of your vault.'';
            homepage = "https://github.com/SaalikLok/lavadocs-obsidian";
        };
    }
);
    "lawlist" = (
    let
        baseUrl = "https://github.com/willem-schlieter/lawlist/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lawlist";
        version = "1.1.0";
        outputHash = "sha256-bA3x9V3q9s/68tV7HmvOd7+srzTuTQPYbCHKJgvXhIc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds support for freely configurable, custom list styles (Edit Mode and Read Mode).'';
            homepage = "https://github.com/willem-schlieter/lawlist";
        };
    }
);
    "laws-of-form" = (
    let
        baseUrl = "https://github.com/Kevger/obsidian-laws-of-form/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "laws-of-form";
        version = "1.0.3";
        outputHash = "sha256-lDrhzegOOFU55rAKpRFcTx85fwt+JTCMqqZGIEw367g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create, manage and display Laws of Form expressions like ((a)) (b) = a (b).'';
            homepage = "https://github.com/Kevger/obsidian-laws-of-form";
        };
    }
);
    "layout-manager" = (
    let
        baseUrl = "https://github.com/ShadiestGoat/obsidian-layout-manager/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "layout-manager";
        version = "1.0.8";
        outputHash = "sha256-f8c+a6OFG4C5CbB5hzlcdn10v/lqZmOeTzp6v7A2Ue0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage layouts with context'';
            homepage = "https://github.com/ShadiestGoat/obsidian-layout-manager";
        };
    }
);
    "lazy-plugins" = (
    let
        baseUrl = "https://github.com/alangrainger/obsidian-lazy-plugins/releases/download/1.0.21";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lazy-plugins";
        version = "1.0.21";
        outputHash = "sha256-UGlTEZ9FOInK7ReQZskNBeT3hCoVA9GjucZCKAfmkwI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Load plugins with a delay on startup, so that you can get your app startup down into the sub-second loading time.'';
            homepage = "https://github.com/alangrainger/obsidian-lazy-plugins";
        };
    }
);
    "lds-library-reference" = (
    let
        baseUrl = "https://github.com/ingiestein/obsidian-lds-scriptures-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lds-library-reference";
        version = "1.0.3";
        outputHash = "sha256-rCYID1k4A+gNyVflfxK0Jr+fmDUdl45ylJPZnCBFreQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily insert references to scriptures and conference talks from the Church of Jesus Christ of Latterday Saints.'';
            homepage = "https://github.com/ingiestein/obsidian-lds-scriptures-plugin";
        };
    }
);
    "lds-scriptures-reference" = (
    let
        baseUrl = "https://github.com/pacokwon/obsidian-lds-library-plugin/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lds-scriptures-reference";
        version = "2.0.2";
        outputHash = "sha256-gq3HcmdK+7MvRkLc6RJcyyrWu48rY9U6Pw7a8CS4Xsg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily insert references to LDS scriptures.'';
            homepage = "https://github.com/pacokwon/obsidian-lds-library-plugin";
        };
    }
);
    "leader-hotkeys-obsidian" = (
    let
        baseUrl = "https://github.com/tgrosinger/leader-hotkeys-obsidian/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "leader-hotkeys-obsidian";
        version = "0.2.1";
        outputHash = "sha256-JI4eXgxdfenp3syTAbkUweKUPiIGl0OL3UtzsZUEYqw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add leader hotkey support to any command (like tmux or vim).'';
            homepage = "https://github.com/tgrosinger/leader-hotkeys-obsidian";
        };
    }
);
    "lean-syntax-highlight" = (
    let
        baseUrl = "https://github.com/tomaz1502/lean-syntax-highlight/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lean-syntax-highlight";
        version = "0.0.4";
        outputHash = "sha256-xpcN8JVAgTLraWE+OLaj9rCADe/zRGheTbVoG1iW01Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides live syntax highlight for the Lean programming language'';
            homepage = "https://github.com/tomaz1502/lean-syntax-highlight";
        };
    }
);
    "learnie" = (
    let
        baseUrl = "https://github.com/tankh99/learnie-plugin/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "learnie";
        version = "1.5.2";
        outputHash = "sha256-moV0ZS/nmLEUzARy9kiPASem3gH2g+kbNq9MCJBt9Go=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your learning with active recall and spaced repetition. Track changes, create review questions, and streamline your study process for more effective, long-lasting learning.'';
            homepage = "https://github.com/tankh99/learnie-plugin";
        };
    }
);
    "ledger-obsidian" = (
    let
        baseUrl = "https://github.com/tgrosinger/ledger-obsidian/releases/download/0.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ledger-obsidian";
        version = "0.3.3";
        outputHash = "sha256-6t4XmdoNDyPnadHM+Euc2luWyLZHUj8Q76BeUrhbd0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Plain text accounting.'';
            homepage = "https://github.com/tgrosinger/ledger-obsidian";
        };
    }
);
    "legifrance-integration" = (
    let
        baseUrl = "https://github.com/carnetdethese/legifrance-integration/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "legifrance-integration";
        version = "1.3.4";
        outputHash = "sha256-CPYmV1WpYWx3/n3rfpswb1mDQhsySeqYzBI9NYC8wwE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intégration de l"API Légifrance.'';
            homepage = "https://github.com/carnetdethese/legifrance-integration";
        };
    }
);
    "lemons-search" = (
    let
        baseUrl = "https://github.com/mProjectsCode/obsidian-lemons-search-plugin/releases/download/0.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lemons-search";
        version = "0.5.3";
        outputHash = "sha256-ufJ5ki2LgLMbkthSqIEz/Heukhb7qePAxgdAyGP6SZA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A blazingly fast fuzzy finder with file preview.'';
            homepage = "https://github.com/mProjectsCode/obsidian-lemons-search-plugin";
        };
    }
);
    "letterboxd-rss-sync" = (
    let
        baseUrl = "https://github.com/Fleker/letterboxd-for-obsidian/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "letterboxd-rss-sync";
        version = "1.3.0";
        outputHash = "sha256-MO0uXpZsQdQ/dUs9RknZa84AUcvFKAAmr61dLOBbP4M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncs your public Letterboxd diary.'';
            homepage = "https://github.com/Fleker/letterboxd-for-obsidian";
        };
    }
);
    "lexicon-dictionary" = (
    let
        baseUrl = "https://github.com/st-vin/obsidian-lexicon-dictionary-plugin/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lexicon-dictionary";
        version = "1.0.5";
        outputHash = "sha256-V438OshWHr7CWTwo4XoPeFu/kwiCwGsq6qtEwzVyxOk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Look up words and improve vocabulary by using flash card style cards.'';
            homepage = "https://github.com/st-vin/obsidian-lexicon-dictionary-plugin";
        };
    }
);
    "life-in-weeks-calendar" = (
    let
        baseUrl = "https://github.com/szuc/obsidian-life-in-weeks-calendar/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "life-in-weeks-calendar";
        version = "1.0.10";
        outputHash = "sha256-nsN2gzb+VrFJ4mUe0pySYBWGSXrUX4j40e/vjiUDOTE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display your entire life in weeks, with weekly notes integration.'';
            homepage = "https://github.com/szuc/obsidian-life-in-weeks-calendar";
        };
    }
);
    "lighterpack-importer" = (
    let
        baseUrl = "https://github.com/nsiniscalchi/LighterPackObsidianImporter/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lighterpack-importer";
        version = "1.0.0";
        outputHash = "sha256-871gAD0TohiaPReGhj9pghEekVfgLqLK5SF0fd8EAe8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import a packing list from https://lighterpack.com.'';
            homepage = "https://github.com/nsiniscalchi/LighterPackObsidianImporter";
        };
    }
);
    "lilypond" = (
    let
        baseUrl = "https://github.com/dot-asterisk-nl/obsidian-lilypond/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lilypond";
        version = "1.0.1";
        outputHash = "sha256-I5e5DUN32JGTtXj+zqUvc9a756YBNhOKmvCHfW/DuPg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Lilypond templates.'';
            homepage = "https://github.com/dot-asterisk-nl/obsidian-lilypond";
        };
    }
);
    "limitless-lifelogs" = (
    let
        baseUrl = "https://github.com/Maclean-D/obsidian-limitless-lifelogs/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "limitless-lifelogs";
        version = "1.0.6";
        outputHash = "sha256-kF6sNloYw1ykGbEOjDq2AcKIouRBAvfd6gc3fq8t27Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Limitless AI lifelog entries'';
            homepage = "https://github.com/Maclean-D/obsidian-limitless-lifelogs";
        };
    }
);
    "line-arrange" = (
    let
        baseUrl = "https://github.com/chitwan27/lineArrange/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "line-arrange";
        version = "1.7.0";
        outputHash = "sha256-EBAwVz6whwo9JkZmO3LBukmQqTaxFtCyjsYZH6wZk48=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Shuffle, reverse, or sort lines, using either visual width or lexical order.'';
            homepage = "https://github.com/chitwan27/lineArrange";
        };
    }
);
    "line-commands" = (
    let
        baseUrl = "https://github.com/charliecm/obsidian-line-commands/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "line-commands";
        version = "1.1.0";
        outputHash = "sha256-LFjfrClc0ZQnvSyb08HlbKBb9LEmhjf6VS2wc3Jztxs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds commands to quickly select, copy, cut, and paste lines under the selection or cursor.'';
            homepage = "https://github.com/charliecm/obsidian-line-commands";
        };
    }
);
    "line-ending-copyfix" = (
    let
        baseUrl = "https://github.com/KiwiJanus/obsidian-line-ending-copyfix/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "line-ending-copyfix";
        version = "1.1.1";
        outputHash = "sha256-EMW/+vTvJq/bT6mTOcUXKQtX++4JSXhYPg+mNiwAENk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change line endings to CRLF when copying text on Windows (add carriage return).'';
            homepage = "https://github.com/KiwiJanus/obsidian-line-ending-copyfix";
        };
    }
);
    "line-notes-sync" = (
    let
        baseUrl = "https://github.com/onikun94/line_to_obsidian/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "line-notes-sync";
        version = "0.6.1";
        outputHash = "sha256-qsVIdNn7e9XZrFdozRAdUPn1MFco7dIblnN8/LGGPzo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync messages from LINE to your notes.'';
            homepage = "https://github.com/onikun94/line_to_obsidian";
        };
    }
);
    "lineage" = (
    let
        baseUrl = "https://github.com/ycnmhd/obsidian-lineage/releases/download/0.8.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lineage";
        version = "0.8.5";
        outputHash = "sha256-KKLyWh2yY0HDRAE9ic8kNuJhV4/vgi/Ax/fyAciWocc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A writing interface that combines structure and content. Inspired by Gingko Writer.'';
            homepage = "https://github.com/ycnmhd/obsidian-lineage";
        };
    }
);
    "linear" = (
    let
        baseUrl = "https://github.com/caseybecking/obsidian-linear-plugin/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linear";
        version = "1.1.3";
        outputHash = "sha256-6cMeIYuG3bPzO8V6FBejEY2CXEFCfKjTnqVPf8Nuhbk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate Linear issues with advanced filtering, sorting, and visual enhancements. Features include due date indicators, status colors, and comprehensive debug logging.'';
            homepage = "https://github.com/caseybecking/obsidian-linear-plugin";
        };
    }
);
    "linear-integration" = (
    let
        baseUrl = "https://github.com/casals/obsidian-linear-integration-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linear-integration";
        version = "1.0.0";
        outputHash = "sha256-nellbz9NBS3vWSWJXCcvYF5RY70WNoA3HnglVn7KBeg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Linear (https://linear.app) issues with notes. Create, update, and track Linear issues directly from your vault.'';
            homepage = "https://github.com/casals/obsidian-linear-integration-plugin";
        };
    }
);
    "ling-gloss" = (
    let
        baseUrl = "https://github.com/Mijyuoon/obsidian-ling-gloss/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ling-gloss";
        version = "2.2.0";
        outputHash = "sha256-iYjLZ7PmzAxdLPKJH5BxjlpvSqYJcaiysoeuwmzXlwg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format interlinear glosses used in linguistics texts.'';
            homepage = "https://github.com/Mijyuoon/obsidian-ling-gloss";
        };
    }
);
    "link-exploder" = (
    let
        baseUrl = "https://github.com/benhughes/obsidian-link-exploder/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-exploder";
        version = "0.3.0";
        outputHash = "sha256-PPwQufKwT0wEj/4PpBjzOkQUuusP4FpxAz8gudp23Pg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link Exploder creates a canvas from a note, embedding it"s incoming (i.e. backlinks) and outgoing links onto the canvas (as well as the their linked notes).'';
            homepage = "https://github.com/benhughes/obsidian-link-exploder";
        };
    }
);
    "link-favicon" = (
    let
        baseUrl = "https://github.com/joethei/obsidian-link-favicon/releases/download/1.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-favicon";
        version = "1.8.4";
        outputHash = "sha256-a3Px6FzL1iQv8+/E0koKLE3V4PD7tEIamGTelVJP6bk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''See the favicon for a linked website.'';
            homepage = "https://github.com/joethei/obsidian-link-favicon";
        };
    }
);
    "link-formatter" = (
    let
        baseUrl = "https://github.com/dilantha/link-formatter/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-formatter";
        version = "1.0.3";
        outputHash = "sha256-UzCERH39lhTW6rW4Hw4tPD3wPuw9q1hzNJASsnrPIqk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Formats a block of links into a clean markdown list'';
            homepage = "https://github.com/dilantha/link-formatter";
        };
    }
);
    "link-magic" = (
    let
        baseUrl = "https://github.com/AndyReifman/LinkMagic/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-magic";
        version = "1.1.1";
        outputHash = "sha256-HlU6eWTlKxNToqAmxuoo3ZSv0uzbtE6JJMhUzMXj4Zg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically adds links to defined regex.'';
            homepage = "https://github.com/AndyReifman/LinkMagic";
        };
    }
);
    "link-maintainer" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-link-maintainer/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-maintainer";
        version = "0.4.0";
        outputHash = "sha256-4giV/YwSL5Sr7m4JO2mso3jYK2aDdHLUNiJLRmoYXA0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Maintain note links when splitting or reorganizing notes.'';
            homepage = "https://github.com/wenlzhang/obsidian-link-maintainer";
        };
    }
);
    "link-navigation" = (
    let
        baseUrl = "https://github.com/xRyul/link-navigation/releases/download/0.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-navigation";
        version = "0.0.11";
        outputHash = "sha256-jf6zE+w7C59QhqQ7jyZ/gx32po9v+q1bp4PC/VtPWso=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Navigate between incoming links (inlinks), outgoing links (outlinks) N levels deep. Links from Canvas are also supported.'';
            homepage = "https://github.com/xRyul/link-navigation";
        };
    }
);
    "link-opening-restore" = (
    let
        baseUrl = "https://github.com/SmallZombie/link-opening-restore/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-opening-restore";
        version = "1.3.2";
        outputHash = "sha256-vSUayk7UqZQBoM2VE1UOI61fx86P0euofQhgE0OEryM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Restore the link opening method to Ctrl + Left Click.'';
            homepage = "https://github.com/SmallZombie/link-opening-restore";
        };
    }
);
    "link-preview" = (
    let
        baseUrl = "https://github.com/felipetappata/obsidian-link-preview/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-preview";
        version = "0.1.4";
        outputHash = "sha256-a2OKX+eeOPBCo2jo7bEIUehGrE92Hz/GhZnnXhPVWFo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show a preview of external links on hover'';
            homepage = "https://github.com/felipetappata/obsidian-link-preview";
        };
    }
);
    "link-range" = (
    let
        baseUrl = "https://github.com/rmellmer/obsidian-link-range/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-range";
        version = "2.0.0";
        outputHash = "sha256-rEnKStx19nFTkwQS894496J38vYA7pEU9oMYpJcwipg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add ranged wikilink support.'';
            homepage = "https://github.com/rmellmer/obsidian-link-range";
        };
    }
);
    "link-to-verse" = (
    let
        baseUrl = "https://github.com/aygjiay/obsidian-link-to-verse/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-to-verse";
        version = "1.2.0";
        outputHash = "sha256-6X9iXqVEku+lF7XEVxmy338cGrOgVLRzVMYPFpd194U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''From a bible reference selected creates a markdown link to a configured bible site.'';
            homepage = "https://github.com/aygjiay/obsidian-link-to-verse";
        };
    }
);
    "link-tree" = (
    let
        baseUrl = "https://github.com/j-palindrome/obsidian-link-tree/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-tree";
        version = "1.0.0";
        outputHash = "sha256-ffJoM8h5kT40+fArk6SiuUaZrjl+ocSjVeXTaCSQFDw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View file links and backlinks as a recursively expandable, filterable list with editable text, combining the structure of outliners like Dynalist & WorkFlowy with the flexibility of Obsidian.'';
            homepage = "https://github.com/j-palindrome/obsidian-link-tree";
        };
    }
);
    "link-with-alias" = (
    let
        baseUrl = "https://github.com/pvojtechovsky/obsidian-link-with-alias/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "link-with-alias";
        version = "1.0.11";
        outputHash = "sha256-mhnNG7qgz5L5/VCQjVEKP7dvRE7/2MEVSTZzPGTU1a4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create links and aliases in front matter of target document.'';
            homepage = "https://github.com/pvojtechovsky/obsidian-link-with-alias";
        };
    }
);
    "linked-data-helper" = (
    let
        baseUrl = "https://github.com/kometenstaub/linked-data-helper/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linked-data-helper";
        version = "1.0.0";
        outputHash = "sha256-GLWiey4MJiBnrmWJ+w+4U0gWiNU3Zy/ee1GwMax7vA8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prepare data needed by Linked Data Vocabularies.'';
            homepage = "https://github.com/kometenstaub/linked-data-helper";
        };
    }
);
    "linked-data-vocabularies" = (
    let
        baseUrl = "https://github.com/kometenstaub/linked-data-vocabularies/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linked-data-vocabularies";
        version = "2.1.0";
        outputHash = "sha256-P81nUhYxhDV1nAh1Pzl1n6FYMyH4ZFmf/Htnyt4gY5E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add Library of Congress Subject Headings (LCSH) as metadata to your notes.'';
            homepage = "https://github.com/kometenstaub/linked-data-vocabularies";
        };
    }
);
    "linked-note-exporter" = (
    let
        baseUrl = "https://github.com/the-c0d3r/obsidian-linked-note-exporter/releases/download/0.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linked-note-exporter";
        version = "0.2.5";
        outputHash = "sha256-HU+c6WIay0k1Vih/PZNHMGiQnFK6d+Xnf6S4ahsgU44=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export note with all its attachments and linked notes.'';
            homepage = "https://github.com/the-c0d3r/obsidian-linked-note-exporter";
        };
    }
);
    "linkify" = (
    let
        baseUrl = "https://github.com/matthewhchan/linkify/releases/download/1.0.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linkify";
        version = "1.0.16";
        outputHash = "sha256-mJMy8UTlTcIsACvUH1VzANKrPjMwG7yz5AHx0pF59MA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert matching text into links.'';
            homepage = "https://github.com/matthewhchan/linkify";
        };
    }
);
    "links" = (
    let
        baseUrl = "https://github.com/mii-key/obsidian-links/releases/download/1.17.51";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "links";
        version = "1.17.51";
        outputHash = "sha256-x9LIG14VQgNSEi9I6VTTmtKRvs8aBS6fQqnhsILVwOI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manipulate links.'';
            homepage = "https://github.com/mii-key/obsidian-links";
        };
    }
);
    "linkshelf" = (
    let
        baseUrl = "https://github.com/joelseq/obsidian-linkstowr/releases/download/0.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "linkshelf";
        version = "0.9.1";
        outputHash = "sha256-moxqf0yFpS0fPTHQBAFxWCkVE0C2w+5mFqfCKQjHwp0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save links from your browser.'';
            homepage = "https://github.com/joelseq/obsidian-linkstowr";
        };
    }
);
    "liquid-templates" = (
    let
        baseUrl = "https://github.com/oeN/liquid-template/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "liquid-templates";
        version = "0.3.1";
        outputHash = "sha256-GZpqEXDUT23hH0hTJ5rgGVTeGGv201xRAcsCqTf/Wn4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create your templates with LiquidJS tags support.'';
            homepage = "https://github.com/oeN/liquid-template";
        };
    }
);
    "list-outline-helper" = (
    let
        baseUrl = "https://github.com/triangular-sneaky/obsidian-list-outline-helper/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "list-outline-helper";
        version = "1.0.7";
        outputHash = "sha256-LpZoyvIJXUSoHD0dpikAoczB9mDwBtCWzOijL7ASNkc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Utilities to work with list outlines. Currently supports selecting the outline (current line and children)'';
            homepage = "https://github.com/triangular-sneaky/obsidian-list-outline-helper";
        };
    }
);
    "list-style" = (
    let
        baseUrl = "https://github.com/erykwalder/obsidian-list-style/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "list-style";
        version = "0.1.0";
        outputHash = "sha256-+WKCty43Rf4OvVmgHj0xBFvcvna8jSO/kEC2Ty5/BcU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set ordered list style inline. Alphabetic lists, Roman numeral lists, etc.'';
            homepage = "https://github.com/erykwalder/obsidian-list-style";
        };
    }
);
    "list-table-converter" = (
    let
        baseUrl = "https://github.com/paddomanno/obsidian-list-table-converter/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "list-table-converter";
        version = "1.0.5";
        outputHash = "sha256-MXaLHmC27Acz1Gc51K0z3RwQCwkzlnQiOk8+oQnk7Ig=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a list (or multi-line text) to a table.'';
            homepage = "https://github.com/paddomanno/obsidian-list-table-converter";
        };
    }
);
    "listen-up" = (
    let
        baseUrl = "https://github.com/tejas-hosamani/obsidian-plugin-text-to-speech/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "listen-up";
        version = "1.0.7";
        outputHash = "sha256-RffdT9DF7MU6fTkUFEd6GRQ7NNO8bwQFX2q3YPRpMKk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Covert text to natural voice audio, locally - Listen Up!'';
            homepage = "https://github.com/tejas-hosamani/obsidian-plugin-text-to-speech";
        };
    }
);
    "litecite" = (
    let
        baseUrl = "https://github.com/ras0q/obsidian-litecite/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "litecite";
        version = "1.0.12";
        outputHash = "sha256-rneqQAcYVFQAPO2CPv7BeHn2ngb6+vSCuUUmaJKJQps=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates citation notes from a BibTeX / BibLaTeX file'';
            homepage = "https://github.com/ras0q/obsidian-litecite";
        };
    }
);
    "litegallery" = (
    let
        baseUrl = "https://github.com/jpoles1/obsidian-litegal/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "litegallery";
        version = "1.0.6";
        outputHash = "sha256-X01UAddeRikVmsUFyEeFjPFqKYAjidhZZJa9NtZODmQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily create carousel galleries to better organize/view images in your notes.'';
            homepage = "https://github.com/jpoles1/obsidian-litegal";
        };
    }
);
    "literate-haskell" = (
    let
        baseUrl = "https://github.com/jajaperson/obsidian-literate-haskell/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "literate-haskell";
        version = "1.0.1";
        outputHash = "sha256-ImYkV0FaQe7OAw0CJDZ/VtHQuTy0mECXa7NC6rAhrYw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate .lhs files into your PKM.'';
            homepage = "https://github.com/jajaperson/obsidian-literate-haskell";
        };
    }
);
    "live-variables" = (
    let
        baseUrl = "https://github.com/HamzaBenyazid/Live-variables/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "live-variables";
        version = "2.1.1";
        outputHash = "sha256-8wQzvjWelAeeYVYFUtBRBfRQObM02yyeY0cCuPBrwOo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define variables in your note"s properties and reuse them throughout your content.'';
            homepage = "https://github.com/HamzaBenyazid/Live-variables";
        };
    }
);
    "live-wallpaper" = (
    let
        baseUrl = "https://github.com/remememe/Live-Wallpaper/releases/download/1.6.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "live-wallpaper";
        version = "1.6.3";
        outputHash = "sha256-wBC2rEZhdhEDjwRbcBSe7JKRvLOxHKxQZuMRInsFHHE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add dynamic live wallpapers (images/videos) to your workspace with customizable effects'';
            homepage = "https://github.com/remememe/Live-Wallpaper";
        };
    }
);
    "livecodes-playground" = (
    let
        baseUrl = "https://github.com/gapmiss/livecodes-playground/releases/download/0.12.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "livecodes-playground";
        version = "0.12.3";
        outputHash = "sha256-FkY43f489nt8MJSzjaR+p4OuzeNVTzJvW0yk7QW/SoY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Client-side code editor playground - Powered by LiveCodes'';
            homepage = "https://github.com/gapmiss/livecodes-playground";
        };
    }
);
    "llm-docs" = (
    let
        baseUrl = "https://github.com/shane-lamb/obsidian-llm-docs/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "llm-docs";
        version = "1.3.0";
        outputHash = "sha256-dJPgQx/4ccdakzOJtAXrn4VK0H//FyCS5GaQJShtxRc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple LLM (AI chat) client using pure markdown documents'';
            homepage = "https://github.com/shane-lamb/obsidian-llm-docs";
        };
    }
);
    "llm-shortcut" = (
    let
        baseUrl = "https://github.com/chernodub/obsidian-llm-shortcut/releases/download/0.14.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "llm-shortcut";
        version = "0.14.0";
        outputHash = "sha256-a0NQ3mMKNqMCnL1vtf5Oi+JbAfEt8mIx9uTeNJyPGKA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides a way to create shortcuts for commands powered by LLM capabilities.'';
            homepage = "https://github.com/chernodub/obsidian-llm-shortcut";
        };
    }
);
    "llm-summary" = (
    let
        baseUrl = "https://github.com/larksq/obsidian-llm-summary/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "llm-summary";
        version = "0.1.9";
        outputHash = "sha256-GJwuBexlEqHOfWBFOmc/lBpezlcdgVWiy52LykN0ufg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quick note taking with the help of LLMs. LLMs help you to summarize / organize PDFs or existing notes.'';
            homepage = "https://github.com/larksq/obsidian-llm-summary";
        };
    }
);
    "llm-tagger" = (
    let
        baseUrl = "https://github.com/djayatillake/obsidian-llm-tagger/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "llm-tagger";
        version = "1.1.2";
        outputHash = "sha256-cXL25b9waD/U1fGQjlLFB/Tzj+fbFlNd6LMqRk4mkp0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically tag your documents using a local LLM'';
            homepage = "https://github.com/djayatillake/obsidian-llm-tagger";
        };
    }
);
    "llm-test-gen" = (
    let
        baseUrl = "https://github.com/aldo-g/obsidian-llm-test/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "llm-test-gen";
        version = "1.2.2";
        outputHash = "sha256-tE55lpQzrcx0rCSDssAfdhWJV0FCysiBufMpi7EGYew=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate AI-powered test questions from your notes with multiple LLM providers (OpenAI, Claude, Mistral, Gemini, DeepSeek) to enhance your learning and retention.'';
            homepage = "https://github.com/aldo-g/obsidian-llm-test";
        };
    }
);
    "llm-workspace" = (
    let
        baseUrl = "https://github.com/ofalvai/obsidian-llm-workspace/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "llm-workspace";
        version = "1.3.0";
        outputHash = "sha256-DSh+FWnWL/xEhrTKH/JNMRJSOIwkkMK7kultRbh9GbY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use Large Language Models grounded in your notes.'';
            homepage = "https://github.com/ofalvai/obsidian-llm-workspace";
        };
    }
);
    "local-any-files" = (
    let
        baseUrl = "https://github.com/ShermanTsang/obsidian-local-any-files/releases/download/1.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-any-files";
        version = "1.3.5";
        outputHash = "sha256-k/0xjG5dBdywdU5D8Bq9Lr77UnevZJxUd/otK+aKfa0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract and download any external files from links to local files'';
            homepage = "https://github.com/ShermanTsang/obsidian-local-any-files";
        };
    }
);
    "local-backup" = (
    let
        baseUrl = "https://github.com/velviagris/obsidian-local-backup/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-backup";
        version = "0.2.2";
        outputHash = "sha256-4Clw70LMEcKMbinMoEoHrKIuzBZmLXZszDGf9i2ifus=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically creates a local backup of the vault.'';
            homepage = "https://github.com/velviagris/obsidian-local-backup";
        };
    }
);
    "local-bible-ref" = (
    let
        baseUrl = "https://github.com/camelChief/local-bible-ref/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-bible-ref";
        version = "1.3.1";
        outputHash = "sha256-rlcAVsy16wiA3Njdty3DSN6mxTbGYA1O+xRcnrv8XBI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly and easily reference Bible passages stored locally in your vault.'';
            homepage = "https://github.com/camelChief/local-bible-ref";
        };
    }
);
    "local-gpt" = (
    let
        baseUrl = "https://github.com/pfrankov/obsidian-local-gpt/releases/download/4.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-gpt";
        version = "4.2.1";
        outputHash = "sha256-ADrEJC6yigDr8niKNzbn3kQ6z5+9m5F13ncFgGSSvH4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Local Ollama and OpenAI-like GPT"s assistance for maximum privacy and offline access.'';
            homepage = "https://github.com/pfrankov/obsidian-local-gpt";
        };
    }
);
    "local-graphql" = (
    let
        baseUrl = "https://github.com/TwIStOy/obsidian-local-graphql/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-graphql";
        version = "1.0.2";
        outputHash = "sha256-N4PAc3GqZmrIJ43r3Xead39GhszlXAT8ioOSeaGjkCQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export Obsidian data as a local GraphQL server.'';
            homepage = "https://github.com/TwIStOy/obsidian-local-graphql";
        };
    }
);
    "local-llm-helper" = (
    let
        baseUrl = "https://github.com/manimohans/obsidian-local-llm-helper/releases/download/2.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-llm-helper";
        version = "2.4.1";
        outputHash = "sha256-NXUV39YA2AdEAyFjLtUDZkCnGgVN7G/uDEgaXhLRlFQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use a local LLM server to augment your notes'';
            homepage = "https://github.com/manimohans/obsidian-local-llm-helper";
        };
    }
);
    "local-media-embedder" = (
    let
        baseUrl = "https://github.com/seyf1elislam/obsidian-LocalMediaEmbedder-plugin/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-media-embedder";
        version = "0.1.4";
        outputHash = "sha256-SYMZ4m/5vW9mADzJqAedg80fRVBELw+6qxuBpztsl+Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed videos and images and audios from your local device in your notes.'';
            homepage = "https://github.com/seyf1elislam/obsidian-LocalMediaEmbedder-plugin";
        };
    }
);
    "local-quotes" = (
    let
        baseUrl = "https://github.com/sundevista/local-quotes/releases/download/1.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-quotes";
        version = "1.9.0";
        outputHash = "sha256-Rg+kHSymTQaur3laHT3Tpsqs6WPnRBBuV+gpaxy8SSk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collect your quotes from all over the vault and embed them in different locations and watch them refreshing in real-time.'';
            homepage = "https://github.com/sundevista/local-quotes";
        };
    }
);
    "local-rss" = (
    let
        baseUrl = "https://github.com/onikun94/obsidian-local-rss/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "local-rss";
        version = "0.2.1";
        outputHash = "sha256-/draBl8TIZKESJP0nSeZhKY1I7fqQ8aMUpavkUToyPc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Download RSS feed articles to local files.'';
            homepage = "https://github.com/onikun94/obsidian-local-rss";
        };
    }
);
    "log-keeper" = (
    let
        baseUrl = "https://github.com/JimJamBimBam/obsidian-log-keeper/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "log-keeper";
        version = "3.1.0";
        outputHash = "sha256-Fjr8GvrUgoyk1uxAss+gxDr6+HG9TsDCBA26ucvVK8c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate time stamps automatically as changes are made to a note.'';
            homepage = "https://github.com/JimJamBimBam/obsidian-log-keeper";
        };
    }
);
    "logos-refs" = (
    let
        baseUrl = "https://github.com/joey-kilgore/logos-refs/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "logos-refs";
        version = "1.3.1";
        outputHash = "sha256-w12DfX8PKATJPPeoB0EDbBjkl3UoJwdqPm9k6cFxSao=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily take refs from Logos and connect them within your notes'';
            homepage = "https://github.com/joey-kilgore/logos-refs";
        };
    }
);
    "logstravaganza" = (
    let
        baseUrl = "https://github.com/czottmann/obsidian-logstravaganza/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "logstravaganza";
        version = "2.3.0";
        outputHash = "sha256-41vCMQKOVSOgyVCkTtrHiutE4q7Do25dRcc6hEEOjcQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple proxy for `console.*()` calls which copies log messages and uncaught exceptions to a note.'';
            homepage = "https://github.com/czottmann/obsidian-logstravaganza";
        };
    }
);
    "long-sentence-highlighter" = (
    let
        baseUrl = "https://github.com/RobertMeissner/obsidian-long-sentence-highlighter/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "long-sentence-highlighter";
        version = "1.0.1";
        outputHash = "sha256-l2y1JIhi/QcgncBQfxyChrBP+0ZNqPhfV/GB3BeWJMU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlights sentences that exceed a configurable word count threshold to help improve writing clarity and readability.'';
            homepage = "https://github.com/RobertMeissner/obsidian-long-sentence-highlighter";
        };
    }
);
    "longform" = (
    let
        baseUrl = "https://github.com/kevboh/longform/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "longform";
        version = "2.1.0";
        outputHash = "sha256-rbkKOz9IF+jdO+oqwOPcdD4ZuHdN7J2iXb2/SPott6U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps you write and edit novels, screenplays, and other long projects.'';
            homepage = "https://github.com/kevboh/longform";
        };
    }
);
    "longtime-diary" = (
    let
        baseUrl = "https://github.com/sawamaru/obsidian-LongtimeDiary/releases/download/0.9.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "longtime-diary";
        version = "0.9.4";
        outputHash = "sha256-BNSfmkFGhYpnFk2x0ivACSphXhZRCw2eo1q0YUjfSNg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show past Daily notes on the same day in previous years.'';
            homepage = "https://github.com/sawamaru/obsidian-LongtimeDiary";
        };
    }
);
    "loom" = (
    let
        baseUrl = "https://github.com/cosmicoptima/loom/releases/download/1.22.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "loom";
        version = "1.22.6";
        outputHash = "sha256-V1B2/Fcy1N09c6wAxnGrNe0TUtxmINsQAm0OHph2IPM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A recursively branching interface to GPT-3 and other language models.'';
            homepage = "https://github.com/cosmicoptima/loom";
        };
    }
);
    "lovely-mindmap" = (
    let
        baseUrl = "https://github.com/shaunhurryup/lovely-mindmap/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lovely-mindmap";
        version = "1.0.4";
        outputHash = "sha256-mSQXyZ/6wPpnk54AtQglDbA9iEYMe1UvihVfiZv9iU0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Build your own knowledge graph with smiles :-)'';
            homepage = "https://github.com/shaunhurryup/lovely-mindmap";
        };
    }
);
    "lskypro-auto-upload" = (
    let
        baseUrl = "https://github.com/NekoTarou/lskypro-auto-upload/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lskypro-auto-upload";
        version = "1.0.6";
        outputHash = "sha256-V/w9pWZlHKNauaIXiWdlRdoXus9QwWbopSwSQDFO7d4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto upload images from clipboard to Lskypro.'';
            homepage = "https://github.com/NekoTarou/lskypro-auto-upload";
        };
    }
);
    "lskypro-upload-v2" = (
    let
        baseUrl = "https://github.com/Aceak/Lskypro-Upload-V2/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lskypro-upload-v2";
        version = "1.1.3";
        outputHash = "sha256-9tjlJn9r59tVs1kz8tJ1aWeD4Jc40i3Z5gT1LnuEj5Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto upload local images to LskyPro.'';
            homepage = "https://github.com/Aceak/Lskypro-Upload-V2";
        };
    }
);
    "luhman" = (
    let
        baseUrl = "https://github.com/Dyldog/luhman-obsidian-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "luhman";
        version = "1.2.0";
        outputHash = "sha256-qQCSrmbrxRlKIfD3keXVnoylcuS+8DOKVILyr81okBg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands for handling a zettelkasten with Luhman-style IDs as filenames.'';
            homepage = "https://github.com/Dyldog/luhman-obsidian-plugin";
        };
    }
);
    "lumberjack-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/lumberjack-obsidian/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lumberjack-obsidian";
        version = "1.3.0";
        outputHash = "sha256-/I8pJ0d9b5ZFJeYkr58vNCra2ahBzf08d2bKBhuWK2U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Log your thoughts! Lumberjack adds URL commands to help you axe inefficiency and get right to writing.'';
            homepage = "https://github.com/ryanjamurphy/lumberjack-obsidian";
        };
    }
);
    "lunar-calendar" = (
    let
        baseUrl = "https://github.com/WHG555/lunar-calendar/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lunar-calendar";
        version = "1.2.0";
        outputHash = "sha256-dH9oZJeuPaSNoEbXMMxvsfBN2nd37eL66t9Az3SYnnQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''一个支持农历的日历'';
            homepage = "https://github.com/WHG555/lunar-calendar";
        };
    }
);
    "lyrics" = (
    let
        baseUrl = "https://github.com/eatgrass/obsidian-lyrics/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "lyrics";
        version = "1.1.0";
        outputHash = "sha256-GeSv3vSX11YnHzbknArTpOKJNvm4P9PVI4QCpbZ2ut4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance the audio player with interacive lyrics.'';
            homepage = "https://github.com/eatgrass/obsidian-lyrics";
        };
    }
);
    "mac-os-keyboard-nav-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/macOS-keyboard-nav-obsidian/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mac-os-keyboard-nav-obsidian";
        version = "1.1.2";
        outputHash = "sha256-0N4qxbEDH1xJD1R8LpgNSRtp/BNAHhqqyo+8Kun9vxk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable alt+↑ and alt+↓ keyboard navigation.'';
            homepage = "https://github.com/ryanjamurphy/macOS-keyboard-nav-obsidian";
        };
    }
);
    "macros" = (
    let
        baseUrl = "https://github.com/JamesCliffordSpratt/macros/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "macros";
        version = "2.4.0";
        outputHash = "sha256-9z3++6f3J4HHFlkHFruMk6ypqTzw4gz8eKAFUOuHUUo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track and visualize your daily macros, meals and nutrition inside your notes.'';
            homepage = "https://github.com/JamesCliffordSpratt/macros";
        };
    }
);
    "magic-calendar" = (
    let
        baseUrl = "https://github.com/Vaccarini-Lorenzo/MagicCalendar/releases/download/1.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "magic-calendar";
        version = "1.1.9";
        outputHash = "sha256-bGHMbCRDQrrywvjeA2pYQhjSurIK1BidyJMc2DmC2tg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Leverage natural language processing techniques to find calendar events in Markdown notes, seamlessly synchronizing them with a calendar of choice.'';
            homepage = "https://github.com/Vaccarini-Lorenzo/MagicCalendar";
        };
    }
);
    "magic-move" = (
    let
        baseUrl = "https://github.com/imfenghuang/obsidian-magic-move/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "magic-move";
        version = "0.0.1";
        outputHash = "sha256-bfnHCsmmlSvPPl4ON6Y7RAYl0k9nC5udl9aVg1KpmXE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Animating code blocks with markdown and code syntax highlighting with beautiful themes.'';
            homepage = "https://github.com/imfenghuang/obsidian-magic-move";
        };
    }
);
    "magiedit" = (
    let
        baseUrl = "https://github.com/magitools/magiedit-obsidian/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "magiedit";
        version = "1.0.2";
        outputHash = "sha256-mAZEh3iF/yN6+QbsYgepB/wvzX0BBQZnv5K1uDRAc8A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official plugin to interact with Magiedit"s API'';
            homepage = "https://github.com/magitools/magiedit-obsidian";
        };
    }
);
    "mahgen-renderer" = (
    let
        baseUrl = "https://github.com/MichaelFW-ui/mahgen-renderer/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mahgen-renderer";
        version = "1.0.10";
        outputHash = "sha256-eejBEjuTPtRdyM2uLfRkuBKZWxYUalwnkwBM+6kjvIY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display mahjong images inline or in block using Mahgen.'';
            homepage = "https://github.com/MichaelFW-ui/mahgen-renderer";
        };
    }
);
    "mahjong-renderer" = (
    let
        baseUrl = "https://github.com/h-sphere/obsidian-mahjong-renderer/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mahjong-renderer";
        version = "0.1.0";
        outputHash = "sha256-NQXizJv1hDdDhuj5s/2domwPBogw4+sMiQMl+YLQ7BQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render mahjong tiles (riichi mahjong) using MPSZ notation'';
            homepage = "https://github.com/h-sphere/obsidian-mahjong-renderer";
        };
    }
);
    "make-md" = (
    let
        baseUrl = "https://github.com/Make-md/makemd/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "make-md";
        version = "1.3.4";
        outputHash = "sha256-LKINzxWc2xIey232VU4G0Wj77zj5deQa70/Z91AmmNY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make.md brings you features that supercharges Obsidian. Sort your files in custom order and add file icons using Spaces. Edit inline embeds with Flow Editor. And style your text and add new Markdown blocks without writing Markdown using Maker Mode.'';
            homepage = "https://github.com/Make-md/makemd";
        };
    }
);
    "manictime" = (
    let
        baseUrl = "https://github.com/manictime/manictime-obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "manictime";
        version = "1.0.0";
        outputHash = "sha256-su4rp1ctqj8z/+6OYufU0+pCY3rRs8yXq2+ajkhXnAM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send the path of the active file to locally installed ManicTime client.'';
            homepage = "https://github.com/manictime/manictime-obsidian";
        };
    }
);
    "mantou-ai" = (
    let
        baseUrl = "https://github.com/ravenSanstete/Obsidian-MantouAI/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mantou-ai";
        version = "1.0.0";
        outputHash = "sha256-nLkBNHqvHBWz2cGLNIOqWCUZSourAy8b6i/mgcJ8/2o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Work as a personal assistant for translation, writing polish, general Q&A, summarizing, using the power of large language models.'';
            homepage = "https://github.com/ravenSanstete/Obsidian-MantouAI";
        };
    }
);
    "manual-sorting" = (
    let
        baseUrl = "https://github.com/kh4f/manual-sorting/releases/download/3.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "manual-sorting";
        version = "3.2.4";
        outputHash = "sha256-Kv17FGmT58nhZGZIyMsKLjsqiPRGLorDHYxsiIzDyo0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Drag"n"Drop sorting within file explorer.'';
            homepage = "https://github.com/kh4f/manual-sorting";
        };
    }
);
    "map-of-content" = (
    let
        baseUrl = "https://github.com/Robin-Haupt-1/Obsidian-Map-of-Content/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "map-of-content";
        version = "1.4.0";
        outputHash = "sha256-ZCExM/MqusCltECLYWoVbQpq5uM3c+Yh2olcCqdmjv8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically generate a Map of Content for your vault.'';
            homepage = "https://github.com/Robin-Haupt-1/Obsidian-Map-of-Content";
        };
    }
);
    "mapbox-location" = (
    let
        baseUrl = "https://github.com/aaronczichon/obsidian-location-plugin/releases/download/2.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mapbox-location";
        version = "2.1.3";
        outputHash = "sha256-mcyBUSKcoKW7sEZfa5jTYu3pQ/34FP7t1YV9ZGd5xhY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Renders an mapbox location image based on given coordinates.'';
            homepage = "https://github.com/aaronczichon/obsidian-location-plugin";
        };
    }
);
    "maps" = (
    let
        baseUrl = "https://github.com/obsidianmd/obsidian-maps/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "maps";
        version = "0.1.6";
        outputHash = "sha256-vCxzBCps/2ZQ/StXtSlfbFSqt5k/OUxtTlqxzm5tFZo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a map layout to bases so you can display notes as an interactive map view.'';
            homepage = "https://github.com/obsidianmd/obsidian-maps";
        };
    }
);
    "mark-open-files" = (
    let
        baseUrl = "https://github.com/gasparschott/obsidian-mark-open-files/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mark-open-files";
        version = "1.2.2";
        outputHash = "sha256-PrKdcEtTPstg3vw/rpg640c+IueSUokqOkLHHzygVmY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhances the File Explorer by adding a marker to all the File Explorer items that are currently open in the workspace.'';
            homepage = "https://github.com/gasparschott/obsidian-mark-open-files";
        };
    }
);
    "markdoc" = (
    let
        baseUrl = "https://github.com/kamoshi/obsidian-markdoc/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdoc";
        version = "0.0.1";
        outputHash = "sha256-Fbsyqwe0xXLxkWhqpLtSXvERNp8fWO1n9/yzkujE5pM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Basic support for Markdoc files.'';
            homepage = "https://github.com/kamoshi/obsidian-markdoc";
        };
    }
);
    "markdown-attributes" = (
    let
        baseUrl = "https://github.com/javalent/markdown-attributes/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-attributes";
        version = "1.2.2";
        outputHash = "sha256-ci5/I03zSZARPszoiGA1sBr6Tjcrqsm/aHh3gkmXJcg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add Markdown attributes to elements.'';
            homepage = "https://github.com/javalent/markdown-attributes";
        };
    }
);
    "markdown-blogger" = (
    let
        baseUrl = "https://github.com/afazio1/obsidian-markdown-blogger/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-blogger";
        version = "1.1.0";
        outputHash = "sha256-/1pJs21YcicMXRw8d6IyFdig2LzpnZF26kLuii5gGnk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Push Markdown notes to your local blog, portfolio, or static site. Works with Astro.js, Next.js, and any other framework configured to render Markdown pages.'';
            homepage = "https://github.com/afazio1/obsidian-markdown-blogger";
        };
    }
);
    "markdown-chords" = (
    let
        baseUrl = "https://github.com/dnotes/obsidian-markdown-chords/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-chords";
        version = "1.0.4";
        outputHash = "sha256-qgCMSjBc8yVhuzc3r/AhdxiAz0cQsYUDGAOeo8nPo6E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add musical chord notation and chord diagrams for stringed instruments (e.g. guitar) in Markdown. Supports chords in any Western scale/mode, including extended jazz chords.'';
            homepage = "https://github.com/dnotes/obsidian-markdown-chords";
        };
    }
);
    "markdown-hijacker" = (
    let
        baseUrl = "https://github.com/especialkim/synaptic-bridge/releases/download/0.1.51";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-hijacker";
        version = "0.1.51";
        outputHash = "sha256-8vwhomxkSniRTDJe6120YhLsj6OYl3B8gh3cG0rBhiA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Beyond the Vault. One hub for every Markdown, everywhere'';
            homepage = "https://github.com/especialkim/synaptic-bridge";
        };
    }
);
    "markdown-link-space-encoder" = (
    let
        baseUrl = "https://github.com/rkosova/obsidian-markdown-link-space-encoder/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-link-space-encoder";
        version = "1.1.0";
        outputHash = "sha256-vjbjJ/Ql64m07Vf+M/0jzp9Ou28PFpY1bUgN8DL4CTI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically encode spaces to %20 in Markdown-style links.'';
            homepage = "https://github.com/rkosova/obsidian-markdown-link-space-encoder";
        };
    }
);
    "markdown-media-card" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-markdown-media-card/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-media-card";
        version = "1.2.0";
        outputHash = "sha256-vQEJjpPoE/mp/AFtj7DWpu8bX0ocJ2mqltGDuBCYCUM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert media information cards in Markdown, such as books, music, movies, etc.'';
            homepage = "https://github.com/zhouhua/obsidian-markdown-media-card";
        };
    }
);
    "markdown-prettifier" = (
    let
        baseUrl = "https://github.com/cristianvasquez/obsidian-prettify/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-prettifier";
        version = "0.1.3";
        outputHash = "sha256-mVDjJAXjNcD8pBINrTCYaRpMjYX3ez8DfsiqV8Je7w0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fix and reformats ugly Markdown.'';
            homepage = "https://github.com/cristianvasquez/obsidian-prettify";
        };
    }
);
    "markdown-shortcuts" = (
    let
        baseUrl = "https://github.com/JulesGuesnon/obsidian-markdown-shortcuts/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-shortcuts";
        version = "0.4.0";
        outputHash = "sha256-T5aG63lB1WVABBWB9NQL4TY5mnH64qLZnWk7Emxzeno=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write Markdown from shortcuts.'';
            homepage = "https://github.com/JulesGuesnon/obsidian-markdown-shortcuts";
        };
    }
);
    "markdown-sync-scroll" = (
    let
        baseUrl = "https://github.com/XeroAlpha/markdown-sync-scroll/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-sync-scroll";
        version = "0.1.0";
        outputHash = "sha256-5ywZiWmDpXZ7vuylLiL+p9Kfugghl6rfnpKroYoN1Yg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allow two linked Markdown views to scroll synchronously.'';
            homepage = "https://github.com/XeroAlpha/markdown-sync-scroll";
        };
    }
);
    "markdown-table-editor" = (
    let
        baseUrl = "https://github.com/ganesshkumar/obsidian-table-editor/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-table-editor";
        version = "0.3.1";
        outputHash = "sha256-lqOwI973i5l95i7Ob6VM+p5clYBzMBvMfJ5tsEqar0Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An editor for Markdown tables. Open CSV, Microsoft Excel/Google Sheets data as Markdown tables.'';
            homepage = "https://github.com/ganesshkumar/obsidian-table-editor";
        };
    }
);
    "markdown-tags" = (
    let
        baseUrl = "https://github.com/binarynoir/obsidian-markdown-tags/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-tags";
        version = "1.3.1";
        outputHash = "sha256-vMR5olJyUWQv05i03/0vFh4ptzIdTI9IksRCISAdXXU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your Markdown documents with tags and arrow tags. Use predefined or custom labels, customizable colors, and arrow indicators to visually track tasks and statuses.'';
            homepage = "https://github.com/binarynoir/obsidian-markdown-tags";
        };
    }
);
    "markdown-timeline" = (
    let
        baseUrl = "https://github.com/recklyss/markdown-timeline/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-timeline";
        version = "0.0.6";
        outputHash = "sha256-E2XoW55Zw9eVmXxRI5trNLQU6LbTE4J9zBZKff+pUbM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert markdown into timeline visualization'';
            homepage = "https://github.com/recklyss/markdown-timeline";
        };
    }
);
    "markdown-to-slack-message" = (
    let
        baseUrl = "https://github.com/idreamer/markdown-to-slack-message/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-to-slack-message";
        version = "0.0.1";
        outputHash = "sha256-CUT3yYm7qK2gtOYbhuWbm5KFbmbJmVPtTaghqbgpDmI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a Markdown note to the Slack message blocks that enable you to send to Slack.'';
            homepage = "https://github.com/idreamer/markdown-to-slack-message";
        };
    }
);
    "markdown-tree" = (
    let
        baseUrl = "https://github.com/carvah/markdown-tree-plugin/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markdown-tree";
        version = "0.1.0";
        outputHash = "sha256-0Uf3sb9e8056Bg/BNQD7r3RiZ06qEpsXvK1cZzPvcOc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a beautiful and intuitive directory tree using Markdown-oriented code style using tabs, spaces and enters.'';
            homepage = "https://github.com/carvah/markdown-tree-plugin";
        };
    }
);
    "marker-api" = (
    let
        baseUrl = "https://github.com/L3-N0X/obsidian-marker/releases/download/1.4.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "marker-api";
        version = "1.4.6";
        outputHash = "sha256-LEKlA/rIBDG59J3wZ7ACTso2qwBy4P2ADhASjHcZLGI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert PDFs to rich Markdown, including images and ocr using the marker api'';
            homepage = "https://github.com/L3-N0X/obsidian-marker";
        };
    }
);
    "markfilesync" = (
    let
        baseUrl = "https://github.com/rouvenjahnke/markwhen-file-sync/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markfilesync";
        version = "1.1.0";
        outputHash = "sha256-k6FbM4HkFR6sU3KIg5zhtNknUXt2QEqhxzbWjFXRrlA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize properties from your notes with a Markwhen timeline file'';
            homepage = "https://github.com/rouvenjahnke/markwhen-file-sync";
        };
    }
);
    "markitdown" = (
    let
        baseUrl = "https://github.com/ethanolivertroy/obsidian-markitdown/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markitdown";
        version = "2.1.0";
        outputHash = "sha256-WU5HeLD3fNG5DpLluQ5cbIOBmm7sQ9+6gv1hVhonfLg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert PDFs, Office documents, images, and other file formats to Markdown using Microsoft"s Markitdown tool'';
            homepage = "https://github.com/ethanolivertroy/obsidian-markitdown";
        };
    }
);
    "markline" = (
    let
        baseUrl = "https://github.com/hotoo/obsidian-markline/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markline";
        version = "1.2.2";
        outputHash = "sha256-sF2LH/yanl4Flp8VtJEKTriC0yd3zvA7Ux3CAtc0r1A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Timeline view from Markdown'';
            homepage = "https://github.com/hotoo/obsidian-markline";
        };
    }
);
    "markmap-to-csv" = (
    let
        baseUrl = "https://github.com/pj4316/markmap-to-csv-obsidian/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markmap-to-csv";
        version = "1.0.1";
        outputHash = "sha256-ONopL3GuxXYhFdK3BZRRDfPJE3v0vWPkAgS6BiXGfeE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Converts Markmap data to CSV format.'';
            homepage = "https://github.com/pj4316/markmap-to-csv-obsidian";
        };
    }
);
    "markpilot" = (
    let
        baseUrl = "https://github.com/taichimaeda/markpilot/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markpilot";
        version = "1.2.5";
        outputHash = "sha256-dX2zrzT9irTbjbeC3wTgeno/4kadfqn7P97wRCezL00=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Inline completions and chat view powered by OpenAI.'';
            homepage = "https://github.com/taichimaeda/markpilot";
        };
    }
);
    "markwhen" = (
    let
        baseUrl = "https://github.com/mark-when/obsidian-plugin/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "markwhen";
        version = "0.0.7";
        outputHash = "sha256-HIn0tm5aIDJ1f68erbNdqRGv2R/iVIZ1ufID0LeOsR4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create timelines, gantt charts, calendars, and more using markwhen.'';
            homepage = "https://github.com/mark-when/obsidian-plugin";
        };
    }
);
    "marp" = (
    let
        baseUrl = "https://github.com/JichouP/obsidian-marp-plugin/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "marp";
        version = "1.5.0";
        outputHash = "sha256-9jKxfZQ8qws2H9l0jx4thL71SeMgQ/ib6H4/m1jeS9o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create slide decks in Markdown with Marp.'';
            homepage = "https://github.com/JichouP/obsidian-marp-plugin";
        };
    }
);
    "marp-slides" = (
    let
        baseUrl = "https://github.com/samuele-cozzi/obsidian-marp-slides/releases/download/0.46.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "marp-slides";
        version = "0.46.1";
        outputHash = "sha256-IhnDkjZQ8n46lh1+gHoYVrbtM7zf+Q3DGyEtqBTr9bQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Marp presentations.'';
            homepage = "https://github.com/samuele-cozzi/obsidian-marp-slides";
        };
    }
);
    "masking-type" = (
    let
        baseUrl = "https://github.com/Telehakke/masking-type/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "masking-type";
        version = "1.2.0";
        outputHash = "sha256-r92yf/EvtRgFz4afdYqTPFUdqmf2nkT64x18oa1TIEQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mask bold, italic, and highlight'';
            homepage = "https://github.com/Telehakke/masking-type";
        };
    }
);
    "mass-create" = (
    let
        baseUrl = "https://github.com/vellikhor/mass-create/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mass-create";
        version = "1.0.3";
        outputHash = "sha256-rmQ4jY4CZkhbyIoIOwFCYHXZBE+kyu532lKXVitvXuY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create large quantities of notes easily at one time.'';
            homepage = "https://github.com/vellikhor/mass-create";
        };
    }
);
    "mastodon-threading" = (
    let
        baseUrl = "https://github.com/elpamplina/mastodon-threading/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mastodon-threading";
        version = "1.2.0";
        outputHash = "sha256-hiSMcPkzCRJ9t79KimAfSo4xtrJsf6W5NvuDQtxtEaY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Compose and post threads to Mastodon.'';
            homepage = "https://github.com/elpamplina/mastodon-threading";
        };
    }
);
    "match-syntax" = (
    let
        baseUrl = "https://github.com/rivea0/obsidian-match-syntax/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "match-syntax";
        version = "1.2.0";
        outputHash = "sha256-MogAMx0ci/6rCZntHAd3Zvzr79xRxNxc39Q1FtvJeS0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A flexible, regex-like lookups for your notes.'';
            homepage = "https://github.com/rivea0/obsidian-match-syntax";
        };
    }
);
    "material-symbols" = (
    let
        baseUrl = "https://github.com/cberane/obsidian-material-symbols/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "material-symbols";
        version = "1.0.0";
        outputHash = "sha256-QkcHDpTR5KMc0tnOkg4q/OVThxTDpY0DxS48BUtquLc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add Google"s Material Symbols (outlined).'';
            homepage = "https://github.com/cberane/obsidian-material-symbols";
        };
    }
);
    "math-booster" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-latex-theorem-equation-referencer/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "math-booster";
        version = "2.2.0";
        outputHash = "sha256-GPHQ2b8rjK47Fj74LbXgT5HLs9yj5CHJy/cGxosykNE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A powerful indexing & referencing system for theorems & equations in your vault. Bring LaTeX-like workflow into Obsidian with theorem environments, automatic equation numbering, and more.'';
            homepage = "https://github.com/RyotaUshio/obsidian-latex-theorem-equation-referencer";
        };
    }
);
    "math-in-callout" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-math-in-callout/releases/download/0.3.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "math-in-callout";
        version = "0.3.8";
        outputHash = "sha256-w+By34nAQ1kPZ7biU5NDqQasCJbA3g25uUVcsJrSiR4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add better Live Preview support for math rendering inside callouts & blockquotes.'';
            homepage = "https://github.com/RyotaUshio/obsidian-math-in-callout";
        };
    }
);
    "math-indicator-changer" = (
    let
        baseUrl = "https://github.com/Ori-Replication/obsidian-math-indicator-changer/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "math-indicator-changer";
        version = "1.0.7";
        outputHash = "sha256-0xlgWb4RpHyN2unu7eik5/nYoFFGiOH6L47XOOfuUeU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change the math indecator from parentheses & brackets to $, make the math formula generated by GPT & Other AI display correctly.'';
            homepage = "https://github.com/Ori-Replication/obsidian-math-indicator-changer";
        };
    }
);
    "mathematica-plot" = (
    let
        baseUrl = "https://github.com/MarcosNicolau/obsidian-mathematica-plot/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mathematica-plot";
        version = "1.0.0";
        outputHash = "sha256-gD9dUwSz3f25+c8f2aVhwReyQAz90OtBjyY0ZcTERXE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render graphs using Wolfram Mathematica code.'';
            homepage = "https://github.com/MarcosNicolau/obsidian-mathematica-plot";
        };
    }
);
    "mathlive" = (
    let
        baseUrl = "https://github.com/danzilberdan/obsidian-mathlive/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mathlive";
        version = "1.4.1";
        outputHash = "sha256-0VbBg8UmrtvDQu3fY/SDSP5m7749q3AcwMxS4zbUiFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Faster and more intuitive MathJax editing using MathLive.'';
            homepage = "https://github.com/danzilberdan/obsidian-mathlive";
        };
    }
);
    "mathlive-in-editor-mode" = (
    let
        baseUrl = "https://github.com/MizarZh/mathlive-in-editor-mode/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mathlive-in-editor-mode";
        version = "0.4.0";
        outputHash = "sha256-c8h7YNKfBfZDTRHKX4XigxGOez2WjdhaPPmgyarcWDg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''MathLive input in editor mode'';
            homepage = "https://github.com/MizarZh/mathlive-in-editor-mode";
        };
    }
);
    "mathpad" = (
    let
        baseUrl = "https://github.com/Canna71/obsidian-mathpad/releases/download/0.8.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mathpad";
        version = "0.8.14";
        outputHash = "sha256-RpUtRf4aOvL/FurOrQWuzpK4XMBjthUXz3PIHbLYPAc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Computer algebra system and calculator.'';
            homepage = "https://github.com/Canna71/obsidian-mathpad";
        };
    }
);
    "mathtype" = (
    let
        baseUrl = "https://github.com/slateblua/mathtype/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mathtype";
        version = "1.0.2";
        outputHash = "sha256-o2EtzPeCgWWCkEdcTqtnG1XHYiipwmvJ49ue8rAbTeQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get MathJax syntax suggestions from natural language.'';
            homepage = "https://github.com/slateblua/mathtype";
        };
    }
);
    "matter" = (
    let
        baseUrl = "https://github.com/getmatterapp/obsidian-matter/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "matter";
        version = "1.1.4";
        outputHash = "sha256-i8r6YAbiqDgtrDMF0NxR7cVNDwfdejhpbqnjJ1wMKZQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official Matter integration.'';
            homepage = "https://github.com/getmatterapp/obsidian-matter";
        };
    }
);
    "maximise-active-pane-obsidian" = (
    let
        baseUrl = "https://github.com/deathau/maximise-active-pane-obsidian/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "maximise-active-pane-obsidian";
        version = "0.0.3";
        outputHash = "sha256-Srm5KUcT6eWkrybUBaf2+EvntExvYsLt/jhwoW+45ek=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fill the workspace with the active pane.'';
            homepage = "https://github.com/deathau/maximise-active-pane-obsidian";
        };
    }
);
    "mblog-publish" = (
    let
        baseUrl = "https://github.com/kingwrcy/obsidian-mblog/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mblog-publish";
        version = "1.0.0";
        outputHash = "sha256-gs062rH1rbbC3jDbcuQr6NQEJDkNToY2dcl+C7o1Daw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''通过Obsidian发布文章到MBlog平台'';
            homepage = "https://github.com/kingwrcy/obsidian-mblog";
        };
    }
);
    "mcp-tools" = (
    let
        baseUrl = "https://github.com/jacksteamdev/obsidian-mcp-tools/releases/download/0.2.31";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mcp-tools";
        version = "0.2.31";
        outputHash = "sha256-Z78iKaeEKKVfTOehGgaspm7FkHZbxPF1xJBnBazmZD8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Securely connect Claude Desktop to your vault with semantic search, templates, and file management capabilities.'';
            homepage = "https://github.com/jacksteamdev/obsidian-mcp-tools";
        };
    }
);
    "md-cal-gen" = (
    let
        baseUrl = "https://github.com/zachatrocity/md-cal-gen/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "md-cal-gen";
        version = "1.0.3";
        outputHash = "sha256-s+wQVq/Lmi0rfI5gUPqkpiTckiHYnwNmpIpTtV4AGGc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An intentionally simple markdown table calendar generator'';
            homepage = "https://github.com/zachatrocity/md-cal-gen";
        };
    }
);
    "md-image-caption" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-image-caption/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "md-image-caption";
        version = "1.0.5";
        outputHash = "sha256-nT3lzhBpPY5RubWhVNuvic1a7VSXWK3Vb3mY84xR7wY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate Markdown-based image captions.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-image-caption";
        };
    }
);
    "md-merge" = (
    let
        baseUrl = "https://github.com/tosatur/obsidian-md-merge/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "md-merge";
        version = "0.0.3";
        outputHash = "sha256-JRIRnfz08p2CTEISNvCGGSLT1Cxz8Tqh4BbxS8mC9m4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Merge all markdown files from a folder into a single file.'';
            homepage = "https://github.com/tosatur/obsidian-md-merge";
        };
    }
);
    "mdfriday" = (
    let
        baseUrl = "https://github.com/mdfriday/obsidian-friday-plugin/releases/download/26.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mdfriday";
        version = "26.4.1";
        outputHash = "sha256-/ccPMtOXPVNDj5Yih6/pmDz6cQ5famr7Yvpben1E0D8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Notes to Website. Friday helps you turn Markdown documents into websites in minutes.'';
            homepage = "https://github.com/mdfriday/obsidian-friday-plugin";
        };
    }
);
    "mdx" = (
    let
        baseUrl = "https://github.com/yulei-chen/obsidian-mdx/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mdx";
        version = "0.1.0";
        outputHash = "sha256-vKSmA8l7ekPGzIaBgDmO7tptGPcw6XXaE9bE4PNcwyM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preview MDX in Obsidian, with support for Code Hike.'';
            homepage = "https://github.com/yulei-chen/obsidian-mdx";
        };
    }
);
    "mdx-as-md-obsidian" = (
    let
        baseUrl = "https://github.com/mkozhukharenko/mdx-as-md-obsidian/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mdx-as-md-obsidian";
        version = "0.0.1";
        outputHash = "sha256-nMbXxOps6IubHdHHRaG3u9JhcF7axhAOmN13Yo4dTGY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit mdx files as Markdown.'';
            homepage = "https://github.com/mkozhukharenko/mdx-as-md-obsidian";
        };
    }
);
    "media-companion" = (
    let
        baseUrl = "https://github.com/Nick-de-Bruin/obsidian-media-companion/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "media-companion";
        version = "1.2.2";
        outputHash = "sha256-HWCX/0RIS6R/rRIL4p8D3H+h2wYBPoeXFbKzbBMa2jY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates a searchable gallery and sidecar files for attachments such as images and videos. The sidecar files allow you to add notes and tags to your media files.'';
            homepage = "https://github.com/Nick-de-Bruin/obsidian-media-companion";
        };
    }
);
    "media-extended" = (
    let
        baseUrl = "https://github.com/aidenlx/media-extended/releases/download/4.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "media-extended";
        version = "4.2.0";
        outputHash = "sha256-78hbzmEixZTB7St/6T36Z/5AheROgl0YZRXWB3PlY6A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate, manage video or audio directly in your notes, with enhanced playback and timestamp support. (Closed source)'';
            homepage = "https://github.com/aidenlx/media-extended";
        };
    }
);
    "media-notes" = (
    let
        baseUrl = "https://github.com/jemstelos/obsidian-media-notes/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "media-notes";
        version = "1.3.0";
        outputHash = "sha256-d9NvYV9rYHY3PVM/5HOPFtWnnHTUoBsChnQRTNMxQb8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take notes on YouTube videos and podcasts with media controls and timestamps.'';
            homepage = "https://github.com/jemstelos/obsidian-media-notes";
        };
    }
);
    "media-slider" = (
    let
        baseUrl = "https://github.com/amatya-aditya/obsidian-media-slider/releases/download/1.3.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "media-slider";
        version = "1.3.6";
        outputHash = "sha256-/OKbegsIQhMJ0tZHSCNmp2Tf//CjjtKKLsNZaAR0Tg0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A media slider for images, gifs, audios, videos, and PDFs.'';
            homepage = "https://github.com/amatya-aditya/obsidian-media-slider";
        };
    }
);
    "media-sync" = (
    let
        baseUrl = "https://github.com/fnya/media-sync/releases/download/0.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "media-sync";
        version = "0.0.15";
        outputHash = "sha256-p8rk0ZFYe8lWL9UBydV77hSNvXJbj5kWv+wWLwwY+rk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Download images from the image URLs in notes and display the content.'';
            homepage = "https://github.com/fnya/media-sync";
        };
    }
);
    "mediaviewer" = (
    let
        baseUrl = "https://github.com/Devon22/obsidian-mediaviewer/releases/download/2.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mediaviewer";
        version = "2.0.11";
        outputHash = "sha256-uoiXu82ihERBu9Q2CLG1jV6KLCK0+QmF7+OCLZOiVzY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View and manage media files within your notes.'';
            homepage = "https://github.com/Devon22/obsidian-mediaviewer";
        };
    }
);
    "medium-importer" = (
    let
        baseUrl = "https://github.com/arumie/obsidian-medium-importer-plugin/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "medium-importer";
        version = "0.4.1";
        outputHash = "sha256-VynURZ9guOxlE67sl8zBIpTzJyBlDG4kQV2y0BI+EgE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Medium posts into your vault.'';
            homepage = "https://github.com/arumie/obsidian-medium-importer-plugin";
        };
    }
);
    "mehrmaid" = (
    let
        baseUrl = "https://github.com/huterguier/obsidian-mehrmaid/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mehrmaid";
        version = "0.0.2";
        outputHash = "sha256-Ek+1xGqLWZQdGvQGVcwXmuNeX8lOHazftIhU2D83WMM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables you to put Markdown inside of Mermaid diagrams.'';
            homepage = "https://github.com/huterguier/obsidian-mehrmaid";
        };
    }
);
    "meld-build" = (
    let
        baseUrl = "https://github.com/meld-cp/obsidian-build/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "meld-build";
        version = "0.1.9";
        outputHash = "sha256-wbxwibJ22wAoPY/ROtwsq2Ux1ujAFoT9WnQlD46R23g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write and execute (sandboxed) JavaScript to render templates, query DataView and create dynamic notes.'';
            homepage = "https://github.com/meld-cp/obsidian-build";
        };
    }
);
    "meld-calc" = (
    let
        baseUrl = "https://github.com/meld-cp/obsidian-calc/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "meld-calc";
        version = "1.0.2";
        outputHash = "sha256-8Cn5BDlGAUKLfxnaSUAtOtWhv1lHV1wl/LmF7THuI3o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Do math! Evaluate math expressions within your notes.'';
            homepage = "https://github.com/meld-cp/obsidian-calc";
        };
    }
);
    "meld-encrypt" = (
    let
        baseUrl = "https://github.com/meld-cp/obsidian-encrypt/releases/download/2.4.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "meld-encrypt";
        version = "2.4.5";
        outputHash = "sha256-zUaDRZgbHtpUWnRvb8fNQxN8nq7rhoJd1lrV1t03NfI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide secrets in your notes.'';
            homepage = "https://github.com/meld-cp/obsidian-encrypt";
        };
    }
);
    "memochron" = (
    let
        baseUrl = "https://github.com/formax68/memoChron/releases/download/1.13.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "memochron";
        version = "1.13.1";
        outputHash = "sha256-kyO1H/bpfGudPMqi+TN25F7WzfuRKXwk6aMMzA7uL/0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Calendar integration and note creation with support for public iCalendar URLs.'';
            homepage = "https://github.com/formax68/memoChron";
        };
    }
);
    "memodack" = (
    let
        baseUrl = "https://github.com/memodack/memodack/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "memodack";
        version = "1.6.1";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Your second language memory tool'';
            homepage = "https://github.com/memodack/memodack";
        };
    }
);
    "memories" = (
    let
        baseUrl = "https://github.com/DIMFLIX/obsidian-memories/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "memories";
        version = "1.4.2";
        outputHash = "sha256-MHNZITzT4XxYhWN60WKBoNceyCLw0cyyAi8BEYJMy/k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display media galleries with images, videos, and audio directly in notes.'';
            homepage = "https://github.com/DIMFLIX/obsidian-memories";
        };
    }
);
    "memorization" = (
    let
        baseUrl = "https://github.com/nwindian/Memorization-Plugin/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "memorization";
        version = "0.7.0";
        outputHash = "sha256-T85O8JMg21fWoPkrZmSJhEKVz0Zn0QjQhOsR+iADM5U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate study index notes using a spaced repetition algorithm (SM-2).'';
            homepage = "https://github.com/nwindian/Memorization-Plugin";
        };
    }
);
    "memos-ai-sync" = (
    let
        baseUrl = "https://github.com/leoleelxh/obsidian-memos-ai-sync/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "memos-ai-sync";
        version = "1.0.4";
        outputHash = "sha256-+IldyBXYoj4hu3BytZHak4744i4ymn9KDl2b7EB/VRg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Memos content powered by AI'';
            homepage = "https://github.com/leoleelxh/obsidian-memos-ai-sync";
        };
    }
);
    "memos-sync" = (
    let
        baseUrl = "https://github.com/RyoJerryYu/obsidian-memos-sync/releases/download/0.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "memos-sync";
        version = "0.5.2";
        outputHash = "sha256-H/jOT24rzs5Y59jWggC6Aic6xwYutN2mBHox8qT05wg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncing memos from a Memos server to your daily note. Fully compatible with official Daily Notes plugin, Calendar plugin and Periodic Notes plugin.'';
            homepage = "https://github.com/RyoJerryYu/obsidian-memos-sync";
        };
    }
);
    "mention-things" = (
    let
        baseUrl = "https://github.com/stracker-phil/obsidian-mention-things/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mention-things";
        version = "1.1.0";
        outputHash = "sha256-mIG8epGeJXy6ZWhi775BUuK7jZG6B9iyYmd73JT/zBY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define a list of prefixes to trigger an autocomplete suggestion to insert a link.'';
            homepage = "https://github.com/stracker-phil/obsidian-mention-things";
        };
    }
);
    "merge-notes" = (
    let
        baseUrl = "https://github.com/fnya/merge-notes/releases/download/0.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "merge-notes";
        version = "0.0.11";
        outputHash = "sha256-gOsWc5XV7xG8EQmbxVuKLDgryC7rZ8QrDE8dNESD9E8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Merge the selected notes.'';
            homepage = "https://github.com/fnya/merge-notes";
        };
    }
);
    "mermaid-helper" = (
    let
        baseUrl = "https://github.com/FrancescoDiCursi/Mermaid.js-Helper-OMH-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mermaid-helper";
        version = "1.0.0";
        outputHash = "sha256-M+DxRx0Mw6nsothFMkULcON5gFs5602r05P69BiCEoQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Additional commands for mermaid.js workflow and more.'';
            homepage = "https://github.com/FrancescoDiCursi/Mermaid.js-Helper-OMH-plugin";
        };
    }
);
    "mermaid-icons" = (
    let
        baseUrl = "https://github.com/toshs/obsidian-mermaid-icons/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mermaid-icons";
        version = "1.1.3";
        outputHash = "sha256-t/C2Zhemt7qwZrMkEzChFT1L/NwN2+ISmwzopDgfV0c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use Font Awesome and other icon sets within your Mermaid diagrams.'';
            homepage = "https://github.com/toshs/obsidian-mermaid-icons";
        };
    }
);
    "mermaid-popup" = (
    let
        baseUrl = "https://github.com/gitcpy/obsidian-diagram-popup/releases/download/0.2.69";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mermaid-popup";
        version = "0.2.69";
        outputHash = "sha256-A0+5AQINq6+P6uciHWN1jfLyjMTB6B89evD4RuWB00Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show diagrams, from Mermaid, PlantUML, Graphviz and so on, in a draggable and zoomable popup'';
            homepage = "https://github.com/gitcpy/obsidian-diagram-popup";
        };
    }
);
    "mermaid-themes" = (
    let
        baseUrl = "https://github.com/jvsteiner/mermaid-themes/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mermaid-themes";
        version = "0.1.3";
        outputHash = "sha256-FIdbTMZUj1q8Z77bSXIR+/pM2BXs9c0zN60SUxbXFmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Alternate support for mermaid.js that supports theming and customization.'';
            homepage = "https://github.com/jvsteiner/mermaid-themes";
        };
    }
);
    "mermaid-tools" = (
    let
        baseUrl = "https://github.com/dartungar/obsidian-mermaid/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mermaid-tools";
        version = "1.3.0";
        outputHash = "sha256-j/FpAqwBI4vOtGfmolwc+OZGCMioLUBDDKsUt6C3pQQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improved Mermaid.js experience: visual toolbar with common elements and more.'';
            homepage = "https://github.com/dartungar/obsidian-mermaid";
        };
    }
);
    "mesh-ai" = (
    let
        baseUrl = "https://github.com/chasebank87/mesh-ai/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mesh-ai";
        version = "1.3.1";
        outputHash = "sha256-u1LVCDaBLm+94h4wiIT4E4/2V/n3sbVVc+cDLdasjUE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables you to store AI prompts as patterns and run them seemlessly with most providers.'';
            homepage = "https://github.com/chasebank87/mesh-ai";
        };
    }
);
    "messager" = (
    let
        baseUrl = "https://github.com/xiaotianhu/obsidian-messager/releases/download/1.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "messager";
        version = "1.1.8";
        outputHash = "sha256-ZClowUz6zcu7s6tiqaaOdOirV3CVmgy3d8ihxnqw8Qs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save messages into vault which sending through WeChat/HTTP API/Email'';
            homepage = "https://github.com/xiaotianhu/obsidian-messager";
        };
    }
);
    "metadata-auto-classifier" = (
    let
        baseUrl = "https://github.com/GoBeromsu/Metadata-Auto-Classifier/releases/download/1.12.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metadata-auto-classifier";
        version = "1.12.9";
        outputHash = "sha256-YCHWPOpgAR4z+vDsY4aTktRYOyvmu9nBwBWuXgpwfrs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically classifies and applies metadata to your notes.'';
            homepage = "https://github.com/GoBeromsu/Metadata-Auto-Classifier";
        };
    }
);
    "metadata-extractor" = (
    let
        baseUrl = "https://github.com/kometenstaub/metadata-extractor/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metadata-extractor";
        version = "1.1.0";
        outputHash = "sha256-/KjQK35EEZU3AkvmYMGBiz0YuHl+b5NhSBOUZtW91YE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Metadata export (on a schedule) for integration with third-party apps like launchers or graph analysis software.'';
            homepage = "https://github.com/kometenstaub/metadata-extractor";
        };
    }
);
    "metadata-hider" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-metadata-hider/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metadata-hider";
        version = "1.0.2";
        outputHash = "sha256-/KWrsrIh7Jlryf5SJXWfiCWcLBUL7/WP6DdCnEnfGW4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide specific metadata property or if its value is empty.'';
            homepage = "https://github.com/Benature/obsidian-metadata-hider";
        };
    }
);
    "metadata-icon" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-metadata-icon/releases/download/0.0.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metadata-icon";
        version = "0.0.16";
        outputHash = "sha256-V316jBkvveGDKQR4rmMOEEjM6blESANDNFJ06Az780c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set custom icons for your properties.'';
            homepage = "https://github.com/Benature/obsidian-metadata-icon";
        };
    }
);
    "metadata-menu" = (
    let
        baseUrl = "https://github.com/mdelobelle/metadatamenu/releases/download/0.8.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metadata-menu";
        version = "0.8.12";
        outputHash = "sha256-YBPjzpQSNreZ28Qed+xEeQZWwh7yZmzOgQyvuZPY5Fw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''For data quality enthusiasts and Dataview users: access and manage the metadata of your notes.'';
            homepage = "https://github.com/mdelobelle/metadatamenu";
        };
    }
);
    "metaedit" = (
    let
        baseUrl = "https://github.com/chhoumann/MetaEdit/releases/download/1.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metaedit";
        version = "1.8.4";
        outputHash = "sha256-fNVzrQWzqVU0c3gX0Wokpyb8hqkndn5T1HvkiPkzIjg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your metadata.'';
            homepage = "https://github.com/chhoumann/MetaEdit";
        };
    }
);
    "metafolders" = (
    let
        baseUrl = "https://github.com/makary-s/obsidian-metafolders/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metafolders";
        version = "1.1.4";
        outputHash = "sha256-H64IAO64yH3rL/C1m0wBl0AFy8OPjr5J+bjV9aUvPJI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Multidimensional note navigation.'';
            homepage = "https://github.com/makary-s/obsidian-metafolders";
        };
    }
);
    "metal-archives" = (
    let
        baseUrl = "https://github.com/vincenzocaputo/obsidian-metal-archives-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "metal-archives";
        version = "1.2.0";
        outputHash = "sha256-uL6I4EDq6UQoTIp+DD5lxEQepO6HHrpCMxSzzwbTRkQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes about metal bands and album from Metal Archives.'';
            homepage = "https://github.com/vincenzocaputo/obsidian-metal-archives-plugin";
        };
    }
);
    "micro-templates" = (
    let
        baseUrl = "https://github.com/epszaw/obsidian-micro-templates/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "micro-templates";
        version = "1.0.3";
        outputHash = "sha256-BHwk8wsM1b5oXyLyKAV+Eex87gBklgBLoHbIKU6gdpw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Flexible embedded micro templates powered by Javascript functions.'';
            homepage = "https://github.com/epszaw/obsidian-micro-templates";
        };
    }
);
    "microblog-publish-plugin" = (
    let
        baseUrl = "https://github.com/otaviocc/obsidian-microblog/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "microblog-publish-plugin";
        version = "3.0.0";
        outputHash = "sha256-IklNO2pcFR5KfJVFDJfx6Obc/3BlNiSGqAq1IhqE/WI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish notes to Micro.blog.'';
            homepage = "https://github.com/otaviocc/obsidian-microblog";
        };
    }
);
    "midi-logger" = (
    let
        baseUrl = "https://github.com/maybe-hello-world/midi-logger/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "midi-logger";
        version = "0.1.2";
        outputHash = "sha256-dM4eRoqV/QUNvTG5ZYE+1589XY53bGoyY9BSg2gsAhY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert parsed musical notes from MIDI input devices.'';
            homepage = "https://github.com/maybe-hello-world/midi-logger";
        };
    }
);
    "mindmap" = (
    let
        baseUrl = "https://github.com/OneCalmCloud/obsidian-mindmap/releases/download/0.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mindmap";
        version = "0.3.4";
        outputHash = "sha256-v+BwgqPmK9UxS5RBRnLZE5890G3FNxTcPGlFA80ptGI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes with Mindmaps.'';
            homepage = "https://github.com/OneCalmCloud/obsidian-mindmap";
        };
    }
);
    "mini-rag" = (
    let
        baseUrl = "https://github.com/jjwheatley/mini-rag/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mini-rag";
        version = "1.0.0";
        outputHash = "sha256-jA02YkL7pfgfGbNOyEf5FLQcISPdeYXGXoZ8N7Ip1Ks=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Leverage Retrieval Augmented Generation (RAG) for your notes using a locally running LLM or AI.'';
            homepage = "https://github.com/jjwheatley/mini-rag";
        };
    }
);
    "mini-toolbar" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Mini-Toolbar/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mini-toolbar";
        version = "0.0.3";
        outputHash = "sha256-lwriFE3L+sfJMMXS+UcrPfTrcZalHzf2gpCClEv+ksA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mini context toolbar in editor.'';
            homepage = "https://github.com/Quorafind/Obsidian-Mini-Toolbar";
        };
    }
);
    "mini-vimrc" = (
    let
        baseUrl = "https://github.com/cabra-arretado/mini-vimrc-obsidian/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mini-vimrc";
        version = "1.1.0";
        outputHash = "sha256-1lO2JxTsbj5EjCCRCwsIlOrlsKCZa7nJplj8OCDvrog=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add Vim keybindings via .vimrc'';
            homepage = "https://github.com/cabra-arretado/mini-vimrc-obsidian";
        };
    }
);
    "minidoro" = (
    let
        baseUrl = "https://github.com/ShaktiSampadSwain/minidoro/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minidoro";
        version = "1.0.11";
        outputHash = "sha256-JIqlPXzORnr1IPCrHojYcDdpfI6IaIUQj/efyk1cPCA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A minimal Pomodoro timer to help you focus.'';
            homepage = "https://github.com/ShaktiSampadSwain/minidoro";
        };
    }
);
    "minimal-quiz" = (
    let
        baseUrl = "https://github.com/Lutu-gl/Obsidian-Minimal-Quiz/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minimal-quiz";
        version = "1.0.0";
        outputHash = "sha256-Ui94oZD6TkzOSIor8uDBDsJ7nNV0N5808kPl0AsskWw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Start a quick Markdown-based quiz on your current note. Simple, straightforward, and fully integrated — similar to flashcards in Anki or Quizlet!'';
            homepage = "https://github.com/Lutu-gl/Obsidian-Minimal-Quiz";
        };
    }
);
    "minimap" = (
    let
        baseUrl = "https://github.com/YairSegel/ObsidianMinimap/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minimap";
        version = "1.2.0";
        outputHash = "sha256-b5EI0SatBMPIs0qOk3yH8gy/1BB8+45/cdRsu3CtHGc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a minimap to your Markdown notes.'';
            homepage = "https://github.com/YairSegel/ObsidianMinimap";
        };
    }
);
    "minimize-on-close" = (
    let
        baseUrl = "https://github.com/alberti42/obsidian-minimize-on-close/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minimize-on-close";
        version = "1.3.3";
        outputHash = "sha256-kfZohk8pw8kynQZ+V0ijj7pCjl8daR4/N0uMIDJEwVg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Minimizes the app window to an icon after closing the last open pane'';
            homepage = "https://github.com/alberti42/obsidian-minimize-on-close";
        };
    }
);
    "minio-uploader" = (
    let
        baseUrl = "https://github.com/seebin/obsidian-minio-uploader-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minio-uploader";
        version = "1.0.2";
        outputHash = "sha256-uUpnY9V/F+0coLlBcL/8hnGMbsuRL4V8wCDb0flTE28=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images, videos, audio, PDFs, and other files to Minio OSS.'';
            homepage = "https://github.com/seebin/obsidian-minio-uploader-plugin";
        };
    }
);
    "minitabs" = (
    let
        baseUrl = "https://github.com/ssjy1919/Obsidian-minitabs/releases/download/0.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minitabs";
        version = "0.0.12";
        outputHash = "sha256-Bgcbk3tvElwgq8bJGNX841qRcuZHcU0pRqz8hUKEazQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize a set of nested tabs through code blocks.'';
            homepage = "https://github.com/ssjy1919/Obsidian-minitabs";
        };
    }
);
    "minote-sync" = (
    let
        baseUrl = "https://github.com/emac/obsidian-minote-plugin/releases/download/0.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "minote-sync";
        version = "0.1.8";
        outputHash = "sha256-WhdWHbM09sIF6PoGVdQb9Oel9ULiNrjsW8ZzcdDjLak=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Minote(小米笔记) into your vault.'';
            homepage = "https://github.com/emac/obsidian-minote-plugin";
        };
    }
);
    "missing-link-file-creator" = (
    let
        baseUrl = "https://github.com/Lemon695/obsidian-missing-link-file-creator/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "missing-link-file-creator";
        version = "1.0.12";
        outputHash = "sha256-kZdWptnwvtU/7geywFNdm8jukr/2KUBtWn3X89w3q5s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates missing linked files and detects missing wiki links.'';
            homepage = "https://github.com/Lemon695/obsidian-missing-link-file-creator";
        };
    }
);
    "misskey-connector" = (
    let
        baseUrl = "https://github.com/minimarimo3/Obsidian-plugin-for-Misskey/releases/download/2.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "misskey-connector";
        version = "2.2.1";
        outputHash = "sha256-LNpLKN672VZAfCkaasEsy03OR40ZD8kaCePUl+KF7A0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables posting and embedding Misskey notes.'';
            homepage = "https://github.com/minimarimo3/Obsidian-plugin-for-Misskey";
        };
    }
);
    "mixa" = (
    let
        baseUrl = "https://github.com/mixasite/obsidian-mixa/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mixa";
        version = "1.0.2";
        outputHash = "sha256-iGI3a29da/cxmYmXc7g0zXHTkMOcPFJtQJauSW6l/ls=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes and blog posts with Mixa.'';
            homepage = "https://github.com/mixasite/obsidian-mixa";
        };
    }
);
    "mlir-syntax-highlight" = (
    let
        baseUrl = "https://github.com/Lewuathe/obsidian-mlir-syntax-highlight/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mlir-syntax-highlight";
        version = "0.0.7";
        outputHash = "sha256-JV101xBW4lVl4vbJ4KGU31uWWBx6m/q/i5lNxOO0oVo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show syntax highlighing for MLIR in code blocks the editor.'';
            homepage = "https://github.com/Lewuathe/obsidian-mlir-syntax-highlight";
        };
    }
);
    "mobile-sidebar-notes" = (
    let
        baseUrl = "https://github.com/ckep1/obsidian-mobile-sidebar-notes/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mobile-sidebar-notes";
        version = "1.2.0";
        outputHash = "sha256-N/O/3oLU5ZSuLhymOT1kmZf1X8+OMFlpVsJT5uKM618=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open notes & new tabs in the sidebar in the mobile app.'';
            homepage = "https://github.com/ckep1/obsidian-mobile-sidebar-notes";
        };
    }
);
    "moc-link-helper" = (
    let
        baseUrl = "https://github.com/BogdanCodreanu/obsidian-moc-link-helper/releases/download/1.1.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "moc-link-helper";
        version = "1.1.12";
        outputHash = "sha256-vUPlwqf8oRJwVZX+/lxKrmuC67/ZFcOfU0Er+m9v0Wk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps with some MOC janitor-linking tasks.'';
            homepage = "https://github.com/BogdanCodreanu/obsidian-moc-link-helper";
        };
    }
);
    "mochi-cards-exporter" = (
    let
        baseUrl = "https://github.com/kalibetre/mochi-cards-exporter/releases/download/0.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mochi-cards-exporter";
        version = "0.2.3";
        outputHash = "sha256-7HvgxT0E1So630f7vqZgfqWto4GxEaQFfMZtjeSWav0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export Markdown notes to Mochi cards.'';
            homepage = "https://github.com/kalibetre/mochi-cards-exporter";
        };
    }
);
    "mochi-cards-pro" = (
    let
        baseUrl = "https://github.com/xHayden/obsidian-mochi-cards-pro/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mochi-cards-pro";
        version = "0.0.5";
        outputHash = "sha256-UpC1EmC5EA9Gt9ivkNL4cIYDjgkNWXJS/6fKtMhieXo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create flashcards on Mochi.cards using the API provided by Mochi"s Pro subscription.'';
            homepage = "https://github.com/xHayden/obsidian-mochi-cards-pro";
        };
    }
);
    "modal-opener" = (
    let
        baseUrl = "https://github.com/likemuuxi/obsidian-modal-opener/releases/download/1.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "modal-opener";
        version = "1.2.9";
        outputHash = "sha256-AMUS/ds/cVC52O92PX7YzHIy7b1DQ1YCOipfQrfl4ic=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open files and links in modal windows, or create and edit compatible files in modal windows.'';
            homepage = "https://github.com/likemuuxi/obsidian-modal-opener";
        };
    }
);
    "modalforms" = (
    let
        baseUrl = "https://github.com/danielo515/obsidian-modal-form/releases/download/1.64.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "modalforms";
        version = "1.64.4";
        outputHash = "sha256-MRwcIsbfLe8tnvANMWMMDU1/7LW9sqd2U6e6uH2zLJA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define forms for capturing data that you will be able to open from anywhere you can run JavaScript.'';
            homepage = "https://github.com/danielo515/obsidian-modal-form";
        };
    }
);
    "mode-manager" = (
    let
        baseUrl = "https://github.com/dk949/obsidian-mode-manager/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mode-manager";
        version = "0.3.1";
        outputHash = "sha256-AwmDJd85fyeugcS0J4EtsgBlL9vG7jRAEN/RSXo7jSg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provide an easier way to manipulate reading/editing and preview/source mode'';
            homepage = "https://github.com/dk949/obsidian-mode-manager";
        };
    }
);
    "model-viewer" = (
    let
        baseUrl = "https://github.com/janispritzkau/obsidian-model-viewer/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "model-viewer";
        version = "1.0.5";
        outputHash = "sha256-7nvj1B6XbOfeiMPNbRvNKQYD2iHbL5g9evE8V/qE4bo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View and embed interactive 3D models directly in your vault, powered by Google"s <model-viewer> component. Supports the glTF and GLB file formats.'';
            homepage = "https://github.com/janispritzkau/obsidian-model-viewer";
        };
    }
);
    "modules" = (
    let
        baseUrl = "https://github.com/polyipseity/obsidian-modules/releases/download/2.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "modules";
        version = "2.5.0";
        outputHash = "sha256-cgYkT0+ZFkvhhc/n+obC1pmjUxOMbfiBnVIfTRoTIOI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Load JavaScript and related languages like TypeScript modules from the vault and the Internet.'';
            homepage = "https://github.com/polyipseity/obsidian-modules";
        };
    }
);
    "monokakido-copilot" = (
    let
        baseUrl = "https://github.com/NoHeartPen/obsidian-monokakido-copilot-plugin/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "monokakido-copilot";
        version = "0.0.3";
        outputHash = "sha256-f5Vzn7/gamnAZwKjndytqR8KSU38MGWuhWVXrabllg8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''During editing, simply double-press the option key to search with the Dictionaries by Monokakido.'';
            homepage = "https://github.com/NoHeartPen/obsidian-monokakido-copilot-plugin";
        };
    }
);
    "mononote" = (
    let
        baseUrl = "https://github.com/czottmann/obsidian-mononote/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mononote";
        version = "1.2.2";
        outputHash = "sha256-5zTy6fXI/YSmLWGMlgN22d4brU0eGtjbEHMz+9iYygA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Ensure each note occupies only one tab. If a note is already open, its existing tab will be focussed instead of opening the same file in the current tab.'';
            homepage = "https://github.com/czottmann/obsidian-mononote";
        };
    }
);
    "mood-tracker" = (
    let
        baseUrl = "https://github.com/dartungar/obsidian-mood-tracker/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mood-tracker";
        version = "1.7.0";
        outputHash = "sha256-I6LXXmSiNF5RtSvsV4H05Sk13A+4ZaKb9A8UcSkFzTc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track your moods & emotions easily. Visualize tracked history and browse the past entries.'';
            homepage = "https://github.com/dartungar/obsidian-mood-tracker";
        };
    }
);
    "moon-server-publisher" = (
    let
        baseUrl = "https://github.com/Dzoukr/MoonServerObsidianPlugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "moon-server-publisher";
        version = "1.0.0";
        outputHash = "sha256-PlAaqfu26jUGZfxk1VJwlTAI+ppFcg828XEQstw2C5s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes directly to Moon server instance.'';
            homepage = "https://github.com/Dzoukr/MoonServerObsidianPlugin";
        };
    }
);
    "moredraw" = (
    let
        baseUrl = "https://github.com/webceoboy/moredraw-obsidian/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "moredraw";
        version = "1.0.4";
        outputHash = "sha256-HELYpXVAw9iP2KNFVZ/43opSaDIyY1Acn6aPrGV0NJA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A infinite canvas whiteboard with ai to draw flowchart ,mind map and other diagram.'';
            homepage = "https://github.com/webceoboy/moredraw-obsidian";
        };
    }
);
    "morgen-tasks" = (
    let
        baseUrl = "https://github.com/morgen-so/morgen-obsidian/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "morgen-tasks";
        version = "1.0.7";
        outputHash = "sha256-wvOZlLK9zQH4J1fmwBJEl9R55JSV9simsldsBSynDWg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Plan, time block, and track tasks from your vault in any calendar using Morgen.'';
            homepage = "https://github.com/morgen-so/morgen-obsidian";
        };
    }
);
    "most-used" = (
    let
        baseUrl = "https://github.com/levi-ivel/MostUsed/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "most-used";
        version = "1.0.3";
        outputHash = "sha256-LAiplDA41oeWamdRwwflhDRaHW71T9yeS5WaNqu4Dfo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates a list from your 100 most commonly used words'';
            homepage = "https://github.com/levi-ivel/MostUsed";
        };
    }
);
    "moulinette" = (
    let
        baseUrl = "https://github.com/SvenWerlen/moulinette-obsidian-plugin/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "moulinette";
        version = "0.6.0";
        outputHash = "sha256-p0BF6j3z3p4D5EJg9H7TvUrJ5+BfyAtq+XdY+hMHONw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search, browse and download TTRPG (tabletop role-playing game) content from Moulinette Cloud.'';
            homepage = "https://github.com/SvenWerlen/moulinette-obsidian-plugin";
        };
    }
);
    "mouse-navigation" = (
    let
        baseUrl = "https://github.com/Hobeom/obsidian-mouse-navigation/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mouse-navigation";
        version = "1.1.0";
        outputHash = "sha256-6LipnqCQ2APaNsOoudTPwYmapcJ8IijgZTRWQOES6Vs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables smooth navigation using mouse gestures for scrolling and switching pages.'';
            homepage = "https://github.com/Hobeom/obsidian-mouse-navigation";
        };
    }
);
    "mousewheel-image-zoom" = (
    let
        baseUrl = "https://github.com/nicojeske/mousewheel-image-zoom/releases/download/1.0.24";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mousewheel-image-zoom";
        version = "1.0.24";
        outputHash = "sha256-sVMb30gcW0304SCX1awj2ehd2hCDxHxFeRwDqBbpFHg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Increase or decrease the size of an image by scrolling.'';
            homepage = "https://github.com/nicojeske/mousewheel-image-zoom";
        };
    }
);
    "move-cursor-on-startup" = (
    let
        baseUrl = "https://github.com/Treadder/move-cursor-on-startup/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "move-cursor-on-startup";
        version = "1.0.0";
        outputHash = "sha256-Zt5p0iY97ZoIUHdARM8LpMcXMmW1j47mOxOMSpaxSNk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move cursor right then left briefly on startup --> first opened note. Makes DataView expressions "activate" automatically instead of waiting for user interaction.'';
            homepage = "https://github.com/Treadder/move-cursor-on-startup";
        };
    }
);
    "move-files" = (
    let
        baseUrl = "https://github.com/nitishkhurana/obsidian-move-files-plugin/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "move-files";
        version = "1.3.0";
        outputHash = "sha256-llcUa1qA/Mi6u+Jk3sYaHCD+JTA2kSGqzHmB4656xws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Moves all the files linked to a open md file to a folder and updates the link in md file.'';
            homepage = "https://github.com/nitishkhurana/obsidian-move-files-plugin";
        };
    }
);
    "movie-obsidian" = (
    let
        baseUrl = "https://github.com/onuraycicek/obsidian-movie/releases/download/0.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "movie-obsidian";
        version = "0.2.5";
        outputHash = "sha256-+gdbrxTuQ7irCnuNLgy8yzsDklimhfdPBbSlZvnJR8Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search movies info and trailers.'';
            homepage = "https://github.com/onuraycicek/obsidian-movie";
        };
    }
);
    "movie-search" = (
    let
        baseUrl = "https://github.com/Gubchik123/obsidian-movie-search-plugin/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "movie-search";
        version = "1.3.1";
        outputHash = "sha256-1M27cqor/CPAZukKS/D1tYALB5d7+wQUQjKj/cX0a3w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps you find movies and create notes.'';
            homepage = "https://github.com/Gubchik123/obsidian-movie-search-plugin";
        };
    }
);
    "moviegrabber" = (
    let
        baseUrl = "https://github.com/Superschnizel/Obsidian-Moviegrabber/releases/download/1.1.23";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "moviegrabber";
        version = "1.1.23";
        outputHash = "sha256-H1zTQbCEIT7xMplsAb54201sNG8Pby14wAwx+We9M7g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Grab movie data from public APIs and transform it into notes that can be used with dataview and properties.'';
            homepage = "https://github.com/Superschnizel/Obsidian-Moviegrabber";
        };
    }
);
    "mp-preview" = (
    let
        baseUrl = "https://github.com/Yeban8090/mp-preview/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mp-preview";
        version = "1.0.6";
        outputHash = "sha256-qm0fEEY8W715MRL9g+k2A1k3DZeRBsDsWtwbEVLAgic=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preview and convert Markdown files to MP format'';
            homepage = "https://github.com/Yeban8090/mp-preview";
        };
    }
);
    "mpv-links" = (
    let
        baseUrl = "https://github.com/patsh90/mpv-obsidian-plugin/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mpv-links";
        version = "1.0.10";
        outputHash = "sha256-7D+2rA1tPD8d7maescoH5cjPt137gGgGB2mYgyBKgXM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add mpv links with timestamps'';
            homepage = "https://github.com/patsh90/mpv-obsidian-plugin";
        };
    }
);
    "mrdoc" = (
    let
        baseUrl = "https://github.com/zmister2016/obsidian-mrdoc/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mrdoc";
        version = "1.1.3";
        outputHash = "sha256-T8a3OEVmOn6iDd709MX0atal7ldjau7GiNX1sCPKx1c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize documents between your vault and MrDoc.'';
            homepage = "https://github.com/zmister2016/obsidian-mrdoc";
        };
    }
);
    "mrj-crosslink-between-notes" = (
    let
        baseUrl = "https://github.com/mrjackphil/obsidian-crosslink-between-notes/releases/download/0.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mrj-crosslink-between-notes";
        version = "0.0.9";
        outputHash = "sha256-Ry/phSZgHZ+U6roDXg7oM9LriP3I4x++/HbCSZIx5B8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A command to add a link to the current note at the bottom of selected notes.'';
            homepage = "https://github.com/mrjackphil/obsidian-crosslink-between-notes";
        };
    }
);
    "mrj-jump-to-link" = (
    let
        baseUrl = "https://github.com/mrjackphil/obsidian-jump-to-link/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mrj-jump-to-link";
        version = "2.0.1";
        outputHash = "sha256-uHT5URY7vjrMN7BKobTd+e5ar6tb5tJh9lIJwHnmt7A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly navigate between links, or jump to any word on the page using hotkeys.'';
            homepage = "https://github.com/mrjackphil/obsidian-jump-to-link";
        };
    }
);
    "mrj-text-expand" = (
    let
        baseUrl = "https://github.com/mrjackphil/obsidian-text-expand/releases/download/0.11.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mrj-text-expand";
        version = "0.11.5";
        outputHash = "sha256-K3OYOTaj06c8v7/ORUIdqfwRr25AW6yq2Hdoi8k6Lpk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and paste/transclude links to found files.'';
            homepage = "https://github.com/mrjackphil/obsidian-text-expand";
        };
    }
);
    "msg-handler" = (
    let
        baseUrl = "https://github.com/ozntel/obsidian-msg-handler/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "msg-handler";
        version = "0.0.6";
        outputHash = "sha256-Bt4pMnu4dflqkTDQCeUpyPIHeUKRPJpUwPr721S/0yo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily display and search MSG files from Outlook in your vault.'';
            homepage = "https://github.com/ozntel/obsidian-msg-handler";
        };
    }
);
    "mtg-card-links" = (
    let
        baseUrl = "https://github.com/aedans/mtg-card-links/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mtg-card-links";
        version = "1.2.0";
        outputHash = "sha256-4lKNWOjFhKP/tDhu9EqvBp/6uEqVGvh1Z8k68sCLMXk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link to Magic the Gathering cards by enclosing the card name in square brackets.'';
            homepage = "https://github.com/aedans/mtg-card-links";
        };
    }
);
    "multi-column-markdown" = (
    let
        baseUrl = "https://github.com/ckRobinson/multi-column-markdown/releases/download/0.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multi-column-markdown";
        version = "0.9.1";
        outputHash = "sha256-1b5SAM7ez2mLhxFJevMExYhUbrSFGAmwq8/typy5C+k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Markdown documents with multiple columns of content viewable in reading mode.'';
            homepage = "https://github.com/ckRobinson/multi-column-markdown";
        };
    }
);
    "multi-line-formatting" = (
    let
        baseUrl = "https://github.com/nmady/obsidian-multi-line-formatting/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multi-line-formatting";
        version = "0.4.1";
        outputHash = "sha256-VQhWNcc18np4HZ3SmZiU3ut5J/6Ez9c7DjkdFwzX0GA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Apply formatting to selected text, dealing with the paragraph breaks.'';
            homepage = "https://github.com/nmady/obsidian-multi-line-formatting";
        };
    }
);
    "multi-properties" = (
    let
        baseUrl = "https://github.com/technohiker/obsidian-multi-properties/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multi-properties";
        version = "1.4.0";
        outputHash = "sha256-iJn2Qj5ctHAv6HFaK3l/jhuLr58/SNa040Mpi3AGDeA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add properties to multiple notes at once. Either right-click a folder or select multiple notes and right-click the selection.'';
            homepage = "https://github.com/technohiker/obsidian-multi-properties";
        };
    }
);
    "multi-state-checkbox-switcher" = (
    let
        baseUrl = "https://github.com/KubaMiszcz/MultiStateCheckBoxSwitcher/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multi-state-checkbox-switcher";
        version = "1.0.4";
        outputHash = "sha256-CT3liVEcQeOLdcSmloaFAnc6v66gldBY1vOZ0JLV8Rw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Click to handle multistate checkboxes.'';
            homepage = "https://github.com/KubaMiszcz/MultiStateCheckBoxSwitcher";
        };
    }
);
    "multi-tag" = (
    let
        baseUrl = "https://github.com/technohiker/obsidian-multi-tag/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multi-tag";
        version = "0.10.0";
        outputHash = "sha256-ls6Us1nys+wgrBVm0yoLzvE1vQaeFqDs+wb5As/PNNA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add tags to multiple notes at once. Either right-click a folder, or select multiple notes and right-click the selection.'';
            homepage = "https://github.com/technohiker/obsidian-multi-tag";
        };
    }
);
    "multilingual" = (
    let
        baseUrl = "https://github.com/leolazou/obsidian-multilingual/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multilingual";
        version = "1.0.0";
        outputHash = "sha256-0N6XODW8DPZHqKGMZlPms8bMK4CCMv8XBWSlpWPOF8s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simplify linking notes across multiple languages by automatically adding translations of note names into aliases. Designed for multilingual users.'';
            homepage = "https://github.com/leolazou/obsidian-multilingual";
        };
    }
);
    "multiplatform-highlights-import" = (
    let
        baseUrl = "https://github.com/wwwkieran/obsidian-multiplatform-highlights-import/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multiplatform-highlights-import";
        version = "0.1.2";
        outputHash = "sha256-czV5fXxcGGWm02UgTNg2dBeq+5HsUNBuhFVHHBXOSrk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import and consolidate highlights from different reading sources. Supports reconciling books across reading sources.'';
            homepage = "https://github.com/wwwkieran/obsidian-multiplatform-highlights-import";
        };
    }
);
    "multiple-daily-notes" = (
    let
        baseUrl = "https://github.com/vaibzzz123/multiple-daily-notes/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multiple-daily-notes";
        version = "1.0.2";
        outputHash = "sha256-7+br1i+xFeLhhdBu6HvANtvv5cmPk9+Jlm0IyRqV89U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create multiple daily notes in one vault, with additional configuration options including time offsets for creating notes past midnight, choosing the ribbon icon to use for the note, and more.'';
            homepage = "https://github.com/vaibzzz123/multiple-daily-notes";
        };
    }
);
    "multiple-notes-outline" = (
    let
        baseUrl = "https://github.com/iiz00/obsidian-multiple-notes-outline/releases/download/0.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "multiple-notes-outline";
        version = "0.6.2";
        outputHash = "sha256-1zQL+0VDG0vNA+AX0OgzY4aXOsLN4XsfxVVgfzdRsRA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add custom views which show outlines of multiple notes with headings, links, tags and list items.'';
            homepage = "https://github.com/iiz00/obsidian-multiple-notes-outline";
        };
    }
);
    "music-code-blocks" = (
    let
        baseUrl = "https://github.com/abcjs-music/obsidian-plugin-abcjs/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "music-code-blocks";
        version = "1.6.2";
        outputHash = "sha256-oT/HbzfbTpcdPI+pCc09nRR6nCvTUZHPULlGCudk91o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render music sheets directly from code blocks using ABC music notation via abcjs.'';
            homepage = "https://github.com/abcjs-music/obsidian-plugin-abcjs";
        };
    }
);
    "musical-text-highlighter" = (
    let
        baseUrl = "https://github.com/tynanpurdy/musical-text/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "musical-text-highlighter";
        version = "2.0.0";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight sentences with color based on their length as you write.'';
            homepage = "https://github.com/tynanpurdy/musical-text";
        };
    }
);
    "mxmind" = (
    let
        baseUrl = "https://github.com/webceoboy/mxmind-obsidian/releases/download/1.0.20";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mxmind";
        version = "1.0.20";
        outputHash = "sha256-bfM0FTNjojB/9+1B5hL0R//6Gvl6Vu9BYNTrrjaihAM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert Markdown files to a mind map,mind map editor.'';
            homepage = "https://github.com/webceoboy/mxmind-obsidian";
        };
    }
);
    "my-svgs" = (
    let
        baseUrl = "https://github.com/ce-omarbadawy/obsidian-my-svgs/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "my-svgs";
        version = "1.0.1";
        outputHash = "sha256-HsRGPVZRxbCDu03usGXWwPAC98RFF9sOkyizFaEzXnw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Registers your own SVGs into the global icon library for use with other plugins or however you want.'';
            homepage = "https://github.com/ce-omarbadawy/obsidian-my-svgs";
        };
    }
);
    "my-thesaurus" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-my-thesaurus/releases/download/0.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "my-thesaurus";
        version = "0.0.10";
        outputHash = "sha256-OqqYfJWtyo/4U1FNW5DsTwjGWi6nGR5JHc26AVlKEAY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automagically adds tags based on a simple csv file or a Markdown table.'';
            homepage = "https://github.com/Mara-Li/obsidian-my-thesaurus";
        };
    }
);
    "my-typewriter-line" = (
    let
        baseUrl = "https://github.com/dmo-code/MyTypewriterLine/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "my-typewriter-line";
        version = "1.0.3";
        outputHash = "sha256-TrVslgdb6XllcanMll7eniQ3rEKDlNJaz9+QWsHOw7E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep the active line in focus with customizable top and bottom scroll padding for typewriter-like editing.'';
            homepage = "https://github.com/dmo-code/MyTypewriterLine";
        };
    }
);
    "my-anime-list-text-exporter" = (
    let
        baseUrl = "https://github.com/Xmoncoco/my_anime_list_text_exporter/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "my-anime-list-text-exporter";
        version = "1.4.1";
        outputHash = "sha256-/NnJNsJJUkdGja3un5b+nZjrZysKbX5G1jy4Vit4jgE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''add anime data for your notes.'';
            homepage = "https://github.com/Xmoncoco/my_anime_list_text_exporter";
        };
    }
);
    "mysnippets-plugin" = (
    let
        baseUrl = "https://github.com/chetachiezikeuzor/MySnippets-Plugin/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "mysnippets-plugin";
        version = "1.2.3";
        outputHash = "sha256-pt5CcVIGjK+FuHarnAv5b3pQ9FDlPrJ6ZStbfHohuvg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A status bar menu allowing you to quickly toggle snippets on and off.'';
            homepage = "https://github.com/chetachiezikeuzor/MySnippets-Plugin";
        };
    }
);
    "nai4obsidian" = (
    let
        baseUrl = "https://github.com/SalokinGreen/NAI4Obsidian/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nai4obsidian";
        version = "1.3.0";
        outputHash = "sha256-VpfbV0u7GO1AunMYu92DrfkUJqU56Ad3p/lcmGzk7TY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate text with NovelAI"s models.'';
            homepage = "https://github.com/SalokinGreen/NAI4Obsidian";
        };
    }
);
    "nav-link-header" = (
    let
        baseUrl = "https://github.com/ahts4962/nav-link-header/releases/download/2.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nav-link-header";
        version = "2.8.0";
        outputHash = "sha256-9QN+vM6Tl2KsTb0mwUsC4iDPQ+51Ehf/1VmoYQtClLI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display navigation links at the top of the notes.'';
            homepage = "https://github.com/ahts4962/nav-link-header";
        };
    }
);
    "nav-weight" = (
    let
        baseUrl = "https://github.com/shu307/obsidian-nav-weight/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nav-weight";
        version = "1.1.3";
        outputHash = "sha256-UklJKoCV/kqpi4LVVfr2PVXolwqAAej4HuTdSHALR5Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sort your navigation items by markdown frontmatter.'';
            homepage = "https://github.com/shu307/obsidian-nav-weight";
        };
    }
);
    "negative-heading" = (
    let
        baseUrl = "https://github.com/cyne-wulf/obsidian-negative-heading/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "negative-heading";
        version = "1.0.0";
        outputHash = "sha256-eQkLlEpfCBS7Z64pAFXNFtQs7x6wzAl5Jo4W+qemnQs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Discord-style "-#" lines as compact headings in reading view and the editor.'';
            homepage = "https://github.com/cyne-wulf/obsidian-negative-heading";
        };
    }
);
    "neighbouring-files" = (
    let
        baseUrl = "https://github.com/FabianUntermoser/obsidian-neighbouring-files-plugin/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "neighbouring-files";
        version = "1.1.3";
        outputHash = "sha256-C2kj4x+7Mkmk3P/wlrm3rW8RgtqlH3Suss3kRehRDRM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Navigate to the next and previous file in the current directory.'';
            homepage = "https://github.com/FabianUntermoser/obsidian-neighbouring-files-plugin";
        };
    }
);
    "neo4j-graph-view" = (
    let
        baseUrl = "https://github.com/HEmile/obsidian-neo4j-graph-view/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "neo4j-graph-view";
        version = "0.2.6";
        outputHash = "sha256-4N/XXPnT33YydprcZVE/HviGQAa9sQmrXsMF5sCeJyQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Advanced graph visualization and querying using Neo4j.'';
            homepage = "https://github.com/HEmile/obsidian-neo4j-graph-view";
        };
    }
);
    "nerdfont-icon-picker" = (
    let
        baseUrl = "https://github.com/xavwe/obsidian-nerdfont-icon-picker/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nerdfont-icon-picker";
        version = "1.0.2";
        outputHash = "sha256-/2Bl5y7aO/8pTueqxBpBDamRCGnH3hb8Y56oOwPgbC0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and insert nerdfont icons.'';
            homepage = "https://github.com/xavwe/obsidian-nerdfont-icon-picker";
        };
    }
);
    "nested-daily-todos" = (
    let
        baseUrl = "https://github.com/thomasbrezinski/obsidian-nested-daily-todos/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nested-daily-todos";
        version = "1.2.2";
        outputHash = "sha256-KbIWMg9eI18lyI7bbq5n7HrMFubcKohYnaRyU6x9n2o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Carry over incomplete todos from Daily Notes grouped by headers, with support for nesting and flexible todo states.'';
            homepage = "https://github.com/thomasbrezinski/obsidian-nested-daily-todos";
        };
    }
);
    "net-clip" = (
    let
        baseUrl = "https://github.com/hariiy-sys/Obsidian-NetClip/releases/download/1.4.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "net-clip";
        version = "1.4.5";
        outputHash = "sha256-zBc1AFE7bApL4ookACqUC5dp9hgu2KI2pNR1TQWCIUI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Clip, save, search, and browse web pages'';
            homepage = "https://github.com/hariiy-sys/Obsidian-NetClip";
        };
    }
);
    "netwik" = (
    let
        baseUrl = "https://github.com/fivol/netwik-obsidian/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "netwik";
        version = "1.0.3";
        outputHash = "sha256-/1QhMkyrE0RuJR2k33lMneHjbY91xq7VUFS7qCAgyBg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Union Vault. Access a global network of notes. Anyone can create, view or edit notes. All changes will be synchronized between all participants.'';
            homepage = "https://github.com/fivol/netwik-obsidian";
        };
    }
);
    "neurovox" = (
    let
        baseUrl = "https://github.com/Synaptic-Labs-AI/NeuroVox/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "neurovox";
        version = "1.0.4";
        outputHash = "sha256-dtQ/6pmwwm3ebr3RrtBRfKKbLYtoNtV6pwRcItuWfrw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhances your note-taking with voice transcription and AI capabilities.'';
            homepage = "https://github.com/Synaptic-Labs-AI/NeuroVox";
        };
    }
);
    "new-3d-graph" = (
    let
        baseUrl = "https://github.com/Apoo711/obsidian-3d-graph/releases/download/2.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "new-3d-graph";
        version = "2.4.1";
        outputHash = "sha256-0CD26YnSBQGvLXzIfYaJIsC7OGPHl0Mxxypcqz3D5n8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize your Vault in 3D with a powerful, highly customizable, and filterable graph.'';
            homepage = "https://github.com/Apoo711/obsidian-3d-graph";
        };
    }
);
    "new-file-name" = (
    let
        baseUrl = "https://github.com/TheLoneWanderer4/obsidian-uuid-title/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "new-file-name";
        version = "1.0.0";
        outputHash = "sha256-S7pFaQQLR2BVys7DZIYQ6M6zBXuSD0yuf7Y/vKMq4pc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change the default filename used for new notes'';
            homepage = "https://github.com/TheLoneWanderer4/obsidian-uuid-title";
        };
    }
);
    "new-note-fixer" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-new-note-fixer/releases/download/2.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "new-note-fixer";
        version = "2.0.5";
        outputHash = "sha256-8OoRDmLNa51Plf8W0Ez57QJWwiN4aE59dHCWtXZvOXI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Unifies the way non-existing notes are created when clicking on their links.'';
            homepage = "https://github.com/mnaoumov/obsidian-new-note-fixer";
        };
    }
);
    "new-tab-default-page" = (
    let
        baseUrl = "https://github.com/chrisgrieser/new-tab-default-page/releases/download/0.11.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "new-tab-default-page";
        version = "0.11.9";
        outputHash = "sha256-iACIwRv5dMDRBleTQZEHb4egEQ8vNzE9ho0lA5A/HPw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a note of your choice when creating a new tab, like in the browser.'';
            homepage = "https://github.com/chrisgrieser/new-tab-default-page";
        };
    }
);
    "new-tab-plus" = (
    let
        baseUrl = "https://github.com/Raphlette/obsidian-new-tab-plus/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "new-tab-plus";
        version = "1.4.2";
        outputHash = "sha256-iLMMqCHyoIixFtvwJyDM9mN6QLqYQeHoCloK2SviPlc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allow to open markdown files, graph and canvas in new tab as the default behavior.'';
            homepage = "https://github.com/Raphlette/obsidian-new-tab-plus";
        };
    }
);
    "newledge" = (
    let
        baseUrl = "https://github.com/DepartingAgain/obsidian-newledge/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "newledge";
        version = "1.0.4";
        outputHash = "sha256-VSPqfeUb1ks9kpkIu46ouOU4CMasuKk9Ac2cj+sbulc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Newledge data.'';
            homepage = "https://github.com/DepartingAgain/obsidian-newledge";
        };
    }
);
    "newslog" = (
    let
        baseUrl = "https://github.com/protoavatar/obsidian-newsletters/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "newslog";
        version = "1.0.3";
        outputHash = "sha256-WsulgaiJT4qj4cI5zaHY79AB7yFbzxkYXgOdUbMpRvY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync newsletters and kindle highlights from your newslog.me daily bundles.'';
            homepage = "https://github.com/protoavatar/obsidian-newsletters";
        };
    }
);
    "next-link" = (
    let
        baseUrl = "https://github.com/jdluque/next-link/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "next-link";
        version = "0.1.1";
        outputHash = "sha256-sVdmC+HGbdPpC+RzKeSmwSoIZF5lfZGraf+UZR/Ur5o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Jump quickly between note links.'';
            homepage = "https://github.com/jdluque/next-link";
        };
    }
);
    "next-toc" = (
    let
        baseUrl = "https://github.com/Raven-Pensieve/obsidian-next-toc/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "next-toc";
        version = "2.0.0";
        outputHash = "sha256-37e1TO7SrlouEC1kVa55Ne1Byv0waqKByB074uFfHmc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Floating panel displaying the current document"s reading progress, table of contents, and navigation aids.'';
            homepage = "https://github.com/Raven-Pensieve/obsidian-next-toc";
        };
    }
);
    "nextcloud-link-fixer" = (
    let
        baseUrl = "https://github.com/KFreon/nextcloud-link-fixer/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nextcloud-link-fixer";
        version = "1.0.0";
        outputHash = "sha256-dnEhLk+0FUOTXydBCtGE07zuxipZclSRDWgLAtonou0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Nextcloud breaks Wiki-links. This fixes them.'';
            homepage = "https://github.com/KFreon/nextcloud-link-fixer";
        };
    }
);
    "nexus-ai-chat-importer" = (
    let
        baseUrl = "https://github.com/Superkikim/nexus-ai-chat-importer/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nexus-ai-chat-importer";
        version = "1.6.1";
        outputHash = "sha256-jZnq64FXkRcvq2AWWups0fZJ9hJ0L4/hMNzDfQ12BN0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import conversations from ChatGPT export files.'';
            homepage = "https://github.com/Superkikim/nexus-ai-chat-importer";
        };
    }
);
    "nifty-links" = (
    let
        baseUrl = "https://github.com/x-Ai/obsidian-nifty-links/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nifty-links";
        version = "1.4.0";
        outputHash = "sha256-kvQx1DDvKNGo23Lc5hdN6SL8L67UCVIsqb7q2QaPqqE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate elegant, Notion-style rich link cards to enhance your note-taking experience.'';
            homepage = "https://github.com/x-Ai/obsidian-nifty-links";
        };
    }
);
    "ninja-cursor" = (
    let
        baseUrl = "https://github.com/vrtmrz/ninja-cursor/releases/download/0.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ninja-cursor";
        version = "0.0.13";
        outputHash = "sha256-Wc7FPMyytHaYVanglgo+BA7fstok4i+DfeJcbbHI8dY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance cursor visibility.'';
            homepage = "https://github.com/vrtmrz/ninja-cursor";
        };
    }
);
    "nl-syntax-highlighting" = (
    let
        baseUrl = "https://github.com/artisticat1/nl-syntax-highlighting/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nl-syntax-highlighting";
        version = "0.4.0";
        outputHash = "sha256-FLyE0xBLYdkXAzruJFKOC8V3WWP2BEJMyvKpyR8pEXs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight adjectives, nouns, adverbs, verbs, and conjunctions in the editor.'';
            homepage = "https://github.com/artisticat1/nl-syntax-highlighting";
        };
    }
);
    "nldates-obsidian" = (
    let
        baseUrl = "https://github.com/argenos/nldates-obsidian/releases/download/0.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nldates-obsidian";
        version = "0.6.2";
        outputHash = "sha256-xbefGDCx7hH8onudG5S3cEGgFhd1vjXJo0jJm9t5ZyA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create date-links based on natural language.'';
            homepage = "https://github.com/argenos/nldates-obsidian";
        };
    }
);
    "no-dupe-leaves" = (
    let
        baseUrl = "https://github.com/scambier/obsidian-no-dupe-leaves/releases/download/0.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "no-dupe-leaves";
        version = "0.0.12";
        outputHash = "sha256-vAHGeZr3AYeOkcZhMwWxnxCNlH8vAeiXexM6ilwmhT8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Don"t reopen notes that are already open.'';
            homepage = "https://github.com/scambier/obsidian-no-dupe-leaves";
        };
    }
);
    "no-empty-windows" = (
    let
        baseUrl = "https://github.com/popscallion/obsidian-no-empty-windows/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "no-empty-windows";
        version = "1.1.0";
        outputHash = "sha256-f2kK6I83HzeFx4SPkj6gdYvAK3MmSMYzb+bc0tAND4I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Close the window with cmd+W on macOS when the last tab is closed.'';
            homepage = "https://github.com/popscallion/obsidian-no-empty-windows";
        };
    }
);
    "node-auto-resize" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Node-Auto-Resize/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "node-auto-resize";
        version = "1.2.1";
        outputHash = "sha256-E/A8bPsRvtUVLR8qdz/oZSIbRqATa2XcquWCdfj2KFY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically resize the node when the content changes.'';
            homepage = "https://github.com/Quorafind/Obsidian-Node-Auto-Resize";
        };
    }
);
    "node-factor" = (
    let
        baseUrl = "https://github.com/CalfMoon/node-factor/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "node-factor";
        version = "3.0.0";
        outputHash = "sha256-6wW95y+9bZkxhfV1OcE7w0FhI+yihc7u2byR6EHGQ6A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize factors effecting node size in graph.'';
            homepage = "https://github.com/CalfMoon/node-factor";
        };
    }
);
    "node-flow" = (
    let
        baseUrl = "https://github.com/LincZero/obsidian-node-flow/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "node-flow";
        version = "1.1.0";
        outputHash = "sha256-ekGMn/NAwlBZEBejrfUygL6rjFom+ZjBn2kNzqNhLI4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render node streams like `ComfyUi`, `UE`, `Houdini`, `Blender`, etc., to make it easy to write relevant notes. JSON describes the chart, compared to screenshots, making it easier to modify later. The plugin is also compatible with blogs.'';
            homepage = "https://github.com/LincZero/obsidian-node-flow";
        };
    }
);
    "noor" = (
    let
        baseUrl = "https://github.com/MKSherbini/obsidian-noor/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "noor";
        version = "0.0.6";
        outputHash = "sha256-c5YTGBLoQA0WlgTm0/PdH7lnHtPxBNhTGTXYcdn634I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Aims to help Muslims stay enlightened with Islam, Quran, Hadith, and Sunnah'';
            homepage = "https://github.com/MKSherbini/obsidian-noor";
        };
    }
);
    "nostr-writer" = (
    let
        baseUrl = "https://github.com/jamesmagoo/nostr-writer/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nostr-writer";
        version = "2.2.0";
        outputHash = "sha256-Z1ITIpWNQCc8PufiziJV1V4/dFU/U2J6oykhoL/MJ48=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your writing directly to Nostr.'';
            homepage = "https://github.com/jamesmagoo/nostr-writer";
        };
    }
);
    "note-2-tag-generator" = (
    let
        baseUrl = "https://github.com/augustin7698/note-2-tag-generator/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-2-tag-generator";
        version = "1.0.0";
        outputHash = "sha256-G5m9tPmGX8YD4X5WklDC0XmUg1LTNM18ifBJxCZNM9U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate tags from notes without openai key in multiple languages'';
            homepage = "https://github.com/augustin7698/note-2-tag-generator";
        };
    }
);
    "note-aliases" = (
    let
        baseUrl = "https://github.com/pulsovi/obsidian-note-aliases/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-aliases";
        version = "1.2.0";
        outputHash = "sha256-TROgTwFLf0Kx+TBCu4DgMXbR6AizpDq2qnh0dd7upwg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage aliases of notes.'';
            homepage = "https://github.com/pulsovi/obsidian-note-aliases";
        };
    }
);
    "note-annotations" = (
    let
        baseUrl = "https://github.com/jancbeck/obsidian-note-annotations/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-annotations";
        version = "1.0.1";
        outputHash = "sha256-SB392yUxfGmIrfT0sNzbrH2FwOIhp0nN3jM2MhZA9OE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add multi-colored highlights and comments to your notes.'';
            homepage = "https://github.com/jancbeck/obsidian-note-annotations";
        };
    }
);
    "note-archiver" = (
    let
        baseUrl = "https://github.com/thenomadlad/obsidian-note-archiver/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-archiver";
        version = "0.1.0";
        outputHash = "sha256-iRgTMaLB69esfiWaLCRStgU8OqnSSBNk+UJnIP66DJg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tools to archive your notes in another folder.'';
            homepage = "https://github.com/thenomadlad/obsidian-note-archiver";
        };
    }
);
    "note-atomizer" = (
    let
        baseUrl = "https://github.com/Binxly/Atomizer/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-atomizer";
        version = "1.2.0";
        outputHash = "sha256-7k/Xoae7HSJQ8MUXT7dPIDAb8C1hqbbJm3tNP1SWUII=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn any text into insightful atomic notes.'';
            homepage = "https://github.com/Binxly/Atomizer";
        };
    }
);
    "note-batcher" = (
    let
        baseUrl = "https://github.com/MrAnyx/obsidian-note-batcher/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-batcher";
        version = "1.0.0";
        outputHash = "sha256-4wW63lP1KO/YXurSJH1HBTDqdF2egTaRjiFIx5XizLM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create all unresolvered links with a single click.'';
            homepage = "https://github.com/MrAnyx/obsidian-note-batcher";
        };
    }
);
    "note-chain" = (
    let
        baseUrl = "https://github.com/zigholding/obsidian-notechain-plugin/releases/download/1.3.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-chain";
        version = "1.3.7";
        outputHash = "sha256-Uf0eFYBNDsMIEg0Q1fzls0S8H7ZpTA1lsLd+FPFSADM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Thoughts as river, notes as chain. Add prev and next notes to a notes, and order files by the chain in File Explorer.'';
            homepage = "https://github.com/zigholding/obsidian-notechain-plugin";
        };
    }
);
    "note-codes" = (
    let
        baseUrl = "https://github.com/SilverEzhik/obsidian-note-codes/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-codes";
        version = "0.0.2";
        outputHash = "sha256-UYiYmK2Vqj+ldShpc5jtKpUW69J2EfKmlqpE8MCueuQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reference your notes from anywhere with simple 4-character codes.'';
            homepage = "https://github.com/SilverEzhik/obsidian-note-codes";
        };
    }
);
    "note-companion-folder" = (
    let
        baseUrl = "https://github.com/vkodocha/NoteCompanionFolder/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-companion-folder";
        version = "0.0.7";
        outputHash = "sha256-x/T14oiq76AmXfsGSKTV5anumuBzWQSb6t7PJYcZVWI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage a separate folder of attachments for each note.'';
            homepage = "https://github.com/vkodocha/NoteCompanionFolder";
        };
    }
);
    "note-definitions" = (
    let
        baseUrl = "https://github.com/dominiclet/obsidian-note-definitions/releases/download/0.30.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-definitions";
        version = "0.30.0";
        outputHash = "sha256-GiLIxw75AYItQmeX8vs5vQPEPzrYTlL3iGXsA+U+wu8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Personal dictionary for your notes'';
            homepage = "https://github.com/dominiclet/obsidian-note-definitions";
        };
    }
);
    "note-favicon" = (
    let
        baseUrl = "https://github.com/mdklab/note-favicon/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-favicon";
        version = "1.4.3";
        outputHash = "sha256-785hCUveIaZep3tPOLbHjpH2Yu9lTI7IbckLS6137WE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extracts a URL from the frontmatter of notes and displays an associated favicon image next to the note title in the file tree. Supports standard URLs and base64-encoded images.'';
            homepage = "https://github.com/mdklab/note-favicon";
        };
    }
);
    "note-folder-autorename" = (
    let
        baseUrl = "https://github.com/pjeby/note-folder-autorename/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-folder-autorename";
        version = "0.1.2";
        outputHash = "sha256-0c4FtUeeQ4Lo9ILU1KI6ROzXsyBHP4wO37tYLk7hD70=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn notes into folders and automatically move/rename their folders when they move or are renamed.'';
            homepage = "https://github.com/pjeby/note-folder-autorename";
        };
    }
);
    "note-from-form" = (
    let
        baseUrl = "https://github.com/ArhiChief/obsidian-note-from-form/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-from-form";
        version = "1.0.2";
        outputHash = "sha256-6jfrFjjIJr9UojZATtQxZmfyWXQ5g4eFvVwNDdbcSHs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define dynamic input form and use it to create notes'';
            homepage = "https://github.com/ArhiChief/obsidian-note-from-form";
        };
    }
);
    "note-gallery" = (
    let
        baseUrl = "https://github.com/pashashocky/obsidian-note-gallery/releases/download/0.0.60";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-gallery";
        version = "0.0.60";
        outputHash = "sha256-p/FR0BfhLKNEAUZMIWoP7UPr3m7orzmSvow3OzChXzw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A masonry gallery that will visualize your notes, similar to Craft note view.'';
            homepage = "https://github.com/pashashocky/obsidian-note-gallery";
        };
    }
);
    "note-id" = (
    let
        baseUrl = "https://github.com/dominikmayer/obsidian-note-id/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-id";
        version = "1.4.1";
        outputHash = "sha256-rQMib2e2T56DCxTfi70F/d31WEg8IShweOEsqcQuFLA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays notes by their ID, enabling structured sequences for manuscripts or a Zettelkasten ("Folgezettel").'';
            homepage = "https://github.com/dominikmayer/obsidian-note-id";
        };
    }
);
    "note-linker-with-previewer" = (
    let
        baseUrl = "https://github.com/nickrallison/obsidian-note-linker-with-previewer/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-linker-with-previewer";
        version = "1.0.10";
        outputHash = "sha256-QFt39Y3Lad+0zheatj7yOovDyRjzZYR2UbqmgZ/h2rQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link your notes together.'';
            homepage = "https://github.com/nickrallison/obsidian-note-linker-with-previewer";
        };
    }
);
    "note-locker" = (
    let
        baseUrl = "https://github.com/Felvesthe/Note-Locker/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-locker";
        version = "1.3.1";
        outputHash = "sha256-+dPXSwCLicwTN+zxxSIogVhK0Lay0Rudwv38HbDJlP0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Lock notes to open in preview mode by default.'';
            homepage = "https://github.com/Felvesthe/Note-Locker";
        };
    }
);
    "note-metrics" = (
    let
        baseUrl = "https://github.com/Andre-Diamond/note-metrics/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-metrics";
        version = "1.1.4";
        outputHash = "sha256-C8AprFNNSVGj+r/N6F6S+LkyYdUBkF3FERRCFRwxXeo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Parses the tags in your Daily Notes and create interactive charts in a dashboard view.'';
            homepage = "https://github.com/Andre-Diamond/note-metrics";
        };
    }
);
    "note-mover-shortcut" = (
    let
        baseUrl = "https://github.com/bueckerlars/obsidian-note-mover-shortcut/releases/download/0.7.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-mover-shortcut";
        version = "0.7.2";
        outputHash = "sha256-o3zWqfYjuU8ep1OZ0iviWeXMcuWiPBH3JrUHPgZXuHo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly and easily move notes to a predefined folder. Perfect for organizing your notes.'';
            homepage = "https://github.com/bueckerlars/obsidian-note-mover-shortcut";
        };
    }
);
    "note-navigator" = (
    let
        baseUrl = "https://github.com/mudnug/note-navigator/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-navigator";
        version = "1.1.0";
        outputHash = "sha256-5j6A5BYZxZGuIF7wmhzdlOz9+AfniRM/ubpzXWgbSw8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Streamlines note review by automatically navigating to the next note upon deletion. Adds commands to go to the previous and next note, respecting user-selected sort order.'';
            homepage = "https://github.com/mudnug/note-navigator";
        };
    }
);
    "note-placeholder" = (
    let
        baseUrl = "https://github.com/XZSt4nce/note-placeholder/releases/download/2.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-placeholder";
        version = "2.1.10";
        outputHash = "sha256-a9vUoynUZXX8MgvrafxoMwg9H9hj42ixBAiXt9HVuzM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replaces text of note link to placeholder in view mode.'';
            homepage = "https://github.com/XZSt4nce/note-placeholder";
        };
    }
);
    "note-progressbar" = (
    let
        baseUrl = "https://github.com/ryohma0510/obsidian-note-progressbar/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-progressbar";
        version = "1.0.2";
        outputHash = "sha256-NqXvXFHyzSinHwmsdGIv2CH8OQxP11KthjYdYuhvZtI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays a live progress bar summarizing checkbox completion in the active note.'';
            homepage = "https://github.com/ryohma0510/obsidian-note-progressbar";
        };
    }
);
    "note-promixity" = (
    let
        baseUrl = "https://github.com/jlweston/obsidian-note-proximity-plugin/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-promixity";
        version = "1.0.9";
        outputHash = "sha256-Ad13JWxuvWdkr2U7F+RWH+7bRrsTeW8mvvmm+laGi1Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find similar notes based on the frequency of terms within the vault.'';
            homepage = "https://github.com/jlweston/obsidian-note-proximity-plugin";
        };
    }
);
    "note-refactor-obsidian" = (
    let
        baseUrl = "https://github.com/lynchjames/note-refactor-obsidian/releases/download/1.8.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-refactor-obsidian";
        version = "1.8.2";
        outputHash = "sha256-RkS+ogBdeQh4hX/V0nVrYz15T0kFyfW9auw5J2iT29E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract note content into new notes and split notes.'';
            homepage = "https://github.com/lynchjames/note-refactor-obsidian";
        };
    }
);
    "note-reviewer" = (
    let
        baseUrl = "https://github.com/TravisLinkey/note-reviewer/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-reviewer";
        version = "2.0.1";
        outputHash = "sha256-YEdt02QT6a/av2LSolHVdORK5g/KIeCN1opseD97cCI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Plugin to filter and review notes'';
            homepage = "https://github.com/TravisLinkey/note-reviewer";
        };
    }
);
    "note-splitter" = (
    let
        baseUrl = "https://github.com/decaf-dev/obsidian-note-splitter/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-splitter";
        version = "1.3.0";
        outputHash = "sha256-fjLfMkUzb/bOYZZ0JRuEBqQAWmjqUWfHl7mCseOjmQQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Split a note into individual notes based on a delimiter.'';
            homepage = "https://github.com/decaf-dev/obsidian-note-splitter";
        };
    }
);
    "note-status" = (
    let
        baseUrl = "https://github.com/devonthesofa/obsidian-note-status/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-status";
        version = "3.1.0";
        outputHash = "sha256-cpwokaxOvfoJKSwVPEr8cxCeH+kpXWWymqkX3YzISkU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Organize notes with customizable statuses. Track progress through an intuitive status pane, dropdown selector, and file explorer integration.'';
            homepage = "https://github.com/devonthesofa/obsidian-note-status";
        };
    }
);
    "note-sync" = (
    let
        baseUrl = "https://github.com/zigholding/obsidian-notesync-plugin/releases/download/0.6.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-sync";
        version = "0.6.3";
        outputHash = "sha256-YytvV4UYo5VED6nXvPpEpXJTaNV2kec4AOlNjUQluWA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes or plugins between vaults.'';
            homepage = "https://github.com/zigholding/obsidian-notesync-plugin";
        };
    }
);
    "note-synchronizer" = (
    let
        baseUrl = "https://github.com/tansongchen/obsidian-note-synchronizer/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-synchronizer";
        version = "0.1.6";
        outputHash = "sha256-dhjr/ggk169i7Fz6Fy2rXfLXEk1/sb95Z3Jds+r0gOY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize your notes to other note-based software like Anki, following more strictly the principles of Zettelkasten and treating each file as a note.'';
            homepage = "https://github.com/tansongchen/obsidian-note-synchronizer";
        };
    }
);
    "note-to-mp" = (
    let
        baseUrl = "https://github.com/sunbooshi/note-to-mp/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-to-mp";
        version = "2.1.1";
        outputHash = "sha256-gdScJsmt+mF+2BmlUuKSgAX+J7ikzGHRyCjDidBjANQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send notes to WeChat MP drafts, or copy notes to WeChat MP editor, perfect preservation of note styles, support code highlighting, line numbers in code, and support local image uploads.'';
            homepage = "https://github.com/sunbooshi/note-to-mp";
        };
    }
);
    "note-to-red" = (
    let
        baseUrl = "https://github.com/Yeban8090/note-to-red/releases/download/1.0.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-to-red";
        version = "1.0.19";
        outputHash = "sha256-22bVpdEqf2HYbfkU8EakRGynp8f09kKsmytU6wTojOU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert Markdown notes to RED (Xiaohongshu) style images'';
            homepage = "https://github.com/Yeban8090/note-to-red";
        };
    }
);
    "note-toolbar" = (
    let
        baseUrl = "https://github.com/chrisgurney/obsidian-note-toolbar/releases/download/1.30.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-toolbar";
        version = "1.30.18";
        outputHash = "sha256-GyfNLqBsO8pGxeYIAD/HVvZihoPX+Omz+i5X8MLPyvQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add customizable toolbars to your notes.'';
            homepage = "https://github.com/chrisgurney/obsidian-note-toolbar";
        };
    }
);
    "note-uid-generator" = (
    let
        baseUrl = "https://github.com/Netajam/obsidian_note_uid_generator/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "note-uid-generator";
        version = "1.3.0";
        outputHash = "sha256-ijRUZcoarU7Az+Z1wzL+W+QR/GcahInoCRcIzUGpv60=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically or manually generates Unique IDs (UIDs) for notes and registers them in metadata (frontmatter).'';
            homepage = "https://github.com/Netajam/obsidian_note_uid_generator";
        };
    }
);
    "notebook-navigator" = (
    let
        baseUrl = "https://github.com/johansan/notebook-navigator/releases/download/2.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notebook-navigator";
        version = "2.6.2";
        outputHash = "sha256-8HbNqKvq1p9mfgepo+WmKndigpHP219W3P9uilHdU80=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replace the default file explorer with a clean two-pane interface featuring folder tree, tag browsing, file previews, keyboard navigation, drag-and-drop, pinned notes, and customizable display options.'';
            homepage = "https://github.com/johansan/notebook-navigator";
        };
    }
);
    "notemd" = (
    let
        baseUrl = "https://github.com/Jacobinwwey/obsidian-NotEMD/releases/download/1.8.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notemd";
        version = "1.8.2";
        outputHash = "sha256-kPMYXdfvsZRjaXJ8Ik//i0pF3wMK7RGf4kMvCkrvkjs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrates with various LLMs to process notes, generate wiki-links, and create concept notes'';
            homepage = "https://github.com/Jacobinwwey/obsidian-NotEMD";
        };
    }
);
    "notepix" = (
    let
        baseUrl = "https://github.com/AyushParkara/NotePix/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notepix";
        version = "1.4.3";
        outputHash = "sha256-E9bnaK8UzeSyf6sWZNvtrmlI/ecd+Ipo6u0K3FD3SgA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically uploads images to your public or private GitHub repository, replacing local links with the corresponding remote URLs'';
            homepage = "https://github.com/AyushParkara/NotePix";
        };
    }
);
    "notes-dater" = (
    let
        baseUrl = "https://github.com/PaulTreanor/notes-dater/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes-dater";
        version = "1.0.2";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add created on and last updated on dates of the active note to the status bar.'';
            homepage = "https://github.com/PaulTreanor/notes-dater";
        };
    }
);
    "notes-explorer" = (
    let
        baseUrl = "https://github.com/tu2-atmanand/obsidian-notes-explorer/releases/download/2.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes-explorer";
        version = "2.7.1";
        outputHash = "sha256-Ei9eNepWIY1ie18QeOqzo5owbHtBWe95Hnqg41U9WCg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Explore your notes as cards in gallery or masonry view. A new way to revise your content.'';
            homepage = "https://github.com/tu2-atmanand/obsidian-notes-explorer";
        };
    }
);
    "notes-merger" = (
    let
        baseUrl = "https://github.com/niffka/notes-merger/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes-merger";
        version = "1.3.2";
        outputHash = "sha256-/7LbDoYug4JF0qcD4CAtAyODckCwGCOdU4WJO/G5Kkw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Merge notes into a single Markdown document based on index Markdown file.'';
            homepage = "https://github.com/niffka/notes-merger";
        };
    }
);
    "notes-refresher" = (
    let
        baseUrl = "https://github.com/connorpark24/refresher-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes-refresher";
        version = "1.1.0";
        outputHash = "sha256-JR1KkvDt+6fwB/P5ry3varUnkxfl2ckrG/f3BpJUCjE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides AI-generated refresher summaries of notes from your Vault using GPT-4.'';
            homepage = "https://github.com/connorpark24/refresher-plugin";
        };
    }
);
    "notes-sync-share" = (
    let
        baseUrl = "https://github.com/Alt-er/obsidian-sync-share/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes-sync-share";
        version = "1.2.3";
        outputHash = "sha256-CeRF3BpF1ulPzkndOukr0K+e74oGFObSr7mVA3j25tM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync and share (publish) your notes in your own private service.'';
            homepage = "https://github.com/Alt-er/obsidian-sync-share";
        };
    }
);
    "notes-to-strapi-export-article-ai" = (
    let
        baseUrl = "https://github.com/CinquinAndy/notes-to-strapi-export-article-ai/releases/download/3.0.271";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes-to-strapi-export-article-ai";
        version = "3.0.271";
        outputHash = "sha256-czBniVPD06er+1HGmG8ptDH4Z2um/aQ6G0Lg/UJY11o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Effortlessly export your notes to Strapi CMS with AI-powered handling and SEO optimization.'';
            homepage = "https://github.com/CinquinAndy/notes-to-strapi-export-article-ai";
        };
    }
);
    "notes2tweets" = (
    let
        baseUrl = "https://github.com/Tej-Sharma/notes2tweets-obsidian/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notes2tweets";
        version = "1.0.5";
        outputHash = "sha256-oS11sdVu7uqO4Zoyjh+gBrNVhuMF0cAg2plvUdqbv7I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate tweets automatically from all the notes you took'';
            homepage = "https://github.com/Tej-Sharma/notes2tweets-obsidian";
        };
    }
);
    "notesmith" = (
    let
        baseUrl = "https://github.com/csteamengine/notesmith/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notesmith";
        version = "1.0.3";
        outputHash = "sha256-qD7Glyfht5Q7cZLqRBf0w8eKxa4iMAiLAtkrmHImFEk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''NoteSmith is a powerful note-refining plugin using OpenAI"s API. It allows you to refine your notes, generate summaries, and create new content based on your existing notes.'';
            homepage = "https://github.com/csteamengine/notesmith";
        };
    }
);
    "noteson-publish" = (
    let
        baseUrl = "https://github.com/shapkinaa/noteson-obsidian-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "noteson-publish";
        version = "1.1.0";
        outputHash = "sha256-+0LjldNc1dltFA+xe83ytGt+GFrGjAkdtRiSHKe282c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make single notes instantly available on the web.'';
            homepage = "https://github.com/shapkinaa/noteson-obsidian-plugin";
        };
    }
);
    "notetweet" = (
    let
        baseUrl = "https://github.com/chhoumann/notetweet_obsidian/releases/download/0.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notetweet";
        version = "0.5.2";
        outputHash = "sha256-iR38jUN9dR8kaaZbhkMHQsNhav7ED4argq0GMlNPB/w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post to Twitter.'';
            homepage = "https://github.com/chhoumann/notetweet_obsidian";
        };
    }
);
    "notice-controller" = (
    let
        baseUrl = "https://github.com/juan-miii/obsidian-notice-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notice-controller";
        version = "1.0.3";
        outputHash = "sha256-QDjNlFRDUInva+zXyMEhh7C58hJgD4/BN9mxn96b9qs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manages notifications at startup.'';
            homepage = "https://github.com/juan-miii/obsidian-notice-plugin";
        };
    }
);
    "notice-logger" = (
    let
        baseUrl = "https://github.com/gapmiss/notice-logger/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "notice-logger";
        version = "0.1.4";
        outputHash = "sha256-996q1SUU6iQwAsEx0TEgDFEzlXXE6sTYaEuv6TVmVdI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Logs all notices to the developer console, with optional prefix.'';
            homepage = "https://github.com/gapmiss/notice-logger";
        };
    }
);
    "nova" = (
    let
        baseUrl = "https://github.com/shawnduggan/nova/releases/download/1.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nova";
        version = "1.5.5";
        outputHash = "sha256-aycpXViLUXYQNiA2+GqE3EYxamYKwbbt7iaesZ7m0Rw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Your AI writing partner that edits exactly where you want - select text and transform, or chat at cursor position.'';
            homepage = "https://github.com/shawnduggan/nova";
        };
    }
);
    "novel-word-count" = (
    let
        baseUrl = "https://github.com/isaaclyman/novel-word-count-obsidian/releases/download/4.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "novel-word-count";
        version = "4.6.1";
        outputHash = "sha256-t+8kfdDeaUsvqBR1qRg0W02J85i+KWuWklCuhsNKhCs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a word count, page count, creation date, or other statistics for each file, folder and vault in the File Explorer pane.'';
            homepage = "https://github.com/isaaclyman/novel-word-count-obsidian";
        };
    }
);
    "nsfw-filter" = (
    let
        baseUrl = "https://github.com/catvatar/Obsidian-NSFW-Plugin/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nsfw-filter";
        version = "1.0.11";
        outputHash = "sha256-6qc4ZroBvQx6LxWBzhYBGat0XKsV2QQMZrs10aU5PKY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds customizable and easly togglable NSFW filter'';
            homepage = "https://github.com/catvatar/Obsidian-NSFW-Plugin";
        };
    }
);
    "nuke-orphans" = (
    let
        baseUrl = "https://github.com/sandorex/nuke-orphans-plugin/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nuke-orphans";
        version = "1.2.5";
        outputHash = "sha256-zml8UaB1UlCH6htGNbZFnyRZoS6a06R/BEcW45L0JPg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Trash orphaned files and attachments.'';
            homepage = "https://github.com/sandorex/nuke-orphans-plugin";
        };
    }
);
    "number-headings-obsidian" = (
    let
        baseUrl = "https://github.com/onlyafly/number-headings-obsidian/releases/download/1.16.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "number-headings-obsidian";
        version = "1.16.0";
        outputHash = "sha256-4xdYVy3NMCz0vTzdnjDTA69xFe1GsBPwWIxlUfHzAAk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically number or re-number headings.'';
            homepage = "https://github.com/onlyafly/number-headings-obsidian";
        };
    }
);
    "numerals" = (
    let
        baseUrl = "https://github.com/gtg922r/obsidian-numerals/releases/download/1.9.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "numerals";
        version = "1.9.3";
        outputHash = "sha256-v9COXDMedeYZyvN+m5KWgQpDWmVEDZBkqfq+3nG6bDI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn any code block into an advanced calculator. Evaluate math expressions on each line of a code block, including units, currency, and optional TeX rendering.'';
            homepage = "https://github.com/gtg922r/obsidian-numerals";
        };
    }
);
    "nutstore-sync" = (
    let
        baseUrl = "https://github.com/nutstore/obsidian-nutstore-sync/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nutstore-sync";
        version = "1.1.3";
        outputHash = "sha256-0sd9+4lXDQfim0W6efvOETYIL+j9WEsbd/zAu14FVgc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your vault with Nutstore/坚果云 using WebDAV protocol.'';
            homepage = "https://github.com/nutstore/obsidian-nutstore-sync";
        };
    }
);
    "nyanbar" = (
    let
        baseUrl = "https://github.com/xhyabunny/nyanbar/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "nyanbar";
        version = "1.0.1";
        outputHash = "sha256-UEYMQmFVJ2XAqEjfAzosMlxycz5UgEolJesoLx5nd5c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Nyan Cat Progress Bar generator!'';
            homepage = "https://github.com/xhyabunny/nyanbar";
        };
    }
);
    "o2" = (
    let
        baseUrl = "https://github.com/songkg7/o2/releases/download/2.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "o2";
        version = "2.4.1";
        outputHash = "sha256-i88hI6itBsnRvZiIm4ks33a5pADevIuA7g4ghc4m/N0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert Obsidian Markdown syntax to other Markdown platforms such as Jekyll.'';
            homepage = "https://github.com/songkg7/o2";
        };
    }
);
    "ob2static-site" = (
    let
        baseUrl = "https://github.com/yy4382/obsidian-static-site-export/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ob2static-site";
        version = "2.1.1";
        outputHash = "sha256-SBfr+xrrozo5BTFluvAPUncRMkwF6ADhbuf8QGixeMM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export specific notes to general Markdown files for static page generator like Hugo, Hexo, Astro and more.'';
            homepage = "https://github.com/yy4382/obsidian-static-site-export";
        };
    }
);
    "obisidian-note-linker" = (
    let
        baseUrl = "https://github.com/AlexW00/obsidian-note-linker/releases/download/1.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obisidian-note-linker";
        version = "1.2.9";
        outputHash = "sha256-e5awFr09YcSFy72vsvkNJ0FolwGuSiTvQlHBiIJWGyo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically find and create new links between notes.'';
            homepage = "https://github.com/AlexW00/obsidian-note-linker";
        };
    }
);
    "object-oriented-thinking" = (
    let
        baseUrl = "https://github.com/TiagoJacinto/obsidian-object-oriented-thinking/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "object-oriented-thinking";
        version = "1.0.10";
        outputHash = "sha256-BGViIOcLT5ttil/VcvnMZrpuuISW+VtbMgNLVDuuwm8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add inheritance-like behavior to notes.'';
            homepage = "https://github.com/TiagoJacinto/obsidian-object-oriented-thinking";
        };
    }
);
    "object-writer" = (
    let
        baseUrl = "https://github.com/IagoGrah/obsidian-object-writer/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "object-writer";
        version = "1.1.1";
        outputHash = "sha256-lnIIgnb2jiMyOm19NhWZIrWlNIsTFNLzNnjvvfZB0Zo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Utility for object writing with random words.'';
            homepage = "https://github.com/IagoGrah/obsidian-object-writer";
        };
    }
);
    "obligator" = (
    let
        baseUrl = "https://github.com/Newbrict/obsidian-obligator/releases/download/5.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obligator";
        version = "5.2.0";
        outputHash = "sha256-eoZcqoOYok1IL7ESHOTtkQtTT/n1ZNaKmFvf3msGaaw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A fully featured replacement for the built-in daily notes plugin. Obligator functions like a virtual bullet journal by copying over unchecked to-do items to your new daily note, along with adding any scheduled items you"ve set up.'';
            homepage = "https://github.com/Newbrict/obsidian-obligator";
        };
    }
);
    "oblogger" = (
    let
        baseUrl = "https://github.com/loftTech/obsidian-oblogger/releases/download/0.9.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "oblogger";
        version = "0.9.2";
        outputHash = "sha256-kv6O1E5AiU0QT+4i1jQ4oPmpSS340dlwwNN6DKE4RbA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tag explorer and frontmatter logger.'';
            homepage = "https://github.com/loftTech/obsidian-oblogger";
        };
    }
);
    "obs-text-wrapper" = (
    let
        baseUrl = "https://github.com/smx0/obs-text-wrapper/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obs-text-wrapper";
        version = "1.0.0";
        outputHash = "sha256-N9H4ByAKKUgNnTC/BXcsiKjpRZw0KyoegUVzdao9TeA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly wrap selected text with HTML tags by using a shortcut or from the command palette.'';
            homepage = "https://github.com/smx0/obs-text-wrapper";
        };
    }
);
    "obsidian-2hop-links-plugin" = (
    let
        baseUrl = "https://github.com/tokuhirom/obsidian-2hop-links-plugin/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-2hop-links-plugin";
        version = "0.9.0";
        outputHash = "sha256-wQWIz6YXxKjctI0aHH8CI1eBOeV2cxCzCe5LRSiUfqc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the page linked 2hop ahead.'';
            homepage = "https://github.com/tokuhirom/obsidian-2hop-links-plugin";
        };
    }
);
    "obsidian-5e-statblocks" = (
    let
        baseUrl = "https://github.com/Obsidian-TTRPG-Community/fantasy-statblocks/releases/download/4.10.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-5e-statblocks";
        version = "4.10.3";
        outputHash = "sha256-VAZ4CbPyEUsLusGGRS9WR1te5/Vzxu7E9QkkHywKaoQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create, manage and view a Fantasy Bestiary with Dungeons and Dragons style statblocks.'';
            homepage = "https://github.com/Obsidian-TTRPG-Community/fantasy-statblocks";
        };
    }
);
    "obsidian-account-linker" = (
    let
        baseUrl = "https://github.com/qwegat/Obsidian-Account-Linker/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-account-linker";
        version = "1.0.2";
        outputHash = "sha256-b2/ngygQL9kbjaO6VIPnci6p6vh19M0lYkhYxVPaBDA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Describe external service accounts in the frontmatter.'';
            homepage = "https://github.com/qwegat/Obsidian-Account-Linker";
        };
    }
);
    "obsidian-achievements" = (
    let
        baseUrl = "https://github.com/Zachatoo/obsidian-achievements/releases/download/0.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-achievements";
        version = "0.0.10";
        outputHash = "sha256-A8i+geYKsWOvd2SOUeTk0GsTHskkAfsMmj4muqMwDWE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add achievements to Obsidian.'';
            homepage = "https://github.com/Zachatoo/obsidian-achievements";
        };
    }
);
    "obsidian-activity-logger" = (
    let
        baseUrl = "https://github.com/Creling/obsidian-activity-logger/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-activity-logger";
        version = "0.1.0";
        outputHash = "sha256-qs27liN14pQo7i92Qv4bGGj9OCsQZPdYd8gLypnEyes=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Log your activities like creating notes, modifying notes, deleting notes and so on.'';
            homepage = "https://github.com/Creling/obsidian-activity-logger";
        };
    }
);
    "obsidian-admonition" = (
    let
        baseUrl = "https://github.com/ebullient/obsidian-admonition/releases/download/12.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-admonition";
        version = "12.0.2";
        outputHash = "sha256-R1QPEwyyXvNILxfLaB+U68b6Y5NUx2caZne/DKTxqn4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Admonition block-styled content.'';
            homepage = "https://github.com/ebullient/obsidian-admonition";
        };
    }
);
    "obsidian-advanced-codeblock" = (
    let
        baseUrl = "https://github.com/lijyze/obsidian-advanced-codeblock/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-advanced-codeblock";
        version = "1.1.5";
        outputHash = "sha256-IWMaShXxC1k9qdzY2y7U05j8MTyGbrIBqAw5b1InoAg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add additional features to code blocks.'';
            homepage = "https://github.com/lijyze/obsidian-advanced-codeblock";
        };
    }
);
    "obsidian-advanced-new-file" = (
    let
        baseUrl = "https://github.com/vanadium23/obsidian-advanced-new-file/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-advanced-new-file";
        version = "1.6.0";
        outputHash = "sha256-mXBmEgiuKGa/pTTBir/4+OdXb81l9wkUVFBy2dIdo0s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes in chosen folder.'';
            homepage = "https://github.com/vanadium23/obsidian-advanced-new-file";
        };
    }
);
    "obsidian-advanced-slides" = (
    let
        baseUrl = "https://github.com/MSzturc/obsidian-advanced-slides/releases/download/1.20.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-advanced-slides";
        version = "1.20.0";
        outputHash = "sha256-AKtNX2ldU7I2uwOzjZaipNqY511LzLG42m/W5xBSWu0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Markdown-based presentations.'';
            homepage = "https://github.com/MSzturc/obsidian-advanced-slides";
        };
    }
);
    "obsidian-advanced-uri" = (
    let
        baseUrl = "https://github.com/Vinzent03/obsidian-advanced-uri/releases/download/1.46.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-advanced-uri";
        version = "1.46.1";
        outputHash = "sha256-sYDgyXjBCGFoAth3xCGh9E+umSbmEr4bx3d/Wb2VaHc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Control everything with URI.'';
            homepage = "https://github.com/Vinzent03/obsidian-advanced-uri";
        };
    }
);
    "obsidian-aggregator" = (
    let
        baseUrl = "https://github.com/Seraphli/obsidian-aggregator/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-aggregator";
        version = "0.3.1";
        outputHash = "sha256-1GKbdGyjUqDTMfVar7WQNGLpGM1uaMH5DW+d61DIgiA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Gather information from files, and make a summary in the file.'';
            homepage = "https://github.com/Seraphli/obsidian-aggregator";
        };
    }
);
    "obsidian-agile-task-notes" = (
    let
        baseUrl = "https://github.com/BoxThatBeat/obsidian-agile-task-notes/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-agile-task-notes";
        version = "2.1.0";
        outputHash = "sha256-fJNFDXi19cT7n3R8IrY7eXDcmmmvNQKiMUK7xOrDTBI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import your tasks from your TFS (Azure or Jira) to take notes on them and make todo-lists!'';
            homepage = "https://github.com/BoxThatBeat/obsidian-agile-task-notes";
        };
    }
);
    "obsidian-alias-from-heading" = (
    let
        baseUrl = "https://github.com/basham/obsidian-alias-from-heading/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-alias-from-heading";
        version = "1.1.2";
        outputHash = "sha256-6QJXegB03ZrZhhLSBQkGwXpqYPQ7PsR+Cqy4Xz572Wo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Implicitly add an alias matching the first heading in a document.'';
            homepage = "https://github.com/basham/obsidian-alias-from-heading";
        };
    }
);
    "obsidian-amazingmarvin-plugin" = (
    let
        baseUrl = "https://github.com/ikuyarihS/obsidian-amazingmarvin-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-amazingmarvin-plugin";
        version = "1.0.1";
        outputHash = "sha256-9hIoQkoM0M+qWgxs+uhfDfmTxrgUZoyNKIyWgGQIfxc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get data from Amazing Marvin.'';
            homepage = "https://github.com/ikuyarihS/obsidian-amazingmarvin-plugin";
        };
    }
);
    "obsidian-ankibridge" = (
    let
        baseUrl = "https://github.com/JeppeKlitgaard/ObsidianAnkiBridge/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-ankibridge";
        version = "0.8.0";
        outputHash = "sha256-u2FJy9JN65CCDCzRGrf2RI3kTtyyqSsQ7WwUyOLyKNM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Yet Another Anki Bridge. Focuses on a strict grammar and integrity of your data.'';
            homepage = "https://github.com/JeppeKlitgaard/ObsidianAnkiBridge";
        };
    }
);
    "obsidian-annotator" = (
    let
        baseUrl = "https://github.com/elias-sundqvist/obsidian-annotator/releases/download/0.2.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-annotator";
        version = "0.2.11";
        outputHash = "sha256-X0N+S7nmelocbuFyX5fymEubD1uP6gQjXl8GePAlO6k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Read and annotate PDFs and EPUB files.'';
            homepage = "https://github.com/elias-sundqvist/obsidian-annotator";
        };
    }
);
    "obsidian-another-quick-switcher" = (
    let
        baseUrl = "https://github.com/tadashi-aikawa/obsidian-another-quick-switcher/releases/download/14.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-another-quick-switcher";
        version = "14.6.0";
        outputHash = "sha256-9z//wWQIjrgANet913mWz0wV68Krwcr2s1LGh42RPl8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Another choice of Quick switcher.'';
            homepage = "https://github.com/tadashi-aikawa/obsidian-another-quick-switcher";
        };
    }
);
    "obsidian-apple-reminders-plugin" = (
    let
        baseUrl = "https://github.com/urishiraval/obsidian-apple-reminders-plugin/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-apple-reminders-plugin";
        version = "2.0.0";
        outputHash = "sha256-A0kFB5UJmPw0obk6isjgtAljGlct3TXegE+YVwb5Vag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Apple Reminders.'';
            homepage = "https://github.com/urishiraval/obsidian-apple-reminders-plugin";
        };
    }
);
    "obsidian-apply-patterns" = (
    let
        baseUrl = "https://github.com/jglev/obsidian-apply-patterns-plugin/releases/download/2.2.0-beta0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-apply-patterns";
        version = "2.2.0-beta0";
        outputHash = "sha256-Ck/71piCKja+2XfYTMKTuMPQK5Be/dH5MI6tRHdauAg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Apply custom patterns of find-and-replace in succession to text.'';
            homepage = "https://github.com/jglev/obsidian-apply-patterns-plugin";
        };
    }
);
    "obsidian-argdown-plugin" = (
    let
        baseUrl = "https://github.com/amdecker/obsidian-argdown-plugin/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-argdown-plugin";
        version = "2.0.2";
        outputHash = "sha256-/nOv8QTjAsTpYESXas2XKiEJiBZApHXTaVf5EBIFpOU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write Argdown code blocks and view the maps in reading mode.'';
            homepage = "https://github.com/amdecker/obsidian-argdown-plugin";
        };
    }
);
    "obsidian-asciidoc-blocks" = (
    let
        baseUrl = "https://github.com/juracy/obsidian-asciidoc-blocks/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-asciidoc-blocks";
        version = "1.0.2";
        outputHash = "sha256-Th/ZaURzrPzN9Ym/anx5UeLo8YSNpfXiiemdJEeeo28=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render asciidoc blocks, initially asciidoc tables.'';
            homepage = "https://github.com/juracy/obsidian-asciidoc-blocks";
        };
    }
);
    "obsidian-asciimath" = (
    let
        baseUrl = "https://github.com/widcardw/obsidian-asciimath/releases/download/0.8.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-asciimath";
        version = "0.8.2";
        outputHash = "sha256-HYpgKQGAg/pksbovZqanauxR/TYWUyDiBql3pi6eoME=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add asciimath support.'';
            homepage = "https://github.com/widcardw/obsidian-asciimath";
        };
    }
);
    "obsidian-attendance" = (
    let
        baseUrl = "https://github.com/Tiim/obsidian-attendance/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-attendance";
        version = "0.6.1";
        outputHash = "sha256-7CKsjfjWaVZR7tmjr4HPTseN7PAPI1osig2gXTM+rOY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take attendance directly inside of your notes.'';
            homepage = "https://github.com/Tiim/obsidian-attendance";
        };
    }
);
    "obsidian-audio-notes" = (
    let
        baseUrl = "https://github.com/jjmaldonis/obsidian-audio-notes/releases/download/0.7.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-audio-notes";
        version = "0.7.2";
        outputHash = "sha256-/ZZ1wZdmLEQAyvLdYCZgNhxm9pWhk46yVRltyUXxXtE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes for podcasts and audio files, and automatically generate transcripts while listening so you can quickly take notes on the best parts of the podcast.'';
            homepage = "https://github.com/jjmaldonis/obsidian-audio-notes";
        };
    }
);
    "obsidian-audio-player" = (
    let
        baseUrl = "https://github.com/noonesimg/obsidian-audio-player/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-audio-player";
        version = "0.1.1";
        outputHash = "sha256-vyMuTlQNU9BS+45JMKVo6QJxQL89jEjVpl9vsegcQYY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Audio player with background playback, bookmarks and wave visualiser instead of the default HTML5 audio.'';
            homepage = "https://github.com/noonesimg/obsidian-audio-player";
        };
    }
);
    "obsidian-auto-hide" = (
    let
        baseUrl = "https://github.com/skjar/obsidian-auto-hide/releases/download/0.4.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-auto-hide";
        version = "0.4.8";
        outputHash = "sha256-Yn72XooAFCkeIaqU44p23orXrxQZ7rdpOj2GweoOcvc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collapse sidebars when clicking on the editor/viewer panel.'';
            homepage = "https://github.com/skjar/obsidian-auto-hide";
        };
    }
);
    "obsidian-auto-link-title" = (
    let
        baseUrl = "https://github.com/zolrath/obsidian-auto-link-title/releases/download/1.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-auto-link-title";
        version = "1.5.5";
        outputHash = "sha256-fmGIDBkqtXrLlYqOnDGElSdTGklxQxmkt9Q85F6guFI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically fetches the titles of links from the web.'';
            homepage = "https://github.com/zolrath/obsidian-auto-link-title";
        };
    }
);
    "obsidian-auto-pair-chinese-symbol" = (
    let
        baseUrl = "https://github.com/renmu123/obsidian-auto-pair-chinese-symbol/releases/download/0.9.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-auto-pair-chinese-symbol";
        version = "0.9.5";
        outputHash = "sha256-t//0qDtmq8IU58Awdc6flK7j869iF+42rtwybs2615Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto-pair Chinese symbols.'';
            homepage = "https://github.com/renmu123/obsidian-auto-pair-chinese-symbol";
        };
    }
);
    "obsidian-auto-split" = (
    let
        baseUrl = "https://github.com/jsartelle/obsidian-auto-split/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-auto-split";
        version = "1.1.2";
        outputHash = "sha256-iMBXpYkZQgBs8s2YHgF5DekD30KuSypkvMx6UHrhxV0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open notes with split editor & preview.'';
            homepage = "https://github.com/jsartelle/obsidian-auto-split";
        };
    }
);
    "obsidian-autocomplete-plugin" = (
    let
        baseUrl = "https://github.com/yeboster/autocomplete-obsidian/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-autocomplete-plugin";
        version = "0.8.0";
        outputHash = "sha256-sOvCqc46/KLn0lE0Opx2BSxpCfTywyGA+agwhYfY8BU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Autocomplete text to increase your typing speed.'';
            homepage = "https://github.com/yeboster/autocomplete-obsidian";
        };
    }
);
    "obsidian-autoscroll" = (
    let
        baseUrl = "https://github.com/petr-nazarov/obsidian-autoscroll/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-autoscroll";
        version = "1.1.4";
        outputHash = "sha256-BiRi4JrxgNAA0H4viawg7SzTW/tsjxJ3KaFx3hs3ezM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically scroll down the content with the provided speed.'';
            homepage = "https://github.com/petr-nazarov/obsidian-autoscroll";
        };
    }
);
    "obsidian-awesome-flashcard" = (
    let
        baseUrl = "https://github.com/AwesomeDog/obsidian-awesome-flashcard/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-awesome-flashcard";
        version = "0.1.2";
        outputHash = "sha256-O4xiBWirq3kBiTXOmyMVOu7n4VQiyDGtBQyU8lvJs6M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Handy Anki integration.'';
            homepage = "https://github.com/AwesomeDog/obsidian-awesome-flashcard";
        };
    }
);
    "obsidian-badge" = (
    let
        baseUrl = "https://github.com/linjunpop/obsidian-badge/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-badge";
        version = "0.4.1";
        outputHash = "sha256-MVvThfuPPM5vid7GT5Tj8iaNXBJoSgYv80keevaaaOE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render progress bars in your notes.'';
            homepage = "https://github.com/linjunpop/obsidian-badge";
        };
    }
);
    "obsidian-banners" = (
    let
        baseUrl = "https://github.com/noatpad/obsidian-banners/releases/download/2.0.5-beta";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-banners";
        version = "2.0.5-beta";
        outputHash = "sha256-AueIFEsRcVrQfbVbXL0QVu8SkNp0vtLnCOBwv2whSF0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add banner images to your notes!'';
            homepage = "https://github.com/noatpad/obsidian-banners";
        };
    }
);
    "obsidian-basetag" = (
    let
        baseUrl = "https://github.com/darrenkuro/obsidian-basetag/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-basetag";
        version = "1.2.1";
        outputHash = "sha256-BZ4qVzaKhx+bRUSXtU1jQy6pbEAFB6B09uKDGGx4vjI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render the basename of tags in preview mode.'';
            homepage = "https://github.com/darrenkuro/obsidian-basetag";
        };
    }
);
    "obsidian-bbcode" = (
    let
        baseUrl = "https://github.com/salockhart/obsidian-bbcode/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-bbcode";
        version = "1.3.0";
        outputHash = "sha256-WdZg8zkH9TO4IQ1jytMbO49OpbYt8yPk9voLBJiTEyQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert Markdown files to BBCode.'';
            homepage = "https://github.com/salockhart/obsidian-bbcode";
        };
    }
);
    "obsidian-bellboy" = (
    let
        baseUrl = "https://github.com/shakedlokits/obsidian-bellboy/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-bellboy";
        version = "1.0.10";
        outputHash = "sha256-3a+fx09Rb6EEm0qRXZ6opImVvNUpJo4E8EM43r3UwAs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Opinionated file structure manager.'';
            homepage = "https://github.com/shakedlokits/obsidian-bellboy";
        };
    }
);
    "obsidian-better-command-palette" = (
    let
        baseUrl = "https://github.com/AlexBieg/obsidian-better-command-palette/releases/download/0.17.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-better-command-palette";
        version = "0.17.1";
        outputHash = "sha256-BPEFlBrT/BVNyivtFGy7L7+jd4XYhlaZw00lt5uOQMA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A command palette that does all of the things you want it to do.'';
            homepage = "https://github.com/AlexBieg/obsidian-better-command-palette";
        };
    }
);
    "obsidian-bible-linker" = (
    let
        baseUrl = "https://github.com/kuchejak/obsidian-bible-linker-plugin/releases/download/1.5.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-bible-linker";
        version = "1.5.16";
        outputHash = "sha256-ALFUVn9MiOEnZY1vEAJeDjvO/OTyP/C6g8jHj4ERDeg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link/Copy multiple Bible verses easily using your own Bible files. Compatible with the Bible Study Kit.'';
            homepage = "https://github.com/kuchejak/obsidian-bible-linker-plugin";
        };
    }
);
    "obsidian-bible-reference" = (
    let
        baseUrl = "https://github.com/tim-hub/obsidian-bible-reference/releases/download/25.12.27";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-bible-reference";
        version = "25.12.27";
        outputHash = "sha256-E3WGM7vslT7V8c9lgNYzcS3OgmnZfOihZpyg7aegnMk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take Bible study notes easily. Automatically suggests Bible verses as references.'';
            homepage = "https://github.com/tim-hub/obsidian-bible-reference";
        };
    }
);
    "obsidian-binary-file-manager-plugin" = (
    let
        baseUrl = "https://github.com/qawatake/obsidian-binary-file-manager-plugin/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-binary-file-manager-plugin";
        version = "0.3.0";
        outputHash = "sha256-3qW/9gDnm0enQXOI6FbUhrzr2lfYqpEuoaidlngSPhY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Detect new binary files in the vault and create Markdown files with metadata.'';
            homepage = "https://github.com/qawatake/obsidian-binary-file-manager-plugin";
        };
    }
);
    "obsidian-book-search-plugin" = (
    let
        baseUrl = "https://github.com/anpigon/obsidian-book-search-plugin/releases/download/0.7.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-book-search-plugin";
        version = "0.7.5";
        outputHash = "sha256-fkjJqazNXH+bKX+6CYPWA7pJ+N3qEkvHmT8iUvrWPhE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create book notes and insert book metadata from providers including Google Books and Naver.'';
            homepage = "https://github.com/anpigon/obsidian-book-search-plugin";
        };
    }
);
    "obsidian-bulk-rename-plugin" = (
    let
        baseUrl = "https://github.com/OlegLustenko/obsidian-bulk-rename/releases/download/0.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-bulk-rename-plugin";
        version = "0.5.2";
        outputHash = "sha256-Rvjb8IuIo4sQ4pOAbtHMtWLLQxbjxEDx+bcOyIVg0RQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename files based on a pattern.'';
            homepage = "https://github.com/OlegLustenko/obsidian-bulk-rename";
        };
    }
);
    "obsidian-buttondown-plugin" = (
    let
        baseUrl = "https://github.com/caro401/obsidian-buttondown/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-buttondown-plugin";
        version = "1.1.2";
        outputHash = "sha256-r9lyHZ/Fb3UZNCGvEFpUWv6O/kuo1US+QYowoTu5chc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send your notes to your buttondown.email account as email drafts.'';
            homepage = "https://github.com/caro401/obsidian-buttondown";
        };
    }
);
    "obsidian-calibre-plugin" = (
    let
        baseUrl = "https://github.com/caronchen/obsidian-calibre-plugin/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-calibre-plugin";
        version = "1.0.8";
        outputHash = "sha256-1HvCdC6/fLeRZ9Anwi2TrAhjw8V6yeGR2dPo60iikQA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Access your Calibre libraries and read books directly in Obsidian.'';
            homepage = "https://github.com/caronchen/obsidian-calibre-plugin";
        };
    }
);
    "obsidian-camera" = (
    let
        baseUrl = "https://github.com/aldrinjenson/obsidian-camera/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-camera";
        version = "1.8.0";
        outputHash = "sha256-KYndCEXsx5K6BP/+HH0AxTjbAIW9arPdImhBxamVNYQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and save snaps or video recordings.'';
            homepage = "https://github.com/aldrinjenson/obsidian-camera";
        };
    }
);
    "obsidian-canvas-conversation" = (
    let
        baseUrl = "https://github.com/AndreBaltazar8/obsidian-canvas-conversation/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-canvas-conversation";
        version = "1.1.1";
        outputHash = "sha256-IbVXHP3UV+YQftJlTLxRm60VemW1v7HJjAKaiDOZ7j4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a canvas conversation using ChatGPT.'';
            homepage = "https://github.com/AndreBaltazar8/obsidian-canvas-conversation";
        };
    }
);
    "obsidian-card-view-mode" = (
    let
        baseUrl = "https://github.com/yo-goto/obsidian-card-view-mode/releases/download/0.4.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-card-view-mode";
        version = "0.4.10";
        outputHash = "sha256-/YjHH25Z2p5gV8TFkXiSTEPryRh1ZjlF4owanz4D3Xk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable to view your notes as cards.'';
            homepage = "https://github.com/yo-goto/obsidian-card-view-mode";
        };
    }
);
    "obsidian-card-view-switcher-plugin" = (
    let
        baseUrl = "https://github.com/qawatake/obsidian-card-view-switcher-plugin/releases/download/0.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-card-view-switcher-plugin";
        version = "0.4.3";
        outputHash = "sha256-yauc2tC/h2r2vQVhsVmpFGci1j5bcH1iXzdE4g0mYok=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quick switcher with card view.'';
            homepage = "https://github.com/qawatake/obsidian-card-view-switcher-plugin";
        };
    }
);
    "obsidian-carry-forward" = (
    let
        baseUrl = "https://github.com/jglev/obsidian-carry-forward/releases/download/1.4.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-carry-forward";
        version = "1.4.5";
        outputHash = "sha256-wjhKlCbVS2dvfZJoDtJuahF8417uMqqnz7hYiO2WPo8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy text from a note, linking back to its copied source, or copy a link to a note block.'';
            homepage = "https://github.com/jglev/obsidian-carry-forward";
        };
    }
);
    "obsidian-charts" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-charts/releases/download/3.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-charts";
        version = "3.9.0";
        outputHash = "sha256-a/UjpkDCta9nBI8G6V2Ea4n9NlE+cOVF7j9+wSX6Y0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily create interactive charts in your notes.'';
            homepage = "https://github.com/phibr0/obsidian-charts";
        };
    }
);
    "obsidian-chartsview-plugin" = (
    let
        baseUrl = "https://github.com/caronchen/obsidian-chartsview-plugin/releases/download/1.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-chartsview-plugin";
        version = "1.2.8";
        outputHash = "sha256-g7Fs9WylL2CwSRqvK2Bv13KBxi2xMJqO2gaqtmuCAns=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize data from your notes with plots and graphs.'';
            homepage = "https://github.com/caronchen/obsidian-chartsview-plugin";
        };
    }
);
    "obsidian-chat-view" = (
    let
        baseUrl = "https://github.com/adifyr/obsidian-chat-view/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-chat-view";
        version = "1.6.0";
        outputHash = "sha256-eYZkG8BHUjCJa6bXFR8raIkKd8LgXRwx+yTy9WJoNCg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Chat View lets you quickly and easily create elegant Chat UIs in your Markdown Files.'';
            homepage = "https://github.com/adifyr/obsidian-chat-view";
        };
    }
);
    "obsidian-checkbox3states-plugin" = (
    let
        baseUrl = "https://github.com/hrenaud/obsidian-checkbox3states-plugin/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-checkbox3states-plugin";
        version = "1.1.5";
        outputHash = "sha256-m/lnUQX7Qj7ycMAd6MpO81birfEC9rX4em+jjxZ+EGQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A third state to checkbox list.'';
            homepage = "https://github.com/hrenaud/obsidian-checkbox3states-plugin";
        };
    }
);
    "obsidian-checklist-plugin" = (
    let
        baseUrl = "https://github.com/delashum/obsidian-checklist-plugin/releases/download/2.2.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-checklist-plugin";
        version = "2.2.14";
        outputHash = "sha256-v16dmPHVwLMEzmXHMGUCltji0kGuQmCOIP8RPjO/FLw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Consolidate checklists across all files into a single view.'';
            homepage = "https://github.com/delashum/obsidian-checklist-plugin";
        };
    }
);
    "obsidian-checklist-reset" = (
    let
        baseUrl = "https://github.com/lhansford/obsidian-checklist-reset/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-checklist-reset";
        version = "1.4.2";
        outputHash = "sha256-+Mnf94ld8rNs9b1DsOZ0jZghsblEOkjdGRFzHWvHSWI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a command to reset the state of any checklists in a document.'';
            homepage = "https://github.com/lhansford/obsidian-checklist-reset";
        };
    }
);
    "obsidian-chessboard" = (
    let
        baseUrl = "https://github.com/THeK3nger/obsidian-chessboard/releases/download/0.16.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-chessboard";
        version = "0.16.0";
        outputHash = "sha256-Neds+wt+IFH6X5G/AYgGuE2bZ04eX3myEhnzEWs5Lx8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render chess positions diagrams in note preview.'';
            homepage = "https://github.com/THeK3nger/obsidian-chessboard";
        };
    }
);
    "obsidian-chevereto-image-uploader" = (
    let
        baseUrl = "https://github.com/kkzzhizhou/obsidian-chevereto-image-uploader/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-chevereto-image-uploader";
        version = "1.0.1";
        outputHash = "sha256-oMx04Q9XAmOYtlvrv4Ktf2tpoXMLEYRiLQ/+ca8l/6o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images in your clipboard to Chevereto.'';
            homepage = "https://github.com/kkzzhizhou/obsidian-chevereto-image-uploader";
        };
    }
);
    "obsidian-chorded-hotkeys" = (
    let
        baseUrl = "https://github.com/TreyMeyers/obsidian-chorded-hotkeys/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-chorded-hotkeys";
        version = "1.1.0";
        outputHash = "sha256-Zxh4hn7mZZ1WA+k3zR5ulho5qb7I62+KzeXjLvGTeTc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Type multiple letters at the same time to trigger text insertion, template insertion, or command execution.'';
            homepage = "https://github.com/TreyMeyers/obsidian-chorded-hotkeys";
        };
    }
);
    "obsidian-circuitjs" = (
    let
        baseUrl = "https://github.com/StevenGann/obsidian-circuitjs/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-circuitjs";
        version = "1.2.2";
        outputHash = "sha256-l3JUFoKEuxkFEs+3otVtZDHzZkO2hXqImERMpikftyY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed CircuitJS circuit simulations into notes.'';
            homepage = "https://github.com/StevenGann/obsidian-circuitjs";
        };
    }
);
    "obsidian-citation-plugin" = (
    let
        baseUrl = "https://github.com/hans/obsidian-citation-plugin/releases/download/0.4.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-citation-plugin";
        version = "0.4.5";
        outputHash = "sha256-8jTh/9HH8XBIthai94XbgSOHj10GOViu66iFudnU5Yo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically search and insert citations from a Zotero library.'';
            homepage = "https://github.com/hans/obsidian-citation-plugin";
        };
    }
);
    "obsidian-clipper" = (
    let
        baseUrl = "https://github.com/jgchristopher/obsidian-clipper/releases/download/1.0.0-beta.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-clipper";
        version = "1.0.0-beta.9";
        outputHash = "sha256-7n1ovV3NxkEyQyyGZKmF9ZNPEKJqWUMWSMdpRAEEfSY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Capture highlights from the web.'';
            homepage = "https://github.com/jgchristopher/obsidian-clipper";
        };
    }
);
    "obsidian-cloudinary-uploader" = (
    let
        baseUrl = "https://github.com/jordanhandy/obsidian-cloudinary-uploader/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-cloudinary-uploader";
        version = "1.0.2";
        outputHash = "sha256-JdhowLEaV6bMMkbnfr5g7BrcAHPw+vq6dJwIGO+N4Y4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload pasted images from your clipboard to Cloudinary.'';
            homepage = "https://github.com/jordanhandy/obsidian-cloudinary-uploader";
        };
    }
);
    "obsidian-code-preview" = (
    let
        baseUrl = "https://github.com/hankjs/obsidian-code-preview/releases/download/1.3.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-code-preview";
        version = "1.3.11";
        outputHash = "sha256-OwBUC9UhtM9sNHp6YU7amTraTNKHm7x1VLOPC/fz7kY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Code block preview by file path.'';
            homepage = "https://github.com/hankjs/obsidian-code-preview";
        };
    }
);
    "obsidian-codeblock-labels" = (
    let
        baseUrl = "https://github.com/stbowers/obsidian-codeblock-labels/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-codeblock-labels";
        version = "1.0.0";
        outputHash = "sha256-dML39JwjDGdX76VsArqLrT6ZEO1FBDg7Pq41bIPqh0I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add labels to fenced code blocks.'';
            homepage = "https://github.com/stbowers/obsidian-codeblock-labels";
        };
    }
);
    "obsidian-collapse-all-plugin" = (
    let
        baseUrl = "https://github.com/nathonius/obsidian-collapse-all/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-collapse-all-plugin";
        version = "2.1.0";
        outputHash = "sha256-lGxaqeElUCyFb4GyR9TT5zDXRJHBiSBiPPdEVS/NqRA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extend collapse/expand all with commands that can be bound to hotkeys.'';
            homepage = "https://github.com/nathonius/obsidian-collapse-all";
        };
    }
);
    "obsidian-columns" = (
    let
        baseUrl = "https://github.com/tnichols217/obsidian-columns/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-columns";
        version = "1.6.1";
        outputHash = "sha256-WENFlpWjzkTWoy43NHBBv96Yn9CzJVRMoIlSNd//TNA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create columns in Markdown.'';
            homepage = "https://github.com/tnichols217/obsidian-columns";
        };
    }
);
    "obsidian-command-alias-plugin" = (
    let
        baseUrl = "https://github.com/yajamon/obsidian-command-alias-plugin/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-command-alias-plugin";
        version = "2.1.1";
        outputHash = "sha256-W/lM2vw/5Cz+ze/Xq5FxyyXnCRkLj7NZJ8pKqCkFmFk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Give aliases to commands.'';
            homepage = "https://github.com/yajamon/obsidian-command-alias-plugin";
        };
    }
);
    "obsidian-command-palette-minus-plugin" = (
    let
        baseUrl = "https://github.com/qawatake/obsidian-command-palette-minus-plugin/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-command-palette-minus-plugin";
        version = "0.1.1";
        outputHash = "sha256-lpIU25u8mxAyBigcjcc1n83ShzJdpMu5WzgbTH2loFs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Command palette without unwanted commands.'';
            homepage = "https://github.com/qawatake/obsidian-command-palette-minus-plugin";
        };
    }
);
    "obsidian-completr" = (
    let
        baseUrl = "https://github.com/tth05/obsidian-completr/releases/download/3.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-completr";
        version = "3.2.0";
        outputHash = "sha256-K82igiVuw7JYNz3hL5uJDk+gEyvJzhOLr1D3wX5FWbw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Advanced auto-completion for LaTeX, frontmatter, and standard writing.'';
            homepage = "https://github.com/tth05/obsidian-completr";
        };
    }
);
    "obsidian-contacts" = (
    let
        baseUrl = "https://github.com/vbeskrovnov/obsidian-contacts/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-contacts";
        version = "1.2.0";
        outputHash = "sha256-B9R2AKM59Sv/C2z4nk/QfEefPkpnWp5jnkOgRpq/Sv8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage and organize contacts.'';
            homepage = "https://github.com/vbeskrovnov/obsidian-contacts";
        };
    }
);
    "obsidian-contextual-typography" = (
    let
        baseUrl = "https://github.com/mgmeyers/obsidian-contextual-typography/releases/download/2.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-contextual-typography";
        version = "2.2.5";
        outputHash = "sha256-uu8m+Vy3jteQqQOLbE5y8sTeFrb8PUyP28hIMaeB5ak=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a data-tag-name attribute to all top-level divs in preview mode containing the child"s tag name, allowing contextual typography styling.'';
            homepage = "https://github.com/mgmeyers/obsidian-contextual-typography";
        };
    }
);
    "obsidian-copy-block-link" = (
    let
        baseUrl = "https://github.com/mgmeyers/obsidian-copy-block-link/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-copy-block-link";
        version = "1.0.4";
        outputHash = "sha256-LQvrw+YInrSIfk9GBegdUenGlWo9pDzBR/Q4n7zpXPc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get links to blocks and headings from the right-click menu.'';
            homepage = "https://github.com/mgmeyers/obsidian-copy-block-link";
        };
    }
);
    "obsidian-copy-search-url" = (
    let
        baseUrl = "https://github.com/czottmann/obsidian-copy-search-url/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-copy-search-url";
        version = "1.2.1";
        outputHash = "sha256-uhQZSukSjkFj6mEPGTes8U9KgdGBQwQC5847NIPg9bc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a button to the search view to copy the search URL.'';
            homepage = "https://github.com/czottmann/obsidian-copy-search-url";
        };
    }
);
    "obsidian-core-search-assistant-plugin" = (
    let
        baseUrl = "https://github.com/qawatake/obsidian-core-search-assistant-plugin/releases/download/0.9.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-core-search-assistant-plugin";
        version = "0.9.4";
        outputHash = "sha256-IP+7aQ6sL8hrKx1nlACLokFaQXzWdOtRl7rCjWLVsD4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance built-in search: keyboard interface, card preview, bigger preview.'';
            homepage = "https://github.com/qawatake/obsidian-core-search-assistant-plugin";
        };
    }
);
    "obsidian-crypto-lookup" = (
    let
        baseUrl = "https://github.com/kinabalu/obsidian-crypto-lookup/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-crypto-lookup";
        version = "0.0.2";
        outputHash = "sha256-APrY4PSjygIQJ0CJxKjWs7DupLQmDT/VZO6Uoq0h2VE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use the Cryptonator API to pull prices for crypto in a target currency.'';
            homepage = "https://github.com/kinabalu/obsidian-crypto-lookup";
        };
    }
);
    "obsidian-csv-table" = (
    let
        baseUrl = "https://github.com/coddingtonbear/obsidian-csv-table/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-csv-table";
        version = "1.2.0";
        outputHash = "sha256-EUp7JqP83cyyYoiXtHN46T7GdaxPwwD7B+YNcgQWNlc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render CSV data as a table within your notes.'';
            homepage = "https://github.com/coddingtonbear/obsidian-csv-table";
        };
    }
);
    "obsidian-cursor-location-plugin" = (
    let
        baseUrl = "https://github.com/spslater/obsidian-cursor-location-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-cursor-location-plugin";
        version = "1.0.0";
        outputHash = "sha256-qmkopVzMr861soXVexLti7L7Qw6nJLrPhknZuMEZ+wU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the location of the cursor (character and line number).'';
            homepage = "https://github.com/spslater/obsidian-cursor-location-plugin";
        };
    }
);
    "obsidian-custom-attachment-location" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-custom-attachment-location/releases/download/10.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-custom-attachment-location";
        version = "10.3.4";
        outputHash = "sha256-WtodPlanxKP/lL6foxHwlvQYr6UQxUwSOdhn/g6bUuU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize attachment location with variables($filename, $data, etc) like Typora.'';
            homepage = "https://github.com/mnaoumov/obsidian-custom-attachment-location";
        };
    }
);
    "obsidian-custom-file-extensions-plugin" = (
    let
        baseUrl = "https://github.com/MeepTech/obsidian-custom-file-extensions-plugin/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-custom-file-extensions-plugin";
        version = "1.2.1";
        outputHash = "sha256-mc71VLKoHoAmoXmuBjOs+SLatpE5OhVn6FsmBvhMvis=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple and modular control over what views open what file extensions from the app settings.'';
            homepage = "https://github.com/MeepTech/obsidian-custom-file-extensions-plugin";
        };
    }
);
    "obsidian-custom-frames" = (
    let
        baseUrl = "https://github.com/Ellpeck/ObsidianCustomFrames/releases/download/2.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-custom-frames";
        version = "2.5.0";
        outputHash = "sha256-oXwuwMyTb8oak/f76By050SFXds3Cr5TllL68VQhcl8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn web apps into panes using iframes with custom styling. Also comes with presets for Google Keep, Todoist and more.'';
            homepage = "https://github.com/Ellpeck/ObsidianCustomFrames";
        };
    }
);
    "obsidian-daf-yomi" = (
    let
        baseUrl = "https://github.com/lyonsquark/obsidian-daf-yomi/releases/download/1.15.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-daf-yomi";
        version = "1.15.2";
        outputHash = "sha256-OFKeqHpHqyINKS6AEkyMFUhNeWwg4LGIVWNfxEjEMcA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prepare Daf Yomi notes.'';
            homepage = "https://github.com/lyonsquark/obsidian-daf-yomi";
        };
    }
);
    "obsidian-daily-named-folder" = (
    let
        baseUrl = "https://github.com/NemoAndrea/obsidian-daily-named-folder/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-daily-named-folder";
        version = "0.1.6";
        outputHash = "sha256-22eN8Ro/R/34miV8XuwxGguBNEbw2Sr1G9W7vccnbWc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Like daily notes, but nested in a daily folder with a oneline summary. Better for attachment management & glanceability.'';
            homepage = "https://github.com/NemoAndrea/obsidian-daily-named-folder";
        };
    }
);
    "obsidian-daily-note-outline" = (
    let
        baseUrl = "https://github.com/iiz00/obsidian-daily-note-outline/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-daily-note-outline";
        version = "1.5.0";
        outputHash = "sha256-rPiGmd2bXlgFRVwy6ojbxIbfyTQfgIycCfVWdLXZs/E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a custom view which shows outline of multiple daily notes with headings, links, tags and list items.'';
            homepage = "https://github.com/iiz00/obsidian-daily-note-outline";
        };
    }
);
    "obsidian-daily-notes-opener" = (
    let
        baseUrl = "https://github.com/reorx/obsidian-daily-notes-opener/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-daily-notes-opener";
        version = "2.0.2";
        outputHash = "sha256-JdCisU46wzsQ8k3pZfGNqlJjuj5QorP56r7D7mcEwNI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily open daily notes and periodic notes in new pane; customize periodic notes background; quick append new line to daily notes.'';
            homepage = "https://github.com/reorx/obsidian-daily-notes-opener";
        };
    }
);
    "obsidian-daily-notes-viewer" = (
    let
        baseUrl = "https://github.com/johnsonhong997/obsidian-daily-notes-viewer/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-daily-notes-viewer";
        version = "0.2.1";
        outputHash = "sha256-c2XqBD0WwJeVSh3pOI19ip7UyotXn++AwIC2m7ru7yI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Help you to view some recent daily notes on one page.'';
            homepage = "https://github.com/johnsonhong997/obsidian-daily-notes-viewer";
        };
    }
);
    "obsidian-daily-stats" = (
    let
        baseUrl = "https://github.com/dhruvik7/obsidian-daily-stats/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-daily-stats";
        version = "1.0.4";
        outputHash = "sha256-eIwCBH9/AE8gJOadIYlAyQ/SLjwSevhFSxD/KhUtwqw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track your daily word count.'';
            homepage = "https://github.com/dhruvik7/obsidian-daily-stats";
        };
    }
);
    "obsidian-dangling-links" = (
    let
        baseUrl = "https://github.com/graydon/obsidian-dangling-links/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dangling-links";
        version = "0.3.0";
        outputHash = "sha256-p/5M5/KwrgY+DmuGps7x5y1oXUbTVSO5dvEZMoHTzkc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a sidebar showing any dangling links in a vault. Dangling links are orphaned links that don"t point to anything in the vault.'';
            homepage = "https://github.com/graydon/obsidian-dangling-links";
        };
    }
);
    "obsidian-dashing-cursor" = (
    let
        baseUrl = "https://github.com/9r0x/obsidian-dashing-cursor/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dashing-cursor";
        version = "0.0.2";
        outputHash = "sha256-PsbmuaoL06TMZJdU/DUs0oe3/7Nkp4fVzHxyVDGLwzA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable a dashing cursor that follows the page scroll.'';
            homepage = "https://github.com/9r0x/obsidian-dashing-cursor";
        };
    }
);
    "obsidian-dataset-aid" = (
    let
        baseUrl = "https://github.com/connerohnesorge/Text-Dataset-Aid-Plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dataset-aid";
        version = "1.0.1";
        outputHash = "sha256-vEmIZvLEO+22pk1KsCVjboH3sMtoFzGCa7UKCinEegs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create personal datasets for fine-tuning language models.'';
            homepage = "https://github.com/connerohnesorge/Text-Dataset-Aid-Plugin";
        };
    }
);
    "obsidian-day-and-night" = (
    let
        baseUrl = "https://github.com/CyberT17/obsidian-day-and-night/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-day-and-night";
        version = "1.2.1";
        outputHash = "sha256-u9QbRZ93IC4lZcN3/ANWt6bkxy+IrCo+Uf50OR4QvW4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically toggle themes between day theme and night theme on a set time schedule.'';
            homepage = "https://github.com/CyberT17/obsidian-day-and-night";
        };
    }
);
    "obsidian-day-planner" = (
    let
        baseUrl = "https://github.com/ivan-lednev/obsidian-day-planner/releases/download/0.28.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-day-planner";
        version = "0.28.0";
        outputHash = "sha256-ZbGh7hlZ0YVu8mkefVqnmRzAI9OkhLTA8DjS7ZRN6AU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Day planning from a task list in a Markdown note with enhanced time block functionality.'';
            homepage = "https://github.com/ivan-lednev/obsidian-day-planner";
        };
    }
);
    "obsidian-desmos" = (
    let
        baseUrl = "https://github.com/Nigecat/obsidian-desmos/releases/download/0.6.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-desmos";
        version = "0.6.8";
        outputHash = "sha256-5wqClevq/aXq3SmKstxwlG2pkCpaZsBQQ4g/7F4EM4E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed Desmos graphs into your notes.'';
            homepage = "https://github.com/Nigecat/obsidian-desmos";
        };
    }
);
    "obsidian-diagrams-net" = (
    let
        baseUrl = "https://github.com/jensmtg/obsidian-diagrams-net/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-diagrams-net";
        version = "1.0.4";
        outputHash = "sha256-iLwVPlmbloE9QZubpfDYtPy4aKWK0vbdMIf9CBOV8uQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable diagrams.net (previously draw.io) type diagrams, with the diagrams.net embedded editor.'';
            homepage = "https://github.com/jensmtg/obsidian-diagrams-net";
        };
    }
);
    "obsidian-dialogue-plugin" = (
    let
        baseUrl = "https://github.com/holubj/obsidian-dialogue-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dialogue-plugin";
        version = "1.0.2";
        outputHash = "sha256-mlY6oR0c4Z4HNC6QYH4GiwKhJR5fvDnQkRF4/3A5ZSw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create dialogues in Markdown.'';
            homepage = "https://github.com/holubj/obsidian-dialogue-plugin";
        };
    }
);
    "obsidian-dice-roller" = (
    let
        baseUrl = "https://github.com/Obsidian-TTRPG-Community/dice-roller/releases/download/11.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dice-roller";
        version = "11.4.2";
        outputHash = "sha256-1IE0uAEbeS070VXc8g6KfIQdIb6SmxWU5Ahgn4+lozY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a little randomness to your notes!'';
            homepage = "https://github.com/Obsidian-TTRPG-Community/dice-roller";
        };
    }
);
    "obsidian-dictionary-plugin" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-dictionary/releases/download/2.22.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dictionary-plugin";
        version = "2.22.0";
        outputHash = "sha256-RK0sGF9Ttf3mKSgm+q00zepeVunvZ76DBbsCa4cglIc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A multilingual dictionary that shows word definitions in the sidebar and popover synonyms.'';
            homepage = "https://github.com/phibr0/obsidian-dictionary";
        };
    }
);
    "obsidian-dirtreeist" = (
    let
        baseUrl = "https://github.com/k4a-l/obsidian-dirtreeist/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dirtreeist";
        version = "0.2.0";
        outputHash = "sha256-pIwYYJIm0EAG0i0wab5JqmyMghp7yvz067A/Qtfhypo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render a directory Structure Diagram from a Markdown lists in codeblock.'';
            homepage = "https://github.com/k4a-l/obsidian-dirtreeist";
        };
    }
);
    "obsidian-discordrpc" = (
    let
        baseUrl = "https://github.com/lukeleppan/obsidian-discordrpc/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-discordrpc";
        version = "1.5.1";
        outputHash = "sha256-6vEfjmmn2h3ZkN2ItwEhIGPZf9klkNKQY+LTg5QNSqo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Update your Discord Status to show your friends what you are working on.'';
            homepage = "https://github.com/lukeleppan/obsidian-discordrpc";
        };
    }
);
    "obsidian-divide-and-conquer" = (
    let
        baseUrl = "https://github.com/mikedidomizio/obsidian-divide-and-conquer/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-divide-and-conquer";
        version = "2.0.0";
        outputHash = "sha256-fJMqEINILSP760Yad43kwR5l53MMqGN1qktWXcQig5k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands for bulk enabling/disabling of plugins. Useful for debugging when you have many plugins.'';
            homepage = "https://github.com/mikedidomizio/obsidian-divide-and-conquer";
        };
    }
);
    "obsidian-douban-plugin" = (
    let
        baseUrl = "https://github.com/Wanxp/obsidian-douban/releases/download/2.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-douban-plugin";
        version = "2.3.3";
        outputHash = "sha256-E3UJGmxvQD9BeETvHJfrI7WBEj87iH82cz5/X/ZapHY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import movies/books/musics/notes/games info data from Douban.'';
            homepage = "https://github.com/Wanxp/obsidian-douban";
        };
    }
);
    "obsidian-doubleshift" = (
    let
        baseUrl = "https://github.com/Qwyntex/doubleshift/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-doubleshift";
        version = "2.3.0";
        outputHash = "sha256-3DkOIUn/df6Z/y7DCAdtnL5cS4Ckshd5RLehOdNqbHY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open the command palette by pressing Shift (or any other key) twice like in IntelliJ and create your own shortcuts.'';
            homepage = "https://github.com/Qwyntex/doubleshift";
        };
    }
);
    "obsidian-dropbox-backups" = (
    let
        baseUrl = "https://github.com/ryanpcmcquen/obsidian-dropbox-backups/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dropbox-backups";
        version = "2.1.0";
        outputHash = "sha256-NtZvhw+zy9nPOrM7VHTKqiSksh3GHc4rqFJqMFTeY0I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automated Dropbox backups of your entire vault.'';
            homepage = "https://github.com/ryanpcmcquen/obsidian-dropbox-backups";
        };
    }
);
    "obsidian-dynamic-background" = (
    let
        baseUrl = "https://github.com/samuelsong70/obsidian-dynamic-background/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dynamic-background";
        version = "1.0.6";
        outputHash = "sha256-TiD9ZArjUARQgq7fNoEZEXK1c5SNu408QJePl5wquoE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adding dynamic background effects to the editor.'';
            homepage = "https://github.com/samuelsong70/obsidian-dynamic-background";
        };
    }
);
    "obsidian-dynamic-embed" = (
    let
        baseUrl = "https://github.com/dabravin/obsidian-dynamic-embed/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dynamic-embed";
        version = "1.1.0";
        outputHash = "sha256-f/jQQd1ji8E9o9KBAfPr77ai/cg500CirMCnBRIt7lE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed snippets, templates and any linkable by delegating the current scope to the embedded file, treating it as content instead of a reference.'';
            homepage = "https://github.com/dabravin/obsidian-dynamic-embed";
        };
    }
);
    "obsidian-dynamic-highlights" = (
    let
        baseUrl = "https://github.com/nothingislost/obsidian-dynamic-highlights/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dynamic-highlights";
        version = "0.3.1";
        outputHash = "sha256-vwZldGyNksRdAEdaJa7ntCj3Oa072efICsyYlydnRiE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamically highlight text based on cursor selection or search query with full regex, mobile, and live preview support.'';
            homepage = "https://github.com/nothingislost/obsidian-dynamic-highlights";
        };
    }
);
    "obsidian-dynbedded" = (
    let
        baseUrl = "https://github.com/MMoMM-org/obsidian-dynbedded/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-dynbedded";
        version = "1.2.1";
        outputHash = "sha256-IRi9z7uP12bXrEqNE9tYJtwROflUnfZ2Ha0q1KYhYsQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamic embeds.'';
            homepage = "https://github.com/MMoMM-org/obsidian-dynbedded";
        };
    }
);
    "obsidian-echarts" = (
    let
        baseUrl = "https://github.com/cumany/obsidian-echarts/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-echarts";
        version = "0.0.3";
        outputHash = "sha256-Kfr07J0OY/DKBOIJHAiXT2/qyplmQSpKOvplIEhrMmU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render echarts.'';
            homepage = "https://github.com/cumany/obsidian-echarts";
        };
    }
);
    "obsidian-editor-shortcuts" = (
    let
        baseUrl = "https://github.com/timhor/obsidian-editor-shortcuts/releases/download/1.14.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-editor-shortcuts";
        version = "1.14.0";
        outputHash = "sha256-Zi9DbeLY9DkXE/LDm4zTgx2EMqxM2BkPmOsjJS2J91U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add keyboard shortcuts (hotkeys) commonly found in code editors such as Visual Studio Code (VS Code) or Sublime Text.'';
            homepage = "https://github.com/timhor/obsidian-editor-shortcuts";
        };
    }
);
    "obsidian-electron-window-tweaker" = (
    let
        baseUrl = "https://github.com/mgmeyers/obsidian-electron-window-tweaker/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-electron-window-tweaker";
        version = "1.0.8";
        outputHash = "sha256-+zYWvn55BECdkbcHQNHNm2YrCHfl1ATC6FLnDKmdr6w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tweak various Electron window settings.'';
            homepage = "https://github.com/mgmeyers/obsidian-electron-window-tweaker";
        };
    }
);
    "obsidian-embedded-code-title" = (
    let
        baseUrl = "https://github.com/tadashi-aikawa/obsidian-embedded-code-title/releases/download/0.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-embedded-code-title";
        version = "0.5.5";
        outputHash = "sha256-tQl2v0A+Ry2Wl19cSYzgSP2tlWx5PbIVBHpz7lmHguM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed a title in code blocks.'';
            homepage = "https://github.com/tadashi-aikawa/obsidian-embedded-code-title";
        };
    }
);
    "obsidian-emoji-toolbar" = (
    let
        baseUrl = "https://github.com/oliveryh/obsidian-emoji-toolbar/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-emoji-toolbar";
        version = "1.0.0";
        outputHash = "sha256-q9GeQ+5rVTiLHWTau1U+UPIi0JTV6nqSqlwvicQg13U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly search for and insert emojis into your editor.'';
            homepage = "https://github.com/oliveryh/obsidian-emoji-toolbar";
        };
    }
);
    "obsidian-emotion-picker" = (
    let
        baseUrl = "https://github.com/dartungar/obsidian-emotion-picker/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-emotion-picker";
        version = "1.2.1";
        outputHash = "sha256-9dz5G9WS93FG7mNx+8XBnW8W4VmKnQDWWnPEqdZkX0w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Choose an emotion from a list to insert into a note.'';
            homepage = "https://github.com/dartungar/obsidian-emotion-picker";
        };
    }
);
    "obsidian-enhancing-export" = (
    let
        baseUrl = "https://github.com/mokeyish/obsidian-enhancing-export/releases/download/1.11.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-enhancing-export";
        version = "1.11.1";
        outputHash = "sha256-eEBD63UkbSpU/zBHx6Qgi8vHcUpoUQG4M1g9nnUeONc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhanced export based on Pandoc. Allows export to formats like HTML, DOCX, ePub and PDF or Hugo.'';
            homepage = "https://github.com/mokeyish/obsidian-enhancing-export";
        };
    }
);
    "obsidian-enhancing-mindmap" = (
    let
        baseUrl = "https://github.com/MarkMindCkm/obsidian-enhancing-mindmap/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-enhancing-mindmap";
        version = "1.2.5";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit mindmaps with Markdown.'';
            homepage = "https://github.com/MarkMindCkm/obsidian-enhancing-mindmap";
        };
    }
);
    "obsidian-epub-plugin" = (
    let
        baseUrl = "https://github.com/caronchen/obsidian-epub-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-epub-plugin";
        version = "1.0.3";
        outputHash = "sha256-oMrMStan1GAkdeIkZdXoHko1al6FnT5JASKX0TIfI7I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open documents with ".epub" file extension.'';
            homepage = "https://github.com/caronchen/obsidian-epub-plugin";
        };
    }
);
    "obsidian-etherpad-plugin" = (
    let
        baseUrl = "https://github.com/egradman/obsidian-etherpad-lite/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-etherpad-plugin";
        version = "1.0.6";
        outputHash = "sha256-wAA1B27kTmDtp8L4Ppfz1PPp2HW9K9o2y3/GIRr0EKE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy and sync notes with an Etherpad Lite server to unlock easy web-based collaboration with others.'';
            homepage = "https://github.com/egradman/obsidian-etherpad-lite";
        };
    }
);
    "obsidian-excalidraw-plugin" = (
    let
        baseUrl = "https://github.com/zsviczian/obsidian-excalidraw-plugin/releases/download/2.22.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-excalidraw-plugin";
        version = "2.22.1";
        outputHash = "sha256-qy78ro+i0Ftn1HLnuRS6vBQb5LjEVM6Y2uPl8WiMdS0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit and view Excalidraw drawings.'';
            homepage = "https://github.com/zsviczian/obsidian-excalidraw-plugin";
        };
    }
);
    "obsidian-excel-to-markdown-table" = (
    let
        baseUrl = "https://github.com/ganesshkumar/obsidian-excel-to-markdown-table/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-excel-to-markdown-table";
        version = "0.4.0";
        outputHash = "sha256-tIs0TNyYcecL5EjJcJL2v3qre0DDwfov8dgUXBnQuRU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste data from Microsoft Excel, Google Sheets, Apple Numbers and LibreOffice Calc as Markdown tables.'';
            homepage = "https://github.com/ganesshkumar/obsidian-excel-to-markdown-table";
        };
    }
);
    "obsidian-export-image" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-export-image/releases/download/2.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-export-image";
        version = "2.4.4";
        outputHash = "sha256-N7+wAJxqdzZ2f/gYdyxbmjxAGLwWzOCQ+ADBeMIHlWE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily convert your article to image.'';
            homepage = "https://github.com/zhouhua/obsidian-export-image";
        };
    }
);
    "obsidian-export-to-tex" = (
    let
        baseUrl = "https://github.com/raineszm/obsidian-export-to-tex/releases/download/0.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-export-to-tex";
        version = "0.4.2";
        outputHash = "sha256-S2Knpi1b18S8B4XqHTxBWVzOovJuZBoYrpRed7IqEvw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export vault files in a format amenable to pasting into a TeX document.'';
            homepage = "https://github.com/raineszm/obsidian-export-to-tex";
        };
    }
);
    "obsidian-extlnkhelper-plugin" = (
    let
        baseUrl = "https://github.com/nakalsio/obsidian-danpung/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-extlnkhelper-plugin";
        version = "0.8.0";
        outputHash = "sha256-YGGxF7bgt/AmkUwgR/BkBur5j3iufepw5qhmQXICtZ8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makie inserting external links easier to your notes.'';
            homepage = "https://github.com/nakalsio/obsidian-danpung";
        };
    }
);
    "obsidian-extract-pdf-annotations" = (
    let
        baseUrl = "https://github.com/munach/obsidian-extract-pdf-annotations/releases/download/1.9.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-extract-pdf-annotations";
        version = "1.9.4";
        outputHash = "sha256-7AO8D/UBzo/Ln1oWHan9J4yY5Ok9rjSjHPbHJwHAGZM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract PDF annotations (notes and highlights) and sort them by topic.'';
            homepage = "https://github.com/munach/obsidian-extract-pdf-annotations";
        };
    }
);
    "obsidian-extract-pdf-highlights" = (
    let
        baseUrl = "https://github.com/akaalias/obsidian-extract-pdf-highlights/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-extract-pdf-highlights";
        version = "0.0.4";
        outputHash = "sha256-OQtCCPs82DppPDWOmyUHZW8VSvX4FcPbCon5J5s7Sbk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract highlights, underlines and annotations from your PDFs.'';
            homepage = "https://github.com/akaalias/obsidian-extract-pdf-highlights";
        };
    }
);
    "obsidian-file-cleaner" = (
    let
        baseUrl = "https://github.com/johnsonhong997/obsidian-file-cleaner/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-file-cleaner";
        version = "0.1.5";
        outputHash = "sha256-JKxdpKD6nw8WC8ePvMO+QB3klXDmXOuK09ZpBVEKHzU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Clean empty files and unused attachments in your vault.'';
            homepage = "https://github.com/johnsonhong997/obsidian-file-cleaner";
        };
    }
);
    "obsidian-file-color" = (
    let
        baseUrl = "https://github.com/ecustic/obsidian-file-color/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-file-color";
        version = "1.1.0";
        outputHash = "sha256-PirgZyE15JLTmO+YPTdnH6++qC5LYufzCeyALrpD61E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set colors on folders and files in the file tree.'';
            homepage = "https://github.com/ecustic/obsidian-file-color";
        };
    }
);
    "obsidian-file-cooker" = (
    let
        baseUrl = "https://github.com/ivaneye/obsidian-files-cooker/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-file-cooker";
        version = "2.0.0";
        outputHash = "sha256-+r8Br2HacN6YBMLMX7zO0Q4fGxs+9iC1n9wa+FajHRo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Deal batch notes from search results,current file, or Dataview query string.'';
            homepage = "https://github.com/ivaneye/obsidian-files-cooker";
        };
    }
);
    "obsidian-file-info-plugin" = (
    let
        baseUrl = "https://github.com/CattailNu/obsidian-file-info-panel-plugin/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-file-info-plugin";
        version = "1.2.5";
        outputHash = "sha256-TsU6RUWFlDCaQNpjFqXEDJmOLtjS6OtiJoxQ3lnND5g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a File Info view that displays the active file"s date created, date modified, file size, and links to open the file in its native application and to open the file"s folder.'';
            homepage = "https://github.com/CattailNu/obsidian-file-info-panel-plugin";
        };
    }
);
    "obsidian-file-link" = (
    let
        baseUrl = "https://github.com/marcjulianschwarz/obsidian-file-link/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-file-link";
        version = "1.6.0";
        outputHash = "sha256-WZG+L56mauTyuFXDCW9YCDEINm/P1xo+/foRGH1o9u4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add better external file links to notes.'';
            homepage = "https://github.com/marcjulianschwarz/obsidian-file-link";
        };
    }
);
    "obsidian-file-path-to-uri" = (
    let
        baseUrl = "https://github.com/MichalBures/obsidian-file-path-to-uri/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-file-path-to-uri";
        version = "1.4.1";
        outputHash = "sha256-MwxqrUlj3UHU6VznhW5xnZrH21CxZFpLBCtIbt2bNNo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert file path to URI for easier use of links to local files outside of Obsidian.'';
            homepage = "https://github.com/MichalBures/obsidian-file-path-to-uri";
        };
    }
);
    "obsidian-filename-emoji-remover" = (
    let
        baseUrl = "https://github.com/YTolun/obsidian-filename-emoji-remover/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-filename-emoji-remover";
        version = "1.0.1";
        outputHash = "sha256-xFcc3JS3N77LfjUnM66i1CzujXCoB5zZZYMPQPloytQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically remove emojis from filenames. Main purpose is to get rid of Dropbox sync issues for Readwise imported content.'';
            homepage = "https://github.com/YTolun/obsidian-filename-emoji-remover";
        };
    }
);
    "obsidian-filename-heading-sync" = (
    let
        baseUrl = "https://github.com/dvcrn/obsidian-filename-heading-sync/releases/download/1.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-filename-heading-sync";
        version = "1.11.0";
        outputHash = "sha256-UQOpGwxauZO42l/OXseuqzb7NsRyCDB4nrbbBOb04BI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep the filename with the first heading of a file in sync.'';
            homepage = "https://github.com/dvcrn/obsidian-filename-heading-sync";
        };
    }
);
    "obsidian-find-and-replace-in-selection" = (
    let
        baseUrl = "https://github.com/TClark1011/obsidian-find-and-replace-in-selection/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-find-and-replace-in-selection";
        version = "1.1.0";
        outputHash = "sha256-WIQCqm9/OFN2vK2lnJCYlAjVPMqgdOoWUmHDl0kXwuk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replace text within your current selection.'';
            homepage = "https://github.com/TClark1011/obsidian-find-and-replace-in-selection";
        };
    }
);
    "obsidian-flexible-pomo" = (
    let
        baseUrl = "https://github.com/grassbl8d/flexible-pomo-obsidian/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-flexible-pomo";
        version = "0.6.0";
        outputHash = "sha256-X+ejBzVnk9Ia/dWVIoqfWcqelcrY5PZ/1oVQzvBRQd4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a pomodoro timer to your status bar. This pomodoro has additional options such as early log and extend.'';
            homepage = "https://github.com/grassbl8d/flexible-pomo-obsidian";
        };
    }
);
    "obsidian-focus-mode" = (
    let
        baseUrl = "https://github.com/ryanpcmcquen/obsidian-focus-mode/releases/download/1.11.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-focus-mode";
        version = "1.11.5";
        outputHash = "sha256-e8K0F7yvtSFwa1gdi1bamzPoTMp4m2sSGw0p5z2SA1M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a toggle to hide interface elements so you can focus on your note.'';
            homepage = "https://github.com/ryanpcmcquen/obsidian-focus-mode";
        };
    }
);
    "obsidian-focus-plugin" = (
    let
        baseUrl = "https://github.com/nagi1999a/obsidian-focus-plugin/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-focus-plugin";
        version = "1.3.2";
        outputHash = "sha256-IsZr+2Ye2zmNiGYLGIuWl9nwcZuoAhb47gHGw9gdBIU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight and focus on the currently selected heading.'';
            homepage = "https://github.com/nagi1999a/obsidian-focus-plugin";
        };
    }
);
    "obsidian-folder-focus-mode" = (
    let
        baseUrl = "https://github.com/grochowski/obsidian-folder-focus-mode/releases/download/0.9.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-folder-focus-mode";
        version = "0.9.13";
        outputHash = "sha256-rmgF4LgmL8cbgDzXYYDYDXdXoZFgZYbpOM+18aUsm6o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Focus the file explorer on chosen folder and its files and subdirectories, while hiding all the other elements.'';
            homepage = "https://github.com/grochowski/obsidian-folder-focus-mode";
        };
    }
);
    "obsidian-folder-index" = (
    let
        baseUrl = "https://github.com/turulix/obsidian-folder-index/releases/download/1.0.30";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-folder-index";
        version = "1.0.30";
        outputHash = "sha256-ihfN/Y/r/EFceNecnVsm9YeRPRjd2jZHynaTELJ27Nw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically generate a Map of Content for the current Folder and Subfolders.'';
            homepage = "https://github.com/turulix/obsidian-folder-index";
        };
    }
);
    "obsidian-footnotes" = (
    let
        baseUrl = "https://github.com/MichaBrugger/obsidian-footnotes/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-footnotes";
        version = "0.1.3";
        outputHash = "sha256-BDtafgJ0MDQGIeY4yc6bpq4atuSEqicM5Dsyzc+Q+TY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makes creating footnotes more fun!'';
            homepage = "https://github.com/MichaBrugger/obsidian-footnotes";
        };
    }
);
    "obsidian-format-code" = (
    let
        baseUrl = "https://github.com/iVariable/Obsidian-Format-Code/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-format-code";
        version = "0.0.1";
        outputHash = "sha256-kNjDJlhaZOcOkVC1qQ+lBob4lArKNM9wQlWTUITh28s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands to format code (internally uses prettier).'';
            homepage = "https://github.com/iVariable/Obsidian-Format-Code";
        };
    }
);
    "obsidian-front-matter-title-plugin" = (
    let
        baseUrl = "https://github.com/snezhig/obsidian-front-matter-title/releases/download/3.13.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-front-matter-title-plugin";
        version = "3.13.1";
        outputHash = "sha256-D1OL2SSbfQCbdqOMxseyY6v4dViEfFOSEYvsUtbpv18=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define a title in frontmatter to be displayed as the filename.'';
            homepage = "https://github.com/snezhig/obsidian-front-matter-title";
        };
    }
);
    "obsidian-frontmatter-tag-suggest" = (
    let
        baseUrl = "https://github.com/jmilldotdev/obsidian-frontmatter-tag-suggest/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-frontmatter-tag-suggest";
        version = "0.4.1";
        outputHash = "sha256-a9Pve6q9bL/QnlohbMgiF7VN1AAWitPoRmbsBr8rFsU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Autocomplete tags in the frontmatter tags field.'';
            homepage = "https://github.com/jmilldotdev/obsidian-frontmatter-tag-suggest";
        };
    }
);
    "obsidian-full-calendar" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-full-calendar/releases/download/0.10.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-full-calendar";
        version = "0.10.7";
        outputHash = "sha256-8yQKHDkZEx/VmZa9Hx9bli9RI5yoRSj05+PGQ+dadlI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep events and manage your calendar alongside all your other notes in your vault.'';
            homepage = "https://github.com/obsidian-community/obsidian-full-calendar";
        };
    }
);
    "obsidian-fullscreen-plugin" = (
    let
        baseUrl = "https://github.com/Razumihin/obsidian-fullscreen-plugin/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-fullscreen-plugin";
        version = "0.1.2";
        outputHash = "sha256-JulRIt001S9JfE1/0LildlKO9m9GCgMylB9YDAuQiO8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a command to view a single document leaf in a fullscreen focus mode.'';
            homepage = "https://github.com/Razumihin/obsidian-fullscreen-plugin";
        };
    }
);
    "obsidian-functionplot" = (
    let
        baseUrl = "https://github.com/leonhma/obsidian-functionplot/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-functionplot";
        version = "1.2.1";
        outputHash = "sha256-aCjFVWuuuWWXnMN7yaGJ3fQryzyGAsjDtuTbSjNd9qg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render mathematical functions in a Markdown code block.'';
            homepage = "https://github.com/leonhma/obsidian-functionplot";
        };
    }
);
    "obsidian-furigana" = (
    let
        baseUrl = "https://github.com/uonr/obsidian-furigana/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-furigana";
        version = "0.0.3";
        outputHash = "sha256-oOVNM2Q/EmJn+ChKk3PhLGrvmo8AmudeoBxut9TAKoM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display furigana using <ruby> syntax.'';
            homepage = "https://github.com/uonr/obsidian-furigana";
        };
    }
);
    "obsidian-fuzzytag" = (
    let
        baseUrl = "https://github.com/adriandersen/obsidian-fuzzytag/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-fuzzytag";
        version = "1.1.0";
        outputHash = "sha256-EKPYi1tx1k9epgr+RWg8U5bzjfx7KSBkPP/J6QdkMjs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fuzzy match autocomplete tags in frontmatter.'';
            homepage = "https://github.com/adriandersen/obsidian-fuzzytag";
        };
    }
);
    "obsidian-gist" = (
    let
        baseUrl = "https://github.com/linjunpop/obsidian-gist/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-gist";
        version = "0.10.0";
        outputHash = "sha256-upJsVNLjL1UvGrL0SSkxZAivCHvgcRAtc8G7LzfyxeM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the GitHub Gist.'';
            homepage = "https://github.com/linjunpop/obsidian-gist";
        };
    }
);
    "obsidian-git" = (
    let
        baseUrl = "https://github.com/Vinzent03/obsidian-git/releases/download/2.38.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-git";
        version = "2.38.2";
        outputHash = "sha256-M9DaGqa2ILEJ0GMJGSqry0VXDn5HYRkl1sfxpsUh9bQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate Git version control with automatic backup and other advanced features.'';
            homepage = "https://github.com/Vinzent03/obsidian-git";
        };
    }
);
    "obsidian-gitlab-issues" = (
    let
        baseUrl = "https://github.com/benr77/obsidian-gitlab-issues/releases/download/0.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-gitlab-issues";
        version = "0.1.10";
        outputHash = "sha256-EqJrzikVJAjPmzn7R+ABi65sLgmN5QooVOFo7E2QLlI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Gitlab issues.'';
            homepage = "https://github.com/benr77/obsidian-gitlab-issues";
        };
    }
);
    "obsidian-global-hotkeys" = (
    let
        baseUrl = "https://github.com/mjessome/obsidian-global-hotkeys/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-global-hotkeys";
        version = "0.1.2";
        outputHash = "sha256-vzYFALYauV50cr75IYgyvybmXEuYcWeku+Vl45q2gys=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Configurable system-wide hotkeys for running commands.'';
            homepage = "https://github.com/mjessome/obsidian-global-hotkeys";
        };
    }
);
    "obsidian-go-to-line" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-go-to-line/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-go-to-line";
        version = "0.0.2";
        outputHash = "sha256-LipUB3XJd9sWhT9aQqIAW6rHb3uchUjO4GXlMd6Mml8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a Go to Line command.'';
            homepage = "https://github.com/phibr0/obsidian-go-to-line";
        };
    }
);
    "obsidian-golinks" = (
    let
        baseUrl = "https://github.com/xavdid/obsidian-golinks/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-golinks";
        version = "0.2.0";
        outputHash = "sha256-7ihOCoTR9yOJDDsr3Jh7p3nw//dTWZ14/h15gYPt4bo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render go/links as clickable links.'';
            homepage = "https://github.com/xavdid/obsidian-golinks";
        };
    }
);
    "obsidian-google-lookup" = (
    let
        baseUrl = "https://github.com/ntawileh/obsidian-google-lookup/releases/download/1.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-google-lookup";
        version = "1.8.1";
        outputHash = "sha256-UqwaGFb9NkRvA1kbx+pfmYDLkEe94WQAgJXSgZ4i5qA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import contact and calendar event information from your Google account.'';
            homepage = "https://github.com/ntawileh/obsidian-google-lookup";
        };
    }
);
    "obsidian-google-tasks" = (
    let
        baseUrl = "https://github.com/YukiGasai/obsidian-google-tasks/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-google-tasks";
        version = "1.5.2";
        outputHash = "sha256-IcRXEUfnHR3FMrGdCiAezOUTTRGzfqVZkx6PMW3GKJ4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interact with your Google Tasks.'';
            homepage = "https://github.com/YukiGasai/obsidian-google-tasks";
        };
    }
);
    "obsidian-grandfather" = (
    let
        baseUrl = "https://github.com/noatpad/obsidian-grandfather/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-grandfather";
        version = "1.0.1";
        outputHash = "sha256-ZmL03j8dlDH89jpljK52qmBpi9FG3/TaxEmf9uAcstM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display the time and date in the status bar.'';
            homepage = "https://github.com/noatpad/obsidian-grandfather";
        };
    }
);
    "obsidian-graphviz" = (
    let
        baseUrl = "https://github.com/QAMichaelPeng/obsidian-graphviz/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-graphviz";
        version = "1.0.5";
        outputHash = "sha256-eEsAZ+xyrT1cfJX74FuIPKo9L7OHUmh1l0CRqJgnC88=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Graphviz diagrams.'';
            homepage = "https://github.com/QAMichaelPeng/obsidian-graphviz";
        };
    }
);
    "obsidian-group-snippets" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-group-snippets/releases/download/1.10.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-group-snippets";
        version = "1.10.3";
        outputHash = "sha256-klsjkmQXETlSrGR6F3eOKU4jdMXp4DDpwKDR3NvUiV4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create folder of snippets to activate them in one click!'';
            homepage = "https://github.com/Mara-Li/obsidian-group-snippets";
        };
    }
);
    "obsidian-habit-tracker" = (
    let
        baseUrl = "https://github.com/duoani/obsidian-habit-tracker/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-habit-tracker";
        version = "1.0.4";
        outputHash = "sha256-RYX7gOBB2oum5PMzi11cDHSRC3FaU5jyqFfeT5Jkbmg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a simple month view for visualizing your punch records.'';
            homepage = "https://github.com/duoani/obsidian-habit-tracker";
        };
    }
);
    "obsidian-habitica-integration" = (
    let
        baseUrl = "https://github.com/SuperChamp234/habitica-sync/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-habitica-integration";
        version = "1.0.2";
        outputHash = "sha256-6TdABUaxOqQHN+7QNivMRKYO6QXUHWHUc2hwOogG1G0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate Habitica tasks and stats.'';
            homepage = "https://github.com/SuperChamp234/habitica-sync";
        };
    }
);
    "obsidian-hackernews" = (
    let
        baseUrl = "https://github.com/arpitbbhayani/obsidian-hackernews/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hackernews";
        version = "1.0.5";
        outputHash = "sha256-G2D7hc3n5tpftpd1UEplLA3DXbQnidwS+TW5wHD4QP4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Periodically fetches and displays top stories from HackerNews.'';
            homepage = "https://github.com/arpitbbhayani/obsidian-hackernews";
        };
    }
);
    "obsidian-handlebars" = (
    let
        baseUrl = "https://github.com/sbquinlan/obsidian-handlebars/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-handlebars";
        version = "1.0.11";
        outputHash = "sha256-/RiVaGD4elqZtZmCuV7vc+6uHIDE93vcYc0fXxDnMkc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add support for Handlebars template blocks in notes.'';
            homepage = "https://github.com/sbquinlan/obsidian-handlebars";
        };
    }
);
    "obsidian-heading-shifter" = (
    let
        baseUrl = "https://github.com/k4a-l/obsidian-heading-shifter/releases/download/1.10.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-heading-shifter";
        version = "1.10.2";
        outputHash = "sha256-Ik8w3VCr0BO9Mvov9IIrvHlP6gwLigZONVJqsWq7E+s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily Shift and Change Markdown headings.'';
            homepage = "https://github.com/k4a-l/obsidian-heading-shifter";
        };
    }
);
    "obsidian-hide-sidebars-when-narrow" = (
    let
        baseUrl = "https://github.com/NomarCub/obsidian-hide-sidebars-on-window-resize/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hide-sidebars-when-narrow";
        version = "1.1.1";
        outputHash = "sha256-M5PBEPUiLqZbOXg2u4MAbj8v+/cOJF6YWR78UCnFKJA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically hides the sidebars when your window is narrow on window resize.'';
            homepage = "https://github.com/NomarCub/obsidian-hide-sidebars-on-window-resize";
        };
    }
);
    "obsidian-hider" = (
    let
        baseUrl = "https://github.com/kepano/obsidian-hider/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hider";
        version = "1.6.1";
        outputHash = "sha256-qwjMV34K9FTuDtjPIHnmb3qeBClucuWNbiYaa9pylek=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide UI elements such as tooltips, status, titlebar and more.'';
            homepage = "https://github.com/kepano/obsidian-hider";
        };
    }
);
    "obsidian-highlightpublicnotes-plugin" = (
    let
        baseUrl = "https://github.com/dennisseidel/highlightpublicnotes-obsidian-plugin/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-highlightpublicnotes-plugin";
        version = "1.6.0";
        outputHash = "sha256-BLo1Q2A+IxGpDE5ZOO6r/Uwr/IBy9Q8Trio0NAbX94k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Warn that a note is public (based on a frontmatter attribute) by coloring the note red.'';
            homepage = "https://github.com/dennisseidel/highlightpublicnotes-obsidian-plugin";
        };
    }
);
    "obsidian-hotkeys-chords" = (
    let
        baseUrl = "https://github.com/trenta3/obsidian-hotkeys-chords/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hotkeys-chords";
        version = "0.1.0";
        outputHash = "sha256-ETKoSBtoE7m8YYinlDNJUvWBjK8Flq3U2WIpNNMyJPM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Configurable hotkeys chords to activate commands.'';
            homepage = "https://github.com/trenta3/obsidian-hotkeys-chords";
        };
    }
);
    "obsidian-hotkeys-for-specific-files" = (
    let
        baseUrl = "https://github.com/Vinzent03/obsidian-hotkeys-for-specific-files/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hotkeys-for-specific-files";
        version = "1.4.1";
        outputHash = "sha256-T+2cLMqyA4HVC68I0N5rnKLisItG9+6/0hrx6WLBtSQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add commands for specific files and open them with a hotkey.'';
            homepage = "https://github.com/Vinzent03/obsidian-hotkeys-for-specific-files";
        };
    }
);
    "obsidian-hotkeys-for-templates" = (
    let
        baseUrl = "https://github.com/Vinzent03/obsidian-hotkeys-for-templates/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hotkeys-for-templates";
        version = "1.4.3";
        outputHash = "sha256-zGeAFIDrxAsB000ZxR6m1liJEBlqDbuoB0993JND10U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add commands to insert specific templates with a hotkey.'';
            homepage = "https://github.com/Vinzent03/obsidian-hotkeys-for-templates";
        };
    }
);
    "obsidian-hover-editor" = (
    let
        baseUrl = "https://github.com/nothingislost/obsidian-hover-editor/releases/download/0.11.29";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hover-editor";
        version = "0.11.29";
        outputHash = "sha256-Elm48BjmoQe4hUbr/HQEvUx3nMejchpdQ85o5Z2uw6I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform the Page Preview hover popover into a fully working editor instance.'';
            homepage = "https://github.com/nothingislost/obsidian-hover-editor";
        };
    }
);
    "obsidian-html-plugin" = (
    let
        baseUrl = "https://github.com/nuthrash/obsidian-html-plugin/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-html-plugin";
        version = "1.0.13";
        outputHash = "sha256-XUZWknS3B8Zs5PDcaItX0Uep9uY+k3AEw9Hs08QUcjQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open .html and .htm files.'';
            homepage = "https://github.com/nuthrash/obsidian-html-plugin";
        };
    }
);
    "obsidian-html-tags-autocomplete" = (
    let
        baseUrl = "https://github.com/bicarlsen/obsidian_html_tags_autocomplete/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-html-tags-autocomplete";
        version = "0.0.7";
        outputHash = "sha256-IIjjDzxfu1p3BnlrD4mo0GqXtciwXgwODgrFOH79OF4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically add closing HTML tags.'';
            homepage = "https://github.com/bicarlsen/obsidian_html_tags_autocomplete";
        };
    }
);
    "obsidian-hyphenation" = (
    let
        baseUrl = "https://github.com/7596ff/obsidian-hyphenation/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hyphenation";
        version = "0.1.0";
        outputHash = "sha256-KI5/wxRxvAtvJcZYcw4srwzduthXtaTUqYD+gigrgjQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable justified text and hyphenation.'';
            homepage = "https://github.com/7596ff/obsidian-hyphenation";
        };
    }
);
    "obsidian-hypothesis-plugin" = (
    let
        baseUrl = "https://github.com/weichenw/obsidian-hypothesis-plugin/releases/download/0.1.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-hypothesis-plugin";
        version = "0.1.19";
        outputHash = "sha256-eQpLgW0uSdWIMyNhMX0GQx5e0f6oUDl3KgISlrQq8Ko=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Hypothesis highlights.'';
            homepage = "https://github.com/weichenw/obsidian-hypothesis-plugin";
        };
    }
);
    "obsidian-icon-folder" = (
    let
        baseUrl = "https://github.com/FlorianWoelki/obsidian-iconize/releases/download/2.14.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-icon-folder";
        version = "2.14.7";
        outputHash = "sha256-A2BiRW6IhECtXWyZhzgMQxMgWXN/37zCEvRs6HWjb8Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add icons to anything in Obsidian, including files, folders, and text.'';
            homepage = "https://github.com/FlorianWoelki/obsidian-iconize";
        };
    }
);
    "obsidian-icon-shortcodes" = (
    let
        baseUrl = "https://github.com/aidenlx/obsidian-icon-shortcodes/releases/download/0.9.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-icon-shortcodes";
        version = "0.9.7";
        outputHash = "sha256-rf8EHdCGl428FP7qQ1qDeltQEMcbFqDfBCUW+UmE928=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert emoji and custom icons with shortcodes.'';
            homepage = "https://github.com/aidenlx/obsidian-icon-shortcodes";
        };
    }
);
    "obsidian-icon-swapper" = (
    let
        baseUrl = "https://github.com/mgmeyers/obsidian-icon-swapper/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-icon-swapper";
        version = "0.0.6";
        outputHash = "sha256-kvAwSC2gus1Q6O7WIhg8wwGs/a+R3yn5Cd+crCuuwnc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Swap out Obsidian"s default icons.'';
            homepage = "https://github.com/mgmeyers/obsidian-icon-swapper";
        };
    }
);
    "obsidian-icons-plugin" = (
    let
        baseUrl = "https://github.com/visini/obsidian-icons-plugin/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-icons-plugin";
        version = "0.3.0";
        outputHash = "sha256-XybRoSIMGd2jQ1vCL1XJnxCSo1UrAzQNYlCU4saC/mE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add icons to your notes.'';
            homepage = "https://github.com/visini/obsidian-icons-plugin";
        };
    }
);
    "obsidian-image-auto-upload-plugin" = (
    let
        baseUrl = "https://github.com/renmu123/obsidian-image-auto-upload-plugin/releases/download/4.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-image-auto-upload-plugin";
        version = "4.1.0";
        outputHash = "sha256-nfIZ5bPcxfPlmE1ZIpYNKCL8WPL+rvA/vFO7Jj6fh4c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images from your clipboard by PicGo.'';
            homepage = "https://github.com/renmu123/obsidian-image-auto-upload-plugin";
        };
    }
);
    "obsidian-image-caption" = (
    let
        baseUrl = "https://github.com/bicarlsen/obsidian_image_caption/releases/download/0.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-image-caption";
        version = "0.0.13";
        outputHash = "sha256-Ql3kuhDRga+tE/Y1/UE3dGH+HiOUM0xPOpUjJbl9qNk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add caption to images.'';
            homepage = "https://github.com/bicarlsen/obsidian_image_caption";
        };
    }
);
    "obsidian-image-gallery" = (
    let
        baseUrl = "https://github.com/lucaorio/obsidian-image-gallery/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-image-gallery";
        version = "1.1.1";
        outputHash = "sha256-uiIk86eWnXnVca/R49dkO5bQUN56lk/vkkryUnypwCM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A zero setup masonry image gallery for Obsidian.'';
            homepage = "https://github.com/lucaorio/obsidian-image-gallery";
        };
    }
);
    "obsidian-image-layouts" = (
    let
        baseUrl = "https://github.com/vertis/obsidian-image-layouts/releases/download/0.15.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-image-layouts";
        version = "0.15.5";
        outputHash = "sha256-VD5Sz9omlUm91Ge4dfZFOoFC4nX5qFj70IpId3a52mU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add beautiful image layouts to your notes.'';
            homepage = "https://github.com/vertis/obsidian-image-layouts";
        };
    }
);
    "obsidian-image-toolkit" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-image-toolkit/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-image-toolkit";
        version = "1.4.3";
        outputHash = "sha256-wUxpOpORV/583YODiRFIUoDiEMzXl3pxd8ySckQXlms=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Click images to preview with zoom, move, rotate, flip, invert, and copy.'';
            homepage = "https://github.com/obsidian-community/obsidian-image-toolkit";
        };
    }
);
    "obsidian-image-uploader" = (
    let
        baseUrl = "https://github.com/Creling/obsidian-image-uploader/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-image-uploader";
        version = "0.3.2";
        outputHash = "sha256-PdVC//oxKV4Ya9d9ah87Uu2KRRHfOHYawPrHESpViAE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload the image in your clipboard to any image hosting automatically when pasting.'';
            homepage = "https://github.com/Creling/obsidian-image-uploader";
        };
    }
);
    "obsidian-imgur-plugin" = (
    let
        baseUrl = "https://github.com/gavvvr/obsidian-imgur-plugin/releases/download/2.6.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-imgur-plugin";
        version = "2.6.3";
        outputHash = "sha256-Z4haFWd59q13+PbzE61qleodRW8d8rLJOEaqMiN4n9Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images from your clipboard to imgur.com and embeds uploaded image to your note.'';
            homepage = "https://github.com/gavvvr/obsidian-imgur-plugin";
        };
    }
);
    "obsidian-import-json" = (
    let
        baseUrl = "https://github.com/farling42/obsidian-import-json/releases/download/0.38.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-import-json";
        version = "0.38.4";
        outputHash = "sha256-3HkDIn1/Gxouyumb09TB6gvrKm88gIrcj5Y8jAvhaa8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import a JSON file containing an array of data, creating notes from a Handlebars template file.'';
            homepage = "https://github.com/farling42/obsidian-import-json";
        };
    }
);
    "obsidian-importer" = (
    let
        baseUrl = "https://github.com/obsidianmd/obsidian-importer/releases/download/1.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-importer";
        version = "1.8.4";
        outputHash = "sha256-k9VYvC0NLOZ+emwMHn/supa4v7rIR4mBJAlTyTqNcHo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import data from Notion, Evernote, Apple Notes, Microsoft OneNote, Google Keep, Bear, Roam, and HTML files.'';
            homepage = "https://github.com/obsidianmd/obsidian-importer";
        };
    }
);
    "obsidian-incremental-writing" = (
    let
        baseUrl = "https://github.com/bjsi/incremental-writing/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-incremental-writing";
        version = "0.4.1";
        outputHash = "sha256-45EZatLXTjqDjb7MQSLSy9qmlHhnN+/0Ll3Fjbw9wqE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Incrementally review notes and blocks over time.'';
            homepage = "https://github.com/bjsi/incremental-writing";
        };
    }
);
    "obsidian-itinerary" = (
    let
        baseUrl = "https://github.com/coddingtonbear/obsidian-itinerary/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-itinerary";
        version = "1.3.0";
        outputHash = "sha256-8xfClBeXEremwFUbljBA+A/x3ogOnTAPlQj+HX5lqIQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make planning your trip or event easier by rendering a calendar from event information found in your notes.'';
            homepage = "https://github.com/coddingtonbear/obsidian-itinerary";
        };
    }
);
    "obsidian-ivre-plugin" = (
    let
        baseUrl = "https://github.com/ivre/obsidian-ivre-plugin/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-ivre-plugin";
        version = "1.4.0";
        outputHash = "sha256-EGj7ey/N34X0mQBykdFWHzXJqCe98e8PIJfUfzFjNYs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Grab data from IVRE and bring it into your notes.'';
            homepage = "https://github.com/ivre/obsidian-ivre-plugin";
        };
    }
);
    "obsidian-javascript-init" = (
    let
        baseUrl = "https://github.com/ryanpcmcquen/obsidian-javascript-init/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-javascript-init";
        version = "1.0.2";
        outputHash = "sha256-oxqIDD7LzkTDimuJKdJwRHUk+7QHRP7ZJAMlBZFC8NI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run JavaScript when Obsidian loads, or at any other time.'';
            homepage = "https://github.com/ryanpcmcquen/obsidian-javascript-init";
        };
    }
);
    "obsidian-jira-issue" = (
    let
        baseUrl = "https://github.com/marc0l92/obsidian-jira-issue/releases/download/1.58.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-jira-issue";
        version = "1.58.1";
        outputHash = "sha256-CofIC+9twnIyu3V/JZlRxs3nuecvlG3syojtK+lfhAU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track the progress of Atlassian Jira issues from your notes.'';
            homepage = "https://github.com/marc0l92/obsidian-jira-issue";
        };
    }
);
    "obsidian-journey-plugin" = (
    let
        baseUrl = "https://github.com/akaalias/obsidian-journey-plugin/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-journey-plugin";
        version = "0.0.7";
        outputHash = "sha256-FXb6iJeZSpdrDj9qJfp+x93sQVFb040iJK+m2dw6SJ8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Discover the story between your notes.'';
            homepage = "https://github.com/akaalias/obsidian-journey-plugin";
        };
    }
);
    "obsidian-jsonifier" = (
    let
        baseUrl = "https://github.com/KjellConnelly/obsidian-jsonifier/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-jsonifier";
        version = "1.0.1";
        outputHash = "sha256-HqRZMxDLvo4VjMq2xxiJKoZ9gZTv5kZ0dWf1dL6RnbI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''JSON.stringify() or JSON.parse() highlighted text and copy to clipboard.'';
            homepage = "https://github.com/KjellConnelly/obsidian-jsonifier";
        };
    }
);
    "obsidian-jtab" = (
    let
        baseUrl = "https://github.com/davfive/obsidian-jtab/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-jtab";
        version = "1.2.3";
        outputHash = "sha256-M5399hswPmXRZs+GQQF0R5rw+XaDgINe7KaylfdHTEA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add guitar chords and tabs to your notes with jTab code blocks.'';
            homepage = "https://github.com/davfive/obsidian-jtab";
        };
    }
);
    "obsidian-juliandate" = (
    let
        baseUrl = "https://github.com/THeK3nger/obsidian-juliandate/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-juliandate";
        version = "1.0.5";
        outputHash = "sha256-jKYUkGj9D2dtvcwWkuENG4WVP44OlynccsuzL4CD59I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a shortcut to insert the current Julian date for astronomical observations.'';
            homepage = "https://github.com/THeK3nger/obsidian-juliandate";
        };
    }
);
    "obsidian-jump-to-date-plugin" = (
    let
        baseUrl = "https://github.com/TfTHacker/obsidian42-jump-to-date/releases/download/1.0.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-jump-to-date-plugin";
        version = "1.0.25";
        outputHash = "sha256-S/b1HI1JnFd4H3cyoQw/u52LXTOCfGHWdk2sXAFiOos=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Popup calendar for quickly navigating dates.'';
            homepage = "https://github.com/TfTHacker/obsidian42-jump-to-date";
        };
    }
);
    "obsidian-kanban" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-kanban/releases/download/2.0.51";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-kanban";
        version = "2.0.51";
        outputHash = "sha256-vIkrZsVkYtXuRG0sjNo9z+GKqd8AjunOm0zm3VSqaU0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Markdown-backed Kanban boards.'';
            homepage = "https://github.com/obsidian-community/obsidian-kanban";
        };
    }
);
    "obsidian-key-sequence-shortcut" = (
    let
        baseUrl = "https://github.com/anselmwang/obsidian-key-sequence-shortcut/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-key-sequence-shortcut";
        version = "2.0.2";
        outputHash = "sha256-cOSC4mWVbewvlnPL8DOqBbzXAW1KQC8FF104h08bh7s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute commands with short key sequences. For example, "tp" for "Toggle Preview" and "tb" for "Toggle Sidebar". Easier to remember.'';
            homepage = "https://github.com/anselmwang/obsidian-key-sequence-shortcut";
        };
    }
);
    "obsidian-kindle-export" = (
    let
        baseUrl = "https://github.com/SimeonLukas/obsidian-kindle-export/releases/download/0.0.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-kindle-export";
        version = "0.0.14";
        outputHash = "sha256-CsVnhL3pl/wAPSWaAiiY0gYtQv4KIg6/Npao6IyCCdQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export your notes to your Kindle, including embedded notes and images.'';
            homepage = "https://github.com/SimeonLukas/obsidian-kindle-export";
        };
    }
);
    "obsidian-kindle-plugin" = (
    let
        baseUrl = "https://github.com/hadynz/obsidian-kindle-plugin/releases/download/2.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-kindle-plugin";
        version = "2.3.1";
        outputHash = "sha256-CtlyDwkkTXqGvhKcTC/j+9gsJvCTDHhTBp7Ce6VY9v4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Kindle book highlights using your Amazon login or uploading your My Clippings file.'';
            homepage = "https://github.com/hadynz/obsidian-kindle-plugin";
        };
    }
);
    "obsidian-kobo-highlights-importer-plugin" = (
    let
        baseUrl = "https://github.com/OGKevin/obsidian-kobo-highlights-import/releases/download/5.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-kobo-highlights-importer-plugin";
        version = "5.0.0";
        outputHash = "sha256-KGQjXSXQgxht3jVyUhpD+tkUgyW15TB1gZwj9jIjsSI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import highlights from Kobo devices.'';
            homepage = "https://github.com/OGKevin/obsidian-kobo-highlights-import";
        };
    }
);
    "obsidian-koreader-plugin" = (
    let
        baseUrl = "https://github.com/Edo78/obsidian-koreader-sync/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-koreader-plugin";
        version = "0.6.1";
        outputHash = "sha256-P1QtVulG9JafSo0YxfBuSpxkZNMKOY71KZl90eFq+sA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync highlights/notes from KOReader.'';
            homepage = "https://github.com/Edo78/obsidian-koreader-sync";
        };
    }
);
    "obsidian-kroki" = (
    let
        baseUrl = "https://github.com/gregzuro/obsidian-kroki/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-kroki";
        version = "1.2.4";
        outputHash = "sha256-7LGroGXZ/kClUHcXbJGwryfCKBYdYkOVVoFQb9qE/Mk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Kroki diagrams.'';
            homepage = "https://github.com/gregzuro/obsidian-kroki";
        };
    }
);
    "obsidian-languagetool-plugin" = (
    let
        baseUrl = "https://github.com/Clemens-E/obsidian-languagetool-plugin/releases/download/0.3.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-languagetool-plugin";
        version = "0.3.8";
        outputHash = "sha256-x7FSBhXPy59txS8IeR9gQKpAmLTjvR0zI8U2MTgrIis=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''advanced spell/grammar checks with the help of language-tool.'';
            homepage = "https://github.com/Clemens-E/obsidian-languagetool-plugin";
        };
    }
);
    "obsidian-latex" = (
    let
        baseUrl = "https://github.com/wei2912/obsidian-latex/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-latex";
        version = "0.4.1";
        outputHash = "sha256-O2nnO/mv+FzQutfsBu5Y9eZIezQisr3ldlnR/T/+xcA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable additional MathJax packages and adds a global preamble for MathJax.'';
            homepage = "https://github.com/wei2912/obsidian-latex";
        };
    }
);
    "obsidian-latex-environments" = (
    let
        baseUrl = "https://github.com/raineszm/obsidian-latex-environments/releases/download/0.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-latex-environments";
        version = "0.4.2";
        outputHash = "sha256-no8ZcM/tDk/PFIRjCiR24b9WgTvaATB0Z4Yua7bN6e4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly insert and change LaTeX environments within math environments.'';
            homepage = "https://github.com/raineszm/obsidian-latex-environments";
        };
    }
);
    "obsidian-latex-suite" = (
    let
        baseUrl = "https://github.com/artisticat1/obsidian-latex-suite/releases/download/1.11.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-latex-suite";
        version = "1.11.5";
        outputHash = "sha256-HBFzlukpIHJPuSTCaagJaygQwOUEkVjdAIjuDGG+yKA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make typesetting LaTeX math as fast as handwriting through snippets, text expansion, and editor enhancements.'';
            homepage = "https://github.com/artisticat1/obsidian-latex-suite";
        };
    }
);
    "obsidian-leaflet-plugin" = (
    let
        baseUrl = "https://github.com/javalent/obsidian-leaflet/releases/download/6.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-leaflet-plugin";
        version = "6.0.5";
        outputHash = "sha256-RBogsPElRdZQD9UvwdxfB2X1gN5UY1VmbyYkK07GLFg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interactive maps inside your notes.'';
            homepage = "https://github.com/javalent/obsidian-leaflet";
        };
    }
);
    "obsidian-limelight" = (
    let
        baseUrl = "https://github.com/smikula/obsidian-limelight/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-limelight";
        version = "1.1.0";
        outputHash = "sha256-WJXq0rEi45sKPGtJ0IAo7u7aWBKIyEDDdp4xdrvH994=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Spotlight your active pane.'';
            homepage = "https://github.com/smikula/obsidian-limelight";
        };
    }
);
    "obsidian-lineup-builder" = (
    let
        baseUrl = "https://github.com/James-Fallon/obsidian-lineup-builder/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-lineup-builder";
        version = "1.0.1";
        outputHash = "sha256-VSo5LdEFVFHWOtEcJzJIEPPYUUTYRV8N1GxTTIUWjzQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Build football lineups.'';
            homepage = "https://github.com/James-Fallon/obsidian-lineup-builder";
        };
    }
);
    "obsidian-link-archive" = (
    let
        baseUrl = "https://github.com/tomzorz/obsidian-link-archive/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-link-archive";
        version = "0.2.0";
        outputHash = "sha256-Xh5XNFhQrzC97M9bMb7N0kmrW9fCC1kV4u6ojq/MheI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Archive links in your note so they"re available to you even if the original site goes down or gets removed.'';
            homepage = "https://github.com/tomzorz/obsidian-link-archive";
        };
    }
);
    "obsidian-link-converter" = (
    let
        baseUrl = "https://github.com/ozntel/obsidian-link-converter/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-link-converter";
        version = "0.1.6";
        outputHash = "sha256-D8MT3QlfDyRHN3g5OdGT6j2wZbajXKokgd0UZE0Hpkw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Scan all your links in the vault and convert them to your desired format.'';
            homepage = "https://github.com/ozntel/obsidian-link-converter";
        };
    }
);
    "obsidian-link-embed" = (
    let
        baseUrl = "https://github.com/Seraphli/obsidian-link-embed/releases/download/2.10.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-link-embed";
        version = "2.10.5";
        outputHash = "sha256-Bq4J79Ye2vRy84Pfqm9nB7VC46DuGE+aJeF1uCNQYQE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert URLs in your notes into embeded previews.'';
            homepage = "https://github.com/Seraphli/obsidian-link-embed";
        };
    }
);
    "obsidian-link-indexer" = (
    let
        baseUrl = "https://github.com/aviskase/obsidian-link-indexer/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-link-indexer";
        version = "1.0.0";
        outputHash = "sha256-uzol+SMosVAOkNE0jomnbLbRSdx6w9RRSlDnJVB0r5M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate index notes with links based on various conditions.'';
            homepage = "https://github.com/aviskase/obsidian-link-indexer";
        };
    }
);
    "obsidian-link-opener" = (
    let
        baseUrl = "https://github.com/zorazrr/obsidian-link-opener/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-link-opener";
        version = "1.0.2";
        outputHash = "sha256-WRF/yEz5yWzBjdijegs1/rVV8oxZvaqxZtn0+2Ps3A8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open external links using a modal or a tab.'';
            homepage = "https://github.com/zorazrr/obsidian-link-opener";
        };
    }
);
    "obsidian-linter" = (
    let
        baseUrl = "https://github.com/platers/obsidian-linter/releases/download/1.31.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-linter";
        version = "1.31.2";
        outputHash = "sha256-+2t+FygrXq19FdnIPaN20FiZTp10j9DeqccmW1CTb9w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format and style your notes. Linter can be used to format YAML tags, aliases, arrays, and metadata; footnotes; headings; spacing; math blocks; regular Markdown contents like list, italics, and bold styles; and more with the use of custom rule options.'';
            homepage = "https://github.com/platers/obsidian-linter";
        };
    }
);
    "obsidian-list-callouts" = (
    let
        baseUrl = "https://github.com/mgmeyers/obsidian-list-callouts/releases/download/1.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-list-callouts";
        version = "1.2.9";
        outputHash = "sha256-GLgTP5QigM+3bI8Wn/5yPtoxzvwwPzRv0csWxmDMqxI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create simple callouts in lists.'';
            homepage = "https://github.com/mgmeyers/obsidian-list-callouts";
        };
    }
);
    "obsidian-list-modified" = (
    let
        baseUrl = "https://github.com/franciskafieh/obsidian-list-modified/releases/download/3.7.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-list-modified";
        version = "3.7.2";
        outputHash = "sha256-IROU+MLiGcSjuqGOpaL9S1bkfA0eHsADIrhC9x8r3HA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link all modified files meeting certain criteria to a daily note.'';
            homepage = "https://github.com/franciskafieh/obsidian-list-modified";
        };
    }
);
    "obsidian-livesync" = (
    let
        baseUrl = "https://github.com/vrtmrz/obsidian-livesync/releases/download/0.25.59";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-livesync";
        version = "0.25.59";
        outputHash = "sha256-E7UZXkLT3HE4o9p6VcHhaQn/rxye9h897ehRp9KpP1k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Community implementation of self-hosted livesync. Reflect your vault changes to some other devices immediately. Please make sure to disable other synchronize solutions to avoid content corruption or duplication.'';
            homepage = "https://github.com/vrtmrz/obsidian-livesync";
        };
    }
);
    "obsidian-living-graph" = (
    let
        baseUrl = "https://github.com/geoffreysflaminglasersword/obsidian-living-graph/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-living-graph";
        version = "1.0.3";
        outputHash = "sha256-i7jv5Xs8mBhBqnE5qQXgnKewKz4nEXQHvdFnH0xcrs4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A for-fun graph plugin.'';
            homepage = "https://github.com/geoffreysflaminglasersword/obsidian-living-graph";
        };
    }
);
    "obsidian-local-file-interface-plugin" = (
    let
        baseUrl = "https://github.com/qawatake/obsidian-local-file-interface-plugin/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-local-file-interface-plugin";
        version = "0.1.0";
        outputHash = "sha256-bvc5MMUUHMUIpxvrj2uutphcLG+dqInGAuwYQdR1Qxk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands for moving files in and out of the vault.'';
            homepage = "https://github.com/qawatake/obsidian-local-file-interface-plugin";
        };
    }
);
    "obsidian-local-images" = (
    let
        baseUrl = "https://github.com/aleksey-rezvov/obsidian-local-images/releases/download/0.14.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-local-images";
        version = "0.14.2";
        outputHash = "sha256-ki08wR6FYuSx1khEHQZxk8mzdBHfJ9qT+TxLdrX0SBc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find all links to external images in your notes, download and save images locally, and adjust the image links in your notes to point to the saved image files.'';
            homepage = "https://github.com/aleksey-rezvov/obsidian-local-images";
        };
    }
);
    "obsidian-local-images-plus" = (
    let
        baseUrl = "https://github.com/Sergei-Korneev/obsidian-local-images-plus/releases/download/0.16.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-local-images-plus";
        version = "0.16.4";
        outputHash = "sha256-m8g+Fhxg5Rzqcg/2QlE6EQmuSqRdSLTl6UTctsTFLzc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A reincarnation of Local Images to download images in Markdown notes to local storage.'';
            homepage = "https://github.com/Sergei-Korneev/obsidian-local-images-plus";
        };
    }
);
    "obsidian-local-rest-api" = (
    let
        baseUrl = "https://github.com/coddingtonbear/obsidian-local-rest-api/releases/download/3.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-local-rest-api";
        version = "3.6.1";
        outputHash = "sha256-yHM7EPZ5/XbPaJzuctmDrNCP7YXvQV63sMOqQyaWwIc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Unlock your automation needs by interacting with your notes over a secure REST API.'';
            homepage = "https://github.com/coddingtonbear/obsidian-local-rest-api";
        };
    }
);
    "obsidian-lock-screen-plugin" = (
    let
        baseUrl = "https://github.com/ericbiewener/obsidian-lock-screen-plugin/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-lock-screen-plugin";
        version = "1.3.0";
        outputHash = "sha256-aqaaY6TYVl1+2ba7RUZ8TWu9JX5mw21UMjGzS3ux9tE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Protect your vault with a lock screen.'';
            homepage = "https://github.com/ericbiewener/obsidian-lock-screen-plugin";
        };
    }
);
    "obsidian-map-view" = (
    let
        baseUrl = "https://github.com/esm7/obsidian-map-view/releases/download/6.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-map-view";
        version = "6.1.4";
        outputHash = "sha256-VJ42xcevJkM0Fj6kxzrAf5ZHSwH/9PNDvU/VyHvm8fA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An interactive map view for your notes.'';
            homepage = "https://github.com/esm7/obsidian-map-view";
        };
    }
);
    "obsidian-mark-and-select" = (
    let
        baseUrl = "https://github.com/anselmwang/obsidian-mark-and-select/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-mark-and-select";
        version = "1.0.2";
        outputHash = "sha256-I9KgZRreFri9YXwuBv9Ve5pn12DGERejRPQdN8xjTBI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set mark, move cursors freely and select from mark to cursor position.'';
            homepage = "https://github.com/anselmwang/obsidian-mark-and-select";
        };
    }
);
    "obsidian-markdown-export-plugin" = (
    let
        baseUrl = "https://github.com/bingryan/obsidian-markdown-export-plugin/releases/download/1.0.39";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-markdown-export-plugin";
        version = "1.0.39";
        outputHash = "sha256-H8eAbcaCHItJtz2VBNRn8hl5PgOFXpZWM12zPwUsJy0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export Markdown to a package, including images.'';
            homepage = "https://github.com/bingryan/obsidian-markdown-export-plugin";
        };
    }
);
    "obsidian-markdown-file-suffix" = (
    let
        baseUrl = "https://github.com/git-no/obsidian-markdown-file-suffix/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-markdown-file-suffix";
        version = "1.0.1";
        outputHash = "sha256-2BRB9sX3Xpn1QtuXwrFJUbuckVvPlFgnB02hKd8Xon8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use additional files like .mdx / .svx as if they were Markdown.'';
            homepage = "https://github.com/git-no/obsidian-markdown-file-suffix";
        };
    }
);
    "obsidian-markdown-formatting-assistant-plugin" = (
    let
        baseUrl = "https://github.com/Reocin/obsidian-markdown-formatting-assistant-plugin/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-markdown-formatting-assistant-plugin";
        version = "0.4.1";
        outputHash = "sha256-uFv6M8jLZsfeZymi0xCTY/0K9o4mbyYX6xOYrnWGuZM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A formatting sidebar for Markdown and a command line interface to facilitate a faster workflow.'';
            homepage = "https://github.com/Reocin/obsidian-markdown-formatting-assistant-plugin";
        };
    }
);
    "obsidian-markdown-furigana" = (
    let
        baseUrl = "https://github.com/steven-kraft/obsidian-markdown-furigana/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-markdown-furigana";
        version = "1.3.0";
        outputHash = "sha256-SDSd7xGcRA6b5YFuWOZmJ/FbU+VWXJh36+SExQH/pEQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display furigana using Markdown syntax.'';
            homepage = "https://github.com/steven-kraft/obsidian-markdown-furigana";
        };
    }
);
    "obsidian-markmind" = (
    let
        baseUrl = "https://github.com/MarkMindCkm/obsidian-markmind/releases/download/3.4.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-markmind";
        version = "3.4.8";
        outputHash = "sha256-PLA9GMJdxy7ut2Fx0yXNlxgNXkidTseX/lXYj/jpSxc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mind map, outline and PDF annotation tool. (Closed source)'';
            homepage = "https://github.com/MarkMindCkm/obsidian-markmind";
        };
    }
);
    "obsidian-math-plus" = (
    let
        baseUrl = "https://github.com/ocapraro/obsidian-math-plus/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-math-plus";
        version = "0.7.0";
        outputHash = "sha256-OBCFe2smxJ06d7OQNf7PjEpZNhzrchbksTXGzVoBGnQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take math notes using Excalidraw.'';
            homepage = "https://github.com/ocapraro/obsidian-math-plus";
        };
    }
);
    "obsidian-matrix" = (
    let
        baseUrl = "https://github.com/MohrJonas/obsidian-matrix/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-matrix";
        version = "1.4.0";
        outputHash = "sha256-nbqPtayumdKvNUHmF5ewF0L2+K3K6IbihVPkKcSPK1Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Utility to easily create LaTeX matrices.'';
            homepage = "https://github.com/MohrJonas/obsidian-matrix";
        };
    }
);
    "obsidian-md-to-jira" = (
    let
        baseUrl = "https://github.com/muckmuck96/obsidian-md-to-jira/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-md-to-jira";
        version = "0.3.0";
        outputHash = "sha256-3OxraGxB1o6eE5N2e4skG/bbzLKer36gQwyuZvJMZMo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert notes or selections to Jira markup and vice versa.'';
            homepage = "https://github.com/muckmuck96/obsidian-md-to-jira";
        };
    }
);
    "obsidian-media-db-plugin" = (
    let
        baseUrl = "https://github.com/mProjectsCode/obsidian-media-db-plugin/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-media-db-plugin";
        version = "0.8.0";
        outputHash = "sha256-Gb+UjETPaDuiKe0ghka00IxWX8L2TkLBrycJiCDhBg4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Query multiple APIs for movies, series, anime, games, music releases and wiki articles, and import them into your vault.'';
            homepage = "https://github.com/mProjectsCode/obsidian-media-db-plugin";
        };
    }
);
    "obsidian-memos" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Thino/releases/download/3.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-memos";
        version = "3.0.10";
        outputHash = "sha256-Ye8LNxOVEOXHyoBQCmRyz47VMTYnFMHI6giLaWGocMo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly capture memos and display them in the sidebar with a heatmap. (Closed source)'';
            homepage = "https://github.com/Quorafind/Obsidian-Thino";
        };
    }
);
    "obsidian-meta-bind-plugin" = (
    let
        baseUrl = "https://github.com/mProjectsCode/obsidian-meta-bind-plugin/releases/download/1.4.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-meta-bind-plugin";
        version = "1.4.8";
        outputHash = "sha256-/kUkGAKDQxGkmRcHZiRSZH4zUW/dpZFZyjgx3MpZIaI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make your notes interactive with inline input fields, metadata displays, and buttons.'';
            homepage = "https://github.com/mProjectsCode/obsidian-meta-bind-plugin";
        };
    }
);
    "obsidian-metronome-plugin" = (
    let
        baseUrl = "https://github.com/curtgrimes/obsidian-metronome-plugin/releases/download/0.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-metronome-plugin";
        version = "0.8.1";
        outputHash = "sha256-uMf4NX6FTYW5Hb2DSLnZVLXgasCwimNCEuDhBdCUDIo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add interactive metronomes to your notes.'';
            homepage = "https://github.com/curtgrimes/obsidian-metronome-plugin";
        };
    }
);
    "obsidian-min-width" = (
    let
        baseUrl = "https://github.com/doitian/obsidian-min-width/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-min-width";
        version = "1.0.4";
        outputHash = "sha256-fVGY9oFe3VAcVCQZ2UdWDJ/9fAgm97Ke/n/w0u3KRzY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set the Minimum Width of the Active Pane.'';
            homepage = "https://github.com/doitian/obsidian-min-width";
        };
    }
);
    "obsidian-min3ditorhotkeys-plugin" = (
    let
        baseUrl = "https://github.com/d-sauer/Obsidian-Min3ditorHotkeys-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-min3ditorhotkeys-plugin";
        version = "1.0.1";
        outputHash = "sha256-HU6PKUUgcpXNXO9bCrCY190xGIXoUA6RVCbColnz/u8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Additional editor hotkeys inspired by coding editors.'';
            homepage = "https://github.com/d-sauer/Obsidian-Min3ditorHotkeys-plugin";
        };
    }
);
    "obsidian-mind-map" = (
    let
        baseUrl = "https://github.com/lynchjames/obsidian-mind-map/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-mind-map";
        version = "1.1.0";
        outputHash = "sha256-TSmVpxwi8ChDraiDSe819A7aoI9zPrgPzzLh+7LzO7k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display Markdown notes as mind maps using Markmap.'';
            homepage = "https://github.com/lynchjames/obsidian-mind-map";
        };
    }
);
    "obsidian-mindmap-nextgen" = (
    let
        baseUrl = "https://github.com/james-tindal/obsidian-mindmap-nextgen/releases/download/1.16.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-mindmap-nextgen";
        version = "1.16.0";
        outputHash = "sha256-tpNsaVqWL/N96t+XHahLiMcFB7BbH4+Q0XVY4Imuacw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preview notes as Markmap mind maps.'';
            homepage = "https://github.com/james-tindal/obsidian-mindmap-nextgen";
        };
    }
);
    "obsidian-minimal-settings" = (
    let
        baseUrl = "https://github.com/kepano/obsidian-minimal-settings/releases/download/8.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-minimal-settings";
        version = "8.2.2";
        outputHash = "sha256-tK5wl1uAMlo04Sdao+mtcopxzhbvhMzRBQT3Xl9pTw4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Control the colors and fonts in Minimal Theme.'';
            homepage = "https://github.com/kepano/obsidian-minimal-settings";
        };
    }
);
    "obsidian-mkdocs-publisher" = (
    let
        baseUrl = "https://github.com/Enveloppe/obsidian-enveloppe/releases/download/7.8.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-mkdocs-publisher";
        version = "7.8.2";
        outputHash = "sha256-/JHYZxw8PByW5g3+lW0ikNZhIpniMEVQhGFf/D26nfU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes to a preconfigured GitHub repository.'';
            homepage = "https://github.com/Enveloppe/obsidian-enveloppe";
        };
    }
);
    "obsidian-mtg" = (
    let
        baseUrl = "https://github.com/omardelarosa/obsidian-mtg/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-mtg";
        version = "1.0.9";
        outputHash = "sha256-KXq/5gEwbNiAQdkb5/aZZ1LLuWMElC0LhBkP09EP+Fw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage Magic: The Gathering decks and card lists as notes.'';
            homepage = "https://github.com/omardelarosa/obsidian-mtg";
        };
    }
);
    "obsidian-new-bullet-with-time" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-New-Bullet-With-Time/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-new-bullet-with-time";
        version = "2.1.0";
        outputHash = "sha256-xIonz+zR5YVe2f9lzmuDvCSNmt58wBkZBHqj8cTbQF0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically add the current time to new bullet lines.'';
            homepage = "https://github.com/Quorafind/Obsidian-New-Bullet-With-Time";
        };
    }
);
    "obsidian-new-note-new-window" = (
    let
        baseUrl = "https://github.com/Pr0dt0s/new-note-new-window/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-new-note-new-window";
        version = "1.1.1";
        outputHash = "sha256-3PaSmvM13+3pzH4s53UW4NC2d4ytQ39cphqkjaYxs+E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily open new notes in a floating window.'';
            homepage = "https://github.com/Pr0dt0s/new-note-new-window";
        };
    }
);
    "obsidian-nomnoml-diagram" = (
    let
        baseUrl = "https://github.com/Daeik/obsidian-nomnoml-diagram/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-nomnoml-diagram";
        version = "1.2.0";
        outputHash = "sha256-9wgOx0oeEAKYdZ4JgS8MJNS1TN/pRgbHl2nT3X5t/Cg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Draw nomnoml diagrams.'';
            homepage = "https://github.com/Daeik/obsidian-nomnoml-diagram";
        };
    }
);
    "obsidian-note-autocreator" = (
    let
        baseUrl = "https://github.com/SimonTC/obsidian-note-autocreation/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-note-autocreator";
        version = "1.6.0";
        outputHash = "sha256-pQFwSf5lO9NZcrgP2PoaE/Keue62JbqcvBZhpAXgmek=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically create notes when links are created to them.'';
            homepage = "https://github.com/SimonTC/obsidian-note-autocreation";
        };
    }
);
    "obsidian-note-content-pusher" = (
    let
        baseUrl = "https://github.com/lizard-heart/obsidian-note-content-pusher/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-note-content-pusher";
        version = "1.0.4";
        outputHash = "sha256-je/yvbI7GC4aNAHj48TshweC7oHnEyJDWGpbxGTxJV8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prepend or append specified content to a note (existing or new) without opening another pane.'';
            homepage = "https://github.com/lizard-heart/obsidian-note-content-pusher";
        };
    }
);
    "obsidian-notes-from-template" = (
    let
        baseUrl = "https://github.com/mo-seph/obsidian-note-from-template/releases/download/0.1.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-notes-from-template";
        version = "0.1.13";
        outputHash = "sha256-zhDJ2K4z7Sqn2rwnhWV5xzpHr402O7eT3S2ue+GxXNI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create new notes from Templates - for each Template, provides a Command to trigger it, and a form to fill in any variables in the template.'';
            homepage = "https://github.com/mo-seph/obsidian-note-from-template";
        };
    }
);
    "obsidian-notion-video" = (
    let
        baseUrl = "https://github.com/LastKnightCoder/obsidian-notion-video/releases/download/0.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-notion-video";
        version = "0.0.8";
        outputHash = "sha256-1i8EorOEQGVFl45S5kxG0LqfIhtSydEiEkqmrhU5zG0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed Notion videos.'';
            homepage = "https://github.com/LastKnightCoder/obsidian-notion-video";
        };
    }
);
    "obsidian-ocr" = (
    let
        baseUrl = "https://github.com/MohrJonas/obsidian-ocr/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-ocr";
        version = "2.0.1";
        outputHash = "sha256-sZt9z9e82KZPqzM+ixemy1O0P36K5Av6nHuhQvmUhsQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use optical character recognition to search for text in you images and PDFs.'';
            homepage = "https://github.com/MohrJonas/obsidian-ocr";
        };
    }
);
    "obsidian-old-note-admonitor" = (
    let
        baseUrl = "https://github.com/tadashi-aikawa/obsidian-old-note-admonitor/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-old-note-admonitor";
        version = "0.8.0";
        outputHash = "sha256-jWzt2VFvC88wjBGlolhkejXju1fZNUOQ9s5iMJKPBNg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show warnings if the note has not been updated for over specific days.'';
            homepage = "https://github.com/tadashi-aikawa/obsidian-old-note-admonitor";
        };
    }
);
    "obsidian-omnivore" = (
    let
        baseUrl = "https://github.com/omnivore-app/obsidian-omnivore/releases/download/1.10.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-omnivore";
        version = "1.10.4";
        outputHash = "sha256-Mwly+KlPlRywxF1P9FveurGyg0VpyJgiiwy0RXTIb+U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import your saved Omnivore articles and highlights.'';
            homepage = "https://github.com/omnivore-app/obsidian-omnivore";
        };
    }
);
    "obsidian-open-file-by-magic-date" = (
    let
        baseUrl = "https://github.com/SimplGy/obsidian-open-file-by-magic-date/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-open-file-by-magic-date";
        version = "0.1.2";
        outputHash = "sha256-P8B642Avez/NyF2yZHg+KRfkH97xyiuXbaKP6zz66mQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define a hotkey and Moment.js pattern for the file that is most important to you (eg: your daily/weekly/monthly note).'';
            homepage = "https://github.com/SimplGy/obsidian-open-file-by-magic-date";
        };
    }
);
    "obsidian-open-in-other-editor" = (
    let
        baseUrl = "https://github.com/yekingyan/obsidian-open-in-other-editor/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-open-in-other-editor";
        version = "0.0.5";
        outputHash = "sha256-3UyDi/w9T/AjVoDiDhTbwKg2EGsZZj2b1aQ7Y9PGL34=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open current active file in gVim or VS Code.'';
            homepage = "https://github.com/yekingyan/obsidian-open-in-other-editor";
        };
    }
);
    "obsidian-open-link-with" = (
    let
        baseUrl = "https://github.com/MamoruDS/obsidian-open-link-with/releases/download/0.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-open-link-with";
        version = "0.1.10";
        outputHash = "sha256-HHNYG+vOgJHBzyTUajqX9BotCDmqSL2bZkuQf4hWmNo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open external link with specific browser.'';
            homepage = "https://github.com/MamoruDS/obsidian-open-link-with";
        };
    }
);
    "obsidian-org-mode" = (
    let
        baseUrl = "https://github.com/ryanpcmcquen/obsidian-org-mode/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-org-mode";
        version = "1.4.1";
        outputHash = "sha256-sCItbEGJyMeSjdGt0v2DLFj+pX8q8+US51AxzHuj0YU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add Org Mode support.'';
            homepage = "https://github.com/ryanpcmcquen/obsidian-org-mode";
        };
    }
);
    "obsidian-orthography" = (
    let
        baseUrl = "https://github.com/denisoed/obsidian-orthography/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-orthography";
        version = "3.1.0";
        outputHash = "sha256-I+KgUWTWrapafBNacHo8Wxn5ALvIfhXloQcod7a2Zm4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Check & fix orthography errors in text.'';
            homepage = "https://github.com/denisoed/obsidian-orthography";
        };
    }
);
    "obsidian-oura-plugin" = (
    let
        baseUrl = "https://github.com/kinabalu/obsidian-oura-plugin/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-oura-plugin";
        version = "0.2.6";
        outputHash = "sha256-J4RoENzgq8T9C2g/9QpeRtiUZgDv9tplo4OX7u7YAPw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Oura Ring data into a note.'';
            homepage = "https://github.com/kinabalu/obsidian-oura-plugin";
        };
    }
);
    "obsidian-outliner" = (
    let
        baseUrl = "https://github.com/vslinko/obsidian-outliner/releases/download/4.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-outliner";
        version = "4.10.0";
        outputHash = "sha256-EswMQTzB1MjjNoSXzdCVPmcXZN8tc9oU5pl547uQjjQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Work with your lists like in Workflowy or Roam Research.'';
            homepage = "https://github.com/vslinko/obsidian-outliner";
        };
    }
);
    "obsidian-overdue" = (
    let
        baseUrl = "https://github.com/parente/obsidian-overdue/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-overdue";
        version = "1.0.0";
        outputHash = "sha256-bMRF1yPjyrOVFQFxBNXEJvBm8yDM2eIlufoLoAvVEqw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mark items as [[Overdue]] if they are not checked off by their due date.'';
            homepage = "https://github.com/parente/obsidian-overdue";
        };
    }
);
    "obsidian-pandoc" = (
    let
        baseUrl = "https://github.com/OliverBalfour/obsidian-pandoc/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pandoc";
        version = "0.4.1";
        outputHash = "sha256-uhxeyLBGd9Xe4zsggxQtsQDGroXoL2rronsxOZwjifc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands to export to Pandoc-supported formats like DOCX, ePub and PDF.'';
            homepage = "https://github.com/OliverBalfour/obsidian-pandoc";
        };
    }
);
    "obsidian-pandoc-reference-list" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-pandoc-reference-list/releases/download/2.0.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pandoc-reference-list";
        version = "2.0.25";
        outputHash = "sha256-OsOOmko1DWpDEegF1KwLTqWDvjd6R0Q/C8O+FQKKlJM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a formatted reference in the sidebar for each Pandoc citekey present in the current document.'';
            homepage = "https://github.com/obsidian-community/obsidian-pandoc-reference-list";
        };
    }
);
    "obsidian-pangu" = (
    let
        baseUrl = "https://github.com/Natumsol/obsidian-pangu/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pangu";
        version = "1.6.2";
        outputHash = "sha256-QL1tDqfB/qG6nWRMFnfZHkthbVSRC1UkLjZLGPi6Wgw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add spaces between Chinese characters and English alphabet. A boon for typographically compulsive people.'';
            homepage = "https://github.com/Natumsol/obsidian-pangu";
        };
    }
);
    "obsidian-party" = (
    let
        baseUrl = "https://github.com/shap-po/obsidian-party/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-party";
        version = "2.1.0";
        outputHash = "sha256-Y6G6KhVl6c0zb7wji1l6BdtvUCSUM75Vh7dG/IC7zRg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An implementation of party.js. Create confetti, sparkles, and even custom effects in your notes!'';
            homepage = "https://github.com/shap-po/obsidian-party";
        };
    }
);
    "obsidian-paste-as-html" = (
    let
        baseUrl = "https://github.com/maotong06/obsidian-paste-as-html-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-paste-as-html";
        version = "1.0.2";
        outputHash = "sha256-S3N6bJ7lpxWHXnY+PZxA33QXJl3sBeek4L3YwR4KpCc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste As HTML, Keep the original CSS style. Paste from web browser.'';
            homepage = "https://github.com/maotong06/obsidian-paste-as-html-plugin";
        };
    }
);
    "obsidian-paste-image-rename" = (
    let
        baseUrl = "https://github.com/reorx/obsidian-paste-image-rename/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-paste-image-rename";
        version = "1.6.1";
        outputHash = "sha256-qIlcFtDsysgZWthp1VocECBoeuQb9RB/SQnm6crnGNI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename image after pasting, support name pattern and auto renaming.'';
            homepage = "https://github.com/reorx/obsidian-paste-image-rename";
        };
    }
);
    "obsidian-paste-png-to-jpeg" = (
    let
        baseUrl = "https://github.com/musug/obsidian-paste-png-to-jpeg/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-paste-png-to-jpeg";
        version = "1.0.4";
        outputHash = "sha256-pJZQN+iWkh9/xB4Hbmh1NXLIERXMRQKFAh2NjnKHsdU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Screenshot png to jpeg and compress and rename.'';
            homepage = "https://github.com/musug/obsidian-paste-png-to-jpeg";
        };
    }
);
    "obsidian-paste-to-current-indentation" = (
    let
        baseUrl = "https://github.com/jglev/obsidian-paste-mode/releases/download/5.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-paste-to-current-indentation";
        version = "5.0.2";
        outputHash = "sha256-S8bhrBl522cJayljULj1pbEIsd6wAdqgd9YbmpX8TrQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste content and mark block quotes at any level of indentation.'';
            homepage = "https://github.com/jglev/obsidian-paste-mode";
        };
    }
);
    "obsidian-path-finder" = (
    let
        baseUrl = "https://github.com/jerrywcy/obsidian-path-finder/releases/download/1.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-path-finder";
        version = "1.1.7";
        outputHash = "sha256-QWF5DFqM5h25jC4+yfuph5sljHYKY61wDFTQZrSSzds=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find all paths between two notes and render them as graph or text.'';
            homepage = "https://github.com/jerrywcy/obsidian-path-finder";
        };
    }
);
    "obsidian-path-title" = (
    let
        baseUrl = "https://github.com/jdeal/obsidian-path-title-plugin/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-path-title";
        version = "1.0.7";
        outputHash = "sha256-qpBM7GPfvOxQ17oPsrpvwTJ8bbhlyOV2AevLQYsB4Vg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add the path (or optional replacement) to the filename title of each pane.'';
            homepage = "https://github.com/jdeal/obsidian-path-title-plugin";
        };
    }
);
    "obsidian-pending-notes" = (
    let
        baseUrl = "https://github.com/ulisesantana/obsidian-pending-notes/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pending-notes";
        version = "0.10.0";
        outputHash = "sha256-2D0u5MejPdwlCtMbzCIhid0vLcaQ8McvKz3DbaKJMJI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search links without notes in your vault.'';
            homepage = "https://github.com/ulisesantana/obsidian-pending-notes";
        };
    }
);
    "obsidian-plantuml" = (
    let
        baseUrl = "https://github.com/joethei/obsidian-plantuml/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plantuml";
        version = "1.8.0";
        outputHash = "sha256-dGB/p8VP5u9D4QZXr3J7fPm2Mo4d3/I7qnGAeMNtfyc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate PlantUML diagrams.'';
            homepage = "https://github.com/joethei/obsidian-plantuml";
        };
    }
);
    "obsidian-plotly" = (
    let
        baseUrl = "https://github.com/Dmytro-Shulha/obsidian-plotly/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plotly";
        version = "0.0.6";
        outputHash = "sha256-NLANxr3juj8KfyrQzAqlGilWlpTtqm/PSwMo6b8fwWU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed Plotly charts in your notes.'';
            homepage = "https://github.com/Dmytro-Shulha/obsidian-plotly";
        };
    }
);
    "obsidian-pluck" = (
    let
        baseUrl = "https://github.com/kevboh/obsidian-pluck/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pluck";
        version = "1.0.1";
        outputHash = "sha256-mTEBaPKMTMh5jS2KXb1B9Lzf0JFNgyjqYz1yDCJw8F4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly create notes from web pages.'';
            homepage = "https://github.com/kevboh/obsidian-pluck";
        };
    }
);
    "obsidian-plugin-dynamodb" = (
    let
        baseUrl = "https://github.com/leenattress/obsidian-plugin-dynamodb/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-dynamodb";
        version = "1.0.1";
        outputHash = "sha256-YlwrwMV18XkOGUDvserDtpS5SWfKyxKoiZK3smnLnxM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Query AWS DynamoDB and render tables inside documents.'';
            homepage = "https://github.com/leenattress/obsidian-plugin-dynamodb";
        };
    }
);
    "obsidian-plugin-groups" = (
    let
        baseUrl = "https://github.com/Mocca101/obsidian-plugin-groups/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-groups";
        version = "2.1.0";
        outputHash = "sha256-y838r/irCGml1+3soj5WQEaq+MjGoimgL2Gx23pFCsA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your Plugins with groups: Enable & disable multiple plugins at once or delay their startup to speed up your Obsidian start up time.'';
            homepage = "https://github.com/Mocca101/obsidian-plugin-groups";
        };
    }
);
    "obsidian-plugin-prettier" = (
    let
        baseUrl = "https://github.com/hipstersmoothie/obsidian-plugin-prettier/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-prettier";
        version = "0.1.1";
        outputHash = "sha256-kEeIaI4aZ43fPu4kliGMFuHg2OhKwMO02mHFFTUw81c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Opinionated formatting for your notes.'';
            homepage = "https://github.com/hipstersmoothie/obsidian-plugin-prettier";
        };
    }
);
    "obsidian-plugin-tagged-documents-viewer" = (
    let
        baseUrl = "https://github.com/mgeduld/obsidian-tagged-documents-viewer/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-tagged-documents-viewer";
        version = "1.0.2";
        outputHash = "sha256-XO2Uc7v6Ffj0tgzQkLcVdcSeA2IMufat0mW2tQOTCNE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a modal with scrollable content of all documents that contain a specific tag or tags.'';
            homepage = "https://github.com/mgeduld/obsidian-tagged-documents-viewer";
        };
    }
);
    "obsidian-plugin-time-diff" = (
    let
        baseUrl = "https://github.com/dominiczaq/obsidian-plugin-time-diff/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-time-diff";
        version = "1.2.0";
        outputHash = "sha256-1x82rNmeP2AO+JQTVB0yXmU6svSaFFEIztuOcACEt+c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Calculate and displays diff in hours and minutes between two dates in `timediff` Markdown block.'';
            homepage = "https://github.com/dominiczaq/obsidian-plugin-time-diff";
        };
    }
);
    "obsidian-plugin-toc" = (
    let
        baseUrl = "https://github.com/hipstersmoothie/obsidian-plugin-toc/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-toc";
        version = "0.2.0";
        outputHash = "sha256-ChbfB0UeFkKflLgJqQWczSr7TyoIkT2uxbvGpsoCIA0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a table of contents for a note.'';
            homepage = "https://github.com/hipstersmoothie/obsidian-plugin-toc";
        };
    }
);
    "obsidian-plugin-todo" = (
    let
        baseUrl = "https://github.com/larslockefeer/obsidian-plugin-todo/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-todo";
        version = "0.2.8";
        outputHash = "sha256-4IZUltPypvJipnxjHAlXzs+jCPGgkggike+8t934yyk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collect all outstanding TODOs from your vault and presents them in lists Today, Scheduled, Inbox and Someday/Maybe.'';
            homepage = "https://github.com/larslockefeer/obsidian-plugin-todo";
        };
    }
);
    "obsidian-plugin-update-tracker" = (
    let
        baseUrl = "https://github.com/swar8080/obsidian-plugin-update-tracker/releases/download/1.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-plugin-update-tracker";
        version = "1.7.0";
        outputHash = "sha256-fGVRf+Qg/8NCaRU1Z3pjBYKJ78ABJu+wjCIJbzX6KqA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Know when installed plugins have updates and evaluate the risk of upgrading.'';
            homepage = "https://github.com/swar8080/obsidian-plugin-update-tracker";
        };
    }
);
    "obsidian-pomodoro-plugin" = (
    let
        baseUrl = "https://github.com/tokuhirom/obsidian-pomodoro-plugin/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pomodoro-plugin";
        version = "0.0.2";
        outputHash = "sha256-VWG5PzmR6LCh8KmR3non6wnUubrYdjfr4Ky0dO6PUWk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple pomodoro time tracking.'';
            homepage = "https://github.com/tokuhirom/obsidian-pomodoro-plugin";
        };
    }
);
    "obsidian-power-search" = (
    let
        baseUrl = "https://github.com/aviral-batra/obsidian-power-search/releases/download/0.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-power-search";
        version = "0.0.13";
        outputHash = "sha256-rBxwhKpJ+wbxa5Sc1YQ+DeHzxJwMYZB2IUeElbiD1Sg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search notes based on current line.'';
            homepage = "https://github.com/aviral-batra/obsidian-power-search";
        };
    }
);
    "obsidian-pretty-bibtex" = (
    let
        baseUrl = "https://github.com/sandrofigo/obsidian-pretty-bibtex/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-pretty-bibtex";
        version = "2.0.1";
        outputHash = "sha256-l5ELYcfN9CRXkVFBk/LdUgQOf/jkxzWId8uIFSgR/CE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show raw BibTeX bibliography entries in a prettier way.'';
            homepage = "https://github.com/sandrofigo/obsidian-pretty-bibtex";
        };
    }
);
    "obsidian-project-garden" = (
    let
        baseUrl = "https://github.com/bgoosman/obsidian-project-garden/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-project-garden";
        version = "1.0.4";
        outputHash = "sha256-Nof58KJ711drR4aTZK9RvhR3oHyNiZjDj87LcdN+DdE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''See all your projects in one place.'';
            homepage = "https://github.com/bgoosman/obsidian-project-garden";
        };
    }
);
    "obsidian-prominent-starred-files" = (
    let
        baseUrl = "https://github.com/javalent/prominent-files/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-prominent-starred-files";
        version = "2.0.2";
        outputHash = "sha256-F/1ioxq6Tax/ViMv2DPL1z1wOJQpUuSDtPJe/XU9Oig=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prominently display bookmarked notes in the file explorer.'';
            homepage = "https://github.com/javalent/prominent-files";
        };
    }
);
    "obsidian-qrcode-plugin" = (
    let
        baseUrl = "https://github.com/rudimuc/obsidian-qrcode/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-qrcode-plugin";
        version = "1.2.1";
        outputHash = "sha256-EWy0L1BVNjrSoQ505I8kgjRP3RYqBjQcg9tXWPPHuvY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''QR code generator.'';
            homepage = "https://github.com/rudimuc/obsidian-qrcode";
        };
    }
);
    "obsidian-query-language" = (
    let
        baseUrl = "https://github.com/jplattel/obsidian-query-language/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-query-language";
        version = "2.1.1";
        outputHash = "sha256-snxP85i0NZlY41l2/lXdP2zJNn2pbKfcthPXNCvqtTA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Query notes and represent data.'';
            homepage = "https://github.com/jplattel/obsidian-query-language";
        };
    }
);
    "obsidian-quickshare" = (
    let
        baseUrl = "https://github.com/mcndt/obsidian-quickshare/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-quickshare";
        version = "1.3.1";
        outputHash = "sha256-zVBkSuL5mRlODcSfN/ULo7VTgk2L18vQpFXcjPvObx8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Securely share your notes with one click. Notes are end-to-end encrypted. No API keys or configuration required.'';
            homepage = "https://github.com/mcndt/obsidian-quickshare";
        };
    }
);
    "obsidian-quiet-outline" = (
    let
        baseUrl = "https://github.com/guopenghui/obsidian-quiet-outline/releases/download/0.5.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-quiet-outline";
        version = "0.5.12";
        outputHash = "sha256-5MRWrv9GTf77+N/ilUe6iAGtgv5OLXfb/YannmjH+Aw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make outline quiet and more powerful, including no-auto-expand, rendering heading as Markdown, and search support.'';
            homepage = "https://github.com/guopenghui/obsidian-quiet-outline";
        };
    }
);
    "obsidian-quran-lookup" = (
    let
        baseUrl = "https://github.com/abuibrahim2/quranlookup/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-quran-lookup";
        version = "0.3.1";
        outputHash = "sha256-IfPagfFVNoFoIySjc2eywTigQvtPMvMKSeXkQIqNUi4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily add Quran verses and translations.'';
            homepage = "https://github.com/abuibrahim2/quranlookup";
        };
    }
);
    "obsidian-raindrop-highlights" = (
    let
        baseUrl = "https://github.com/kaiiiz/obsidian-raindrop-highlights-plugin/releases/download/0.0.24";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-raindrop-highlights";
        version = "0.0.24";
        outputHash = "sha256-QueKfrdZNL+wWkHuiUPPn4w8m16bccLlIgMmQONelPw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Raindrop.io highlights.'';
            homepage = "https://github.com/kaiiiz/obsidian-raindrop-highlights-plugin";
        };
    }
);
    "obsidian-random-todo" = (
    let
        baseUrl = "https://github.com/NatiAris/obsidian-random-todo/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-random-todo";
        version = "0.1.3";
        outputHash = "sha256-92qWVv5VLPE+m1C7dboCtZCpM1uTXPvo4U6Whld2ZkA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a random file containing your custom to-do marker, or a random marker at its position.'';
            homepage = "https://github.com/NatiAris/obsidian-random-todo";
        };
    }
);
    "obsidian-rant" = (
    let
        baseUrl = "https://github.com/lanice/obsidian-rant/releases/download/0.5.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-rant";
        version = "0.5.9";
        outputHash = "sha256-j3+s9MLMFwzYtbix0KeeyK11TwHzf5cJQK2JZsIzL7E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Thin wrapper around the high-level procedural templating language Rant.'';
            homepage = "https://github.com/lanice/obsidian-rant";
        };
    }
);
    "obsidian-rapid-notes" = (
    let
        baseUrl = "https://github.com/valteriomon/obsidian-rapid-notes/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-rapid-notes";
        version = "1.2.6";
        outputHash = "sha256-dzdbSEWQuUaVODhrh1qvmH8v8X4zU1f/JG+S7NNwCCQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and place notes quickly in specific folders based on predefined prefixes.'';
            homepage = "https://github.com/valteriomon/obsidian-rapid-notes";
        };
    }
);
    "obsidian-react-components" = (
    let
        baseUrl = "https://github.com/elias-sundqvist/obsidian-react-components/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-react-components";
        version = "0.1.6";
        outputHash = "sha256-xShaz4S4KfkccE9fl/xY4cqFcD8g0c+lXnc2Ldg2y0A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write and use React (Jsx) components in your notes.'';
            homepage = "https://github.com/elias-sundqvist/obsidian-react-components";
        };
    }
);
    "obsidian-read-it-later" = (
    let
        baseUrl = "https://github.com/DominikPieper/obsidian-ReadItLater/releases/download/0.11.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-read-it-later";
        version = "0.11.4";
        outputHash = "sha256-0AzN8Dhs7Bl2x7IHwqBFb/ftfM9E6NMu/eR0n9/vYYc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save online content to your Vault, utilize embedded template engine and organize your reading list to your needs. Preserve the web with ReadItLater.'';
            homepage = "https://github.com/DominikPieper/obsidian-ReadItLater";
        };
    }
);
    "obsidian-reading-time" = (
    let
        baseUrl = "https://github.com/avr/obsidian-reading-time/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-reading-time";
        version = "1.1.2";
        outputHash = "sha256-IsAy/x2jF570dUWHx28jF24lywSn0xCcS9dr+UExSew=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add the current note"s reading time to the status bar.'';
            homepage = "https://github.com/avr/obsidian-reading-time";
        };
    }
);
    "obsidian-readlater" = (
    let
        baseUrl = "https://github.com/Canna71/obsidian-readlater/releases/download/0.8.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-readlater";
        version = "0.8.2";
        outputHash = "sha256-rm7rHeYDijKUZyULLxXBYkCIt/b2Z5+Gr4W07dKhHnA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synch web pages to Markdown and integrate with read-it-later apps (Pocket, Instapaper).'';
            homepage = "https://github.com/Canna71/obsidian-readlater";
        };
    }
);
    "obsidian-readwise" = (
    let
        baseUrl = "https://github.com/renehernandez/obsidian-readwise/releases/download/0.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-readwise";
        version = "0.0.9";
        outputHash = "sha256-DPzULnlS9K2VdUbqMId43GCBYdfHDhiMjJfnQaQzySQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Readwise highlights into your notes.'';
            homepage = "https://github.com/renehernandez/obsidian-readwise";
        };
    }
);
    "obsidian-recall" = (
    let
        baseUrl = "https://github.com/martin-jw/obsidian-recall/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-recall";
        version = "0.1.1";
        outputHash = "sha256-qRpua5W8bTgVuisPWYrA2CiIwv+LgEHJDRLoRHS3k+U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A flexible and configurable spaced repetition plugin with multiple algorithms to choose from.'';
            homepage = "https://github.com/martin-jw/obsidian-recall";
        };
    }
);
    "obsidian-redirect" = (
    let
        baseUrl = "https://github.com/jglev/obsidian-redirect/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-redirect";
        version = "0.2.0";
        outputHash = "sha256-5E8dVqUvmtA2+CQRpB6omGKUQ0A4sayj1FKSzhHyrME=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Facilitate management of especially non-Markdown files, by allowing aliases to be set on any file.'';
            homepage = "https://github.com/jglev/obsidian-redirect";
        };
    }
);
    "obsidian-regex-pipeline" = (
    let
        baseUrl = "https://github.com/No3371/obsidian-regex-pipeline/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-regex-pipeline";
        version = "1.4.0";
        outputHash = "sha256-zhX/zADE0FcR3ZRtet1cYV3Lto/bOzrbscBdbpB2c7o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set up custom regex rules to automatically format notes.'';
            homepage = "https://github.com/No3371/obsidian-regex-pipeline";
        };
    }
);
    "obsidian-regex-replace" = (
    let
        baseUrl = "https://github.com/Gru80/obsidian-regex-replace/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-regex-replace";
        version = "1.2.0";
        outputHash = "sha256-bkL16Oqjvvd/zGr1LNaQUiuI05ilhKT1ARTwq4pE9HE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A find/replace dialog which optionally supports regular expressions and scope (full document or text selection).'';
            homepage = "https://github.com/Gru80/obsidian-regex-replace";
        };
    }
);
    "obsidian-related-notes-finder" = (
    let
        baseUrl = "https://github.com/lifegems/obsidian-related-notes-finder/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-related-notes-finder";
        version = "1.1.0";
        outputHash = "sha256-dUu5La/OgciC2xvp5ieMBdFN16FGqgUfc9O+YUhAC7E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add extra features for finding related notes.'';
            homepage = "https://github.com/lifegems/obsidian-related-notes-finder";
        };
    }
);
    "obsidian-relation-pane" = (
    let
        baseUrl = "https://github.com/mottox2/obsidian-relation-pane/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-relation-pane";
        version = "0.0.5";
        outputHash = "sha256-kHPJjDr8mAAjDWAw7Lrf5BTdN+IbtFKtMqDWjMVF6Ag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a panel that summarize relations between notes.'';
            homepage = "https://github.com/mottox2/obsidian-relation-pane";
        };
    }
);
    "obsidian-relative-find" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-relative-find/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-relative-find";
        version = "0.5.0";
        outputHash = "sha256-PT1wZKRqPuKQU8/VED1EIODhm352+1D2ttBsFSo7fMI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search relative to your cursor position.'';
            homepage = "https://github.com/phibr0/obsidian-relative-find";
        };
    }
);
    "obsidian-relative-line-numbers" = (
    let
        baseUrl = "https://github.com/nadavspi/obsidian-relative-line-numbers/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-relative-line-numbers";
        version = "3.1.0";
        outputHash = "sha256-aSFlRONkwHVuoLBgSYbxqGExz0ofDIbsjl2UxUvj0Tw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable relative line numbers in editor mode.'';
            homepage = "https://github.com/nadavspi/obsidian-relative-line-numbers";
        };
    }
);
    "obsidian-relativenumber" = (
    let
        baseUrl = "https://github.com/thisdotrob/obsidian-relativenumber-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-relativenumber";
        version = "1.0.0";
        outputHash = "sha256-tQAxPAsqVlXQq6HK/GNGpwBCaWW6mEch/DL+K1qtfG0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display relative line numbers in the editor"s gutter.'';
            homepage = "https://github.com/thisdotrob/obsidian-relativenumber-plugin";
        };
    }
);
    "obsidian-release-timeline" = (
    let
        baseUrl = "https://github.com/cakechaser/obsidian-release-timeline/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-release-timeline";
        version = "1.3.0";
        outputHash = "sha256-ks0x/F0kFQaWnwccNOsJR5J4rXQcxEk7naEQkHdOEsg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Release timeline rendered based on notes metadata with a Dataview-like syntax.'';
            homepage = "https://github.com/cakechaser/obsidian-release-timeline";
        };
    }
);
    "obsidian-remember-file-state" = (
    let
        baseUrl = "https://github.com/ludovicchabant/obsidian-remember-file-state/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-remember-file-state";
        version = "1.1.2";
        outputHash = "sha256-+aGg0CWHdpPhbC+6lQ+d487QNIcmMFtdfxda6pzdQ2A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remember cursor position, selection, scrolling, and more for each file.'';
            homepage = "https://github.com/ludovicchabant/obsidian-remember-file-state";
        };
    }
);
    "obsidian-reminder-plugin" = (
    let
        baseUrl = "https://github.com/uphy/obsidian-reminder/releases/download/1.2.0-beta.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-reminder-plugin";
        version = "1.2.0-beta.1";
        outputHash = "sha256-U+9gTVXtefQDaA57ajqXS02waEaypqWV3K0eZO7cm7k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage Markdown TODOs with reminder.'';
            homepage = "https://github.com/uphy/obsidian-reminder";
        };
    }
);
    "obsidian-reset-font-size" = (
    let
        baseUrl = "https://github.com/luckman212/obsidian-reset-font-size/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-reset-font-size";
        version = "1.0.2";
        outputHash = "sha256-FWVq4Ckfg0tj7fOTf2xGzsncPRvwKFfuMI4YCOoNoco=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a button and command to reset the font size back to its default value.'';
            homepage = "https://github.com/luckman212/obsidian-reset-font-size";
        };
    }
);
    "obsidian-reveal-active-file" = (
    let
        baseUrl = "https://github.com/shichongrui/obsidian-reveal-active-file/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-reveal-active-file";
        version = "1.0.3";
        outputHash = "sha256-FZN3B/msNQJ9iqIibxgJGXuWImo+ouw5sBunUI+vYzU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically reveal the currently active file in the side navigation.'';
            homepage = "https://github.com/shichongrui/obsidian-reveal-active-file";
        };
    }
);
    "obsidian-review-notes-plugin" = (
    let
        baseUrl = "https://github.com/tjandy98/obsidian-review-notes-plugin/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-review-notes-plugin";
        version = "1.1.1";
        outputHash = "sha256-WB4vbTUDC1ARd3JvWsnLTh2AdW3cDMfuVegjdhS4EPQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show recently modified and newly created files.'';
            homepage = "https://github.com/tjandy98/obsidian-review-notes-plugin";
        };
    }
);
    "obsidian-rewarder" = (
    let
        baseUrl = "https://github.com/Gnopps/obsidian-rewarder/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-rewarder";
        version = "0.4.0";
        outputHash = "sha256-ANTDwFvYr151Mez2ag4WX8C8psJJPbFJIaQHkpfYw9U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reward yourself for completing tasks/todos, highly configurable.'';
            homepage = "https://github.com/Gnopps/obsidian-rewarder";
        };
    }
);
    "obsidian-rich-links" = (
    let
        baseUrl = "https://github.com/dhamaniasad/obsidian-rich-links/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-rich-links";
        version = "1.2.0";
        outputHash = "sha256-kjnzcrxDPN+Y94nsTYzPx7OjjLh59fLeJAED/9eYkcc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert URLs in your notes to rich link previews.'';
            homepage = "https://github.com/dhamaniasad/obsidian-rich-links";
        };
    }
);
    "obsidian-rollover-daily-todos" = (
    let
        baseUrl = "https://github.com/lumoe/obsidian-rollover-daily-todos/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-rollover-daily-todos";
        version = "1.2.0";
        outputHash = "sha256-/4wt6U4mJ5j5wicMWFB9Q0zldy5B4vfqpHJ/1qaIaAo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rollover any unchecked checkboxes from your last daily note into today"s note.'';
            homepage = "https://github.com/lumoe/obsidian-rollover-daily-todos";
        };
    }
);
    "obsidian-rtl" = (
    let
        baseUrl = "https://github.com/esm7/obsidian-rtl/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-rtl";
        version = "2.0.1";
        outputHash = "sha256-OwHiskKnAWmDq/RKmMYSE64ilrLEh+Dd4WszeeWnNg0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Right-to-left (RTL) text direction support for languages like Arabic, Hebrew, and Farsi.'';
            homepage = "https://github.com/esm7/obsidian-rtl";
        };
    }
);
    "obsidian-sakana-widget" = (
    let
        baseUrl = "https://github.com/Quorafind/obsidian-sakana-widget/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-sakana-widget";
        version = "2.1.0";
        outputHash = "sha256-/8/gO3R+xIMX86jeCkZVoeyfaiJo+ZK55JKXPRrD5XI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add the Sakana! widget.'';
            homepage = "https://github.com/Quorafind/obsidian-sakana-widget";
        };
    }
);
    "obsidian-save-as-gist" = (
    let
        baseUrl = "https://github.com/ghedamat/obsidian-save-as-gist/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-save-as-gist";
        version = "0.0.2";
        outputHash = "sha256-pTexGkLHSvIDDLISDtu5ROMD+dvs2j9cyVt6ir0lJls=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save current note as a GitHub Gist.'';
            homepage = "https://github.com/ghedamat/obsidian-save-as-gist";
        };
    }
);
    "obsidian-screwdriver" = (
    let
        baseUrl = "https://github.com/vrtmrz/obsidian-screwdriver/releases/download/0.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-screwdriver";
        version = "0.0.9";
        outputHash = "sha256-1KgbDYAUfI7x2ady8YKvoOHV+Ve/F31gtC0MNfaEOUA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Utility to put any files in and out under your vault.'';
            homepage = "https://github.com/vrtmrz/obsidian-screwdriver";
        };
    }
);
    "obsidian-scroll-offset" = (
    let
        baseUrl = "https://github.com/lijyze/scroll-offset/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-scroll-offset";
        version = "1.0.4";
        outputHash = "sha256-tPK65egFj3szfd8qcwyJFLYQHm3edxgNtVTOgFkBTsE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Preserve custom distances before or after cursor.'';
            homepage = "https://github.com/lijyze/scroll-offset";
        };
    }
);
    "obsidian-scroll-to-top-plugin" = (
    let
        baseUrl = "https://github.com/cloudhao1999/obsidian-scroll-to-top-plugin/releases/download/2.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-scroll-to-top-plugin";
        version = "2.1.6";
        outputHash = "sha256-VGFt84RqY+TM5EBUrcaaOwep1H55TU5ze7tCmFmZWac=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a button to scroll to the top of the current note.'';
            homepage = "https://github.com/cloudhao1999/obsidian-scroll-to-top-plugin";
        };
    }
);
    "obsidian-search-everywhere-plugin" = (
    let
        baseUrl = "https://github.com/Mom0aut/obsidian-search-everywhere/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-search-everywhere-plugin";
        version = "1.0.10";
        outputHash = "sha256-TAR922bdIvjWoF3L10g/E+uT0VKKPPPNG63XvU4mqe4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search everywhere by pressing double shift like in IntelliJ.'';
            homepage = "https://github.com/Mom0aut/obsidian-search-everywhere";
        };
    }
);
    "obsidian-sentence-navigator" = (
    let
        baseUrl = "https://github.com/timhor/obsidian-sentence-navigator/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-sentence-navigator";
        version = "1.3.0";
        outputHash = "sha256-5I6Av0dKfcegAo/A7hqlQcQFBz6dZmPP5+5FhVvMmT8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manipulate sentences as a unit of movement.'';
            homepage = "https://github.com/timhor/obsidian-sentence-navigator";
        };
    }
);
    "obsidian-sequence-hotkeys" = (
    let
        baseUrl = "https://github.com/moolmanruan/obsidian-sequence-hotkeys/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-sequence-hotkeys";
        version = "0.6.0";
        outputHash = "sha256-eWZeY3/86dIpwRtrTV+dd1w5eUk/C1hbQpqWieKKKy4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set hotkeys with key sequences instead of a single chord.'';
            homepage = "https://github.com/moolmanruan/obsidian-sequence-hotkeys";
        };
    }
);
    "obsidian-share-as-gist" = (
    let
        baseUrl = "https://github.com/timrogers/obsidian-share-as-gist/releases/download/1.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-share-as-gist";
        version = "1.9.0";
        outputHash = "sha256-jVcCZ0nzEVfWPqh1XiSPpZtvG4lZ8LQRMOBhq9JLbGQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share a note as a GitHub.com Gist.'';
            homepage = "https://github.com/timrogers/obsidian-share-as-gist";
        };
    }
);
    "obsidian-shellcommands" = (
    let
        baseUrl = "https://github.com/Taitava/obsidian-shellcommands/releases/download/0.23.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-shellcommands";
        version = "0.23.0";
        outputHash = "sha256-ZnQzdKlFMFE3sctRbtEBiLrs7tQ9DbsZ5SCNsr9EoNM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define system commands that you want to execute via command palette, hotkeys, URI links or automated events. E.g. open external applications or perform automated file modifications.'';
            homepage = "https://github.com/Taitava/obsidian-shellcommands";
        };
    }
);
    "obsidian-shortcut-launcher" = (
    let
        baseUrl = "https://github.com/macstories/obsidian-shortcut-launcher/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-shortcut-launcher";
        version = "1.1.0";
        outputHash = "sha256-RiiX18aPN4N0iodn2Bpjie8VIJ26uSe84jsKWyPBeUc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Trigger shortcuts in Apple"s Shortcuts app from Obsidian with custom commands.'';
            homepage = "https://github.com/macstories/obsidian-shortcut-launcher";
        };
    }
);
    "obsidian-shortcuts-for-starred-files" = (
    let
        baseUrl = "https://github.com/Vinzent03/obsidian-shortcuts-for-starred-files/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-shortcuts-for-starred-files";
        version = "1.1.1";
        outputHash = "sha256-ejBYhZ7WAYepn1hplHsrUbobQYFDPW4un7LUwFlNQ8g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set an individual hotkey for the first 9 bookmarked files and open them just with your keyboard.'';
            homepage = "https://github.com/Vinzent03/obsidian-shortcuts-for-starred-files";
        };
    }
);
    "obsidian-show-file-path" = (
    let
        baseUrl = "https://github.com/ravimashru/obsidian-show-file-path/releases/download/0.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-show-file-path";
        version = "0.5.2";
        outputHash = "sha256-66wr9iDJDbUDG3U2msJT0RFVyfbKZ5JdyL7Rqk8ptBU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show the full path of the currently open file in the status bar.'';
            homepage = "https://github.com/ravimashru/obsidian-show-file-path";
        };
    }
);
    "obsidian-sidebar-expand-on-hover" = (
    let
        baseUrl = "https://github.com/toiq/obsidian-sidebar-expand-on-hover/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-sidebar-expand-on-hover";
        version = "1.0.1";
        outputHash = "sha256-u4sH8u4noMZqyGIOg//8inQqJkvVNalbYM9bcypU1FQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Expand or collapse the sidebar when your mouse is hovering on the left ribbon.'';
            homepage = "https://github.com/toiq/obsidian-sidebar-expand-on-hover";
        };
    }
);
    "obsidian-simple-mention" = (
    let
        baseUrl = "https://github.com/der-tobi/obsidian-simple-mention/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-simple-mention";
        version = "1.0.0";
        outputHash = "sha256-Kwt97mXgjDzfHXLEBhlKS2FNNat3De1zOC65cli7mqc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get highlighted mentions and mention suggestions. Find all occurrences of a mention.'';
            homepage = "https://github.com/der-tobi/obsidian-simple-mention";
        };
    }
);
    "obsidian-siteswap" = (
    let
        baseUrl = "https://github.com/tdresser/obsidian-siteswap/releases/download/0.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-siteswap";
        version = "0.1.8";
        outputHash = "sha256-0t+WKwFmhTWhTGCfKqfmCQihwH8/VFKEcMeLxNUc5Lw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize Juggling Pattern Siteswap via the JugglingLab gif server.'';
            homepage = "https://github.com/tdresser/obsidian-siteswap";
        };
    }
);
    "obsidian-smart-links" = (
    let
        baseUrl = "https://github.com/kemayo/obsidian-smart-links/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-smart-links";
        version = "1.0.2";
        outputHash = "sha256-E30vEsVV+XrurYqF5e1HUG6Is2+r6Z7hX60rnPmCzjc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically detect strings using regular expressions and turn them into links in reading view. Useful for stock symbols, issue tracking, etc.'';
            homepage = "https://github.com/kemayo/obsidian-smart-links";
        };
    }
);
    "obsidian-smart-typography" = (
    let
        baseUrl = "https://github.com/mgmeyers/obsidian-smart-typography/releases/download/1.0.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-smart-typography";
        version = "1.0.18";
        outputHash = "sha256-A9RhtvHEMS9dJsj51tqvSYgOBbZ07aOMPQeGu/F+dIg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert quotes to curly quotes, dashes to em dashes, and periods to ellipses.'';
            homepage = "https://github.com/mgmeyers/obsidian-smart-typography";
        };
    }
);
    "obsidian-snippetor" = (
    let
        baseUrl = "https://github.com/ebullient/obsidian-snippetor/releases/download/0.4.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-snippetor";
        version = "0.4.6";
        outputHash = "sha256-90HzWFXz/xUb3ULA84V3XMFlE0t1ZRxjJNak4S6iRYU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and tweak common snippets.'';
            homepage = "https://github.com/ebullient/obsidian-snippetor";
        };
    }
);
    "obsidian-soomda" = (
    let
        baseUrl = "https://github.com/michaellee/soomda/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-soomda";
        version = "1.0.4";
        outputHash = "sha256-+8x76+1CaavnKauvF+JZDdL3WIf9mkeoMH/wNg/Gt9w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly hide your sidebars.'';
            homepage = "https://github.com/michaellee/soomda";
        };
    }
);
    "obsidian-sort-and-permute-lines" = (
    let
        baseUrl = "https://github.com/Vinzent03/obsidian-sort-and-permute-lines/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-sort-and-permute-lines";
        version = "0.7.0";
        outputHash = "sha256-hm3BpHuUsSuPGCj7Vdmc0v/adf3vWm9NJCi4cnoUUAo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sort and Permute lines in whole file or selection.'';
            homepage = "https://github.com/Vinzent03/obsidian-sort-and-permute-lines";
        };
    }
);
    "obsidian-spaced-repetition" = (
    let
        baseUrl = "https://github.com/st3v3nmw/obsidian-spaced-repetition/releases/download/1.14.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-spaced-repetition";
        version = "1.14.0";
        outputHash = "sha256-wgROrm00/s5jgFrF+UfgpjZZpOJw/UIOLUn3MpfF9w0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fight the forgetting curve by reviewing flashcards & entire notes.'';
            homepage = "https://github.com/st3v3nmw/obsidian-spaced-repetition";
        };
    }
);
    "obsidian-stack-overflow" = (
    let
        baseUrl = "https://github.com/bramses/obsidian-stack-overflow/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-stack-overflow";
        version = "1.0.1";
        outputHash = "sha256-jhxToXrN4mN/W9Z+v5WUBjy5yNiOQ4Kwz7XeYqn4B/k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy and paste Stack Overflow answers.'';
            homepage = "https://github.com/bramses/obsidian-stack-overflow";
        };
    }
);
    "obsidian-state-switcher" = (
    let
        baseUrl = "https://github.com/lijyze/obsidian-state-switcher/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-state-switcher";
        version = "1.3.3";
        outputHash = "sha256-uGW3Zc4d9VXC7bWPmHYm8bRHjEHgJvSTf+SYQhNE3mk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep you away from directly operating YAML frontmatter.'';
            homepage = "https://github.com/lijyze/obsidian-state-switcher";
        };
    }
);
    "obsidian-static-file-server" = (
    let
        baseUrl = "https://github.com/elias-sundqvist/obsidian-static-file-server/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-static-file-server";
        version = "0.0.4";
        outputHash = "sha256-mNo0g9ecr+pI64FCPIKLGME7dbDl05Ip5U6SUxkclp0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Host subfolders as static file servers.'';
            homepage = "https://github.com/elias-sundqvist/obsidian-static-file-server";
        };
    }
);
    "obsidian-statusbar-pomo" = (
    let
        baseUrl = "https://github.com/kzhovn/statusbar-pomo-obsidian/releases/download/0.1.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-statusbar-pomo";
        version = "0.1.15";
        outputHash = "sha256-7eVwKvago5yOPCu/Q0bbotd+CPDNJ50QozqzPxvgkWg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a pomodoro timer to your status bar.'';
            homepage = "https://github.com/kzhovn/statusbar-pomo-obsidian";
        };
    }
);
    "obsidian-steemit" = (
    let
        baseUrl = "https://github.com/anpigon/obsidian-steemit-plugin/releases/download/0.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-steemit";
        version = "0.4.4";
        outputHash = "sha256-s774lekCM+fb8vckURrfREGdHjxSdRVci617DdsG6TM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish documents to Steemit.'';
            homepage = "https://github.com/anpigon/obsidian-steemit-plugin";
        };
    }
);
    "obsidian-stille" = (
    let
        baseUrl = "https://github.com/michaellee/stille/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-stille";
        version = "1.3.4";
        outputHash = "sha256-qTaqJQfafdLIozqDcUhJ4nYk9W7DWaiES+saqqBDhzU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Focus on your writing, a section at a time.'';
            homepage = "https://github.com/michaellee/stille";
        };
    }
);
    "obsidian-stopwatch-plugin" = (
    let
        baseUrl = "https://github.com/tokuhirom/obsidian-stopwatch-plugin/releases/download/0.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-stopwatch-plugin";
        version = "0.0.8";
        outputHash = "sha256-ARiEgd+5t555HLOT0xu2j82J+d3Z8cmZyuGcZ5FZay8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a stopwatch in the sidebar.'';
            homepage = "https://github.com/tokuhirom/obsidian-stopwatch-plugin";
        };
    }
);
    "obsidian-structured-plugin" = (
    let
        baseUrl = "https://github.com/dobrovolsky/obsidian-structure/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-structured-plugin";
        version = "1.1.2";
        outputHash = "sha256-9JmgDM4jqu7jADGUrduoafqSzPWcJlLMH5Sa+O4ZVag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Structured plugin. Create hierarchy in notes using "."'';
            homepage = "https://github.com/dobrovolsky/obsidian-structure";
        };
    }
);
    "obsidian-style-settings" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-style-settings/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-style-settings";
        version = "1.0.9";
        outputHash = "sha256-oJ65ABQshqa0MO6JJ68VvUehRictlGVq3INteOJKN+s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adjust theme, plugin, and snippet CSS variables.'';
            homepage = "https://github.com/obsidian-community/obsidian-style-settings";
        };
    }
);
    "obsidian-stylist" = (
    let
        baseUrl = "https://github.com/ixth/obsidian-stylist/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-stylist";
        version = "0.0.4";
        outputHash = "sha256-OcN9f3Kv0UGVtkGO8f0kJcQKBGljIVePwfsD0w+Z5Dk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add classes and styles on Markdown blocks.'';
            homepage = "https://github.com/ixth/obsidian-stylist";
        };
    }
);
    "obsidian-table-generator" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Table-Generator/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-table-generator";
        version = "1.4.1";
        outputHash = "sha256-fv2A5mlvb2I7PguYCjuv4+60qp+2yqydpzR3EErN9OE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate Markdown tables quickly like Typora.'';
            homepage = "https://github.com/Quorafind/Obsidian-Table-Generator";
        };
    }
);
    "obsidian-table-to-csv-exporter" = (
    let
        baseUrl = "https://github.com/metawops/obsidian-table-to-csv-export/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-table-to-csv-exporter";
        version = "0.1.4";
        outputHash = "sha256-zqweiEDQXXT7NxVB0WJhsiOPBgJ8HlgbdJhh6wLaBx4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export tables from a pane in reading mode into CSV files.'';
            homepage = "https://github.com/metawops/obsidian-table-to-csv-export";
        };
    }
);
    "obsidian-tagfolder" = (
    let
        baseUrl = "https://github.com/vrtmrz/obsidian-tagfolder/releases/download/0.18.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tagfolder";
        version = "0.18.13";
        outputHash = "sha256-4dthwO1EO/YCJCOgMNW3Qg/VECjF435BixDBZ2SN0No=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show tags as folder.'';
            homepage = "https://github.com/vrtmrz/obsidian-tagfolder";
        };
    }
);
    "obsidian-task-archiver" = (
    let
        baseUrl = "https://github.com/ivan-lednev/obsidian-task-archiver/releases/download/0.26.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-task-archiver";
        version = "0.26.1";
        outputHash = "sha256-TpLlZSkwgtXD3/rIkJ88iCIjR5fJy8aZtiAJ9g/OITE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move completed tasks to an archive with a date tree.'';
            homepage = "https://github.com/ivan-lednev/obsidian-task-archiver";
        };
    }
);
    "obsidian-task-collector" = (
    let
        baseUrl = "https://github.com/ebullient/obsidian-task-collector/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-task-collector";
        version = "1.2.1";
        outputHash = "sha256-fG0N7wz2KcRm+hFmGLDncxGXAcRjkahLWo/nDSs0rNA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change task status and collect tasks within a document using hotkeys and context menus.'';
            homepage = "https://github.com/ebullient/obsidian-task-collector";
        };
    }
);
    "obsidian-task-marker" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-task-marker/releases/download/0.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-task-marker";
        version = "0.6.2";
        outputHash = "sha256-1wf/TLnLP6Ei0Gjuk3pNSuNJL+Nj1sGg42W4AwABDdo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change task status and append text with hotkeys and context menu. Create, complete, cancel and mark tasks, cycle among configured task statuses, and append text (automatically).'';
            homepage = "https://github.com/wenlzhang/obsidian-task-marker";
        };
    }
);
    "obsidian-task-progress-bar" = (
    let
        baseUrl = "https://github.com/taskgenius/taskgenius-plugin/releases/download/9.14.0-beta.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-task-progress-bar";
        version = "9.14.0-beta.4";
        outputHash = "sha256-PoHD4ZmW90B5FOfeurcqjLrnBMZnZvn2kdfG4j62Zuo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Comprehensive task management that includes progress bars, task status cycling, and advanced task tracking features.'';
            homepage = "https://github.com/taskgenius/taskgenius-plugin";
        };
    }
);
    "obsidian-tasks-plugin" = (
    let
        baseUrl = "https://github.com/obsidian-tasks-group/obsidian-tasks/releases/download/7.23.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tasks-plugin";
        version = "7.23.1";
        outputHash = "sha256-LWGBRoS2YkDrbO4y6VsJNetUivytlRyD2r2lvlnCcBk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track tasks across your vault. Supports due dates, recurring tasks, done dates, sub-set of checklist items, and filtering.'';
            homepage = "https://github.com/obsidian-tasks-group/obsidian-tasks";
        };
    }
);
    "obsidian-telegraph-publish" = (
    let
        baseUrl = "https://github.com/reorx/obsidian-telegraph-publish/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-telegraph-publish";
        version = "1.2.4";
        outputHash = "sha256-9YnSjif5mruOlwmF0UmEtKENbSJptoXKyW+DM1UUJhE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your note to a Telegraph page.'';
            homepage = "https://github.com/reorx/obsidian-telegraph-publish";
        };
    }
);
    "obsidian-temple" = (
    let
        baseUrl = "https://github.com/garyng/obsidian-temple/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-temple";
        version = "0.4.0";
        outputHash = "sha256-Fcwkk4rAgl/WWx5yFjIMre4RH0TbYt2it6i3mQLw5Qc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Templating powered by Nunjucks.'';
            homepage = "https://github.com/garyng/obsidian-temple";
        };
    }
);
    "obsidian-text-expander-js" = (
    let
        baseUrl = "https://github.com/jon-heard/obsidian-inline-scripts/releases/download/0.24.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-text-expander-js";
        version = "0.24.12";
        outputHash = "sha256-f6Z5+cn3u4s1LwCWrxGOoK3Eu7PHud5Jax3WylOD9B8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Type text shortcuts which are then replaced with JavaScript generated text.'';
            homepage = "https://github.com/jon-heard/obsidian-inline-scripts";
        };
    }
);
    "obsidian-text-format" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-text-format/releases/download/3.2.1-b1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-text-format";
        version = "3.2.1-b1";
        outputHash = "sha256-IRi7b8jnjDXNIUQMBGw95UzjPDn1bfJXg5pwEDd0//w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format selected text upper/lower/capitalize.'';
            homepage = "https://github.com/Benature/obsidian-text-format";
        };
    }
);
    "obsidian-textgenerator-plugin" = (
    let
        baseUrl = "https://github.com/nhaouari/obsidian-textgenerator-plugin/releases/download/0.8.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-textgenerator-plugin";
        version = "0.8.7";
        outputHash = "sha256-UVWoYBQR44s3chJAJZaU1qRcVoQZnne/D9qlUY2d1mg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate text content using GPT-3 (OpenAI).'';
            homepage = "https://github.com/nhaouari/obsidian-textgenerator-plugin";
        };
    }
);
    "obsidian-theme-design-utilities" = (
    let
        baseUrl = "https://github.com/chrisgrieser/obsidian-theme-design-utilities/releases/download/0.22.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-theme-design-utilities";
        version = "0.22.4";
        outputHash = "sha256-wC9MYtpIincFrJ/zHaROTi0tLO7a43C+RpEUFqDDMKg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Some utilities and quality-of-life features for designers of Obsidian themes.'';
            homepage = "https://github.com/chrisgrieser/obsidian-theme-design-utilities";
        };
    }
);
    "obsidian-theme-toggler" = (
    let
        baseUrl = "https://github.com/larsmagnus/obsidian-theme-toggler/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-theme-toggler";
        version = "1.0.1";
        outputHash = "sha256-LdgxBUQ8FF6euw8aESf7R0gyrLWhQHi3EeVhzhyL7CU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle light or dark mode separately for each tab.'';
            homepage = "https://github.com/larsmagnus/obsidian-theme-toggler";
        };
    }
);
    "obsidian-things-link" = (
    let
        baseUrl = "https://github.com/gavinmn/obsidian-things-link/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-things-link";
        version = "1.1.2";
        outputHash = "sha256-FyRkw0Q7lh7aBiuoHcZ6KtJ2iWLVgDcC52jg2aja0WY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Seamlessly link an Note to a Things Project.'';
            homepage = "https://github.com/gavinmn/obsidian-things-link";
        };
    }
);
    "obsidian-things3-sync" = (
    let
        baseUrl = "https://github.com/royxue/obsidian-things3-sync/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-things3-sync";
        version = "1.3.0";
        outputHash = "sha256-3tJDtJMKCKo2iJ/VYlrNF/jSj4C752LC0Ul8MLHwxqE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync between Obsidian and Things3. Supports multi-language, tags and dates.'';
            homepage = "https://github.com/royxue/obsidian-things3-sync";
        };
    }
);
    "obsidian-thumbnails" = (
    let
        baseUrl = "https://github.com/Meikul/obsidian-thumbnails/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-thumbnails";
        version = "1.3.2";
        outputHash = "sha256-fBIWJhahXMCZ+D4ojjNE937H23FcT01pY1bTWDMrY+Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert YouTube thumbnails into your notes.'';
            homepage = "https://github.com/Meikul/obsidian-thumbnails";
        };
    }
);
    "obsidian-tidy-footnotes" = (
    let
        baseUrl = "https://github.com/charliecm/obsidian-tidy-footnotes/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tidy-footnotes";
        version = "0.1.2";
        outputHash = "sha256-Ug+VH0meUpk2YCklt5H8iY5AD/V0lSBHtA0q688Nj7o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tidy your footnotes seamlessly.'';
            homepage = "https://github.com/charliecm/obsidian-tidy-footnotes";
        };
    }
);
    "obsidian-tikzjax" = (
    let
        baseUrl = "https://github.com/artisticat1/obsidian-tikzjax/releases/download/0.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tikzjax";
        version = "0.5.2";
        outputHash = "sha256-Q2JkoNNJZSe3uIxEEsPYeXDvRVGNRE3NPl27u/3CN+s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render LaTeX and TikZ diagrams in your notes.'';
            homepage = "https://github.com/artisticat1/obsidian-tikzjax";
        };
    }
);
    "obsidian-timeline" = (
    let
        baseUrl = "https://github.com/George-debug/obsidian-timeline/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-timeline";
        version = "1.1.0";
        outputHash = "sha256-l+xfJspL+l+Q//FKWR8weM/gQ6Sl6gIJzR9Fs5oy8Fw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Build great timelines.'';
            homepage = "https://github.com/George-debug/obsidian-timeline";
        };
    }
);
    "obsidian-timestamp-notes" = (
    let
        baseUrl = "https://github.com/juliang22/ObsidianTimestampNotes/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-timestamp-notes";
        version = "1.0.8";
        outputHash = "sha256-ZK9lz1LAVE6zchiWn0dDmvY2T79RQHjfrFR1qTgpzjM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Side-by-side notetaking with videos. Annotate your notes with timestamps to directly control the video and remember where each note comes from.'';
            homepage = "https://github.com/juliang22/ObsidianTimestampNotes";
        };
    }
);
    "obsidian-timestamper" = (
    let
        baseUrl = "https://github.com/Gru80/obsidian-timestamper/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-timestamper";
        version = "1.3.0";
        outputHash = "sha256-VCKdx2xCUjN6nvO+DReKmtMsGXPKkisC3cXDqwHpjiU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert a customized or predefined time- or date-stamp at the current cursor position.'';
            homepage = "https://github.com/Gru80/obsidian-timestamper";
        };
    }
);
    "obsidian-title-index" = (
    let
        baseUrl = "https://github.com/renmu123/obsidian-markdown-index/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-title-index";
        version = "0.4.0";
        outputHash = "sha256-LzVs6x9PINKF65GxJqMrO18JXkIGuWu9aXQA85drHX4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add serial numbers to your Markdown title.'';
            homepage = "https://github.com/renmu123/obsidian-markdown-index";
        };
    }
);
    "obsidian-title-serial-number-plugin" = (
    let
        baseUrl = "https://github.com/yalvhe2009/obsidian-title-serial-number-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-title-serial-number-plugin";
        version = "1.1.0";
        outputHash = "sha256-92KQ/85VmaWV5Wll8kEA65WbJZOEv293MYeIXpUByOA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add serial numbers to your Markdown title.'';
            homepage = "https://github.com/yalvhe2009/obsidian-title-serial-number-plugin";
        };
    }
);
    "obsidian-to-anki-plugin" = (
    let
        baseUrl = "https://github.com/ObsidianToAnki/Obsidian_to_Anki/releases/download/3.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-to-anki-plugin";
        version = "3.6.0";
        outputHash = "sha256-vjmCizr1oeDISqjswNIPX4/JUOrzOE7VLK6vtn8Pzuk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Anki integration designed for efficient bulk exporting. Previously known as Obsidian_to_Anki.'';
            homepage = "https://github.com/ObsidianToAnki/Obsidian_to_Anki";
        };
    }
);
    "obsidian-to-flomo" = (
    let
        baseUrl = "https://github.com/kaminono/obsidian-to-flomo/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-to-flomo";
        version = "0.1.9";
        outputHash = "sha256-uiUHdXCkzsqX+t2Oc1DXW9LEMJ3jDLqKZAKynDBRRp4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly share content to Flomo.'';
            homepage = "https://github.com/kaminono/obsidian-to-flomo";
        };
    }
);
    "obsidian-to-notion" = (
    let
        baseUrl = "https://github.com/EasyChris/obsidian-to-notion/releases/download/0.7.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-to-notion";
        version = "0.7.6";
        outputHash = "sha256-bu+CpPfxfGGzuyt9F4r+spdw6T0n5nwLozbcD9o+JYw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share files to Notion with the Notion API.'';
            homepage = "https://github.com/EasyChris/obsidian-to-notion";
        };
    }
);
    "obsidian-todoist-link" = (
    let
        baseUrl = "https://github.com/dennisseidel/obsidian-todoist-link/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-todoist-link";
        version = "2.2.0";
        outputHash = "sha256-02yDvCJk5+kzzM1uHvddAwXL3gqFmd6z4uR7YOIMgZ4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Todoist tasks and projects including bidirectional links from Obsidian.'';
            homepage = "https://github.com/dennisseidel/obsidian-todoist-link";
        };
    }
);
    "obsidian-toggl-integration" = (
    let
        baseUrl = "https://github.com/mcndt/obsidian-toggl-integration/releases/download/0.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-toggl-integration";
        version = "0.11.0";
        outputHash = "sha256-D+y0a/36z6A839jHYLG9hNQ0jUpTMEBzbz0a7Ka5UT4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add integration with the Toggl Track API to manage your timers.'';
            homepage = "https://github.com/mcndt/obsidian-toggl-integration";
        };
    }
);
    "obsidian-toggle-case" = (
    let
        baseUrl = "https://github.com/MatthewAlner/obsidian-toggle-case/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-toggle-case";
        version = "1.1.0";
        outputHash = "sha256-rtAPrIrjB2bONbrxwkPYTnGi1FA1/SO0TAHL4Wi8lio=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set a hotkey to toggle between `lowercase` `UPPERCASE` and `Title Case`.'';
            homepage = "https://github.com/MatthewAlner/obsidian-toggle-case";
        };
    }
);
    "obsidian-toggle-list" = (
    let
        baseUrl = "https://github.com/thingnotok/obsidian-toggle-list/releases/download/1.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-toggle-list";
        version = "1.2.7";
        outputHash = "sha256-vsorsVeDvuNV2gLx5QpMjvS4vYYnyL7q40B5YuIsqEM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle the checklist states (paragraph/list/checklist/custom styles).'';
            homepage = "https://github.com/thingnotok/obsidian-toggle-list";
        };
    }
);
    "obsidian-toggle-meta-yaml-plugin" = (
    let
        baseUrl = "https://github.com/hua03/obsidian-toggle-meta-yaml-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-toggle-meta-yaml-plugin";
        version = "1.2.0";
        outputHash = "sha256-03nHYsetIajmCeYdr63R4kihcBv8L1uPmLa7e5Mjmz0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle metadata YAML.'';
            homepage = "https://github.com/hua03/obsidian-toggle-meta-yaml-plugin";
        };
    }
);
    "obsidian-tomorrows-daily-note" = (
    let
        baseUrl = "https://github.com/frankolson/obsidian-tomorrows-daily-note/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tomorrows-daily-note";
        version = "1.4.0";
        outputHash = "sha256-c0nsnbN92vkZEe2fLfK8UZjx0CaWUOI5FQ01bCj3kbo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create tomorrow"s daily note for preemptive planning.'';
            homepage = "https://github.com/frankolson/obsidian-tomorrows-daily-note";
        };
    }
);
    "obsidian-topic-linking" = (
    let
        baseUrl = "https://github.com/liammagee/obsidian-topic-linking/releases/download/0.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-topic-linking";
        version = "0.1.9";
        outputHash = "sha256-oSjWC8Rta8CKMieTgsHzMJVjbYezTJ1Kblq8R/jNOFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert PDF files and web links to Markdown, and create topics from Markdown.'';
            homepage = "https://github.com/liammagee/obsidian-topic-linking";
        };
    }
);
    "obsidian-tracker" = (
    let
        baseUrl = "https://github.com/pyrochlore/obsidian-tracker/releases/download/1.19.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tracker";
        version = "1.19.0";
        outputHash = "sha256-bABAuomWF1/i5CRuHbOktgFZGr89Liw5OLD/NNNa750=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track occurrences and numbers in your notes.'';
            homepage = "https://github.com/pyrochlore/obsidian-tracker";
        };
    }
);
    "obsidian-transcription" = (
    let
        baseUrl = "https://github.com/djmango/obsidian-transcription/releases/download/3.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-transcription";
        version = "3.4.0";
        outputHash = "sha256-KYBEa8lgLBhgFHbWrji3CIO89HbaMPtD3yyI7OH0vDc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create high-quality transcriptions via Whisper from Markdown linked audio files.'';
            homepage = "https://github.com/djmango/obsidian-transcription";
        };
    }
);
    "obsidian-translator" = (
    let
        baseUrl = "https://github.com/luhaifeng666/obsidian-translator/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-translator";
        version = "0.4.1";
        outputHash = "sha256-V4DiUKEeImphQZm4h3/UWjSCNIOMluz/qdGi4Jn05+8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Translate selected text.'';
            homepage = "https://github.com/luhaifeng666/obsidian-translator";
        };
    }
);
    "obsidian-trash-explorer" = (
    let
        baseUrl = "https://github.com/proog/obsidian-trash-explorer/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-trash-explorer";
        version = "1.2.4";
        outputHash = "sha256-sSTRU4B6V89aAujstPc8pg7eLpYHd5OpHffuLYzm9FA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Restore and delete files from the Obsidian .trash folder.'';
            homepage = "https://github.com/proog/obsidian-trash-explorer";
        };
    }
);
    "obsidian-trello" = (
    let
        baseUrl = "https://github.com/nathonius/obsidian-trello/releases/download/2.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-trello";
        version = "2.3.4";
        outputHash = "sha256-uW4uvof6qfZLQByxK967LN0DSn+kEW/UrfTJMIlHqR4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Connect Trello cards to notes.'';
            homepage = "https://github.com/nathonius/obsidian-trello";
        };
    }
);
    "obsidian-tressel" = (
    let
        baseUrl = "https://github.com/tresselteam/obsidian-tressel/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tressel";
        version = "0.2.8";
        outputHash = "sha256-JbikcwudwUbqMpuvjGTl/dmR3v2HrAKfDlEWX7NvqNs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official Tressel plugin to sync/export your tweets and threads.'';
            homepage = "https://github.com/tresselteam/obsidian-tressel";
        };
    }
);
    "obsidian-trim-whitespace" = (
    let
        baseUrl = "https://github.com/zlovatt/obsidian-trim-whitespace/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-trim-whitespace";
        version = "0.4.1";
        outputHash = "sha256-WNAvpXmmGcKX+UMi8FcSSMAebxZdVYYWe+SC+lZq0eM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Trim unnecessary whitespace.'';
            homepage = "https://github.com/zlovatt/obsidian-trim-whitespace";
        };
    }
);
    "obsidian-tts" = (
    let
        baseUrl = "https://github.com/joethei/obsidian-tts/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tts";
        version = "0.6.0";
        outputHash = "sha256-oipXyanviaTIQj1SQ5wCDDZNjT/rgiQ5Ld0vpLCqW68=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hear your notes.'';
            homepage = "https://github.com/joethei/obsidian-tts";
        };
    }
);
    "obsidian-tweaks" = (
    let
        baseUrl = "https://github.com/JeppeKlitgaard/ObsidianTweaks/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tweaks";
        version = "0.4.0";
        outputHash = "sha256-MbJ8MMATwttLSy10qjspmZCDlu6eVU/7wPP+r1Rys94=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add convenient tweaks including improved toggling and ergonomic commands.'';
            homepage = "https://github.com/JeppeKlitgaard/ObsidianTweaks";
        };
    }
);
    "obsidian-tweet-to-markdown" = (
    let
        baseUrl = "https://github.com/kbravh/obsidian-tweet-to-markdown/releases/download/2.12.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-tweet-to-markdown";
        version = "2.12.1";
        outputHash = "sha256-uLk3eVeb31l+5qp3xFMLbY9igxgde+ubZ7wHitDFUwM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save tweets as Markdown files, along with their images, polls, etc.'';
            homepage = "https://github.com/kbravh/obsidian-tweet-to-markdown";
        };
    }
);
    "obsidian-underline" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-underline/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-underline";
        version = "1.3.0";
        outputHash = "sha256-i8wrSqkurC7cQOuxb3LC/mMX1u0Nx9sBcvcpV4mgMbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add underline with shortcut.'';
            homepage = "https://github.com/Benature/obsidian-underline";
        };
    }
);
    "obsidian-upcoming" = (
    let
        baseUrl = "https://github.com/charliecm/obsidian-upcoming/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-upcoming";
        version = "0.4.0";
        outputHash = "sha256-aHNRKZ1lMPs1VLfZ71rSb7E/hQ99/wjC84WaieaPVXg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open upcoming daily notes in their own panes.'';
            homepage = "https://github.com/charliecm/obsidian-upcoming";
        };
    }
);
    "obsidian-user-plugins" = (
    let
        baseUrl = "https://github.com/mnowotnik/obsidian-user-plugins/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-user-plugins";
        version = "1.4.0";
        outputHash = "sha256-6vI9VtSMBEWzYGDmrtRYbYSUbKzndKXuPJolm7hCoO0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use js files or snippets to code your own quick and dirty plugins.'';
            homepage = "https://github.com/mnowotnik/obsidian-user-plugins";
        };
    }
);
    "obsidian-vault-changelog" = (
    let
        baseUrl = "https://github.com/philoserf/obsidian-vault-changelog/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-vault-changelog";
        version = "1.5.0";
        outputHash = "sha256-6wGd6kshhVK5a/1onIkEjmXUt2AUt+SjclWGjzA87X4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Maintain a changelog of recently edited notes.'';
            homepage = "https://github.com/philoserf/obsidian-vault-changelog";
        };
    }
);
    "obsidian-vault-statistics-plugin" = (
    let
        baseUrl = "https://github.com/bkyle/obsidian-vault-statistics-plugin/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-vault-statistics-plugin";
        version = "0.1.3";
        outputHash = "sha256-nCuGcpYoDDUGHG4SPv6OzdDm3D2MRXUSPb0Sp6EayME=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Status bar item with vault statistics such as number of notes, files, attachments, and links.'';
            homepage = "https://github.com/bkyle/obsidian-vault-statistics-plugin";
        };
    }
);
    "obsidian-vega" = (
    let
        baseUrl = "https://github.com/Justin-J-K/obsidian-vega/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-vega";
        version = "0.1.2";
        outputHash = "sha256-Xw9TP6VduwrVw+olJqdqEW2rapoKYCbP3WZ9wrSvy4s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create highly-customizable data visualizations like line charts and scatter plots using Vega or Vega-Lite.'';
            homepage = "https://github.com/Justin-J-K/obsidian-vega";
        };
    }
);
    "obsidian-version-history-diff" = (
    let
        baseUrl = "https://github.com/kometenstaub/obsidian-version-history-diff/releases/download/2.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-version-history-diff";
        version = "2.3.4";
        outputHash = "sha256-XO1ruIEANEDhPHUsBXyQR4o5ZBPyBRriOurU7u+aAE8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Diff the version history of the core Sync and File Recovery plugins and Git for the active file.'';
            homepage = "https://github.com/kometenstaub/obsidian-version-history-diff";
        };
    }
);
    "obsidian-view-mode-by-frontmatter" = (
    let
        baseUrl = "https://github.com/bwydoogh/obsidian-force-view-mode-of-note/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-view-mode-by-frontmatter";
        version = "1.2.2";
        outputHash = "sha256-zDz6jGl0RUrZYXE3M5sDUPg7wUKmDi98B4/AuEg94eM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Force the view mode for a note by using frontmatter: YAML block with "obsidian_ui_mode" as key.'';
            homepage = "https://github.com/bwydoogh/obsidian-force-view-mode-of-note";
        };
    }
);
    "obsidian-vim-multibyte-char-search" = (
    let
        baseUrl = "https://github.com/anselmwang/obsidian-vim-multibyte-char-search/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-vim-multibyte-char-search";
        version = "1.0.4";
        outputHash = "sha256-7VVljWe4JdN0b0TLz71F9e7iRayauiVAnfBChFsaRa4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search multibyte characters by the first character of corresponding ASCII encoding of input method. For example, for Chinese, search by the first character of Pinyin.'';
            homepage = "https://github.com/anselmwang/obsidian-vim-multibyte-char-search";
        };
    }
);
    "obsidian-vimrc-support" = (
    let
        baseUrl = "https://github.com/esm7/obsidian-vimrc-support/releases/download/0.10.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-vimrc-support";
        version = "0.10.2";
        outputHash = "sha256-b9k/o2gy/3O/8g3qK8NVBCvRwB2VO0KENDE/BpBLAL4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto-load a startup file with Vim commands.'';
            homepage = "https://github.com/esm7/obsidian-vimrc-support";
        };
    }
);
    "obsidian-vocabulary-view" = (
    let
        baseUrl = "https://github.com/nnshi-s/obsidian-vocabulary-view-plugin/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-vocabulary-view";
        version = "0.1.4";
        outputHash = "sha256-mi1bNabxT5vFHVqMYuDlpJcXXqLT8TC+DwbnC+aTEns=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write down some words with their explanations and preview them in a vocabulary test style.'';
            homepage = "https://github.com/nnshi-s/obsidian-vocabulary-view-plugin";
        };
    }
);
    "obsidian-wakatime" = (
    let
        baseUrl = "https://github.com/wakatime/obsidian-wakatime/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wakatime";
        version = "1.0.0";
        outputHash = "sha256-Cjecxs1wp5TeBcTSVAONZjiKT0lRT8M/jD/9Hg66eXI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatic time tracking and metrics generated from your usage activity.'';
            homepage = "https://github.com/wakatime/obsidian-wakatime";
        };
    }
);
    "obsidian-wavedrom" = (
    let
        baseUrl = "https://github.com/kingsquirrel152/obsidian-wavedrom/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wavedrom";
        version = "0.1.1";
        outputHash = "sha256-Dd5zBDvrOwG22zpF8LQYAIPiLy49Ig+de5sFP0jRTGs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create wavedrom plots.'';
            homepage = "https://github.com/kingsquirrel152/obsidian-wavedrom";
        };
    }
);
    "obsidian-webhooks" = (
    let
        baseUrl = "https://github.com/trashhalo/obsidian-webhooks/releases/download/0.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-webhooks";
        version = "0.0.6";
        outputHash = "sha256-52PbOM1huyH0v4Chw9OyQ6AUZQjpaNaMFSbMc33rZ9M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Connect your editor to the internet of things through webhooks.'';
            homepage = "https://github.com/trashhalo/obsidian-webhooks";
        };
    }
);
    "obsidian-week-planner" = (
    let
        baseUrl = "https://github.com/rwirdemann/obsidian-week-planner/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-week-planner";
        version = "0.1.4";
        outputHash = "sha256-E2d43+woceQMmk3XMoTvN8x2doeYIIWZJznXEOHV8zQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define commands for creating planning documents and moving tasks between them.'';
            homepage = "https://github.com/rwirdemann/obsidian-week-planner";
        };
    }
);
    "obsidian-weread-plugin" = (
    let
        baseUrl = "https://github.com/zhaohongxuan/obsidian-weread-plugin/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-weread-plugin";
        version = "1.4.0";
        outputHash = "sha256-52KrkHn+Faw3Oo2uxYlbLvP6FNMBhZRVESEFgzGGIQQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Tencent Weread highlights and annotations.'';
            homepage = "https://github.com/zhaohongxuan/obsidian-weread-plugin";
        };
    }
);
    "obsidian-wikipedia" = (
    let
        baseUrl = "https://github.com/jmilldotdev/obsidian-wikipedia/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wikipedia";
        version = "1.0.3";
        outputHash = "sha256-oIB73PSYmVJ3YczbsDRRtgsGsNN6tw8P6WOvkaYOOWI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get the first section of Wikipedia for a note title or search term.'';
            homepage = "https://github.com/jmilldotdev/obsidian-wikipedia";
        };
    }
);
    "obsidian-word-sprint" = (
    let
        baseUrl = "https://github.com/kinabalu/obsidian-word-sprint/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-word-sprint";
        version = "0.3.2";
        outputHash = "sha256-XaHCijX0/quqmFJE7TUmaTD9WVICJqpEMpN3rBogQgo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Word Sprint for your writing projects like NaNoWriMo.'';
            homepage = "https://github.com/kinabalu/obsidian-word-sprint";
        };
    }
);
    "obsidian-wordnet-plugin" = (
    let
        baseUrl = "https://github.com/TfTHacker/Obsidian-WordNet/releases/download/2.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wordnet-plugin";
        version = "2.0.6";
        outputHash = "sha256-bTb6vlHu4e0mLVnnBiamB3TJMSySnP9CCGbNzqk+3HU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A large lexical database of English developed by Princeton University.'';
            homepage = "https://github.com/TfTHacker/Obsidian-WordNet";
        };
    }
);
    "obsidian-wordnik" = (
    let
        baseUrl = "https://github.com/lizard-heart/obsidian-wordnik-definitions/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wordnik";
        version = "1.0.1";
        outputHash = "sha256-S7A0koyhc4wBqfipuCowN/24s44qbyQ/pHcpQ0MOXtA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Grab information from Wordnik for a topic and bring it into your notes.'';
            homepage = "https://github.com/lizard-heart/obsidian-wordnik-definitions";
        };
    }
);
    "obsidian-wordpress" = (
    let
        baseUrl = "https://github.com/devbean/obsidian-wordpress/releases/download/0.18.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wordpress";
        version = "0.18.0";
        outputHash = "sha256-STiCSr8cH93ONIa86MFClRd86COAgVVM/WsuVuS0ySs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish to WordPress.'';
            homepage = "https://github.com/devbean/obsidian-wordpress";
        };
    }
);
    "obsidian-wordy" = (
    let
        baseUrl = "https://github.com/nqthqn/obsidian-wordy/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wordy";
        version = "1.3.1";
        outputHash = "sha256-zwVmd2ASrFWu4JAE5SoJqtPyoIIGdlchlYsB/N3lwJI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Thesaurus, Rhymes and more using the Datamuse API. Find related words easily.'';
            homepage = "https://github.com/nqthqn/obsidian-wordy";
        };
    }
);
    "obsidian-wrap-with-shortcuts" = (
    let
        baseUrl = "https://github.com/manic/obsidian-wrap-with-shortcuts/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-wrap-with-shortcuts";
        version = "1.3.0";
        outputHash = "sha256-VZD5XhM1bJgOTGORNksGOam72uPyC1GabDxivUuHlSY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Wrap selected text in custom tags with shortcuts. E.g.: underline, sub, ruby(フリガナ).'';
            homepage = "https://github.com/manic/obsidian-wrap-with-shortcuts";
        };
    }
);
    "obsidian-youglish-plugin" = (
    let
        baseUrl = "https://github.com/nhaouari/obsidian-youglish-plugin/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-youglish-plugin";
        version = "0.0.3";
        outputHash = "sha256-9jkJof2/+ygk3goDazD9IkcS5wnLTkAHsNGU24PcAd8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use YouTube to improve your pronunciation. YouGlish gives you fast, unbiased answers about how words is spoken by real people and in context.'';
            homepage = "https://github.com/nhaouari/obsidian-youglish-plugin";
        };
    }
);
    "obsidian-zoom" = (
    let
        baseUrl = "https://github.com/vslinko/obsidian-zoom/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-zoom";
        version = "1.1.2";
        outputHash = "sha256-iP+wITIW9OBpTJLdnjrbYOnlZrI9mnIO3LPQwnVZfhw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Zoom into heading and lists.'';
            homepage = "https://github.com/vslinko/obsidian-zoom";
        };
    }
);
    "obsidian-zotero-desktop-connector" = (
    let
        baseUrl = "https://github.com/obsidian-community/obsidian-zotero-integration/releases/download/3.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian-zotero-desktop-connector";
        version = "3.2.1";
        outputHash = "sha256-1SIwi8VvP/mFZk7VuWA7IpxtrfDfY8tXWHL5JZQJj9A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert and import citations, bibliographies, notes, and PDF annotations from Zotero.'';
            homepage = "https://github.com/obsidian-community/obsidian-zotero-integration";
        };
    }
);
    "obsidian42-brat" = (
    let
        baseUrl = "https://github.com/TfTHacker/obsidian42-brat/releases/download/2.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian42-brat";
        version = "2.0.4";
        outputHash = "sha256-Lh8qZFnAu4fslpwCtYMiyxCv9yP9+eG6ANxVp0xk3GY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily install a beta version of a plugin for testing.'';
            homepage = "https://github.com/TfTHacker/obsidian42-brat";
        };
    }
);
    "obsidian42-strange-new-worlds" = (
    let
        baseUrl = "https://github.com/TfTHacker/obsidian42-strange-new-worlds/releases/download/2.3.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian42-strange-new-worlds";
        version = "2.3.7";
        outputHash = "sha256-qWtO9Ec1x5e/mksxKCZAf5dNBflOzUcB/xNVyN+WNPs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reveal networked thought and the strange new worlds created by your vault.'';
            homepage = "https://github.com/TfTHacker/obsidian42-strange-new-worlds";
        };
    }
);
    "obsidian42-text-transporter" = (
    let
        baseUrl = "https://github.com/TfTHacker/obsidian42-text-transporter/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidian42-text-transporter";
        version = "1.0.12";
        outputHash = "sha256-YsiM9C/3arKRo95KIAfVO3vRok9f6ZhDGOS2SkPI7jE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Advanced text tools for working with text in your vault.'';
            homepage = "https://github.com/TfTHacker/obsidian42-text-transporter";
        };
    }
);
    "obsidoom" = (
    let
        baseUrl = "https://github.com/twibiral/ObsiDOOM/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsidoom";
        version = "1.2.0";
        outputHash = "sha256-wk8yWVGsDjROMBPHr3+EWyjQSwqLTiOtGSK5MTukkIs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Play DOOM and many other retro games in Obsidian. You can also play Prince of Persia, Mortal Combat, GTA, Sim City, and Need for Speed.'';
            homepage = "https://github.com/twibiral/ObsiDOOM";
        };
    }
);
    "obsifetch" = (
    let
        baseUrl = "https://github.com/tabibyte/obsifetch/releases/download/1.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsifetch";
        version = "1.3.2";
        outputHash = "sha256-YHoeJNdoTx136KZHqbfV1ZhFLuZ3wYcya3AsFAkq6Kg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A neofetch-style vault information display.'';
            homepage = "https://github.com/tabibyte/obsifetch";
        };
    }
);
    "obsimian-exporter" = (
    let
        baseUrl = "https://github.com/motif-software/obsimian/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsimian-exporter";
        version = "0.2.0";
        outputHash = "sha256-rQg+vYknqWPU4xsqUijdwA3ZepaH3BPvLeS5LOHz9y8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simulation framework for testing Obsidian plugins.'';
            homepage = "https://github.com/motif-software/obsimian";
        };
    }
);
    "obsius-publish" = (
    let
        baseUrl = "https://github.com/jonstodle/obsius-obsidian-plugin/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsius-publish";
        version = "1.4.0";
        outputHash = "sha256-jxOObJeqPIKg4qZ6cIWQy7LW7Uy5qiQUF/3SrcacYag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make single notes instantly available on the web.'';
            homepage = "https://github.com/jonstodle/obsius-obsidian-plugin";
        };
    }
);
    "obsnippets" = (
    let
        baseUrl = "https://github.com/Chrstn67/Snippetsaurus/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "obsnippets";
        version = "1.0.11";
        outputHash = "sha256-Vjbw8ijla3rymmA82wGkm8JUD1LeQOwO7J5m7yoggxM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and managing text and code snippets.'';
            homepage = "https://github.com/Chrstn67/Snippetsaurus";
        };
    }
);
    "occura-word-highlighter" = (
    let
        baseUrl = "https://github.com/Krusty84/obsidian-occura-plugin/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "occura-word-highlighter";
        version = "1.5.0";
        outputHash = "sha256-7ta9jwP1Cb9BwpeZ7FHhvOUecsY0NhNto3/ynMXK7ys=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find and highlight all occurrences of selected text in notes, similar to Notepad++ or IDEs.'';
            homepage = "https://github.com/Krusty84/obsidian-occura-plugin";
        };
    }
);
    "ocr-extractor" = (
    let
        baseUrl = "https://github.com/jritzi/ocr-extractor/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ocr-extractor";
        version = "2.1.0";
        outputHash = "sha256-rZ7xPSKFQHtmyZzEiUSDaxSX8OkCpYcVJ422eYkaAio=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract text from documents, images, etc. and store it as Markdown in your notes.'';
            homepage = "https://github.com/jritzi/ocr-extractor";
        };
    }
);
    "oin-gotoheading" = (
    let
        baseUrl = "https://github.com/oin/obsidian-gotoheading/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "oin-gotoheading";
        version = "0.2.2";
        outputHash = "sha256-mCE0BcZ+m4nMIdmFdZFuY/EtMVcvAL7D7QQhYpCr8Mk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly navigate between headings.'';
            homepage = "https://github.com/oin/obsidian-gotoheading";
        };
    }
);
    "ollama" = (
    let
        baseUrl = "https://github.com/hinterdupfinger/obsidian-ollama/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ollama";
        version = "0.0.1";
        outputHash = "sha256-9Yy4SL3E89MqhWIZhdPMSFRAfbrW3jchsvCa6nU5uwM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable the usage of Ollama within your notes.'';
            homepage = "https://github.com/hinterdupfinger/obsidian-ollama";
        };
    }
);
    "ollama-chat" = (
    let
        baseUrl = "https://github.com/brumik/obsidian-ollama-chat/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ollama-chat";
        version = "0.1.4";
        outputHash = "sha256-X2xt93Y8i1tQQMfFA1l1ycikzWpEX0n8rEmrHzd7oMA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Chat with your notes using Ollama and LlamaIndex.'';
            homepage = "https://github.com/brumik/obsidian-ollama-chat";
        };
    }
);
    "omglol-statuslog-publish" = (
    let
        baseUrl = "https://github.com/MayMeow/obsidian-omglol-statuslog/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "omglol-statuslog-publish";
        version = "1.0.3";
        outputHash = "sha256-vkmEwKIPbSVgTqN2LjD9TsQFX3iJE6aaMWfAROmJVFA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post selected text to OMG.lol statuslog.'';
            homepage = "https://github.com/MayMeow/obsidian-omglol-statuslog";
        };
    }
);
    "omnisearch" = (
    let
        baseUrl = "https://github.com/scambier/obsidian-omnisearch/releases/download/1.28.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "omnisearch";
        version = "1.28.2";
        outputHash = "sha256-9r/8tcK8qc7fDqY1e/MymMFwNejzjguOMkrug3MuAzo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intelligent search for your notes, PDFs, and OCR for images.'';
            homepage = "https://github.com/scambier/obsidian-omnisearch";
        };
    }
);
    "on-this-day" = (
    let
        baseUrl = "https://github.com/jose-elias-alvarez/obsidian-on-this-day/releases/download/2.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "on-this-day";
        version = "2.0.5";
        outputHash = "sha256-oFc133gkVjySVMLdZ5KyZAbYjfiZlvY4TF0AlWcGeOI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show your daily notes from this day in a simple panel view.'';
            homepage = "https://github.com/jose-elias-alvarez/obsidian-on-this-day";
        };
    }
);
    "on-this-day-i" = (
    let
        baseUrl = "https://github.com/benstuart0/on-this-day-i-obsidian/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "on-this-day-i";
        version = "1.2.0";
        outputHash = "sha256-mpNv01wNID4Ya14WACRiBzEq4vZHlCyoen01oshO7Mg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Summarize a calendar date in years past with Daily Journals'';
            homepage = "https://github.com/benstuart0/on-this-day-i-obsidian";
        };
    }
);
    "one-step-wiki-link" = (
    let
        baseUrl = "https://github.com/busyoGG/OneStepWikiLink/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "one-step-wiki-link";
        version = "1.0.4";
        outputHash = "sha256-Gcm1jhpJB9YvDMgzNEJ8IIwIS3dS5Frb+zmO5b3ZCL4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''一步添加 wiki 链接'';
            homepage = "https://github.com/busyoGG/OneStepWikiLink";
        };
    }
);
    "onlyworlds-builder" = (
    let
        baseUrl = "https://github.com/OnlyWorlds/obsidian-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "onlyworlds-builder";
        version = "1.1.0";
        outputHash = "sha256-EKDMWkoqQh1soNTgFeWodku8qzWCWY+po7imoJIkp0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''World building toolset with OnlyWorlds integration.'';
            homepage = "https://github.com/OnlyWorlds/obsidian-plugin";
        };
    }
);
    "ons" = (
    let
        baseUrl = "https://github.com/skye-z/ons/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ons";
        version = "1.0.1";
        outputHash = "sha256-urzkBIHCScG1Qu2M9m5UNSxOacNd7FQyvw7NcSApfy0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep Vault synchronized with the NAS on your home intranet.'';
            homepage = "https://github.com/skye-z/ons";
        };
    }
);
    "onto-tracker" = (
    let
        baseUrl = "https://github.com/jdchart/onto-tracker/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "onto-tracker";
        version = "1.0.0";
        outputHash = "sha256-CBZwvpl1hJNHRSjdZ5LiKBfR2sBF5rpei1yy+dB9rmc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage projects according to an ontology.'';
            homepage = "https://github.com/jdchart/onto-tracker";
        };
    }
);
    "onyx-boox-extractor" = (
    let
        baseUrl = "https://github.com/akosbalasko/Onyx-Boox-Annotation-Highlight-Extractor/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "onyx-boox-extractor";
        version = "1.1.0";
        outputHash = "sha256-POdktSCnP0+XrgkVFzvXl7I8NA+mCef7GexZZXTVEOw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract annotations and highlights files exported from Onyx Boox tablets, and convert them to reference, literature and permanent notes fitting to the Zettelkasten method.'';
            homepage = "https://github.com/akosbalasko/Onyx-Boox-Annotation-Highlight-Extractor";
        };
    }
);
    "open-as-md" = (
    let
        baseUrl = "https://github.com/kursad-k/obsidian-openasmd/releases/download/0.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-as-md";
        version = "0.0.8";
        outputHash = "sha256-ou1t+W7isGvhMpLYspS2YXbu+JAqdDgVUk0c4j9mUaQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit non-md file types as Markdown files.'';
            homepage = "https://github.com/kursad-k/obsidian-openasmd";
        };
    }
);
    "open-editors" = (
    let
        baseUrl = "https://github.com/4Source/open-editors-obsidian-plugin/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-editors";
        version = "0.2.1";
        outputHash = "sha256-Lb67tJ18Oe15xpv/ztMtn+I2XBGsbHQkbR9KrmwkU5U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a view which shows the opened windows, groups, tabs and editors inside them. Makes managing the open editors easier.'';
            homepage = "https://github.com/4Source/open-editors-obsidian-plugin";
        };
    }
);
    "open-files-with-commands" = (
    let
        baseUrl = "https://github.com/LostPaul/ob-open-files-with-commands/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-files-with-commands";
        version = "1.2.3";
        outputHash = "sha256-LchNcL8JfJKQx7YZx4cUnQkFbgYX8aOViEOBf6KIG9U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create commands that only open one file at the time and that can be used with the commander plugin.'';
            homepage = "https://github.com/LostPaul/ob-open-files-with-commands";
        };
    }
);
    "open-gate" = (
    let
        baseUrl = "https://github.com/nguyenvanduocit/obsidian-open-gate/releases/download/1.11.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-gate";
        version = "1.11.14";
        outputHash = "sha256-UPJ5ul3hosQ3coLWI9CaVbx9fUw91u3X9N0Pwtdb/5o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed any website to Obsidian, from now all, you have anything you need in one place.'';
            homepage = "https://github.com/nguyenvanduocit/obsidian-open-gate";
        };
    }
);
    "open-in-cursor" = (
    let
        baseUrl = "https://github.com/awaken233/open-in-cursor/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-in-cursor";
        version = "1.0.11";
        outputHash = "sha256-dHLASY+YmEB9KMhAe5yQuScHqfjLT8t5u1OQIxqYh6Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a hotkey to open the current file in Cursor IDE and jump to the cursor position'';
            homepage = "https://github.com/awaken233/open-in-cursor";
        };
    }
);
    "open-in-github" = (
    let
        baseUrl = "https://github.com/Hacker-C/obsidian-open-in-github-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-in-github";
        version = "1.0.3";
        outputHash = "sha256-w9r5iToWuvNNzqL4p5o8I9YsC13D0hV0HUD2+ciEimU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open the current project or file in github.com.'';
            homepage = "https://github.com/Hacker-C/obsidian-open-in-github-plugin";
        };
    }
);
    "open-in-new-tab" = (
    let
        baseUrl = "https://github.com/patleeman/obsidian-open-in-new-tab/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-in-new-tab";
        version = "1.0.9";
        outputHash = "sha256-Ibi2oJf4+LgBlKV7GYP78fE9Yelcf/X2MjQP4n/KE/E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open files in new tabs.'';
            homepage = "https://github.com/patleeman/obsidian-open-in-new-tab";
        };
    }
);
    "open-in-terminal" = (
    let
        baseUrl = "https://github.com/Feng6611/Obsidian-open-in-Teminal/releases/download/0.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-in-terminal";
        version = "0.9.1";
        outputHash = "sha256-Fnd/9KHBoOx971L37/jg0Qqf2BQh8N7Dx+G9BVXsx3k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open your vault in a new terminal window or launch Claude Code, Codex CLI, or Gemini CLI directly from a terminal.'';
            homepage = "https://github.com/Feng6611/Obsidian-open-in-Teminal";
        };
    }
);
    "open-interpreter" = (
    let
        baseUrl = "https://github.com/MikeBirdTech/obsidian-open-interpreter/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-interpreter";
        version = "1.3.1";
        outputHash = "sha256-oKUpOAZuA8QQ9oMn89NkQfsOmBouRtPHInsM3tPx3iI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An AI assistant that you can control with natural language. The power of Open Interpreter in your vault'';
            homepage = "https://github.com/MikeBirdTech/obsidian-open-interpreter";
        };
    }
);
    "open-note-to-window-title" = (
    let
        baseUrl = "https://github.com/jplattel/open-note-to-window-title/releases/download/0.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-note-to-window-title";
        version = "0.3.5";
        outputHash = "sha256-Olg4w97cltA+xXM+twfLp0MA9ils5UaiWuCMH38rACo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show the current open note in the window title.'';
            homepage = "https://github.com/jplattel/open-note-to-window-title";
        };
    }
);
    "open-or-create-file-command" = (
    let
        baseUrl = "https://github.com/iparips/open-or-create-file-obsidian-plugin/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-or-create-file-command";
        version = "0.3.2";
        outputHash = "sha256-b+MTovWXY/szbTB4MJ8O4aSxXgvNXjdzKGnVQ+W8MKM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create custom commands that open or create files using configurable patterns and templates.'';
            homepage = "https://github.com/iparips/open-or-create-file-obsidian-plugin";
        };
    }
);
    "open-plugin-settings" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-open-settings/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-plugin-settings";
        version = "1.4.0";
        outputHash = "sha256-C78+9WUL+xf1V81Wni9Sccq4yKHKYjsR1E1N7m0x4r0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a command to open a specified plugin settings.'';
            homepage = "https://github.com/Mara-Li/obsidian-open-settings";
        };
    }
);
    "open-related-url" = (
    let
        baseUrl = "https://github.com/dpickett/open-related-url/releases/download/0.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-related-url";
        version = "0.0.12";
        outputHash = "sha256-r5l6L/nSydqpT2FFm6iQXKoJKzW+7goookAPm/77y7k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open URLs found in a note"s YAML frontmatter.'';
            homepage = "https://github.com/dpickett/open-related-url";
        };
    }
);
    "open-tab-settings" = (
    let
        baseUrl = "https://github.com/jesse-r-s-hines/obsidian-open-tab-settings/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-tab-settings";
        version = "2.0.1";
        outputHash = "sha256-Sg4Sih1DrIwKVmjp1U6tQZnJkslcaEy5xBbttk+Wd8s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds options to customize how tabs are opened, including open in new tab by default, preventing duplicate tabs, and more.'';
            homepage = "https://github.com/jesse-r-s-hines/obsidian-open-tab-settings";
        };
    }
);
    "open-vscode" = (
    let
        baseUrl = "https://github.com/NomarCub/obsidian-open-vscode/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-vscode";
        version = "1.4.2";
        outputHash = "sha256-xgoYYMOcjCMwDISAgKGqyE8PVV0TlBmraZh+DIbABvM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Ribbon button and command to open vault as a Visual Studio Code (VSCode) workspace.'';
            homepage = "https://github.com/NomarCub/obsidian-open-vscode";
        };
    }
);
    "open-weather" = (
    let
        baseUrl = "https://github.com/willasm/obsidian-open-weather/releases/download/1.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-weather";
        version = "1.8.1";
        outputHash = "sha256-DTaEUmbjKxJMsgUXR6VCdBJw1FG4scCRaeBKQyUgWPo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Return the current weather from OpenWeather in a configurable string format.'';
            homepage = "https://github.com/willasm/obsidian-open-weather";
        };
    }
);
    "open-with" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-open-with/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-with";
        version = "0.3.2";
        outputHash = "sha256-vZbrKA98I1errUdE6Skvhp7q1Kcw4JnBoKl/sdkxZMw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add multiple other programs to open notes with.'';
            homepage = "https://github.com/phibr0/obsidian-open-with";
        };
    }
);
    "open-with-nldates" = (
    let
        baseUrl = "https://github.com/charliecm/obsidian-open-with-nldates/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "open-with-nldates";
        version = "1.0.0";
        outputHash = "sha256-5kWRcbu+TylQFDVRYGEe+6rDAEZfm/UzIy9XUH5vdww=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open a daily note using natural language. Requires "Natural Language Dates" plugin to work.'';
            homepage = "https://github.com/charliecm/obsidian-open-with-nldates";
        };
    }
);
    "openapi-renderer" = (
    let
        baseUrl = "https://github.com/Ssentiago/obsidian-openapi-renderer/releases/download/4.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "openapi-renderer";
        version = "4.5.1";
        outputHash = "sha256-rp7ROsxBzjldkTFOX83uj5I2TzDGkbCVztp2R2rYA8g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate OpenAPI specification management with features for version control, visualization, editing, and easy navigation of API specs.'';
            homepage = "https://github.com/Ssentiago/obsidian-openapi-renderer";
        };
    }
);
    "openaugi" = (
    let
        baseUrl = "https://github.com/bitsofchris/openaugi-obsidian-plugin/releases/download/0.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "openaugi";
        version = "0.5.2";
        outputHash = "sha256-ZJVQUeU7nBobml/z8r+9sn+0Ifj/db3VPiPHJk8muFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Parse your voice notes into atomic notes, tasks, and summaries. OpenAugi is the voice to self-organizing second brain. While taking a voice note say "auggie" to help the agent. Augmented Intelligence is AI for thinkers.'';
            homepage = "https://github.com/bitsofchris/openaugi-obsidian-plugin";
        };
    }
);
    "opener" = (
    let
        baseUrl = "https://github.com/lukemt/obsidian-opener/releases/download/2.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "opener";
        version = "2.2.9";
        outputHash = "sha256-qqRnDf5pxhor6UZlzm0deTnSzRV7cyTAWefTDTOY/RE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open links in new tabs by default. If the note is already open in another tab, it switches to it. Can also open PDFs and other file formats in System Apps when cmd/ctrl is hold.'';
            homepage = "https://github.com/lukemt/obsidian-opener";
        };
    }
);
    "openwords" = (
    let
        baseUrl = "https://github.com/insile/OpenWords/releases/download/1.0.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "openwords";
        version = "1.0.17";
        outputHash = "sha256-LhTIwhza2+0Jl78ael6GjyssfW1IRgWd9jpW4R0dah4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''用于英语学习中背单词与单词管理的插件'';
            homepage = "https://github.com/insile/OpenWords";
        };
    }
);
    "optimize-canvas-connections" = (
    let
        baseUrl = "https://github.com/felixchenier/obsidian-optimize-canvas-connections/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "optimize-canvas-connections";
        version = "1.0.0";
        outputHash = "sha256-QBzWVxoNTH3X0VxKlMFE2CLOA0RVTpq5kcBKIqjgpyc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Declutter a canvas by reconnecting notes using their nearest edges.'';
            homepage = "https://github.com/felixchenier/obsidian-optimize-canvas-connections";
        };
    }
);
    "order-list" = (
    let
        baseUrl = "https://github.com/lizard-heart/obsidian-order-list-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "order-list";
        version = "1.0.2";
        outputHash = "sha256-nQA+G57YyeMRfHd7Njw1oIDGDjmNlG++8espH++Qc34=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add "Order selected list" command to take the selected list and order it by the number at the end.'';
            homepage = "https://github.com/lizard-heart/obsidian-order-list-plugin";
        };
    }
);
    "organized-daily-notes" = (
    let
        baseUrl = "https://github.com/duchangkim/organized-daily-notes/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "organized-daily-notes";
        version = "1.0.5";
        outputHash = "sha256-h67fLt6VjATs8P3jSqCf4KmDN7j+ipEEwVlu/9OGIY0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically organizes your daily notes into customizable folder structures (Year/Month/Week) for systematic note management'';
            homepage = "https://github.com/duchangkim/organized-daily-notes";
        };
    }
);
    "orgmode-cm6" = (
    let
        baseUrl = "https://github.com/BBazard/obsidian-orgmode-cm6/releases/download/2.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "orgmode-cm6";
        version = "2.11.0";
        outputHash = "sha256-weefg2CXwKLJFHenjq7hhrORw4O7vHRsGctviL4J1is=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit Orgmode files.'';
            homepage = "https://github.com/BBazard/obsidian-orgmode-cm6";
        };
    }
);
    "orion-publish" = (
    let
        baseUrl = "https://github.com/seanrcollings/orion-publish-plugin/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "orion-publish";
        version = "0.2.2";
        outputHash = "sha256-6gRidZmawkA7+L+U2C/paiqJ+am1seixfQHismbCCMg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly and easily publish your notes to the web with Orion Publish.'';
            homepage = "https://github.com/seanrcollings/orion-publish-plugin";
        };
    }
);
    "outline-converter" = (
    let
        baseUrl = "https://github.com/masaki39/outline-converter/releases/download/1.5.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "outline-converter";
        version = "1.5.4";
        outputHash = "sha256-ZF0EQ/raUlmMUcmQ6lZg/q5D10i5B4BPaQYklXuJnWM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert outline to continuous text.'';
            homepage = "https://github.com/masaki39/outline-converter";
        };
    }
);
    "outline-plus" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-outline-plus/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "outline-plus";
        version = "0.2.0";
        outputHash = "sha256-Zq6F2LzqJ8M3EGIaOveGf5471+ZZdMxyvXuBQtIL66s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Markdown inside the outline view.'';
            homepage = "https://github.com/RyotaUshio/obsidian-outline-plus";
        };
    }
);
    "outline-task-list" = (
    let
        baseUrl = "https://github.com/alexandrerbb/obsidian-outline-tasklist-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "outline-task-list";
        version = "1.1.0";
        outputHash = "sha256-bZ5Oh1AbBE8eYuv4SB4sSdzjpn0NJde8ihLyDoe6/6Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a note"s outline to a task list.'';
            homepage = "https://github.com/alexandrerbb/obsidian-outline-tasklist-plugin";
        };
    }
);
    "outlook-meeting-notes" = (
    let
        baseUrl = "https://github.com/davidingerslev/outlook-meeting-notes/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "outlook-meeting-notes";
        version = "0.2.1";
        outputHash = "sha256-1yw3IXytVCIVfKWcmvcFHfAa90bM3F56MAlCgzb+TK0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creates meeting notes for Outlook appointments and meetings.'';
            homepage = "https://github.com/davidingerslev/outlook-meeting-notes";
        };
    }
);
    "oz-calendar" = (
    let
        baseUrl = "https://github.com/ozntel/oz-calendar/releases/download/0.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "oz-calendar";
        version = "0.3.4";
        outputHash = "sha256-SP/96c0m6bPdy+dSXlCfyZtpZUzWOSZ9C3KHuhyn7Cw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View your notes in Calendar using any YAML key with a date.'';
            homepage = "https://github.com/ozntel/oz-calendar";
        };
    }
);
    "oz-clear-unused-images" = (
    let
        baseUrl = "https://github.com/ozntel/oz-clear-unused-images-obsidian/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "oz-clear-unused-images";
        version = "1.1.1";
        outputHash = "sha256-e+ndvpRJdbL8ktgFq5InlCssDbKmSt90J8Edf0c1uIE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Clear the images that you are not using anymore in your Markdown notes to save space.'';
            homepage = "https://github.com/ozntel/oz-clear-unused-images-obsidian";
        };
    }
);
    "oz-image-plugin" = (
    let
        baseUrl = "https://github.com/ozntel/oz-image-in-editor-obsidian/releases/download/2.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "oz-image-plugin";
        version = "2.2.6";
        outputHash = "sha256-tu2/ENG1i7HGGBEAKAD4vc1h4QMogqIdt6yym5zbZE0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View Images, Transclusions, iFrames and PDF Files within the Editor without a necessity to switch to Preview.'';
            homepage = "https://github.com/ozntel/oz-image-in-editor-obsidian";
        };
    }
);
    "ozanshare-publish" = (
    let
        baseUrl = "https://github.com/ozntel/ozanshare-publish-plugin/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ozanshare-publish";
        version = "0.3.1";
        outputHash = "sha256-2cRcr3HLLb+RIPGPOFzDK6RUvR7U/B/vpPKINP9bGWA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your Markdown notes with a single click from your vault. (Closed source)'';
            homepage = "https://github.com/ozntel/ozanshare-publish-plugin";
        };
    }
);
    "packup4ai" = (
    let
        baseUrl = "https://github.com/shuxueshuxue/PackUp4AI/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "packup4ai";
        version = "1.2.4";
        outputHash = "sha256-7ngiosJLArQz4oQFN5DpPWyIFJDPTuYEQQ+LtJAp1W8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collect related notes based on links/backlinks to provide focused context for external AI chatbots. Explore note relationships visually and export the bundle.'';
            homepage = "https://github.com/shuxueshuxue/PackUp4AI";
        };
    }
);
    "page-gallery" = (
    let
        baseUrl = "https://github.com/tokenshift/obsidian-page-gallery/releases/download/0.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "page-gallery";
        version = "0.8.4";
        outputHash = "sha256-+KUs9SmWGfHxYrot6j/A8WlRKyekx6nR2wsuPGs6HOI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create an embeddable gallery based on selected page contents.'';
            homepage = "https://github.com/tokenshift/obsidian-page-gallery";
        };
    }
);
    "page-heading-from-links" = (
    let
        baseUrl = "https://github.com/beet/page-headings-obsidian-plugin/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "page-heading-from-links";
        version = "0.2.0";
        outputHash = "sha256-riK/nISKm4C1j3pyZJuKaehZ8BJMkyNQ/y/8GgILxAY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Populate page headings from the file name.'';
            homepage = "https://github.com/beet/page-headings-obsidian-plugin";
        };
    }
);
    "page-properties" = (
    let
        baseUrl = "https://github.com/necauqua/obsidian-page-properties/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "page-properties";
        version = "1.0.3";
        outputHash = "sha256-H5+YkhiAUfBFEuJWPJaoBUpnY5A0CQnXRfbiYgxXzTg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add page properties similar to Logseq.'';
            homepage = "https://github.com/necauqua/obsidian-page-properties";
        };
    }
);
    "page-scroll" = (
    let
        baseUrl = "https://github.com/chenshutian9610/obsidian-pagescroll-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "page-scroll";
        version = "1.0.1";
        outputHash = "sha256-53khDmoPR+HCDvRx/R6DLHPtzXnHQvJDCjjeY46CnKM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Page Up|Down|Top|Bottom'';
            homepage = "https://github.com/chenshutian9610/obsidian-pagescroll-plugin";
        };
    }
);
    "painter" = (
    let
        baseUrl = "https://github.com/KraXen72/obsidian-painter/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "painter";
        version = "1.1.0";
        outputHash = "sha256-miGM/bqcc2manTFcH8FUN4YQDdOP9A5vZQwVMS2qOBI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paint text different colors.'';
            homepage = "https://github.com/KraXen72/obsidian-painter";
        };
    }
);
    "palta-note" = (
    let
        baseUrl = "https://github.com/mrniket/palta-obsidian-plugin/releases/download/1.7.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "palta-note";
        version = "1.7.3";
        outputHash = "sha256-rntX+hzki0A4rU79nollDW9ir+LqofM/Zxs/YzsIL1A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Bhatkhande notation for Tabla.'';
            homepage = "https://github.com/mrniket/palta-obsidian-plugin";
        };
    }
);
    "pandoc-extended-markdown" = (
    let
        baseUrl = "https://github.com/ErrorTzy/obsidian-pandoc-extended-markdown/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pandoc-extended-markdown";
        version = "1.6.0";
        outputHash = "sha256-55bAgjG/lypCLfFUee8tFuqMOPVsjTm5Zn9lXh5m1N0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Pandoc extended markdown syntax: fancy lists, definition lists, example lists with cross-references, superscripts, and subscripts.'';
            homepage = "https://github.com/ErrorTzy/obsidian-pandoc-extended-markdown";
        };
    }
);
    "pane-relief" = (
    let
        baseUrl = "https://github.com/pjeby/pane-relief/releases/download/0.5.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pane-relief";
        version = "0.5.9";
        outputHash = "sha256-9k6jzNkv400fKqh9s+LGonulITNSzNlQ/arcEtJjifU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Per-pane history, hotkeys for pane movement + navigation, and more.'';
            homepage = "https://github.com/pjeby/pane-relief";
        };
    }
);
    "paper-importer" = (
    let
        baseUrl = "https://github.com/chenzhekl/obsidian_paper_importer/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paper-importer";
        version = "1.5.0";
        outputHash = "sha256-7Y7x1vh+km8AiztdBhBG2JOYPXgN+Fpnc21nJmkfidk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import papers from arXiv with one click.'';
            homepage = "https://github.com/chenzhekl/obsidian_paper_importer";
        };
    }
);
    "paperless" = (
    let
        baseUrl = "https://github.com/Talal-A/obsidian-paperless/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paperless";
        version = "0.2.0";
        outputHash = "sha256-dJZk9zwzCloGo0okRDVic7bf9mrX6WL57nNJVGmXYN0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Link your paperless-ngx documents within your vault.'';
            homepage = "https://github.com/Talal-A/obsidian-paperless";
        };
    }
);
    "papers" = (
    let
        baseUrl = "https://github.com/willjhliang/obsidian-papers/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "papers";
        version = "1.0.1";
        outputHash = "sha256-40r++CI/Ig7tzJswPg/Chm6d3rlpXSSQ8Hm8+4nowHY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Retrieve and import research papers.'';
            homepage = "https://github.com/willjhliang/obsidian-papers";
        };
    }
);
    "papyrus" = (
    let
        baseUrl = "https://github.com/Papyrus-doc-ai/papyrus-obsidian/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "papyrus";
        version = "1.1.0";
        outputHash = "sha256-a5TO5/GTuEebnzb8h1ZdUHH/noOJAazYe4wKPjXQ/f4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''The most amazing documentation assistant'';
            homepage = "https://github.com/Papyrus-doc-ai/papyrus-obsidian";
        };
    }
);
    "para-shortcuts" = (
    let
        baseUrl = "https://github.com/gOATiful/para-shortcuts/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "para-shortcuts";
        version = "1.0.7";
        outputHash = "sha256-wm4vCsSXyH2IgCf4pSBSpReHPr4ExzYeKq/4TrfUhdo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Useful commands to set up and manage your knowledge using the PARA method.'';
            homepage = "https://github.com/gOATiful/para-shortcuts";
        };
    }
);
    "para-workflower" = (
    let
        baseUrl = "https://github.com/trucke/para-workflower/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "para-workflower";
        version = "0.1.5";
        outputHash = "sha256-+WY1RtMaRSyKIQBFfZovjrvGpQBxy3+opPjy8TI1u/k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helpful commands for starting and working in your vault with the PARA method.'';
            homepage = "https://github.com/trucke/para-workflower";
        };
    }
);
    "password-audit" = (
    let
        baseUrl = "https://github.com/0xPrashanthSec/password-audit/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "password-audit";
        version = "2.0.2";
        outputHash = "sha256-sgYX/31Afr88wLHWVNFbd3ilaS5c+RLz96/pylu2oA8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A plugin to audit passwords for strength and breaches, and generate secure passwords.'';
            homepage = "https://github.com/0xPrashanthSec/password-audit";
        };
    }
);
    "password-protect" = (
    let
        baseUrl = "https://github.com/Aspharmyx/obsidian-password-protect/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "password-protect";
        version = "1.1.6";
        outputHash = "sha256-fbl2cJ0yYsILlrS+Iq2oaYQHgyPGNIMfHNt42NJmfg8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Password protect your notes.'';
            homepage = "https://github.com/Aspharmyx/obsidian-password-protect";
        };
    }
);
    "password-protection" = (
    let
        baseUrl = "https://github.com/qing3962/password-protection/releases/download/1.1.33";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "password-protection";
        version = "1.1.33";
        outputHash = "sha256-qX/RoKNgpY+VC3Q3IY09EJv91w86QC/vwMVSF+sFD+E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Lock and protect your private notes and diary with a password, no encrypt, no decrypt.'';
            homepage = "https://github.com/qing3962/password-protection";
        };
    }
);
    "paste-as-embed" = (
    let
        baseUrl = "https://github.com/i-m-mll/obsidian-paste-as-embed/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-as-embed";
        version = "0.1.4";
        outputHash = "sha256-WB6jenJB3NVHn4rgaTNrtKVn4+2+nG1vFjIlJhRuQ/c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Redirect pasted text into a separate note, and embed it.'';
            homepage = "https://github.com/i-m-mll/obsidian-paste-as-embed";
        };
    }
);
    "paste-as-file-link" = (
    let
        baseUrl = "https://github.com/mbedded/obsidian-paste-file-link/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-as-file-link";
        version = "1.3.0";
        outputHash = "sha256-xbENx40xwGc55cDIb6KAFErMGhMPkdDP2jL9Ug2o9Pc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste clipboard content as file links into existing notes, when a file with this name is existing.'';
            homepage = "https://github.com/mbedded/obsidian-paste-file-link";
        };
    }
);
    "paste-from-history" = (
    let
        baseUrl = "https://github.com/Karakaz/obsidian-paste-from-history/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-from-history";
        version = "1.3.0";
        outputHash = "sha256-TZg+7orx2+WfgegRqUa3qlhiaU/ZNSJATGTnzIJNRu4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste from the editor"s recent clipboard history.'';
            homepage = "https://github.com/Karakaz/obsidian-paste-from-history";
        };
    }
);
    "paste-image-into-property" = (
    let
        baseUrl = "https://github.com/Nitero/obsidian-paste-image-into-property/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-image-into-property";
        version = "1.0.6";
        outputHash = "sha256-OF7a8TQ8NLuwt0IDa3FRWIKGEGa6FxbITQVlRWqYXXE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows pasting images from the clipboard into frontmatter properties in live preview.'';
            homepage = "https://github.com/Nitero/obsidian-paste-image-into-property";
        };
    }
);
    "paste-image-rename-convert" = (
    let
        baseUrl = "https://github.com/Yiaos/obsidian-paste-image-rename-convert/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-image-rename-convert";
        version = "1.0.0";
        outputHash = "sha256-9rk3bEp/APveOYQVQ7GVXe2giFhpWrzc4QYetW/eug4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Renames pasted images and covert the formats'';
            homepage = "https://github.com/Yiaos/obsidian-paste-image-rename-convert";
        };
    }
);
    "paste-link" = (
    let
        baseUrl = "https://github.com/jose-elias-alvarez/obsidian-paste-link/releases/download/3.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-link";
        version = "3.3.1";
        outputHash = "sha256-5+cPVvO7qVgkfZXML88njGdG3vgMDEGI3fjo3iZANGQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intelligently paste Markdown links.'';
            homepage = "https://github.com/jose-elias-alvarez/obsidian-paste-link";
        };
    }
);
    "paste-quote" = (
    let
        baseUrl = "https://github.com/brokensandals/obsidian-paste-quote-plugin/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-quote";
        version = "1.3.1";
        outputHash = "sha256-k05rLsYZ+knTSV2b5F+pS9xceXCO3IZGUvMQ3Vwg7ls=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps with formatting and citing quotes.'';
            homepage = "https://github.com/brokensandals/obsidian-paste-quote-plugin";
        };
    }
);
    "paste-reformatter" = (
    let
        baseUrl = "https://github.com/keathmilligan/obsidian-paste-reformatter/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-reformatter";
        version = "1.4.1";
        outputHash = "sha256-45Ty6c8F61El7fhkqQrTvg7ICE8ZOfogcE+S4sHAOdE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reformats pasted text for precise control.'';
            homepage = "https://github.com/keathmilligan/obsidian-paste-reformatter";
        };
    }
);
    "paste-transform" = (
    let
        baseUrl = "https://github.com/rekby/obsidian-paste-transform/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "paste-transform";
        version = "1.0.1";
        outputHash = "sha256-2EsGMLj0unBC9oqrpAKyxwhJs51Gt9na4uWjaUFgL0s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Modify text from the clipboard by regexp rules'';
            homepage = "https://github.com/rekby/obsidian-paste-transform";
        };
    }
);
    "pasterly" = (
    let
        baseUrl = "https://github.com/easternkite/Pasterly/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pasterly";
        version = "1.2.0";
        outputHash = "sha256-HrbfYBXIQ6DKiTknvqUCTvZ9JtdNERdComDSe1XLFww=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically upload clipboard images to Firebase Storage and insert them as markdown links!'';
            homepage = "https://github.com/easternkite/Pasterly";
        };
    }
);
    "pathlinker" = (
    let
        baseUrl = "https://github.com/Kay607/obsidian-pathlinker/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pathlinker";
        version = "1.4.3";
        outputHash = "sha256-qu0EMMx9K80vkH8kJOhvhrTmNczGX7PnISFsYDy7XqM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Embed external files into your notes.'';
            homepage = "https://github.com/Kay607/obsidian-pathlinker";
        };
    }
);
    "pdf-emojis" = (
    let
        baseUrl = "https://github.com/mmarusiak/pdf-emojis-plugin/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf-emojis";
        version = "0.1.4";
        outputHash = "sha256-w9sj9aNTg3F0dtWz8sWbsYhSwYRpqAp7Wjbx7kXLeuI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps you exporting all your emojis to pdf (even those in headings)!'';
            homepage = "https://github.com/mmarusiak/pdf-emojis-plugin";
        };
    }
);
    "pdf-folder-to-markdowns" = (
    let
        baseUrl = "https://github.com/crishood/pdf-folder-to-markdowns/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf-folder-to-markdowns";
        version = "1.1.0";
        outputHash = "sha256-EfAYR+E/tle9UPII0f9QzpV4SI2M3YJMCoP2sHZzNXk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a folder of PDFs into Markdown files with embedded PDFs'';
            homepage = "https://github.com/crishood/pdf-folder-to-markdowns";
        };
    }
);
    "pdf-paste" = (
    let
        baseUrl = "https://github.com/Cormac-C/obsidian-paste-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf-paste";
        version = "1.0.0";
        outputHash = "sha256-J+zUmRouvILlVZ6oijpAewJVVcades0o8Oi3Imw9EYA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improve copy-paste from PDFs by cleaning newlines and hyphens.'';
            homepage = "https://github.com/Cormac-C/obsidian-paste-plugin";
        };
    }
);
    "pdf-plus" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-pdf-plus/releases/download/0.40.31";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf-plus";
        version = "0.40.31";
        outputHash = "sha256-6d0ZEQWZHbK9XRQad1HN6QpAjxIK6HTq5PUMFhhm2bk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''The most Obsidian-native PDF annotation tool ever.'';
            homepage = "https://github.com/RyotaUshio/obsidian-pdf-plus";
        };
    }
);
    "pdf-printer" = (
    let
        baseUrl = "https://github.com/cubexy/obsidian-pdf-printer/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf-printer";
        version = "1.1.4";
        outputHash = "sha256-75gBITod5917qWhLAkh3xznQe4k/N8R/oUYYv5VTk1o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert PDF documents to images directly in your notes without external dependencies.'';
            homepage = "https://github.com/cubexy/obsidian-pdf-printer";
        };
    }
);
    "pdf-writer" = (
    let
        baseUrl = "https://github.com/jkom4/obsidian-pdf-writer/releases/download/0.12.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf-writer";
        version = "0.12.1";
        outputHash = "sha256-74pkOqokbDoNAd5rBjsOMEY3mpqDqL8JRqpYlh7FJLo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''to write and fill a PDF.'';
            homepage = "https://github.com/jkom4/obsidian-pdf-writer";
        };
    }
);
    "pdf2img" = (
    let
        baseUrl = "https://github.com/RasmusAChr/PDF2Images/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pdf2img";
        version = "1.3.1";
        outputHash = "sha256-gFHW4AdRqmOq2HW5LzUHWGJWCI+m3EB/+4xuK38nKQU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turns a PDF into a series of images.'';
            homepage = "https://github.com/RasmusAChr/PDF2Images";
        };
    }
);
    "peekaboo" = (
    let
        baseUrl = "https://github.com/Lio5n/peekaboo/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "peekaboo";
        version = "1.3.0";
        outputHash = "sha256-KlQxRMi2QsJ2636X7vASSiipSghPrbgDiOzJX0sO3hY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Protect your privacy by setting a password to hide files.'';
            homepage = "https://github.com/Lio5n/peekaboo";
        };
    }
);
    "peerdraft" = (
    let
        baseUrl = "https://github.com/peerdraft/obsidian-plugin/releases/download/2.16.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "peerdraft";
        version = "2.16.1";
        outputHash = "sha256-APhADGSU+WoNeuJc42P/56yyvyhg9uVM9seq7mqKZLA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Real-time, instant collaboration on documents or folders. Whether for quick note-taking or building a team knowledge base, Peerdaft syncs with your collaborators" vaults and also offers a Web Editor.'';
            homepage = "https://github.com/peerdraft/obsidian-plugin";
        };
    }
);
    "performosu" = (
    let
        baseUrl = "https://github.com/ruikurenaii/performium/releases/download/1.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "performosu";
        version = "1.8.1";
        outputHash = "sha256-qsyN4LxD2EklRRvv0cxPVWuqs6NsOHSPidHrMlglP90=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrates the osu! Performance Points system to to gamify your note-taking experience!'';
            homepage = "https://github.com/ruikurenaii/performium";
        };
    }
);
    "perilous-writing" = (
    let
        baseUrl = "https://github.com/sameersismail/obsidian-perilous-writing/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "perilous-writing";
        version = "1.3.0";
        outputHash = "sha256-3faCGkW/yS1n4oqM2eCgZhhXSKi7UgrCWTEpgykjO+0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write continuously—or lose all progress.'';
            homepage = "https://github.com/sameersismail/obsidian-perilous-writing";
        };
    }
);
    "periodic-notes" = (
    let
        baseUrl = "https://github.com/liamcain/obsidian-periodic-notes/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "periodic-notes";
        version = "1.0.0";
        outputHash = "sha256-+mSbxZh06gJC2u06IrF2F7nXLBt4b3HVLsQGcsG+nZE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create/manage your daily, weekly, and monthly notes.'';
            homepage = "https://github.com/liamcain/obsidian-periodic-notes";
        };
    }
);
    "periodic-para" = (
    let
        baseUrl = "https://github.com/quanru/obsidian-lifeos/releases/download/1.25.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "periodic-para";
        version = "1.25.0";
        outputHash = "sha256-v7WRD2f1mzkQFx2drD7CRqkBCSVplA3mzCNE9vqDSX4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Life management system.'';
            homepage = "https://github.com/quanru/obsidian-lifeos";
        };
    }
);
    "periodic-table" = (
    let
        baseUrl = "https://github.com/R-Cramer4/Periodic-Table-Obsidian/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "periodic-table";
        version = "2.0.0";
        outputHash = "sha256-VbArfdVXhdlLR7H1ybutL70hZYvnXqjWBGVymSdwHP0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View a periodic table of elements in the sidebar.'';
            homepage = "https://github.com/R-Cramer4/Periodic-Table-Obsidian";
        };
    }
);
    "permalink-opener" = (
    let
        baseUrl = "https://github.com/kepano/obsidian-permalink-opener/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "permalink-opener";
        version = "1.0.2";
        outputHash = "sha256-olYEwJR/nLIS3vp6KDuZ8W32I+28Iy0Ip3VYyygPbyE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open URLs based on a permalink or slug in the note properties. Useful with static site generator such as Jekyll, Hugo, Eleventy, etc.'';
            homepage = "https://github.com/kepano/obsidian-permalink-opener";
        };
    }
);
    "perplexity-converter" = (
    let
        baseUrl = "https://github.com/heseber/perplexity-converter/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "perplexity-converter";
        version = "1.0.6";
        outputHash = "sha256-KHb6cYjS4dpKqugJG3IhSwxiVwoiu1GQKd7dCIsXWlQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fix references and their hyperlinks in text pasted from Perplexity.'';
            homepage = "https://github.com/heseber/perplexity-converter";
        };
    }
);
    "persian-calendar" = (
    let
        baseUrl = "https://github.com/karfekr/obsidian-persian-calendar/releases/download/4.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "persian-calendar";
        version = "4.5.1";
        outputHash = "sha256-Db9zpLl5hdrydS3HRmpohXbUOMVHTOQHneoIvWeN2KM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Persian Calendar for managing periodic notes based on persian solar (shamsi) calendar.'';
            homepage = "https://github.com/karfekr/obsidian-persian-calendar";
        };
    }
);
    "persistent-graph" = (
    let
        baseUrl = "https://github.com/Sanqui/obsidian-persistent-graph/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "persistent-graph";
        version = "0.1.5";
        outputHash = "sha256-0Vm0CI+N5+8SN82H2kxzlxU2fA26yWpKzf/sAzMkPgA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save and restore the positions of nodes on your graph.'';
            homepage = "https://github.com/Sanqui/obsidian-persistent-graph";
        };
    }
);
    "persistent-links" = (
    let
        baseUrl = "https://github.com/ivan-lednev/obsidian-persistent-links/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "persistent-links";
        version = "0.2.0";
        outputHash = "sha256-VducgfETyCMv6SYhISKSqmW4aabrG26L4juq1WAjJEk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically repair internal links to blocks and headings when moving them between files.'';
            homepage = "https://github.com/ivan-lednev/obsidian-persistent-links";
        };
    }
);
    "personal-assistant" = (
    let
        baseUrl = "https://github.com/edonyzpc/personal-assistant/releases/download/1.5.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "personal-assistant";
        version = "1.5.8";
        outputHash = "sha256-OTZwJT18IEIGRur89sVD2TY/mBtx+s9mKNYTgP5tmPY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered workflows to streamline the management of records, callouts, frontmatter, graph view, themes, and plugins.'';
            homepage = "https://github.com/edonyzpc/personal-assistant";
        };
    }
);
    "personal-development-plan" = (
    let
        baseUrl = "https://github.com/artemkorsakov/personal-development-plan/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "personal-development-plan";
        version = "1.0.8";
        outputHash = "sha256-Ir3aSdZrtrvRvsOUpTKcTSdRI1qp4+kPfwLPlXQ2akI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Build your Personal Development Plan.'';
            homepage = "https://github.com/artemkorsakov/personal-development-plan";
        };
    }
);
    "personal-os" = (
    let
        baseUrl = "https://github.com/GengAd/obsidian-personal-os/releases/download/3.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "personal-os";
        version = "3.0.2";
        outputHash = "sha256-ns0Y3iM37NOiuKB+Cc1UnGm+yKaqvZw3xl/rrsXYE3Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Streamlining task management and productivity with a touch of gamification.'';
            homepage = "https://github.com/GengAd/obsidian-personal-os";
        };
    }
);
    "pexels-banner" = (
    let
        baseUrl = "https://github.com/jparkerweb/pixel-banner/releases/download/3.6.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pexels-banner";
        version = "3.6.18";
        outputHash = "sha256-qrQoU2TzokNytqXFB3oshgscd1cBed2movFbCU9DUY0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your notes with customizable banner images, including AI-generated designs and a curated store of downloadable banners. Transform your workspace with visually stunning headers that add context, improve aesthetics, and take your note-taking beyond the ordinary.'';
            homepage = "https://github.com/jparkerweb/pixel-banner";
        };
    }
);
    "pf2-action-icons" = (
    let
        baseUrl = "https://github.com/thiagocoutinhor/pf2-action-icons/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pf2-action-icons";
        version = "1.1.4";
        outputHash = "sha256-+/XjYIgVDJWrOaP3No5OWcw8UX9fSx31gIVz65cw48g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display Pathfinder 2E action icons.'';
            homepage = "https://github.com/thiagocoutinhor/pf2-action-icons";
        };
    }
);
    "pf2e-statblocks" = (
    let
        baseUrl = "https://github.com/pixley/pf2e-statblock-for-obsidian/releases/download/3.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pf2e-statblocks";
        version = "3.4.0";
        outputHash = "sha256-Kbxa3P9nUKGYk/S7WU7w9X9ylnI6BbUBEgEIi6TFMvc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Pathfinder 2e statblocks cleanly, using only Markdown-based syntax.'';
            homepage = "https://github.com/pixley/pf2e-statblock-for-obsidian";
        };
    }
);
    "phone-to-roam-to-obsidian" = (
    let
        baseUrl = "https://github.com/dgarrett/phone-to-roam-to-obsidian/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "phone-to-roam-to-obsidian";
        version = "1.0.2";
        outputHash = "sha256-kK/CZ5zbT0bxgFwvZXwVv5/trv3Aq3ZPADqSYgNfWzU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An unofficial client for phonetonote.com (previously phonetoroam.com).'';
            homepage = "https://github.com/dgarrett/phone-to-roam-to-obsidian";
        };
    }
);
    "photopea-editor" = (
    let
        baseUrl = "https://github.com/KarmaToast40340/photopea-editor/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "photopea-editor";
        version = "1.0.0";
        outputHash = "sha256-sHQFFcVwZSjs1zIO5gwNMqU5wDFptuWSDdFrQ8PkpUQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically opens images in Photopea when clicked in your vault.'';
            homepage = "https://github.com/KarmaToast40340/photopea-editor";
        };
    }
);
    "phrasesync" = (
    let
        baseUrl = "https://github.com/digvijay-s-todiwal/phrasesync/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "phrasesync";
        version = "1.3.0";
        outputHash = "sha256-Cf3vXKHklCbPBsZCzQBrzaE4v63bk3lLeP30x5hD5Qg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto-suggests internal links mid-sentence from note titles, headings, and block references.'';
            homepage = "https://github.com/digvijay-s-todiwal/phrasesync";
        };
    }
);
    "pia-viewer" = (
    let
        baseUrl = "https://github.com/dldisud/obsidian-pia-viewer/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pia-viewer";
        version = "1.1.1";
        outputHash = "sha256-N3geWlWk6Bm2031WQvD8omNPMQ7YmR4n8hDTY3oRdxY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make it look mobile on pc'';
            homepage = "https://github.com/dldisud/obsidian-pia-viewer";
        };
    }
);
    "pickly-page-blend" = (
    let
        baseUrl = "https://github.com/dmitrichev/pickly-page-blend/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pickly-page-blend";
        version = "1.0.3";
        outputHash = "sha256-4hGmyMesIwLSrGEjp2KJ7Gx+D8bhPNEG8+e+bvJfYS4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes in one click.'';
            homepage = "https://github.com/dmitrichev/pickly-page-blend";
        };
    }
);
    "pieces-for-developers" = (
    let
        baseUrl = "https://github.com/pieces-app/obsidian-pieces/releases/download/2.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pieces-for-developers";
        version = "2.0.1";
        outputHash = "sha256-s4EZsPSPclp/uisapaaloPfoKqOB9BClsi1ANP0Fiyc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Streamline your coding workflow with powerful features for capturing, managing, translating, and enhancing code snippets. (Closed source)'';
            homepage = "https://github.com/pieces-app/obsidian-pieces";
        };
    }
);
    "pin-enhancer" = (
    let
        baseUrl = "https://github.com/Sheeplet1/obsidian-pin-enhancer/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pin-enhancer";
        version = "1.0.1";
        outputHash = "sha256-d1Le/5/4D9QL4CPvKeWi44vVQsJDTn6ay34If3fR9l4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prevents closure of pinned tabs'';
            homepage = "https://github.com/Sheeplet1/obsidian-pin-enhancer";
        };
    }
);
    "pinboard-sync" = (
    let
        baseUrl = "https://github.com/Automatt/obsidian-pinboard-sync/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pinboard-sync";
        version = "1.1.4";
        outputHash = "sha256-/ZZFKejMUXP0F2KP6PtMTC9UmAGd1pMGwACAp0F1/I4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync Pinboard.in links with your daily notes.'';
            homepage = "https://github.com/Automatt/obsidian-pinboard-sync";
        };
    }
);
    "pinned-daily-notes" = (
    let
        baseUrl = "https://github.com/docmarionum1/obsidian-pinned-daily-notes/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pinned-daily-notes";
        version = "1.2.0";
        outputHash = "sha256-YqxYW0HyXYU/N4v6Y3f8GrTWz0a2OgHfN+LaaKpCfgY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamically update a pinned tab with today"s daily note'';
            homepage = "https://github.com/docmarionum1/obsidian-pinned-daily-notes";
        };
    }
);
    "pinned-notes" = (
    let
        baseUrl = "https://github.com/vasilcoin002/pinned-notes-plugin-obsidian/releases/download/2.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pinned-notes";
        version = "2.0.4";
        outputHash = "sha256-b6aEhqRGUubAgE43IzE0iG1WdR4ySHEA2dpz7YUjk5U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pin frequently-used notes on Ribbon actions (left sidebar)'';
            homepage = "https://github.com/vasilcoin002/pinned-notes-plugin-obsidian";
        };
    }
);
    "pintora" = (
    let
        baseUrl = "https://github.com/amiaslee/obsidian-pintora/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pintora";
        version = "1.0.0";
        outputHash = "sha256-4lNXThFtX7OBqcQBOnKxgBJexrXBmMlsiY54f1Vk2Sg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates diagrams using Pintora.'';
            homepage = "https://github.com/amiaslee/obsidian-pintora";
        };
    }
);
    "pinyin-replacer" = (
    let
        baseUrl = "https://github.com/LarrySAL/pinyin-replacer/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pinyin-replacer";
        version = "1.0.1";
        outputHash = "sha256-tzZpIxIQzJc/jmgOAkW3viVKJvghY6foBs00yDqd0EQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use the Pinyin tones without having to install extra keyboard layouts.'';
            homepage = "https://github.com/LarrySAL/pinyin-replacer";
        };
    }
);
    "pivotal-tracker-integration" = (
    let
        baseUrl = "https://github.com/JonnyDeates/obsidian-pivotal-tracker-integration-plugin/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pivotal-tracker-integration";
        version = "1.0.6";
        outputHash = "sha256-UIp58cca9uyHP1BkaMnAchHNMm/md8O9NnBY4iOCq4k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pull stories, bugs, chores from Pivotal Tracker.'';
            homepage = "https://github.com/JonnyDeates/obsidian-pivotal-tracker-integration-plugin";
        };
    }
);
    "pixel-perfect-image" = (
    let
        baseUrl = "https://github.com/johansan/pixel-perfect-image/releases/download/1.0.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pixel-perfect-image";
        version = "1.0.25";
        outputHash = "sha256-PVEh2SsxpABc7UeaqshAyi3sWkKXbcRx/Dh/DDZLPTw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pixel perfect 100% image resizing, copy to clipboard, show image in Finder/Explorer, edit image in external editor, and much more.'';
            homepage = "https://github.com/johansan/pixel-perfect-image";
        };
    }
);
    "pixel-pets" = (
    let
        baseUrl = "https://github.com/LucasHJin/obsidian-pets/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pixel-pets";
        version = "1.5.1";
        outputHash = "sha256-Fk6xWFFVryMXMJWWl0aLFce+titgcu+ioiUQgDe1qDA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds cute pixel pets.'';
            homepage = "https://github.com/LucasHJin/obsidian-pets";
        };
    }
);
    "pkvs" = (
    let
        baseUrl = "https://github.com/iamrecursion/obsidian-pkvs/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pkvs";
        version = "1.1.2";
        outputHash = "sha256-sUvqUT337dURsX+DZQt3D+UaxF5/WT4ZN2raeKybAkk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides a persistent key-value store for use in scripts in Obsidian.'';
            homepage = "https://github.com/iamrecursion/obsidian-pkvs";
        };
    }
);
    "plot-vectors-graphs" = (
    let
        baseUrl = "https://github.com/nicoletanyt/obsidian-plugin-graphs/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plot-vectors-graphs";
        version = "1.0.2";
        outputHash = "sha256-5RQWlmGaqIBsUapesx1BxzylTtcRfDEn0mEHjnUEawM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates graphs and vectors.'';
            homepage = "https://github.com/nicoletanyt/obsidian-plugin-graphs";
        };
    }
);
    "plugin-manager" = (
    let
        baseUrl = "https://github.com/ohm-en/obsidian-plugin-manager/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plugin-manager";
        version = "0.1.5";
        outputHash = "sha256-ULEFA7LcTSSGeazj6myRgdoF6oA0XoJbuOvMtrXoQM0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extend plugin management.'';
            homepage = "https://github.com/ohm-en/obsidian-plugin-manager";
        };
    }
);
    "plugin-reloader" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-plugin-reloader/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plugin-reloader";
        version = "0.0.3";
        outputHash = "sha256-AFCcF8l+N1DnbA97JsU9Gha1BA9Rui76RsAJhH0QcDs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manually reload plugins.'';
            homepage = "https://github.com/Benature/obsidian-plugin-reloader";
        };
    }
);
    "plugin-repl" = (
    let
        baseUrl = "https://github.com/talwrii/plugin-repl/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plugin-repl";
        version = "1.2.0";
        outputHash = "sha256-nEPN/W5tjPHUtvuZoyQhE9D5XDxfrJxrSg3dc59oIas=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create automation using Javascript scripts from within your notes. Super light-weight plugins, access the plugin api, rapidly iterate for plugin development. Emacs inspired.'';
            homepage = "https://github.com/talwrii/plugin-repl";
        };
    }
);
    "plugin-update-locker" = (
    let
        baseUrl = "https://github.com/Lemon695/obsidian-plugin-update-locker/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plugin-update-locker";
        version = "1.0.5";
        outputHash = "sha256-UC30Wu90Y90UCUpa18O8UEFWhedo+8uRnAoLW9Bzu8c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prevent specific plugins from being updated'';
            homepage = "https://github.com/Lemon695/obsidian-plugin-update-locker";
        };
    }
);
    "plugins-annotations" = (
    let
        baseUrl = "https://github.com/alberti42/obsidian-plugins-annotations/releases/download/1.7.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plugins-annotations";
        version = "1.7.11";
        outputHash = "sha256-jAk37b85yDQXq0M7e/6X8Wq9UM8yY6QwLR7jGdy07Ds=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows adding personal comments to each installed plugin.'';
            homepage = "https://github.com/alberti42/obsidian-plugins-annotations";
        };
    }
);
    "plugins-galore" = (
    let
        baseUrl = "https://github.com/plugins-galore/obsidian-plugins-galore/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "plugins-galore";
        version = "0.1.1";
        outputHash = "sha256-7jqArjquHwvAFuFCvDeNxDhn63KBdjYJCL7UGkZB1AE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily sideload other plugins.'';
            homepage = "https://github.com/plugins-galore/obsidian-plugins-galore";
        };
    }
);
    "pocketbook-cloud-highlight-importer" = (
    let
        baseUrl = "https://github.com/lenalebt/obsidian-pocketbook-cloud-highlight-importer/releases/download/0.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pocketbook-cloud-highlight-importer";
        version = "0.1.7";
        outputHash = "sha256-HOcO7L3BiN2BHBpapIcf78G4YOe+P3W5im/z+X4G72U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import notes and highlights from your Pocketbook Cloud account.'';
            homepage = "https://github.com/lenalebt/obsidian-pocketbook-cloud-highlight-importer";
        };
    }
);
    "podcast-note" = (
    let
        baseUrl = "https://github.com/marcjulianschwarz/obsidian-podcast-note/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "podcast-note";
        version = "1.3.3";
        outputHash = "sha256-tz6SHxm690zNFl7FC6zvWUm+Zm/hNOfD74uerj656s8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Podcast Note lets you automatically add podcast information to your notes.'';
            homepage = "https://github.com/marcjulianschwarz/obsidian-podcast-note";
        };
    }
);
    "podnotes" = (
    let
        baseUrl = "https://github.com/chhoumann/PodNotes/releases/download/2.15.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "podnotes";
        version = "2.15.1";
        outputHash = "sha256-+v9DjWQL21129A5Oh4munWjsa0LbJujLJcM9A5Mto40=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write notes on podcasts with ease.'';
            homepage = "https://github.com/chhoumann/PodNotes";
        };
    }
);
    "poker" = (
    let
        baseUrl = "https://github.com/mAAdhaTTah/obsidian-poker/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "poker";
        version = "1.2.3";
        outputHash = "sha256-MOMi4Ca8Fu85fHUJYur6ZLrAI8NnQGmrhTZHPy5UQpk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily document and view your poker hands.'';
            homepage = "https://github.com/mAAdhaTTah/obsidian-poker";
        };
    }
);
    "poker-range" = (
    let
        baseUrl = "https://github.com/marplek/obsidian-poker-range/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "poker-range";
        version = "1.0.0";
        outputHash = "sha256-sEL/QGHj+a7xrvVaTG0iZ0M7QGOPoN0i7VLVETOpfD8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a poker range grid'';
            homepage = "https://github.com/marplek/obsidian-poker-range";
        };
    }
);
    "pomobar" = (
    let
        baseUrl = "https://github.com/semanticdata/obsidian-pomodoro/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pomobar";
        version = "1.6.1";
        outputHash = "sha256-OvdzxKCCmwub07G7j/bMLdSBWoGmLyC0QSqHEsVxpJs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple Pomodoro timer that lives in your status bar. Left click to start/stop, right click to reset when paused, middle click to cycle between 25/15/5 minute durations.'';
            homepage = "https://github.com/semanticdata/obsidian-pomodoro";
        };
    }
);
    "pomodoro-planner" = (
    let
        baseUrl = "https://github.com/onesvat/obsidian-pomodoro-planner/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pomodoro-planner";
        version = "1.0.7";
        outputHash = "sha256-siLFRzdV/i0V8aslgzB9nmWMO09CXmuXCPKeKowWhrc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates a pomodoro schedule plan.'';
            homepage = "https://github.com/onesvat/obsidian-pomodoro-planner";
        };
    }
);
    "pomodoro-timer" = (
    let
        baseUrl = "https://github.com/eatgrass/obsidian-pomodoro-timer/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pomodoro-timer";
        version = "1.2.3";
        outputHash = "sha256-lRAmyHbzvuCkQkVPtC6pNrQb3uFwu/GlKCFJJrCtrvc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your daily focus using the Pomodoro Technique.'';
            homepage = "https://github.com/eatgrass/obsidian-pomodoro-timer";
        };
    }
);
    "pomodoro-widget" = (
    let
        baseUrl = "https://github.com/gvvim/obsidian-pomodoro-widget/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pomodoro-widget";
        version = "1.0.2";
        outputHash = "sha256-zqjI/+Q0uesU8R9Ybp/LJKEtfFnWcT+UOZnHtZmV8vo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides a widget based on a pomodoro kitchen timer. It"s designed to be haptic, and has a constant ticking sound, and an alarm sound that can be toggled.'';
            homepage = "https://github.com/gvvim/obsidian-pomodoro-widget";
        };
    }
);
    "popkit" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-popkit/releases/download/1.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "popkit";
        version = "1.1.10";
        outputHash = "sha256-/ReeKGjVjsaYYOOzKn1MrQ2/0Crj8taGqNv2CvPRFKM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Select text to instantly access quick tools'';
            homepage = "https://github.com/zhouhua/obsidian-popkit";
        };
    }
);
    "post-to-bluesky" = (
    let
        baseUrl = "https://github.com/RieTamura/Post-To-Bluesky/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "post-to-bluesky";
        version = "0.1.5";
        outputHash = "sha256-qUiPbwtagW9gfNV4nIZIBhP5/weCZZOoDZxmL6VwLw4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post selected text or entire note to Bluesky.'';
            homepage = "https://github.com/RieTamura/Post-To-Bluesky";
        };
    }
);
    "post-webhook" = (
    let
        baseUrl = "https://github.com/Masterb1234/obsidian-post-webhook/releases/download/1.2.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "post-webhook";
        version = "1.2.5";
        outputHash = "sha256-SP4tKLK+3TZHAVEAa9G9HGvKlVtovg/fdt+oetHVs1Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send notes to webhook endpoints, for seamless integration with i.a. n8n, Make.com, and Zapier.'';
            homepage = "https://github.com/Masterb1234/obsidian-post-webhook";
        };
    }
);
    "postfix" = (
    let
        baseUrl = "https://github.com/bhagyas/obsidian-postfix-plugin/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "postfix";
        version = "1.1.3";
        outputHash = "sha256-I+iMqb/aPnIXiumv4hLgb6JWaTDlvxg0GrvSvt7ojLU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use postfix completions to automatically format text.'';
            homepage = "https://github.com/bhagyas/obsidian-postfix-plugin";
        };
    }
);
    "potato-indexer" = (
    let
        baseUrl = "https://github.com/LoyalPotato/potato-indexer/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "potato-indexer";
        version = "1.4.0";
        outputHash = "sha256-LCSdP85iMapoP2cHfH73JpR836sCany0bnIJJJK95LA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate a content index based on your selection or the whole file.'';
            homepage = "https://github.com/LoyalPotato/potato-indexer";
        };
    }
);
    "power-mode" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-power-mode/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "power-mode";
        version = "1.0.8";
        outputHash = "sha256-ECfQcktnzNk2SqCR/k4xXprWZ4LBGmsSP08n0YdQJ7k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Active POWER MODE!!!!'';
            homepage = "https://github.com/zhouhua/obsidian-power-mode";
        };
    }
);
    "prettier" = (
    let
        baseUrl = "https://github.com/GoodbyeNJN/obsidian-plugin-prettier/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "prettier";
        version = "2.0.2";
        outputHash = "sha256-b+zIXcbITl0hIVJDHXLsnyOebQbwnyhX4sEwDwsY4do=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format your notes with Prettier and custom formatting options.'';
            homepage = "https://github.com/GoodbyeNJN/obsidian-plugin-prettier";
        };
    }
);
    "prettier-format" = (
    let
        baseUrl = "https://github.com/dylanarmstrong/obsidian-prettier-plugin/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "prettier-format";
        version = "0.2.0";
        outputHash = "sha256-LNMVZTlZk98Tgasc+EwU2LuXK9//qXqdzBtxEAFL9KI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format files with Prettier using built-in settings for configuration.'';
            homepage = "https://github.com/dylanarmstrong/obsidian-prettier-plugin";
        };
    }
);
    "pretty-properties" = (
    let
        baseUrl = "https://github.com/anareaty/pretty-properties/releases/download/1.10.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pretty-properties";
        version = "1.10.17";
        outputHash = "sha256-SUPfb6T9v3pWRJbdIv2LLeMYOZLv9HQbx8DGIsHb4/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Makes note properties look more fun: adds side image, banners, list property colors and allows to hide specific properties.'';
            homepage = "https://github.com/anareaty/pretty-properties";
        };
    }
);
    "previous-daily-note" = (
    let
        baseUrl = "https://github.com/talau/obsidian-previous-daily-note/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "previous-daily-note";
        version = "1.1.1";
        outputHash = "sha256-rLW9xcV85zDzqrNDsfVMb595rOP+XracKZeemr3HXa8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Unlike the "Daily notes" command "Open previous daily note", it opens the previous daily note starting from today, rather than the daily note currently open. It also provides a ribbon icon and a command to perform this action.'';
            homepage = "https://github.com/talau/obsidian-previous-daily-note";
        };
    }
);
    "print" = (
    let
        baseUrl = "https://github.com/marijnbent/obsidian-print/releases/download/0.5.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "print";
        version = "0.5.3";
        outputHash = "sha256-mf6/fZh85d/w6efZMyn2Aet/ASr3sTHlmDuWL9ayWm8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Print notes and documents directly from your workspace.'';
            homepage = "https://github.com/marijnbent/obsidian-print";
        };
    }
);
    "prioritize" = (
    let
        baseUrl = "https://github.com/Tekknoman/obsidian-prio-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "prioritize";
        version = "1.0.2";
        outputHash = "sha256-y0NNpqn6p2JnoutrlmpbqrB1T4pnxe3RjCsiF7mhOTE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prioritize your tasks and notes.'';
            homepage = "https://github.com/Tekknoman/obsidian-prio-plugin";
        };
    }
);
    "private-ai" = (
    let
        baseUrl = "https://github.com/gabosgab/ObsidianPrivateAI/releases/download/1.0.64";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "private-ai";
        version = "1.0.64";
        outputHash = "sha256-219A/tr1Fs91AKd8ZduAdu5mqXyCcwfExIce1XOidAU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Effortlessly chat with your notes using locally hosted AI.  Private by design, your notes never leave the device and use locally processing only.'';
            homepage = "https://github.com/gabosgab/ObsidianPrivateAI";
        };
    }
);
    "private-mode" = (
    let
        baseUrl = "https://github.com/markusmo3/obsidian-private-mode/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "private-mode";
        version = "1.2.0";
        outputHash = "sha256-q1XQB+U1chDuBY081IJYAxIWUzBvnDOIRcjHtcqJLhc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple #private mode'';
            homepage = "https://github.com/markusmo3/obsidian-private-mode";
        };
    }
);
    "progress-clocks" = (
    let
        baseUrl = "https://github.com/tokenshift/obsidian-progress-clocks/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "progress-clocks";
        version = "0.3.2";
        outputHash = "sha256-znAcH6vAaRhd02pstsa02naEn8ksP2NkZxbLMpDprcE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Progress clocks and other useful widgets for real-time status tracking.'';
            homepage = "https://github.com/tokenshift/obsidian-progress-clocks";
        };
    }
);
    "progress-tracker" = (
    let
        baseUrl = "https://github.com/vannamhh/progress-tracker/releases/download/1.5.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "progress-tracker";
        version = "1.5.4";
        outputHash = "sha256-wyALiID8CWoH9AiOBCFVdl7WGGwunm5fteCq9dfsMnM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track task completion with a visual progress bar in your sidebar, auto update column Kanban'';
            homepage = "https://github.com/vannamhh/progress-tracker";
        };
    }
);
    "progressbar" = (
    let
        baseUrl = "https://github.com/zwpaper/obsidian-progressbar/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "progressbar";
        version = "0.7.0";
        outputHash = "sha256-qKMNABXZ3V8mpKKzpk+u7VXErqTQ8aCCxFEkEA+Wp/w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render CodeBlock into a progress bar based on time or manually.'';
            homepage = "https://github.com/zwpaper/obsidian-progressbar";
        };
    }
);
    "project-browser" = (
    let
        baseUrl = "https://github.com/daledesilva/obsidian_project-browser/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "project-browser";
        version = "0.3.2";
        outputHash = "sha256-ahFuhQ1GG5Qr/PWYcU3jFoFFSq2vjS8fgA5KCIgaU3I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replaces your new tab window with a browseable list of the files and folders in your vault.'';
            homepage = "https://github.com/daledesilva/obsidian_project-browser";
        };
    }
);
    "project-euler-stats" = (
    let
        baseUrl = "https://github.com/artemkorsakov/project-euler-stats/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "project-euler-stats";
        version = "1.0.4";
        outputHash = "sha256-fanibVf0IEkc6r8kZGtbOwl8MifX+3gLY2Q3aYZzIqs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get statistics from Project Euler.'';
            homepage = "https://github.com/artemkorsakov/project-euler-stats";
        };
    }
);
    "project-tasks" = (
    let
        baseUrl = "https://github.com/paulpaterson/obsidian-project-tasks/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "project-tasks";
        version = "0.1.3";
        outputHash = "sha256-QqlhyAQlrMScv0OqTd6utNRpFLlXP++zS9tbu7whvOo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to easily create linked tasks for projects by managing the task ID"s and dependencies.'';
            homepage = "https://github.com/paulpaterson/obsidian-project-tasks";
        };
    }
);
    "proletarian-wizard" = (
    let
        baseUrl = "https://github.com/cfe84/obsidian-pw/releases/download/1.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "proletarian-wizard";
        version = "1.9.0";
        outputHash = "sha256-UDQfL9JyAqY34ShqLKqQk+SpU3OCOY89NVDqdMO2xe4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track your tasks across all the notes in your workspace. Organize your day. Plan your work'';
            homepage = "https://github.com/cfe84/obsidian-pw";
        };
    }
);
    "prologue" = (
    let
        baseUrl = "https://github.com/joshavanier/prologue/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "prologue";
        version = "1.0.1";
        outputHash = "sha256-yZqnA2CqMmUw1nAlig3H+MaGY55AWIo6t3tv/JY6L2k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a random quote or reminder on the new tab page.'';
            homepage = "https://github.com/joshavanier/prologue";
        };
    }
);
    "prompt" = (
    let
        baseUrl = "https://github.com/hungsu/obsidian-prompt/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "prompt";
        version = "1.1.0";
        outputHash = "sha256-oeVzjuaZzIkHF2wQIozz/ArOFjxtEndoHdV3cST1mEA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show inspirational prompts, from a prompt file in your vault.'';
            homepage = "https://github.com/hungsu/obsidian-prompt";
        };
    }
);
    "prompt-crafter" = (
    let
        baseUrl = "https://github.com/fabricehong/obsidian-prompt-crafter-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "prompt-crafter";
        version = "1.0.3";
        outputHash = "sha256-2+6BY0NQA9cpqclarKW23rXTNS1FxraY1W7gun/3I08=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create reusable, modular prompts.'';
            homepage = "https://github.com/fabricehong/obsidian-prompt-crafter-plugin";
        };
    }
);
    "proofreader" = (
    let
        baseUrl = "https://github.com/chrisgrieser/obsidian-proofreader/releases/download/1.8.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "proofreader";
        version = "1.8.2";
        outputHash = "sha256-MGIiG2cFzBYj18XeV4jZm5R96JosX1zTKKwaoyWdtDo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-based proofreading and stylistic improvements for your writing. Changes are inserted as suggestions directly in the editor, similar to suggested changes in word processing apps.'';
            homepage = "https://github.com/chrisgrieser/obsidian-proofreader";
        };
    }
);
    "protected-note" = (
    let
        baseUrl = "https://github.com/mmiksaa/obsidian-protected-note/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "protected-note";
        version = "2.0.0";
        outputHash = "sha256-lCFLqEUVnaW9Rn3qkLb0+KU4mElpT/s5qJ7G8kCMpfI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set password to protect your notes from other people. Simple, Fast.'';
            homepage = "https://github.com/mmiksaa/obsidian-protected-note";
        };
    }
);
    "pseudo-mica" = (
    let
        baseUrl = "https://github.com/aaaaalexis/obsidian-pseudo-mica/releases/download/2.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pseudo-mica";
        version = "2.2.1";
        outputHash = "sha256-XhtoasB5SNfsPDN22G4qmMkSJhaCY4Bf6Qi5PmMMmy0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mica-like background effect for Windows.'';
            homepage = "https://github.com/aaaaalexis/obsidian-pseudo-mica";
        };
    }
);
    "pseudocode-in-obs" = (
    let
        baseUrl = "https://github.com/ytliu74/obsidian-pseudocode/releases/download/1.6.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pseudocode-in-obs";
        version = "1.6.4";
        outputHash = "sha256-CuWm1PNCOC1PLNsOJpxkeljv/SX4b52xlf8LMeiSbGo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render LaTeX-style pseudocode inside a code block.'';
            homepage = "https://github.com/ytliu74/obsidian-pseudocode";
        };
    }
);
    "pt-url-helper" = (
    let
        baseUrl = "https://github.com/kndshein/obsidian-pt-url-helper/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pt-url-helper";
        version = "0.1.4";
        outputHash = "sha256-CLgkN31wt1Ug08zWs5LW06beRh/MUj7Kqk/MlV43vlU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically creates a Markdown link for Pivotal Tracker stories.'';
            homepage = "https://github.com/kndshein/obsidian-pt-url-helper";
        };
    }
);
    "publish-note-to-mowen" = (
    let
        baseUrl = "https://github.com/zhuSilence/obsidian-mowen-plugin/releases/download/0.0.29";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "publish-note-to-mowen";
        version = "0.0.29";
        outputHash = "sha256-Whg440z6dHm5XnHrY9gQGc2zud5Utmk29FTHLgvVnUs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish note to Mowen note mini program.'';
            homepage = "https://github.com/zhuSilence/obsidian-mowen-plugin";
        };
    }
);
    "publish-to-dev" = (
    let
        baseUrl = "https://github.com/stroiman/obsidian-dev-publish/releases/download/0.11.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "publish-to-dev";
        version = "0.11.2";
        outputHash = "sha256-m36LWm1ZOo47h1UVFQLvN8Hj5ptT1p2iY9lnTPTJSoQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish and update notes as articles on DEV (https://dev.to)'';
            homepage = "https://github.com/stroiman/obsidian-dev-publish";
        };
    }
);
    "publish-to-discourse" = (
    let
        baseUrl = "https://github.com/woodchen-ink/obsidian-publish-to-discourse/releases/download/1.0.36";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "publish-to-discourse";
        version = "1.0.36";
        outputHash = "sha256-VLgs+3XtQi1VF47E+ka6hde8HOn63rkwp1WH9IiEHBY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post notes to the Discourse forum.'';
            homepage = "https://github.com/woodchen-ink/obsidian-publish-to-discourse";
        };
    }
);
    "publish-url" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-publish-url/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "publish-url";
        version = "1.0.8";
        outputHash = "sha256-h09OVCZbJBUmbPsGJyMqvuv30e25RIyKEbZuU3mU01Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy Obsidian Publish URL to the clipboard.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-publish-url";
        };
    }
);
    "pubscale" = (
    let
        baseUrl = "https://github.com/piriwata/pubScale/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pubscale";
        version = "0.2.0";
        outputHash = "sha256-Z6y1W4UQNbN5e+sRGEhIxPXn+FzQaK8BJoYsEx2otjY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Seamlessly sync Markdown notes into PlanetScale tables.'';
            homepage = "https://github.com/piriwata/pubScale";
        };
    }
);
    "pug-templates" = (
    let
        baseUrl = "https://github.com/nicholas-wilcox/pug-templates-obsidian-plugin/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pug-templates";
        version = "0.4.1";
        outputHash = "sha256-XA0MOl16U8E+8zT3lqey/SOJA2zWUEzNdciWXNbC794=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use the Pug templating engine in your vault.'';
            homepage = "https://github.com/nicholas-wilcox/pug-templates-obsidian-plugin";
        };
    }
);
    "pure-chat-llm" = (
    let
        baseUrl = "https://github.com/TheJusticeMan/pure-chat-llm/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pure-chat-llm";
        version = "2.0.3";
        outputHash = "sha256-yxd2B0cbzE9cETvWH0XlKSVFGySWR8qDv20QC93hQQc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn notes into conversations with chatGPT'';
            homepage = "https://github.com/TheJusticeMan/pure-chat-llm";
        };
    }
);
    "pycalc" = (
    let
        baseUrl = "https://github.com/pycalc-plugin/obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "pycalc";
        version = "1.0.0";
        outputHash = "sha256-B0xWCXWWUmM/gvNCG53sGiouUoUUdxZ1kpwOVhQBNI4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Executing Python code directly within the editor by pressing the Enter key.'';
            homepage = "https://github.com/pycalc-plugin/obsidian";
        };
    }
);
    "python-lab-plugin" = (
    let
        baseUrl = "https://github.com/cristianvasquez/obsidian-lab/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "python-lab-plugin";
        version = "0.2.6";
        outputHash = "sha256-JSEFGRaOJ3iV+jt3LGbLnyTz5O/fePMzXUO4uKjukZc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An interface to experiment with Python scripts and more.'';
            homepage = "https://github.com/cristianvasquez/obsidian-lab";
        };
    }
);
    "qatt" = (
    let
        baseUrl = "https://github.com/sytone/obsidian-queryallthethings/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "qatt";
        version = "1.3.0";
        outputHash = "sha256-RBzOPNRd74S1MoRLZYJQ3hpRnQAm2+M46zJ2c6UK+rQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute SQL queries against your data in Obsidian and render it using templates.'';
            homepage = "https://github.com/sytone/obsidian-queryallthethings";
        };
    }
);
    "qb-reader-parser" = (
    let
        baseUrl = "https://github.com/J-Barta/qb-reader-parser/releases/download/1.9.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "qb-reader-parser";
        version = "1.9.5";
        outputHash = "sha256-BTX+DXb8wcVnI9dAQQiTT0vkMUxjsCoz67UXxNu84kg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically parse tossups from QB Reader into a format readable by the Obsidian_to_Anki plugin.'';
            homepage = "https://github.com/J-Barta/qb-reader-parser";
        };
    }
);
    "qiniu-image-uploader" = (
    let
        baseUrl = "https://github.com/jianzs/obsidian-qiniu-image-uploader/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "qiniu-image-uploader";
        version = "1.1.5";
        outputHash = "sha256-pDgny9mDdsqeuXT09Lyz/H0q4mcwJZWyQjABSH9gB28=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upload images from your clipboard to qiniu.com and embed uploaded image to your note.'';
            homepage = "https://github.com/jianzs/obsidian-qiniu-image-uploader";
        };
    }
);
    "qmd-as-md-obsidian" = (
    let
        baseUrl = "https://github.com/danieltomasz/qmd-as-md-obsidian/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "qmd-as-md-obsidian";
        version = "0.0.3";
        outputHash = "sha256-m/OxUcMAUiLTt1hsFhtNrc7qYMz1t+zzhvBs6Y96eJ0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View files with .qmd extension. QMD files contain a combination of Markdown and executable code cells and are a format supported by Quarto open source publishing system.'';
            homepage = "https://github.com/danieltomasz/qmd-as-md-obsidian";
        };
    }
);
    "quadro" = (
    let
        baseUrl = "https://github.com/chrisgrieser/obsidian-quadro/releases/download/1.28.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quadro";
        version = "1.28.2";
        outputHash = "sha256-DeHYlha+/PrjNBAR8zSvG8AESbqG4DRXvIXWC6mjSoA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Qualitative Data Analysis (QDA) for social scientists. An open alternative to MAXQDA and atlas.ti, using Markdown to store data and research codes.'';
            homepage = "https://github.com/chrisgrieser/obsidian-quadro";
        };
    }
);
    "quail" = (
    let
        baseUrl = "https://github.com/lyricat/obsidian-quail/releases/download/2.0.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quail";
        version = "2.0.15";
        outputHash = "sha256-Zu2V5ZgRQhwWPq8lGHlsmUFPkEtmdkJoJhE6INMMmbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save, publish, delivery notes via Quail.ink as newsletters and blogs.'';
            homepage = "https://github.com/lyricat/obsidian-quail";
        };
    }
);
    "quarto-exporter" = (
    let
        baseUrl = "https://github.com/AndreasThinks/obsidian-to-quarto-exporter/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quarto-exporter";
        version = "1.1.1";
        outputHash = "sha256-wBG7lsfpXf5FCgPVXMa8j8qxu7Ju28OneQz53hr96b0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export notes to Quarto-compatible QMD files.'';
            homepage = "https://github.com/AndreasThinks/obsidian-to-quarto-exporter";
        };
    }
);
    "quartz-syncer" = (
    let
        baseUrl = "https://github.com/saberzero1/quartz-syncer/releases/download/1.13.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quartz-syncer";
        version = "1.13.6";
        outputHash = "sha256-WuAqGyXMcET0Y97xJ2FbV+RDa6NdoWls6PkokAd8Tk4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage and publish your notes to Quartz, the fast, batteries-included static-site generator.'';
            homepage = "https://github.com/saberzero1/quartz-syncer";
        };
    }
);
    "query-json" = (
    let
        baseUrl = "https://github.com/Rooyca/query-json/releases/download/0.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "query-json";
        version = "0.1.8";
        outputHash = "sha256-eFLkKi500rPZTvPd1ZTYq3FicX3hCdWrJ4tC8Syl9o8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Read, query and work with JSON.'';
            homepage = "https://github.com/Rooyca/query-json";
        };
    }
);
    "querydash" = (
    let
        baseUrl = "https://github.com/liufree/obsidian-querydash/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "querydash";
        version = "2.0.3";
        outputHash = "sha256-hTMfr7lYjSwby4pghfRzCWqStnqsPnz9+5y+jwJhlb8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add new views and enhanced features for bases. Inspired by Dataview and bases, this includes search, sorting, and pagination functionalities similar to Notion.'';
            homepage = "https://github.com/liufree/obsidian-querydash";
        };
    }
);
    "quick-cards" = (
    let
        baseUrl = "https://github.com/abcamus/obsidian-quick-card/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-cards";
        version = "1.0.1";
        outputHash = "sha256-wdseMU9Z7zmen1GFZyeect5e+jVrcgZ3HMIJ1EQYpec=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Cardify your files.'';
            homepage = "https://github.com/abcamus/obsidian-quick-card";
        };
    }
);
    "quick-emoji" = (
    let
        baseUrl = "https://github.com/asibilia/obsidian-quick-emoji/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-emoji";
        version = "1.3.0";
        outputHash = "sha256-IlmAzH+Y8feCk23AQS4W74Qfh8jaZQ2laOdwTxStVkM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quick, in-editor, emoji inserting. Type ":" to start selecting an emoji to insert.'';
            homepage = "https://github.com/asibilia/obsidian-quick-emoji";
        };
    }
);
    "quick-explorer" = (
    let
        baseUrl = "https://github.com/pjeby/quick-explorer/releases/download/0.2.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-explorer";
        version = "0.2.16";
        outputHash = "sha256-I9x06wfdHyb+VSOUyR2DT+y1tbeBAAwylouBGqxlvvc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Perform file explorer operations (and see your current file path) from the title bar, using the mouse or keyboard.'';
            homepage = "https://github.com/pjeby/quick-explorer";
        };
    }
);
    "quick-file-name" = (
    let
        baseUrl = "https://github.com/Wapply/obsidian-quick-file-name/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-file-name";
        version = "1.0.3";
        outputHash = "sha256-dLQbCuNc/fG6sJYsr1+rlXWqVOarrDpO2puLVJ0Dxg8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates a note with a random string as its name.'';
            homepage = "https://github.com/Wapply/obsidian-quick-file-name";
        };
    }
);
    "quick-latex" = (
    let
        baseUrl = "https://github.com/joeyuping/quick_latex_obsidian/releases/download/2.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-latex";
        version = "2.6.5";
        outputHash = "sha256-FjuuVpuTxhuJhPKbfskdfhMkQqNv1ZZUKxcSvKzh5lo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simplify and speed up LaTeX math typing.'';
            homepage = "https://github.com/joeyuping/quick_latex_obsidian";
        };
    }
);
    "quick-links" = (
    let
        baseUrl = "https://github.com/iafisher/obsidian-quick-links/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-links";
        version = "0.2.6";
        outputHash = "sha256-q4jwYviQN4ww0OIQL3TM2A+Z8FQQCqSYkHa1aLmgkxE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create quick link shortcuts to Wikipedia and other sites'';
            homepage = "https://github.com/iafisher/obsidian-quick-links";
        };
    }
);
    "quick-matrix" = (
    let
        baseUrl = "https://github.com/thewheelbarrow/quick-matrix/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-matrix";
        version = "1.0.1";
        outputHash = "sha256-h/7RMUiEGSbhZI54UIfYCzLH9ERJGJjC24nJXpkuRuk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds command to make LaTeX matrices quickly.'';
            homepage = "https://github.com/thewheelbarrow/quick-matrix";
        };
    }
);
    "quick-nav" = (
    let
        baseUrl = "https://github.com/exoticknight/quick-nav/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-nav";
        version = "1.0.6";
        outputHash = "sha256-zd52iKLlgJkBoEwzagQnGI6dq5JJ2JUKmdcm01Pp1QQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Swift cursor navigation plugin to enhance editing.'';
            homepage = "https://github.com/exoticknight/quick-nav";
        };
    }
);
    "quick-notes" = (
    let
        baseUrl = "https://github.com/SeanMcOwen/Quick-Note-Obsidian-Plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-notes";
        version = "1.0.1";
        outputHash = "sha256-OPtNvVhbi/thL4B67QIjkdIzlyWDlkoHEDvGCHfVQuI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Speeds up some note taking abilities and allows for creating notes/links in the background'';
            homepage = "https://github.com/SeanMcOwen/Quick-Note-Obsidian-Plugin";
        };
    }
);
    "quick-open" = (
    let
        baseUrl = "https://github.com/itsonlyjames/obsidian-quick-select/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-open";
        version = "1.5.1";
        outputHash = "sha256-Td4XdND+bpxNwY9yNPwWstCi/hY66G2kfb3sUxhE6A8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly select items in any modal using keyboard shortcuts. Supercharge your workflow with fast, efficient item selection in Obsidian modals.'';
            homepage = "https://github.com/itsonlyjames/obsidian-quick-select";
        };
    }
);
    "quick-peek-sidebar" = (
    let
        baseUrl = "https://github.com/bwya77/obsidian-quick-peek-sidebar/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-peek-sidebar";
        version = "1.0.4";
        outputHash = "sha256-fXnBX3M519ZcecxfaMBZ/onKQhnfV5xmskNijUR8Xy8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Opens the left and/or right side panel by hovering over it.'';
            homepage = "https://github.com/bwya77/obsidian-quick-peek-sidebar";
        };
    }
);
    "quick-preview" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-quick-preview/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-preview";
        version = "0.6.1";
        outputHash = "sha256-pOspmNJ3XUOZaVZvsmDytgHdPR1auf5NAV1vtf/10MU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly preview a suggestion before selecting it in link suggestions & quick switcher.'';
            homepage = "https://github.com/RyotaUshio/obsidian-quick-preview";
        };
    }
);
    "quick-share-note-to-gist" = (
    let
        baseUrl = "https://github.com/chaintng/quick-share-note-to-gist/releases/download/1.0.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-share-note-to-gist";
        version = "1.0.18";
        outputHash = "sha256-vQpvjr6i3fcKegZaB+WxgnOiJk9kzOCC+KzDQdYJMKc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quick publish and share notes to GitHub Gist and its image by upload images to Imgur.'';
            homepage = "https://github.com/chaintng/quick-share-note-to-gist";
        };
    }
);
    "quick-snippets-and-navigation" = (
    let
        baseUrl = "https://github.com/ieviev/obsidian-keyboard-shortcuts/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-snippets-and-navigation";
        version = "1.0.13";
        outputHash = "sha256-XgcYq7rlY+++nORbXhWuiMwzcTV/VLFFr472/UB6ddM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keyboard navigation up/down for headings
- Configurable default code block and callout
- Copy code block via keyboard shortcut.'';
            homepage = "https://github.com/ieviev/obsidian-keyboard-shortcuts";
        };
    }
);
    "quick-tagger" = (
    let
        baseUrl = "https://github.com/Gorkycreator/obsidian-quick-tagger/releases/download/2.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quick-tagger";
        version = "2.2.4";
        outputHash = "sha256-HReZ5g4mD2TLsMFC0whWfqMIS97axQI4bs9/qr8Qhwk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add and remove tags quickly. Tag search results, bulk tag, and add dedicated buttons/commands for your favorites!'';
            homepage = "https://github.com/Gorkycreator/obsidian-quick-tagger";
        };
    }
);
    "quickadd" = (
    let
        baseUrl = "https://github.com/chhoumann/quickadd/releases/download/2.12.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quickadd";
        version = "2.12.0";
        outputHash = "sha256-IEb/xQG5DTW3EHEYK4jZlCa0bXQ0dTdYtdHX/ofxydQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly add new notes or content to your vault.'';
            homepage = "https://github.com/chhoumann/quickadd";
        };
    }
);
    "quicklink" = (
    let
        baseUrl = "https://github.com/Jamailar/QuickLink-Obsidian/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quicklink";
        version = "1.2.1";
        outputHash = "sha256-TMk/HUxUIwmz4ZQ6I1h12P/7NmbFuCDAxiJQpaQr5FE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly create links to files using @ trigger character'';
            homepage = "https://github.com/Jamailar/QuickLink-Obsidian";
        };
    }
);
    "quickly" = (
    let
        baseUrl = "https://github.com/tmfelwu/obsidian-inbox/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quickly";
        version = "0.1.3";
        outputHash = "sha256-KVjz7ktw99t5h0V3mUTuyK16Kz0VHtv2zJ8gEbRF0Ys=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly navigate and create notes through OS shortcut keys.'';
            homepage = "https://github.com/tmfelwu/obsidian-inbox";
        };
    }
);
    "quicknote" = (
    let
        baseUrl = "https://github.com/jamesgreenblue/obsidian-quicknote/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quicknote";
        version = "1.0.1";
        outputHash = "sha256-c25P6etbe5dnczjY2eAdpm8e+M5WRLNMoFKxzq1mBX0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a quick note in a floating window (on command or by right-clicking the Obsidian app icon).'';
            homepage = "https://github.com/jamesgreenblue/obsidian-quicknote";
        };
    }
);
    "quip" = (
    let
        baseUrl = "https://github.com/sblakey/obsidian-quip/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quip";
        version = "1.1.0";
        outputHash = "sha256-nAD+9vFVJ/Sb4xPZSqXpWuPVVyPV5Q1URnW+9Oswf8o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Commands to publish notes to Quip.com.'';
            homepage = "https://github.com/sblakey/obsidian-quip";
        };
    }
);
    "quiz-generator" = (
    let
        baseUrl = "https://github.com/ECuiDev/obsidian-quiz-generator/releases/download/2.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quiz-generator";
        version = "2.1.2";
        outputHash = "sha256-WGIS3z89I4bxdF6/5ddgjyyohpcsJ3/2bstglg77i10=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate interactive flashcards from your notes using models from OpenAI (ChatGPT), Google (Gemini), Ollama (local LLMs), and more. Or manually create your own to use with the quiz UI.'';
            homepage = "https://github.com/ECuiDev/obsidian-quiz-generator";
        };
    }
);
    "quote-of-the-day" = (
    let
        baseUrl = "https://github.com/twentytwokhz/quote-of-the-day/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quote-of-the-day";
        version = "1.1.2";
        outputHash = "sha256-bmElIT32dG895ycwAkKUgbewN/zcasZFTnKuCwAazlQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert random quotes in the editor.'';
            homepage = "https://github.com/twentytwokhz/quote-of-the-day";
        };
    }
);
    "quote-share" = (
    let
        baseUrl = "https://github.com/nguyenvanduocit/quote-share/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quote-share";
        version = "1.0.6";
        outputHash = "sha256-5Bdz97Ps2gtvM/g/qH7oThuDIukQetXKxPEpJFki+wM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily generate beautiful gradient images from text and share them on social media.'';
            homepage = "https://github.com/nguyenvanduocit/quote-share";
        };
    }
);
    "quoth" = (
    let
        baseUrl = "https://github.com/erykwalder/quoth/releases/download/0.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quoth";
        version = "0.7.1";
        outputHash = "sha256-6YF0A9KY2M0IZtJEWU4cUq2C/Ie7hsGLYYwBGNA8nlE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''More flexible embedding. Embed precise selections, inline embeds, optionally include author and title.'';
            homepage = "https://github.com/erykwalder/quoth";
        };
    }
);
    "quran-helper" = (
    let
        baseUrl = "https://github.com/AmmarCodes/obsidian-quran-helper-plugin/releases/download/1.13.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "quran-helper";
        version = "1.13.0";
        outputHash = "sha256-wTfadEygfKTPxbAEcAKWPjv2t+Q3OVulk+lUBTW2DMk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find and insert any Quran Ayah (verse) in your notes.'';
            homepage = "https://github.com/AmmarCodes/obsidian-quran-helper-plugin";
        };
    }
);
    "radial-timeline" = (
    let
        baseUrl = "https://github.com/EricRhysTaylor/Radial-Timeline/releases/download/6.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "radial-timeline";
        version = "6.0.1";
        outputHash = "sha256-ht1WdBdXf7hmhpJ6pLTYuSte0TJPIWErHSJxRUOEWr8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A radial timeline for creative fiction writing projects that displays scenes organized by act, subplot, and numeric order for a complete view of the project.'';
            homepage = "https://github.com/EricRhysTaylor/Radial-Timeline";
        };
    }
);
    "rainbow-colored-sidebar" = (
    let
        baseUrl = "https://github.com/Kovah/obsidian-rainbow-colored-sidebar/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rainbow-colored-sidebar";
        version = "1.3.0";
        outputHash = "sha256-xD1zno7A9p644hq4UttvfwA3iEi3p/e66Q4GXKbLRXk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically color your sidebar like a rainbow. No configuration needed.'';
            homepage = "https://github.com/Kovah/obsidian-rainbow-colored-sidebar";
        };
    }
);
    "rainbow-folders-fixer" = (
    let
        baseUrl = "https://github.com/dee158/obsidian-rainbow-folders-fixer/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rainbow-folders-fixer";
        version = "1.0.0";
        outputHash = "sha256-73UtSEhQK36MrYU6IputBMTUDZUexUXqg5++7jmnyws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Stop rainbow folders from changing colors when you scroll through the File explorer.'';
            homepage = "https://github.com/dee158/obsidian-rainbow-folders-fixer";
        };
    }
);
    "random-broken-link" = (
    let
        baseUrl = "https://github.com/janTatesa/obsidian-open-random-broken-link/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "random-broken-link";
        version = "2.0.2";
        outputHash = "sha256-LPbmF2L2xpgUsMAdLvWOuwrQ47uSvQkAwj6ibJFRGqc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Expand your knowledge by visiting random broken links.'';
            homepage = "https://github.com/janTatesa/obsidian-open-random-broken-link";
        };
    }
);
    "random-names" = (
    let
        baseUrl = "https://github.com/palfrey/obsidian-random-names/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "random-names";
        version = "0.1.0";
        outputHash = "sha256-bHbjJtecZ2iAP+EmiN7uL6QDkw4jGNuvCY/DvnDpDoU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates random names.'';
            homepage = "https://github.com/palfrey/obsidian-random-names";
        };
    }
);
    "random-retrieval" = (
    let
        baseUrl = "https://github.com/JeanJean-rxl/random-retrieval-plugin/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "random-retrieval";
        version = "1.0.7";
        outputHash = "sha256-MwjmwBvxvBZCaPx2dS9bIe8WdPu25TVWh/NHXCcvOu8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Random Note Retrieval based on LLM.'';
            homepage = "https://github.com/JeanJean-rxl/random-retrieval-plugin";
        };
    }
);
    "random-structural-diary-plugin" = (
    let
        baseUrl = "https://github.com/ShockThunder/RandomStructuralDiary/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "random-structural-diary-plugin";
        version = "1.1.2";
        outputHash = "sha256-klxzwVkgfVkgyRl0v4Cqk04rMb9ahVo8t1Vttto40qw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pick random questions from a prepared question list and answer different questions each time.'';
            homepage = "https://github.com/ShockThunder/RandomStructuralDiary";
        };
    }
);
    "random-wikipedia-article" = (
    let
        baseUrl = "https://github.com/SpencerF718/obsidian-random-wikipedia/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "random-wikipedia-article";
        version = "1.0.3";
        outputHash = "sha256-+oERsdjqGiNsv1xGJf+LPyUflXf4m2MjZAzSNARq2Vo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generates a note for a random Wikipedia article.'';
            homepage = "https://github.com/SpencerF718/obsidian-random-wikipedia";
        };
    }
);
    "rapid-ai" = (
    let
        baseUrl = "https://github.com/ahmed3developer/rapid-ai/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rapid-ai";
        version = "1.0.4";
        outputHash = "sha256-lO8jB0D7ZPPUbZG+m7yusUXr1JNMPi6lk3psjhIffv0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An AI assistant for selected text, along with generating content with Markdown. Shortcuts and quick action buttons provide instant AI assistance.'';
            homepage = "https://github.com/ahmed3developer/rapid-ai";
        };
    }
);
    "reactive-notes" = (
    let
        baseUrl = "https://github.com/Prodigist/ReactiveNotes/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reactive-notes";
        version = "1.0.5";
        outputHash = "sha256-hgbKKVMNgVYLNj0lQQVlYXqvedY4B9g34gLROyz5l0k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform your vault into a reactive computational environment. Create dynamic React components directly in your notes.'';
            homepage = "https://github.com/Prodigist/ReactiveNotes";
        };
    }
);
    "readability-score" = (
    let
        baseUrl = "https://github.com/zuchka/obsidian-readability/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "readability-score";
        version = "0.1.5";
        outputHash = "sha256-nR72OZWI121bOZBmV2Avl8YfrLfYd+isWDxKBpudEYQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Score the readabilty of your writing using the Flesch Reading Ease (FRE) formula.'';
            homepage = "https://github.com/zuchka/obsidian-readability";
        };
    }
);
    "readavocado-sync" = (
    let
        baseUrl = "https://github.com/innneang/obsidian-readavocado-sync/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "readavocado-sync";
        version = "1.0.0";
        outputHash = "sha256-poa09+VE8uMkzXsBnao7dv4Yi0z/oQ0hoEF4OPTYQjY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Readavocado highlights.'';
            homepage = "https://github.com/innneang/obsidian-readavocado-sync";
        };
    }
);
    "readeck-importer" = (
    let
        baseUrl = "https://github.com/makebit/obsidian-readeck-importer/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "readeck-importer";
        version = "3.1.0";
        outputHash = "sha256-K219ecxOb45wQSTMKJSzYShGhuJmsbNsMTdJ+QFFxyI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import bookmarks from Readeck.'';
            homepage = "https://github.com/makebit/obsidian-readeck-importer";
        };
    }
);
    "reader-mode" = (
    let
        baseUrl = "https://github.com/dominikmayer/obsidian-reader-mode/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reader-mode";
        version = "1.0.4";
        outputHash = "sha256-9jcR/81moM6UaB1VGr1hWj2UmJZKXAAB4asp3mUyfNw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Ensures notes are always opened in reader mode.'';
            homepage = "https://github.com/dominikmayer/obsidian-reader-mode";
        };
    }
);
    "reading-comments" = (
    let
        baseUrl = "https://github.com/BumbrT/obsidian-reading-comments/releases/download/1.0.29";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reading-comments";
        version = "1.0.29";
        outputHash = "sha256-O4KWLDU4lfXLFJMAzjU2fHQWAwQRd8BkSWAuKcmtzz0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create inline comments while you read books or articles. Comments could be grouped hierarchically by tags.'';
            homepage = "https://github.com/BumbrT/obsidian-reading-comments";
        };
    }
);
    "reading-progress-desktop" = (
    let
        baseUrl = "https://github.com/qian-shen/obsidian-reading-progress-desktop/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reading-progress-desktop";
        version = "1.0.1";
        outputHash = "sha256-QHPegSmz7xPQmkCO6FEYmGw8FIqzJMfXWpt/OKcFrCs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A progress bar for some views in the status bar(desktopAPP).'';
            homepage = "https://github.com/qian-shen/obsidian-reading-progress-desktop";
        };
    }
);
    "reading-view-enhancer" = (
    let
        baseUrl = "https://github.com/Galacsh/obsidian-reading-view-enhancer/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reading-view-enhancer";
        version = "0.2.1";
        outputHash = "sha256-J3hGwrQ7z/+FqfMPnXaWZLMEpJP69so8OsUY38c/eCk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use arrow keys to navigate between text blocks and fold/unfold elements.'';
            homepage = "https://github.com/Galacsh/obsidian-reading-view-enhancer";
        };
    }
);
    "readwise-mirror" = (
    let
        baseUrl = "https://github.com/jsonMartin/readwise-mirror/releases/download/2.4.0-beta.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "readwise-mirror";
        version = "2.4.0-beta.6";
        outputHash = "sha256-flTTcqE8hq2L4kLxG9GuclLWoIeZMzhek/q/VyYkiwg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mirror your Readwise library.'';
            homepage = "https://github.com/jsonMartin/readwise-mirror";
        };
    }
);
    "readwise-official" = (
    let
        baseUrl = "https://github.com/readwiseio/obsidian-readwise/releases/download/3.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "readwise-official";
        version = "3.0.2";
        outputHash = "sha256-fb3wmq3q/Qc+VOR80Iw7M+ucK3eyUA328g/FwVzUucw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official Readwise integration.'';
            homepage = "https://github.com/readwiseio/obsidian-readwise";
        };
    }
);
    "reason" = (
    let
        baseUrl = "https://github.com/jshph/obsidian-enzyme/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reason";
        version = "0.4.0";
        outputHash = "sha256-dLfeyL/dLouo8CYT+DMRfjzX7VX12O47dMzK2KyKbK4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A REPL to digest your thoughts'';
            homepage = "https://github.com/jshph/obsidian-enzyme";
        };
    }
);
    "recent-files-obsidian" = (
    let
        baseUrl = "https://github.com/tgrosinger/recent-files-obsidian/releases/download/1.7.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recent-files-obsidian";
        version = "1.7.7";
        outputHash = "sha256-LKBfEzsv/MPn5xbjFr67Bv7z783Mu4UXapiOkmnKdRM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display a list of recently opened files.'';
            homepage = "https://github.com/tgrosinger/recent-files-obsidian";
        };
    }
);
    "recent-notes" = (
    let
        baseUrl = "https://github.com/kamil-rudnicki/obsidian-recent-notes/releases/download/1.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recent-notes";
        version = "1.5.5";
        outputHash = "sha256-t0kqBxafZPA3b/K/se8GAkocvFxdusUAihKlZR/qtLE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''List of recently edited notes with previews by time periods.'';
            homepage = "https://github.com/kamil-rudnicki/obsidian-recent-notes";
        };
    }
);
    "recent-tab-switcher" = (
    let
        baseUrl = "https://github.com/samuelrawrs/recent-tab-switcher/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recent-tab-switcher";
        version = "1.0.1";
        outputHash = "sha256-D6cLXln0EZY6AZbulngOVCI1xbPrWtDPvKZt+S/73Wk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Switch to the most recently used tab.'';
            homepage = "https://github.com/samuelrawrs/recent-tab-switcher";
        };
    }
);
    "recently-added-files" = (
    let
        baseUrl = "https://github.com/Lemon695/obsidian-recently-added-files/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recently-added-files";
        version = "1.1.2";
        outputHash = "sha256-+nRxZoYRRgJRJYIehr/pOsSBiHIhBBca1MPMQaBKnUU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''List files by last added, including pictures, MP4s, PDFs etc.'';
            homepage = "https://github.com/Lemon695/obsidian-recently-added-files";
        };
    }
);
    "recipe-grabber" = (
    let
        baseUrl = "https://github.com/seethroughdev/obsidian-recipe-grabber/releases/download/0.21.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recipe-grabber";
        version = "0.21.0";
        outputHash = "sha256-MOpwQvPp4tuzirsgB+tzYY/5zuC3tZGRmmEAgSwfmM0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly grab the important contents of any online recipe.'';
            homepage = "https://github.com/seethroughdev/obsidian-recipe-grabber";
        };
    }
);
    "recipe-view" = (
    let
        baseUrl = "https://github.com/lachsh/obsidian-recipe-view/releases/download/0.3.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recipe-view";
        version = "0.3.6";
        outputHash = "sha256-X30SPIcMRBM8ve8eoZCf1Ay535wc+n3YF8tYmriyIJw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View your notes as interactive recipe cards while you cook.'';
            homepage = "https://github.com/lachsh/obsidian-recipe-view";
        };
    }
);
    "reclipped-official" = (
    let
        baseUrl = "https://github.com/tech-reclipped/ReClipped-Obsidian-Official/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reclipped-official";
        version = "1.0.4";
        outputHash = "sha256-OLu5Mp78HCoIsFyPLUWn6WkrRogAwThbdB6SP/b6DWI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official ReClipped integration.'';
            homepage = "https://github.com/tech-reclipped/ReClipped-Obsidian-Official";
        };
    }
);
    "recursive-copy" = (
    let
        baseUrl = "https://github.com/StructByLightning/obsidian-recursive-copy/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "recursive-copy";
        version = "1.0.0";
        outputHash = "sha256-pQL7tar2+s/dl4FwbsbqG2OLj85Uu9CEP2sPDoaMeI0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Recursively copies all Markdown files in a folder, concatenates them, and copies them into the clipboard.'';
            homepage = "https://github.com/StructByLightning/obsidian-recursive-copy";
        };
    }
);
    "red-pen" = (
    let
        baseUrl = "https://github.com/lucasmelin/red-pen/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "red-pen";
        version = "1.0.1";
        outputHash = "sha256-qSov/9gNeyTu3ZG9j2xJN4WItAXgr5qyZYc1s2tlWFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Red Pen acts as a proofreader for your writing.'';
            homepage = "https://github.com/lucasmelin/red-pen";
        };
    }
);
    "reference-map" = (
    let
        baseUrl = "https://github.com/anoopkcn/obsidian-reference-map/releases/download/1.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reference-map";
        version = "1.8.1";
        outputHash = "sha256-Fx00q/UDOQx0mxj/9E//CwHw6bDpnInKWoWYH7yUFLo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reference and citation map for literature review and discovery.'';
            homepage = "https://github.com/anoopkcn/obsidian-reference-map";
        };
    }
);
    "reflection" = (
    let
        baseUrl = "https://github.com/brandonkboswell/reflection/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reflection";
        version = "1.0.3";
        outputHash = "sha256-oj0+CNB7eCPnVeoCGF96ApKvoTMr6CuXxJpVOptDUII=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show daily and weekly notes from this day in years past.'';
            homepage = "https://github.com/brandonkboswell/reflection";
        };
    }
);
    "refresh-preview" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-refresh-any-view/releases/download/3.5.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "refresh-preview";
        version = "3.5.19";
        outputHash = "sha256-oQyOTQ9pju5OMcD6MhhWqH8xxQLba47vtUFaT9JIH14=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows to refresh any view without reopening it.'';
            homepage = "https://github.com/mnaoumov/obsidian-refresh-any-view";
        };
    }
);
    "regex-line-filter" = (
    let
        baseUrl = "https://github.com/64MM4-KN1F3/regex-line-filter/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "regex-line-filter";
        version = "0.1.3";
        outputHash = "sha256-K8pt4MWcTZkGRrLVEVuppMqWRuBmCzJQiKhyKCMZ9S0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Filter a note with regex so that only lines that match are displayed.'';
            homepage = "https://github.com/64MM4-KN1F3/regex-line-filter";
        };
    }
);
    "regex-mark" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-regex-mark/releases/download/1.11.1-4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "regex-mark";
        version = "1.11.1-4";
        outputHash = "sha256-Cn4Sttt4aNifvDCQQPH3d95UYohO+1VJhgykJ8zZZaU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add custom CSS classes to text based on regular expressions.'';
            homepage = "https://github.com/Mara-Li/obsidian-regex-mark";
        };
    }
);
    "related-notes" = (
    let
        baseUrl = "https://github.com/mrboxtobox/obsidian-related-notes/releases/download/4.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "related-notes";
        version = "4.1.1";
        outputHash = "sha256-EXxQkzCXIUv8cWrSc+Skc5c+JjC5CwsqYHwLjE5qdkQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Discover related notes in your vault.'';
            homepage = "https://github.com/mrboxtobox/obsidian-related-notes";
        };
    }
);
    "related-notes-by-tag" = (
    let
        baseUrl = "https://github.com/chrishoward-projects/related-notes-by-tag/releases/download/0.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "related-notes-by-tag";
        version = "0.4.4";
        outputHash = "sha256-SDJ5IHZCN1tTcBywX+V1P8iHuzkJX4FmOHGi+7Fqfyw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays list of notes in the sidebar that share tags with the currently active note.'';
            homepage = "https://github.com/chrishoward-projects/related-notes-by-tag";
        };
    }
);
    "relative-timestamps" = (
    let
        baseUrl = "https://github.com/agctute/relative-timestamps/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "relative-timestamps";
        version = "1.0.2";
        outputHash = "sha256-sxGbIBRqR4FBDWPN82byC5UDiFRxPOerbLfQBNTHOVs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track the time between log entries.'';
            homepage = "https://github.com/agctute/relative-timestamps";
        };
    }
);
    "relax" = (
    let
        baseUrl = "https://github.com/Syr0/R.E.L.A.X./releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "relax";
        version = "1.6.1";
        outputHash = "sha256-E/yofinP1YOjA+cdE9Q9+cVDQV19ebNgVVsqgOdYSqg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Multi-regex management for data linking and batch processing across selection, files, and folders.'';
            homepage = "https://github.com/Syr0/R.E.L.A.X.";
        };
    }
);
    "remember-cursor-position" = (
    let
        baseUrl = "https://github.com/dy-sh/obsidian-remember-cursor-position/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remember-cursor-position";
        version = "1.0.11";
        outputHash = "sha256-Ood1EMzTYjezf6rNKYEjT/5A6iqnkqDGxEg/Zppv5yo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remember cursor and scroll position for each note.'';
            homepage = "https://github.com/dy-sh/obsidian-remember-cursor-position";
        };
    }
);
    "remember-scrollposition" = (
    let
        baseUrl = "https://github.com/s-blu/obsidian-remember-scrollposition/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remember-scrollposition";
        version = "1.0.5";
        outputHash = "sha256-VJHrsgLqSfawhTmHhPq/vBwr0E3lKtT9gu37M5tTECw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remembers the scroll position in your notes and returns you to your last position upon opening a note.'';
            homepage = "https://github.com/s-blu/obsidian-remember-scrollposition";
        };
    }
);
    "remote-fetch" = (
    let
        baseUrl = "https://github.com/Shaharyar-developer/remote-fetch/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remote-fetch";
        version = "1.0.1";
        outputHash = "sha256-nwGhIQTMG5RBuw3IbzlGGycr2l529H56wdcApJhdHyE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Download files from URLs directly into your vault with CORS proxy support.'';
            homepage = "https://github.com/Shaharyar-developer/remote-fetch";
        };
    }
);
    "remotely-save" = (
    let
        baseUrl = "https://github.com/remotely-save/remotely-save/releases/download/0.5.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remotely-save";
        version = "0.5.25";
        outputHash = "sha256-nMbh0e2HyIvOqToIE8hlJdBoSylmANlZOI/g/pG90sw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes between local and cloud with smart conflict: S3 (Amazon S3/Cloudflare R2/Backblaze B2/...), Dropbox, webdav (NextCloud/InfiniCLOUD/Synology/...), OneDrive, Google Drive (GDrive), Box, pCloud, Yandex Disk, Koofr, Azure Blob Storage.'';
            homepage = "https://github.com/remotely-save/remotely-save";
        };
    }
);
    "remotely-secure" = (
    let
        baseUrl = "https://github.com/sboesen/remotely-sync/releases/download/0.4.49";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remotely-secure";
        version = "0.4.49";
        outputHash = "sha256-itQdkfFXjajfwpP3aeQQlvAkJh6uk8cpoWbFNO/zuxI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Security fixes for the remotely-save unofficial plugin allowing users to synchronize notes between local device and the cloud service. Not backwards compatible.'';
            homepage = "https://github.com/sboesen/remotely-sync";
        };
    }
);
    "remove-empty-folders" = (
    let
        baseUrl = "https://github.com/fnya/remove-empty-folders/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remove-empty-folders";
        version = "0.0.3";
        outputHash = "sha256-Pwo0ToUJeqzPyEuaKp7mkNR18cgbQ/lu9LD2ykzJv0g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily remove empty folders.'';
            homepage = "https://github.com/fnya/remove-empty-folders";
        };
    }
);
    "remove-html-tag" = (
    let
        baseUrl = "https://github.com/gitcpy/obsidian-remove-html-tag/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remove-html-tag";
        version = "1.0.4";
        outputHash = "sha256-LXl8zdP9OMMQz/g7jA4iNe28ncX5q81wWU62d6lCKfk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remove HTML tags from Markdown files'';
            homepage = "https://github.com/gitcpy/obsidian-remove-html-tag";
        };
    }
);
    "remove-newlines" = (
    let
        baseUrl = "https://github.com/HandcartCactus/obsidian-remove-newlines/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remove-newlines";
        version = "1.0.7";
        outputHash = "sha256-qHjcHE+EzpJOzpO7y7TvCq1+Erf4uIjhUxhdyZGwt9I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remove line breaks or blank lines from text selected in the editor, or as you paste from the clipboard.'';
            homepage = "https://github.com/HandcartCactus/obsidian-remove-newlines";
        };
    }
);
    "remove-unused-block-ids" = (
    let
        baseUrl = "https://github.com/isdmg/obsidian-remove-unused-block-ids/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "remove-unused-block-ids";
        version = "1.0.9";
        outputHash = "sha256-1x4DL/ylLh4/JgTNIIZAZkmTHIGq7WNtjOEoRM3hQeU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Remove unused block ids in your vault.'';
            homepage = "https://github.com/isdmg/obsidian-remove-unused-block-ids";
        };
    }
);
    "rename-file-to-selection" = (
    let
        baseUrl = "https://github.com/AavaGames/obsidian-rename-file-to-selection/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rename-file-to-selection";
        version = "1.0.1";
        outputHash = "sha256-FWz8dq4YDlx/x3X152xZBhSglj8mmbkBYUyuGq+lDK8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename your file to the current text selection.'';
            homepage = "https://github.com/AavaGames/obsidian-rename-file-to-selection";
        };
    }
);
    "rendered-block-link-suggestions" = (
    let
        baseUrl = "https://github.com/RyotaUshio/obsidian-rendered-block-link-suggestions/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rendered-block-link-suggestions";
        version = "0.5.0";
        outputHash = "sha256-b8kkilCetHMUsi8Jcvo/envmKnAsi9OONpWZjW7mrXc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Upgrade Obsidian"s built-in link suggestions with block Markdown rendering.'';
            homepage = "https://github.com/RyotaUshio/obsidian-rendered-block-link-suggestions";
        };
    }
);
    "repeat-plugin" = (
    let
        baseUrl = "https://github.com/prncc/obsidian-repeat-plugin/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "repeat-plugin";
        version = "2.0.0";
        outputHash = "sha256-GIwhP1ED5nUPoZZn2QqUGXJm2ka3E8lgfCuZ2pYeVQw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Review notes using periodic or spaced repetition.'';
            homepage = "https://github.com/prncc/obsidian-repeat-plugin";
        };
    }
);
    "replace-all" = (
    let
        baseUrl = "https://github.com/patrickchiang/obsidian-replace-all/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "replace-all";
        version = "1.0.5";
        outputHash = "sha256-omQBQFMZ+VY7dwt7x1Ef1ZI8MD0ZntdadgSl8NU0Bkg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds replace all functionality into search.'';
            homepage = "https://github.com/patrickchiang/obsidian-replace-all";
        };
    }
);
    "replace-pencil" = (
    let
        baseUrl = "https://github.com/penyt/replace-pencil/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "replace-pencil";
        version = "1.0.5";
        outputHash = "sha256-Roqsqw5ZItuIVkAr/Yj4cMzqZMJE0uwQwTdIpJp0zJE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily replace the custom variable in the code block.'';
            homepage = "https://github.com/penyt/replace-pencil";
        };
    }
);
    "replicate" = (
    let
        baseUrl = "https://github.com/dsebastien/obsidian-replicate/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "replicate";
        version = "1.0.3";
        outputHash = "sha256-MTD3R4wTbxSzgtCTR0DT80klxV8YOLYZ4qKr3nlzkWs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Replicate.com integration. Generate images using AI.'';
            homepage = "https://github.com/dsebastien/obsidian-replicate";
        };
    }
);
    "rescuetime" = (
    let
        baseUrl = "https://github.com/Tatz884/RescueTime-Obsidian/releases/download/0.2.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rescuetime";
        version = "0.2.8";
        outputHash = "sha256-xcQ/kbWvyeoB4wObysdL2VGxS2TOugrR0PW1IyYSKMg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View your RescueTime data.'';
            homepage = "https://github.com/Tatz884/RescueTime-Obsidian";
        };
    }
);
    "research-quest" = (
    let
        baseUrl = "https://github.com/narthur/research-quest/releases/download/1.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "research-quest";
        version = "1.1.9";
        outputHash = "sha256-TWUNowym3dDP6GZjE0ZIPyyABRgEhNRRvgyDO+EE47k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use AI to generate and track research questions based on your notes.'';
            homepage = "https://github.com/narthur/research-quest";
        };
    }
);
    "restore-tab-key" = (
    let
        baseUrl = "https://github.com/jrymk/restore-tab-key/releases/download/1.18.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "restore-tab-key";
        version = "1.18.6";
        outputHash = "sha256-i48uRebSnlGnJALIaPxkaQuYR07Pf2u2gIjXZBsxnmE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Restore tab key behavior: tab key inserts a tab, the way it should be.'';
            homepage = "https://github.com/jrymk/restore-tab-key";
        };
    }
);
    "reveal-active-file-button" = (
    let
        baseUrl = "https://github.com/claremacrae/reveal-active-file-button-plugin/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reveal-active-file-button";
        version = "2.0.3";
        outputHash = "sha256-zCIbzNprd+xBOwIpuVssET+X8hrI418OBjErOmLs/k4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a button to the top of the File Explorer, to reveal the active file.'';
            homepage = "https://github.com/claremacrae/reveal-active-file-button-plugin";
        };
    }
);
    "reveal-folded" = (
    let
        baseUrl = "https://github.com/d7sd6u/obsidian-reveal-folded/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reveal-folded";
        version = "1.1.2";
        outputHash = "sha256-Ft/t+DmmJomHap0Pv+ZZK1qkO1LQmKgcc30pvGhsrgk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Reveal the current file in the file explorer while collapsing all other tree items.'';
            homepage = "https://github.com/d7sd6u/obsidian-reveal-folded";
        };
    }
);
    "reverse-prompter" = (
    let
        baseUrl = "https://github.com/ryanhalliday/obsidian-reverse-prompter/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "reverse-prompter";
        version = "1.0.10";
        outputHash = "sha256-6EL9xPOtplT+JCm1D0LUUrsS2vagOv13AKdZGSXMg3Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate prompts to keep you writing with AI.'';
            homepage = "https://github.com/ryanhalliday/obsidian-reverse-prompter";
        };
    }
);
    "review-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/review-obsidian/releases/download/1.6.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "review-obsidian";
        version = "1.6.6";
        outputHash = "sha256-f58E3KtRuEybtXVNwwMygfPeft1dp/ESu/zflRNdvGc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a link to the current note to a daily note on a future date (or a past date, you time traveller).'';
            homepage = "https://github.com/ryanjamurphy/review-obsidian";
        };
    }
);
    "revisionist" = (
    let
        baseUrl = "https://github.com/ProfSynapse/obsidian-revisionist/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "revisionist";
        version = "0.3.0";
        outputHash = "sha256-12eYu1npYaoxlI6kNlWB9o1JYPJa1rZPGyGaFF1EdfA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI-powered text revision for your notes.'';
            homepage = "https://github.com/ProfSynapse/obsidian-revisionist";
        };
    }
);
    "ribbon-divider" = (
    let
        baseUrl = "https://github.com/andrewmcgivery/obsidian-ribbon-divider/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ribbon-divider";
        version = "1.1.1";
        outputHash = "sha256-5gvYSXa+G4hUt3371+10In+Fs3sdrKE156et9hXc4Bg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add dividers to the ribbon to space out your icons.'';
            homepage = "https://github.com/andrewmcgivery/obsidian-ribbon-divider";
        };
    }
);
    "rich-foot" = (
    let
        baseUrl = "https://github.com/jparkerweb/rich-foot/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rich-foot";
        version = "2.0.0";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds backlink tags and created/modified dates to the footer of your notes.'';
            homepage = "https://github.com/jparkerweb/rich-foot";
        };
    }
);
    "rich-text-editor-shortcuts" = (
    let
        baseUrl = "https://github.com/joshuawootonn/obsidian-rich-text-editor-shortcuts/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rich-text-editor-shortcuts";
        version = "1.0.6";
        outputHash = "sha256-LDZJ5uFsvKbHLeB0IcI7EI63vI+pcbAi565uO3bqwr0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and toggle checkboxes, paste links wrapping your current selection, and toggle underline without leaving the keyboard.'';
            homepage = "https://github.com/joshuawootonn/obsidian-rich-text-editor-shortcuts";
        };
    }
);
    "ridian" = (
    let
        baseUrl = "https://github.com/MichelNivard/Ridian/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ridian";
        version = "0.1.2";
        outputHash = "sha256-MSdmLTAaZ/XNdtt6f5yQxRDzwL4tsONsnbS2RQxzbzw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute R code blocks and display outputs and plots & render documents with Quarto.'';
            homepage = "https://github.com/MichelNivard/Ridian";
        };
    }
);
    "ring-a-secretary" = (
    let
        baseUrl = "https://github.com/vrtmrz/ring-a-secretary/releases/download/0.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ring-a-secretary";
        version = "0.0.8";
        outputHash = "sha256-aQwj9125FeVjul9NncAHDLy8MBZbPGQ3h5sAU+9El3I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Yet another ChatGPT-powered digital secretary.'';
            homepage = "https://github.com/vrtmrz/ring-a-secretary";
        };
    }
);
    "rofi-helper" = (
    let
        baseUrl = "https://github.com/digitalsignalperson/obsidian-rofi-helper/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rofi-helper";
        version = "1.0.0";
        outputHash = "sha256-KqVsQUC2BMnUa7vJjGGw+ULNlaL0bzbx0yE6OeTT2hg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a leaf ID parameter to the URI protocol for switching between open tabs with Rofi. A sample Rofi script is included.'';
            homepage = "https://github.com/digitalsignalperson/obsidian-rofi-helper";
        };
    }
);
    "role-switch" = (
    let
        baseUrl = "https://github.com/zafrem/obsidian-role-switch/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "role-switch";
        version = "0.1.1";
        outputHash = "sha256-28vMHSCIGUXLY0ROYaMji0LxMFmdpbbzzRCcmFOLk5Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Switch between different work roles (developer, writer, researcher, etc.) with intentional transitions and session tracking.'';
            homepage = "https://github.com/zafrem/obsidian-role-switch";
        };
    }
);
    "rollover-weekly-todo" = (
    let
        baseUrl = "https://github.com/shsethi/obsidian-rollover-weekly-todos/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rollover-weekly-todo";
        version = "1.0.9";
        outputHash = "sha256-a5KrPFcOo2s5b1KD2mN/Vz/OqWnmJp6WgX5EYF1JWYo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rollover unchecked todo items from the previous weekly note'';
            homepage = "https://github.com/shsethi/obsidian-rollover-weekly-todos";
        };
    }
);
    "root-folder-context-menu" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-root-folder-context-menu/releases/download/1.4.35";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "root-folder-context-menu";
        version = "1.4.35";
        outputHash = "sha256-DOwJBERFmkjqjaulDFjK2d/el25FD6kgarq1fUpKw3o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables context menu for vault root folder'';
            homepage = "https://github.com/mnaoumov/obsidian-root-folder-context-menu";
        };
    }
);
    "rpg-manager" = (
    let
        baseUrl = "https://github.com/carlonicora/obsidian-rpg-manager/releases/download/4.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rpg-manager";
        version = "4.2.1";
        outputHash = "sha256-JnXZHUhGJANGYMAeNdpZ2tfx+WMIBlJTxfPMA4JvlMo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tabletop role playing game campaign manager.'';
            homepage = "https://github.com/carlonicora/obsidian-rpg-manager";
        };
    }
);
    "rpg-stat-tracker" = (
    let
        baseUrl = "https://github.com/name/obsidian-rpg/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rpg-stat-tracker";
        version = "1.0.6";
        outputHash = "sha256-62VT29uRpZgd+HcB+pk1QzgP3UMXgJfva+eej2lzx3k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''RPG-like stat tracker.'';
            homepage = "https://github.com/name/obsidian-rpg";
        };
    }
);
    "rss-copyist" = (
    let
        baseUrl = "https://github.com/aoout/obsidian-rss-copyist/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rss-copyist";
        version = "1.0.4";
        outputHash = "sha256-+f8qmahZXH/hGr2i0kjLVwPV1V/n5s3O7E3DVqiw1a0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Get RSS articles as notes.'';
            homepage = "https://github.com/aoout/obsidian-rss-copyist";
        };
    }
);
    "rss-dashboard" = (
    let
        baseUrl = "https://github.com/amatya-aditya/obsidian-rss-dashboard/releases/download/2.3.0-alpha.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rss-dashboard";
        version = "2.3.0-alpha.3";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A dashboard for organizing and consuming RSS feeds, YouTube channels, and podcasts with smart tagging, media playback, and seamless content flow.'';
            homepage = "https://github.com/amatya-aditya/obsidian-rss-dashboard";
        };
    }
);
    "rsync" = (
    let
        baseUrl = "https://github.com/GanapathyRaman/rsync-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rsync";
        version = "1.0.0";
        outputHash = "sha256-IEw7U9QyPFxP2TW5Q3n1lWB8PpY+N42KlOTY4hFoS1I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes and automate backups using Rsync.'';
            homepage = "https://github.com/GanapathyRaman/rsync-plugin";
        };
    }
);
    "rtl-math-text" = (
    let
        baseUrl = "https://github.com/orelby/obsidian-rtl-math-text-plugin/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "rtl-math-text";
        version = "0.0.2";
        outputHash = "sha256-XSXrlL6rzmkvJh+bBm4C4Wl2UTr2zNHLsjGR3KHy8OM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mix right-to-left and left-to-right text in math expressions using configurable commands.'';
            homepage = "https://github.com/orelby/obsidian-rtl-math-text-plugin";
        };
    }
);
    "ruby-wasm" = (
    let
        baseUrl = "https://github.com/geeknees/obsidian-ruby-wasm-plugin/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ruby-wasm";
        version = "0.0.1";
        outputHash = "sha256-ZSukliK6MUWMtoSXyUpoiTJRs/RfA695gGbJ3iTCTdg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run ruby code in your notes using WebAssembly.'';
            homepage = "https://github.com/geeknees/obsidian-ruby-wasm-plugin";
        };
    }
);
    "ruled-template" = (
    let
        baseUrl = "https://github.com/YPetremann/obsidian-ruled-template/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ruled-template";
        version = "1.1.0";
        outputHash = "sha256-ZYPGa9MF0hISykYUxz71zl4rDn9J1gye+l0GgHOYkkI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use templates based on rules when creating a new file.'';
            homepage = "https://github.com/YPetremann/obsidian-ruled-template";
        };
    }
);
    "run" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-run/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "run";
        version = "1.0.8";
        outputHash = "sha256-wXP2Fki+9+Lk/jRkWaPrYNur946xOp046ZK34xJ4qlQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate Markdown from dataview query and JavaScript.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-run";
        };
    }
);
    "runjs" = (
    let
        baseUrl = "https://github.com/eoureo/obsidian-runjs/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "runjs";
        version = "0.8.0";
        outputHash = "sha256-Dib6Js3lcqFvvgoi6YV3+jLAP2k8DEpFSKKZcoBEoOg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run JavaScript code.'';
            homepage = "https://github.com/eoureo/obsidian-runjs";
        };
    }
);
    "runsh" = (
    let
        baseUrl = "https://github.com/Deedone/obsidian-runsh/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "runsh";
        version = "1.0.4";
        outputHash = "sha256-UQDCJ9sjgvZeRXMcBfUg/LtZgIqsTFscOEmJP9tu5r8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create buttons that run shell commands from your notes.'';
            homepage = "https://github.com/Deedone/obsidian-runsh";
        };
    }
);
    "s3-attachments-storage" = (
    let
        baseUrl = "https://github.com/ttax00/obsidian-s3/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "s3-attachments-storage";
        version = "0.1.2";
        outputHash = "sha256-LYs99L57gO/cjRT4s+4BVRLBBugFPL042uvTJNs1dYs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Storage and retrieval of media attachments on S3 compatible services.'';
            homepage = "https://github.com/ttax00/obsidian-s3";
        };
    }
);
    "s3-image-uploader" = (
    let
        baseUrl = "https://github.com/jvsteiner/s3-image-uploader/releases/download/0.2.47";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "s3-image-uploader";
        version = "0.2.47";
        outputHash = "sha256-ha7FKvY+Gtzbi9zIMxs8jNiBbZjiL9FSrvVQxUPIOlU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Self-host images on AWS S3.'';
            homepage = "https://github.com/jvsteiner/s3-image-uploader";
        };
    }
);
    "s3agle" = (
    let
        baseUrl = "https://github.com/turnercore/s3agle/releases/download/0.5.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "s3agle";
        version = "0.5.9";
        outputHash = "sha256-ibXgsypU01/Wv9mL2cdmEpXSpm5NfohILRISB6xADqU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use S3 providers and/or Eagle to manage vault attachments locally and remotely.'';
            homepage = "https://github.com/turnercore/s3agle";
        };
    }
);
    "safe-filename-linter" = (
    let
        baseUrl = "https://github.com/sneakyfoxes/obsidian-safe-filename-linter/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "safe-filename-linter";
        version = "1.1.4";
        outputHash = "sha256-yE+THVv5uaJf7zvY4o11JV9juBFbkOBBB3C6QeFoBDk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Lint filenames for invalid or troublesome characters.'';
            homepage = "https://github.com/sneakyfoxes/obsidian-safe-filename-linter";
        };
    }
);
    "safelearn-formatter" = (
    let
        baseUrl = "https://github.com/UnterrainerInformatik/safeLearn-Obsidian-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "safelearn-formatter";
        version = "1.0.1";
        outputHash = "sha256-blFgn6w6jkGXf1cFGBtTAlhBmg58oANTYg4xnR8vDys=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Offers visual aids for tags specific for SafeLearn (an open-source project) such as fragments, permission blocks, and side-by-side layouts for Reveal.js.'';
            homepage = "https://github.com/UnterrainerInformatik/safeLearn-Obsidian-plugin";
        };
    }
);
    "samepage" = (
    let
        baseUrl = "https://github.com/samepage-network/obsidian-samepage/releases/download/2.7.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "samepage";
        version = "2.7.2";
        outputHash = "sha256-pCHC1dD3AeJL+7XeGAyoNHcWMVlXyqsCKffkiGLHVKA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Official SamePage client, the intra tool-for-thought protocol.'';
            homepage = "https://github.com/samepage-network/obsidian-samepage";
        };
    }
);
    "sankey" = (
    let
        baseUrl = "https://github.com/finnromaneessen/obsidian-sankey/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sankey";
        version = "1.1.0";
        outputHash = "sha256-dmuZU6+FkPr+FsyzbpxRhzkOLETDLMHwkPy2IdUQNqk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Sankey diagrams in your notes.'';
            homepage = "https://github.com/finnromaneessen/obsidian-sankey";
        };
    }
);
    "scales-chords" = (
    let
        baseUrl = "https://github.com/egradman/scales-chords/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scales-chords";
        version = "1.0.1";
        outputHash = "sha256-y7DiYfZp9J1DA1cdQcTzPqor0rXzmFnZWh9SLH5uEsQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Capture musical tab notation. Chords become clickable links to modal images (provided by scales-chords.com).'';
            homepage = "https://github.com/egradman/scales-chords";
        };
    }
);
    "scholar" = (
    let
        baseUrl = "https://github.com/lolipopshock/obsidian-scholar/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scholar";
        version = "1.5.0";
        outputHash = "sha256-nYS3zhacl2A6C6JlzGXiBAP5lJv6BVaG1ofsV8bdCUA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage research library and streamline research workflow.'';
            homepage = "https://github.com/lolipopshock/obsidian-scholar";
        };
    }
);
    "scrambling-title-animations" = (
    let
        baseUrl = "https://github.com/DvorakDwarf/scrambling-title-animations/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scrambling-title-animations";
        version = "1.0.0";
        outputHash = "sha256-OyEmRHrGnvoQCFvNDNJ05fqARnjyfxoaGNbnazse0nw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Animates the title of any note you open by scrambling and revealing it in several visually appealing ways.'';
            homepage = "https://github.com/DvorakDwarf/scrambling-title-animations";
        };
    }
);
    "scratchpad" = (
    let
        baseUrl = "https://github.com/kvh03/obsidian-scratchpad/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scratchpad";
        version = "0.2.2";
        outputHash = "sha256-NdxrZT1k4rrZGtCQ3oL4jxvJw8f4TEwYtgtPMo8gpFw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take temporary notes and draw freehand in a sidebar scratchpad.'';
            homepage = "https://github.com/kvh03/obsidian-scratchpad";
        };
    }
);
    "screengarden-obsidian" = (
    let
        baseUrl = "https://github.com/screendotgarden/screengarden-obsidian/releases/download/2.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "screengarden-obsidian";
        version = "2.1.2";
        outputHash = "sha256-navj9O4J6ogLZggj5gcPOBVpf/AGun0oLpp0QCo9OlQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Realtime multiplayer collaboration, live cursors without merge conflicts, folder sync, share links, web editing, and more—for teams or working solo.'';
            homepage = "https://github.com/screendotgarden/screengarden-obsidian";
        };
    }
);
    "scribe" = (
    let
        baseUrl = "https://github.com/Mikodin/obsidian-scribe/releases/download/2.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scribe";
        version = "2.3.1";
        outputHash = "sha256-AmeXedcJlNuk009ZkQzKZln3SFxQF0wu+3l2amFgl3M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Record voice notes, Fill in lost thoughts, Transcribe the audio, Summarize & Visualize the text - All in one clip'';
            homepage = "https://github.com/Mikodin/obsidian-scribe";
        };
    }
);
    "script-launcher" = (
    let
        baseUrl = "https://github.com/AlessandroRuggiero/script-launcher/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "script-launcher";
        version = "1.6.0";
        outputHash = "sha256-9j+bNffg8blShLpzmshL5jwLJGjZUWfxnGUWePcXOYE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add scripts shortcuts on your bottom bar and launch them.'';
            homepage = "https://github.com/AlessandroRuggiero/script-launcher";
        };
    }
);
    "scripture-indexer" = (
    let
        baseUrl = "https://github.com/jdrbrn/obsidian-scripture-indexer/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scripture-indexer";
        version = "1.3.0";
        outputHash = "sha256-/6Gaw4QCrdWP5mDt3IeW1ewmngOHP9ZY2/JlqvTyIfk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Indexes references to scriptures in notes.'';
            homepage = "https://github.com/jdrbrn/obsidian-scripture-indexer";
        };
    }
);
    "scroll-speed" = (
    let
        baseUrl = "https://github.com/flolu/obsidian-scroll-speed/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scroll-speed";
        version = "1.4.0";
        outputHash = "sha256-iIQ9yMg4h5/VFKVMfJPFKdmQvgDjpDirG6uDSs+YDaE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change the scroll speed.'';
            homepage = "https://github.com/flolu/obsidian-scroll-speed";
        };
    }
);
    "scroll-to-nav-top" = (
    let
        baseUrl = "https://github.com/mariomui/scroll-to-nav-top/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scroll-to-nav-top";
        version = "1.0.13";
        outputHash = "sha256-UF07mUXrmztFfmqr7DYUi6Aqj+rD+peTYLvonYjOmHE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Scrolls File Explorer to top position.'';
            homepage = "https://github.com/mariomui/scroll-to-nav-top";
        };
    }
);
    "scroll-with-jk" = (
    let
        baseUrl = "https://github.com/LukasKorotaj/Scroll-With-j-k-in-Obsidian/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scroll-with-jk";
        version = "1.1.3";
        outputHash = "sha256-wOJzxePsu4wvBayO7rBceLcD1lVfByzsq3aURSWYKcA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Scroll in reading view with j/k keys. Scroll to top with gg and to bottom with G.'';
            homepage = "https://github.com/LukasKorotaj/Scroll-With-j-k-in-Obsidian";
        };
    }
);
    "scroller" = (
    let
        baseUrl = "https://github.com/coignard/obsidian-scroller/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scroller";
        version = "1.4.3";
        outputHash = "sha256-wEEKlokgtonP+ATjMINWpWq7WctuLtoKW8ROBm2jG7U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds typewriter mode, focus mode and commands to quickly scroll to the top or bottom of note.'';
            homepage = "https://github.com/coignard/obsidian-scroller";
        };
    }
);
    "scrolling" = (
    let
        baseUrl = "https://github.com/omeyenburg/obsidian-scrolling/releases/download/1.3.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scrolling";
        version = "1.3.8";
        outputHash = "sha256-VKOhxbbE2fH+esrcOyt+DT765xKVGNCuWj8hgigE4PE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improve scrolling and keep your cursor exactly where you want it.'';
            homepage = "https://github.com/omeyenburg/obsidian-scrolling";
        };
    }
);
    "scrybble-ink" = (
    let
        baseUrl = "https://github.com/Scrybbling-together/scrybble/releases/download/3.23.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "scrybble-ink";
        version = "3.23.0";
        outputHash = "sha256-lDqdYXqDhQTkeCLWVmOnhOzyJwC2YbExGNBwoMAUkcQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize highlights from your ReMarkable tablet.'';
            homepage = "https://github.com/Scrybbling-together/scrybble";
        };
    }
);
    "seafile" = (
    let
        baseUrl = "https://github.com/conql/obsidian-seafile/releases/download/0.3.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "seafile";
        version = "0.3.6";
        outputHash = "sha256-e0Un9zkJZPEsY+8qGK13MAXZejD+1i7guwVqD2omIL4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync notes across devices using Seafile.'';
            homepage = "https://github.com/conql/obsidian-seafile";
        };
    }
);
    "search-in-canvas" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Search-In-Canvas/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "search-in-canvas";
        version = "1.1.1";
        outputHash = "sha256-ibIDaB162Rd4diGC1jrhDDnH/1tQBVlBdKycKVcCTRI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search text in canvas.'';
            homepage = "https://github.com/Quorafind/Obsidian-Search-In-Canvas";
        };
    }
);
    "search-on-internet" = (
    let
        baseUrl = "https://github.com/HEmile/obsidian-search-on-internet/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "search-on-internet";
        version = "0.5.0";
        outputHash = "sha256-CpoA2f/hhssSiXMIfhVHFoggq/hqEMV8BSPfK4RdecU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add context menu items to search the internet based on the title of your note.'';
            homepage = "https://github.com/HEmile/obsidian-search-on-internet";
        };
    }
);
    "searchpp" = (
    let
        baseUrl = "https://github.com/nhaouari/searchpp/releases/download/0.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "searchpp";
        version = "0.0.1";
        outputHash = "sha256-HaZfSxxeb8DkdBwRiwmP8U6o091Mr9x2BAxh/hBEIDo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert text context search results in the active note.'';
            homepage = "https://github.com/nhaouari/searchpp";
        };
    }
);
    "segerlab" = (
    let
        baseUrl = "https://github.com/ksemkav/obsidian-segerlab-plugin/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "segerlab";
        version = "1.0.4";
        outputHash = "sha256-eQHbjuFjdq75nVJUhQ9vPQme5j5o7loNGlLsyTM+MaY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Renders calculator views within notes from JSON data copied from the Segerlab app.'';
            homepage = "https://github.com/ksemkav/obsidian-segerlab-plugin";
        };
    }
);
    "select-and-complete" = (
    let
        baseUrl = "https://github.com/macro21KGB/select-and-complete/releases/download/3.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "select-and-complete";
        version = "3.1.3";
        outputHash = "sha256-whjzBvBdjjpjFmUXx9f52OyRAO+KHTTxgqK2hLCXC9Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Select something and let the AI complete it for you.'';
            homepage = "https://github.com/macro21KGB/select-and-complete";
        };
    }
);
    "select-current-line" = (
    let
        baseUrl = "https://github.com/gokulk16/select-current-line-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "select-current-line";
        version = "1.0.0";
        outputHash = "sha256-8208AviRgC/2/8vFiAZmXryu8QCR8yeArY88FXPEoc8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Select the current line where the cursor is placed. Press "ESC" key to select.'';
            homepage = "https://github.com/gokulk16/select-current-line-plugin";
        };
    }
);
    "select-word" = (
    let
        baseUrl = "https://github.com/ConnorEspino/ObsidianSelectWord/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "select-word";
        version = "1.0.0";
        outputHash = "sha256-DMQtZopBRi9Im76DA55mqJkCAOe1v+FBmHf3dR28k60=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Selects the word that is closest to the caret.'';
            homepage = "https://github.com/ConnorEspino/ObsidianSelectWord";
        };
    }
);
    "semalogic" = (
    let
        baseUrl = "https://github.com/MM-GO/UseSemaLogic/releases/download/2.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "semalogic";
        version = "2.3.2";
        outputHash = "sha256-ATfUlF+aTp1Am3TxmmSWFs+7U+ALb5jrtrMDwouzcLE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Real-time use of the SemaLogic formal language'';
            homepage = "https://github.com/MM-GO/UseSemaLogic";
        };
    }
);
    "semantic-canvas" = (
    let
        baseUrl = "https://github.com/aarongilly/obsidian-semantic-canvas-plugin/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "semantic-canvas";
        version = "1.2.1";
        outputHash = "sha256-BImoclRkZRWWq8GLOtuNwWMR9FZ27s9pV44+xFdJLLA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create semantic knowledge graphs using Canvases to modify note properties graphically.'';
            homepage = "https://github.com/aarongilly/obsidian-semantic-canvas-plugin";
        };
    }
);
    "send-note" = (
    let
        baseUrl = "https://github.com/jvsteiner/send-note/releases/download/0.8.39";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "send-note";
        version = "0.8.39";
        outputHash = "sha256-D3UVbRv5UV1rDjtBWr4zsSQFo0Z292vcO+H9EhR2G/A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Instantly send a note, to other users so they can import them into their vault via URL.'';
            homepage = "https://github.com/jvsteiner/send-note";
        };
    }
);
    "send-to-canvas" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-send-to-canvas/releases/download/0.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "send-to-canvas";
        version = "0.3.4";
        outputHash = "sha256-/l4nl2SssJXEF+0zEps1dK9QmQjcM8/FB7EypyjAuzM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send tasks, blocks, and notes to Canvas files as plain text, links, and embeds.'';
            homepage = "https://github.com/wenlzhang/obsidian-send-to-canvas";
        };
    }
);
    "send-to-ghost" = (
    let
        baseUrl = "https://github.com/Southpaw1496/obsidian-send-to-ghost/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "send-to-ghost";
        version = "1.0.5";
        outputHash = "sha256-KobuHD+qWLtSj7cEan4BZPImDeSNbW/bXvlhjM3oXi8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Send and publish notes to your Ghost blog.'';
            homepage = "https://github.com/Southpaw1496/obsidian-send-to-ghost";
        };
    }
);
    "sentence-rhythm" = (
    let
        baseUrl = "https://github.com/adamfletcher/obsidian-sentence-rhythm/releases/download/0.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sentence-rhythm";
        version = "0.4.1";
        outputHash = "sha256-LBMUxNkwuMT34lxJqj1ITAUQP6UHIO4rebICZ5JSGRE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds toggleable colored highlights to sentences based on their length so you can easily see the rhythm of your writing.'';
            homepage = "https://github.com/adamfletcher/obsidian-sentence-rhythm";
        };
    }
);
    "sentinel" = (
    let
        baseUrl = "https://github.com/gsarig/obsidian-sentinel/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sentinel";
        version = "1.2.1";
        outputHash = "sha256-lLSgwNfcN006tgsGPZjG0k3aIzUKQdNMu1k4ZiDig4o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Trigger actions based on document visibility changes.'';
            homepage = "https://github.com/gsarig/obsidian-sentinel";
        };
    }
);
    "serendipity" = (
    let
        baseUrl = "https://github.com/ggauravr/obsidian-serendipity-plugin/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "serendipity";
        version = "1.0.9";
        outputHash = "sha256-0BNbdPQLAcXuCe1hlmoDeAjQMTQWY08boRQ9W/yb3Yc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Forces serendipitous discoveries by displaying random notes from your vault each time you open the app'';
            homepage = "https://github.com/ggauravr/obsidian-serendipity-plugin";
        };
    }
);
    "session-notes" = (
    let
        baseUrl = "https://github.com/tabibyte/session-notes/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "session-notes";
        version = "1.2.0";
        outputHash = "sha256-dwLxpqHfzRXNG9rLX5mfTHo5arkfOW4/Plf4m3Utgoc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create temporary one-off session notes.'';
            homepage = "https://github.com/tabibyte/session-notes";
        };
    }
);
    "setlist-fm-sync" = (
    let
        baseUrl = "https://github.com/Fleker/setlist-for-obsidian/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "setlist-fm-sync";
        version = "1.0.4";
        outputHash = "sha256-qcforicokcNwWzUfgwglx9ZIQJ6ah4MFJfhwcS0g/f8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncs your setlist.fm attended concerts.'';
            homepage = "https://github.com/Fleker/setlist-for-obsidian";
        };
    }
);
    "sets" = (
    let
        baseUrl = "https://github.com/Canna71/obsidian-sets/releases/download/1.0.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sets";
        version = "1.0.14";
        outputHash = "sha256-VyjWmq9W+WXJThSmJmFtECU4/meCkQ8Tpc6xZsGipgA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create, edit and search sets of notes like Notion or AnyType DBs.'';
            homepage = "https://github.com/Canna71/obsidian-sets";
        };
    }
);
    "settings-management" = (
    let
        baseUrl = "https://github.com/xhuajin/obsidian-settings-management/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "settings-management";
        version = "1.0.7";
        outputHash = "sha256-5G4wOsofYlATMLkSh6OQ73gbPm10QDY3Wwa/BNm0vSI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage settings options, including show enabled/disabled plugins and css, grid layout, save current plugins/css enable config for quick enable/disable, etc.'';
            homepage = "https://github.com/xhuajin/obsidian-settings-management";
        };
    }
);
    "settings-profiles" = (
    let
        baseUrl = "https://github.com/4Source/settings-profiles-obsidian-plugin/releases/download/0.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "settings-profiles";
        version = "0.7.1";
        outputHash = "sha256-DaARpeRUK2Rhb0nXMv7GkhlPclhLQ6OWGYU6ugnDr0U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to create various global settings profiles. You can sync them between different vaults. To keep all your settings in sync, you"ll never have to manually adjust them again for every vault you have or create in the future.'';
            homepage = "https://github.com/4Source/settings-profiles-obsidian-plugin";
        };
    }
);
    "settings-search" = (
    let
        baseUrl = "https://github.com/javalent/settings-search/releases/download/1.3.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "settings-search";
        version = "1.3.10";
        outputHash = "sha256-KSEgw9Xn/Xk5six5Qd44pPuVOZUaZNfyIAylfRC717E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Globally search settings.'';
            homepage = "https://github.com/javalent/settings-search";
        };
    }
);
    "shaahmaat-md" = (
    let
        baseUrl = "https://github.com/MihailKovachev/shaahmaat-md/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shaahmaat-md";
        version = "1.0.0";
        outputHash = "sha256-hjHfot/aXKHZGvTONNomHsF5iTbUSdPi8dRE+DayZdo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render chess positions.'';
            homepage = "https://github.com/MihailKovachev/shaahmaat-md";
        };
    }
);
    "share-as-zip" = (
    let
        baseUrl = "https://github.com/friebetill/obsidian-share-as-zip/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "share-as-zip";
        version = "0.0.3";
        outputHash = "sha256-+/iC7gVifbiy2fKO8bTWVJrd1MnqUTNCWb6UixHsZCY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share notes and their links as a zip folder.'';
            homepage = "https://github.com/friebetill/obsidian-share-as-zip";
        };
    }
);
    "share-my-plugin-list" = (
    let
        baseUrl = "https://github.com/Benature/obsidian-share-my-plugin-list/releases/download/0.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "share-my-plugin-list";
        version = "0.3.3";
        outputHash = "sha256-dt6PzznzNSrLveW9DJ+2tJVhobKQCxtvphK0tv5MveI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share the enabled plugins in list/table format.'';
            homepage = "https://github.com/Benature/obsidian-share-my-plugin-list";
        };
    }
);
    "share-note" = (
    let
        baseUrl = "https://github.com/alangrainger/share-note/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "share-note";
        version = "1.3.1";
        outputHash = "sha256-3eNvl5TvOzrE+iTjslaIDwMrZkYmpUiv+jMvLTJ43ls=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Instantly share/publish a note, with the full theme and content exactly like you see in Obsidian. Data is shared encrypted by default, and only you and the person you send it to have the key.'';
            homepage = "https://github.com/alangrainger/share-note";
        };
    }
);
    "share-to-cubox" = (
    let
        baseUrl = "https://github.com/Redwinam/obsidian-cubox/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "share-to-cubox";
        version = "1.0.0";
        outputHash = "sha256-zLtAWWFvRMXR4zMV8i02WUfn0UPQYyokDWM+Dr664+Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share notes to Cubox.'';
            homepage = "https://github.com/Redwinam/obsidian-cubox";
        };
    }
);
    "share-to-notionnext" = (
    let
        baseUrl = "https://github.com/jxpeng98/obsidian-to-NotionNext/releases/download/2.8.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "share-to-notionnext";
        version = "2.8.4";
        outputHash = "sha256-oGia1MKiymX9Kr7iA/PBtGLq16Wm6R8MUejM2fHdhOk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share files to any Notion database using the Notion API, originally created by EasyChris/obsidian-to-notion.'';
            homepage = "https://github.com/jxpeng98/obsidian-to-NotionNext";
        };
    }
);
    "share-via-notepad-tab" = (
    let
        baseUrl = "https://github.com/revolter/obsidian-share-via-notepad-tab-plugin/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "share-via-notepad-tab";
        version = "1.1.0";
        outputHash = "sha256-Q7fvqLG4pRdOtOLeYhHcfJAqrOh3fZLe+/jTJzIQHus=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Share notes via Notepad Tab (https://notepadtab.com).'';
            homepage = "https://github.com/revolter/obsidian-share-via-notepad-tab-plugin";
        };
    }
);
    "sheet-plus" = (
    let
        baseUrl = "https://github.com/ljcoder2015/obsidian-sheet-plus/releases/download/2.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sheet-plus";
        version = "2.10.0";
        outputHash = "sha256-QZUmLbyROymkYbWH3QtkbNDI4w2PLYWtohxvX5p436Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create Excel-like spreadsheets and easily embed them in Markdown.'';
            homepage = "https://github.com/ljcoder2015/obsidian-sheet-plus";
        };
    }
);
    "sheets" = (
    let
        baseUrl = "https://github.com/NicoNekoru/obsidan-advanced-table-xt/releases/download/1.2.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sheets";
        version = "1.2.10";
        outputHash = "sha256-tkkXLAsU8SrsWWruVjXgGk+0QnU3DfPGqzzINCmPVrI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Vertical headers, merged cells, and custom CSS tables with Advanced Tables compatability.'';
            homepage = "https://github.com/NicoNekoru/obsidan-advanced-table-xt";
        };
    }
);
    "shell-path-copy" = (
    let
        baseUrl = "https://github.com/ckelsoe/obsidian-shell-path-copy/releases/download/1.17.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shell-path-copy";
        version = "1.17.1";
        outputHash = "sha256-kXzq0bmo5VCUhS9RL0bdeHyqOV/1WwXt5mI1Ls+kHiE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly copy vault file and folder paths for AI coding tools (Claude Code, Gemini CLI) - works on desktop and mobile with Windows/Unix formats'';
            homepage = "https://github.com/ckelsoe/obsidian-shell-path-copy";
        };
    }
);
    "shiki-highlighter" = (
    let
        baseUrl = "https://github.com/mProjectsCode/obsidian-shiki-plugin/releases/download/0.7.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shiki-highlighter";
        version = "0.7.7";
        outputHash = "sha256-pU1ti3KyFCnLkSi0dr3V9+eCq7EgkFRs86LBcGs3qzY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight code blocks with Shiki.'';
            homepage = "https://github.com/mProjectsCode/obsidian-shiki-plugin";
        };
    }
);
    "shogi-kif-viewer" = (
    let
        baseUrl = "https://github.com/Luis8492/ShogiView/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shogi-kif-viewer";
        version = "1.1.0";
        outputHash = "sha256-9fvwceJurSk0+Vi6bdUmkuXyTx6ce7bBzsrE3jwaz4Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render interactive shogi boards from KIF code blocks in notes.'';
            homepage = "https://github.com/Luis8492/ShogiView";
        };
    }
);
    "short-internal-links-to-headings" = (
    let
        baseUrl = "https://github.com/scottwillmoore/obsidian-short-links/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "short-internal-links-to-headings";
        version = "1.1.5";
        outputHash = "sha256-2yqSjz82nqFinLc+pgE5+G0vzsuBdNW56oPDZmckey4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display short internal links to files, notes, headings, and blocks.'';
            homepage = "https://github.com/scottwillmoore/obsidian-short-links";
        };
    }
);
    "short-tab-name" = (
    let
        baseUrl = "https://github.com/Shumpei-Tanaka/obsidian-short-tab-name/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "short-tab-name";
        version = "1.0.0";
        outputHash = "sha256-s9VFH2hhhMXYiV5VIgNtGd0H2GqlM0sB+v5Ys8rNAws=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set tab name to short for UID user.'';
            homepage = "https://github.com/Shumpei-Tanaka/obsidian-short-tab-name";
        };
    }
);
    "shortcut-edit-mode" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-edit-shortcut/releases/download/0.0.9-3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shortcut-edit-mode";
        version = "0.0.9-3";
        outputHash = "sha256-hwwGrJA0IDL4710y3J3dUQlnq7KfqsxdoToH9UWFGcE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a button in the file header, in edit mode, to switch between source and live-preview.'';
            homepage = "https://github.com/Mara-Li/obsidian-edit-shortcut";
        };
    }
);
    "shortcuts-extender" = (
    let
        baseUrl = "https://github.com/ryjjin/Obsidian-shortcuts-extender/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shortcuts-extender";
        version = "2.2.0";
        outputHash = "sha256-uQbnEVuZwZavrbKoFucO/Tl/twuF/+8xwAUohyKTJPE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use shortcuts for input special symbols without language switching.'';
            homepage = "https://github.com/ryjjin/Obsidian-shortcuts-extender";
        };
    }
);
    "show-diff" = (
    let
        baseUrl = "https://github.com/ivan-lednev/obsidian-automatic-changelog/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "show-diff";
        version = "0.1.0";
        outputHash = "sha256-2chiKORbW3zuOGK9l67bOlxByWDLLac4GWnmMGf/B5Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render Git diffs in your notes.'';
            homepage = "https://github.com/ivan-lednev/obsidian-automatic-changelog";
        };
    }
);
    "show-whitespace-cm6" = (
    let
        baseUrl = "https://github.com/ebullient/obsidian-show-whitespace-cm6/releases/download/0.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "show-whitespace-cm6";
        version = "0.2.9";
        outputHash = "sha256-hyz3b321fHkI2IDS9xrcit2N75zGTOG5uVkh9KG0VOg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''CSS styles and CM6 extensions to highlight whitespace in Source and Live Preview modes.'';
            homepage = "https://github.com/ebullient/obsidian-show-whitespace-cm6";
        };
    }
);
    "shrink-pinned-tabs" = (
    let
        baseUrl = "https://github.com/nicosomb/obsidian-shrink-pinned-tabs/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shrink-pinned-tabs";
        version = "1.0.7";
        outputHash = "sha256-7PRVTIKDTTmh8aeyNcoj23+3s+NKV73nE9n6iKQjzZk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Shrinks pinned tabs to save screen space.'';
            homepage = "https://github.com/nicosomb/obsidian-shrink-pinned-tabs";
        };
    }
);
    "shukuchi" = (
    let
        baseUrl = "https://github.com/tadashi-aikawa/shukuchi/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "shukuchi";
        version = "0.8.0";
        outputHash = "sha256-T4wF+hrx6fWDYOpBoT/0/xymXwMkqD0jW2jyu5wW0Lg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Teleport to links (URL or internal link) and jump to their destinations.'';
            homepage = "https://github.com/tadashi-aikawa/shukuchi";
        };
    }
);
    "side-note" = (
    let
        baseUrl = "https://github.com/mofukuru/SideNote/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "side-note";
        version = "1.0.5";
        outputHash = "sha256-UWDkNJppFUOLUD59UkfV6eoOYwt11WwmNfhH+PYsRxE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add comment on the part of sentence and refer in comment view.'';
            homepage = "https://github.com/mofukuru/SideNote";
        };
    }
);
    "sidebar-highlights" = (
    let
        baseUrl = "https://github.com/trevware/obsidian-sidebar-highlights/releases/download/1.36.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sidebar-highlights";
        version = "1.36.0";
        outputHash = "sha256-1cLGcVLGdfPlhgKSvT9bxGlkO+SZ0KfMErtkbCJ0bCU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View and manage text highlights, comments on highlights, native comments, and collections for your highlights.'';
            homepage = "https://github.com/trevware/obsidian-sidebar-highlights";
        };
    }
);
    "sidebar-resizer" = (
    let
        baseUrl = "https://github.com/jeetsukumaran/obsidian-sidebar-resizer/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sidebar-resizer";
        version = "1.0.11";
        outputHash = "sha256-D3qBB2TRKRE+ben4+S6+Gbr8TdZP6lcb3eXxbW+Ia3A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adjust the sidebar sizes easily.'';
            homepage = "https://github.com/jeetsukumaran/obsidian-sidebar-resizer";
        };
    }
);
    "sigma" = (
    let
        baseUrl = "https://github.com/monesga/obsidian-sigma/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sigma";
        version = "1.0.4";
        outputHash = "sha256-9eCHj1cwsSy1loF5yTJArymVcLbdm5s1uJ6yMIg9UVI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use notes as calculation sheets.'';
            homepage = "https://github.com/monesga/obsidian-sigma";
        };
    }
);
    "silicon" = (
    let
        baseUrl = "https://github.com/deepfates/silicon/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "silicon";
        version = "1.0.3";
        outputHash = "sha256-+baXhSas49BPBDp106b8c24P0smt/60Ui4vG+dYpBIQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add some intelligence to your notes with Silicon AI.'';
            homepage = "https://github.com/deepfates/silicon";
        };
    }
);
    "similar-notes" = (
    let
        baseUrl = "https://github.com/joybro/obsidian-similar-notes/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "similar-notes";
        version = "1.2.0";
        outputHash = "sha256-yzlzFZ1tuyWcK31/jVYN+2AE9D3aEFsPtaaCRSo4tgs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find semantically similar notes using AI. Local models (mobile & desktop) or cloud APIs.'';
            homepage = "https://github.com/joybro/obsidian-similar-notes";
        };
    }
);
    "simple-anki-sync" = (
    let
        baseUrl = "https://github.com/lukmay/simple-anki-sync/releases/download/1.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-anki-sync";
        version = "1.6.0";
        outputHash = "sha256-yFA6R1qZaMFy0Gryex2guLom0PqYQFw6fc7fFyxsXlA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple way to sync basic flashcards with Anki with zero time tweaking settings and 100 % of your time learning.'';
            homepage = "https://github.com/lukmay/simple-anki-sync";
        };
    }
);
    "simple-archiver" = (
    let
        baseUrl = "https://github.com/mfarr/obsidian-archive/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-archiver";
        version = "0.6.1";
        outputHash = "sha256-mIKsgavjK0IVnifUnW/K51UY/lsDOiRFxz3XbEiPQqs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move old, stinky notes and folders to an archive, where they belong.'';
            homepage = "https://github.com/mfarr/obsidian-archive";
        };
    }
);
    "simple-banner" = (
    let
        baseUrl = "https://github.com/eatcodeplay/obsidian-simple-banner/releases/download/0.5.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-banner";
        version = "0.5.9";
        outputHash = "sha256-IIpj3t99/tm2nk4dzhd3qRyDn1vgSfqQ6/dQ81HRBqU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visually enhance your notes with a customizable banner. Supports icons and time/date display.'';
            homepage = "https://github.com/eatcodeplay/obsidian-simple-banner";
        };
    }
);
    "simple-canvasearch" = (
    let
        baseUrl = "https://github.com/ddalexb/obsidian-simple-canvasearch/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-canvasearch";
        version = "1.0.2";
        outputHash = "sha256-iwb4s6jl0eHIh3Dd8u54Ol0NNy6xw8vGVM8YJxMifKk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly fuzzysearch notes, cards and their content and shift focus to them within the currently opened canvas.'';
            homepage = "https://github.com/ddalexb/obsidian-simple-canvasearch";
        };
    }
);
    "simple-citations" = (
    let
        baseUrl = "https://github.com/masaki39/simple-citations/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-citations";
        version = "1.5.1";
        outputHash = "sha256-pnTwFvBBc8u/COTraAXQ2hoPz5zBZKvJBd8JBQDENNE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add & update simple literature notes from Zotero.'';
            homepage = "https://github.com/masaki39/simple-citations";
        };
    }
);
    "simple-code-formatter" = (
    let
        baseUrl = "https://github.com/wiop93256/Simple-Code-Formmater/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-code-formatter";
        version = "1.2.2";
        outputHash = "sha256-yLfPNymjxmj6a49zoezEnPc+sYS2eAG0Bf2CAmGeEFM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Format the code block where the cursor is.'';
            homepage = "https://github.com/wiop93256/Simple-Code-Formmater";
        };
    }
);
    "simple-colored-folder" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-simple-colored-folder/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-colored-folder";
        version = "1.3.0";
        outputHash = "sha256-GUejEEKGsDkjFln1CGNwYUTIKDeCVEhAK8JvVFHufHk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automagically add color to roots folders, and customize it with Style Settings.'';
            homepage = "https://github.com/Mara-Li/obsidian-simple-colored-folder";
        };
    }
);
    "simple-columns" = (
    let
        baseUrl = "https://github.com/Josie1902/Simple-Columns/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-columns";
        version = "1.0.8";
        outputHash = "sha256-fZ2UGwjCp+i+mJxvhnHXO68JVFz+0e5pHMDyyiV8PHs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and manage columns in your notes effortlessly, featuring customizable columns with easy-to-resize widths.'';
            homepage = "https://github.com/Josie1902/Simple-Columns";
        };
    }
);
    "simple-dice-roller" = (
    let
        baseUrl = "https://github.com/yeeshue99/SimpleDiceRoller/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-dice-roller";
        version = "1.2.1";
        outputHash = "sha256-gaT8Dqpfrfe0q83PEA/7E3DPYdH0uCpAABPUJrLjEac=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simulate and average dice formulas.'';
            homepage = "https://github.com/yeeshue99/SimpleDiceRoller";
        };
    }
);
    "simple-disguise" = (
    let
        baseUrl = "https://github.com/ctxinf/simple-disguise/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-disguise";
        version = "1.0.0";
        outputHash = "sha256-Dorl7/IDu7K9OdnCRzTj6o9Ncs2OWzlnYXfc997/VzI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''disguise/obscure/hide the markdown content in a very simple way.'';
            homepage = "https://github.com/ctxinf/simple-disguise";
        };
    }
);
    "simple-file-info" = (
    let
        baseUrl = "https://github.com/lukas-cap/simple-file-info/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-file-info";
        version = "1.0.3";
        outputHash = "sha256-WGHNrkvkNxV7b0trRPC2jqy6QuTEH2bU8wyR32uh18U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A lightweight file info pane with native look and feel.'';
            homepage = "https://github.com/lukas-cap/simple-file-info";
        };
    }
);
    "simple-file-push" = (
    let
        baseUrl = "https://github.com/huedaya/obsidian-simple-file-push/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-file-push";
        version = "0.0.4";
        outputHash = "sha256-dWfJIM5LMmLhiPHEIuj4vYNOMKINBArbbFHf6Z1hpn0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Push Markdown file to API endpoint.'';
            homepage = "https://github.com/huedaya/obsidian-simple-file-push";
        };
    }
);
    "simple-focus" = (
    let
        baseUrl = "https://github.com/linqing24/obsidian-simple-focus/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-focus";
        version = "0.0.4";
        outputHash = "sha256-9vLWtcbnTIpjHz6gEoAbRA0i2oD+G7nrR0HNC4mwOSw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to focus on a specific file or folder'';
            homepage = "https://github.com/linqing24/obsidian-simple-focus";
        };
    }
);
    "simple-image-inserter" = (
    let
        baseUrl = "https://github.com/jdholtz/obsidian-image-inserter/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-image-inserter";
        version = "0.1.0";
        outputHash = "sha256-vJLi7tOz1JvsGBAWkPK27c1gZDzG7ErOJbuBHA8Gcts=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add images from the file system into notes through a built-in file picker.'';
            homepage = "https://github.com/jdholtz/obsidian-image-inserter";
        };
    }
);
    "simple-note-quiz" = (
    let
        baseUrl = "https://github.com/beginner137/Obsidian-simple-note-quiz/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-note-quiz";
        version = "1.1.1";
        outputHash = "sha256-buKXWZm0pVYy55APM1kAux3KUVFvglVxOy98E1pJ5Ko=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Start a simple quiz on your current note.'';
            homepage = "https://github.com/beginner137/Obsidian-simple-note-quiz";
        };
    }
);
    "simple-note-review" = (
    let
        baseUrl = "https://github.com/dartungar/obsidian-simple-note-review/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-note-review";
        version = "1.2.6";
        outputHash = "sha256-0pUAWi+bX4EYJFkYTk0hNBZm9ILSGOGOWk5d3FQoBZs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple, customizable plugin for easy note review, resurfacing and repetition.'';
            homepage = "https://github.com/dartungar/obsidian-simple-note-review";
        };
    }
);
    "simple-password" = (
    let
        baseUrl = "https://github.com/ShadiestGoat/obsidian-simple-password/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-password";
        version = "1.4.0";
        outputHash = "sha256-M9YI4sVhXz5eYMCp0Q3nfOJY3tHiMEwO2/Lb1ZUDAQ0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Protect your vault behind a password'';
            homepage = "https://github.com/ShadiestGoat/obsidian-simple-password";
        };
    }
);
    "simple-prompt" = (
    let
        baseUrl = "https://github.com/arumie/obsidian-simple-prompt-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-prompt";
        version = "1.0.1";
        outputHash = "sha256-6glzCJHrO3adLbyBL76ampkWB1ACbbtt8fhXuX8eW2s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple interface to generate or rewrite content using LLMs based on user input.'';
            homepage = "https://github.com/arumie/obsidian-simple-prompt-plugin";
        };
    }
);
    "simple-quiz" = (
    let
        baseUrl = "https://github.com/IvanKalmar/simple-quiz/releases/download/0.2.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-quiz";
        version = "0.2.10";
        outputHash = "sha256-/7YHlgasK7UAU5JCovBAReOfAmTD188JziGTHoTfAGE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creating simple quizzes.'';
            homepage = "https://github.com/IvanKalmar/simple-quiz";
        };
    }
);
    "simple-rss" = (
    let
        baseUrl = "https://github.com/monnierant/obsidian-simple-rss/releases/download/0.3.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-rss";
        version = "0.3.12";
        outputHash = "sha256-Mwp2c2enph+QXh4mS0PWAKor+sIfo6WGkPEHXsfF0Bc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collect RSS articles into notes.'';
            homepage = "https://github.com/monnierant/obsidian-simple-rss";
        };
    }
);
    "simple-sketch" = (
    let
        baseUrl = "https://github.com/Yohh/obsidian-simple-sketch/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-sketch";
        version = "1.0.9";
        outputHash = "sha256-wi0FifZeWMFJchSElENNYw4uH/m2KTWoQxphmRuvfn4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create minimalist sketches in a dedicated view, draw with a pencil, generate shapes, add text, save it to the vault or download it as an image.'';
            homepage = "https://github.com/Yohh/obsidian-simple-sketch";
        };
    }
);
    "simple-steam-auth" = (
    let
        baseUrl = "https://github.com/dreamscached/obsidian-simple-steam-auth/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-steam-auth";
        version = "1.0.10";
        outputHash = "sha256-ZGZK3rF/yQMh98e/smYqOEsi0Jdlv63ktsMHtNEEseY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate Steam Guard codes right in your vault.'';
            homepage = "https://github.com/dreamscached/obsidian-simple-steam-auth";
        };
    }
);
    "simple-tab-indent" = (
    let
        baseUrl = "https://github.com/hoomersinpsom/simple-tab-indent/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-tab-indent";
        version = "1.1.0";
        outputHash = "sha256-HySTqtztyuaSnQ6ysQPFJdtPVMR6A4tY0AStoPaEpMA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pressing Tab inserts a zero-width space + real tab, giving true indentation without triggering Markdown code blocks. Includes a settings panel to change the CSS tab width.'';
            homepage = "https://github.com/hoomersinpsom/simple-tab-indent";
        };
    }
);
    "simple-table-math" = (
    let
        baseUrl = "https://github.com/eatcodeplay/obsidian-simple-table-math/releases/download/0.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-table-math";
        version = "0.2.4";
        outputHash = "sha256-lT4zXlBVgJdTQOIr4RI3JtLCC9CAgf4clKRJQGdniuw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Do some math (sum, average, etc.) in your markdown tables.'';
            homepage = "https://github.com/eatcodeplay/obsidian-simple-table-math";
        };
    }
);
    "simple-time-tracker" = (
    let
        baseUrl = "https://github.com/Ellpeck/ObsidianSimpleTimeTracker/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-time-tracker";
        version = "1.2.2";
        outputHash = "sha256-vcvDkWAxB4lEqgRXJD/qJpqgNaeMAoS0tF0nFFXOGtk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Multi-purpose time trackers for your notes.'';
            homepage = "https://github.com/Ellpeck/ObsidianSimpleTimeTracker";
        };
    }
);
    "simple-todo" = (
    let
        baseUrl = "https://github.com/elliotxx/obsidian-simple-todo/releases/download/0.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-todo";
        version = "0.2.7";
        outputHash = "sha256-k9PD4KLBWH2JIpitQXasAyEvqmkh3AqZCOSPY2EZJC4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A minimalist text-based todo manager (Text-Based GTD) for efficient task management.'';
            homepage = "https://github.com/elliotxx/obsidian-simple-todo";
        };
    }
);
    "simple-vault-importer" = (
    let
        baseUrl = "https://github.com/WebInspectInc/obsidian-simple-template-importer/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simple-vault-importer";
        version = "1.0.3";
        outputHash = "sha256-G8QFra/m6tpiu/wFXwK1dFsDWWR5SV6tdFZpg/W82nM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import starter vaults into your own vault.'';
            homepage = "https://github.com/WebInspectInc/obsidian-simple-template-importer";
        };
    }
);
    "simsapa" = (
    let
        baseUrl = "https://github.com/simsapa/simsapa-obsidian/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "simsapa";
        version = "0.5.0";
        outputHash = "sha256-w/Oentald0x24QXgRuJYpF306iK95dDt+Qe1P+w1LUQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Pāli dictionary and sutta search using Simsapa Dhamma Reader. Open a sidebar or double-click to lookup Pāli words in the dictionary, or search in the suttas.'';
            homepage = "https://github.com/simsapa/simsapa-obsidian";
        };
    }
);
    "single-file-daily-notes" = (
    let
        baseUrl = "https://github.com/pranavmangal/obsidian-single-file-daily-notes/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "single-file-daily-notes";
        version = "1.4.1";
        outputHash = "sha256-931dbdoXQtUouOwL0WvLnd0gUQ4lvJHZQbLfI0niuU4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and manage daily notes in a single file.'';
            homepage = "https://github.com/pranavmangal/obsidian-single-file-daily-notes";
        };
    }
);
    "size-history" = (
    let
        baseUrl = "https://github.com/pbrw/obsidian-size-history/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "size-history";
        version = "1.0.0";
        outputHash = "sha256-2ydG4HE7kJGg0ER26g4DovIrwW5x99s2QFfs9vgVHbs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Admire the growth of your vault with a "hand-drawn" chart.'';
            homepage = "https://github.com/pbrw/obsidian-size-history";
        };
    }
);
    "slackify-note" = (
    let
        baseUrl = "https://github.com/jeremyoverman/obsidian-slackify-note/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slackify-note";
        version = "1.2.2";
        outputHash = "sha256-8OMBYW716TDmyBHrxdPlSj0kfkJUUMyg7ZEiXr0qjb0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert a note to Slack-compliant Markdown.'';
            homepage = "https://github.com/jeremyoverman/obsidian-slackify-note";
        };
    }
);
    "slash-commander" = (
    let
        baseUrl = "https://github.com/alephpiece/obsidian-slash-commander/releases/download/0.3.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slash-commander";
        version = "0.3.8";
        outputHash = "sha256-chM7X2DonG7OdMBOpFUqnN9mi227JZFny3eBi6lK4IE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customize the slash command list, assign each command an icon.'';
            homepage = "https://github.com/alephpiece/obsidian-slash-commander";
        };
    }
);
    "slash-complete" = (
    let
        baseUrl = "https://github.com/Spiderpig86/slash-complete/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slash-complete";
        version = "1.0.2";
        outputHash = "sha256-+HAXl1F33RKlzschCfyLAvGcsu+DU6s+bOEsHyhMr64=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds Notion-style slash command autocompletion, enabling fast and intuitive insertion of Markdown blocks, formatting, and commands directly from the editor. This is a direct replacement for the default slash command plugin.'';
            homepage = "https://github.com/Spiderpig86/slash-complete";
        };
    }
);
    "slash-snippets" = (
    let
        baseUrl = "https://github.com/echo-saurav/slash-snippets-plugin/releases/download/1.0.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slash-snippets";
        version = "1.0.18";
        outputHash = "sha256-fjL5BAwQbXTBGyo78il9/ShrICv5vvfbc5cRYMBf56I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use slash command to insert quick text'';
            homepage = "https://github.com/echo-saurav/slash-snippets-plugin";
        };
    }
);
    "slide-note" = (
    let
        baseUrl = "https://github.com/Phantom1003/obsidian-slide-note/releases/download/0.0.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slide-note";
        version = "0.0.17";
        outputHash = "sha256-OSbgxh8bJyd2en5FZmg+jH5062XrqCy5DlBYoFt6GDo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Conveniently take notes on PDF course slides.'';
            homepage = "https://github.com/Phantom1003/obsidian-slide-note";
        };
    }
);
    "slides-extended" = (
    let
        baseUrl = "https://github.com/ebullient/obsidian-slides-extended/releases/download/2.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slides-extended";
        version = "2.3.5";
        outputHash = "sha256-y/NXlap4zE3/LgvDeP+c31+KKUl/Svo1OUtZIKrI8s4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create markdown-based reveal.js presentations. Fork of Advanced Slides.'';
            homepage = "https://github.com/ebullient/obsidian-slides-extended";
        };
    }
);
    "slurp" = (
    let
        baseUrl = "https://github.com/inhumantsar/slurp/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "slurp";
        version = "0.2.0";
        outputHash = "sha256-/psRbNZps0+1ku6EjZB6iNdbxY7Oqyp14Oz4uZ+CRj0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Slurps webpages and saves them as clean, uncluttered Markdown.'';
            homepage = "https://github.com/inhumantsar/slurp";
        };
    }
);
    "smart-chatgpt" = (
    let
        baseUrl = "https://github.com/brianpetro/smart-chatgpt-obsidian/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-chatgpt";
        version = "1.3.1";
        outputHash = "sha256-7tPe/G+xPu7oyTzBwSg4bU/bUtXIsKVEBzayJpq7R1A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate OpenAI"s ChatGPT seamlessly in notes. Automatically saves links, allows marking threads as done, and integrates with Dataview.'';
            homepage = "https://github.com/brianpetro/smart-chatgpt-obsidian";
        };
    }
);
    "smart-composer" = (
    let
        baseUrl = "https://github.com/glowingjade/obsidian-smart-composer/releases/download/1.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-composer";
        version = "1.2.9";
        outputHash = "sha256-tDoPCD+4HbvkWBw6CGZ6M8/IPkMSBxV+SA5u2WFoMC0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI chat with note context, smart writing assistance, and one-click edits for your vault.'';
            homepage = "https://github.com/glowingjade/obsidian-smart-composer";
        };
    }
);
    "smart-connections" = (
    let
        baseUrl = "https://github.com/brianpetro/obsidian-smart-connections/releases/download/4.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-connections";
        version = "4.3.0";
        outputHash = "sha256-phBU6DQM9FQ42d7GVoekOYjnEnQ3PPz7q5D/uPTqLgo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI link discovery copilot. See related notes as you write. Lookup using semantic (vector) search across your vault. Zero-setup local model for embeddings, no API keys, private.'';
            homepage = "https://github.com/brianpetro/obsidian-smart-connections";
        };
    }
);
    "smart-connections-visualizer" = (
    let
        baseUrl = "https://github.com/Mossy1022/Smart-Connections-Visualizer/releases/download/1.0.27";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-connections-visualizer";
        version = "1.0.27";
        outputHash = "sha256-EqjDNu/pSg7BsMK1YVE+3dC59XDzRz70IVip8W7O/z4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View your Smart Connections in a visualized format.'';
            homepage = "https://github.com/Mossy1022/Smart-Connections-Visualizer";
        };
    }
);
    "smart-context" = (
    let
        baseUrl = "https://github.com/brianpetro/smart-context-obsidian/releases/download/3.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-context";
        version = "3.1.0";
        outputHash = "sha256-YZpckQPJ9dPYuKbRBUiiPuhoFN9Kx9xR+y7EzffLCZw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Instantly copy folder contents or open notes—excluding specified headings—into your clipboard. Perfect for providing curated context to tools like ChatGPT, improving responses, and streamlining your AI workflows.'';
            homepage = "https://github.com/brianpetro/smart-context-obsidian";
        };
    }
);
    "smart-day-night-switcher" = (
    let
        baseUrl = "https://github.com/Andrii256/ops_obsidian_smart-day-night-switcher/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-day-night-switcher";
        version = "1.0.3";
        outputHash = "sha256-kH3P8PW6q9yB3h8eoeNdfOs5LoeZWY6Z5gZZ6Y87LZo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intelligently determines sunrise and sunset times and automatically switches the color scheme to light or dark mode.'';
            homepage = "https://github.com/Andrii256/ops_obsidian_smart-day-night-switcher";
        };
    }
);
    "smart-export" = (
    let
        baseUrl = "https://github.com/LittleHaku/obsidian-smart-export/releases/download/1.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-export";
        version = "1.11.0";
        outputHash = "sha256-Ts3m2AxXHA42VODlR/S21S81WAH86dIY+DsAfGz66xA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Plugin that follows wikilinks to a configurable depth, joining the notes into a single export.'';
            homepage = "https://github.com/LittleHaku/obsidian-smart-export";
        };
    }
);
    "smart-html-select" = (
    let
        baseUrl = "https://github.com/IsaiaScope/smart-html-select-plugin/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-html-select";
        version = "0.0.5";
        outputHash = "sha256-IkopWnOzb9Z4KWBpG2Wki4JXfvejQiKZ2kmfZZxKWjk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create select that change the markdown on change adding an action button to the ribbon (left sidebar)'';
            homepage = "https://github.com/IsaiaScope/smart-html-select-plugin";
        };
    }
);
    "smart-link-alias" = (
    let
        baseUrl = "https://github.com/vpcano/obsidian-smart-link-alias/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-link-alias";
        version = "1.0.0";
        outputHash = "sha256-VckpzTXPyYGo2nMZh5+ZEzJ0HhND/LiohK5Uk6yLBrg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your internal links management with dynamic alias customization. Display short, full, or combined titles for your notes effortlessly.'';
            homepage = "https://github.com/vpcano/obsidian-smart-link-alias";
        };
    }
);
    "smart-link-formatter" = (
    let
        baseUrl = "https://github.com/ccmdi/smart-link-formatter/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-link-formatter";
        version = "1.4.3";
        outputHash = "sha256-pE9Wnx8ksxKU488SmBnLWZ7PeZNIzVJM/MGNCPwot1U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically fetches page metadata from pasted links, with explicit support for specific websites.'';
            homepage = "https://github.com/ccmdi/smart-link-formatter";
        };
    }
);
    "smart-memos" = (
    let
        baseUrl = "https://github.com/Mossy1022/Smart-Memos/releases/download/1.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-memos";
        version = "1.1.8";
        outputHash = "sha256-hk6RYsAt0S8RwlH5olVnQyI6HnfVZ4rwdVsYIEGSWt0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create personalized and intelligent analysis, brainstorms, summaries, transcriptions, and more for audio recordings that can be imported or spoken directly into a note'';
            homepage = "https://github.com/Mossy1022/Smart-Memos";
        };
    }
);
    "smart-random-note" = (
    let
        baseUrl = "https://github.com/erichalldev/obsidian-smart-random-note/releases/download/0.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-random-note";
        version = "0.2.2";
        outputHash = "sha256-0oc9Oc8gpfTX+VyUlbTWyFC2KVQhRJbrsVf8iJ6ucvs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open random notes with greater control.'';
            homepage = "https://github.com/erichalldev/obsidian-smart-random-note";
        };
    }
);
    "smart-rename" = (
    let
        baseUrl = "https://github.com/mnaoumov/obsidian-smart-rename/releases/download/2.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-rename";
        version = "2.2.1";
        outputHash = "sha256-10lKEORbw1/v/8XPyL7doT4yuBvGTkRNKz6/zd1yAj0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename notes keeping previous title in existing links.'';
            homepage = "https://github.com/mnaoumov/obsidian-smart-rename";
        };
    }
);
    "smart-second-brain" = (
    let
        baseUrl = "https://github.com/your-papa/obsidian-Smart2Brain/releases/download/2.0.0-beta";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-second-brain";
        version = "2.0.0-beta";
        outputHash = "sha256-iwMK0nHq6NkmNw4otVgwkEc/sIv8p8fkH9GyIofpNbw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interact with your privacy focused assistant by leveraging Ollama or OpenAI and making your second brain even smarter.'';
            homepage = "https://github.com/your-papa/obsidian-Smart2Brain";
        };
    }
);
    "smart-templates" = (
    let
        baseUrl = "https://github.com/brianpetro/obsidian-smart-templates/releases/download/2.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-templates";
        version = "2.1.0";
        outputHash = "sha256-J/08MCN2Wz44Vy9BAUweySPiFUFyIgkPmlF8Bu/9sHk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI powered templates for generating structured content. Works with Local Models, Anthropic Claude, Gemini, OpenAI & more.'';
            homepage = "https://github.com/brianpetro/obsidian-smart-templates";
        };
    }
);
    "smart-text-mover" = (
    let
        baseUrl = "https://github.com/Ankush-Chander/obsidian-smart-move-text/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-text-mover";
        version = "1.0.5";
        outputHash = "sha256-fve832zOpyNObyvNo2nbcdAxuSqdCLZN5lRZyakQoLE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intelligent way to arrange text/links in file.'';
            homepage = "https://github.com/Ankush-Chander/obsidian-smart-move-text";
        };
    }
);
    "smart-title" = (
    let
        baseUrl = "https://github.com/maxzyma/obsidian-plugin-smart-title/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-title";
        version = "1.0.1";
        outputHash = "sha256-5uAfhDZbXHp+P7T1a7RHNqRiKiSZYbQ9yO7xNWZiPVQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically extract tags and aliases from the title.'';
            homepage = "https://github.com/maxzyma/obsidian-plugin-smart-title";
        };
    }
);
    "smart-vault-visualizer" = (
    let
        baseUrl = "https://github.com/Mossy1022/Smart-Connections-Vault-Visualizer/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smart-vault-visualizer";
        version = "1.0.4";
        outputHash = "sha256-cHFHTkSJkIZMauBOzWzmWq3LUGu7PYp1kJVSamPRuJw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualizes and manages smart clusters and cluster groups in your vault.'';
            homepage = "https://github.com/Mossy1022/Smart-Connections-Vault-Visualizer";
        };
    }
);
    "smooth-cursor" = (
    let
        baseUrl = "https://github.com/busyoGG/SmoothCursor/releases/download/2.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smooth-cursor";
        version = "2.0.6";
        outputHash = "sha256-kHl75/rB8k4Z2wwjIGGhGznb4WeQxB8Slq74anGXggk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''平滑光标 Smooth Cursor'';
            homepage = "https://github.com/busyoGG/SmoothCursor";
        };
    }
);
    "smooth-navigator" = (
    let
        baseUrl = "https://github.com/gasparschott/smooth-navigator/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smooth-navigator";
        version = "1.0.7";
        outputHash = "sha256-xSceTUw3L82BCBKlGhdR0W/6eoK1fs9BbcrKd3mV3X8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Smoothly cycle through open files and splits via the keyboard.'';
            homepage = "https://github.com/gasparschott/smooth-navigator";
        };
    }
);
    "smort-obsidian" = (
    let
        baseUrl = "https://github.com/SmortApp/obsidian-smort/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "smort-obsidian";
        version = "1.0.1";
        outputHash = "sha256-XWnkg0miysx1AznkcmrFvsX0WV1MiP4pNhRsoJkTY1Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add Smort.io articles to Obsidian. Smort.io lets you easily edit, annotate and share articles.'';
            homepage = "https://github.com/SmortApp/obsidian-smort";
        };
    }
);
    "snippet-commands-obsidian" = (
    let
        baseUrl = "https://github.com/deathau/snippet-commands-obsidian/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "snippet-commands-obsidian";
        version = "0.0.2";
        outputHash = "sha256-RfCP2OZq0UPyQInhceqAMlcBd9jWkMaqvgaMvYeRahc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Register custom CSS snippets as commands (which you can bind hotkeys to).'';
            homepage = "https://github.com/deathau/snippet-commands-obsidian";
        };
    }
);
    "snippets" = (
    let
        baseUrl = "https://github.com/cristianvasquez/obsidian-snippets-plugin/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "snippets";
        version = "0.0.4";
        outputHash = "sha256-FS3aRZBkPKA6g4ymxNjK18xPHhLhj/2R40ctqhcL/pM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute simple scripts/snippets. This plugin is experimental.'';
            homepage = "https://github.com/cristianvasquez/obsidian-snippets-plugin";
        };
    }
);
    "snippets-manager" = (
    let
        baseUrl = "https://github.com/ramandv/obsidian-snippets-manager/releases/download/1.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "snippets-manager";
        version = "1.1.7";
        outputHash = "sha256-9Z7eefsIZA03m5DBaathCSY2vC2KDfxfppKmkLmZJHI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Snippets manager with full mobile support. You can manage code snippets, infos like passport number, email signature or anything.'';
            homepage = "https://github.com/ramandv/obsidian-snippets-manager";
        };
    }
);
    "snsvrno-tags" = (
    let
        baseUrl = "https://github.com/snsvrno/snsvrno-short-tags/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "snsvrno-tags";
        version = "0.1.5";
        outputHash = "sha256-3XBPLmkAs7N2gEL170I9YDzvlnPtjbrMfzG5DnPzIdg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Gives more options on how to display tags in preview mode.'';
            homepage = "https://github.com/snsvrno/snsvrno-short-tags";
        };
    }
);
    "soc-toolkit" = (
    let
        baseUrl = "https://github.com/michaelmassoni/obsidian-soc-toolkit/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "soc-toolkit";
        version = "1.6.2";
        outputHash = "sha256-sygElfxk/5nMeoA02N3AqCPBAMObrcc3aHZVC5kNLwI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A collection of tools for cyber security analysts.'';
            homepage = "https://github.com/michaelmassoni/obsidian-soc-toolkit";
        };
    }
);
    "solidtime-integration" = (
    let
        baseUrl = "https://github.com/pronicx/obsidian-solidtime-integration/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "solidtime-integration";
        version = "0.2.0";
        outputHash = "sha256-XPntyMdh1H35JPScbI5lThRz6A6WYtqG2MV9gYsnKsU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Connect SolidTime to track your work time directly within your vault.'';
            homepage = "https://github.com/pronicx/obsidian-solidtime-integration";
        };
    }
);
    "solo-rpg-toolkit" = (
    let
        baseUrl = "https://github.com/alexkurowski/solo-toolkit/releases/download/0.7.15";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "solo-rpg-toolkit";
        version = "0.7.15";
        outputHash = "sha256-DKwXayj82UiagjlQ3CofsIc6dAkHnloRCFs4ttpue9g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helpful tools for playing TTRPG games, geared towards solo play with a GM emulator.'';
            homepage = "https://github.com/alexkurowski/solo-toolkit";
        };
    }
);
    "solve" = (
    let
        baseUrl = "https://github.com/LiamRiddell/obsidian-solve/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "solve";
        version = "1.1.0";
        outputHash = "sha256-v7tlzLonTPV6/04rlFmc/4fs4ojKOa3nNS551XSzFQM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Your Math Maestro! Real-time calculations without AI (e.g. ChatGPT) fuss. From date magic ("Now + 20 days") to arithmetic flair ("10 + 5 / (2 * 2)"), your trusted sidekick in every note. More brilliance on the way!'';
            homepage = "https://github.com/LiamRiddell/obsidian-solve";
        };
    }
);
    "sonkil" = (
    let
        baseUrl = "https://github.com/ohyoungpark/obsidian-sonkil/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sonkil";
        version = "0.1.5";
        outputHash = "sha256-5wc311ROWWJVhVQXnrPGJMUBH0AKyk01Q3FgQvH2mao=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides Emacs-style text operations like Kill/Yank (Kill Ring), multi-cursor editing, and visual mark selection.'';
            homepage = "https://github.com/ohyoungpark/obsidian-sonkil";
        };
    }
);
    "sort-frontmatter" = (
    let
        baseUrl = "https://github.com/mariomui/obsidian-sort-frontmatter/releases/download/1.0.25";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sort-frontmatter";
        version = "1.0.25";
        outputHash = "sha256-lGVNawQabgjaMcxyi/3LE+9zfNVsOAF6nKe7Pk1F/7A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sort frontmatter recursively.'';
            homepage = "https://github.com/mariomui/obsidian-sort-frontmatter";
        };
    }
);
    "sortable-tables" = (
    let
        baseUrl = "https://github.com/filippov112/obsidian-sortable-tables/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sortable-tables";
        version = "1.0.0";
        outputHash = "sha256-DOs0HlGAEacXEBt8f7S7lw7Vn6RKCBHJB1/58XqRerY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds sortable columns to markdown tables in preview mode.'';
            homepage = "https://github.com/filippov112/obsidian-sortable-tables";
        };
    }
);
    "soundscapes" = (
    let
        baseUrl = "https://github.com/andrewmcgivery/obsidian-soundscapes/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "soundscapes";
        version = "1.5.0";
        outputHash = "sha256-JAVsY+GjAicF/Ox9L+Y3PuPGKkcpxJBNFL2wtUSQKb8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a music/ambiance (E.g. lofi, white noise) player to the status bar to help with concentration'';
            homepage = "https://github.com/andrewmcgivery/obsidian-soundscapes";
        };
    }
);
    "source-code-note" = (
    let
        baseUrl = "https://github.com/waiting0324/obsidian-code-note/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "source-code-note";
        version = "1.0.0";
        outputHash = "sha256-OBPFSZJoaoqE8cHrbz8c4lWeR3+LPGmBUGzevivNlKk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Organize source code note easily.'';
            homepage = "https://github.com/waiting0324/obsidian-code-note";
        };
    }
);
    "source-scanner" = (
    let
        baseUrl = "https://github.com/gerrie-myburgh/source-scanner/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "source-scanner";
        version = "2.0.0";
        outputHash = "sha256-QlGa7wrT7o5cW8YIx1yuwnOd98/lVKuWLdu6oDvjmSY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Scanner that extracts comments from source and places it in Markdown files.'';
            homepage = "https://github.com/gerrie-myburgh/source-scanner";
        };
    }
);
    "sourcemode-styling" = (
    let
        baseUrl = "https://github.com/chrishoward-projects/sourcemode-styling/releases/download/0.2.31";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sourcemode-styling";
        version = "0.2.31";
        outputHash = "sha256-3w3GLLGZIUMm1IVSfhDH+Y3yMxqGKK5aphEjWiRMiyw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides a customisable raw look in source mode using a monospace font to clearly differentiate from Live Preview.'';
            homepage = "https://github.com/chrishoward-projects/sourcemode-styling";
        };
    }
);
    "spaced-everything" = (
    let
        baseUrl = "https://github.com/zachmueller/spaced-everything/releases/download/1.3.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spaced-everything";
        version = "1.3.4";
        outputHash = "sha256-WYok93IPc6r4kfKPvVMkHMQausIfkqq3e5ONBy2VPaQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Apply spaced repetition algorithms to everything in your vault.'';
            homepage = "https://github.com/zachmueller/spaced-everything";
        };
    }
);
    "spaced-repetition-ai" = (
    let
        baseUrl = "https://github.com/ai-learning-tools/obsidian-spaced-repetition-ai/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spaced-repetition-ai";
        version = "1.0.1";
        outputHash = "sha256-tIznUQgLu+jv2eJWNac1Nmth69q90JN+Fp19hdiSzTI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Review, generate, and add flashcards for your notes using AI'';
            homepage = "https://github.com/ai-learning-tools/obsidian-spaced-repetition-ai";
        };
    }
);
    "spacekeys" = (
    let
        baseUrl = "https://github.com/jlumpe/obsidian-spacekeys/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spacekeys";
        version = "0.5.0";
        outputHash = "sha256-Rh9ulAO87S6xyP1jgJky34v17t7xg28QYDVPznYGooU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Define hotkeys based on sequences of keypresses.'';
            homepage = "https://github.com/jlumpe/obsidian-spacekeys";
        };
    }
);
    "speech2text-helper" = (
    let
        baseUrl = "https://github.com/mwoz123/speech-to-text-keyboard-helper/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "speech2text-helper";
        version = "1.1.2";
        outputHash = "sha256-pb1Cmdrrk7xonxCOKl0oqUD34np3Md642eDvaXQLDtQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Make Google Speech to Text on Android available from the command palette.'';
            homepage = "https://github.com/mwoz123/speech-to-text-keyboard-helper";
        };
    }
);
    "spellcheck-toggler" = (
    let
        baseUrl = "https://github.com/julzerinos/spellcheck-toggler-obsidian-plugin/releases/download/1.4.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spellcheck-toggler";
        version = "1.4.4";
        outputHash = "sha256-hUorePM95kPj9mW77AruBILWrehOK42Ffa1YNSV4Q+Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle spellchecking for types of text blocks in the editing view.'';
            homepage = "https://github.com/julzerinos/spellcheck-toggler-obsidian-plugin";
        };
    }
);
    "spirits-book" = (
    let
        baseUrl = "https://github.com/ahlrodrigues/spirits-book/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spirits-book";
        version = "1.0.0";
        outputHash = "sha256-1552O/8fuvVDdLCmk6P7KHy2OL41NvrbzspJbzYdWlE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Study and explore The Spirit"s Book with search, random questions, and favorites.'';
            homepage = "https://github.com/ahlrodrigues/spirits-book";
        };
    }
);
    "spoiler-block-obsidian" = (
    let
        baseUrl = "https://github.com/AllJavi/spoiler-block-obsidian/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spoiler-block-obsidian";
        version = "1.0.0";
        outputHash = "sha256-ESMRQwJONkeAGy05hVrDH2LsPZIsINtHemv7/7Zt/ls=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide information until you want to reveal it.'';
            homepage = "https://github.com/AllJavi/spoiler-block-obsidian";
        };
    }
);
    "spoilers" = (
    let
        baseUrl = "https://github.com/jacobtread/obsidian-spoilers/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spoilers";
        version = "1.0.5";
        outputHash = "sha256-bB12xQMx2+rLcSj/qvF6ICT87XxJBJIvRYH9FT2+J6E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide and reveal blocks of information'';
            homepage = "https://github.com/jacobtread/obsidian-spoilers";
        };
    }
);
    "spotify-api" = (
    let
        baseUrl = "https://github.com/Darren-project/obsidian-spotify/releases/download/1.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spotify-api";
        version = "1.6.5";
        outputHash = "sha256-u2XSRaPXlRpxuXx+GsRr2zbQ8lymMj4h0C0bzWsYQo4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Exposes Spotify API'';
            homepage = "https://github.com/Darren-project/obsidian-spotify";
        };
    }
);
    "spotify-link" = (
    let
        baseUrl = "https://github.com/studiowebux/obsidian-spotify-link/releases/download/1.15.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spotify-link";
        version = "1.15.2";
        outputHash = "sha256-rdqpUrDQmT/z5Hc536ztggfkVu7Xl4ii9M6QkhtKkrs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Include the song you"re currently listening to in your note.'';
            homepage = "https://github.com/studiowebux/obsidian-spotify-link";
        };
    }
);
    "spotify-links" = (
    let
        baseUrl = "https://github.com/Cutaiar/obsidian-song-links/releases/download/0.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spotify-links";
        version = "0.3.2";
        outputHash = "sha256-V9j6hUkoJAIfz7fMQ29ODDWKTb4njsR5whvwUwicEWU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert a link to the song currently playing on your Spotify.'';
            homepage = "https://github.com/Cutaiar/obsidian-song-links";
        };
    }
);
    "spreadsheets" = (
    let
        baseUrl = "https://github.com/divamgupta/obsidian-spreadsheets/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "spreadsheets";
        version = "1.0.1";
        outputHash = "sha256-wJn0WKlf6UD46gft26gAr2mSiZg0YkMRTq6eSqCJRmg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and edit advanced spreadsheets. Rich formatting, formulas, cells, filtering and more!'';
            homepage = "https://github.com/divamgupta/obsidian-spreadsheets";
        };
    }
);
    "sqlite-db" = (
    let
        baseUrl = "https://github.com/stfrigerio/sqliteDB/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sqlite-db";
        version = "1.0.3";
        outputHash = "sha256-wS2EApTi95c84UD+ob+8fDdQKj5p11dX3lDPqn4KuXg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interact with local SQLite files in your notes'';
            homepage = "https://github.com/stfrigerio/sqliteDB";
        };
    }
);
    "sqlseal" = (
    let
        baseUrl = "https://github.com/h-sphere/sql-seal/releases/download/0.40.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sqlseal";
        version = "0.40.0";
        outputHash = "sha256-juQXtErnHvbGlhbDx9ZMfFLpStMkTEW3JRR9JFMbuWQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use SQL in your notes to query your vault files and CSV content'';
            homepage = "https://github.com/h-sphere/sql-seal";
        };
    }
);
    "sqlseal-charts" = (
    let
        baseUrl = "https://github.com/h-sphere/sql-seal-charts/releases/download/0.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sqlseal-charts";
        version = "0.4.2";
        outputHash = "sha256-KBdEJsVNK5irrSJGxk2tNX06KMDLR1yPt0mT3tcJSj4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate charts from your data. Graph line charts, pie charts, scatter plots and more.'';
            homepage = "https://github.com/h-sphere/sql-seal-charts";
        };
    }
);
    "squiggle" = (
    let
        baseUrl = "https://github.com/jqhoogland/obsidian-squiggle/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "squiggle";
        version = "0.1.2";
        outputHash = "sha256-v1c2YH7d8BMUjX8bOz/Myp0IWW+bfbdfKKcnsND4DZ8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Probabilistic estimation with Squiggle in your notes.'';
            homepage = "https://github.com/jqhoogland/obsidian-squiggle";
        };
    }
);
    "standardform" = (
    let
        baseUrl = "https://github.com/philphilphil/obsidian-standardform/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "standardform";
        version = "1.2.0";
        outputHash = "sha256-tskEokNS/mYI/pO6Djo6Yt2HIJwPYBHho29dM9gfS+Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transforms code blocks with Standard Form logical syntax into clean, styled renderings. Perfect for philosophy, logic, and argument reconstructions.'';
            homepage = "https://github.com/philphilphil/obsidian-standardform";
        };
    }
);
    "start-page" = (
    let
        baseUrl = "https://github.com/kuzzh/obsidian-startpage/releases/download/0.3.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "start-page";
        version = "0.3.6";
        outputHash = "sha256-Zlru7P9l6vrwOpFG4AEXsRQCoMopAwKw4Tb75PzVDYA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically opens a customized homepage upon startup, displaying pinned and recent notes.'';
            homepage = "https://github.com/kuzzh/obsidian-startpage";
        };
    }
);
    "stashpad-docs" = (
    let
        baseUrl = "https://github.com/stashpad/obsidian-to-stashpad/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "stashpad-docs";
        version = "1.0.1";
        outputHash = "sha256-ayRXz/liCiOPlz3PxrNQanX2COgUVOHwJo5g4BHsNR0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a Stashpad Doc from your notes.'';
            homepage = "https://github.com/stashpad/obsidian-to-stashpad";
        };
    }
);
    "statblock-sidekick" = (
    let
        baseUrl = "https://github.com/n21rl/stablock-sidekick/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "statblock-sidekick";
        version = "1.0.3";
        outputHash = "sha256-QQPyPaD2LwUMJhNvGETm1XpogqU6X3B5hUB5d20li2I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and expand D&D 5e statblocks.'';
            homepage = "https://github.com/n21rl/stablock-sidekick";
        };
    }
);
    "status-bar-clock" = (
    let
        baseUrl = "https://github.com/marty331/status-bar-clock-obsidian/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "status-bar-clock";
        version = "1.0.10";
        outputHash = "sha256-sj+IVmt7nrupxUJF4vzP4f26XJPXVN+3g3p5MWemt5s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Status bar clock.'';
            homepage = "https://github.com/marty331/status-bar-clock-obsidian";
        };
    }
);
    "status-bar-quote" = (
    let
        baseUrl = "https://github.com/yesjinu/StatusBarQuote/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "status-bar-quote";
        version = "0.1.0";
        outputHash = "sha256-VwiNRyD8IwRnJRcFD8mrPXCDskC7+Oy2Szr3xKEbifY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show your favorite quote in the status bar.'';
            homepage = "https://github.com/yesjinu/StatusBarQuote";
        };
    }
);
    "statusbar-organizer" = (
    let
        baseUrl = "https://github.com/Opisek/obsidian-statusbar-organizer/releases/download/2.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "statusbar-organizer";
        version = "2.1.5";
        outputHash = "sha256-DCWKqXNbgsZnjYjoEq4mHpCgH9zIjcm4x9dIM1r/hak=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rearrange and hide status bar elements.'';
            homepage = "https://github.com/Opisek/obsidian-statusbar-organizer";
        };
    }
);
    "statuslol-post" = (
    let
        baseUrl = "https://github.com/ericmwalk/obsidian-omglol-publisher/releases/download/2.5.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "statuslol-post";
        version = "2.5.5";
        outputHash = "sha256-a7xceQiLsdkjKvdJr/yB88/hvU+QEtbzX4F8NKhvkOY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to post to weblogs.lol, status.lol, some.pics and paste.lol.'';
            homepage = "https://github.com/ericmwalk/obsidian-omglol-publisher";
        };
    }
);
    "stenography-obsidian" = (
    let
        baseUrl = "https://github.com/bramses/stenography-obsidian/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "stenography-obsidian";
        version = "1.0.3";
        outputHash = "sha256-61mbsoOEQwv9pytQuSmYmcl5K1cygmsmoFO46SLUlHc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Translate code blocks into simple English using Machine Learning with the Stenography API.'';
            homepage = "https://github.com/bramses/stenography-obsidian";
        };
    }
);
    "steward" = (
    let
        baseUrl = "https://github.com/googlicius/obsidian-steward/releases/download/2.7.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "steward";
        version = "2.7.3";
        outputHash = "sha256-30c04/yeKeegWb7xQWN6D0j8eZKP0vqSySKi3y6GFgs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An AI-powered assistant that helps you with your notes: Fast search, flexible commands, and chat with LLMs.'';
            homepage = "https://github.com/googlicius/obsidian-steward";
        };
    }
);
    "sticky-heading" = (
    let
        baseUrl = "https://github.com/imshenshen/obsidian-sticky-heading/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sticky-heading";
        version = "1.3.1";
        outputHash = "sha256-6DJxflfynzVuPDgx8HtTztyxq5APLB9iB+uqHdGduVA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sticky headings and shows the heading level'';
            homepage = "https://github.com/imshenshen/obsidian-sticky-heading";
        };
    }
);
    "sticky-notes" = (
    let
        baseUrl = "https://github.com/Abdo-reda/obsidian-sticky-notes-plugin/releases/download/0.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sticky-notes";
        version = "0.9.1";
        outputHash = "sha256-/wzmXXNZidhisuMvNIo0I9frFmBpouX8iYQpu3IYazE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create sticky notes popups.'';
            homepage = "https://github.com/Abdo-reda/obsidian-sticky-notes-plugin";
        };
    }
);
    "storyclock" = (
    let
        baseUrl = "https://github.com/jonzfisher/obsidian-chronostory/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "storyclock";
        version = "1.0.2";
        outputHash = "sha256-u82GsEkW+I6+PaziGTt9baPMZ2miACdWbiVAgDAob14=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Map timing onto a storyclock. Inspired by Plot Devices Storyclock.'';
            homepage = "https://github.com/jonzfisher/obsidian-chronostory";
        };
    }
);
    "storyteller-suite" = (
    let
        baseUrl = "https://github.com/Maws7140/obsidian-storyteller-suite/releases/download/1.7.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "storyteller-suite";
        version = "1.7.8";
        outputHash = "sha256-Mi43MC0ztqzBC0qqz8MqIXy2CgR7k+75JKeZkbl4k/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A comprehensive suite for managing characters, locations, events, and galleries for your stories.'';
            homepage = "https://github.com/Maws7140/obsidian-storyteller-suite";
        };
    }
);
    "strava-sync" = (
    let
        baseUrl = "https://github.com/watsonbox/obsidian-strava-sync/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "strava-sync";
        version = "1.1.0";
        outputHash = "sha256-R1M245Hrk7iUZIC7gG5DKF08KhlVZ/OLFRHLoWO61V4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync activities from Strava.'';
            homepage = "https://github.com/watsonbox/obsidian-strava-sync";
        };
    }
);
    "streams" = (
    let
        baseUrl = "https://github.com/bfloydd/streams/releases/download/1.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "streams";
        version = "1.2.9";
        outputHash = "sha256-IhVpy5ktsYTlFFbejmHKIzJJtZQ1kHdIA1q01dhIb7g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and manage multiple Daily Note streams'';
            homepage = "https://github.com/bfloydd/streams";
        };
    }
);
    "structured-tree" = (
    let
        baseUrl = "https://github.com/Rudtrack/structured-tree/releases/download/2.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "structured-tree";
        version = "2.6.0";
        outputHash = "sha256-yO3Qzm56HU7fLHDGc/hDmKbrJqPbzgdul0mwDsQ1CVk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A file explorer for navigating hierarchical notes separated by "."'';
            homepage = "https://github.com/Rudtrack/structured-tree";
        };
    }
);
    "stu-repo-helper" = (
    let
        baseUrl = "https://github.com/yingflower/obsidian-stu-repo-helper/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "stu-repo-helper";
        version = "0.1.1";
        outputHash = "sha256-u7VQS+rSspj4DE8D/3cLX4AabHwIg/z7lODqRJ6zPmE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage student repositories.'';
            homepage = "https://github.com/yingflower/obsidian-stu-repo-helper";
        };
    }
);
    "style-importer" = (
    let
        baseUrl = "https://github.com/joshrouwhorst/style-importer/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "style-importer";
        version = "1.0.4";
        outputHash = "sha256-7odv5ekBAM3ba9ocAL/pcEPcZBlI140ibJErnH7Nqw4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import a stylesheet from a URL into your snippets folder.'';
            homepage = "https://github.com/joshrouwhorst/style-importer";
        };
    }
);
    "style-text" = (
    let
        baseUrl = "https://github.com/juanjoarranz/style-text-obsidian-plugin/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "style-text";
        version = "0.1.0";
        outputHash = "sha256-LsmcOSCfbkAhaKX/MMwZdEGrQY6Rpx+j92oJX4xyx3Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Apply custom CSS styles to selected text in your notes.'';
            homepage = "https://github.com/juanjoarranz/style-text-obsidian-plugin";
        };
    }
);
    "subdivider" = (
    let
        baseUrl = "https://github.com/MediosZ/obsidian-subdivider/releases/download/0.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "subdivider";
        version = "0.1.7";
        outputHash = "sha256-+6eEz0XcpsAkMo/dnbGBo6ei6nyRbRz6GUCL5axr5c8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert your notes into nested folders, and automatically create separate files for each subheading.'';
            homepage = "https://github.com/MediosZ/obsidian-subdivider";
        };
    }
);
    "substitutions" = (
    let
        baseUrl = "https://github.com/BambusControl/obsidian-substitutions/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "substitutions";
        version = "0.6.0";
        outputHash = "sha256-m2wOjkjSKkmZEehS0Muwx4JkIu3qR/NpToEDvFY5ohk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically replace text fragments with symbols or different text'';
            homepage = "https://github.com/BambusControl/obsidian-substitutions";
        };
    }
);
    "suggest-notes" = (
    let
        baseUrl = "https://github.com/Doggy-Footprint/Suggest-Notes/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "suggest-notes";
        version = "1.0.7";
        outputHash = "sha256-LDznGfgm/xejOC8u4zImBk103MjFQC7kFIaR0ZK5XJA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Suggest notes as selected by user.'';
            homepage = "https://github.com/Doggy-Footprint/Suggest-Notes";
        };
    }
);
    "super-duper-audio-recorder" = (
    let
        baseUrl = "https://github.com/madpin/super-duper-audio-recorder/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "super-duper-audio-recorder";
        version = "1.1.1";
        outputHash = "sha256-N5gKjG9sG4u4j77LCC8D/NUHO+ImS57Tki31AMo0Ufc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Records audio directly, with input device and folder configuration, similar to the core one, but better'';
            homepage = "https://github.com/madpin/super-duper-audio-recorder";
        };
    }
);
    "supercharged-links-obsidian" = (
    let
        baseUrl = "https://github.com/mdelobelle/obsidian_supercharged_links/releases/download/0.13.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "supercharged-links-obsidian";
        version = "0.13.10";
        outputHash = "sha256-Hw3cdMRHmx++/tjKqpPSW88ofx6tBpGWsmqD9ogersY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add attributes to internal links with the values of target note"s frontmatter attributes.'';
            homepage = "https://github.com/mdelobelle/obsidian_supercharged_links";
        };
    }
);
    "supernote" = (
    let
        baseUrl = "https://github.com/philips/supernote-obsidian-plugin/releases/download/2.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "supernote";
        version = "2.9.1";
        outputHash = "sha256-8tF5OK/wPejHOSsrTuWD/jwHG4WgzkcNGroCkEMWZ1w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View Supernote notes, generate markdown from note and capture screen mirror.'';
            homepage = "https://github.com/philips/supernote-obsidian-plugin";
        };
    }
);
    "superstition" = (
    let
        baseUrl = "https://github.com/shuxueshuxue/superstition/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "superstition";
        version = "1.0.2";
        outputHash = "sha256-ta087H+6Ro3MMff5Z8Z7O/7ibLEUtuKSH34JVfA8I04=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage routines with traditional calendar concept of 宜/忌.'';
            homepage = "https://github.com/shuxueshuxue/superstition";
        };
    }
);
    "supsub" = (
    let
        baseUrl = "https://github.com/wjgoarxiv/obsidian-supsub/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "supsub";
        version = "1.1.1";
        outputHash = "sha256-OMDg1c6YId2qAsEyqW8+domFBgVJZi5cnmGoorB7X48=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily format superscripts and subscripts in your notes.'';
            homepage = "https://github.com/wjgoarxiv/obsidian-supsub";
        };
    }
);
    "surfing" = (
    let
        baseUrl = "https://github.com/PKM-er/Obsidian-Surfing/releases/download/0.9.14";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "surfing";
        version = "0.9.14";
        outputHash = "sha256-hdmM0bJ/udNqgqBmqJJY7lsrq4pkX57CLgNXsH4I3RQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Surf the net like a web browser.'';
            homepage = "https://github.com/PKM-er/Obsidian-Surfing";
        };
    }
);
    "svelte-syntax-highlighter" = (
    let
        baseUrl = "https://github.com/typhoon-kim/obsidian-svelte-syntax-highlighter/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "svelte-syntax-highlighter";
        version = "1.0.3";
        outputHash = "sha256-GMhgD3TA1BPqmVih0Y3DDbpHyKpYSjTX0ZoBwvOabkE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syntax highlighting for Svelte code blocks.'';
            homepage = "https://github.com/typhoon-kim/obsidian-svelte-syntax-highlighter";
        };
    }
);
    "svg-styler" = (
    let
        baseUrl = "https://github.com/arg1998/obsidian-svg-styler/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "svg-styler";
        version = "1.1.2";
        outputHash = "sha256-2w+rYPYQv1F8Z2dPr8bezAt1CZ/fE4xLscO7dLjReTo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Change the color and other style properies of an embded SVG file'';
            homepage = "https://github.com/arg1998/obsidian-svg-styler";
        };
    }
);
    "swiftlatex-render" = (
    let
        baseUrl = "https://github.com/gboyd068/obsidian-swiftlatex-render/releases/download/0.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "swiftlatex-render";
        version = "0.6.1";
        outputHash = "sha256-+afox8SHCz0qb0HFRFAypEBhgdP9QNBG8hWynVE5vMQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render LaTeX in codeblocks into a PDF, without needing to install LaTeX separately.'';
            homepage = "https://github.com/gboyd068/obsidian-swiftlatex-render";
        };
    }
);
    "swiss-army-knife" = (
    let
        baseUrl = "https://github.com/mwoz123/swiss-army-knife-obsidian/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "swiss-army-knife";
        version = "0.9.0";
        outputHash = "sha256-OJEaiRNP9vUViOKb6k7WW6S971bAg4u7DCvowgb8MZo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collection of various utilities (e.g. duplicate empty line remover).'';
            homepage = "https://github.com/mwoz123/swiss-army-knife-obsidian";
        };
    }
);
    "symbol-linking" = (
    let
        baseUrl = "https://github.com/Mara-Li/obsidian-symbol-linking/releases/download/1.4.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "symbol-linking";
        version = "1.4.3";
        outputHash = "sha256-gpnLG5UA90wLhemGMDOA9TYddn/BrOA7W14K3VLzKag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds ability to link with any trigger. Each trigger can limit linking to specific folders or file.'';
            homepage = "https://github.com/Mara-Li/obsidian-symbol-linking";
        };
    }
);
    "symbols-prettifier" = (
    let
        baseUrl = "https://github.com/FlorianWoelki/obsidian-symbols-prettifier/releases/download/1.2.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "symbols-prettifier";
        version = "1.2.4";
        outputHash = "sha256-bfgsxLfVSeTb8yVLOR94eC5jPagjYVtaYTASlmGwpHg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Prettify the symbols with actual symbols you commonly type, like arrows.'';
            homepage = "https://github.com/FlorianWoelki/obsidian-symbols-prettifier";
        };
    }
);
    "symlink-creator" = (
    let
        baseUrl = "https://github.com/pteridin/obsidian_symlink_plugin/releases/download/0.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "symlink-creator";
        version = "0.1.3";
        outputHash = "sha256-MY1NJ9vbsHAh4pymoNwHbRKZX8cCsaqv8vAt9mck/0k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create symlinks to files and folders inside and outside of your vault.'';
            homepage = "https://github.com/pteridin/obsidian_symlink_plugin";
        };
    }
);
    "synaptic-view" = (
    let
        baseUrl = "https://github.com/especialkim/obsidian-synaptic-view/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "synaptic-view";
        version = "0.1.5";
        outputHash = "sha256-7J4AWQ2x7O7XYmaQQDpgNoiyz0gDvPgLOHOSHPybNTU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A dynamic control center for your vault. Unify hubs, notes, tasks, periodic notes, and web resources with intuitive buttons. Replace new tab for instant access.'';
            homepage = "https://github.com/especialkim/obsidian-synaptic-view";
        };
    }
);
    "sync-cnblog" = (
    let
        baseUrl = "https://github.com/lei-ctyh/obsidian-sync-cnblog/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-cnblog";
        version = "1.0.0";
        outputHash = "sha256-X3HQqbhR19Roa7/ZPhLcESbJDBzhrNtgQGi30xSEvyI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''将笔记同步到博客园'';
            homepage = "https://github.com/lei-ctyh/obsidian-sync-cnblog";
        };
    }
);
    "sync-config-folder-to-common-folder" = (
    let
        baseUrl = "https://github.com/codeonquer/obsidian-sync-config-folder-to-common-folder/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-config-folder-to-common-folder";
        version = "1.0.3";
        outputHash = "sha256-4nV52xQFZVY0n0W7gMHoNvSZFVp5tX0neJtL9sRZTwA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync contents from config folder to common folder for backup or other purposes.'';
            homepage = "https://github.com/codeonquer/obsidian-sync-config-folder-to-common-folder";
        };
    }
);
    "sync-contacts-macos" = (
    let
        baseUrl = "https://github.com/motschel123/Mac-Contact-Sync-Obsidian/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-contacts-macos";
        version = "1.0.7";
        outputHash = "sha256-0kdVyg3962cyYxZl7GAxNUcPIciX7exxRwsgjaazO0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your macOS contacts.'';
            homepage = "https://github.com/motschel123/Mac-Contact-Sync-Obsidian";
        };
    }
);
    "sync-db-os" = (
    let
        baseUrl = "https://github.com/ketd/obsidian-sync-DB-OS/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-db-os";
        version = "1.0.2";
        outputHash = "sha256-1R3730SlA+BHIVs2S4z1RA27VlJDlH0dC/U4LeZYl0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''For synchronization between multiple platforms'';
            homepage = "https://github.com/ketd/obsidian-sync-DB-OS";
        };
    }
);
    "sync-google-calendar" = (
    let
        baseUrl = "https://github.com/dexin-qi/obsidian-sync-calendar/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-google-calendar";
        version = "1.1.6";
        outputHash = "sha256-+9A945Gp/VkW2lo5mbpn2fwE2eAy7jXIY9MX0CeON10=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronize events from Google Calendar and manage them like tasks.'';
            homepage = "https://github.com/dexin-qi/obsidian-sync-calendar";
        };
    }
);
    "sync-graph-settings" = (
    let
        baseUrl = "https://github.com/Xallt/sync-graph-settings/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-graph-settings";
        version = "1.5.0";
        outputHash = "sha256-nmc4QCDcR3NH6lqiiHZNbdHJ2t5Qaj4Sbqx4LGU+PXQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync global graph settings (like color groups) to local graphs.'';
            homepage = "https://github.com/Xallt/sync-graph-settings";
        };
    }
);
    "sync-safe-file-names" = (
    let
        baseUrl = "https://github.com/j-maas/sync-safe-file-names/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-safe-file-names";
        version = "1.0.3";
        outputHash = "sha256-g2Q9nOzu4WhaybD6DgwPq3VvyGyJrWVG8DwEnsSWac4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Ensures all file names can be synced accross all platforms.'';
            homepage = "https://github.com/j-maas/sync-safe-file-names";
        };
    }
);
    "sync-to-xlog" = (
    let
        baseUrl = "https://github.com/Otto-J/sync-to-xlog/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-to-xlog";
        version = "0.2.1";
        outputHash = "sha256-Z79nHROY76zXYJFAfJggWmFXJZmF6mvcZ0BGPgDd30w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Push notes to xlog.app.'';
            homepage = "https://github.com/Otto-J/sync-to-xlog";
        };
    }
);
    "sync-vault-ce" = (
    let
        baseUrl = "https://github.com/abcamus/obsidian-sync-vault-ce/releases/download/1.13.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "sync-vault-ce";
        version = "1.13.1";
        outputHash = "sha256-ktkn63bD1BqUVeaTki1NHE1D9mC0xFsVKfbgt8nlB10=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''sync with cloud disk fast and safely.'';
            homepage = "https://github.com/abcamus/obsidian-sync-vault-ce";
        };
    }
);
    "syncftp" = (
    let
        baseUrl = "https://github.com/alex-donnan/SyncFTP/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "syncftp";
        version = "1.3.1";
        outputHash = "sha256-q7bg9ONjy4dkg2N71GlC4T5fLZo6DlqJVyV/6pb8C70=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Connect to an SFTP and push/pull file changes to it.'';
            homepage = "https://github.com/alex-donnan/SyncFTP";
        };
    }
);
    "syncread" = (
    let
        baseUrl = "https://github.com/flyer1b/LightRead-master/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "syncread";
        version = "1.1.4";
        outputHash = "sha256-Z17Rw57NIowQbv5QjiY59qsh4pW1yqtg88Jf5QV0Zso=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync articles from SyncRead.'';
            homepage = "https://github.com/flyer1b/LightRead-master";
        };
    }
);
    "syncthing-integration" = (
    let
        baseUrl = "https://github.com/LBF38/obsidian-syncthing-integration/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "syncthing-integration";
        version = "2.4.0";
        outputHash = "sha256-5hbhrsXZO+3zkv1kLP5dcg/OjCmVdXZNSf/ua4EfcVw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncthing integration.'';
            homepage = "https://github.com/LBF38/obsidian-syncthing-integration";
        };
    }
);
    "syncthing-status-icon" = (
    let
        baseUrl = "https://github.com/Diego-Viero/Syncthing-status-icon-Obsidian-plugin/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "syncthing-status-icon";
        version = "1.1.2";
        outputHash = "sha256-fUJ723Ep1Vdq2onh8ES8rV9ht0OOZPiWcSHG0y0bIHw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Displays a status icon in the status bar showing whether Syncthing is running locally or not.'';
            homepage = "https://github.com/Diego-Viero/Syncthing-status-icon-Obsidian-plugin";
        };
    }
);
    "syrinscape-player-control" = (
    let
        baseUrl = "https://github.com/scooper4711/obsidian-syrinscape/releases/download/1.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "syrinscape-player-control";
        version = "1.9.0";
        outputHash = "sha256-RF6QtHXqmd25xgHjhSk/2yfWRcjEy3CbNHz2A+bDHvg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Control Syrinscape Online Player from inside of notes.'';
            homepage = "https://github.com/scooper4711/obsidian-syrinscape";
        };
    }
);
    "system3-relay" = (
    let
        baseUrl = "https://github.com/No-Instructions/Relay/releases/download/0.8.0-rc3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "system3-relay";
        version = "0.8.0-rc3";
        outputHash = "sha256-rq+v+uqlGGVz0Vwi0cC40GyBja4THVdVO+ENwOCqyak=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Collaborate in real time with live cursors. Share folders. Manage access to updates.'';
            homepage = "https://github.com/No-Instructions/Relay";
        };
    }
);
    "systemsculpt-ai" = (
    let
        baseUrl = "https://github.com/SystemSculpt/obsidian-systemsculpt-ai/releases/download/5.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "systemsculpt-ai";
        version = "5.6.0";
        outputHash = "sha256-oVtldF7rOI7D/HnBQp98ohqlrb/yPzHF0zIWDfj2iog=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhance your data flow with AI-powered tools for note-taking, task management, templates, and so much more.'';
            homepage = "https://github.com/SystemSculpt/obsidian-systemsculpt-ai";
        };
    }
);
    "t4-task-tree-time-totaler" = (
    let
        baseUrl = "https://github.com/estory1/t4-task-tree-time-totaler/releases/download/20250416.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "t4-task-tree-time-totaler";
        version = "20250416.0.0";
        outputHash = "sha256-4EujPca9JRJOCCdFZi0By1KUxkiCTEjrqu7equHuWLI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Calculates and assigns task estimates given subtask estimates in a task tree in a Markdown document.'';
            homepage = "https://github.com/estory1/t4-task-tree-time-totaler";
        };
    }
);
    "tab-file-path" = (
    let
        baseUrl = "https://github.com/johnburnett/obsidian-tab-file-path/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-file-path";
        version = "1.1.0";
        outputHash = "sha256-PBG8RZLHDpP8fqbfOoSTCEXMzhoW606kl5ocSSlYN5o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Shows file paths in tabs'';
            homepage = "https://github.com/johnburnett/obsidian-tab-file-path";
        };
    }
);
    "tab-group-arrangement" = (
    let
        baseUrl = "https://github.com/situ2001/obsidian-tab-group-arrangement/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-group-arrangement";
        version = "1.0.1";
        outputHash = "sha256-gBydo/9SiV31Nxe9DOeS+IrQEgVNxm69jjflRtlItYU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Arrange the tab group in a more flexible way. For now, it supports arranging evenly and expanded like VSCode. User can make action and switch mode of arrangement by clicking the status bar or executing commands.'';
            homepage = "https://github.com/situ2001/obsidian-tab-group-arrangement";
        };
    }
);
    "tab-limit" = (
    let
        baseUrl = "https://github.com/lizard-heart/obsidian-tab-limit/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-limit";
        version = "1.0.1";
        outputHash = "sha256-7KMOMbv0OpsPWHg4eOvUZMCSuOf7p/68RpLGgvTts/4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Limits the number of tabs that can be opened.'';
            homepage = "https://github.com/lizard-heart/obsidian-tab-limit";
        };
    }
);
    "tab-navigator" = (
    let
        baseUrl = "https://github.com/o02c/obsidian-tab-navigator/releases/download/0.7.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-navigator";
        version = "0.7.4";
        outputHash = "sha256-7F7b5l/t5eZ/V0QeLrmvc7+sgPm1Pqrk7KyZbSQl9NY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple Tab Switcher, search open tabs.'';
            homepage = "https://github.com/o02c/obsidian-tab-navigator";
        };
    }
);
    "tab-panels" = (
    let
        baseUrl = "https://github.com/GnoxNahte/obsidian-tab-panels/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-panels";
        version = "1.2.3";
        outputHash = "sha256-IFks046Nl7YdLObnxk25RjdKzcOw58G6+bjEPATSWks=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create tab panels to organize content into sections'';
            homepage = "https://github.com/GnoxNahte/obsidian-tab-panels";
        };
    }
);
    "tab-rotator" = (
    let
        baseUrl = "https://github.com/autohub7/obsidian-tab-rotator/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-rotator";
        version = "1.1.0";
        outputHash = "sha256-mB/LoNxmbPtnXnYxrt1KD7OjwiYzjYC0soJaYLLcYU0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rotate opened files to the left or right with a specified interval.'';
            homepage = "https://github.com/autohub7/obsidian-tab-rotator";
        };
    }
);
    "tab-selector" = (
    let
        baseUrl = "https://github.com/namikaze-40p/obsidian-tab-selector/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-selector";
        version = "0.9.0";
        outputHash = "sha256-XYM8FhDRXtBTE8tGRhJt4fIiyudM9QQ5igBq7cbq/Lo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly switch tabs in various ways.'';
            homepage = "https://github.com/namikaze-40p/obsidian-tab-selector";
        };
    }
);
    "tab-shifter" = (
    let
        baseUrl = "https://github.com/jsrozner/obsidian-tab-shifter/releases/download/2.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tab-shifter";
        version = "2.0.4";
        outputHash = "sha256-9XaPWjnyALBxYOFLt5fUaXIUIEKMAAjt25k6CzNAhzU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables shifting tabs between different tab splits'';
            homepage = "https://github.com/jsrozner/obsidian-tab-shifter";
        };
    }
);
    "table-checkbox-renderer" = (
    let
        baseUrl = "https://github.com/dannns/obsidian-table-checkbox-renderer/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-checkbox-renderer";
        version = "1.0.2";
        outputHash = "sha256-DDz0expAy49Qld5jSyG/iIePv7f+D7eDdIP/A3nsjCM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interactive checkboxes for Markdown tables. Toggle checkboxes in Reading Mode to instantly update the Markdown file. Supports multiple checkboxes per cell and any table layout.'';
            homepage = "https://github.com/dannns/obsidian-table-checkbox-renderer";
        };
    }
);
    "table-checkboxes" = (
    let
        baseUrl = "https://github.com/0x-DLN/obsidian-table-checkboxes/releases/download/3.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-checkboxes";
        version = "3.0.0";
        outputHash = "sha256-VXJDrhqoPyP3hZWFIBqGVIH9MdgPRjCTUgxgthb+tHM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add support for stateful checkboxes inside Markdown tables.'';
            homepage = "https://github.com/0x-DLN/obsidian-table-checkboxes";
        };
    }
);
    "table-editor-obsidian" = (
    let
        baseUrl = "https://github.com/tgrosinger/advanced-tables-obsidian/releases/download/0.22.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-editor-obsidian";
        version = "0.22.2";
        outputHash = "sha256-NzZD7vlmjXIE3N+8GMXoc7rWTJhJLLMvreVo6CzvgSg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improved table navigation, formatting, and manipulation.'';
            homepage = "https://github.com/tgrosinger/advanced-tables-obsidian";
        };
    }
);
    "table-extended" = (
    let
        baseUrl = "https://github.com/aidenlx/table-extended/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-extended";
        version = "1.6.1";
        outputHash = "sha256-KiyQPryk1zsjvSS2riKHduAL2MYR33KgBw8CixX2Ah0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable extended table support with MultiMarkdown 6 syntax.'';
            homepage = "https://github.com/aidenlx/table-extended";
        };
    }
);
    "table-line-break" = (
    let
        baseUrl = "https://github.com/marcelflymark/new-line-break-inside-table-cell/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-line-break";
        version = "1.0.1";
        outputHash = "sha256-XzkueXup4Y0CL/9OC5k4LZkftUsP4jTIRHVs/Ct+u6I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert line breaks into tables on mobile devices via shortcut or toolbar.'';
            homepage = "https://github.com/marcelflymark/new-line-break-inside-table-cell";
        };
    }
);
    "table-list" = (
    let
        baseUrl = "https://github.com/Akaswan/table-list/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-list";
        version = "1.0.0";
        outputHash = "sha256-lHsquH3qtlO4X5FgLVpQ2K6k6nMuAsnTmvjPsJuqiQc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a table view to manage your tasks.'';
            homepage = "https://github.com/Akaswan/table-list";
        };
    }
);
    "table-of-contents-automatic-but-compatible-with-publish" = (
    let
        baseUrl = "https://github.com/BrMacCath/Table-of-Contents/releases/download/3.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "table-of-contents-automatic-but-compatible-with-publish";
        version = "3.2.0";
        outputHash = "sha256-fEfg0HQCZcvFRKiL1BFuqxD0fb6ZLXlwdxhZ6TnFGgI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''This generates a table of contents that is compatible with publish.'';
            homepage = "https://github.com/BrMacCath/Table-of-Contents";
        };
    }
);
    "tabout" = (
    let
        baseUrl = "https://github.com/phibr0/obsidian-tabout/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tabout";
        version = "1.0.1";
        outputHash = "sha256-FxPJIBOoJoXNgyWqZQor8M0xoN55Y5ODctOrK2gJGmg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily "tab out" of links or other Markdown formatting characters.'';
            homepage = "https://github.com/phibr0/obsidian-tabout";
        };
    }
);
    "tabs" = (
    let
        baseUrl = "https://github.com/xhuajin/obsidian-tabs/releases/download/1.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tabs";
        version = "1.1.8";
        outputHash = "sha256-Puu6A8NiGfMXVz8df0ov1YDYc1KGpLN3yL1vObnDMbc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create tabs in your notes.'';
            homepage = "https://github.com/xhuajin/obsidian-tabs";
        };
    }
);
    "tag-breakdown-generator" = (
    let
        baseUrl = "https://github.com/HananoshikaYomaru/obsidian-tag-generator/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-breakdown-generator";
        version = "1.0.10";
        outputHash = "sha256-MW3U/+clAIhEK/7/XlkQJDRZ1refYy5nL9JaciGg5IU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Break down nested tags into multiple parent tags.'';
            homepage = "https://github.com/HananoshikaYomaru/obsidian-tag-generator";
        };
    }
);
    "tag-buddy" = (
    let
        baseUrl = "https://github.com/moremeyou/Obsidian-Tag-Buddy/releases/download/0.6.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-buddy";
        version = "0.6.8";
        outputHash = "sha256-ID++qgUxp4Hx4MiE0qHveJDVgrrZ0EAu1RAYU8eTkK4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add, edit, and remove tags in reading mode. Copy, move, or edit tagged blocks in reading and edit mode.'';
            homepage = "https://github.com/moremeyou/Obsidian-Tag-Buddy";
        };
    }
);
    "tag-group-manager" = (
    let
        baseUrl = "https://github.com/Stargazer-cc/obsidian-tag-group-manager/releases/download/1.8.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-group-manager";
        version = "1.8.12";
        outputHash = "sha256-YlCt7D0+8MVm9IoDryltJ7zFG3OEC6K7j934RPyfHd0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage tag groups and quickly insert tags.'';
            homepage = "https://github.com/Stargazer-cc/obsidian-tag-group-manager";
        };
    }
);
    "tag-index" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-tag-index/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-index";
        version = "0.7.0";
        outputHash = "sha256-JLrdLYGMEzR3r1r1QheigSj7kmHtKQt5jPhbUr/DOL4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create a curated list of important tags to serve as meaningful entry points to your knowledge base.'';
            homepage = "https://github.com/wenlzhang/obsidian-tag-index";
        };
    }
);
    "tag-links" = (
    let
        baseUrl = "https://github.com/zedseven/obsidian-tag-links/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-links";
        version = "1.0.4";
        outputHash = "sha256-WujSBaig5IlYP/i1tuFRfkHoexvclJTx0EOf2hGeU3M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open tags as links using a hotkey.'';
            homepage = "https://github.com/zedseven/obsidian-tag-links";
        };
    }
);
    "tag-many" = (
    let
        baseUrl = "https://github.com/joshua-martius/tagmany/releases/download/0.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-many";
        version = "0.2.7";
        outputHash = "sha256-+tJ7iggMgM9UeDeugCwZ/gB565qwiU2uA/lDUxHnKaw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add the same tag(s) to multiple notes in a folder (optionally including subfolders) at once.'';
            homepage = "https://github.com/joshua-martius/tagmany";
        };
    }
);
    "tag-page-md" = (
    let
        baseUrl = "https://github.com/mjsumpter/obsidian-tag-page/releases/download/2.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-page-md";
        version = "2.0.2";
        outputHash = "sha256-zV+vY/NevQAZi7oH5ShlDZiPjBZtuLKn+qsGSCotqwE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dynamically generate and update tag-specific pages, offering a consolidated view of each tag"s references across your vault.'';
            homepage = "https://github.com/mjsumpter/obsidian-tag-page";
        };
    }
);
    "tag-project-odaimoko" = (
    let
        baseUrl = "https://github.com/Odaimoko/tag-project/releases/download/0.12.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-project-odaimoko";
        version = "0.12.4";
        outputHash = "sha256-+FrlicjO4ZacRdx2E9LdX+38Yr3ZEaS4upyG8FvZuoo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A Project Management Tool: Tag tasks everywhere, Manage in One page.'';
            homepage = "https://github.com/Odaimoko/tag-project";
        };
    }
);
    "tag-summary-plugin" = (
    let
        baseUrl = "https://github.com/macrojd/tag-summary/releases/download/2.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-summary-plugin";
        version = "2.1.5";
        outputHash = "sha256-48JbBtWgtuQ8QeLCd+W5BnyEzQMZf+dMDg7MZy7ires=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create summaries with paragraphs or blocks of text that share the same tag(s).'';
            homepage = "https://github.com/macrojd/tag-summary";
        };
    }
);
    "tag-tactician" = (
    let
        baseUrl = "https://github.com/scottTomaszewski/obsidian-tag-tactician/releases/download/0.9.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-tactician";
        version = "0.9.3";
        outputHash = "sha256-W4E6GBuAAXw0VcKrU9YavfrDrwZDWdpu+t4brhgzwEQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tag operations and helpers'';
            homepage = "https://github.com/scottTomaszewski/obsidian-tag-tactician";
        };
    }
);
    "tag-timer" = (
    let
        baseUrl = "https://github.com/quantavil/Tag-Timer/releases/download/2.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-timer";
        version = "2.2.0";
        outputHash = "sha256-6BoYCK7q48cNgbEjd6fVr/h+HnMZTi2csY4GJXMJZAg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a timer to Tags and get analytics on how long you spend on each task.'';
            homepage = "https://github.com/quantavil/Tag-Timer";
        };
    }
);
    "tag-word-cloud" = (
    let
        baseUrl = "https://github.com/joethei/obsidian-tagcloud/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-word-cloud";
        version = "1.5.0";
        outputHash = "sha256-c9dpxuasxcmcWLQdzKzT2KeWFfTBwylANw52z+WSJpk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show a cloud of your tags, words, or links.'';
            homepage = "https://github.com/joethei/obsidian-tagcloud";
        };
    }
);
    "tag-wrangler" = (
    let
        baseUrl = "https://github.com/pjeby/tag-wrangler/releases/download/0.6.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tag-wrangler";
        version = "0.6.4";
        outputHash = "sha256-s85VVAqn6Ob0iz2VsbuRAqJGv4uYV/VNMzmeX1jdsA4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename, merge, toggle, and search tags from the tag pane.'';
            homepage = "https://github.com/pjeby/tag-wrangler";
        };
    }
);
    "tags-overview" = (
    let
        baseUrl = "https://github.com/christianwannerstedt/obsidian-tags-overview/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tags-overview";
        version = "1.0.4";
        outputHash = "sha256-FgTcRUHq0An8gLy/ZrtZJ2nQ7RS8IFW5zA5E2BGUhSE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An extended tags panel where tagged files can be easily viewed, filtered, and accessed.'';
            homepage = "https://github.com/christianwannerstedt/obsidian-tags-overview";
        };
    }
);
    "tags-routes" = (
    let
        baseUrl = "https://github.com/kctekn/obsidian-TagsRoutes/releases/download/1.2.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tags-routes";
        version = "1.2.3";
        outputHash = "sha256-8OmX2dg+KQoi/xyg7X+WczlgttKrlmHfirxipQG+Cpw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A powerful 3D graph viewer for managing and navigating notes and tags, with advanced visual effects, animations.'';
            homepage = "https://github.com/kctekn/obsidian-TagsRoutes";
        };
    }
);
    "tailwind-snippet" = (
    let
        baseUrl = "https://github.com/nicholas-wilcox/tailwind-snippet-obsidian-plugin/releases/download/0.6.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tailwind-snippet";
        version = "0.6.4";
        outputHash = "sha256-0/6UmUI7Nq7zubLNlZfu577o5ZvbCnxRW4MZJDOi5/I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use TailwindCSS utility classes in your markup.'';
            homepage = "https://github.com/nicholas-wilcox/tailwind-snippet-obsidian-plugin";
        };
    }
);
    "target-word-count" = (
    let
        baseUrl = "https://github.com/twofive-labs/target-word-count/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "target-word-count";
        version = "1.0.9";
        outputHash = "sha256-AAV0/CQDmvDpouRgvzqAbIumkSQOhSmO6c5RnVpr4oc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Disable editing until you"ve added a target number of words.'';
            homepage = "https://github.com/twofive-labs/target-word-count";
        };
    }
);
    "tars" = (
    let
        baseUrl = "https://github.com/TarsLab/obsidian-tars/releases/download/3.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tars";
        version = "3.5.2";
        outputHash = "sha256-ab3oe3myXV6ZcwVbfyCQEM20gHrKYvHFb7gf1clItA4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Text generation based on tag suggestions, using Claude, OpenAI, Ollama, Kimi, Doubao, Qwen, Zhipu, DeepSeek, QianFan & more.'';
            homepage = "https://github.com/TarsLab/obsidian-tars";
        };
    }
);
    "task-board" = (
    let
        baseUrl = "https://github.com/tu2-atmanand/Task-Board/releases/download/1.10.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "task-board";
        version = "1.10.2";
        outputHash = "sha256-fOxDGk4LNVtwKEbDgLYieigwTEbWAIFtSqWaawy3xfI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage all your tasks throughout your vault from a single board and much more...'';
            homepage = "https://github.com/tu2-atmanand/Task-Board";
        };
    }
);
    "task-director" = (
    let
        baseUrl = "https://github.com/cybertramp/obsidian-task-director/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "task-director";
        version = "0.0.2";
        outputHash = "sha256-uB8KmpPOpYt632DU6geInsxFndMDzmZ03rnsPX2CX28=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage and change tasks targeting a specific page'';
            homepage = "https://github.com/cybertramp/obsidian-task-director";
        };
    }
);
    "task-list" = (
    let
        baseUrl = "https://github.com/ted-marozzi/task-list/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "task-list";
        version = "1.0.1";
        outputHash = "sha256-pSZTvLjfmBzxv0ZCXuqSacGxIIIIuPI0PxvYmF8XhA8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable better task management via lists.'';
            homepage = "https://github.com/ted-marozzi/task-list";
        };
    }
);
    "task-list-kanban" = (
    let
        baseUrl = "https://github.com/ErikaRS/task-list-kanban/releases/download/1.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "task-list-kanban";
        version = "1.11.0";
        outputHash = "sha256-e8c1Zcmb2M6VHsKVri0ghtOfEXW4/vwljR37f8sF2i8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Organizes all of the tasks within your files into a kanban view.'';
            homepage = "https://github.com/ErikaRS/task-list-kanban";
        };
    }
);
    "task-mover" = (
    let
        baseUrl = "https://github.com/nemariia/task-mover/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "task-mover";
        version = "1.0.0";
        outputHash = "sha256-TttGYqNcbVf4olgpx14bYjokgl35LBP8J8donvtwImk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Move unfinished tasks to the daily note automatically'';
            homepage = "https://github.com/nemariia/task-mover";
        };
    }
);
    "task-status" = (
    let
        baseUrl = "https://github.com/vburzynski/obsidian-task-status/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "task-status";
        version = "1.2.1";
        outputHash = "sha256-q4CzsiHEOUKdbZj7yEQ8MG+6bEDFxj0EOjYNFn3NZAE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly change checkbox and task status markers.'';
            homepage = "https://github.com/vburzynski/obsidian-task-status";
        };
    }
);
    "taskbone-ocr-plugin" = (
    let
        baseUrl = "https://github.com/schlundd/obsidian-ocr-plugin/releases/download/2.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "taskbone-ocr-plugin";
        version = "2.3.2";
        outputHash = "sha256-6IiPvYXsKt9Hoda4iERCMSR0OVoN7+Nb9X5cjElVC0g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract text and equations from images and PDFs and make it available for search.'';
            homepage = "https://github.com/schlundd/obsidian-ocr-plugin";
        };
    }
);
    "tasknotes" = (
    let
        baseUrl = "https://github.com/callumalpass/tasknotes/releases/download/4.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tasknotes";
        version = "4.5.2";
        outputHash = "sha256-hhRzLMTCiw4zDUYCJbXUHtBIWmiI6Jy27amD0B1xycA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Note-based task management with calendar, pomodoro and time-tracking integration.'';
            homepage = "https://github.com/callumalpass/tasknotes";
        };
    }
);
    "taskrobin" = (
    let
        baseUrl = "https://github.com/taskrobin/Obsidian-Plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "taskrobin";
        version = "1.2.0";
        outputHash = "sha256-GZK+K4aUm65KButuMGIMpwVYuDW/VWd35tXDTfCDX7Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync and save emails and attachment files to your vault with TaskRobin.'';
            homepage = "https://github.com/taskrobin/Obsidian-Plugin";
        };
    }
);
    "tasks-calendar-wrapper" = (
    let
        baseUrl = "https://github.com/Leonezz/obsidian-tasks-calendar-wrapper/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tasks-calendar-wrapper";
        version = "0.8.0";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Simple wrapper for Tasks Calendar and Tasks Timeline.'';
            homepage = "https://github.com/Leonezz/obsidian-tasks-calendar-wrapper";
        };
    }
);
    "tasks-cleaner" = (
    let
        baseUrl = "https://github.com/lowitea/obsidian-tasks-cleaner/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tasks-cleaner";
        version = "1.0.2";
        outputHash = "sha256-m5sxd1UzpqzI8mCTzBBaFwq99P22MRpTsjh0WUA2DrI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find and remove outdated tasks.'';
            homepage = "https://github.com/lowitea/obsidian-tasks-cleaner";
        };
    }
);
    "tasks-map" = (
    let
        baseUrl = "https://github.com/NicoKNL/tasks-map/releases/download/0.28.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tasks-map";
        version = "0.28.0";
        outputHash = "sha256-FVg5Lyy+enSVI2jRf/MmcY3xS3tc9970IlFunV5Z5hk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A visual graph view of your tasks.'';
            homepage = "https://github.com/NicoKNL/tasks-map";
        };
    }
);
    "tasks-packrat-plugin" = (
    let
        baseUrl = "https://github.com/therden/packrat/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tasks-packrat-plugin";
        version = "1.1.2";
        outputHash = "sha256-7VzcRQk+dSfSWwIHsjyOnRlrfNQjcyIdfAuwyVqNPkI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage completed instances of recurring tasks that were created and completed using the Tasks plugin.'';
            homepage = "https://github.com/therden/packrat";
        };
    }
);
    "tasks-to-omnifocus" = (
    let
        baseUrl = "https://github.com/lizard-heart/obsidian-to-omnifocus/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tasks-to-omnifocus";
        version = "1.1.0";
        outputHash = "sha256-pS74eIUO7otBP/kahpnvtd+JCYWU3OGCWI5kL7nfhSE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract tasks from the current note and create them in OmniFocus.'';
            homepage = "https://github.com/lizard-heart/obsidian-to-omnifocus";
        };
    }
);
    "tekken-notation" = (
    let
        baseUrl = "https://github.com/OpTi9/obsidian-tekken-notation/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tekken-notation";
        version = "1.0.1";
        outputHash = "sha256-C4kbcVR5Z5oikmHfEa7MG9mzM3ikdh21+f8JdPSKBOg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Renders Tekken Notation.'';
            homepage = "https://github.com/OpTi9/obsidian-tekken-notation";
        };
    }
);
    "telegram-inbox" = (
    let
        baseUrl = "https://github.com/icealtria/obsidian-telegram-inbox/releases/download/1.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "telegram-inbox";
        version = "1.8.0";
        outputHash = "sha256-CvqnmUrK4LYpBsEL/BdNuDE2wtESqmgeQwBeloFPkaY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Receive messages from Telegram bot and add them to daily note.'';
            homepage = "https://github.com/icealtria/obsidian-telegram-inbox";
        };
    }
);
    "telegram-sync" = (
    let
        baseUrl = "https://github.com/soberhacker/obsidian-telegram-sync/releases/download/4.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "telegram-sync";
        version = "4.0.0";
        outputHash = "sha256-Nvf/IAlnsTmDmdSoav188n+FM36P0RociUUxQiBQ2KU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transfer messages and files from Telegram to Obsidian.'';
            homepage = "https://github.com/soberhacker/obsidian-telegram-sync";
        };
    }
);
    "teleprompter" = (
    let
        baseUrl = "https://github.com/lumetrium/obsidian-teleprompter/releases/download/1.4.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "teleprompter";
        version = "1.4.7";
        outputHash = "sha256-34h04BHO2VGdUjfVF3HUOCDYkCZ4PVLDweFe9KTjRdQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Teleprompter window for live presentations and video production.'';
            homepage = "https://github.com/lumetrium/obsidian-teleprompter";
        };
    }
);
    "template-by-note-name" = (
    let
        baseUrl = "https://github.com/jacoblearned/obsidian-template-by-note-name/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "template-by-note-name";
        version = "1.1.3";
        outputHash = "sha256-GsJzlgZ3fKKLmj2s61nrzH6U1ehPVfAEcCidZn5N62g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically template notes based on their title.'';
            homepage = "https://github.com/jacoblearned/obsidian-template-by-note-name";
        };
    }
);
    "template-filename" = (
    let
        baseUrl = "https://github.com/callumalpass/obsidian-template-filename/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "template-filename";
        version = "1.0.3";
        outputHash = "sha256-lzp4KxdCjLn864bMrTNlPj1bE5MuB9fW2UXfXeTvjRk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create notes with templatable filenames, using date/time formats, random strings, and custom base numbering systems.'';
            homepage = "https://github.com/callumalpass/obsidian-template-filename";
        };
    }
);
    "template-folder" = (
    let
        baseUrl = "https://github.com/LucasOe/obsidian-template-folder/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "template-folder";
        version = "1.0.4";
        outputHash = "sha256-yXOqCCnFTLMX7y4IHOq7CrWXRoqXF2er82cSTUxjLOc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Moves notes to a folder when applying a template.'';
            homepage = "https://github.com/LucasOe/obsidian-template-folder";
        };
    }
);
    "template-search-library" = (
    let
        baseUrl = "https://github.com/Pentchaff/obsidian-search-library/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "template-search-library";
        version = "1.0.0";
        outputHash = "sha256-B8wFi01z/I54iovA3XcXPLnjrlJxtGucWGAB89qlr+o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save search templates for future re-use.'';
            homepage = "https://github.com/Pentchaff/obsidian-search-library";
        };
    }
);
    "templated-daily-notes" = (
    let
        baseUrl = "https://github.com/digitorum/obsidian-templayted-daily-notes/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "templated-daily-notes";
        version = "1.0.1";
        outputHash = "sha256-LiazcpDr8bcWM3q8aoO+TdkhvqtJ+FPI5faVNdhz6Hk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create daily notes with a specified template according to the described settings.'';
            homepage = "https://github.com/digitorum/obsidian-templayted-daily-notes";
        };
    }
);
    "templater-obsidian" = (
    let
        baseUrl = "https://github.com/SilentVoid13/Templater/releases/download/2.19.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "templater-obsidian";
        version = "2.19.3";
        outputHash = "sha256-F9Sfe8GbQfPaQuo/VT4Z2Tm+C65Vwxmq20u1vBk7KUU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and use templates.'';
            homepage = "https://github.com/SilentVoid13/Templater";
        };
    }
);
    "templify" = (
    let
        baseUrl = "https://github.com/Quorafind/Obsidian-Templify/releases/download/0.7.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "templify";
        version = "0.7.2";
        outputHash = "sha256-4oJuvM/h/WR4+Mf+THnppDRaxoR+jEjoj43x3/p4id0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enables flexible conversion of note views into various layout (Closed source).'';
            homepage = "https://github.com/Quorafind/Obsidian-Templify";
        };
    }
);
    "tengwar" = (
    let
        baseUrl = "https://github.com/mikevetkin/obsidian-tengwar/releases/download/1.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tengwar";
        version = "1.0.12";
        outputHash = "sha256-lLx7M+se0yqxZOKXpsPAt1l8+z/22qsg14/kVYpH6rw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Support Tengwar (The Elvish Letters).'';
            homepage = "https://github.com/mikevetkin/obsidian-tengwar";
        };
    }
);
    "tenki" = (
    let
        baseUrl = "https://github.com/ms3056/Tenki/releases/download/0.7.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tenki";
        version = "0.7.9";
        outputHash = "sha256-HJc+IzYiIsQnOimPjky3fwr6fW69XyzsX7+bPNP4moA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple weather display.'';
            homepage = "https://github.com/ms3056/Tenki";
        };
    }
);
    "terminal" = (
    let
        baseUrl = "https://github.com/polyipseity/obsidian-terminal/releases/download/3.23.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "terminal";
        version = "3.23.0";
        outputHash = "sha256-nUPX9vrlRMmakXcg3h0buUPfIcfhTEwaRMFLMZRXw80=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate consoles, shells, and terminals.'';
            homepage = "https://github.com/polyipseity/obsidian-terminal";
        };
    }
);
    "testing-vault" = (
    let
        baseUrl = "https://github.com/pedersen/obsidian-testing-vault/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "testing-vault";
        version = "1.2.0";
        outputHash = "sha256-RwN7e082ha3ekApHvyBttHMQDCFmUXj5m7HZSH4YOHc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Randomized vault generator with links between notes, frontmatter, tags, orphan and leaf notes.'';
            homepage = "https://github.com/pedersen/obsidian-testing-vault";
        };
    }
);
    "text-autocomplete" = (
    let
        baseUrl = "https://github.com/c-degni/text-autocomplete/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-autocomplete";
        version = "1.2.2";
        outputHash = "sha256-si4kmCbpF/C6f+IROyA/Qxq0ApzRkj5+M4FYk2LsCJM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Autocomplete text to type more efficiently.'';
            homepage = "https://github.com/c-degni/text-autocomplete";
        };
    }
);
    "text-block-timer" = (
    let
        baseUrl = "https://github.com/wth461694678/text-block-timer/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-block-timer";
        version = "1.0.9";
        outputHash = "sha256-8pipBM//Zil+zYVcB9dvKJ8aEl9dFnsWYwdJOt2oZxA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a timer to text block to track task time consumption.'';
            homepage = "https://github.com/wth461694678/text-block-timer";
        };
    }
);
    "text-conversions" = (
    let
        baseUrl = "https://github.com/ironsigma/obsidian-text-conversions/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-conversions";
        version = "1.0.0";
        outputHash = "sha256-6qCE37QF+6PoUFIiVliQBQ9k2irDtVhDP7p2CsxtA0M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Perform various text conversions on the selected text.'';
            homepage = "https://github.com/ironsigma/obsidian-text-conversions";
        };
    }
);
    "text-extractor" = (
    let
        baseUrl = "https://github.com/scambier/obsidian-text-extractor/releases/download/0.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-extractor";
        version = "0.7.0";
        outputHash = "sha256-8UP0QBU3zNM6I65YZeslBD1a1LBBRUXNy2XUsBSbB4g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A (companion) plugin to facilitate the extraction of text from images (OCR) and PDFs.'';
            homepage = "https://github.com/scambier/obsidian-text-extractor";
        };
    }
);
    "text-finder" = (
    let
        baseUrl = "https://github.com/nyable/obsidian-text-finder/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-finder";
        version = "0.3.0";
        outputHash = "sha256-agYwuWzOIlTO6WaPZNtyU1BnO1sPEu7aTppnomuVUtc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A search bar that supports search, replace, regular expressions, and case sensitivity.'';
            homepage = "https://github.com/nyable/obsidian-text-finder";
        };
    }
);
    "text-focus" = (
    let
        baseUrl = "https://github.com/usysrc/obsidian-text-focus-plugin/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-focus";
        version = "1.0.5";
        outputHash = "sha256-z3kw8MSYNgtVcG0B3KPS9uhFtAFmoClG7MH6QUsMWnA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Focus the text area when creating new notes'';
            homepage = "https://github.com/usysrc/obsidian-text-focus-plugin";
        };
    }
);
    "text-progress-bar" = (
    let
        baseUrl = "https://github.com/michaeladams/obsidian-text-progress-bar/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-progress-bar";
        version = "0.2.0";
        outputHash = "sha256-8TklHmBLL8FUkJHb8C7+/PGH0mt1K6iZ89ab9jyy9uw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display low-fi text progress bars in your notes.'';
            homepage = "https://github.com/michaeladams/obsidian-text-progress-bar";
        };
    }
);
    "text-snippets-obsidian" = (
    let
        baseUrl = "https://github.com/ArianaKhit/text-snippets-obsidian/releases/download/0.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-snippets-obsidian";
        version = "0.1.2";
        outputHash = "sha256-DlvUBHUZmHkoR/Jp3t5rn6NsRejzgxbLnJpvJZRSwUg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Snippets for faster typing. Replace text templates, create your own, and expand text shortcuts.'';
            homepage = "https://github.com/ArianaKhit/text-snippets-obsidian";
        };
    }
);
    "text-transform" = (
    let
        baseUrl = "https://github.com/ipshing/obsidian-text-transform/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text-transform";
        version = "1.4.1";
        outputHash = "sha256-aV1KQD3XjTbWt75T1t+ss9q41Z0+orxX6ntoBZCr37M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add options to transform text to different casings using keyboard shortcuts.'';
            homepage = "https://github.com/ipshing/obsidian-text-transform";
        };
    }
);
    "text2anki-openai" = (
    let
        baseUrl = "https://github.com/manibatra/obsidian-text2anki-openai/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text2anki-openai";
        version = "1.0.0";
        outputHash = "sha256-nLZ6I+ZyGgrRoaRCAt27qX+e3j/7Bkf4m0HqbD+sogs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Use OpenAI to generate flashcards from text and add them to Anki.'';
            homepage = "https://github.com/manibatra/obsidian-text2anki-openai";
        };
    }
);
    "text2audio" = (
    let
        baseUrl = "https://github.com/luhaifeng666/obsidian-text2audio/releases/download/4.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "text2audio";
        version = "4.1.0";
        outputHash = "sha256-udtU88SCmJvegLfeybOH81fDpbAs3DbJIiXverICgAQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert text to speech.'';
            homepage = "https://github.com/luhaifeng666/obsidian-text2audio";
        };
    }
);
    "textanalysis" = (
    let
        baseUrl = "https://github.com/mihakralj/obsidian-textanalysis/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "textanalysis";
        version = "1.2.1";
        outputHash = "sha256-m0HMApOdStlg5ONU5BzwDfsrhXu+Jt0XpByGldXv3K8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Real-time text analysis on readability, structure, and complexity, incorporating over 30 indicators.'';
            homepage = "https://github.com/mihakralj/obsidian-textanalysis";
        };
    }
);
    "textgrams" = (
    let
        baseUrl = "https://github.com/akopdev/obsidian-textgrams/releases/download/0.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "textgrams";
        version = "0.6.0";
        outputHash = "sha256-yZRdHQwJjPMmnxX86ULD10PgFrjzl+9qA9c9V5QgmWc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Store and visualise ASCII graphics and charts'';
            homepage = "https://github.com/akopdev/obsidian-textgrams";
        };
    }
);
    "tezcat" = (
    let
        baseUrl = "https://github.com/mmargenot/tezcat/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tezcat";
        version = "1.1.0";
        outputHash = "sha256-+EcqCyFF8GGRILBqKatT8iIcyF/Z2847F9IA4HHX988=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Implements AI indexing and search to surface related thoughts to your current work. Look into the mirror.'';
            homepage = "https://github.com/mmargenot/tezcat";
        };
    }
);
    "tg-emoji-search" = (
    let
        baseUrl = "https://github.com/MarsBatya/tg-emoji-search/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tg-emoji-search";
        version = "1.1.4";
        outputHash = "sha256-MKIPirc+pFW48kzQaG88Pb5l0wyCzXf/8QIsDDd92YI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Lets you type out emojis like in Telegram.'';
            homepage = "https://github.com/MarsBatya/tg-emoji-search";
        };
    }
);
    "the-queue" = (
    let
        baseUrl = "https://github.com/koljapluemer/obsidian-the-queue/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "the-queue";
        version = "2.0.3";
        outputHash = "sha256-V64hiGCLBK20BhE2BQVfaLY44r3YEtiB4jKdRSlqfYw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Randomly exposes you to notes from your vault. Supports habits, to-dos, spaced repetition flashcards, iterative reading and more.'';
            homepage = "https://github.com/koljapluemer/obsidian-the-queue";
        };
    }
);
    "thecap-cv-generator" = (
    let
        baseUrl = "https://github.com/xthecapx/Thecap-cv-generator/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "thecap-cv-generator";
        version = "1.0.6";
        outputHash = "sha256-H4TF8KS8fnW1c5KA9Yi1JEIt1tLezr20D1KV0AUhij8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate PDF curriculum from your notes.'';
            homepage = "https://github.com/xthecapx/Thecap-cv-generator";
        };
    }
);
    "theme-by-folder" = (
    let
        baseUrl = "https://github.com/JinmuGo/obsidian-theme-by-folder/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "theme-by-folder";
        version = "1.3.1";
        outputHash = "sha256-gpc4g8Me+dYS4Jc05dhkMW5NHXfrsHK8glM5FKi+Dl4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically switch themes based on the folder of the opened note'';
            homepage = "https://github.com/JinmuGo/obsidian-theme-by-folder";
        };
    }
);
    "theme-picker" = (
    let
        baseUrl = "https://github.com/trey-sedate/obsidian-theme-picker/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "theme-picker";
        version = "1.0.9";
        outputHash = "sha256-oB71hI7lYcqkzhD1xosVoYLbW9uQ26KCcxOlQw8JFro=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly preview and select installed themes.'';
            homepage = "https://github.com/trey-sedate/obsidian-theme-picker";
        };
    }
);
    "theme-toggle" = (
    let
        baseUrl = "https://github.com/gapmiss/theme-toggle/releases/download/0.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "theme-toggle";
        version = "0.1.6";
        outputHash = "sha256-M62bj8w9soaCVCGX0Vnr06zFn7LoFyiY8er5e0wk6qs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Dark/light theme toggle via ribbon icon or command.'';
            homepage = "https://github.com/gapmiss/theme-toggle";
        };
    }
);
    "themed-discord-rpc" = (
    let
        baseUrl = "https://github.com/Mouadhbendjedidi/themed-obsidian-discord-rpc/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "themed-discord-rpc";
        version = "1.2.1";
        outputHash = "sha256-Cb4b44KnXEdApslCmAMPXD8h480YhAdDQ1o5d7Upk5s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A Customizable Discord RPC'';
            homepage = "https://github.com/Mouadhbendjedidi/themed-obsidian-discord-rpc";
        };
    }
);
    "things-logbook" = (
    let
        baseUrl = "https://github.com/liamcain/obsidian-things-logbook/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "things-logbook";
        version = "0.2.0";
        outputHash = "sha256-wRfRdYzzr+ZDwa0Far87O/RfATLZ73puCH/Epz7tdpk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your Things logbook with your daily notes.'';
            homepage = "https://github.com/liamcain/obsidian-things-logbook";
        };
    }
);
    "things3-today" = (
    let
        baseUrl = "https://github.com/wudanyang6/obsidian-things3-today/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "things3-today";
        version = "1.0.1";
        outputHash = "sha256-YV4XwsHhVxuhoMsFdLbBzICLEiFoj4MN3YW6pk3OUc8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage today"s tasks with Things3.'';
            homepage = "https://github.com/wudanyang6/obsidian-things3-today";
        };
    }
);
    "three-noun-prompts" = (
    let
        baseUrl = "https://github.com/jamespeachh/Three-Noun-Prompts/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "three-noun-prompts";
        version = "1.0.3";
        outputHash = "sha256-Wr5k+1g44eQe7p/MFHTIIz8taWXifxh9GNMWdEfCjaQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creative writing prompts that can be synced offline.'';
            homepage = "https://github.com/jamespeachh/Three-Noun-Prompts";
        };
    }
);
    "tick-tones" = (
    let
        baseUrl = "https://github.com/DontBlameMe99/Tick-Tones/releases/download/1.6.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tick-tones";
        version = "1.6.8";
        outputHash = "sha256-yZLVgTmsNwJxYbMqC8v7P17uGpjajvlgNflJUYhVkrc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Plays your custom tone/sound when you tick/complete a checkbox'';
            homepage = "https://github.com/DontBlameMe99/Tick-Tones";
        };
    }
);
    "ticktick" = (
    let
        baseUrl = "https://github.com/viduycheung/ticktick-obsidian/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ticktick";
        version = "1.0.4";
        outputHash = "sha256-Y3tsBLmncggUNTByUEZl8e/A0l+6cocvYmazkQR/Mpw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Check and create tasks in TickTick.'';
            homepage = "https://github.com/viduycheung/ticktick-obsidian";
        };
    }
);
    "ticktick-quickadd-task" = (
    let
        baseUrl = "https://github.com/heymoosh/ticktick-quick-add-obsidian/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ticktick-quickadd-task";
        version = "1.1.0";
        outputHash = "sha256-FHTyr0JzI47sSKC/EtKALVIccHJEe+8PAfeVsTO2Ads=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Instantly creates a TickTick task from the current block of text using a keyboard shortcut. Task includes a copy of the text, and the URI to that exact block of text in your notes. Requires Advanced URI plugin.'';
            homepage = "https://github.com/heymoosh/ticktick-quick-add-obsidian";
        };
    }
);
    "tickticksync" = (
    let
        baseUrl = "https://github.com/thesamim/TickTickSync/releases/download/1.1.17";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tickticksync";
        version = "1.1.17";
        outputHash = "sha256-TQiGyXWyLA1jMzwT/nfK+H9QapYvFAT1qeznQu3NgDM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync TickTick tasks.'';
            homepage = "https://github.com/thesamim/TickTickSync";
        };
    }
);
    "tiddlywiki-import-export" = (
    let
        baseUrl = "https://github.com/lucasbordeau/obsidian-tiddlywiki/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tiddlywiki-import-export";
        version = "1.1.0";
        outputHash = "sha256-hE+X0z9GtgvB6MosWoHOjIzvXja5RBMM0iwPx4lYgfs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import and export TiddlyWiki notes.'';
            homepage = "https://github.com/lucasbordeau/obsidian-tiddlywiki";
        };
    }
);
    "tidit" = (
    let
        baseUrl = "https://github.com/codingthings-com/tidit-obsidian/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tidit";
        version = "1.0.4";
        outputHash = "sha256-cuHzuWyFaNxCAtzXyVIekRM7XhCwp7phKUBofKDyLVg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''tidit adds timestamps to your document as you type — when you want it, how you want it, where you want it.'';
            homepage = "https://github.com/codingthings-com/tidit-obsidian";
        };
    }
);
    "tier-list" = (
    let
        baseUrl = "https://github.com/MoxAlehin/TierList/releases/download/1.6.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tier-list";
        version = "1.6.2";
        outputHash = "sha256-ux+MCx40TyE31L8HaBRpgy8vIVIgLr6yXE4PSA16dqc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visual ranking and organizing content into customizable tier lists.'';
            homepage = "https://github.com/MoxAlehin/TierList";
        };
    }
);
    "tiff-viewer" = (
    let
        baseUrl = "https://github.com/ullmannJan/obsidian-tiff-viewer/releases/download/1.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tiff-viewer";
        version = "1.2.6";
        outputHash = "sha256-z93sv9UkHLVbMNLSLNehV3KZ0bNttcVq9J7OQjZw8E0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View .tif(f) files by generating duplicates in form of .tif(f).png'';
            homepage = "https://github.com/ullmannJan/obsidian-tiff-viewer";
        };
    }
);
    "tiktoker" = (
    let
        baseUrl = "https://github.com/ameyxd/obsidian-tiktoker/releases/download/1.5.59";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tiktoker";
        version = "1.5.59";
        outputHash = "sha256-FxXWOnVnL/ArK0mG2pJS5/zuTfW3FfAPTxD1f81Zfzs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save TikTok videos as markdown notes with embedded content and metadata extraction.'';
            homepage = "https://github.com/ameyxd/obsidian-tiktoker";
        };
    }
);
    "time-bullet" = (
    let
        baseUrl = "https://github.com/pedrogdn/obsidian-time-bullet-plugin/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "time-bullet";
        version = "1.2.0";
        outputHash = "sha256-RyOaLGRNrx7AAzZVvRQKYKJiODbMZ56p2WomTTuZcCw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly add timestamp bullet points to your notes'';
            homepage = "https://github.com/pedrogdn/obsidian-time-bullet-plugin";
        };
    }
);
    "time-inserter" = (
    let
        baseUrl = "https://github.com/heycalmdown/obsidian-time-inserter/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "time-inserter";
        version = "1.0.2";
        outputHash = "sha256-fGhGSBwdke/RV8BDf/g++nDFHLNjlxQGSupzqtJNtDY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert current time at cursor position, rounded to nearest 5-minute interval.'';
            homepage = "https://github.com/heycalmdown/obsidian-time-inserter";
        };
    }
);
    "time-log" = (
    let
        baseUrl = "https://github.com/eddie/obsidian-time-log/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "time-log";
        version = "1.0.3";
        outputHash = "sha256-Xb896K8BLvp7cqpKivRqNjoF8PJdZtPHvep3ta7ZuRo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add timestamps to entries under dated headers for lab notebook style logging.'';
            homepage = "https://github.com/eddie/obsidian-time-log";
        };
    }
);
    "time-ruler" = (
    let
        baseUrl = "https://github.com/j-palindrome/obsidian-time-ruler/releases/download/2.7.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "time-ruler";
        version = "2.7.1";
        outputHash = "sha256-ukhn8mbqq2hlMDw1CjI78Q2XMcZ/T8fEpUU/x2osdsw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A drag-and-drop time ruler combining the best of a task list and a calendar view (integrates with Tasks, Full Calendar, and Dataview).'';
            homepage = "https://github.com/j-palindrome/obsidian-time-ruler";
        };
    }
);
    "time-saver" = (
    let
        baseUrl = "https://github.com/odayou/task-processing-extension/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "time-saver";
        version = "0.0.5";
        outputHash = "sha256-XAGCViBWEKUBo8JxGJe6PuJTBWzWVQO+VbTrAgH3wbA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Save your time. 1. Quickly insert todo directive. 2. Quickly count the time spent on tasks in the current note and the total time spent.'';
            homepage = "https://github.com/odayou/task-processing-extension";
        };
    }
);
    "timecodes" = (
    let
        baseUrl = "https://github.com/gavvvr/obsidian-timecodes-plugin/releases/download/0.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timecodes";
        version = "0.0.2";
        outputHash = "sha256-2PnJB0SwrbnjUj2NZk5Z20T5sFWsWsZL2pJkempH5Hw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Converts raw text timecodes into clickable URLs if a note contains a link to a video'';
            homepage = "https://github.com/gavvvr/obsidian-timecodes-plugin";
        };
    }
);
    "timekeep" = (
    let
        baseUrl = "https://github.com/jacobtread/obsidian-timekeep/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timekeep";
        version = "2.0.0";
        outputHash = "sha256-imceYtgseQhEezqqI4tFdzj6YFP5VvjxDJY/NS419LA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Time tracking.'';
            homepage = "https://github.com/jacobtread/obsidian-timekeep";
        };
    }
);
    "timeline-canvas-creator" = (
    let
        baseUrl = "https://github.com/chris-codes1/timeline-canvas-creator/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timeline-canvas-creator";
        version = "1.0.0";
        outputHash = "sha256-4KF3nRJkiqFIpNRqF05j9npW4aR2gWgD6vXC7J4IwbY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly create timeline structured canvases.'';
            homepage = "https://github.com/chris-codes1/timeline-canvas-creator";
        };
    }
);
    "timeline-schedule" = (
    let
        baseUrl = "https://github.com/Ebonsignori/obsidian-timeline-schedule/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timeline-schedule";
        version = "1.2.1";
        outputHash = "sha256-QcUlzTpDIJ4sugDAN1/qs0x+SGe/4Mu9pxo33xv4opM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Inline timelines generated from human-readable time strings, e.g. "Walk dog (30min)" in a ```schedule codeblock.'';
            homepage = "https://github.com/Ebonsignori/obsidian-timeline-schedule";
        };
    }
);
    "timeline-view" = (
    let
        baseUrl = "https://github.com/b-camphart/timeline-view/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timeline-view";
        version = "1.4.1";
        outputHash = "sha256-e8tokcKc1Tm5sxUohDvPXQ+kwQOYdQxJvIGzF5HXLgg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display your notes in a timeline, based on a given property.'';
            homepage = "https://github.com/b-camphart/timeline-view";
        };
    }
);
    "timelines-revamped" = (
    let
        baseUrl = "https://github.com/seanlowe/obsidian-timelines/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timelines-revamped";
        version = "2.4.0";
        outputHash = "sha256-Qjyrj84pqvqC/rZmPMYPzFX+U4E2KNjvMyVmrmBJ+eU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate a chronological timeline in which all "events" are notes that include a specific tag or set of tags.'';
            homepage = "https://github.com/seanlowe/obsidian-timelines";
        };
    }
);
    "timelive" = (
    let
        baseUrl = "https://github.com/aNNiMON/obsidian-timelive/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timelive";
        version = "1.2.0";
        outputHash = "sha256-ytGRuWwDWvs0BOFwDZgdlsXsbLDZiNbOoYd7z++HK1U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Turn a list of dates into a timeline.'';
            homepage = "https://github.com/aNNiMON/obsidian-timelive";
        };
    }
);
    "timer" = (
    let
        baseUrl = "https://github.com/Raboro/obsidian-timer-plugin/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timer";
        version = "1.2.1";
        outputHash = "sha256-ubmHHVObPUgX1zeS1XxdpYcXWUjUp+q7H5Fw+8IAE+I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Measure time.'';
            homepage = "https://github.com/Raboro/obsidian-timer-plugin";
        };
    }
);
    "timesheet" = (
    let
        baseUrl = "https://github.com/vkostyanetsky/ObsidianTimesheet/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timesheet";
        version = "1.1.3";
        outputHash = "sha256-yFQUK3DmWQ9vf0AKDHs7ZirAZg46lpUPbZ6AdpLPtns=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Timesheet generator for tasks in daily notes.'';
            homepage = "https://github.com/vkostyanetsky/ObsidianTimesheet";
        };
    }
);
    "timestamp-link" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-timestamp-link/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timestamp-link";
        version = "0.1.1";
        outputHash = "sha256-1kDLb8BNCWIL/GVe3BU9Anx17YLj7qCl9OwpNqoDojA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Copy timestamped links to blocks, headings and notes.'';
            homepage = "https://github.com/wenlzhang/obsidian-timestamp-link";
        };
    }
);
    "timestamper" = (
    let
        baseUrl = "https://github.com/coignard/obsidian-timestamper/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timestamper";
        version = "1.0.1";
        outputHash = "sha256-9OGIrHvLvJGIyeRk3w95vTvuKMyP4OrdcZ8cP5K/B2A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert the current timestamp into your notes.'';
            homepage = "https://github.com/coignard/obsidian-timestamper";
        };
    }
);
    "timethings" = (
    let
        baseUrl = "https://github.com/plasmabit/timethings/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timethings";
        version = "1.4.1";
        outputHash = "sha256-nx0ltQODxKbhMYIUYyIB0IUhY611KNMHZ1y8VDEOoL8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show a clock in the corner. Track the total editing time of a note and the last time it was modified.'';
            homepage = "https://github.com/plasmabit/timethings";
        };
    }
);
    "timetracker" = (
    let
        baseUrl = "https://github.com/hedgehog1833/obsidian-timetracker/releases/download/1.6.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "timetracker";
        version = "1.6.7";
        outputHash = "sha256-W+L0DeUbDDMScOluKeSade2+Yc+miMvqNI16m5XCGuE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A stopwatch whose value can be inserted in the editor with a hotkey.'';
            homepage = "https://github.com/hedgehog1833/obsidian-timetracker";
        };
    }
);
    "tiny-habits" = (
    let
        baseUrl = "https://github.com/nazoadiego/tiny-habits/releases/download/1.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tiny-habits";
        version = "1.1.10";
        outputHash = "sha256-8BP8qhi/J8owa7ve/unzl/lJB5TX7vzQukbeKlw+GZI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Habit tracking in a calendar table'';
            homepage = "https://github.com/nazoadiego/tiny-habits";
        };
    }
);
    "tinychart" = (
    let
        baseUrl = "https://github.com/alincoop/obsidian-tinychart/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tinychart";
        version = "0.1.1";
        outputHash = "sha256-wZJpFfLoMZCVcAkVStrD+k5+ju8+XcpV8BvQOGBcEYA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate simple ASCII charts.'';
            homepage = "https://github.com/alincoop/obsidian-tinychart";
        };
    }
);
    "tinypng-image" = (
    let
        baseUrl = "https://github.com/ckt1031/obsidian-tinypng-plugin/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tinypng-image";
        version = "0.4.0";
        outputHash = "sha256-LavNE6/gNSN6HIiLt3LxUywKC5CMMVPDzJu5IFYf3Gc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Compress images using TinyPNG to save your storage.'';
            homepage = "https://github.com/ckt1031/obsidian-tinypng-plugin";
        };
    }
);
    "tipa-support" = (
    let
        baseUrl = "https://github.com/akdemirdeniz/obsidian-tipa/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tipa-support";
        version = "1.0.1";
        outputHash = "sha256-4/zLspP8zl6NOQUECJd52XakKiCXfOmcOhZeTl2r/hY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds support for TIPA phonetic notation.'';
            homepage = "https://github.com/akdemirdeniz/obsidian-tipa";
        };
    }
);
    "title-as-link-text" = (
    let
        baseUrl = "https://github.com/lextoumbourou/obsidian-title-as-link-text/releases/download/1.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "title-as-link-text";
        version = "1.1.10";
        outputHash = "sha256-AwbJLS1ShaZI8sp91yYkoUCjNtauMH7oIy7Y0sb2lK8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically updates link text to use note titles instead of filenames.'';
            homepage = "https://github.com/lextoumbourou/obsidian-title-as-link-text";
        };
    }
);
    "title-generator" = (
    let
        baseUrl = "https://github.com/jaschaephraim/obsidian-title-generator/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "title-generator";
        version = "1.0.3";
        outputHash = "sha256-UIR6qI3hZR9C8VJenSyAqOLXhLR0QgvR/IuFJf1XCe8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly and easily title your notes using OpenAI"s GPT-3.5.'';
            homepage = "https://github.com/jaschaephraim/obsidian-title-generator";
        };
    }
);
    "title-only-tab" = (
    let
        baseUrl = "https://github.com/tristone13th/obsidian-title-only-tab/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "title-only-tab";
        version = "1.0.1";
        outputHash = "sha256-G8TZNgTtFG6M1fhBnTdyQI25/h/baFfHVg9Nk6sa6vI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set tab name to title in frontmatter for Jekyll users'';
            homepage = "https://github.com/tristone13th/obsidian-title-only-tab";
        };
    }
);
    "title-renamer" = (
    let
        baseUrl = "https://github.com/stroiman/obsidian-title-sync/releases/download/1.0.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "title-renamer";
        version = "1.0.6";
        outputHash = "sha256-SVRr/DqDj5ot9HYjxz9PVXTX3r8QHTiQXdI9Y6xTUlk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep top heading in note synced with file name.'';
            homepage = "https://github.com/stroiman/obsidian-title-sync";
        };
    }
);
    "tldraw" = (
    let
        baseUrl = "https://github.com/tldraw/obsidian-plugin/releases/download/1.29.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tldraw";
        version = "1.29.0";
        outputHash = "sha256-bwTMWNkWbiICjb6qJfyYLwr6g/k09S8bDYjkVhIK734=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integration with tldraw, allowing users to draw and edit content on a virtual whiteboard.'';
            homepage = "https://github.com/tldraw/obsidian-plugin";
        };
    }
);
    "tmayoff-meals" = (
    let
        baseUrl = "https://github.com/tmayoff/obsidian-meals/releases/download/1.2.1-prerelease";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tmayoff-meals";
        version = "1.2.1-prerelease";
        outputHash = "sha256-pQpattmS9VmO3ZIQUFn66az8GSmB4IvYhTTCFn6SUmo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Meal planning and recipe manager.'';
            homepage = "https://github.com/tmayoff/obsidian-meals";
        };
    }
);
    "todo-highlighter" = (
    let
        baseUrl = "https://github.com/heavenlygaze/obsidian-todo-highlighter/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todo-highlighter";
        version = "1.0.0";
        outputHash = "sha256-OxMjzgwNAQHcyFFCZgzeymXZoIx6M+s3JDYSJET77EI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlights TODO regex as green.'';
            homepage = "https://github.com/heavenlygaze/obsidian-todo-highlighter";
        };
    }
);
    "todo-sort" = (
    let
        baseUrl = "https://github.com/ryangomba/obsidian-todo-sort/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todo-sort";
        version = "1.0.2";
        outputHash = "sha256-EbMfDV44Pk0vO0b8JWbJLuLBGlLrfAMiu2rwY8xTSOw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sort todos by completion status.'';
            homepage = "https://github.com/ryangomba/obsidian-todo-sort";
        };
    }
);
    "todo-txt-mode" = (
    let
        baseUrl = "https://github.com/rioskit/obsidian-todo-txt-mode/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todo-txt-mode";
        version = "1.0.5";
        outputHash = "sha256-anpxDLSG6rMkjz2vsBIuvJ3VNz+PMRkgSUpiCJc8IVs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Support for todo.txt file format with syntax highlighting and task management'';
            homepage = "https://github.com/rioskit/obsidian-todo-txt-mode";
        };
    }
);
    "todoist-completed-tasks-plugin" = (
    let
        baseUrl = "https://github.com/Ledaryy/obsidian-todoist-completed-tasks/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoist-completed-tasks-plugin";
        version = "1.5.2";
        outputHash = "sha256-R2lp6D5ucGGQFvtVwD2SlsA73ZWOwbxN5BEtss+58iM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add completed Todoist tasks to your notes.'';
            homepage = "https://github.com/Ledaryy/obsidian-todoist-completed-tasks";
        };
    }
);
    "todoist-context-bridge" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-todoist-context-bridge/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoist-context-bridge";
        version = "1.1.1";
        outputHash = "sha256-+gFENzR9hqd1KMGbE26r7kAl4vvTgFEWSYxTZ0m1FbE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Bridge your note-taking and Todoist task management workflows with contextual connections. Seamlessly integrate with Dataview and Tasks plugins.'';
            homepage = "https://github.com/wenlzhang/obsidian-todoist-context-bridge";
        };
    }
);
    "todoist-indicator" = (
    let
        baseUrl = "https://github.com/kapej42/obsidian-todoist-indicator/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoist-indicator";
        version = "1.0.5";
        outputHash = "sha256-MAmCKUEr3Sg1VfDnWeimYdFlae6uvKF3e85C8/l+4J0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Adds a badge to "project" files to indicate a missing link to a Todoist project.'';
            homepage = "https://github.com/kapej42/obsidian-todoist-indicator";
        };
    }
);
    "todoist-review" = (
    let
        baseUrl = "https://github.com/imcauley/todoist-review/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoist-review";
        version = "1.0.2";
        outputHash = "sha256-zQt3WEpDUPoPsFcttYavdOkUEYl9n9pkR68CEf4QvuE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A pane for reviewing overdue tasks from todoist'';
            homepage = "https://github.com/imcauley/todoist-review";
        };
    }
);
    "todoist-sync-plugin" = (
    let
        baseUrl = "https://github.com/jamiebrynes7/obsidian-todoist-plugin/releases/download/2.6.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoist-sync-plugin";
        version = "2.6.0";
        outputHash = "sha256-PqmwWLaZvDhqdoeUWcGcbUbgTZMw30IoIAL/xRM9q+Q=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Materialize Todoist tasks within your notes.'';
            homepage = "https://github.com/jamiebrynes7/obsidian-todoist-plugin";
        };
    }
);
    "todoist-text" = (
    let
        baseUrl = "https://github.com/wesmoncrief/obsidian-todoist-text/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoist-text";
        version = "1.4.2";
        outputHash = "sha256-IQ6qFaemgeKa/2RFY+dxIWpY/OSeby1bEieRNFiwPIs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate your Todoist tasks with Markdown checkboxes.'';
            homepage = "https://github.com/wesmoncrief/obsidian-todoist-text";
        };
    }
);
    "todoistprojectsync" = (
    let
        baseUrl = "https://github.com/stuporfly/ObsidianTodoistProjects/releases/download/0.1.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoistprojectsync";
        version = "0.1.13";
        outputHash = "sha256-Pa8qzU+TMrirQrqcAMeMO3sZIEFgMCi2F8JpPxE7ctM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronizes projects from Todoist, creating a note for each.'';
            homepage = "https://github.com/stuporfly/ObsidianTodoistProjects";
        };
    }
);
    "todos-sort" = (
    let
        baseUrl = "https://github.com/jsifalda/obsidian-todos-sort/releases/download/2.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todos-sort";
        version = "2.3.0";
        outputHash = "sha256-Z3Bg10QYpKA0SyY6oSU3hamyEW+lg0Bph41ahQl2064=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sorting your TODOs (checkboxes) in current note by completion status'';
            homepage = "https://github.com/jsifalda/obsidian-todos-sort";
        };
    }
);
    "todoseq" = (
    let
        baseUrl = "https://github.com/scross01/obsidian-todoseq/releases/download/0.12.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todoseq";
        version = "0.12.2";
        outputHash = "sha256-KBLwo61+vuRd7hlCiZwwq99FDTd4ByoL1UBOlQ5Hgc4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Lightweight keyword-based task tracker using Logseq style keywords.'';
            homepage = "https://github.com/scross01/obsidian-todoseq";
        };
    }
);
    "todotxt" = (
    let
        baseUrl = "https://github.com/mvgrimes/obsidian-todotxt-plugin/releases/download/2.1.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todotxt";
        version = "2.1.7";
        outputHash = "sha256-xMJLCbRE6pnbUfLgcrswLOMu0xFmn/EvGxzLzYQwqhc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage Todo.txt files.'';
            homepage = "https://github.com/mvgrimes/obsidian-todotxt-plugin";
        };
    }
);
    "todotxt-codeblocks" = (
    let
        baseUrl = "https://github.com/benjamonnguyen/obsidian-todotxt-codeblocks/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "todotxt-codeblocks";
        version = "1.0.1";
        outputHash = "sha256-bR9WLLztqIFgWHoEF0C6njHmQZFGC5lH1FeTkI7s9Os=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manage your tasks inside code blocks according to the Todo.txt specification.'';
            homepage = "https://github.com/benjamonnguyen/obsidian-todotxt-codeblocks";
        };
    }
);
    "toggle-readable-line-length" = (
    let
        baseUrl = "https://github.com/drichardson/obsidian-toggle-readable-line-length/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "toggle-readable-line-length";
        version = "1.0.2";
        outputHash = "sha256-Jj+uhiHaxAhZ0aLOvRSlj459P/cxGv1X/mvUXi6bKg8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add command to toggle Readable line length editor setting.'';
            homepage = "https://github.com/drichardson/obsidian-toggle-readable-line-length";
        };
    }
);
    "tokei" = (
    let
        baseUrl = "https://github.com/ms3056/Tokei/releases/download/0.9.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tokei";
        version = "0.9.0";
        outputHash = "sha256-1yWAj6cl2gQnqcZofbqHO3xnOnkR3HAGOgPbcYEy0gw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A simple time and date display.'';
            homepage = "https://github.com/ms3056/Tokei";
        };
    }
);
    "tokenz" = (
    let
        baseUrl = "https://github.com/ferenk/obsidian-tokenz/releases/download/0.8.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tokenz";
        version = "0.8.1";
        outputHash = "sha256-2TIOq1IRexXt3wyRKcOIOdf/GQCoRS/hWXaMXiiXuL4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert any type of short codes into your document. You can add smileys (:), ;), ...) or emojis (:smile:, :wink:, ...). More code maps will be added later. Even you, as a user can create your own.'';
            homepage = "https://github.com/ferenk/obsidian-tokenz";
        };
    }
);
    "tolino-notes-import" = (
    let
        baseUrl = "https://github.com/juergenbr/obsidian-tolino-notes-import/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tolino-notes-import";
        version = "1.1.1";
        outputHash = "sha256-hPFPGYiB6OG+TDBbsXczPiDMGdz+OYaCI6EcN4Wzd8c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import notes from a Tolino E-Reader.'';
            homepage = "https://github.com/juergenbr/obsidian-tolino-notes-import";
        };
    }
);
    "tor2e-statblocks" = (
    let
        baseUrl = "https://github.com/modality/obsidian-the-one-ring-2e-statblocks/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tor2e-statblocks";
        version = "1.0.0";
        outputHash = "sha256-CpJ+GneLp87WyjkwCVY4uTrbsa8/BaTMcCScO/rnoF8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render NPC and adversary statblocks for The One Ring 2E roleplaying game.'';
            homepage = "https://github.com/modality/obsidian-the-one-ring-2e-statblocks";
        };
    }
);
    "track-a-lot" = (
    let
        baseUrl = "https://github.com/revolter/obsidian-track-a-lot-plugin/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "track-a-lot";
        version = "1.5.0";
        outputHash = "sha256-sdCA9aomeBscttEmldPk7PzmYAdU+gfbDMZ0eC3f9BY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Scrapes different webpages, builds lists with the items as Markdown tables, and allows you to track their status.'';
            homepage = "https://github.com/revolter/obsidian-track-a-lot-plugin";
        };
    }
);
    "tracker-plus" = (
    let
        baseUrl = "https://github.com/greater-than/Obsidian-Tracker-Plus/releases/download/1.11.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tracker-plus";
        version = "1.11.1";
        outputHash = "sha256-2wpdJq54gMVXzREGhZNaQQZYFIcmQCSd/C558FNYPGg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track and visualize data from your notes. Works with Tracker Markdown.'';
            homepage = "https://github.com/greater-than/Obsidian-Tracker-Plus";
        };
    }
);
    "trakt-tv" = (
    let
        baseUrl = "https://github.com/Fleker/trakt-for-obsidian/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "trakt-tv";
        version = "1.4.1";
        outputHash = "sha256-VAgFNvprV5NbDbeRtMBRSotBHXKbTTZj1EfrkyH3OQM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Syncs your watched Trakt.tv shows to a file.'';
            homepage = "https://github.com/Fleker/trakt-for-obsidian";
        };
    }
);
    "transcription-audio" = (
    let
        baseUrl = "https://github.com/cha-yh/transcription-audio-obsidian-plugin/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "transcription-audio";
        version = "0.5.0";
        outputHash = "sha256-6Wa75peuanwM36SujHWMZvEQ3AX/QMJCD57GHHgjwPA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transcribe audio files into Markdown notes.'';
            homepage = "https://github.com/cha-yh/transcription-audio-obsidian-plugin";
        };
    }
);
    "transfer-latex-from-gpt" = (
    let
        baseUrl = "https://github.com/xixia123/obsidian-transfer-latex-from-gpt/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "transfer-latex-from-gpt";
        version = "1.0.1";
        outputHash = "sha256-nZFbvzFFLO/jfDfWFdOavURzwI8XOZmIa5T9Cjs7NKY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''This plugin allows you to transfer LaTeX equations generated by GPT.'';
            homepage = "https://github.com/xixia123/obsidian-transfer-latex-from-gpt";
        };
    }
);
    "translate" = (
    let
        baseUrl = "https://github.com/Fevol/obsidian-translate/releases/download/1.4.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "translate";
        version = "1.4.9";
        outputHash = "sha256-RN3Iji1LFsHyygaZzNR1ML4yjLN/yu84jpTVivNOOlc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Translate text and notes with Google Translate, DeepL, Azure, and more.'';
            homepage = "https://github.com/Fevol/obsidian-translate";
        };
    }
);
    "translate-inline" = (
    let
        baseUrl = "https://github.com/kon-foo/Obsidian-Translate-Inline/releases/download/0.1.18";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "translate-inline";
        version = "0.1.18";
        outputHash = "sha256-XgaDRR4ApJtMQFMhWwE08STGUXV5Z5j8BzlKcF1Szlc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Translations at your fingertips. Inline Translations for your Notes.'';
            homepage = "https://github.com/kon-foo/Obsidian-Translate-Inline";
        };
    }
);
    "tray" = (
    let
        baseUrl = "https://github.com/dragonwocky/obsidian-tray/releases/download/0.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tray";
        version = "0.3.5";
        outputHash = "sha256-IWlAkdLkyuhRCa6IBrqv4rgEbJQ+VoqTVep1GcPFxjw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run Obsidian from the system tray for customisable window management and global quick notes.'';
            homepage = "https://github.com/dragonwocky/obsidian-tray";
        };
    }
);
    "tree-diagram" = (
    let
        baseUrl = "https://github.com/limpido/obsidian-tree-diagram/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tree-diagram";
        version = "1.0.0";
        outputHash = "sha256-hnQY/Slbc9HuD0tX+v4+hRe42iXq5JIEdsG/Hpneb28=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert directory structure formatted in tab-indented text to a tree diagram.'';
            homepage = "https://github.com/limpido/obsidian-tree-diagram";
        };
    }
);
    "tree-search" = (
    let
        baseUrl = "https://github.com/catacgc/obsidian-tree-search/releases/download/0.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tree-search";
        version = "0.11.0";
        outputHash = "sha256-JmmPlNtc37fuOYlTiUAEpJFYJeIpM2X3qcHhEA9O0/Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Supercharged search across backlinks and relations in your notes'';
            homepage = "https://github.com/catacgc/obsidian-tree-search";
        };
    }
);
    "treefocus" = (
    let
        baseUrl = "https://github.com/iOSonntag/obsidian-plugin-treefocus/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "treefocus";
        version = "1.1.4";
        outputHash = "sha256-g+sVUj9lx+9W0adpDbCbOSeIDxJo0QuU8X7Z/KeXrl4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight, dim & style your files & folders in the file explorer (navigation) based on predefined or custom rules.'';
            homepage = "https://github.com/iOSonntag/obsidian-plugin-treefocus";
        };
    }
);
    "truth-table-gen" = (
    let
        baseUrl = "https://github.com/Max-Schulten/truth-table-plus/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "truth-table-gen";
        version = "1.1.1";
        outputHash = "sha256-RrTNks6pYdSLxIEkGa195ZrZgwpSN0vntLstWIhJzAY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Generate truth tables quickly in your .md files'';
            homepage = "https://github.com/Max-Schulten/truth-table-plus";
        };
    }
);
    "tsumugi-mark" = (
    let
        baseUrl = "https://github.com/mofukuru/TsumugiMark/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tsumugi-mark";
        version = "1.0.1";
        outputHash = "sha256-BYNtstnpc2emlCnjJ1kfaz5ehM6V08odTt+spbfazm8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''vertical editor for Japanese writer.'';
            homepage = "https://github.com/mofukuru/TsumugiMark";
        };
    }
);
    "tv-tracker" = (
    let
        baseUrl = "https://github.com/Shreshth-mehra/Obsidian-TV-Tracker/releases/download/1.3.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tv-tracker";
        version = "1.3.16";
        outputHash = "sha256-IyVroFccfbvXESDexgX6LaU6JvIEpq98f6aYl5xVcoM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A movie and TV show tracker.'';
            homepage = "https://github.com/Shreshth-mehra/Obsidian-TV-Tracker";
        };
    }
);
    "tw-task-wiki" = (
    let
        baseUrl = "https://github.com/SntTGR/obsidian-tw-task-wiki/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "tw-task-wiki";
        version = "1.3.0";
        outputHash = "sha256-ccGigamlrPbV7f+/Q1kpSID+iSwVXySXfW4vJAignfM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Wrapper and integration around TaskWarrior. Allows you to view and edit tasks in your TaskWarrior database as tables.'';
            homepage = "https://github.com/SntTGR/obsidian-tw-task-wiki";
        };
    }
);
    "type-chinese-like-english" = (
    let
        baseUrl = "https://github.com/Hwenyi/obsidian-type-chinese-like-english/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "type-chinese-like-english";
        version = "1.1.1";
        outputHash = "sha256-qgM7Jh1+IPSBbQJ2KIRPQD8LVv+AzKU1QxY/5b+z6HQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert pinyin to Chinese and LaTeX'';
            homepage = "https://github.com/Hwenyi/obsidian-type-chinese-like-english";
        };
    }
);
    "type-zen-mode" = (
    let
        baseUrl = "https://github.com/IlgamGabdullin/obsidian-typezen/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "type-zen-mode";
        version = "0.0.4";
        outputHash = "sha256-sChQBDAyEj+XAWDLi2I68D6ZB9onhSSDyP15oCMpzIQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Type anything and get in zen mode instantly, no more distraction between you and your thoughts.'';
            homepage = "https://github.com/IlgamGabdullin/obsidian-typezen";
        };
    }
);
    "typecho" = (
    let
        baseUrl = "https://github.com/Chen2226/obsidian-typecho/releases/download/0.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typecho";
        version = "0.1.5";
        outputHash = "sha256-7o9oy5qg4m8MU446iIuCK7oU6MLKHWf3lxvBEKufolw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Post the file to Typecho.'';
            homepage = "https://github.com/Chen2226/obsidian-typecho";
        };
    }
);
    "typefully" = (
    let
        baseUrl = "https://github.com/dsebastien/obsidian-typefully/releases/download/3.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typefully";
        version = "3.2.1";
        outputHash = "sha256-Jzg3EzimGNXI4j/1fHIsyk0OZxqrrX7ibdsDS2wj8a0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Typefully integration. Publish social media posts with ease'';
            homepage = "https://github.com/dsebastien/obsidian-typefully";
        };
    }
);
    "typewriter-mode" = (
    let
        baseUrl = "https://github.com/davisriedel/obsidian-typewriter-mode/releases/download/1.4.0-beta.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typewriter-mode";
        version = "1.4.0-beta.1";
        outputHash = "sha256-CDv1tQvRzcbDmGyFP5zM1Z7e9nngf4BjpboHzGN0/hQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Typewriter scrolling, highlighting of the current line, dimming of unfocused paragraphs and more.'';
            homepage = "https://github.com/davisriedel/obsidian-typewriter-mode";
        };
    }
);
    "typing" = (
    let
        baseUrl = "https://github.com/konodyuk/obsidian-typing/releases/download/0.3.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typing";
        version = "0.3.7";
        outputHash = "sha256-stoP7Gce/ent88lZhyHLBsJkXTORuD5XdPKmz44DlhI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Customizable note types.'';
            homepage = "https://github.com/konodyuk/obsidian-typing";
        };
    }
);
    "typing-assistant" = (
    let
        baseUrl = "https://github.com/Jambo2018/notion-assistant-plugin/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typing-assistant";
        version = "0.3.0";
        outputHash = "sha256-92jxNUh2SA06I5hYlwNpmDfKZ1bhHt9JE5sq+Z9zNyw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Support multiple shortcut menus to improve input efficiency.'';
            homepage = "https://github.com/Jambo2018/notion-assistant-plugin";
        };
    }
);
    "typing-speed" = (
    let
        baseUrl = "https://github.com/Supercip971/obsidian-typing-speed/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typing-speed";
        version = "1.3.0";
        outputHash = "sha256-ZPN4AM9hrdofJqHNPXeCioWJhj88b/bptHqIMV7jIrM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show the current typing speed in the status bar.'';
            homepage = "https://github.com/Supercip971/obsidian-typing-speed";
        };
    }
);
    "typing-transformer-obsidian" = (
    let
        baseUrl = "https://github.com/aptend/typing-transformer-obsidian/releases/download/0.4.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typing-transformer-obsidian";
        version = "0.4.9";
        outputHash = "sha256-imPdOf3XLsDMZlG6FROLL2cGZAVgXWCpjPtnpC8kMfQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Improved, configurable auto formatting as typing.'';
            homepage = "https://github.com/aptend/typing-transformer-obsidian";
        };
    }
);
    "typographer" = (
    let
        baseUrl = "https://github.com/coignard/obsidian-typographer/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typographer";
        version = "1.0.1";
        outputHash = "sha256-pAntRAj7Nc9QcTr0blf/PTeVzElT47xPrnltQevsvFU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enhances typography with smart quotes, custom text replacements and auto-pairing characters.'';
            homepage = "https://github.com/coignard/obsidian-typographer";
        };
    }
);
    "typst" = (
    let
        baseUrl = "https://github.com/fenjalien/obsidian-typst/releases/download/0.10.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typst";
        version = "0.10.0";
        outputHash = "sha256-z5bSMCBnFI1vFpVpqJQUFv3dpFnmPXFPlXtFAcNXKiY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render `typst` code blocks and math blocks to images with Typst.'';
            homepage = "https://github.com/fenjalien/obsidian-typst";
        };
    }
);
    "typst-mate" = (
    let
        baseUrl = "https://github.com/azyarashi/obsidian-typst-mate/releases/download/2.3.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "typst-mate";
        version = "2.3.2";
        outputHash = "sha256-ZE6hvWaOrTaDcWmbBAR3OPQzPzp+sgVEWBPoaA+r7mo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render math expressions with Typst instead of MathJax.'';
            homepage = "https://github.com/azyarashi/obsidian-typst-mate";
        };
    }
);
    "ultimate-todoist-sync" = (
    let
        baseUrl = "https://github.com/HeroBlackInk/ultimate-todoist-sync-for-obsidian/releases/download/2.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ultimate-todoist-sync";
        version = "2.0.3";
        outputHash = "sha256-p1Q3gND8EWD3fuju6gNRFHKfYMm3qX8EBo9gYc3bbyg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Todoist task synchronization.'';
            homepage = "https://github.com/HeroBlackInk/ultimate-todoist-sync-for-obsidian";
        };
    }
);
    "umbpublisher" = (
    let
        baseUrl = "https://github.com/OwainWilliams/umbPublisher/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "umbpublisher";
        version = "1.4.0";
        outputHash = "sha256-e0cpg7NkvEue1IrvK259fPRiuv6c7NPBXraIe363uhE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Published notes to Umbraco CMS 15+'';
            homepage = "https://github.com/OwainWilliams/umbPublisher";
        };
    }
);
    "uncheck-all" = (
    let
        baseUrl = "https://github.com/ShacharHarshuv/obsidian-uncheck-all/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "uncheck-all";
        version = "1.0.3";
        outputHash = "sha256-n0+Y/izsGG7j1NPPtHpQ/PsiyOjaQqN4u0A1uy2Olpw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Uncheck all checkboxes in the current note using one command.'';
            homepage = "https://github.com/ShacharHarshuv/obsidian-uncheck-all";
        };
    }
);
    "unearthed-app" = (
    let
        baseUrl = "https://github.com/Unearthed-App/unearthed-obsidian/releases/download/1.5.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unearthed-app";
        version = "1.5.9";
        outputHash = "sha256-cyTefP/rekRP0Y4OKegTOtNQ7k/m918M6sXNbY+Joks=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Auto Sync Kindle Highlights and Daily Reflection via Unearthed'';
            homepage = "https://github.com/Unearthed-App/unearthed-obsidian";
        };
    }
);
    "unfilled-stats-highlighter" = (
    let
        baseUrl = "https://github.com/White7292/obsidian-hd-unfilled-stats-highlighter/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unfilled-stats-highlighter";
        version = "1.0.0";
        outputHash = "sha256-Xge3sALMXuOHZel1k6PJzEDIWoyL3pxRhAWvlrHSuq0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Streamline your stat/habit tracking process by automatically identifying and prefixing unfilled stats, making them easier to spot and fill out.'';
            homepage = "https://github.com/White7292/obsidian-hd-unfilled-stats-highlighter";
        };
    }
);
    "unicode-search" = (
    let
        baseUrl = "https://github.com/BambusControl/obsidian-unicode-search/releases/download/0.7.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unicode-search";
        version = "0.7.3";
        outputHash = "sha256-zk6BXVN4qSvX9HAQqXK3SAprLc8ERRQMCIYM9Bvxbso=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search and insert Unicode characters into your editor.'';
            homepage = "https://github.com/BambusControl/obsidian-unicode-search";
        };
    }
);
    "unique-attachments" = (
    let
        baseUrl = "https://github.com/dy-sh/obsidian-unique-attachments/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unique-attachments";
        version = "1.0.4";
        outputHash = "sha256-0C43Ohx2nlkMR/BgXMCNAwG/xVl6A/fP3RndddCZ7V4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Rename attachments, making their names unique (based on hashing of file content).'';
            homepage = "https://github.com/dy-sh/obsidian-unique-attachments";
        };
    }
);
    "unit-converter" = (
    let
        baseUrl = "https://github.com/ruszabarov/obsidian-unit-converter/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unit-converter";
        version = "1.5.1";
        outputHash = "sha256-vcCPWahUgcukT6zrpIE62S/6R+pgk8RlizNzWdXDc8I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Converts units right in your Markdown files.'';
            homepage = "https://github.com/ruszabarov/obsidian-unit-converter";
        };
    }
);
    "unitade" = (
    let
        baseUrl = "https://github.com/Falcion/UNITADE.md/releases/download/3.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unitade";
        version = "3.2.7";
        outputHash = "sha256-QJvdue6gGzxc5J/dTNhFf7wLwLlIjgxBg+le3l4yVUg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Treat any file as any view, code editor and syntax highlighting, modals for creating and renaming files, assign views for files, unloading registries and many more for advanced work with non-markdown files.'';
            homepage = "https://github.com/Falcion/UNITADE.md";
        };
    }
);
    "univer" = (
    let
        baseUrl = "https://github.com/dream-num/obsidian-univer/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "univer";
        version = "1.1.5";
        outputHash = "sha256-hr1Oj2icfKSX1kGe/kOQQFPdqs3ADobmszFmz23jxw0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create, edit, and view spreadsheets and documents in various formats like Excel and Word directly within your knowledge base.'';
            homepage = "https://github.com/dream-num/obsidian-univer";
        };
    }
);
    "universal-renderer" = (
    let
        baseUrl = "https://github.com/dgudim/obsidian-universal-renderer/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "universal-renderer";
        version = "1.0.11";
        outputHash = "sha256-jWYppVGqeGIqKyc95ey8guXE03OUsrO1ymlXYJ0CO0s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render various diagrams using system native packages'';
            homepage = "https://github.com/dgudim/obsidian-universal-renderer";
        };
    }
);
    "unlime" = (
    let
        baseUrl = "https://github.com/shandyba/obsidian-lime/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unlime";
        version = "0.4.0";
        outputHash = "sha256-BeqMHmZfqu7/2OFA65ZagkfRWsjdHCFupZJgt5ne59A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle Unlinked mentions ON or OFF within backlinks and Outgoing links panels'';
            homepage = "https://github.com/shandyba/obsidian-lime";
        };
    }
);
    "unofficial-fabric-integration" = (
    let
        baseUrl = "https://github.com/chasebank87/unofficial-fabric-plugin/releases/download/1.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unofficial-fabric-integration";
        version = "1.3.3";
        outputHash = "sha256-9UCdIrA1sDeR2GtFtClaIwVflmJKYxag6lOV4O9lxEA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrates Fabric into your vault'';
            homepage = "https://github.com/chasebank87/unofficial-fabric-plugin";
        };
    }
);
    "unofficial-kinopoisk" = (
    let
        baseUrl = "https://github.com/Alintor/obsidian-kinopoisk-plugin/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "unofficial-kinopoisk";
        version = "1.0.3";
        outputHash = "sha256-O97PKOJSM8abO+OgJskVDDA/QOHN8cHhbin6lM8snUY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Helps you find movies and tv shows via Kinopoisk and create notes.'';
            homepage = "https://github.com/Alintor/obsidian-kinopoisk-plugin";
        };
    }
);
    "update-relative-links" = (
    let
        baseUrl = "https://github.com/val3344/obsidian-update-relative-links/releases/download/2.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "update-relative-links";
        version = "2.1.4";
        outputHash = "sha256-mxcM+bfur/mdloib1maR/crkdh+pQpiewfwVNsnKMK4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Update relative links.'';
            homepage = "https://github.com/val3344/obsidian-update-relative-links";
        };
    }
);
    "update-time" = (
    let
        baseUrl = "https://github.com/dsebastien/obsidian-update-time/releases/download/1.0.13";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "update-time";
        version = "1.0.13";
        outputHash = "sha256-zkgJ1D5uELQp3AwX1D3aNaEVSjKC+la6AyNDTV2UNyY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Update front matter to include creation and last update times'';
            homepage = "https://github.com/dsebastien/obsidian-update-time";
        };
    }
);
    "update-time-on-edit" = (
    let
        baseUrl = "https://github.com/beaussan/update-time-on-edit-obsidian/releases/download/2.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "update-time-on-edit";
        version = "2.5.0";
        outputHash = "sha256-Tn89xZH76hnXFGMmTmv1BgYTEm87g+XILZW1wbvanJU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep frontmatter in sync with the last edit time.'';
            homepage = "https://github.com/beaussan/update-time-on-edit-obsidian";
        };
    }
);
    "update-time-updater" = (
    let
        baseUrl = "https://github.com/muratagawa/update-time-updater/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "update-time-updater";
        version = "0.1.4";
        outputHash = "sha256-QwU9IFJdpY/vHouOyH+DEiHwgvt4FqQBUxFNqt+o1pk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Manually update the modified date field in frontmatter.'';
            homepage = "https://github.com/muratagawa/update-time-updater";
        };
    }
);
    "uri-commands" = (
    let
        baseUrl = "https://github.com/kzhovn/uri-commands-obsidian/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "uri-commands";
        version = "1.0.1";
        outputHash = "sha256-fHDFFk0rs7oGp5hQ5h7YN11Ih8BY0ZFm355fzAPrHLQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Execute URIs from the command palette.'';
            homepage = "https://github.com/kzhovn/uri-commands-obsidian";
        };
    }
);
    "uri-converter" = (
    let
        baseUrl = "https://github.com/wenlzhang/obsidian-uri-converter/releases/download/0.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "uri-converter";
        version = "0.5.0";
        outputHash = "sha256-6lwBs9EkCgfdJU8lvEqq8deVJORGtAqoM9XZXN8eNFo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert URIs to internal links.'';
            homepage = "https://github.com/wenlzhang/obsidian-uri-converter";
        };
    }
);
    "url-display" = (
    let
        baseUrl = "https://github.com/lin-stephanie/obsidian-url-display/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "url-display";
        version = "1.4.0";
        outputHash = "sha256-IxYZNAHZfSDMYts2fVatp05gvI5M7Cfw6SgerVUISGA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Extract and display external URLs of the note.'';
            homepage = "https://github.com/lin-stephanie/obsidian-url-display";
        };
    }
);
    "url-formatter" = (
    let
        baseUrl = "https://github.com/thomassnoeck/url-formatter-obsidian/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "url-formatter";
        version = "1.1.1";
        outputHash = "sha256-/MAu60BnLZTLpdHJTbdqk3XnkEby5MfwLlZCckb8jds=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically formats specific URLs pasted into your notes into clean Markdown links.'';
            homepage = "https://github.com/thomassnoeck/url-formatter-obsidian";
        };
    }
);
    "url-into-selection" = (
    let
        baseUrl = "https://github.com/denolehov/obsidian-url-into-selection/releases/download/1.11.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "url-into-selection";
        version = "1.11.4";
        outputHash = "sha256-I401jjlZXcAPGQVIYERvqV4/3Zu+si0bLAQcDk7xGsY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Paste URL "into" selected text.'';
            homepage = "https://github.com/denolehov/obsidian-url-into-selection";
        };
    }
);
    "url-namer" = (
    let
        baseUrl = "https://github.com/zfei/obsidian-url-namer/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "url-namer";
        version = "1.1.1";
        outputHash = "sha256-o6602pqmGXOcIED5+okcWER14BHM/40Lav+XJEfOT58=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Retrieve the HTML title of web pages to name external links.'';
            homepage = "https://github.com/zfei/obsidian-url-namer";
        };
    }
);
    "url-webview-opener" = (
    let
        baseUrl = "https://github.com/Kieirra/obsidian-url-extension/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "url-webview-opener";
        version = "1.3.1";
        outputHash = "sha256-0ZezFhF3q0b/2GjOsozEYT1H/yFe6kWY8PPXBab5p7Y=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Opens .url files in the internal webview.'';
            homepage = "https://github.com/Kieirra/obsidian-url-extension";
        };
    }
);
    "usher" = (
    let
        baseUrl = "https://github.com/vrtmrz/usher/releases/download/0.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "usher";
        version = "0.0.5";
        outputHash = "sha256-KIoQbxTMjmbna2weB8kQdnYFVW3SXp7TXYZmVGSE2VM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''The overridden config directory manager'';
            homepage = "https://github.com/vrtmrz/usher";
        };
    }
);
    "vantage-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/vantage-obsidian/releases/download/1.4.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vantage-obsidian";
        version = "1.4.2";
        outputHash = "sha256-WLytvjiXETyvFZJiwSp66GGqm0sCnn10Rbj8icbqGwA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Build advanced search queries.'';
            homepage = "https://github.com/ryanjamurphy/vantage-obsidian";
        };
    }
);
    "vare" = (
    let
        baseUrl = "https://github.com/4Source/vare-obsidian-plugin/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vare";
        version = "0.2.0";
        outputHash = "sha256-9JNLa0NcXeY2ybv3Av/uMl7rr0LusNf2tyzkmb82PGQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Now you can easily manage your plugins and themes. Simply select the version you want or install unlisted versions from GitHub. You can also install beta version and switch back if necessary.'';
            homepage = "https://github.com/4Source/vare-obsidian-plugin";
        };
    }
);
    "variant-editor" = (
    let
        baseUrl = "https://github.com/kunalJa/VariantEditor/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "variant-editor";
        version = "1.0.7";
        outputHash = "sha256-wz8B3urrNbYt0Go4uIFnR1eAnJh4ggUTe7EFvk4Ki/o=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create variations of words and sentences and compare them, in context, with a single click'';
            homepage = "https://github.com/kunalJa/VariantEditor";
        };
    }
);
    "varinote" = (
    let
        baseUrl = "https://github.com/gsarig/obsidian-varinote/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "varinote";
        version = "1.1.2";
        outputHash = "sha256-87QeBwvTx/u/QNm1xt9KswxrSN08hUBvyfRVGvdvZrk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add variables in Templates and set their values on-the-fly during the Note creation.'';
            homepage = "https://github.com/gsarig/obsidian-varinote";
        };
    }
);
    "various-complements" = (
    let
        baseUrl = "https://github.com/tadashi-aikawa/obsidian-various-complements-plugin/releases/download/11.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "various-complements";
        version = "11.2.0";
        outputHash = "sha256-1/piS6Pg8xOiuzDB5XPjfI4o3uyl4PMirvRRvp4kZY8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Complete words similar to auto-completion in an IDE.'';
            homepage = "https://github.com/tadashi-aikawa/obsidian-various-complements-plugin";
        };
    }
);
    "vault-2-book" = (
    let
        baseUrl = "https://github.com/Mitra98t/vault2book-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-2-book";
        version = "1.0.0";
        outputHash = "sha256-iiwUNz57R4s4WOcvkh5WLZ1/sznvltm/doWV5cnr+ZM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert your notes to a book creating a single file containing all the notes linked.'';
            homepage = "https://github.com/Mitra98t/vault2book-plugin";
        };
    }
);
    "vault-ai" = (
    let
        baseUrl = "https://github.com/0xneobyte/VaultAI/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-ai";
        version = "1.0.11";
        outputHash = "sha256-LHj2d174OzgizzuJBEHOxSXeAkUsdvQ5phvYMfA4+uo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform your note-taking with an intelligent AI assistant powered by Google"s Gemini AI. Features a sleek floating chat interface for seamless writing assistance, content generation, and smart note enhancement.'';
            homepage = "https://github.com/0xneobyte/VaultAI";
        };
    }
);
    "vault-chat" = (
    let
        baseUrl = "https://github.com/exoascension/vault-chat/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-chat";
        version = "1.3.1";
        outputHash = "sha256-7QI8ovldsfOB3WAAoqtZtrNaxnnEzCAPk2Le4GjJN4A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A ChatGPT bot trained on your vault notes. Ask your AI questions about your own thoughts and ideas!'';
            homepage = "https://github.com/exoascension/vault-chat";
        };
    }
);
    "vault-encrypt" = (
    let
        baseUrl = "https://github.com/Pluppen/obsidian-vault-encrypt-plugin/releases/download/0.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-encrypt";
        version = "0.0.8";
        outputHash = "sha256-ymQ6aUH/jpdp8aziAxI113BFN2NzXLYpEkpyp5Ez1to=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Encrypts and decrypts the contents of the vault using a password.'';
            homepage = "https://github.com/Pluppen/obsidian-vault-encrypt-plugin";
        };
    }
);
    "vault-explorer" = (
    let
        baseUrl = "https://github.com/decaf-dev/obsidian-vault-explorer/releases/download/1.47.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-explorer";
        version = "1.47.4";
        outputHash = "sha256-ix4Ks7VLSwa5UBVGkjtTAK0BNxaASSFJkNZSkk4PEDo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Explore your vault in visual format'';
            homepage = "https://github.com/decaf-dev/obsidian-vault-explorer";
        };
    }
);
    "vault-file-renamer" = (
    let
        baseUrl = "https://github.com/louanfontenele/obsidian-vault-file-renamer/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-file-renamer";
        version = "1.2.2";
        outputHash = "sha256-aS0dQKTjX2AJ1/zuWDltntr7hLYwNMGN/D2gmmqtiBs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically standardizes file names to GitHub style (lowercase, no accents, only -, ., _) while preserving folder structure and file contents. '';
            homepage = "https://github.com/louanfontenele/obsidian-vault-file-renamer";
        };
    }
);
    "vault-full-statistics" = (
    let
        baseUrl = "https://github.com/jtprogru/obsidian-vault-full-statistics-plugin/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-full-statistics";
        version = "1.3.0";
        outputHash = "sha256-BurJtEl0pfCB6uj/8duRPbaE6ki+c9hNfqUuDRL0LyM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Status bar item with vault full statistics such as number of notes, files, attachments, links, tags and quality of vault.'';
            homepage = "https://github.com/jtprogru/obsidian-vault-full-statistics-plugin";
        };
    }
);
    "vault-llm-assistant" = (
    let
        baseUrl = "https://github.com/brianstm/obsidian-vault-llm-assistant/releases/download/1.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-llm-assistant";
        version = "1.0.11";
        outputHash = "sha256-v0GFsabq7hgLLLdYo999XeDzBprdxJWAeRcwJ91Ed+4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Uses AI to answer questions and create notes about your vault with proper citations'';
            homepage = "https://github.com/brianstm/obsidian-vault-llm-assistant";
        };
    }
);
    "vault-name" = (
    let
        baseUrl = "https://github.com/gapmiss/obsidian-vault-name/releases/download/0.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-name";
        version = "0.1.4";
        outputHash = "sha256-hpWOIUKeWZrtAZRUxJqnQ0ieKevSu+UByBbBXPZexaw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display and customize the vault name (title) in the side navigation file explorer.'';
            homepage = "https://github.com/gapmiss/obsidian-vault-name";
        };
    }
);
    "vault-nickname" = (
    let
        baseUrl = "https://github.com/rscopic/obsidian-vault-nickname/releases/download/1.1.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-nickname";
        version = "1.1.11";
        outputHash = "sha256-WlwZyMzWhzEv+OjStHGcPebjRS0kp++HCv+ms8ZYtKE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Override the vault"s display name. Intended to disambiguate vaults with the same folder name when adhering to a common folder structure between projects.'';
            homepage = "https://github.com/rscopic/obsidian-vault-nickname";
        };
    }
);
    "vault-review" = (
    let
        baseUrl = "https://github.com/SashaKryzh/obsidian-vault-review/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-review";
        version = "1.0.3";
        outputHash = "sha256-OU+/cTlaWPrALfRCXh6xpa8k1L1ELld7r++4bB441WY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Randomly review your vault and track progress.'';
            homepage = "https://github.com/SashaKryzh/obsidian-vault-review";
        };
    }
);
    "vault-size-history" = (
    let
        baseUrl = "https://github.com/technerium/obsidian-vault-size-history/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-size-history";
        version = "2.4.0";
        outputHash = "sha256-HW+xwP5mHmvwqdBWz5K6/O9W5p2m6W2IHY8+ufSGI1c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Graph of the Number of Files in the Vault.'';
            homepage = "https://github.com/technerium/obsidian-vault-size-history";
        };
    }
);
    "vault-stats" = (
    let
        baseUrl = "https://github.com/blueheron786/obsidian-stats-plugin/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-stats";
        version = "1.0.0";
        outputHash = "sha256-to02XnCQFrum605rBG4eY66Nw35BPYqZWNr24AcFmyY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Provides methods to retrieve statistics about the vault, such as the number of notes, total word count, recently modified notes, and more.'';
            homepage = "https://github.com/blueheron786/obsidian-stats-plugin";
        };
    }
);
    "vault-sync" = (
    let
        baseUrl = "https://github.com/thewordisbird/VaultSync/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-sync";
        version = "1.0.1";
        outputHash = "sha256-FECLr2ZSxPA9g2pvAi3Q1L7u18qt8J4JUKCxScaUQ/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync vault with cloud storage provider.'';
            homepage = "https://github.com/thewordisbird/VaultSync";
        };
    }
);
    "vault-to-blog" = (
    let
        baseUrl = "https://github.com/barkstone2/vault-to-blog/releases/download/0.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-to-blog";
        version = "0.1.1";
        outputHash = "sha256-uJr6qL/iQMWr74N6xRh5tUoo7cxA0maFgSAwy3DvGF4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish the vault to a GitHub Pages blog.'';
            homepage = "https://github.com/barkstone2/vault-to-blog";
        };
    }
);
    "vault-transfer" = (
    let
        baseUrl = "https://github.com/ImaginaryProgramming/obsidian-vault-transfer/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vault-transfer";
        version = "1.3.0";
        outputHash = "sha256-w677n6GcxGTiQh8s/Zo89yfxnjxbLI8NJ60u9id+Csc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transfers a note from one vault to another.'';
            homepage = "https://github.com/ImaginaryProgramming/obsidian-vault-transfer";
        };
    }
);
    "vcf-contacts" = (
    let
        baseUrl = "https://github.com/broekema41/obsidian-vcf-contacts/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vcf-contacts";
        version = "2.4.0";
        outputHash = "sha256-Fb23V/so7lcJp9fvA6PI/3O5uu8u883nwkJxlCXCdXM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = '' manage, organize, link and interact with your contacts. structured md files according vCard (VCF) standards'';
            homepage = "https://github.com/broekema41/obsidian-vcf-contacts";
        };
    }
);
    "vconsole" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-vconsole/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vconsole";
        version = "1.1.1";
        outputHash = "sha256-mJHuwGY+owfi3+M10qL210XGJlTfcJHGkyNYT4YXFcU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate vConsole for developers to facilitate the debugging of mobile plugins.'';
            homepage = "https://github.com/zhouhua/obsidian-vconsole";
        };
    }
);
    "vector-search" = (
    let
        baseUrl = "https://github.com/ashwin271/obsidian-vector-search/releases/download/0.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vector-search";
        version = "0.3.0";
        outputHash = "sha256-VoDljkKfuKGYPT9E6ShAiR8A/aK8uciJl1FYRHrZnmM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Semantic search for your notes using Ollama and nomic-embed-text embeddings. Requires Ollama installation.'';
            homepage = "https://github.com/ashwin271/obsidian-vector-search";
        };
    }
);
    "verovio-music-renderer" = (
    let
        baseUrl = "https://github.com/kpaede/Verovio-Music-Renderer/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "verovio-music-renderer";
        version = "1.0.9";
        outputHash = "sha256-1Ehb8prRw/8CnFcuSLMHDICS63ANZ7WKJ2Ohdx7uN4w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Verovio Music Renderer displays musical notation from various formats (MEI, MusicXML, abc) and plays it back.'';
            homepage = "https://github.com/kpaede/Verovio-Music-Renderer";
        };
    }
);
    "verse-of-the-day" = (
    let
        baseUrl = "https://github.com/janisringli/verse-of-the-day-for-obsidian/releases/download/2.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "verse-of-the-day";
        version = "2.0.0";
        outputHash = "sha256-XHQlqTgaJN+fz822TptnDpl+J7U920kJAw01pEHDhE8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Lets you add the verse of the day from YouVersion to your notes'';
            homepage = "https://github.com/janisringli/verse-of-the-day-for-obsidian";
        };
    }
);
    "version-control" = (
    let
        baseUrl = "https://github.com/Yuichi-Aragi/Version-Control/releases/download/1.9.44";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "version-control";
        version = "1.9.44";
        outputHash = "sha256-irJ8rztdETRHrG73Ra5ejHX4vl9H2ce8iV4+SqsL6bw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A local, snapshot-based version control system. Lets you create finite number of versions of any note.'';
            homepage = "https://github.com/Yuichi-Aragi/Version-Control";
        };
    }
);
    "vertical-tabs" = (
    let
        baseUrl = "https://github.com/oxdc/obsidian-vertical-tabs/releases/download/0.16.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vertical-tabs";
        version = "0.16.6";
        outputHash = "sha256-ILrIdpWERhehgQHsdsm+u759XqyF9qJKPtoGRn1zDIM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Offer an alternative view that displays open tabs vertically, allowing users to group and organize tabs for a better navigation experience.'';
            homepage = "https://github.com/oxdc/obsidian-vertical-tabs";
        };
    }
);
    "vertical-timeline-list" = (
    let
        baseUrl = "https://github.com/Jalad25/vertical-timeline-list/releases/download/2.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vertical-timeline-list";
        version = "2.1.1";
        outputHash = "sha256-q9Ju+2E0TpEXire01t8QaYNW0BE3rNcuVsdf6Mum8yE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Utilizes lists to create a timeline... or something resembling a timeline.'';
            homepage = "https://github.com/Jalad25/vertical-timeline-list";
        };
    }
);
    "vextab" = (
    let
        baseUrl = "https://github.com/luigman/obsidian-vextab/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vextab";
        version = "1.0.1";
        outputHash = "sha256-MyChT1sNIA4tC+eUrl8wIZyPDaPAws3wCzECMkYqorQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render guitar tablature and music notation using Vextab.'';
            homepage = "https://github.com/luigman/obsidian-vextab";
        };
    }
);
    "view-count" = (
    let
        baseUrl = "https://github.com/decaf-dev/obsidian-view-count/releases/download/2.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "view-count";
        version = "2.5.0";
        outputHash = "sha256-ExQVRWPPgl3YkGh+HI64roTWDZBW2xpQsMWLw9iMAu4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Tracks view count for each vault file.'';
            homepage = "https://github.com/decaf-dev/obsidian-view-count";
        };
    }
);
    "viewer-ftags" = (
    let
        baseUrl = "https://github.com/d7sd6u/obsidian-viewer-ftags/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "viewer-ftags";
        version = "1.1.0";
        outputHash = "sha256-9OoXKEK8BWZTDh/3LP3ylLin09dpAzh3fwQxVt4k3Z0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add file"s ftags as chips at the top of the markdown view.'';
            homepage = "https://github.com/d7sd6u/obsidian-viewer-ftags";
        };
    }
);
    "vika-sync" = (
    let
        baseUrl = "https://github.com/romantic-black/obsidain-vika-sync/releases/download/0.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vika-sync";
        version = "0.3.1";
        outputHash = "sha256-XZ95jVVcLfVmEZe5QQLPDrquRJHJKsZ5FbVu/2JPjdI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync your note to Vika.'';
            homepage = "https://github.com/romantic-black/obsidain-vika-sync";
        };
    }
);
    "vikunja-sync" = (
    let
        baseUrl = "https://github.com/Heiss/obsidian-vikunja-plugin/releases/download/1.0.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vikunja-sync";
        version = "1.0.19";
        outputHash = "sha256-FDar7IYB862jcrUy5W4dHtRWnFpFx1rGNt0abnu/x2w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Synchronizes tasks to Vikunja.'';
            homepage = "https://github.com/Heiss/obsidian-vikunja-plugin";
        };
    }
);
    "vim-im-control" = (
    let
        baseUrl = "https://github.com/hideakitai/obsidian-vim-im-control/releases/download/0.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vim-im-control";
        version = "0.2.0";
        outputHash = "sha256-hL0F9TmGS/nNxUjdBqfET2+jPFgNr5puMj3m7HJDH/w=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Switch input method when `InsertLeave` and `InsertEnter`. Supports macOS, Windows, and Linux.'';
            homepage = "https://github.com/hideakitai/obsidian-vim-im-control";
        };
    }
);
    "vim-im-select" = (
    let
        baseUrl = "https://github.com/ALONELUR/vim-im-select-obsidian/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vim-im-select";
        version = "0.2.1";
        outputHash = "sha256-GxaNLVUkp1zRUigmGHrPQaa3KG1XAqtrBmpX245Jv8k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Support auto select the apposite input method in different vim mode.'';
            homepage = "https://github.com/ALONELUR/vim-im-select-obsidian";
        };
    }
);
    "vim-marker-sharpener" = (
    let
        baseUrl = "https://github.com/artemDvoryadkin/obsidian-vim-marker-sharpener/releases/download/0.2.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vim-marker-sharpener";
        version = "0.2.7";
        outputHash = "sha256-1QObbJtEgatvTkFpmoXTLpsgh3M6TGL4U5E4laa3xuk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Text formatting commands in Vim mode. Supports applying styles (bold, italic, etc.). Works correctly in visual mode with selected text.'';
            homepage = "https://github.com/artemDvoryadkin/obsidian-vim-marker-sharpener";
        };
    }
);
    "vim-toggle" = (
    let
        baseUrl = "https://github.com/connerohnesorge/vim-toggle/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vim-toggle";
        version = "1.1.0";
        outputHash = "sha256-Pw41Mrgn67xktxURSrM+y1g2CnjxU4cUiGeZ94pjMAI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Toggle Vim on/off and customize toggling behavior.'';
            homepage = "https://github.com/connerohnesorge/vim-toggle";
        };
    }
);
    "vim-yank-highlight" = (
    let
        baseUrl = "https://github.com/aleksey-rowan/obsidian-vim-yank-highlight/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vim-yank-highlight";
        version = "1.0.8";
        outputHash = "sha256-K36V3jDSqG5VBHLiEjkkNi3CllH1/fA5tmEm0mrtvzc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Highlight yanked text in Vim mode. Enjoy that subtle animation you"ve missed so much.'';
            homepage = "https://github.com/aleksey-rowan/obsidian-vim-yank-highlight";
        };
    }
);
    "vimium" = (
    let
        baseUrl = "https://github.com/karstenpedersen/obsidian-vimium/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vimium";
        version = "1.0.0";
        outputHash = "sha256-Ya07MNoo5mVfPjjELFuH2h+2amC2ffN+cPARMKfrS3U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Interact with elements using keyboard shortcuts in the spirit of Vim.'';
            homepage = "https://github.com/karstenpedersen/obsidian-vimium";
        };
    }
);
    "virt-folder" = (
    let
        baseUrl = "https://github.com/gr0grig/obsidian-virt-folder/releases/download/1.1.19";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "virt-folder";
        version = "1.1.19";
        outputHash = "sha256-6ZeVOFJGaxnNiCrm1yVhACBpMLHlciHUDdU9Rwfv05g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Creating a hierarchical structure like Luhmann"s Zettelkasten'';
            homepage = "https://github.com/gr0grig/obsidian-virt-folder";
        };
    }
);
    "virtual-footer" = (
    let
        baseUrl = "https://github.com/Signynt/virtual-content/releases/download/1.0.27";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "virtual-footer";
        version = "1.0.27";
        outputHash = "sha256-yUoNoCMByoTebm7ut+OGDyL4SnryFxrfFuL0QGloG38=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display markdown text (including dataview queries or Obsidian bases) at the bottom, top or in the sidebar for all notes which match a specified rule, without modifying them.'';
            homepage = "https://github.com/Signynt/virtual-content";
        };
    }
);
    "virtual-linker" = (
    let
        baseUrl = "https://github.com/vschroeter/obsidian-virtual-linker/releases/download/1.5.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "virtual-linker";
        version = "1.5.2";
        outputHash = "sha256-p2cYzsqPx8KIoWTh6tk60H+zQIHiznb6nwPAY82ByVQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically creates virtual links for text within your notes that match the titles or aliases of other notes in your vault. Create a glossary-like functionality, show unlinked mentions and transform them to real links.'';
            homepage = "https://github.com/vschroeter/obsidian-virtual-linker";
        };
    }
);
    "virustotal-enrich" = (
    let
        baseUrl = "https://github.com/ytisf/virustotal-enrich/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "virustotal-enrich";
        version = "0.0.4";
        outputHash = "sha256-fdVSUzxPy6Rlg6ojffJpIJf7yvWM1g2qJEmo+DdzqJE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enrich your notes with information from VirusTotal.'';
            homepage = "https://github.com/ytisf/virustotal-enrich";
        };
    }
);
    "vision-recall" = (
    let
        baseUrl = "https://github.com/travisvn/obsidian-vision-recall/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vision-recall";
        version = "1.2.0";
        outputHash = "sha256-49qamsX9isXDqpDDIPc4aFXVT6E9q24NTqMl6dY5EFg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform screenshots into searchable notes using AI vision and text analysis.'';
            homepage = "https://github.com/travisvn/obsidian-vision-recall";
        };
    }
);
    "visited-countries" = (
    let
        baseUrl = "https://github.com/IvanPeshykov/obsidian-visited-countries/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "visited-countries";
        version = "1.0.4";
        outputHash = "sha256-C/l8Jsy5Jk6eaET4L8K1U+aJmd3Salx+005aY6qXEzI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Mark and visualize the countries you"ve visited on an interactive world map.'';
            homepage = "https://github.com/IvanPeshykov/obsidian-visited-countries";
        };
    }
);
    "visual-crossing-weather" = (
    let
        baseUrl = "https://github.com/willasm/vc-weather/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "visual-crossing-weather";
        version = "1.1.5";
        outputHash = "sha256-Qzfra2Mfr5ysUS18G1L7nbq61D9pefANIZpxIPUTrgg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visual Crossing Weather API for Your Notes'';
            homepage = "https://github.com/willasm/vc-weather";
        };
    }
);
    "vitepress-publisher" = (
    let
        baseUrl = "https://github.com/tyrad/obsidian-vitepress/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vitepress-publisher";
        version = "1.3.1";
        outputHash = "sha256-Ngv41t8+RvaeazCrGR5CPn2igprrpiMrIKr9XdOYC9M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Conveniently preview and publish Markdown files using vitepress.'';
            homepage = "https://github.com/tyrad/obsidian-vitepress";
        };
    }
);
    "vk-group-notifier" = (
    let
        baseUrl = "https://github.com/filichev-evgeny/obsidianvkupdatenotifier/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vk-group-notifier";
        version = "1.0.2";
        outputHash = "sha256-J9DNso7wKgzMf4kGkFT9cCHlzJqzzGCRppydcQnDDBk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track news posts from vk.com groups.'';
            homepage = "https://github.com/filichev-evgeny/obsidianvkupdatenotifier";
        };
    }
);
    "vlc-bridge" = (
    let
        baseUrl = "https://github.com/zuluwi/obsidian-vlc-bridge/releases/download/0.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vlc-bridge";
        version = "0.2.1";
        outputHash = "sha256-Pl+g478+oAwZ4S5+ukgLGL4+IdwcS1vf0qR7fMay+a4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Take video/movie notes with timestamp links and snapshots from VLC Player.'';
            homepage = "https://github.com/zuluwi/obsidian-vlc-bridge";
        };
    }
);
    "vn-memory-lane" = (
    let
        baseUrl = "https://github.com/bangca85/obsidian-memorylane-plugin/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vn-memory-lane";
        version = "1.0.1";
        outputHash = "sha256-uRhIVpwK64cjdVraC8zwcVKEkoAQU7wKKX6hHoMoe38=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Relive and celebrate your life"s milestones on a personal, interactive timeline. A nostalgic journey through your history with anniversary reminders and cherished memories.'';
            homepage = "https://github.com/bangca85/obsidian-memorylane-plugin";
        };
    }
);
    "vocabulary-cards" = (
    let
        baseUrl = "https://github.com/meniam/obsidian-vocabulary-cards/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vocabulary-cards";
        version = "1.0.3";
        outputHash = "sha256-yGP03LP5RMvcYl9MCNpZHD3ZW2W6MU5sNf4AVwAZm+0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An easy way to display vocabulary words as flashcards and as a list.'';
            homepage = "https://github.com/meniam/obsidian-vocabulary-cards";
        };
    }
);
    "vocabulary-highlighter" = (
    let
        baseUrl = "https://github.com/eatgrass/obsidian-vocab-highlighter/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vocabulary-highlighter";
        version = "1.0.8";
        outputHash = "sha256-LLpIrtDDsha0ySJhSW248vP5nSFtWMAPiG1RIQ/9UCM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hightlight vocabulary based on the word frequency.'';
            homepage = "https://github.com/eatgrass/obsidian-vocab-highlighter";
        };
    }
);
    "voice" = (
    let
        baseUrl = "https://github.com/chrisurf/obsidian-voice/releases/download/1.7.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "voice";
        version = "1.7.3";
        outputHash = "sha256-8+L40RO0D+yIhX5XN/e2qzucoK7bYFmEkHA/8B336jc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Text-to-speech adds sound, audio, and speech to your notes, letting them talk in your workspace, mobile-friendly, perfect for learning or reinforcing ideas as you listen hands-free in an audiobook-like experience.'';
            homepage = "https://github.com/chrisurf/obsidian-voice";
        };
    }
);
    "voicenotes-sync" = (
    let
        baseUrl = "https://github.com/voicenotes-community/voicenotes-sync/releases/download/1.0.21";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "voicenotes-sync";
        version = "1.0.21";
        outputHash = "sha256-qSOXr9PJMkPH0PEbyY+97/xyscwlzIygVgVIKBQlRN4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A plugin for syncing Voicenotes.com data into notes'';
            homepage = "https://github.com/voicenotes-community/voicenotes-sync";
        };
    }
);
    "vox" = (
    let
        baseUrl = "https://github.com/vincentbavitz/obsidian-vox/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vox";
        version = "1.1.1";
        outputHash = "sha256-NMcx4ZELt5rJ5Ft5FCyCbAPVwjCPVHf0Scus8ViUAlI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Intelligently transcribe and categorize your voice notes.'';
            homepage = "https://github.com/vincentbavitz/obsidian-vox";
        };
    }
);
    "vscode-editor" = (
    let
        baseUrl = "https://github.com/sunxvming/obsidian-vscode-editor/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "vscode-editor";
        version = "1.0.5";
        outputHash = "sha256-HGSTSxMwyfb+BLIVfHzOCLgoUpAZRuddss6h3oOtPOc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Edit Code Files like VSCode.'';
            homepage = "https://github.com/sunxvming/obsidian-vscode-editor";
        };
    }
);
    "waka-time-box" = (
    let
        baseUrl = "https://github.com/simonla/obsidian_waka_box/releases/download/1.0.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "waka-time-box";
        version = "1.0.8";
        outputHash = "sha256-JdKHZrGp6w+T7CSmoP5zVclUSrVOwb9MCFjPnkqvG7M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show daily coding activity from WakaTime.'';
            homepage = "https://github.com/simonla/obsidian_waka_box";
        };
    }
);
    "wakatime-kvh" = (
    let
        baseUrl = "https://github.com/Kovah/obsidian-wakatime/releases/download/1.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wakatime-kvh";
        version = "1.0.7";
        outputHash = "sha256-WRuqJrZtrJe3Yxia4SMG+FUX23iHK3XVCDBlvcsdShc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Connect to Wakatime or Wakapi to track the time spent while browsing or writing notes.'';
            homepage = "https://github.com/Kovah/obsidian-wakatime";
        };
    }
);
    "wardley-maps" = (
    let
        baseUrl = "https://github.com/damonsk/obsidian-wardley-maps/releases/download/1.0.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wardley-maps";
        version = "1.0.10";
        outputHash = "sha256-LYCDN7JKh4TdoWcsJOXTrlohSHMCwi9uIJODU6DIp2I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View and edit Wardley Maps directly within your notes using the popular Online Wardley Maps (OWM) format.'';
            homepage = "https://github.com/damonsk/obsidian-wardley-maps";
        };
    }
);
    "watched-metadata" = (
    let
        baseUrl = "https://github.com/NailAhmed/Watched-Metadata/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "watched-metadata";
        version = "1.0.0";
        outputHash = "sha256-GNTah39xeRmtI2BVdWBq8t99d54ZmliE78PLsWtr6m0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Watches for changes in metadata and performs user-specified actions based on these changes.'';
            homepage = "https://github.com/NailAhmed/Watched-Metadata";
        };
    }
);
    "water-tracker" = (
    let
        baseUrl = "https://github.com/LuigiCerone/obsidian-water-tracker/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "water-tracker";
        version = "1.0.0";
        outputHash = "sha256-kVfHmXnE9BeCWCV5MbbOcZKsPZ+UwWu/H9unmo/EPWc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep track of how much water you drunk'';
            homepage = "https://github.com/LuigiCerone/obsidian-water-tracker";
        };
    }
);
    "waveform-player" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-waveform-player/releases/download/0.0.7";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "waveform-player";
        version = "0.0.7";
        outputHash = "sha256-Td7VI6IornzrDVxhgx/wGL+FV6uyR3FV3HRAcIBtbSc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render audio files as waveforms'';
            homepage = "https://github.com/zhouhua/obsidian-waveform-player";
        };
    }
);
    "wayback-archiver" = (
    let
        baseUrl = "https://github.com/IshizuEitaro/obsidian-wayback-archiver/releases/download/1.1.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wayback-archiver";
        version = "1.1.5";
        outputHash = "sha256-wZ1blkfDeNwCrSZcLjPqGsmX/z/xMI0LA54hetwMSbI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically archives web links via Wayback Machine and appends archived versions in notes.'';
            homepage = "https://github.com/IshizuEitaro/obsidian-wayback-archiver";
        };
    }
);
    "waypoint" = (
    let
        baseUrl = "https://github.com/IdreesInc/Waypoint/releases/download/3.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "waypoint";
        version = "3.0.1";
        outputHash = "sha256-WSMEwDftjWHYzy9wwHFlenIsQpcn7SSiUYNWXGwaiOw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily generate dynamic MOCs in your folder notes using waypoints. Enables folders to show up in the graph view and removes the need for messy tags!'';
            homepage = "https://github.com/IdreesInc/Waypoint";
        };
    }
);
    "weather-fetcher" = (
    let
        baseUrl = "https://github.com/fyears/weather-fetcher/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "weather-fetcher";
        version = "0.0.3";
        outputHash = "sha256-qMVaEa0Uy+3Rbn7mNKKl1MsHYHiku+LV0cADCsH2lgI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch and insert current weather into a note.'';
            homepage = "https://github.com/fyears/weather-fetcher";
        };
    }
);
    "weather-widget" = (
    let
        baseUrl = "https://github.com/mr-asa/obsidian_weather/releases/download/0.1.10";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "weather-widget";
        version = "0.1.10";
        outputHash = "sha256-9pZ66OMlVTa4QFOQQiwYD/8h20TJBZ7KRDpUOHlK1YY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Weather widget for display in notes, Canvas, and a separate tab.'';
            homepage = "https://github.com/mr-asa/obsidian_weather";
        };
    }
);
    "webdav-explorer" = (
    let
        baseUrl = "https://github.com/sunmagicshow/obsidian-webdav/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "webdav-explorer";
        version = "1.0.5";
        outputHash = "sha256-TLhseVgerAz0UU78Gzr2FO8tnMEWPpGLEWVdE7ZnnSU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''WebDAV Explorer: Connect to WebDAV server, preview files directly in web view, and generate links through simple drag-and-drop.'';
            homepage = "https://github.com/sunmagicshow/obsidian-webdav";
        };
    }
);
    "webdav-file-explorer" = (
    let
        baseUrl = "https://github.com/red0orange/obsidian-webdav-file-explorer/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "webdav-file-explorer";
        version = "1.3.1";
        outputHash = "sha256-DeKfSYJKR4qpaHAhD3QiUVPH+9WX3QPBTpZ2NcUnUmQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Explore your webdav files.'';
            homepage = "https://github.com/red0orange/obsidian-webdav-file-explorer";
        };
    }
);
    "webdav-image-uploader" = (
    let
        baseUrl = "https://github.com/Koishiiko/obsidian-webdav-image-uploader/releases/download/1.4.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "webdav-image-uploader";
        version = "1.4.1";
        outputHash = "sha256-5JzAXhNiWESc7OpE8KLjykln2T2Gxfqxphu6J3CdMUw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Uploads, downloads and deletes images on WebDAV server within notes.'';
            homepage = "https://github.com/Koishiiko/obsidian-webdav-image-uploader";
        };
    }
);
    "webpage-html-export" = (
    let
        baseUrl = "https://github.com/KosmosisDire/obsidian-webpage-export/releases/download/1.9.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "webpage-html-export";
        version = "1.9.2";
        outputHash = "sha256-u77W5ms+jYU+vCmIwGQwEp9CJIFpGbeQ9pT94uGIZyc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Export html from single files, canvas pages, or whole vaults. Direct access to the exported HTML files allows you to publish your digital garden anywhere. Focuses on flexibility, features, and style parity.'';
            homepage = "https://github.com/KosmosisDire/obsidian-webpage-export";
        };
    }
);
    "webviewer-bookmarks" = (
    let
        baseUrl = "https://github.com/stefandanzl/webviewer-bookmarks/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "webviewer-bookmarks";
        version = "1.5.0";
        outputHash = "sha256-gZyF+T4lsrZCV1CuFW9vdpVnkRrQlJxzPvS11yZsj/s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and manage bookmarks for the built-in Web viewer.'';
            homepage = "https://github.com/stefandanzl/webviewer-bookmarks";
        };
    }
);
    "wechat-public-platform" = (
    let
        baseUrl = "https://github.com/ai-chen2050/obsidian-wechat-public-platform/releases/download/1.0.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wechat-public-platform";
        version = "1.0.9";
        outputHash = "sha256-Vqxp11pj8faX5QEFVvUd1hnjFfL9KJv0z+plhnv9dfA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Release the article from your vault to WeChat, Baidu Baijiahao, or another article release platform.'';
            homepage = "https://github.com/ai-chen2050/obsidian-wechat-public-platform";
        };
    }
);
    "weekly-goal-tracker" = (
    let
        baseUrl = "https://github.com/GitGorman/weekly-goal-tracker/releases/download/1.5.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "weekly-goal-tracker";
        version = "1.5.1";
        outputHash = "sha256-VcC40AFXlD8zBGzd22KY/RuV7VJiEPs9lbd786ml0/A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep track of weekly goals in the status bar'';
            homepage = "https://github.com/GitGorman/weekly-goal-tracker";
        };
    }
);
    "weekly-review" = (
    let
        baseUrl = "https://github.com/brandonkboswell/weekly-review/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "weekly-review";
        version = "1.2.0";
        outputHash = "sha256-XHJO+kNegRC1kCObQsY6PLJ+6K6ycPM2VrpOss2i1Ro=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open all the files you have created in the last week.'';
            homepage = "https://github.com/brandonkboswell/weekly-review";
        };
    }
);
    "weekly-review-linker" = (
    let
        baseUrl = "https://github.com/AdityaKhowalGithub/weekly-review-notes-linker/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "weekly-review-linker";
        version = "1.1.0";
        outputHash = "sha256-7AdF7SyJb94gR4nT3Sa+Kuev3AHc+59pwnmQmUIVlZo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''This links all of the files you have created in the last week into a Weekly Review note.'';
            homepage = "https://github.com/AdityaKhowalGithub/weekly-review-notes-linker";
        };
    }
);
    "wewrite" = (
    let
        baseUrl = "https://github.com/learnerchen-forever/wewrite/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wewrite";
        version = "1.1.6";
        outputHash = "sha256-7Z8MkFzPHmolOo4C0KeBu9E5gxRZbJ/2vI/ohjwva+c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''AI assisting writing, Rendering and publishing notes to WeChat MP platform'';
            homepage = "https://github.com/learnerchen-forever/wewrite";
        };
    }
);
    "whatsapp-backup" = (
    let
        baseUrl = "https://github.com/LuigiCerone/obsidian-whatsapp-backup-importer/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "whatsapp-backup";
        version = "1.1.0";
        outputHash = "sha256-UmTKVDlD6UoD7wE9mTXCv0m6wNtechMaZ8h6kzb8jWQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import WhatsApp export inside your vault.'';
            homepage = "https://github.com/LuigiCerone/obsidian-whatsapp-backup-importer";
        };
    }
);
    "whatsapp-export-note" = (
    let
        baseUrl = "https://github.com/JoaoEmanuell/obsidian-whatsapp-export-note/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "whatsapp-export-note";
        version = "1.0.5";
        outputHash = "sha256-63v2IBG8lYblWn4T9IFJEG+QvK8kUrup2px7rYcgT0k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert the current note for WhatsApp format to share.'';
            homepage = "https://github.com/JoaoEmanuell/obsidian-whatsapp-export-note";
        };
    }
);
    "wheel-tab-switcher" = (
    let
        baseUrl = "https://github.com/22-2/wheel-tab-switcher/releases/download/0.2.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wheel-tab-switcher";
        version = "0.2.9";
        outputHash = "sha256-eFoNlDIEvsqJ7GU6/VIG5UkqO4hOwOrP8WJOiWAz2Zs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Switch between Tabs using your mouse wheel when hovering over tab headers'';
            homepage = "https://github.com/22-2/wheel-tab-switcher";
        };
    }
);
    "whisper" = (
    let
        baseUrl = "https://github.com/nikdanilov/whisper-obsidian-plugin/releases/download/1.9.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "whisper";
        version = "1.9.1";
        outputHash = "sha256-YsezdUCUvFKkWsD5LeGXg+tkKjX21GoHRU/Z461qBjE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Speech-to-text using OpenAI Whisper.'';
            homepage = "https://github.com/nikdanilov/whisper-obsidian-plugin";
        };
    }
);
    "whisperer" = (
    let
        baseUrl = "https://github.com/Falcion/Whisperer.md/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "whisperer";
        version = "1.1.1";
        outputHash = "sha256-7IA7aJJ0TQqY+J8tq+TilF+TmaRxQFYUV3MDV2TcD1M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Play ambience in your vault, assign audio to files through metadata with support of local (from vault) and URLs (Youtube, Soundcloud) versions of audio: works on mobile vaults.'';
            homepage = "https://github.com/Falcion/Whisperer.md";
        };
    }
);
    "white-noise" = (
    let
        baseUrl = "https://github.com/zhouhua/obsidian-white-noise/releases/download/0.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "white-noise";
        version = "0.0.4";
        outputHash = "sha256-sLcpfcUVIebDYXq7lXGKfYDg+DcKFlrp0wWS6STUULg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Play white noise to help you focus on your work.'';
            homepage = "https://github.com/zhouhua/obsidian-white-noise";
        };
    }
);
    "widgets" = (
    let
        baseUrl = "https://github.com/rafaelveiga/obsidian-widgets/releases/download/0.0.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "widgets";
        version = "0.0.11";
        outputHash = "sha256-xtvEjLx0/EvU9kqrdpB+iGo1r25EOgGHE+bn+BIgl7M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add widgets to your notes like clock, countdown, and quotes.'';
            homepage = "https://github.com/rafaelveiga/obsidian-widgets";
        };
    }
);
    "wielder" = (
    let
        baseUrl = "https://github.com/victorb/obsidian-wielder/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wielder";
        version = "1.0.5";
        outputHash = "sha256-mqieAeaIo59rKZp+bB+Tr/KNXLIAayz+dBrXJommwlA=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Run Clojure/ClojureScript inside your notes.'';
            homepage = "https://github.com/victorb/obsidian-wielder";
        };
    }
);
    "wikidata-importer" = (
    let
        baseUrl = "https://github.com/samwho/obsidian-wikidata-importer/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wikidata-importer";
        version = "1.1.3";
        outputHash = "sha256-xjwXHje7zjnXbhw6LklZhtYuRObhjWkL4vnEg8sdG3U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import data from Wikidata into your vault.'';
            homepage = "https://github.com/samwho/obsidian-wikidata-importer";
        };
    }
);
    "wikidocs" = (
    let
        baseUrl = "https://github.com/pahkey/obsidian-wikidocs-plugin/releases/download/1.1.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wikidocs";
        version = "1.1.6";
        outputHash = "sha256-/Ivqgt+Hn2+8MW4TVqRPuyTqGqHK5mhV24nQ2AWopxQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch, edit, and upload WikiDocs books.'';
            homepage = "https://github.com/pahkey/obsidian-wikidocs-plugin";
        };
    }
);
    "wikilinks-to-mdlinks-obsidian" = (
    let
        baseUrl = "https://github.com/agathauy/wikilinks-to-mdlinks-obsidian/releases/download/0.0.12";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wikilinks-to-mdlinks-obsidian";
        version = "0.0.12";
        outputHash = "sha256-8e6n9XcUlw42mIG4Q5lzQMZhYo1U8hfKdwC0VCs7ABI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert wikilinks to Markdown links and vice versa.'';
            homepage = "https://github.com/agathauy/wikilinks-to-mdlinks-obsidian";
        };
    }
);
    "wikipedia-search" = (
    let
        baseUrl = "https://github.com/StrangeGirlMurph/obsidian-wikipedia-helper/releases/download/2.7.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wikipedia-search";
        version = "2.7.0";
        outputHash = "sha256-Fk7IAA1DVO87aji89iF+8MI/6FckdTvt0tg6QqQ+19s=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Search, link, insert and open Wikipedia/Wikimedia articles.'';
            homepage = "https://github.com/StrangeGirlMurph/obsidian-wikipedia-helper";
        };
    }
);
    "window-collapse" = (
    let
        baseUrl = "https://github.com/gquental/obsidian-window-collapse/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "window-collapse";
        version = "1.0.1";
        outputHash = "sha256-BGviwwT+F0tZeDQWHYcvTZFGGr11VnYaWHWjalhz9oM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An easy way to collapse the sidebars without going fullscreen.'';
            homepage = "https://github.com/gquental/obsidian-window-collapse";
        };
    }
);
    "wonderbox" = (
    let
        baseUrl = "https://github.com/Chrstn67/WonderBox/releases/download/1.1.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wonderbox";
        version = "1.1.2";
        outputHash = "sha256-fi9WB5wXtqh8R4cwYQuilC+zvTj+Do72/PiQELTGJrY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create more relevant text sections your tips, top notes, warnings and more'';
            homepage = "https://github.com/Chrstn67/WonderBox";
        };
    }
);
    "word-frequency" = (
    let
        baseUrl = "https://github.com/mts7/obsidian-word-frequency/releases/download/1.3.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "word-frequency";
        version = "1.3.8";
        outputHash = "sha256-4aGN4SU45m2mFWECFNzejqRkBKbFNK/4WPbVT7zybjU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Counts the most frequently used words in a note and displays them in the sidebar.'';
            homepage = "https://github.com/mts7/obsidian-word-frequency";
        };
    }
);
    "wordcraft" = (
    let
        baseUrl = "https://github.com/danferns/obsidian-wordcraft/releases/download/1.1.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wordcraft";
        version = "1.1.1";
        outputHash = "sha256-GrH7SUjMpL/uCZ/mCxRA0zOz3qZZjYOcRs8ydwtn248=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Find rhymes, synonyms, and describing words. Designed for songwriting and poetry.'';
            homepage = "https://github.com/danferns/obsidian-wordcraft";
        };
    }
);
    "wordflow-tracker" = (
    let
        baseUrl = "https://github.com/LeCheenaX/WordFlow-Tracker/releases/download/2.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wordflow-tracker";
        version = "2.2.1";
        outputHash = "sha256-6yxIvGqo3SFNzBLCU5SWl9Yu5mIisnWsC4LHpbSO9fs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Track the changes and stats of your edited note files automatically. Record the modified notes and statistics to your daily note'';
            homepage = "https://github.com/LeCheenaX/WordFlow-Tracker";
        };
    }
);
    "wordwise" = (
    let
        baseUrl = "https://github.com/ckt1031/obsidian-wordwise-plugin/releases/download/1.4.0-beta.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wordwise";
        version = "1.4.0-beta.1";
        outputHash = "sha256-yI6jZUdIyYoK4BBQcepIezUc0iSMAL28k+zQJoqoRXE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Writing companion for AI content generation.'';
            homepage = "https://github.com/ckt1031/obsidian-wordwise-plugin";
        };
    }
);
    "workbench-obsidian" = (
    let
        baseUrl = "https://github.com/ryanjamurphy/workbench-obsidian/releases/download/1.6.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "workbench-obsidian";
        version = "1.6.4";
        outputHash = "sha256-oOkxHltakn/EiIta74oVAADvCKzIFX2BM5KZikn214M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Keep a workbench of knowledge materials.'';
            homepage = "https://github.com/ryanjamurphy/workbench-obsidian";
        };
    }
);
    "workbooks" = (
    let
        baseUrl = "https://github.com/Canna71/obsidian-sheets/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "workbooks";
        version = "1.0.1";
        outputHash = "sha256-2RqUWSEULA7Ii+I87unz4DnSDRhB05/uA+3su5hDxt8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Work with spreadsheets inside your notes.'';
            homepage = "https://github.com/Canna71/obsidian-sheets";
        };
    }
);
    "workona-to-obsidian" = (
    let
        baseUrl = "https://github.com/Holmes555/workona-to-obsidian/releases/download/0.3.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "workona-to-obsidian";
        version = "0.3.3";
        outputHash = "sha256-JqY0pWBXQudXyhI3La8gOssG73mO79RJ3A9qUg80Lds=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Workona resources through generated JSON file.'';
            homepage = "https://github.com/Holmes555/workona-to-obsidian";
        };
    }
);
    "workout-planner" = (
    let
        baseUrl = "https://github.com/SpatariuRares/obsidian-workout-plugin/releases/download/1.4.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "workout-planner";
        version = "1.4.6";
        outputHash = "sha256-5BXSnKrrGbqS4rj22k4xsgjaY7mdEZ4v/BO3IaVj1vE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize workout data with interactive charts and advanced search capabilities.'';
            homepage = "https://github.com/SpatariuRares/obsidian-workout-plugin";
        };
    }
);
    "workout-tracker" = (
    let
        baseUrl = "https://github.com/wanabeunique/obsidian-workout-tracker/releases/download/1.0.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "workout-tracker";
        version = "1.0.5";
        outputHash = "sha256-Do48Jhi0LFpFK3k8aDc2QBIS+FkxQ8NBoHEmyCgaXR8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Log workouts, track progress, and view exercise statistic.'';
            homepage = "https://github.com/wanabeunique/obsidian-workout-tracker";
        };
    }
);
    "workspaces-plus" = (
    let
        baseUrl = "https://github.com/jsmorabito/obsidian-workspaces-plus/releases/download/0.4.16";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "workspaces-plus";
        version = "0.4.16";
        outputHash = "sha256-wU0OrHqbUK7b6dKjMMl8NqRMcdYrNhYiQurHkyiCj8k=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Quickly switch and manage workspaces.'';
            homepage = "https://github.com/jsmorabito/obsidian-workspaces-plus";
        };
    }
);
    "wpm-reading-time" = (
    let
        baseUrl = "https://github.com/santiyounger/wpm-reading-time/releases/download/1.1.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wpm-reading-time";
        version = "1.1.8";
        outputHash = "sha256-FPs81uhzHvEvjlsUYv7gzfaWqfFK//shjGF+vYQszHo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Calculate reading time estimates for selected text using your own reading speed in Words Per Minute.'';
            homepage = "https://github.com/santiyounger/wpm-reading-time";
        };
    }
);
    "wrangle-todos" = (
    let
        baseUrl = "https://github.com/Jeel-Shah/todo-wrangler/releases/download/1.0.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wrangle-todos";
        version = "1.0.4";
        outputHash = "sha256-4nz6OkHN7jeC/BxpDlqRnonUq8ISBBV9bXF1Zgg8974=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Wrangle your TODOs and put them at the bottom of the file.'';
            homepage = "https://github.com/Jeel-Shah/todo-wrangler";
        };
    }
);
    "write-good" = (
    let
        baseUrl = "https://github.com/markahesketh/write-good-obsidian/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "write-good";
        version = "1.2.1";
        outputHash = "sha256-aYf2+ioSIu6lfKdJeN/GNiRVRnuvMxm+y22p9InhYHI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Linter for English prose and improving writing style'';
            homepage = "https://github.com/markahesketh/write-good-obsidian";
        };
    }
);
    "writeas-publisher" = (
    let
        baseUrl = "https://github.com/encima/obsidian-writeas-plugin/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "writeas-publisher";
        version = "1.0.2";
        outputHash = "sha256-7IH2iaJYTVImpTxXQCI9Cx7Xf5nFNRjSyfTPd+Ajp0U=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish notes to any write.as blogs you own.'';
            homepage = "https://github.com/encima/obsidian-writeas-plugin";
        };
    }
);
    "writing" = (
    let
        baseUrl = "https://github.com/johackim/obsidian-writing/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "writing";
        version = "0.4.0";
        outputHash = "sha256-lyx3xZpDoSEe8ULc3rWO7mNZvtbVYlf1KCTEo8FcZUw=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Write and format your next book.'';
            homepage = "https://github.com/johackim/obsidian-writing";
        };
    }
);
    "writing-goals" = (
    let
        baseUrl = "https://github.com/lynchjames/obsidian-writing-goals/releases/download/0.10.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "writing-goals";
        version = "0.10.11";
        outputHash = "sha256-ZaHGyJ0bbV+ajmT9WvS7d8ZTRwhp0DF5rsGDkfwiOsE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Set dynamic writing goals for your notes and folders.'';
            homepage = "https://github.com/lynchjames/obsidian-writing-goals";
        };
    }
);
    "wucai-highlights-official" = (
    let
        baseUrl = "https://github.com/makediff/obsidian-wucai/releases/download/26.4.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wucai-highlights-official";
        version = "26.4.11";
        outputHash = "sha256-NHjTs8NfdQUwq6eGOwK3UWpJ1Em6ixfivPUHa6p3+Gk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync WuCai highlights into your notes.'';
            homepage = "https://github.com/makediff/obsidian-wucai";
        };
    }
);
    "wypst" = (
    let
        baseUrl = "https://github.com/andredalbosco/obsidian-wypst/releases/download/0.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "wypst";
        version = "0.0.3";
        outputHash = "sha256-+0qx1cjjwik9WcpBBu2VjEJc6QNODEHGPAJbgJJK/pI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render math blocks with Typst'';
            homepage = "https://github.com/andredalbosco/obsidian-wypst";
        };
    }
);
    "x-post-saver" = (
    let
        baseUrl = "https://github.com/tanaka-mambinge/x-post-saver/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "x-post-saver";
        version = "1.0.3";
        outputHash = "sha256-0w7bjegjpCv1+1TjVw7NHgj0efklRzZ4w9vLNc/jfZg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Saves X (formerly Twitter) posts" text data to new notes or inside a specific directory in your vault.'';
            homepage = "https://github.com/tanaka-mambinge/x-post-saver";
        };
    }
);
    "x86-flow-graphing" = (
    let
        baseUrl = "https://github.com/dwolfe884/obsidian-x86-flow-graph/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "x86-flow-graphing";
        version = "1.0.2";
        outputHash = "sha256-d++I3+Nw+Pn8yYesRLJTlN/mkcKAu9dTi2EYXPE8E34=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Convert well formatted x86 assembly into appropriate flow graphs using Obsidian canvases.'';
            homepage = "https://github.com/dwolfe884/obsidian-x86-flow-graph";
        };
    }
);
    "xiangqi" = (
    let
        baseUrl = "https://github.com/west-shell/obsidian-xiangqi/releases/download/2.6.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "xiangqi";
        version = "2.6.3";
        outputHash = "sha256-gbmJ9wOafrWGQB6G0k29DVx1oz0HzphfUctWZX5bDFE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Render xiangqi(Chinese chess) positions diagrams in a note.'';
            homepage = "https://github.com/west-shell/obsidian-xiangqi";
        };
    }
);
    "xiaohongshu-importer" = (
    let
        baseUrl = "https://github.com/bnchiang96/xiaohongshu-importer/releases/download/1.1.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "xiaohongshu-importer";
        version = "1.1.3";
        outputHash = "sha256-lKEufDCJjcBHkdM0ia4Lyk/1Jd/CFUepS6JWJyvm2ag=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Import Xiaohongshu (小红书) notes with media and categorization.'';
            homepage = "https://github.com/bnchiang96/xiaohongshu-importer";
        };
    }
);
    "xmind-linker" = (
    let
        baseUrl = "https://github.com/leafney/obsidian-xmind-linker/releases/download/1.3.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "xmind-linker";
        version = "1.3.5";
        outputHash = "sha256-phCNM6IlNn5O/bLYXs0xo8BOqqgGKGF3Y7lQSOU4bRk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''View XMind files in your vault and connect to XMind software for editing.'';
            homepage = "https://github.com/leafney/obsidian-xmind-linker";
        };
    }
);
    "xmind-viewer" = (
    let
        baseUrl = "https://github.com/Ssentiago/obsidian-xmind-viewer/releases/download/0.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "xmind-viewer";
        version = "0.1.0";
        outputHash = "sha256-yjXJeqfPp8Bw6lXgXi8XJYB9j25a+iqiTRT1Bd3amwg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate viewing of your XMind files'';
            homepage = "https://github.com/Ssentiago/obsidian-xmind-viewer";
        };
    }
);
    "xournalpp" = (
    let
        baseUrl = "https://github.com/jonjampen/obsidian-xournalpp/releases/download/1.2.0-beta.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "xournalpp";
        version = "1.2.0-beta.1";
        outputHash = "sha256-2ZyF6dcXwtN64RMPU9GjhkSDspAtclIsdCKNSNl9VJM=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integration with Xournal++ for handwritten notes and annotations.'';
            homepage = "https://github.com/jonjampen/obsidian-xournalpp";
        };
    }
);
    "yaml-table" = (
    let
        baseUrl = "https://github.com/dainakai/obsidian-yaml-table/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yaml-table";
        version = "1.1.4";
        outputHash = "sha256-Lna1oYxfxE9uTHMi6q/z8YV+cE1qWmKeukOmlOmbe14=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform YAML code blocks into HTML tables for better visualization'';
            homepage = "https://github.com/dainakai/obsidian-yaml-table";
        };
    }
);
    "yandex-tracker-issue" = (
    let
        baseUrl = "https://github.com/CubieProg/Obsidian-Yandex-Tracker-Issue/releases/download/1.0.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yandex-tracker-issue";
        version = "1.0.1";
        outputHash = "sha256-cgzvJ5qcRJPAyxQIRXEwKi+KDbSZ2piY4qhX6gSRXR0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Display Yandex Tracker issues in your notes'';
            homepage = "https://github.com/CubieProg/Obsidian-Yandex-Tracker-Issue";
        };
    }
);
    "yanki" = (
    let
        baseUrl = "https://github.com/kitschpatrol/yanki-obsidian/releases/download/1.11.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yanki";
        version = "1.11.1";
        outputHash = "sha256-CadGZdUXDuiF8xeBWha6WN/ePkeW5+wxM7DvwWFEZLo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Sync flashcards from a folder in your vault to Anki. Pure Markdown syntax. No fuss.'';
            homepage = "https://github.com/kitschpatrol/yanki-obsidian";
        };
    }
);
    "yearly-diary-comparator" = (
    let
        baseUrl = "https://github.com/kiitosu/yearly-diary-comparator/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yearly-diary-comparator";
        version = "1.0.0";
        outputHash = "sha256-H8UJO2B5svxZC9C7qtSBlNDbjX36mBrWO7oGW899wHo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Show a side-by-side yearly comparison of diary in daily notes.'';
            homepage = "https://github.com/kiitosu/yearly-diary-comparator";
        };
    }
);
    "yearly-glance" = (
    let
        baseUrl = "https://github.com/Moyf/yearly-glance/releases/download/3.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yearly-glance";
        version = "3.2.0";
        outputHash = "sha256-OBWzgC7/Uq4Nydfj4na+UN+GW2S+aYjpczk21TOImVE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Year at a glance - overview of annual events with customizable management options.'';
            homepage = "https://github.com/Moyf/yearly-glance";
        };
    }
);
    "yesterday" = (
    let
        baseUrl = "https://github.com/dominikmayer/obsidian-yesterday/releases/download/1.3.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yesterday";
        version = "1.3.0";
        outputHash = "sha256-iCh5jtGccUEb3Pcp5r40m+fu87Aw0lwouTl4V1oTaWg=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Transform your notes into a visually stunning diary, integrating dialogs, chat logs, and media content blocks for a seamless journaling experience.'';
            homepage = "https://github.com/dominikmayer/obsidian-yesterday";
        };
    }
);
    "yesterdays-note" = (
    let
        baseUrl = "https://github.com/trevortylerlee/yesterdays-note/releases/download/1.1.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yesterdays-note";
        version = "1.1.0";
        outputHash = "sha256-xldGGZPWLyN23jnUc2gPtAg/HLL+8tKJJLbfxRqg87I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Open yesterday"s daily note.'';
            homepage = "https://github.com/trevortylerlee/yesterdays-note";
        };
    }
);
    "yolo" = (
    let
        baseUrl = "https://github.com/Lapis0x0/obsidian-yolo/releases/download/1.5.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yolo";
        version = "1.5.6";
        outputHash = "sha256-xIue2ChMQOEPjpIvSuhNjDMs6I3HGxKS1hNMC2rw/1c=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Smart, snappy, and multilingual AI assistant for your vault.'';
            homepage = "https://github.com/Lapis0x0/obsidian-yolo";
        };
    }
);
    "you-and-your-research" = (
    let
        baseUrl = "https://github.com/neozhang/you-and-your-research/releases/download/0.9.8";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "you-and-your-research";
        version = "0.9.8";
        outputHash = "sha256-ajXZW2hz5uX+D9fLzbxfbLtZJ8AoQtd4f5JdLbGmnfs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Research with the help of A.I.'';
            homepage = "https://github.com/neozhang/you-and-your-research";
        };
    }
);
    "youhavebeenstaring-plugin" = (
    let
        baseUrl = "https://github.com/fxal/obsidian-youhavebeenstaring-plugin/releases/download/1.2.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youhavebeenstaring-plugin";
        version = "1.2.2";
        outputHash = "sha256-oZ7oZpR+YBJeSHyhERXTWiajxekITQ0TrL8kvcWnKs0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a status bar indicator showing how long your vault has been open.'';
            homepage = "https://github.com/fxal/obsidian-youhavebeenstaring-plugin";
        };
    }
);
    "yourpulse" = (
    let
        baseUrl = "https://github.com/jsifalda/obsipulse-plugin/releases/download/2.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yourpulse";
        version = "2.4.0";
        outputHash = "sha256-rKo/ukr2dz1VLEIpm+80SUYH5v4gOLA4YU1K6kvPwK4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''It"s like your Github profile, but for your vault (featuring daily streak, average daily word count, comprehensive stats and plugins overview).'';
            homepage = "https://github.com/jsifalda/obsipulse-plugin";
        };
    }
);
    "youtrack-fetcher" = (
    let
        baseUrl = "https://github.com/forketyfork/obsidian-youtrack-fetcher/releases/download/0.11.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youtrack-fetcher";
        version = "0.11.1";
        outputHash = "sha256-IrZ45I/omFDbtwB7ijSWny6ay9W2MaeqCoeQ/1Vchz4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetches YouTrack issues into notes'';
            homepage = "https://github.com/forketyfork/obsidian-youtrack-fetcher";
        };
    }
);
    "youtube-downloader" = (
    let
        baseUrl = "https://github.com/ai-chen2050/obsidian-youtube-downloader/releases/download/1.5.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youtube-downloader";
        version = "1.5.0";
        outputHash = "sha256-3E0YJ4BpwYtwxrsSEpoOlYUEquRfVUTL+D6BSVKzzYI=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Download videos from YouTube'';
            homepage = "https://github.com/ai-chen2050/obsidian-youtube-downloader";
        };
    }
);
    "youtube-iframe-timestamps" = (
    let
        baseUrl = "https://github.com/NilsLeo/obsidian-youtube-iframe-timestamps/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youtube-iframe-timestamps";
        version = "1.0.0";
        outputHash = "sha256-n0ZKWPQKKPbJD9uhfAF0U+uKNOkO3WJEpW62120od8I=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Allows you to embed YouTube videos with timestamps directly in your notes, enabling seamless referencing and note-taking without needing to open a separate browser window.'';
            homepage = "https://github.com/NilsLeo/obsidian-youtube-iframe-timestamps";
        };
    }
);
    "youtube-summarizer" = (
    let
        baseUrl = "https://github.com/ozdemir08/youtube-video-summarizer/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youtube-summarizer";
        version = "1.0.2";
        outputHash = "sha256-YhPRNSwoDsTz3SjuGQdDx5enKJ/b7+8/7BWloXOS2i0=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Summarize Youtube videos using ChatGPT 3.5.'';
            homepage = "https://github.com/ozdemir08/youtube-video-summarizer";
        };
    }
);
    "youtube-template" = (
    let
        baseUrl = "https://github.com/sundevista/youtube-template/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youtube-template";
        version = "1.2.1";
        outputHash = "sha256-hYPgSrXmeY6cK7nKbuz53NzyeFt2weH8PsvtpbWzAJY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch YouTube video data into your vault.'';
            homepage = "https://github.com/sundevista/youtube-template";
        };
    }
);
    "youversion-linker" = (
    let
        baseUrl = "https://github.com/jaanonim/obsidian-youversion-linker/releases/download/1.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "youversion-linker";
        version = "1.6.5";
        outputHash = "sha256-rZParutCVIlHs2m5+TSs9XR6VBRXrkskCq8G2ai1Gbo=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Automatically link bible verses in your notes to YouVersion bible.'';
            homepage = "https://github.com/jaanonim/obsidian-youversion-linker";
        };
    }
);
    "yt-summarizer" = (
    let
        baseUrl = "https://github.com/ardakalayci/ytsummarizer/releases/download/0.2.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yt-summarizer";
        version = "0.2.6";
        outputHash = "sha256-GhraI8ZcRZxQ6QEObqzj/PTobGAcHuqxWcp6QusFvu8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Fetch YouTube video transcripts and generate summaries using OpenAI models.'';
            homepage = "https://github.com/ardakalayci/ytsummarizer";
        };
    }
);
    "yt-video-summarizer" = (
    let
        baseUrl = "https://github.com/mbramani/obsidian-yt-video-summarizer/releases/download/1.2.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yt-video-summarizer";
        version = "1.2.0";
        outputHash = "sha256-aHAsF2vtqj/Orz0/nYFr1J+KXe2Ferg+etTCDEK8L5g=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Summarize YouTube videos using Gemini AI. Extract transcripts, generate summaries, and create structured notes.'';
            homepage = "https://github.com/mbramani/obsidian-yt-video-summarizer";
        };
    }
);
    "ytranscript" = (
    let
        baseUrl = "https://github.com/lstrzepek/obsidian-yt-transcript/releases/download/1.3.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "ytranscript";
        version = "1.3.1";
        outputHash = "sha256-KkylF1T0xNaqyzEd5O8IrvEqbDJjBXFpb25QHKX3vyY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Easily fetch transcription for any YouTube video.'';
            homepage = "https://github.com/lstrzepek/obsidian-yt-transcript";
        };
    }
);
    "yuque-publish" = (
    let
        baseUrl = "https://github.com/oylbin/obsidian-yuque-publish/releases/download/1.0.3";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "yuque-publish";
        version = "1.0.3";
        outputHash = "sha256-J6alAw+ge6mP18nqK0YxUzfGdG+fn/b3xX/N2dU9xcQ=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Publish your notes to Yuque.'';
            homepage = "https://github.com/oylbin/obsidian-yuque-publish";
        };
    }
);
    "zen" = (
    let
        baseUrl = "https://github.com/Maxymillion/zen/releases/download/0.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zen";
        version = "0.6.5";
        outputHash = "sha256-GbEWeQNG+aTb2Pv41ulMYeJn7qV7HFhzg0IJupQAh2A=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Add a focus mode.'';
            homepage = "https://github.com/Maxymillion/zen";
        };
    }
);
    "zen-space" = (
    let
        baseUrl = "https://github.com/amatya-aditya/obsidian-zen-space/releases/download/1.2.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zen-space";
        version = "1.2.1";
        outputHash = "sha256-1jKENwKpbEoM+zlC8jSVREUjpBLCKd3NtNgWeAmhyBs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''An enhanced file explorer showing relevant only relevant files when required.'';
            homepage = "https://github.com/amatya-aditya/obsidian-zen-space";
        };
    }
);
    "zenmode" = (
    let
        baseUrl = "https://github.com/paperbenni/obsidian-zenmode/releases/download/1.6.5";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zenmode";
        version = "1.6.5";
        outputHash = "sha256-p2V0z+IlZNqOecoGH/yHL6v+/HcbaHhUCoYNTpBuNL8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Hide most UI elements.'';
            homepage = "https://github.com/paperbenni/obsidian-zenmode";
        };
    }
);
    "zettelflow" = (
    let
        baseUrl = "https://github.com/RafaelGB/Obsidian-ZettelFlow/releases/download/2.11.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zettelflow";
        version = "2.11.0";
        outputHash = "sha256-qHp96rN11C8pUrpxYXFWS+7eKjbNzWPQWrUnfRCZC/8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Create and manage your notes in a Zettelkasten way via Canvas.'';
            homepage = "https://github.com/RafaelGB/Obsidian-ZettelFlow";
        };
    }
);
    "zettelgpt" = (
    let
        baseUrl = "https://github.com/OverRaddit/ZettelGPT/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zettelgpt";
        version = "1.0.0";
        outputHash = "sha256-C9df0Nj9yPLw8/xI5GDxHNkZ2kU4hRlRx48q1WCRoO4=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Effortlessly generate context-aware answers from ChatGPT, while maintaining a visually clear and organized conversation history.'';
            homepage = "https://github.com/OverRaddit/ZettelGPT";
        };
    }
);
    "zettelkasten-branch-tracker" = (
    let
        baseUrl = "https://github.com/thelivingphilosophy/zettelkasten-branch-tracker/releases/download/1.0.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zettelkasten-branch-tracker";
        version = "1.0.0";
        outputHash = "sha256-QNl7AKO9GH10/KgKdnc625wlnrs6wUTyLqi0wbpFeFU=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Specialised graph visualisation for Zettelkasten note hierarchies'';
            homepage = "https://github.com/thelivingphilosophy/zettelkasten-branch-tracker";
        };
    }
);
    "zettelkasten-llm-tools" = (
    let
        baseUrl = "https://github.com/glovguy/obsidian-gpt-zettelkasten/releases/download/1.6.1";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zettelkasten-llm-tools";
        version = "1.6.1";
        outputHash = "sha256-TWN39MA9mG2getFl9Blcff8ej3DWcfUbKE8B3+52ygk=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Zettelkasten note taking powered by Large Language Models (e.g. semantic search).'';
            homepage = "https://github.com/glovguy/obsidian-gpt-zettelkasten";
        };
    }
);
    "zettelkasten-navigation" = (
    let
        baseUrl = "https://github.com/PKM-er/obsidian-zettelkasten-navigation/releases/download/1.1.9";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zettelkasten-navigation";
        version = "1.1.9";
        outputHash = "sha256-vI4EWHPl3llZHyM+8EFhGDC6XcZA2D9Z0tm7XMPHN+E=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Visualize a Luhmann-style zettelkasten.'';
            homepage = "https://github.com/PKM-er/obsidian-zettelkasten-navigation";
        };
    }
);
    "zettelkasten-outliner" = (
    let
        baseUrl = "https://github.com/tylersuzukinelson/zettelkasten-outliner/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zettelkasten-outliner";
        version = "1.0.2";
        outputHash = "sha256-z/K29+CQy5hUjDmK+zldO3LIgaFrjHmXESFbE8TeLDs=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''A list representation for your Zettelkasten.'';
            homepage = "https://github.com/tylersuzukinelson/zettelkasten-outliner";
        };
    }
);
    "zhihu" = (
    let
        baseUrl = "https://github.com/zimya/zhihu_obsidian/releases/download/0.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zhihu";
        version = "0.4.0";
        outputHash = "sha256-CdoWY16dk7JNIHLsiY0ODTau6uXzRiUI8pt8m4R5I/M=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Enable you to publish your articles and answers to Zhihu.'';
            homepage = "https://github.com/zimya/zhihu_obsidian";
        };
    }
);
    "zhongwen-block" = (
    let
        baseUrl = "https://github.com/0918nobita/obsidian-zhongwen-block/releases/download/0.8.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zhongwen-block";
        version = "0.8.0";
        outputHash = "sha256-HG2e8Ne7x9m+7hGO0zqM2kfQOTDVsKlotCGFMq0LNHY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Code blocks with features for Chinese learners.'';
            homepage = "https://github.com/0918nobita/obsidian-zhongwen-block";
        };
    }
);
    "zhongwen-reader" = (
    let
        baseUrl = "https://github.com/natipt/obsidian-zhongwen-reader/releases/download/1.1.4";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zhongwen-reader";
        version = "1.1.4";
        outputHash = "sha256-VlR5AYzOPmp2F9CdJwcxTs5+wQHL2PjIso687/3lb10=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''This lightweight plugin helps you read Chinese more easily with a CEDICT based hover-dictionary and vocabulary saving features.'';
            homepage = "https://github.com/natipt/obsidian-zhongwen-reader";
        };
    }
);
    "zoottelkeeper-obsidian-plugin" = (
    let
        baseUrl = "https://github.com/akosbalasko/zoottelkeeper-obsidian-plugin/releases/download/0.18.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zoottelkeeper-obsidian-plugin";
        version = "0.18.0";
        outputHash = "sha256-TjBrMCUmgpc4N9W2nSdwmBvgkag+qzU8a8m5nDMeLC8=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''It maintains index files in all of your folders in your vault: if you create/delete/move a note, the index files will be updated automatically. It can be used to show folders in Graph View.'';
            homepage = "https://github.com/akosbalasko/zoottelkeeper-obsidian-plugin";
        };
    }
);
    "zotero-bridge" = (
    let
        baseUrl = "https://github.com/vanakat/zotero-bridge/releases/download/1.6.6";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zotero-bridge";
        version = "1.6.6";
        outputHash = "sha256-yknXNuZta1/D05z/0gGwBBkYjj/63Q3KJFzMaXxoqIE=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate with Zotero through ZotServer.'';
            homepage = "https://github.com/vanakat/zotero-bridge";
        };
    }
);
    "zotero-link" = (
    let
        baseUrl = "https://github.com/vanakat/zotero-link/releases/download/1.0.2";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zotero-link";
        version = "1.0.2";
        outputHash = "sha256-/Pb6V9Zo8Hd7dOzf+K0cAGj45oiDi8mIYZBvV0qhtrc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Insert link to a Zotero item.'';
            homepage = "https://github.com/vanakat/zotero-link";
        };
    }
);
    "zotero-sync-client" = (
    let
        baseUrl = "https://github.com/frthjf/obsidian-zotero-sync-client/releases/download/1.4.0";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zotero-sync-client";
        version = "1.4.0";
        outputHash = "sha256-sGbcO7SR65w4/XlefaCqIH8Y8xk44ANoRuumHpmbKXc=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Zotero API client to sync your Zotero library into your vault.'';
            homepage = "https://github.com/frthjf/obsidian-zotero-sync-client";
        };
    }
);
    "zotlit" = (
    let
        baseUrl = "https://github.com/PKM-er/obsidian-zotlit/releases/download/1.1.11";
    in
    pkgs.stdenvNoCC.mkDerivation {
        pname = "zotlit";
        version = "1.1.11";
        outputHash = "sha256-HU/vuHJcUpNGIajLlqia0aoqGKMzru+QLScz9TDMgUY=";
        outputHashMode = "recursive";
        outputHashAlgo = "sha256";

        nativeBuildInputs = [pkgs.curl];
        SSL_CERT_FILE = "${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt";
        phases = ["installPhase"];
        installPhase = ''
            mkdir -p $out
            curl -sfL -o $out/main.js "${baseUrl}/main.js"
            curl -sfL -o $out/manifest.json "${baseUrl}/manifest.json"
            curl -sfL -o $out/styles.css "${baseUrl}/styles.css" 2>/dev/null || true
        '';
        meta = {
            description = ''Integrate with Zotero, create literature notes, and insert citations from a Zotero library.'';
            homepage = "https://github.com/PKM-er/obsidian-zotlit";
        };
    }
);
}